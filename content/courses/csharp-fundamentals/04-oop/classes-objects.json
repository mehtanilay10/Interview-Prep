{
  "id": "lesson-csharp-012",
  "slug": "classes-objects",
  "moduleSlug": "oop",
  "title": "Classes and Objects",
  "description": "Understand the foundation of OOP: how to define classes with fields, properties, methods, and constructors, and how to instantiate objects from those blueprints.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "oop",
    "classes",
    "objects",
    "constructors",
    "properties"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Object-Oriented Programming (OOP) is built on the idea of modeling real-world things as **classes** and **objects**. A class is a blueprint that defines what data an object holds and what actions it can perform. An object is a concrete instance created from that blueprint. In C#, almost everything you work with — from simple strings to complex UI controls — is an object created from a class."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Is a Class?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A class is a user-defined type that bundles **data** (fields/properties) and **behavior** (methods) into a single unit. Think of a class like an architectural blueprint for a house: it specifies the layout, number of rooms, and features, but it is not a house itself. You use that blueprint to build actual houses — those houses are the objects."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Class",
            "definition": "A blueprint or template that defines the structure and behavior of objects."
          },
          {
            "term": "Object (Instance)",
            "definition": "A concrete realization of a class, created at runtime using the new keyword."
          },
          {
            "term": "Field",
            "definition": "A variable declared directly in a class, typically used to store internal state."
          },
          {
            "term": "Property",
            "definition": "A class member that provides a flexible mechanism to read, write, or compute the value of a private field."
          },
          {
            "term": "Method",
            "definition": "A function defined inside a class that describes a behavior the object can perform."
          },
          {
            "term": "Constructor",
            "definition": "A special method that runs when an object is created, used to initialize its state."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "class-object-relationship",
        "caption": "Relationship between a class blueprint and its objects",
        "definition": "classDiagram\n    class Person {\n        +string Name\n        +int Age\n        +Introduce()\n    }\n    Person --|> Object1 : creates\n    Person --|> Object2 : creates\n    class Object1 {\n        Name = \"Alice\"\n        Age = 25\n    }\n    class Object2 {\n        Name = \"Bob\"\n        Age = 30\n    }"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining a Class"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In C#, you define a class using the `class` keyword. By convention, class names use PascalCase. The class body is enclosed in curly braces `{}` and can contain fields, properties, constructors, and methods. The `public` access modifier makes the class visible to other code; omitting it defaults to `internal`, restricting access to the same assembly."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Defining a Simple Class",
        "content": "A Person class with properties, a constructor, and a method.",
        "language": "csharp",
        "code": "// Class definition\npublic class Person\n{\n    // Auto-implemented properties\n    public string Name { get; set; }\n    public int Age { get; set; }\n\n    // Constructor - initializes new Person objects\n    public Person(string name, int age)\n    {\n        Name = name;\n        Age = age;\n    }\n\n    // Method - behavior of a Person\n    public void Introduce()\n    {\n        Console.WriteLine($\"Hi, I'm {Name} and I'm {Age} years old.\");\n    }\n}\n\n// Creating objects (instances) from the Person class\nvar person1 = new Person(\"Alice\", 25);\nvar person2 = new Person(\"Bob\", 30);\n\nperson1.Introduce(); // Output: Hi, I'm Alice and I'm 25 years old.\nperson2.Introduce(); // Output: Hi, I'm Bob and I'm 30 years old."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "The new Keyword",
        "text": "The new keyword allocates memory for the object and calls its constructor. Each call to new Person(...) creates a completely independent object with its own copy of the data."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Fields vs Properties"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Fields are raw variables that store data inside a class. Properties are the public-facing interface to that data, allowing you to add validation, trigger side effects, or compute values on the fly. The C# convention is to make fields `private` and expose them through `public` properties."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Fields and Properties",
        "content": "Comparing raw fields with properties, including validation and computed values.",
        "language": "csharp",
        "code": "public class Product\n{\n    // Private backing field\n    private decimal price;\n    private int stock;\n\n    // Auto-implemented property (compiler creates a hidden backing field)\n    public string Name { get; set; }\n\n    // Property with validation logic\n    public decimal Price\n    {\n        get { return price; }\n        set\n        {\n            // Validation in the setter\n            price = value >= 0 ? value : throw new ArgumentOutOfRangeException(nameof(value));\n        }\n    }\n\n    // Read-only property (set only in constructor)\n    public string Id { get; }\n\n    // Computed (expression-bodied) property\n    public string DisplayName => $\"{Name} (${Price:F2})\";\n\n    // Property with custom get/set logic\n    public int Stock\n    {\n        get { return stock; }\n        set\n        {\n            if (value < 0)\n                throw new ArgumentOutOfRangeException(nameof(value));\n            stock = value;\n        }\n    }\n\n    public Product(string name, decimal price, int stock)\n    {\n        Name = name;\n        Price = price;\n        Stock = stock;\n        Id = Guid.NewGuid().ToString();\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Constructors"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Constructors are special methods that run automatically when you create a new object. They have the same name as the class and no return type — not even `void`. C# supports parameterless constructors, parameterized constructors, and the `this()` keyword for constructor chaining. If you don't declare any constructor, the compiler provides a free parameterless one."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Constructor Chaining",
        "content": "Using this() to chain constructors and avoid duplicate initialization logic.",
        "language": "csharp",
        "code": "public class Order\n{\n    public int Id { get; }\n    public string CustomerName { get; set; }\n    public DateTime CreatedAt { get; }\n    public List<string> Items { get; } = new();\n\n    // Primary constructor (C# 12)\n    public Order(int id, string customerName)\n    {\n        Id = id;\n        CustomerName = customerName;\n        CreatedAt = DateTime.UtcNow;\n    }\n\n    // Secondary constructor chains to primary\n    public Order(int id) : this(id, \"Anonymous\")\n    {\n        // Reuses the primary constructor logic\n    }\n}\n\n// Usage\nvar order1 = new Order(101, \"Alice\");\nvar order2 = new Order(102); // CustomerName defaults to \"Anonymous\""
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Methods"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Methods define what objects can do. They can accept parameters, return values, and be marked `virtual` to allow overriding in derived classes. Expression-bodied members (`=>`) are a shorthand for single-expression methods or properties introduced in C# 6+."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Methods on a Class",
        "content": "Different method signatures including value returns, void methods, and expression-bodied members.",
        "language": "csharp",
        "code": "public class BankAccount\n{\n    public string AccountNumber { get; }\n    public decimal Balance { get; private set; }\n\n    public BankAccount(string accountNumber, decimal initialBalance)\n    {\n        AccountNumber = accountNumber;\n        Balance = initialBalance;\n    }\n\n    // Method with a return value\n    public bool Deposit(decimal amount)\n    {\n        if (amount <= 0) return false;\n        Balance += amount;\n        return true;\n    }\n\n    // Method with out parameter\n    public bool TryWithdraw(decimal amount, out decimal remaining)\n    {\n        if (amount > 0 && amount <= Balance)\n        {\n            Balance -= amount;\n            remaining = Balance;\n            return true;\n        }\n        remaining = Balance;\n        return false;\n    }\n\n    // Expression-bodied method\n    public override string ToString() => $\"Account {AccountNumber}: ${Balance:F2}\";\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Records (C# 9+)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For data-centric classes where equality is based on value rather than identity, C# 9 introduced `record` types. Records are immutable by default, provide value-based equality, and have a concise syntax ideal for DTOs (Data Transfer Objects)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Records vs Classes",
        "content": "Using record types for immutable data with built-in equality and ToString().",
        "language": "csharp",
        "code": "// Traditional class - reference equality\nvar p1 = new PersonClass(\"Alice\", 25);\nvar p2 = new PersonClass(\"Alice\", 25);\nConsole.WriteLine(p1 == p2); // False (different references)\n\n// Record - value equality\nvar r1 = new PersonRecord(\"Alice\", 25);\nvar r2 = new PersonRecord(\"Alice\", 25);\nConsole.WriteLine(r1 == r2); // True (same values)\n\n// Record definition\npublic record PersonRecord(string Name, int Age);\n\n// Positional record\npublic record Point(double X, double Y);\nvar point = new Point(3.0, 4.0);\n\n// Record with additional behavior\npublic record BankAccount(string AccountNumber, decimal Balance)\n{\n    public BankAccount(string accountNumber) : this(accountNumber, 0) { }\n\n    public bool Deposit(decimal amount)\n    {\n        if (amount <= 0) return false;\n        return this with { Balance = Balance + amount };\n    }\n}"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Library Book Class",
        "description": "Create a Book class that models a book in a library. Include ISBN, Title, Author, and Available status. Add a constructor, a Borrow() method, a Return() method, and an IsAvailable property.",
        "steps": [
          "Declare a public class Book with private fields for ISBN, Title, Author, and Available.",
          "Create public properties for ISBN, Title, and Author (auto-implemented).",
          "Create a public bool Available property with a private setter.",
          "Write a constructor that takes ISBN, Title, and Author, and sets Available to true.",
          "Add a public void Borrow() method that sets Available to false.",
          "Add a public void Return() method that sets Available to true.",
          "Override ToString() to return a formatted description of the book."
        ],
        "expectedOutcome": "You can create Book instances and call Borrow()/Return() to manage availability."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Classes and Objects Checklist",
        "items": [
          {
            "text": "I can define a class with the class keyword and PascalCase naming.",
            "hint": "public class MyClass { }"
          },
          {
            "text": "I understand the difference between a field and a property.",
            "hint": "Fields store data; properties expose it with logic."
          },
          {
            "text": "I can write a constructor to initialize an object's state.",
            "hint": "Constructor name must match the class name exactly."
          },
          {
            "text": "I can create objects using the new keyword.",
            "hint": "var obj = new MyClass();"
          },
          {
            "text": "I know when to use a record instead of a class.",
            "hint": "Use records for immutable, value-based data."
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "What is the difference between a class and a struct?",
            "answer": "Classes are reference types stored on the heap, support inheritance, and allow null. Structs are value types stored on the stack, cannot inherit from other classes, and are copied by value. Use structs for small, lightweight data like coordinates or colors."
          },
          {
            "question": "Can a class have multiple constructors?",
            "answer": "Yes. You can overload constructors with different parameter signatures. Use constructor chaining (this(...)) to share initialization logic and avoid duplication."
          },
          {
            "question": "What does the get and set accessor do in a property?",
            "answer": "get returns the property's value; set assigns a new value via the implicit value parameter. You can add validation in set, make one accessor private, or omit one entirely for read-only or write-only properties."
          },
          {
            "question": "What is an auto-implemented property?",
            "answer": "A shorthand syntax like public string Name { get; set; } where the compiler automatically creates a hidden backing field. Use these when you don't need custom validation or logic in the get/set accessors."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Classes bundle data (fields/properties) and behavior (methods) together.",
          "Objects are runtime instances of a class, created with the new keyword.",
          "Properties provide controlled access to private fields with optional validation.",
          "Constructors initialize a new object's state when it is created.",
          "Records (C# 9+) offer concise immutable types with value-based equality."
        ],
        "takeaway": "Classes and objects form the core building blocks of every C# application."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
