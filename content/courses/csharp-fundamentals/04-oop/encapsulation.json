{
  "id": "lesson-csharp-015",
  "slug": "encapsulation",
  "moduleSlug": "oop",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "csharp",
    "oop",
    "encapsulation",
    "access-modifiers",
    "properties"
  ],
  "title": "Encapsulation and Access Modifiers",
  "description": "Learn how to protect class internals using access modifiers, properties, and data-hiding techniques.",
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Encapsulation is the OOP principle of bundling data with the methods that operate on that data, while restricting direct access to some of the object's internal components. Think of it like a car's engine: you use the steering wheel and pedals (public interface) without needing to understand the internal combustion process (hidden implementation)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Encapsulation Matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Without encapsulation, any code could freely modify an object's data, leading to invalid states and hard-to-debug issues. With encapsulation, you enforce rules through a controlled public interface, making your code safer, more maintainable, and easier to test."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Why It Matters",
        "text": "Encapsulation prevents external code from putting your object into an invalid state. For example, a BankAccount balance should never be negative — only the Deposit/Withdraw methods should be allowed to change it."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Encapsulation",
            "definition": "Bundling data and methods together while hiding internal implementation details from outside code."
          },
          {
            "term": "Access Modifier",
            "definition": "A keyword (public, private, protected, internal) that controls where a class member can be accessed from."
          },
          {
            "term": "Getter",
            "definition": "The code block in a property that runs when the property's value is read."
          },
          {
            "term": "Setter",
            "definition": "The code block in a property that runs when the property's value is assigned."
          },
          {
            "term": "Backing Field",
            "definition": "A private field that stores the actual data behind a property."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Access Modifiers Reference"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# provides several access modifiers that control the visibility of classes and their members. Understanding when and where to use each is fundamental to writing well-encapsulated code."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Modifier",
          "Accessible From",
          "Common Use"
        ],
        "rows": [
          [
            "public",
            "Anywhere",
            "Public API — constructors, properties, methods meant for external callers"
          ],
          [
            "private",
            "Same class only",
            "Internal fields and helper methods that should never be exposed"
          ],
          [
            "protected",
            "Same class + derived classes",
            "Members intended for reuse in subclasses via inheritance"
          ],
          [
            "internal",
            "Same assembly/project",
            "Implementation details shared across files in the same project"
          ],
          [
            "protected internal",
            "Same assembly OR derived classes",
            "Members safe for subclassing anywhere OR internal sharing"
          ],
          [
            "private protected",
            "Same class AND derived classes, same assembly only",
            "Members only accessible to derived classes in the same assembly"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Properties: The Primary Encapsulation Tool"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Properties in C# are the preferred way to expose data while maintaining encapsulation. A property looks like a field from the outside but acts like a method internally — the `get` accessor returns the value and the `set` accessor assigns it. You can validate, transform, or log every change through the setter."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Full Encapsulation with Properties",
        "content": "A BankAccount class that completely hides its balance field and controls all modifications through methods and properties.",
        "language": "csharp",
        "code": "public class BankAccount\n{\n    // Private backing field - inaccessible from outside\n    private decimal balance;\n\n    // Public read-only property\n    public decimal Balance\n    {\n        get { return balance; }\n        private set // Only settable from within this class\n        {\n            if (value < 0)\n                throw new ArgumentOutOfRangeException(nameof(value), \"Balance cannot be negative.\");\n            balance = value;\n        }\n    }\n\n    public string AccountNumber { get; }\n    public string OwnerName { get; set; }\n    public bool IsActive { get; private set; } = true;\n\n    public BankAccount(string accountNumber, string ownerName, decimal initialBalance = 0)\n    {\n        AccountNumber = accountNumber ?? throw new ArgumentNullException(nameof(accountNumber));\n        OwnerName = ownerName ?? throw new ArgumentNullException(nameof(ownerName));\n        Balance = initialBalance; // Uses the property's validation\n    }\n\n    // Public method to modify balance - the ONLY way to add money\n    public bool Deposit(decimal amount)\n    {\n        if (!IsActive || amount <= 0) return false;\n        Balance += amount;\n        return true;\n    }\n\n    public bool Withdraw(decimal amount)\n    {\n        if (!IsActive || amount <= 0 || amount > Balance) return false;\n        Balance -= amount;\n        return true;\n    }\n\n    public void CloseAccount()\n    {\n        IsActive = false;\n    }\n}"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Notice how `Balance` has a `public get` but a `private set`. External code can check the balance but cannot directly set it — it must use `Deposit()` or `Withdraw()`. This is the essence of encapsulation: control the mutation of state."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Property Patterns"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# offers several property patterns for different scenarios. Auto-properties are the simplest. Full properties with backing fields give you full control. Expression-bodied properties compute values on demand. Init-only setters allow setting a value only during object initialization."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Property Patterns",
        "content": "Auto-properties, init-only, computed, and full properties with backing fields.",
        "language": "csharp",
        "code": "public class Employee\n{\n    // Auto-property (compiler generates a hidden backing field)\n    public string FirstName { get; set; }\n\n    // Init-only property — set only during object initialization\n    public string LastName { get; init; }\n\n    // Read-only auto-property — set only in constructor\n    public Guid Id { get; } = Guid.NewGuid();\n\n    // Computed (expression-bodied) property\n    public string FullName => $\"{FirstName} {LastName}\";\n\n    // Private setter\n    public string Department { get; private set; }\n\n    // Full property with backing field and validation\n    private decimal salary;\n    public decimal Salary\n    {\n        get { return salary; }\n        set { salary = value >= 0 ? value : throw new ArgumentOutOfRangeException(); }\n    }\n\n    // Indexer — allows array-like access\n    private string[] skills = new string[0];\n    public string this[int index]\n    {\n        get => skills[index];\n        set => skills[index] = value;\n    }\n\n    public Employee(string firstName, string lastName, decimal salary, string department)\n    {\n        FirstName = firstName;\n        LastName = lastName;\n        Salary = salary;\n        Department = department;\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Validation and Data Integrity"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A key benefit of encapsulation is enforcing business rules at the point of data entry. Rather than trusting callers to provide valid data, the class validates internally. This prevents invalid object states that can cause bugs elsewhere in the application."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Validation in Setters",
        "content": "Using property setters to enforce business rules and keep object state valid.",
        "language": "csharp",
        "code": "public class CourseEnrollment\n{\n    public string StudentId { get; }\n    public string CourseCode { get; }\n    private DateTime enrollmentDate;\n    private char grade;\n\n    public DateTime EnrollmentDate\n    {\n        get => enrollmentDate;\n        private set\n        {\n            if (value > DateTime.UtcNow)\n                throw new ArgumentException(\"Enrollment date cannot be in the future.\");\n            enrollmentDate = value;\n        }\n    }\n\n    public char Grade\n    {\n        get => grade;\n        set\n        {\n            char[] validGrades = { 'A', 'B', 'C', 'D', 'F' };\n            if (!validGrades.Contains(char.ToUpperInvariant(value)))\n                throw new ArgumentException($\"'{value}' is not a valid grade.\");\n            grade = char.ToUpperInvariant(value);\n        }\n    }\n\n    public bool IsPassing => grade != 'F';\n\n    public CourseEnrollment(string studentId, string courseCode)\n    {\n        StudentId = studentId ?? throw new ArgumentNullException(nameof(studentId));\n        CourseCode = courseCode ?? throw new ArgumentNullException(nameof(courseCode));\n        EnrollmentDate = DateTime.UtcNow;\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Encapsulation Mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Anti-patterns to Avoid",
        "items": [
          "Making fields public — always use properties instead.",
          "Exposing mutable collections directly — return a copy or use ReadOnlyCollection<T>.",
          "Using empty getters/setters that add no value — either fully expose or fully encapsulate.",
          "Doing heavy work in property getters — properties should be lightweight.",
          "Mixing public and private logic in the same method — keep methods focused."
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Encapsulate a Temperature Class",
        "description": "Create a Temperature class that stores Celsius privately. Expose a public Celsius property with validation (range: -273.15 to double.MaxValue) and a Fahrenheit property that converts to and from Celsius. Throw ArgumentOutOfRangeException for invalid values.",
        "steps": [
          "Create a private double _celsius field.",
          "Create a public double Celsius property with validation in the setter.",
          "Create a public double Fahrenheit property that converts to/from Celsius.",
          "Test with values like 0, 100, and -300 (the last one should throw)."
        ],
        "expectedOutcome": "A Temperature object that safely converts between Celsius and Fahrenheit and rejects invalid temperatures."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Property vs Field",
        "cards": [
          {
            "title": "Public Field",
            "description": "A publicly accessible variable directly on the class.",
            "pros": [
              "Simple syntax",
              "Minimal boilerplate"
            ],
            "cons": [
              "No validation possible",
              "Cannot trigger events",
              "No versioning flexibility",
              "Breaks encapsulation"
            ],
            "tags": [
              "avoid"
            ]
          },
          {
            "title": "Public Property",
            "description": "A get/set accessor pair that controls access to private data.",
            "pros": [
              "Validation on write",
              "Data binding support",
              "Versioning flexibility",
              "Can change backing storage without breaking API"
            ],
            "cons": [
              "Slightly more verbose"
            ],
            "tags": [
              "recommended"
            ]
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why not just make all fields public?",
            "answer": "Public fields expose internal state directly with no validation or control. Once a field is public, any code can set it to any value, potentially corrupting the object's state. Properties let you validate and maintain invariants."
          },
          {
            "question": "What is an init-only setter?",
            "answer": "An init accessor (public string Name { get; init; }) allows the property to be set only during object initialization (e.g., in a constructor or object initializer). After that, it becomes effectively read-only."
          },
          {
            "question": "Should I always make fields private?",
            "answer": "Yes, as a general rule. Private fields force all access through methods and properties, which gives you a single point to add logging, validation, or change the internal representation later."
          },
          {
            "question": "What does private protected mean?",
            "answer": "private protected restricts access to members of the same class AND derived classes, but only within the same assembly. It combines the restrictions of both private and protected."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Encapsulation hides implementation details behind a controlled public interface.",
          "Access modifiers (public, private, protected, internal) control visibility.",
          "Properties are the primary mechanism for encapsulation in C#.",
          "Validate data in setters to maintain object invariants.",
          "Private backing fields with public properties is the standard pattern."
        ],
        "takeaway": "Encapsulation is the first pillar of OOP and the foundation of writing maintainable, bug-resistant C# code."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
