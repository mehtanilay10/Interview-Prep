{
  "id": "lesson-csharp-014",
  "slug": "interfaces",
  "moduleSlug": "oop",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "oop",
    "interfaces",
    "contracts",
    "dependency-injection",
    "polymorphism"
  ],
  "title": "Interfaces",
  "description": "Master interface design in C#: contracts, multiple interface implementation, default interface methods, generics, and real-world patterns like dependency injection.",
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "An interface is a contract that defines **what** a class must do, not **how** it does it. It declares method signatures, properties, events, and indexers that implementing classes must provide. In C#, interfaces are the primary tool for achieving polymorphism without inheritance, enabling flexible and testable architecture."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Is an Interface?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An interface defines a set of capabilities that a class promises to fulfill. When a class implements an interface, it guarantees that it provides all the methods and properties declared in that interface. This decouples the consumer of an interface from the concrete class that implements it, allowing you to swap implementations freely."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Interface",
            "definition": "A contract that specifies method signatures and properties a class must implement."
          },
          {
            "term": "Implement",
            "definition": "To fulfill an interface contract by providing concrete method bodies in a class."
          },
          {
            "term": "Polymorphism via Interface",
            "definition": "Treating different objects uniformly through a shared interface type."
          },
          {
            "term": "Dependency Injection",
            "definition": "A design pattern where dependencies are provided (injected) from outside, often via interface types."
          },
          {
            "term": "Default Interface Method",
            "definition": "A method with an implementation inside an interface, introduced in C# 8.0."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "interface-implementation-diagram",
        "caption": "Multiple classes implementing the same IRepository<T> interface",
        "definition": "classDiagram\n    class IRepository {\n        <<interface>>\n        +GetById(int id)\n        +GetAll()\n        +Add(T entity)\n        +Delete(int id)\n    }\n    class SqlRepository {\n        +GetById(id)\n        +GetAll()\n        +Add(entity)\n        +Delete(id)\n    }\n    class InMemoryRepository {\n        +GetById(id)\n        +GetAll()\n        +Add(entity)\n        +Delete(id)\n    }\n    class MongoRepository {\n        +GetById(id)\n        +GetAll()\n        +Add(entity)\n        +Delete(id)\n    }\n\n    IRepository <|.. SqlRepository\n    IRepository <|.. InMemoryRepository\n    IRepository <|.. MongoRepository\n\n    note for IRepository \"Same contract, many implementations\""
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining and Implementing Interfaces"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By convention, C# interface names start with 'I' followed by PascalCase. Interface members are implicitly public and abstract — you cannot add access modifiers or provide a body (prior to C# 8). A class implements an interface using the same colon syntax as inheritance. A class can implement multiple interfaces, solving the multiple-inheritance limitation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generic Interface and Implementation",
        "content": "A generic IRepository<T> interface implemented by a concrete SqlRepository class.",
        "language": "csharp",
        "code": "// Generic interface — reusable for any entity type\npublic interface IRepository<T> where T : class\n{\n    T GetById(int id);\n    IEnumerable<T> GetAll();\n    void Add(T entity);\n    void Update(T entity);\n    void Delete(int id);\n}\n\n// Concrete implementation\npublic class SqlRepository<T> : IRepository<T> where T : class\n{\n    private readonly DbContext _context;\n    private readonly DbSet<T> _dbSet;\n\n    public SqlRepository(DbContext context)\n    {\n        _context = context;\n        _dbSet = context.Set<T>();\n    }\n\n    public T GetById(int id)\n    {\n        return _dbSet.Find(id) ?? throw new KeyNotFoundException();\n    }\n\n    public IEnumerable<T> GetAll()\n    {\n        return _dbSet.ToList();\n    }\n\n    public void Add(T entity)\n    {\n        _dbSet.Add(entity);\n        _context.SaveChanges();\n    }\n\n    public void Update(T entity)\n    {\n        _dbSet.Update(entity);\n        _context.SaveChanges();\n    }\n\n    public void Delete(int id)\n    {\n        var entity = GetById(id);\n        _dbSet.Remove(entity);\n        _context.SaveChanges();\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple Interface Implementation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A class can implement multiple interfaces, which is the primary mechanism for achieving a form of multiple inheritance in C#. This is powerful when a class needs to fulfill multiple distinct contracts — for example, a Document class might be both IPrintable and ISerializable."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple Interfaces",
        "content": "A Document class implementing IPrintable, ISaveable, and ICloneable simultaneously.",
        "language": "csharp",
        "code": "public interface IPrintable\n{\n    void Print();\n    void PrintPreview();\n}\n\npublic interface ISaveable\n{\n    void Save(string path);\n    string Load(string path);\n}\n\npublic interface ICloneable<T>\n{\n    T Clone();\n}\n\n// Implements ALL THREE interfaces\npublic class Document : IPrintable, ISaveable, ICloneable<Document>\n{\n    public string Title { get; set; }\n    public string Content { get; set; }\n\n    // IPrintable implementation\n    public void Print()\n    {\n        Console.WriteLine($\"Printing: {Title}\");\n        Console.WriteLine(Content);\n    }\n\n    public void PrintPreview()\n    {\n        Console.WriteLine($\"[Preview] {Title}...\");\n    }\n\n    // ISaveable implementation\n    public void Save(string path)\n    {\n        File.WriteAllText(path, $\"{Title}\\n{Content}\");\n    }\n\n    public string Load(string path)\n    {\n        return File.ReadAllText(path);\n    }\n\n    // ICloneable<Document> implementation\n    public Document Clone()\n    {\n        return new Document { Title = Title, Content = Content };\n    }\n}\n\n// Usage — polymorphic through any interface\nIPrintable printable = new Document { Title = \"Report\", Content = \"...\" };\nprintable.Print();\n\nISaveable saveable = new Document { Title = \"Report\", Content = \"...\" };\nsaveable.Save(\"report.txt\");"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Default Interface Methods (C# 8+)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Prior to C# 8, interfaces could only declare method signatures — any change to an interface would break all implementing classes. C# 8 introduced default interface methods, allowing you to provide a default implementation directly in the interface. This enables safe interface evolution without forcing all implementors to update immediately."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Default Interface Method",
        "content": "A default implementation in an interface that all implementors inherit automatically.",
        "language": "csharp",
        "code": "public interface INotificationService\n{\n    void Send(string message);\n\n    // Default implementation — available to all implementors\n    void SendWelcomeEmail(string userEmail)\n    {\n        string body = $\"Welcome to our service, {userEmail}!\";\n        Send($\"Email: {body}\");\n    }\n\n    // Static members in interfaces (C# 11)\n    static string ServiceName => \"NotificationHub\";\n}\n\npublic class SmsNotificationService : INotificationService\n{\n    // Must implement the abstract method\n    public void Send(string message)\n    {\n        Console.WriteLine($\"SMS: {message}\");\n    }\n\n    // Inherits SendWelcomeEmail() for free\n    // But can override if needed\n}\n\npublic class PushNotificationService : INotificationService\n{\n    public void Send(string message)\n    {\n        Console.WriteLine($\"Push: {message}\");\n    }\n\n    // Override the default implementation\n    public void SendWelcomeEmail(string userEmail)\n    {\n        Send($\"Welcome push notification for {userEmail}\");\n    }\n}\n\n// Usage\nINotificationService service = new SmsNotificationService();\nservice.SendWelcomeEmail(\"alice@example.com\"); // Uses the interface's default"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Interface Segregation Principle (ISP)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The Interface Segregation Principle is the 'I' in SOLID. It states that clients should not be forced to depend on interfaces they do not use. Instead of one large, fat interface, split it into smaller, more specific interfaces. This makes implementations cleaner and prevents classes from being burdened with methods they don't need."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Fat Interface vs Segregated Interfaces",
        "cards": [
          {
            "title": "Fat Interface (Bad)",
            "description": "One large IWorker interface forcing implementors to provide unused methods.",
            "pros": [
              "Single interface for all workers"
            ],
            "cons": [
              "Forces Robot to implement Eat()",
              "Forces Human to implement Recharge()",
              "Violates ISP",
              "Hard to maintain"
            ]
          },
          {
            "title": "Segregated Interfaces (Good)",
            "description": "Split IWorker into IWorkable, IFeedable, and IRechargeable.",
            "pros": [
              "Each class implements only what it needs",
              "Follows ISP",
              "Easy to extend",
              "Clearer intent"
            ],
            "cons": [
              "More interfaces to manage"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Real-World: Dependency Injection with Interfaces"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Interfaces are the backbone of dependency injection (DI), a pattern where dependencies are provided from the outside rather than created internally. This makes code loosely coupled, easier to test, and simpler to extend. The ASP.NET Core built-in DI container is a common example."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dependency Injection with Interfaces",
        "content": "Using ILogger<T> to decouple logging from business logic.",
        "language": "csharp",
        "code": "// Interface — the contract\npublic interface IOrderService\n{\n    Task<Order> CreateAsync(OrderDto dto);\n    Task<Order?> GetByIdAsync(int id);\n}\n\n// Implementation\npublic class OrderService : IOrderService\n{\n    private readonly IOrderRepository _repository;\n    private readonly ILogger<OrderService> _logger;\n\n    // Dependencies injected via constructor\n    public OrderService(IOrderRepository repository, ILogger<OrderService> logger)\n    {\n        _repository = repository;\n        _logger = logger;\n    }\n\n    public async Task<Order> CreateAsync(OrderDto dto)\n    {\n        _logger.LogInformation(\"Creating order for customer {CustomerId}\", dto.CustomerId);\n        var order = new Order(dto);\n        await _repository.AddAsync(order);\n        return order;\n    }\n\n    public async Task<Order?> GetByIdAsync(int id)\n    {\n        _logger.LogInformation(\"Fetching order {OrderId}\", id);\n        return await _repository.GetByIdAsync(id);\n    }\n}\n\n// Test double — swaps real repository for a mock\npublic class FakeOrderRepository : IOrderRepository\n{\n    private readonly List<Order> _orders = new();\n\n    public Task AddAsync(Order order)\n    {\n        _orders.Add(order);\n        return Task.CompletedTask;\n    }\n\n    public Task<Order?> GetByIdAsync(int id)\n    {\n        return Task.FromResult(_orders.FirstOrDefault(o => o.Id == id));\n    }\n}"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Payment Processing System",
        "description": "Create an IPaymentProcessor interface with ProcessPayment(decimal amount) and Refund(string transactionId) methods. Implement CreditCardProcessor and PayPalProcessor classes. Write a PaymentService that accepts IPaymentProcessor via DI and processes a list of payments.",
        "steps": [
          "Define IPaymentProcessor with ProcessPayment() and Refund() signatures.",
          "Implement CreditCardProcessor with a simple Console.WriteLine confirmation.",
          "Implement PayPalProcessor with a different confirmation message.",
          "Create PaymentService that takes IPaymentProcessor in its constructor.",
          "Test by passing both processor types and verifying polymorphic behavior."
        ],
        "expectedOutcome": "PaymentService works with any IPaymentProcessor implementation without knowing the concrete type."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Interfaces Checklist",
        "items": [
          {
            "text": "I can define an interface using the interface keyword.",
            "hint": "public interface IMyInterface { ... }"
          },
          {
            "text": "I know the naming convention (starts with I).",
            "hint": "IRepository, IService, IValidator"
          },
          {
            "text": "I can implement an interface in a class using the colon syntax.",
            "hint": "class MyClass : IMyInterface"
          },
          {
            "text": "I understand why interfaces enable dependency injection.",
            "hint": "They decouple consumers from concrete implementations."
          },
          {
            "text": "I know what default interface methods are and when to use them.",
            "hint": "C# 8+ feature for evolving interfaces without breaking implementors."
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can an interface have fields?",
            "answer": "No. Interfaces can only declare method signatures, properties, events, and indexers. They cannot contain fields. Starting with C# 11, you can use static abstract members in interfaces (for generic math), but instance fields are still not allowed."
          },
          {
            "question": "Can an interface inherit from another interface?",
            "answer": "Yes. An interface can extend one or more other interfaces, inheriting all their members. The implementing class must fulfill all members from the entire interface hierarchy."
          },
          {
            "question": "Should I use abstract classes or interfaces?",
            "answer": "Use abstract classes when you want to share code and have closely related types. Use interfaces when you want to define a contract that unrelated types can fulfill, or when a class needs to implement multiple contracts. In modern C#, the distinction is smaller with default interface methods."
          },
          {
            "question": "What is explicit interface implementation?",
            "answer": "Explicit implementation (void IPrintable.Print() { }) hides the interface method from the class's public API. The method can only be called through an interface reference. Use it when a class implements multiple interfaces with conflicting method signatures."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Interfaces define contracts — what a class does, not how.",
          "A class can implement multiple interfaces, overcoming single-inheritance limits.",
          "Default interface methods (C# 8+) allow safe interface evolution.",
          "Interfaces are central to dependency injection and testable architecture.",
          "Follow the Interface Segregation Principle — keep interfaces focused."
        ],
        "takeaway": "Interfaces enable flexible, loosely-coupled design by separating 'what' from 'how'."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
