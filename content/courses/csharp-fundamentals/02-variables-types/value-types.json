{
  "id": "lesson-csharp-006",
  "slug": "value-types",
  "moduleSlug": "variables-types",
  "title": "Value Types",
  "description": "Explore C# value types in depth — integers, floating-point numbers, char, bool, structs, and how they differ from reference types in memory and behavior.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 30,
  "tags": [
    "csharp",
    "value-types",
    "structs",
    "primitives",
    "stack",
    "integers",
    "floating-point"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "C# types fall into two broad categories: **value types** and **reference types**. Value types store their data directly in memory, usually on the **stack**. When you assign a value type to another variable, C# makes a complete copy — the two variables are entirely independent after the assignment."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Understanding value types is critical because they behave differently from reference types when you pass them to methods, assign them to other variables, or compare them. Getting this wrong is one of the most common sources of bugs for beginners."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-value-types-stack-heap",
        "caption": "How value types are stored directly on the stack, while reference types live on the heap with a pointer on the stack.",
        "definition": "graph TD\n    subgraph Stack[Stack Memory]\n        A[int a = 10]\n        B[Point p]\n        C[Reference to Person on heap]\n    end\n    subgraph Heap[Heap Memory]\n        D[int X = 10]\n        E[int Y = 20]\n        F[Person object]\n    end\n    B --> D\n    B --> E\n    C --> F\n    style Stack fill:#e1f5fe,stroke:#01579b\n    style Heap fill:#fff3e0,stroke:#e65100"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Built-in Value Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# provides several built-in (primitive) value types. Each has a specific size in memory and a defined range of valid values. Choosing the right type helps your program use memory efficiently and avoids unexpected overflow errors."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Integer Types"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Type",
          "Size",
          "Range",
          "Typical Use"
        ],
        "rows": [
          [
            "byte",
            "8 bits (1 byte)",
            "0 to 255",
            "Binary data, small counters"
          ],
          [
            "sbyte",
            "8 bits (1 byte)",
            "-128 to 127",
            "Signed small integers"
          ],
          [
            "short",
            "16 bits (2 bytes)",
            "-32,768 to 32,767",
            "Legacy COM interop, small ranges"
          ],
          [
            "ushort",
            "16 bits (2 bytes)",
            "0 to 65,535",
            "Unsigned small integers"
          ],
          [
            "int",
            "32 bits (4 bytes)",
            "-2,147,483,648 to 2,147,483,647",
            "Default integer type; most common choice"
          ],
          [
            "uint",
            "32 bits (4 bytes)",
            "0 to 4,294,967,295",
            "Unsigned counts, bitwise operations"
          ],
          [
            "long",
            "64 bits (8 bytes)",
            "-9.2×10¹⁸ to 9.2×10¹⁸",
            "Large numbers, file sizes, timestamps"
          ],
          [
            "ulong",
            "64 bits (8 bytes)",
            "0 to 1.8×10¹⁹",
            "Unsigned large numbers"
          ]
        ]
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`int` is the most commonly used integer type in C#. It offers a good balance of range and performance. The compiler treats bare integer literals (like `42`) as `int` by default. Use `long` when you need to store numbers larger than 2 billion, and use `byte` when memory efficiency matters (such as in large arrays)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Floating-Point Types"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Type",
          "Size",
          "Precision",
          "Typical Use"
        ],
        "rows": [
          [
            "float",
            "32 bits",
            "~7 significant digits",
            "Graphics, games, performance-critical math"
          ],
          [
            "double",
            "64 bits",
            "~15-16 significant digits",
            "Default floating-point type; scientific computing"
          ],
          [
            "decimal",
            "128 bits",
            "28-29 significant digits",
            "Financial calculations where precision is critical"
          ]
        ]
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`float` and `double` use the IEEE 754 standard and are extremely fast but can have tiny rounding errors. For example, `0.1 + 0.2` is not exactly `0.3` in floating-point. `decimal` avoids this by using a base-10 representation, making it ideal for money calculations."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Floating-Point Precision Gotcha",
        "content": "Why double is not always exact",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // Floating-point rounding error\n        double a = 0.1;\n        double b = 0.2;\n        double sum = a + b;\n        Console.WriteLine(sum == 0.3);        // False!\n        Console.WriteLine(sum.ToString(\"R\"));   // 0.30000000000000004\n\n        // decimal handles this correctly\n        decimal m1 = 0.1m;\n        decimal m2 = 0.2m;\n        decimal mSum = m1 + m2;\n        Console.WriteLine(mSum == 0.3m);        // True\n        Console.WriteLine(mSum);                 // 0.3\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "char and bool"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `char` type holds a single Unicode character (16 bits in C#). It uses single quotes: `'A'`. The `bool` type holds either `true` or `false` and is essential for conditions, loops, and logical operations."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "char and bool in Practice",
        "content": "Working with characters and booleans",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // Character — uses single quotes, stores Unicode\n        char grade = 'A';\n        char symbol = '\\u2713'; // ✓ checkmark (Unicode)\n        Console.WriteLine($\"Grade: {grade}, Symbol: {symbol}\");\n\n        // Boolean — true or false only\n        bool isLoggedIn = true;\n        bool hasPermission = false;\n\n        // Boolean from comparison\n        int age = 20;\n        bool isAdult = age >= 18;\n\n        // Boolean logic\n        bool canEnter = isLoggedIn && hasPermission;\n        Console.WriteLine($\"Can enter: {canEnter}\"); // False\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Structs — Custom Value Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can define your own value types using the `struct` keyword. Structs are useful for small, immutable data structures that represent a single value — like a 2D point, a complex number, or a coordinate. Unlike classes, structs cannot inherit from other structs or classes (except `System.ValueType`), and they always have a default parameterless constructor."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Defining and Using a Struct",
        "content": "A Point struct representing a 2D coordinate",
        "language": "csharp",
        "code": "using System;\n\n// Define a custom value type with struct\npublic struct Point\n{\n    public int X { get; set; }\n    public int Y { get; set; }\n\n    // Constructor for convenient initialization\n    public Point(int x, int y)\n    {\n        X = x;\n        Y = y;\n    }\n\n    // Method on the struct\n    public double DistanceFromOrigin()\n    {\n        return Math.Sqrt(X * X + Y * Y);\n    }\n\n    public override string ToString()\n    {\n        return $\"({X}, {Y})\";\n    }\n}\n\nclass Program\n{\n    static void Main()\n    {\n        // Initialize with constructor\n        Point p1 = new Point(3, 4);\n        Console.WriteLine(p1.DistanceFromOrigin()); // 5.0\n\n        // Copy — this is a full copy, not a reference!\n        Point p2 = p1;\n        p2.X = 100;\n        Console.WriteLine(p1); // (3, 4) — unchanged\n        Console.WriteLine(p2); // (100, 4)\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Value Semantics",
        "text": "When you assign a value type (including structs) to another variable, C# copies every bit of data. Modifying the copy does NOT affect the original. This is called value semantics and is the core difference from reference types."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Structs"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Good candidates for structs:",
        "items": [
          "Small data structures (typically under 16 bytes)",
          "Immutable data that represents a single value",
          "Types that would otherwise be passed by value frequently (e.g., `Point`, `Complex`, `Rectangle`)",
          "Performance-critical code where heap allocation overhead matters"
        ]
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Avoid structs when:",
        "items": [
          "The struct is large (more than 16 bytes)",
          "You need inheritance or polymorphism",
          "You need a parameterless constructor that does real work",
          "The type will be boxed frequently (cast to object or interface)"
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-value-copy",
        "caption": "Assigning a value type creates a full copy, so the original and copy are completely independent.",
        "definition": "graph LR\n    A[Original: Point X=3 Y=4] -->|Copy| B[Copy: Point X=3 Y=4]\n    B -->|Modify| C[Copy: Point X=100 Y=4]\n    A -->|Unchanged| D[Original: Point X=3 Y=4]\n    style A fill:#e8f5e9,stroke:#2e7d32\n    style D fill:#e8f5e9,stroke:#2e7d32\n    style C fill:#ffebee,stroke:#c62828"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Value Types Checklist",
        "items": [
          {
            "text": "I know that int, double, bool, char, and struct are value types",
            "hint": "Anything that stores its data directly is a value type."
          },
          {
            "text": "I understand that value types are copied on assignment",
            "hint": "Assigning one to another creates a separate copy."
          },
          {
            "text": "I know when to use struct vs class",
            "hint": "Small, immutable, single-value types are good struct candidates."
          },
          {
            "text": "I can explain the difference between float, double, and decimal",
            "hint": "Use decimal for money; double for most other math."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Create a Temperature Struct",
        "description": "Build a struct that represents a temperature in Celsius, with methods to convert to Fahrenheit and Kelvin.",
        "steps": [
          "Create a struct called `Temperature` with a `Celsius` property (double)",
          "Add a constructor that accepts a Celsius value",
          "Add a read-only property `Fahrenheit` that returns Celsius * 9/5 + 32",
          "Add a read-only property `Kelvin` that returns Celsius + 273.15",
          "Override `ToString()` to return a formatted string like \"25°C / 77°F / 298.15K\"",
          "In `Main`, create two `Temperature` values, copy one to another, modify the copy, and show the original is unchanged"
        ],
        "expectedOutcome": "A working `Temperature` struct with conversion properties and demonstrated value semantics when copied."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Are all primitive types value types?",
            "answer": "Almost all. `int`, `long`, `float`, `double`, `decimal`, `char`, and `bool` are value types. `string` and `object` are reference types, which is a common exception that surprises beginners."
          },
          {
            "question": "What is boxing and unboxing?",
            "answer": "Boxing is when a value type is wrapped inside an `object` reference, copying it to the heap. Unboxing extracts it back. Both have a performance cost and should be avoided in performance-sensitive code."
          },
          {
            "question": "Should I make everything a struct for speed?",
            "answer": "No. Structs only provide a benefit for small, frequently-copied types. Large structs are slower because copying them is expensive. Microsoft recommends structs be under 16 bytes."
          },
          {
            "question": "Can structs be null?",
            "answer": "No — unless you use `Nullable<T>` syntax: `Point? p = null;`. The `?` suffix wraps the value type in a nullable wrapper that can represent null."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Value types store data directly on the stack",
          "Assignment creates a full copy (value semantics)",
          "Built-in value types: int, long, float, double, decimal, char, bool",
          "Use decimal for financial calculations to avoid rounding errors",
          "Structs let you define custom value types",
          "Good structs are small (under 16 bytes) and immutable"
        ],
        "takeaway": "Value types are fast, efficient, and copied by value. Use them for small, self-contained pieces of data."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
