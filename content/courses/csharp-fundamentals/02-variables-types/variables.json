{
  "id": "lesson-csharp-005",
  "slug": "variables",
  "moduleSlug": "variables-types",
  "title": "Variables",
  "description": "Understand what variables are, how to declare them, naming conventions, the var keyword, constants, and best practices for storing data in C#.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "variables",
    "declaration",
    "assignment",
    "naming",
    "constants"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Every C# program works with data — numbers, text, dates, and more. **Variables** are named containers that hold this data in memory while your program runs. Think of them as labeled boxes: each box has a name, a type (what can go inside), and a value (what's currently inside)."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you create a variable, the computer reserves a small area of memory and gives it the name you chose. From that point on, you can read or change the value stored there by referring to the variable name in your code. Understanding variables is the foundation for everything else in C#."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Is a Variable?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A variable has three essential parts: a **name** (identifier), a **type** (what kind of data it holds), and a **value** (the actual data). For example, `int age = 30;` creates a variable named `age` of type `int` with the value `30`. The C# compiler uses the type to know how much memory to allocate and what operations are valid on that data."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Variable",
            "definition": "A named memory location that stores a value of a specific type during program execution."
          },
          {
            "term": "Declaration",
            "definition": "The act of telling the compiler about a variable's name and type before using it."
          },
          {
            "term": "Initialization",
            "definition": "Assigning an initial value to a variable at the time of declaration."
          },
          {
            "term": "Identifier",
            "definition": "The name given to a variable, method, class, or other program element."
          },
          {
            "term": "Literal",
            "definition": "A fixed value written directly in code, such as `42`, `\"Hello\"`, or `true`."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Declaring and Initializing Variables"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In C#, you declare a variable by writing its type followed by its name. You can optionally assign a value at the same time (called initialization). Variables that are declared but not initialized contain default values — for numeric types it's `0`, for `bool` it's `false`, and for reference types it's `null`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Declaration and Initialization",
        "content": "Different ways to create variables in C#",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // 1. Declaration only (gets default value)\n        int score;\n        Console.WriteLine(score); // Compile error: use of unassigned variable\n\n        // 2. Declaration with initialization\n        int lives = 3;\n        string playerName = \"Alice\";\n        bool isGameOver = false;\n\n        // 3. Declare multiple variables of the same type\n        int x = 10, y = 20, z = 30;\n\n        // 4. Change value after declaration\n        lives = 2;\n        Console.WriteLine($\"{playerName} has {lives} lives left\");\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Unassigned Variables",
        "text": "C# does NOT allow reading a local variable before it has been assigned a value. The compiler will give you an error. Class-level fields get a default value automatically, but local variables inside methods do not."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The var Keyword"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `var` keyword lets the compiler infer the variable's type from the value on the right side. It is still **statically typed** — the type is locked in at compile time and cannot change. `var` is not the same as `dynamic`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using var",
        "content": "Type inference in action",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // Type is inferred from the initializer\n        var age = 25;           // int\n        var price = 19.99;      // double\n        var name = \"Bob\";      // string\n        var isActive = true;    // bool\n        var items = new[] { 1, 2, 3 }; // int[]\n\n        // Print inferred types\n        Console.WriteLine(age.GetType());    // System.Int32\n        Console.WriteLine(price.GetType());  // System.Double\n        Console.WriteLine(name.GetType());   // System.String\n\n        // This would NOT compile — type is locked\n        // var score;\n        // score = 10;\n\n        // var requires initialization on the same line\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "When to Use var",
        "text": "Use var when the type is obvious from the right-hand side (e.g., `var list = new List<string>()`). Avoid it when the type is unclear (e.g., `var result = GetData()`), as it reduces readability for other developers."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Naming Conventions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# developers follow well-established naming conventions so that code is readable and consistent across projects. Following these conventions makes your code easier for others (and your future self) to understand."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Context",
          "Convention",
          "Example"
        ],
        "rows": [
          [
            "Local variables & parameters",
            "camelCase",
            "`userName`, `totalPrice`, `itemCount`"
          ],
          [
            "Public fields / methods / properties",
            "PascalCase",
            "`UserName`, `TotalPrice`, `ItemCount`"
          ],
          [
            "Private fields",
            "_camelCase (leading underscore)",
            "`_userName`, `_totalPrice`"
          ],
          [
            "Constants",
            "PascalCase",
            "`MaxRetries`, `DefaultTimeout`"
          ],
          [
            "Avoid",
            "Abbreviations, single letters (except loop counters)",
            "Use `index` not `i`, `count` not `c`"
          ]
        ]
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Rules for Naming Variables",
        "items": [
          "Start with a letter or underscore (`_`) — never with a digit",
          "Use only letters, digits, and underscores — no spaces or special characters",
          "Avoid C# keywords like `class`, `int`, `return` — use `@class` if absolutely necessary",
          "Use meaningful, descriptive names — `customerAge` not `ca`",
          "Stick to English for maximum compatibility with tools and libraries"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Constants with const and readonly"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a value should never change, use `const` or `readonly`. A `const` variable is evaluated at compile time and must be assigned when declared. A `readonly` field is evaluated at runtime and can be assigned either at declaration or inside a constructor."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "const vs readonly",
        "content": "Immutable values in C#",
        "language": "csharp",
        "code": "using System;\n\nclass Config\n{\n    // const — compile-time constant, must be assigned here\n    public const int MaxConnections = 100;\n    public const string AppName = \"InterviewPrep\";\n\n    // readonly — runtime constant, can be assigned in constructor\n    public readonly Guid InstanceId;\n    public readonly DateTime StartTime;\n\n    public Config()\n    {\n        // Can assign readonly fields in the constructor\n        InstanceId = Guid.NewGuid();\n        StartTime = DateTime.UtcNow;\n    }\n\n    public void PrintConfig()\n    {\n        Console.WriteLine($\"{AppName}: max {MaxConnections} connections\");\n        Console.WriteLine($\"Instance: {InstanceId}\");\n    }\n}\n\nclass Program\n{\n    static void Main()\n    {\n        var config = new Config();\n        config.PrintConfig();\n\n        // const is replaced at compile time — no memory overhead\n        int limit = Config.MaxConnections;\n    }\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "const vs readonly",
        "cards": [
          {
            "title": "const",
            "description": "Compile-time constant. Value baked into every assembly that references it.",
            "pros": [
              "Zero runtime overhead",
              "Clearly communicates immutability",
              "Accessible without an instance"
            ],
            "cons": [
              "Must be assigned at declaration",
              "Only works with primitive or string literals",
              "Value is baked into referencing assemblies"
            ],
            "tags": [
              "compile-time",
              "static"
            ]
          },
          {
            "title": "readonly",
            "description": "Runtime constant. Can be assigned in declaration or constructor.",
            "pros": [
              "Can hold reference types like Guid, DateTime",
              "Value set at runtime",
              "Per-instance values possible"
            ],
            "cons": [
              "Only for instance or static fields",
              "Slight runtime cost vs const"
            ],
            "tags": [
              "runtime",
              "flexible"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Default Values",
        "text": "Every type has a default value. For value types like int, bool, and double, it's 0, false, or 0.0. For reference types like string and arrays, it's null. Uninitialized local variables are NOT given a default — the compiler prevents you from reading them before assignment."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Declare Your First Variables",
        "description": "Create a small program that demonstrates variable declaration, initialization, and naming conventions.",
        "steps": [
          "Create a new console application with `dotnet new console`",
          "Inside `Main`, declare a string variable called `studentName` and set it to your name",
          "Declare an `int` called `score` and assign it a value between 0 and 100",
          "Declare a `bool` called `hasPassed` and set it based on whether score >= 60",
          "Use `var` to declare a `double` called `gpa` with a value like 3.75",
          "Print all variables using string interpolation",
          "Change `score` to a new value and print again to confirm variables are mutable"
        ],
        "expectedOutcome": "A console program that declares, initializes, and updates variables, printing formatted output using string interpolation."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "What happens if I use a variable before assigning it?",
            "answer": "The C# compiler will give you error CS0165: 'Use of unassigned local variable'. You must assign a value before reading it."
          },
          {
            "question": "Is var the same as dynamic?",
            "answer": "No. `var` is statically typed — the type is determined at compile time and cannot change. `dynamic` skips compile-time checking and resolves types at runtime."
          },
          {
            "question": "Can I change a const value later?",
            "answer": "No. `const` values are immutable by design. If you need a value that can be set once at runtime, use `readonly` instead."
          },
          {
            "question": "Should I always use var?",
            "answer": "No. Use `var` when the type is clear from the initializer. Use explicit types when the type is not obvious, to improve readability."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Variables are named containers that hold typed data",
          "Declare with `type name = value` or use `var` for inference",
          "Local variables must be assigned before reading",
          "Use camelCase for local variables, PascalCase for constants",
          "Use `const` for compile-time constants, `readonly` for runtime ones",
          "Use meaningful names that describe the data"
        ],
        "takeaway": "Variables are the building blocks of every C# program. Declare them clearly, name them well, and use const or readonly for values that should never change."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
