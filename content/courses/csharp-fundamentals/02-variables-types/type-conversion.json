{
  "id": "lesson-csharp-008",
  "slug": "type-conversion",
  "moduleSlug": "variables-types",
  "title": "Type Conversion",
  "description": "Master implicit and explicit conversions, casting, parsing, TryParse, and the Convert class — with examples of overflow, truncation, and safe conversion patterns.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "conversion",
    "casting",
    "parsing",
    "TryParse",
    "overflow",
    "Convert"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Real programs constantly move data between different types. You might read a number as text from a file, calculate a total as a `double` and need to round it to an `int`, or accept user input as a `string` and convert it to a `DateTime`. **Type conversion** is the mechanism C# uses to transform data from one type to another."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# has two categories of numeric conversion: **implicit** (automatic, no data loss) and **explicit** (requires a cast, possible data loss). For converting between strings and other types, C# provides `Parse`, `TryParse`, and the `Convert` class."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-conversion-flow",
        "caption": "Overview of type conversion paths in C#: numeric conversions and string parsing.",
        "definition": "flowchart TD\n    A[Source Type] --> B{Size / Range?}\n    B -->|Wider / Higher range| C[Implicit Conversion]\n    B -->|Narrower / Lower range| D[Explicit Cast]\n    E[String] --> F{Valid format?}\n    F -->|Yes| G[Parse / TryParse / Convert]\n    F -->|No| H[Exception or false]\n    G --> I[Target Type]\n    C --> I\n    D --> I\n    style C fill:#e8f5e9,stroke:#2e7d32\n    style D fill:#fff3e0,stroke:#e65100\n    style H fill:#ffebee,stroke:#c62828"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Implicit Conversion"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An **implicit conversion** happens automatically when C# is certain no data loss will occur. This typically means converting from a smaller type to a larger type. The compiler does the work without any special syntax."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "From",
          "To",
          "Safe?",
          "Example"
        ],
        "rows": [
          [
            "byte",
            "short, int, long, float, double, decimal",
            "Yes",
            "byte b = 255; int i = b;"
          ],
          [
            "short",
            "int, long, float, double, decimal",
            "Yes",
            "short s = 32000; int i = s;"
          ],
          [
            "int",
            "long, float, double, decimal",
            "Yes",
            "int i = 100; long l = i;"
          ],
          [
            "long",
            "float, double, decimal",
            "Yes",
            "long l = 1000; double d = l;"
          ],
          [
            "char",
            "ushort, int, uint, long, ulong, float, double, decimal",
            "Yes",
            "char c = 'A'; int code = c; // 65"
          ],
          [
            "int → byte",
            "byte",
            "No — explicit cast required",
            "int i = 300; byte b = (byte)i; // 44 (wraps)"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Implicit Conversions",
        "content": "Automatic widening conversions that never lose data",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // int → long (automatic, no data loss)\n        int count = 50000;\n        long bigCount = count;\n        Console.WriteLine(bigCount); // 50000\n\n        // int → double (automatic)\n        int items = 42;\n        double ratio = items;\n        Console.WriteLine(ratio); // 42.0\n\n        // char → int (Unicode code point)\n        char letter = 'A';\n        int code = letter;\n        Console.WriteLine(code); // 65\n\n        // Numeric promotion in expressions\n        int x = 10;\n        double y = 3.14;\n        double result = x + y; // x promoted to double automatically\n        Console.WriteLine(result); // 13.14\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Explicit Conversion (Casting)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When converting from a larger type to a smaller one, C# requires an **explicit cast** using the `(type)` syntax. The compiler cannot guarantee that the value fits in the target type, so it demands that you acknowledge the risk."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Truncation and Overflow",
        "text": "Casting a floating-point number to an integer **truncates** (drops the decimal part, it does NOT round). Casting a number that exceeds the target type's range causes silent overflow (wrapping) in unchecked context, or an OverflowException in checked context."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Explicit Casts and Overflow",
        "content": "Narrowing conversions that can lose data",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // double → int: truncates (does not round)\n        double price = 19.99;\n        int roundedDown = (int)price;\n        Console.WriteLine(roundedDown); // 19 (not 20!)\n\n        // long → int: possible overflow\n        long bigNumber = 3_000_000_000;\n        int smallNumber = (int)bigNumber; // Wraps around!\n        Console.WriteLine(smallNumber); // -1294967296\n\n        // checked context — throws OverflowException\n        checked\n        {\n            try\n            {\n                int max = int.MaxValue;\n                byte b = checked((byte)(max + 1));\n                Console.WriteLine(b); // Won't reach here\n            }\n            catch (OverflowException ex)\n            {\n                Console.WriteLine($\"Overflow caught: {ex.Message}\");\n            }\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "checked and unchecked Contexts"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, integer arithmetic in C# is **unchecked** — overflow wraps around silently. You can change this behavior with the `checked` keyword, which causes an `OverflowException` to be thrown when overflow occurs. This is useful for financial or safety-critical code where silent corruption would be a serious bug."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "checked vs unchecked:",
        "items": [
          "`checked { ... }` — throws OverflowException on integer overflow",
          "`unchecked { ... }` — explicitly allows overflow wrapping",
          "You can also enable checked arithmetic for the entire project in the .csproj: `<CheckForOverflowUnderflow>true</CheckForOverflowUnderflow>`",
          "checked only affects integer types — floating-point overflow produces Infinity or NaN instead"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Parse and TryParse"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "User input, file contents, and API responses almost always arrive as strings. C# provides methods to convert strings to other types. `Parse` throws an exception if the string isn't valid. `TryParse` returns `true` or `false` and outputs the result through an `out` parameter — it never throws."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Parse vs TryParse",
        "content": "Converting strings to numeric types safely",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // Parse — throws FormatException on invalid input\n        string input1 = \"123\";\n        int num1 = int.Parse(input1);\n        Console.WriteLine(num1); // 123\n        // int.Parse(\"abc\"); // CRASH: FormatException\n\n        // TryParse — returns bool, never throws\n        string input2 = \"456\";\n        if (int.TryParse(input2, out int num2))\n        {\n            Console.WriteLine($\"Parsed: {num2}\"); // Parsed: 456\n        }\n        else\n        {\n            Console.WriteLine(\"Invalid number format\");\n        }\n\n        // TryParse with invalid input — safe!\n        string input3 = \"not-a-number\";\n        if (int.TryParse(input3, out int num3))\n        {\n            Console.WriteLine(num3);\n        }\n        else\n        {\n            Console.WriteLine(\"Could not parse — but no crash!\");\n        }\n\n        // Available TryParse methods for other types\n        double.TryParse(\"3.14\", out double d);\n        decimal.TryParse(\"99.99m\", out decimal m);\n        bool.TryParse(\"true\", out bool b);\n        DateTime.TryParse(\"2024-01-15\", out DateTime dt);\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Convert Class"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `System.Convert` class provides static methods for converting between base types. It handles `null` gracefully (converting it to the default value for the target type) and supports many types including `ToBoolean`, `ToDateTime`, `ToDecimal`, and more."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using the Convert Class",
        "content": "Converting types with Convert",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // String to int\n        string ageStr = \"25\";\n        int age = Convert.ToInt32(ageStr);\n        Console.WriteLine(age); // 25\n\n        // String to double\n        string priceStr = \"19.99\";\n        double price = Convert.ToDouble(priceStr);\n        Console.WriteLine(price); // 19.99\n\n        // String to boolean\n        string boolStr = \"true\";\n        bool isActive = Convert.ToBoolean(boolStr);\n        Console.WriteLine(isActive); // True\n\n        // null converts to default value\n        string? nullStr = null;\n        int defaultValue = Convert.ToInt32(nullStr);\n        Console.WriteLine(defaultValue); // 0\n\n        // int to double\n        int count = 42;\n        double d = Convert.ToDouble(count);\n        Console.WriteLine(d); // 42.0\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer TryParse",
        "text": "Use `TryParse` for user input or any data that might be invalid. It's faster than catching exceptions, and exceptions should be reserved for truly exceptional conditions — not expected user mistakes."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Number Parser",
        "description": "Write a program that reads numbers from the user and performs safe conversions.",
        "steps": [
          "Prompt the user to enter an integer with `Console.ReadLine()`",
          "Use `int.TryParse` to safely convert the input",
          "If successful, convert the integer to `double` and divide by 2 — print the result",
          "If it fails, print an error message and ask again (use a loop)",
          "After a successful parse, also parse the same value as `decimal` and print both double and decimal versions side by side",
          "Add a third input: ask for a `bool` using `bool.TryParse` and print the result"
        ],
        "expectedOutcome": "A program that safely reads and converts user input using TryParse, handles invalid input gracefully, and demonstrates the difference between double and decimal representations."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "What is the difference between Parse and Convert?",
            "answer": "`int.Parse(\"abc\")` throws a `FormatException`. `Convert.ToInt32(\"abc\")` also throws. But `Convert.ToInt32(null)` returns `0`, while `int.Parse(null)` throws a `ArgumentNullException`. `Convert` is more forgiving of null."
          },
          {
            "question": "Does casting round the number?",
            "answer": "No. Casting from double/float to an integer type **truncates** — it drops the decimal portion. Use `Math.Round()` before casting if you need rounding."
          },
          {
            "question": "What is narrowing vs widening conversion?",
            "answer": "Widening (implicit) converts from a smaller type to a larger one without data loss. Narrowing (explicit) converts from a larger type to a smaller one and may lose data, requiring an explicit cast."
          },
          {
            "question": "When should I use checked?",
            "answer": "Use `checked` in financial calculations or anywhere that silent overflow would cause incorrect results. For general-purpose code where performance matters and overflow is unlikely, `unchecked` (the default) is fine."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Implicit conversions happen automatically when no data loss is possible",
          "Explicit casts use `(type)` syntax and may truncate or overflow",
          "Casting from double to int truncates — it does NOT round",
          "Use `checked` to detect overflow at runtime",
          "`TryParse` is the safest way to convert strings to numbers",
          "`Convert` class handles null gracefully but still throws on invalid format"
        ],
        "takeaway": "Type conversion is everywhere in C#. Choose the right method: implicit for safe widening, explicit cast with awareness of truncation, and TryParse for user input."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
