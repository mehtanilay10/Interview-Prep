{
  "id": "lesson-csharp-007",
  "slug": "reference-types",
  "moduleSlug": "variables-types",
  "title": "Reference Types",
  "description": "Learn about C# reference types — classes, strings, arrays, and collections — including heap allocation, null references, and shared state.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "reference-types",
    "classes",
    "arrays",
    "strings",
    "heap",
    "collections"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "While value types store data directly, **reference types** store a reference (essentially a memory address) that points to the actual data on the **heap**. When you assign a reference type variable to another, you copy the reference — not the object itself. Both variables now point to the same piece of data on the heap."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "This behavior is called **reference semantics** and it's one of the most important concepts in C#. When you pass a reference type to a method or assign it to another variable, changes made through one reference are visible through all other references to the same object."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-reference-types-heap",
        "caption": "Reference types live on the heap. Variables hold a reference (pointer) to the heap object, not the data itself.",
        "definition": "graph TD\n    subgraph Stack[Stack Memory]\n        A[string name = Alice]\n        B[Person p1]\n        C[Person p2]\n    end\n    subgraph Heap[Heap Memory]\n        D[Person: Name=Alice Age=30]\n    end\n    B -->|reference| D\n    C -->|same reference| D\n    A -->|reference| E[string: Alice]\n    style Stack fill:#e1f5fe,stroke:#01579b\n    style Heap fill:#fff3e0,stroke:#e65100\n    style D fill:#ffebee,stroke:#c62828"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Reference Types"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Built-in reference types in C#:",
        "items": [
          "**string** — immutable sequence of Unicode characters",
          "**array** — fixed-size, zero-based collection of elements of the same type",
          "**class** — the primary way to define custom reference types",
          "**interface** — defines a contract that classes implement",
          "**delegate** — type-safe function pointer for callbacks and events",
          "**List<T>, Dictionary<K,V>** — generic collection types from System.Collections.Generic"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Strings"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Strings in C# are reference types, but they behave unusually in one important way: they are **immutable**. Once a string is created, its content can never change. Any operation that appears to modify a string (like `ToUpper()` or `Replace()`) actually returns a brand new string object, leaving the original untouched."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "String Basics and Common Operations",
        "content": "Creating and manipulating strings in C#",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // String declaration and initialization\n        string firstName = \"Alice\";\n        string lastName = \"Johnson\";\n\n        // Concatenation — creates a NEW string each time\n        string fullName = firstName + \" \" + lastName;\n        Console.WriteLine(fullName); // Alice Johnson\n\n        // String interpolation — preferred for readability\n        string greeting = $\"Hello, {firstName} {lastName}!\";\n        Console.WriteLine(greeting); // Hello, Alice Johnson!\n\n        // Common string methods — each returns a NEW string\n        string upper = firstName.ToUpper();        // ALICE\n        string lower = firstName.ToLower();        // alice\n        int length = firstName.Length;             // 5\n        bool contains = fullName.Contains(\"ice\"); // True\n        string replaced = fullName.Replace(\"Alice\", \"Bob\"); // Bob Johnson\n        string sub = fullName.Substring(0, 5);     // Alice\n        bool startsWith = fullName.StartsWith(\"Ali\"); // True\n        bool endsWith = fullName.EndsWith(\"son\");  // True\n\n        // String is immutable — original is never changed\n        Console.WriteLine(fullName); // Still \"Alice Johnson\"\n\n        // Verbatim strings — no escape sequences needed\n        string path = @\"C:\\Users\\Alice\\Documents\";\n        string multiline = @\"\n            Line 1\n            Line 2\n            Line 3\n        \";\n        Console.WriteLine(path);\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "String Immutability",
        "text": "Because strings are immutable, repeated concatenation in a loop (e.g., `s += x`) creates many temporary strings and is very slow. Use `StringBuilder` for efficient string building in loops."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using StringBuilder for Efficiency",
        "content": "Efficient string building with StringBuilder",
        "language": "csharp",
        "code": "using System;\nusing System.Text;\n\nclass Program\n{\n    static void Main()\n    {\n        // Inefficient: creates many temporary strings\n        // string result = \"\";\n        // for (int i = 0; i < 1000; i++)\n        //     result += i + \" \";\n\n        // Efficient: StringBuilder modifies a buffer in place\n        var sb = new StringBuilder();\n        for (int i = 0; i < 1000; i++)\n        {\n            sb.Append(i);\n            sb.Append(\" \");\n        }\n        string result = sb.ToString();\n        Console.WriteLine(result.Length); // ~5000 characters\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Arrays"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An **array** is a fixed-size, zero-indexed collection of elements of the same type. Arrays are reference types — the array variable holds a reference to the array object on the heap. The array's length is fixed at creation and cannot be changed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Working with Arrays",
        "content": "Declaring, initializing, and iterating arrays",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // Declare and initialize with values\n        int[] scores = new int[] { 95, 82, 77, 64, 91 };\n\n        // Shorthand syntax (compiler infers the type and size)\n        string[] names = { \"Alice\", \"Bob\", \"Charlie\" };\n\n        // Access elements by zero-based index\n        Console.WriteLine(scores[0]);  // 95 (first element)\n        Console.WriteLine(names[2]);   // Charlie (third element)\n\n        // Modify an element\n        scores[0] = 100;\n        Console.WriteLine(scores[0]);  // 100\n\n        // Array length (number of elements)\n        Console.WriteLine(scores.Length); // 5\n\n        // Loop through with foreach (read-only)\n        Console.WriteLine(\"\\nScores:\");\n        foreach (int score in scores)\n        {\n            Console.WriteLine(score);\n        }\n\n        // Loop with index using for\n        Console.WriteLine(\"\\nNames with index:\");\n        for (int i = 0; i < names.Length; i++)\n        {\n            Console.WriteLine($\"{i}: {names[i]}\");\n        }\n\n        // Multidimensional array\n        int[,] matrix = new int[2, 3] { { 1, 2, 3 }, { 4, 5, 6 } };\n        Console.WriteLine(matrix[1, 2]); // 6\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Lists and Generic Collections"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Arrays have a fixed size, which is often inconvenient. The `List<T>` class provides a resizable array-like collection that can grow or shrink dynamically. It is the most commonly used collection in C#. Other useful collections include `Dictionary<TKey, TValue>` for key-value pairs and `Queue<T>` / `Stack<T>` for first-in-first-out and last-in-first-out patterns."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "List<T> in Action",
        "content": "Using the generic List collection",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\nclass Program\n{\n    static void Main()\n    {\n        // Create a list of strings\n        List<string> tasks = new List<string>();\n\n        // Add items\n        tasks.Add(\"Buy groceries\");\n        tasks.Add(\"Write report\");\n        tasks.Add(\"Call dentist\");\n        Console.WriteLine($\"Count: {tasks.Count}\"); // 3\n\n        // Insert at a specific position\n        tasks.Insert(1, \"Pick up dry cleaning\");\n\n        // Remove an item\n        tasks.Remove(\"Write report\");\n\n        // Access by index (like an array)\n        Console.WriteLine(tasks[0]); // Buy groceries\n\n        // Check if an item exists\n        bool hasTask = tasks.Contains(\"Call dentist\"); // True\n\n        // Iterate\n        Console.WriteLine(\"\\nYour tasks:\");\n        foreach (string task in tasks)\n        {\n            Console.WriteLine($\"- {task}\");\n        }\n\n        // Dictionary — key-value pairs\n        Dictionary<string, int> ages = new Dictionary<string, int>\n        {\n            { \"Alice\", 30 },\n            { \"Bob\", 25 },\n            { \"Charlie\", 35 }\n        };\n        Console.WriteLine($\"\\nBob's age: {ages[\"Bob\"]}\"); // 25\n    }\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Value Types vs Reference Types",
        "cards": [
          {
            "title": "Value Types",
            "description": "Store data directly. Copied on assignment. Typically on the stack.",
            "pros": [
              "Fast allocation and deallocation",
              "No garbage collection overhead",
              "Independent copies prevent accidental shared state"
            ],
            "cons": [
              "Copied fully — expensive for large data",
              "Cannot be null (without Nullable<T>)",
              "No inheritance or polymorphism"
            ],
            "tags": [
              "stack",
              "copy",
              "fast"
            ]
          },
          {
            "title": "Reference Types",
            "description": "Store a reference to heap data. Shared on assignment. Garbage collected.",
            "pros": [
              "Efficient for large objects (only the reference is copied)",
              "Supports inheritance and polymorphism",
              "Can represent null (absence of value)"
            ],
            "cons": [
              "Allocation on the heap is slower",
              "Garbage collector overhead",
              "Shared state can cause bugs if not managed carefully"
            ],
            "tags": [
              "heap",
              "shared",
              "gc"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "NullReferenceException",
        "text": "The most common runtime error in C#. It occurs when you try to access a member (method, property, field) on a reference type variable that is `null`. Always check for null before accessing members, or use the null-conditional operator (`?.`) introduced in C# 6."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "NullReferenceException and Safe Access",
        "content": "Avoiding null reference errors",
        "language": "csharp",
        "code": "using System;\n\nclass Person\n{\n    public string Name { get; set; }\n}\n\nclass Program\n{\n    static void Main()\n    {\n        Person? person = GetPerson(); // Might return null\n\n        // Unsafe — crashes if person is null\n        // Console.WriteLine(person.Name.Length);\n\n        // Safe: null-conditional operator (?.)\n        int? length = person?.Name?.Length;\n        Console.WriteLine(length ?? 0); // 0 if null\n\n        // Safe: null-coalescing operator (??)\n        string displayName = person?.Name ?? \"Unknown\";\n        Console.WriteLine(displayName);\n    }\n\n    static Person GetPerson()\n    {\n        return null; // Simulating a failed lookup\n    }\n}"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Task Manager",
        "description": "Create a simple task manager using List<string> and demonstrate reference semantics.",
        "steps": [
          "Create a `List<string>` called `tasks` and add at least 3 tasks",
          "Print the count and each task",
          "Remove one task by value",
          "Demonstrate reference semantics: assign `tasks` to a second list called `shared` and add a new item to `shared`. Print `tasks` to show the change is visible through both references.",
          "Create a `List<string>` called `independentTasks` and use `.ToList()` to create a true copy. Add to `independentTasks` and verify `tasks` is unchanged"
        ],
        "expectedOutcome": "A console program showing shared state via reference assignment and independent copies via `.ToList()`. Understanding why `shared` and `tasks` see the same changes."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Is string a reference type even though it seems to act like a value?",
            "answer": "Yes. `string` is a reference type — the variable holds a reference to the string data on the heap. However, strings are immutable, which makes them behave more like value types in practice (e.g., equality compares content, not reference)."
          },
          {
            "question": "What is the difference between `=` for value types and reference types?",
            "answer": "For value types, `=` copies the entire value. For reference types, `=` copies only the reference (the memory address). Both variables then point to the same heap object."
          },
          {
            "question": "When is an object eligible for garbage collection?",
            "answer": "When no reachable references point to it. The .NET garbage collector periodically reclaims heap memory for objects that are no longer accessible from any live variable or object graph."
          },
          {
            "question": "Why is string immutable?",
            "answer": "Immutability enables string interning (saving memory by sharing identical strings), thread safety, and security (e.g., file paths can't be modified after validation). The trade-off is that any modification creates a new string."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Reference types store a pointer to heap data",
          "Assignment copies the reference, not the object",
          "Changes through one reference are visible through all references",
          "string is a reference type but immutable",
          "Use List<T> for dynamic collections instead of fixed arrays",
          "Always check for null to avoid NullReferenceException"
        ],
        "takeaway": "Reference types enable complex data structures and polymorphism. Understanding when data is shared vs copied is essential for writing correct C# code."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
