{
  "id": "lesson-csharp-026",
  "slug": "methods",
  "moduleSlug": "getting-started",
  "courseSlug": "csharp-fundamentals",
  "title": "Methods",
  "description": "Learn how to define and use methods in C#.",
  "order": 5,
  "difficulty": "beginner",
  "estimatedMinutes": 22,
  "tags": [
    "csharp",
    "methods",
    "functions",
    "parameters",
    "return-types"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Methods are the fundamental building blocks of C# programs. They let you encapsulate reusable logic, organize your code into meaningful units, and make your programs easier to read, test, and maintain. In this lesson, you'll learn how to define methods with parameters and return types, use method overloading, and apply advanced parameter modifiers like `ref`, `out`, and `in`."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is a Method?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A method is a named block of code that performs a specific task. You define a method once, then call (invoke) it whenever you need that task performed. Methods can accept input values called **parameters** and can optionally **return** a result."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Method Anatomy"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Parts of a Method Declaration",
        "items": [
          "**Access modifier** (e.g., `public`, `private`) — Who can call this method",
          "**Return type** (e.g., `int`, `string`, `void`) — What type of value the method gives back",
          "**Method name** (e.g., `CalculateTotal`) — What you call to invoke it",
          "**Parameters** (e.g., `(int a, int b)`) — Input values the method needs",
          "**Method body** (`{ ... }`) — The code that runs when the method is called"
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Method Definition",
        "content": "A simple method that adds two numbers",
        "language": "csharp",
        "code": "public class MathHelper\n{\n    // A method that takes two ints and returns their sum\n    public int Add(int a, int b)\n    {\n        int result = a + b;\n        return result;\n    }\n\n    // A void method — no return value, just performs an action\n    public void PrintResult(int result)\n    {\n        Console.WriteLine($\"Result: {result}\");\n    }\n}\n\n// Usage\nvar helper = new MathHelper();\nint sum = helper.Add(5, 3);        // sum = 8\nhelper.PrintResult(sum);            // Prints: Result: 8"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Return Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The return type tells the compiler what kind of value a method will produce. If a method does not produce a value, use `void`. Otherwise, specify the type of value returned. The `return` keyword sends a value back to the caller and immediately exits the method."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Different Return Types",
        "content": "Methods returning different types",
        "language": "csharp",
        "code": "public class ReturnExamples\n{\n    // Returns a string\n    public string GetGreeting(string name)\n    {\n        return $\"Hello, {name}!\";\n    }\n\n    // Returns a bool\n    public bool IsAdult(int age)\n    {\n        return age >= 18;\n    }\n\n    // Returns a double\n    public double CalculateArea(double radius)\n    {\n        return Math.PI * radius * radius;\n    }\n\n    // Void — no return value\n    public void LogMessage(string message)\n    {\n        Console.WriteLine($\"[LOG] {DateTime.Now}: {message}\");\n        // No return statement needed\n    }\n\n    // Early return\n    public string GetDayName(int dayNumber)\n    {\n        if (dayNumber < 1 || dayNumber > 7)\n        {\n            return \"Invalid day\";  // Exit early\n        }\n        \n        return dayNumber switch\n        {\n            1 => \"Monday\",\n            2 => \"Tuesday\",\n            3 => \"Wednesday\",\n            4 => \"Thursday\",\n            5 => \"Friday\",\n            6 => \"Saturday\",\n            7 => \"Sunday\",\n            _ => \"Unknown\"\n        };\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Void vs Return",
        "text": "A `void` method cannot use `return value;` — it can only use `return;` to exit early. Attempting to return a value from a void method is a compile-time error."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Parameters let you pass data into a method. When you call a method, you provide **arguments** that are assigned to the method's parameters. C# offers several parameter types for different scenarios."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Required vs Optional Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, all parameters are **required** — you must provide a value when calling the method. You can make parameters **optional** by assigning them a default value in the method signature."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Optional Parameters",
        "content": "Using default parameter values",
        "language": "csharp",
        "code": "public class Greeter\n{\n    // Required parameter + optional parameter with default value\n    public void Greet(string name, int repeatCount = 1, bool loudly = false)\n    {\n        for (int i = 0; i < repeatCount; i++)\n        {\n            string message = $\"Hello, {name}!\";\n            if (loudly)\n            {\n                message = message.ToUpper();\n            }\n            Console.WriteLine(message);\n        }\n    }\n}\n\n// Calling with different combinations:\nvar greeter = new Greeter();\ngreeter.Greet(\"Alice\");                    // Uses defaults: 1 time, not loudly\ngreeter.Greet(\"Bob\", 3);                   // 3 times, not loudly\ngreeter.Greet(\"Charlie\", loudly: true);   // Named argument: 1 time, loudly\ngreeter.Greet(\"Dana\", 2, true);            // 2 times, loudly"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Named Arguments",
        "text": "Named arguments let you specify parameters by name instead of position, making calls more readable and allowing you to skip optional parameters. Use `loudly: true` instead of providing every preceding parameter."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "The params Array"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `params` keyword allows a method to accept a variable number of arguments. The compiler creates an array from the arguments, so you can pass as many values as needed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using params for Variable Arguments",
        "content": "A method that accepts any number of integers",
        "language": "csharp",
        "code": "public class Statistics\n{\n    // params allows passing any number of int arguments\n    public int Sum(params int[] numbers)\n    {\n        int total = 0;\n        foreach (int n in numbers)\n        {\n            total += n;\n        }\n        return total;\n    }\n\n    // Can also pass an existing array\n    public double Average(params double[] values)\n    {\n        if (values.Length == 0) return 0;\n        return values.Sum() / values.Length;\n    }\n}\n\n// Usage\nvar stats = new Statistics();\n\nint sum1 = stats.Sum(1, 2, 3, 4, 5);           // sum1 = 15\nint sum2 = stats.Sum(10, 20);                   // sum2 = 30\nint sum3 = stats.Sum();                         // sum3 = 0 (no arguments)\n\nint[] values = { 1, 2, 3, 4, 5, 6 };\ndouble avg = stats.Average(values);             // avg = 3.5"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "params Rules",
        "text": "A method can have only one `params` parameter, and it must be the last parameter. You can pass individual values or an array — the compiler handles both."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Method Overloading"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Method overloading** allows you to define multiple methods with the same name but different parameter lists. The compiler selects the appropriate method based on the number and types of arguments passed. This provides flexibility and a consistent API."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Method Overloading",
        "content": "Multiple Print methods with different parameter signatures",
        "language": "csharp",
        "code": "public class Printer\n{\n    // Overload 1: No parameters\n    public void Print()\n    {\n        Console.WriteLine(\"(no message)\");\n    }\n\n    // Overload 2: One string parameter\n    public void Print(string message)\n    {\n        Console.WriteLine(message);\n    }\n\n    // Overload 3: String + integer for repeated printing\n    public void Print(string message, int repeatCount)\n    {\n        for (int i = 0; i < repeatCount; i++)\n        {\n            Console.WriteLine(message);\n        }\n    }\n\n    // Overload 4: Multiple values\n    public void Print(string message, ConsoleColor color)\n    {\n        Console.ForegroundColor = color;\n        Console.WriteLine(message);\n        Console.ResetColor();\n    }\n}\n\n// Usage\nvar printer = new Printer();\nprinter.Print();                        // (no message)\nprinter.Print(\"Hello!\");                // Hello!\nprinter.Print(\"Spam\", 3);               // Prints Spam 3 times\nprinter.Print(\"Alert!\", ConsoleColor.Red); // Red Alert!"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Resolution Rules"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The compiler resolves overloaded methods at compile time based on: (1) the number of arguments, (2) the types of arguments, and (3) the presence of optional parameters. If no exact match is found, the compiler attempts implicit conversions."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Ambiguous Overloads",
        "text": "Avoid overloads that could cause ambiguity. For example, having both `Print(int)` and `Print(double)` can be confusing when called with `Print(5)` — the compiler prefers `int`, but adding `Print(long)` could create ambiguity."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "ref, out, and in Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, C# passes arguments **by value**, meaning the method receives a copy of the value. For reference types, this means a copy of the reference (the object itself is shared). The `ref`, `out`, and `in` keywords let you pass arguments **by reference**, giving the method direct access to the original variable."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Parameter Passing Keywords",
        "cards": [
          {
            "title": "ref",
            "description": "Passes a variable by reference. The method can read and modify the original variable. The variable must be initialized before calling.",
            "pros": [
              "Method can modify caller's variable",
              "Variable must be initialized before call"
            ],
            "cons": [
              "Can make code harder to follow"
            ],
            "tags": [
              "reference",
              "modifiable"
            ]
          },
          {
            "title": "out",
            "description": "Passes a variable by reference specifically for returning multiple values. The method MUST assign a value before returning. Variable does NOT need initialization.",
            "pros": [
              "Return multiple values from a method",
              "No pre-initialization needed"
            ],
            "cons": [
              "Method is forced to assign a value"
            ],
            "tags": [
              "reference",
              "multiple-return"
            ]
          },
          {
            "title": "in",
            "description": "Passes a variable by reference for read-only access. The method can read but NOT modify the original variable. Useful for large structs to avoid copying.",
            "pros": [
              "Avoids copying large structs",
              "Read-only — safer than ref"
            ],
            "cons": [
              "Only works with variables (not literals)"
            ],
            "tags": [
              "reference",
              "read-only"
            ]
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ref, out, and in Examples",
        "content": "Demonstrating all three parameter passing modes",
        "language": "csharp",
        "code": "public class ReferenceExamples\n{\n    // ref: method reads and modifies the original variable\n    public void DoubleValue(ref int value)\n    {\n        value *= 2;  // Modifies the caller's variable\n    }\n\n    // out: method must assign a value before returning\n    public bool TryParseInt(string input, out int result)\n    {\n        result = 0;\n        return int.TryParse(input, out result);\n    }\n\n    // in: method reads but cannot modify (good for large structs)\n    public long CalculateDistance(in Point point1, in Point point2)\n    {\n        // point1.X = 5; // COMPILE ERROR: cannot modify in parameter\n        long dx = point1.X - point2.X;\n        long dy = point1.Y - point2.Y;\n        return (long)Math.Sqrt(dx * dx + dy * dy);\n    }\n}\n\npublic readonly struct Point\n{\n    public int X { get; }\n    public int Y { get; }\n    public Point(int x, int y) => (X, Y) = (x, y);\n}\n\n// Usage:\nint x = 5;\nvar examples = new ReferenceExamples();\nexamples.DoubleValue(ref x);\nConsole.WriteLine(x);  // Prints: 10\n\nif (examples.TryParseInt(\"42\", out int parsed))\n{\n    Console.WriteLine(parsed);  // Prints: 42\n}\n\nvar p1 = new Point(0, 0);\nvar p2 = new Point(3, 4);\nlong dist = examples.CalculateDistance(in p1, in p2);\nConsole.WriteLine(dist);  // Prints: 5"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "out Requires Assignment",
        "text": "A method with an `out` parameter MUST assign a value to that parameter on every possible execution path before returning. The compiler enforces this — you will get a compile error if you forget."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Recursion"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Recursion** is when a method calls itself to solve a smaller instance of the same problem. Every recursive method needs: (1) a **base case** that stops the recursion, and (2) a **recursive case** that breaks the problem into a smaller one."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "method-call-stack",
        "caption": "Method call stack showing how recursion works",
        "definition": "flowchart TD\n    A[Factorial3] -->|calls| B[Factorial2]\n    B -->|calls| C[Factorial1]\n    C -->|calls| D[Factorial0]\n    D -->|returns 1| C\n    C -->|returns 1*1=1| B\n    B -->|returns 2*1=2| A\n    A -->|returns 6*2=6| E[Result: 6]\n    style A fill:#4F46E5,color:#fff\n    style B fill:#6366F1,color:#fff\n    style C fill:#8B5CF6,color:#fff\n    style D fill:#A78BFA,color:#fff\n    style E fill:#22C55E,color:#fff"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Recursive Factorial",
        "content": "Calculating factorial using recursion",
        "language": "csharp",
        "code": "public class RecursionExamples\n{\n    // Factorial: n! = n * (n-1)!\n    // Base case: 0! = 1\n    public int Factorial(int n)\n    {\n        // Base case: stop the recursion\n        if (n <= 1)\n        {\n            return 1;\n        }\n        \n        // Recursive case: n! = n * (n-1)!\n        return n * Factorial(n - 1);\n    }\n\n    // Fibonacci sequence\n    public int Fibonacci(int n)\n    {\n        if (n <= 0) return 0;\n        if (n == 1) return 1;\n        \n        return Fibonacci(n - 1) + Fibonacci(n - 2);\n    }\n\n    // Sum of digits using recursion\n    public int SumDigits(int number)\n    {\n        if (number == 0) return 0;\n        return (number % 10) + SumDigits(number / 10);\n    }\n}\n\n// Usage\nvar recursion = new RecursionExamples();\nConsole.WriteLine(recursion.Factorial(5));  // 120 (5*4*3*2*1)\nConsole.WriteLine(recursion.Fibonacci(6));   // 8 (0,1,1,2,3,5,8)\nConsole.WriteLine(recursion.SumDigits(12345)); // 15 (1+2+3+4+5)"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Stack Overflow",
        "text": "Every recursive call adds a frame to the call stack. Deep recursion (e.g., Factorial(10000)) can cause a StackOverflowException. For very deep recursion, use iteration or `System.Runtime.CompilerServices.MethodImplOptions.AggressiveInlining` carefully."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Expression-Bodied Members"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Expression-bodied members let you write concise method definitions using the `=>` (lambda) syntax when the method body is a single expression. This reduces boilerplate and improves readability for simple methods."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Expression-Bodied Methods and Properties",
        "content": "Writing concise methods with the => syntax",
        "language": "csharp",
        "code": "public class Circle\n{\n    private double _radius;\n\n    public Circle(double radius)\n    {\n        _radius = radius;\n    }\n\n    // Expression-bodied property\n    public double Area => Math.PI * _radius * _radius;\n\n    // Expression-bodied method\n    public double Circumference => 2 * Math.PI * _radius;\n\n    // Expression-bodied method with parameter\n    public bool Contains(Point point) =>\n        Math.Pow(point.X, 2) + Math.Pow(point.Y, 2) <= Math.Pow(_radius, 2);\n\n    // Expression-bodied constructor\n    public Circle() : this(1.0) { }\n}\n\npublic record Point(double X, double Y);"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "When to Use Expression Bodies",
        "text": "Expression-bodied members are ideal for simple getters, calculations, and conversions. Avoid them for complex logic with multiple statements — use a full method body for readability."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Local Functions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Local functions** are methods defined inside another method. They are useful for helper logic that is only relevant within the containing method. Local functions can access variables from the enclosing scope (closures) and can use `async`/`await`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Local Functions",
        "content": "Using local functions for helper logic",
        "language": "csharp",
        "code": "public class DataProcessor\n{\n    public void ProcessNumbers(int[] numbers)\n    {\n        // Local function: only visible inside ProcessNumbers\n        bool IsPrime(int n)\n        {\n            if (n <= 1) return false;\n            if (n == 2) return true;\n            if (n % 2 == 0) return false;\n            \n            for (int i = 3; i <= Math.Sqrt(n); i += 2)\n            {\n                if (n % i == 0) return false;\n            }\n            return true;\n        }\n\n        // Another local function using the outer variable\n        void PrintPrime(int n)\n        {\n            Console.WriteLine($\"{n} is prime!\");\n        }\n\n        // Use the local functions\n        foreach (int num in numbers)\n        {\n            if (IsPrime(num))\n            {\n                PrintPrime(num);\n            }\n        }\n    }\n}\n\n// Usage\nvar processor = new DataProcessor();\nprocessor.ProcessNumbers(new[] { 1, 2, 3, 4, 5, 17, 20 });\n// Output: 2 is prime!\n//         3 is prime!\n//         5 is prime!\n//         17 is prime!"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Local Functions vs Lambdas",
        "text": "Local functions are more powerful than lambdas: they support generics, can have access modifiers, support `ref`/`out` parameters, and have better stack trace readability. Use local functions for complex helpers; use lambdas for short, inline delegates."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Method Overloading vs Default Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Both method overloading and optional parameters with default values can achieve similar results. Choosing between them depends on your API design preferences."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Method Overloading vs Optional Parameters",
        "cards": [
          {
            "title": "Method Overloading",
            "description": "Define multiple methods with the same name but different parameter signatures.",
            "pros": [
              "Clear intent — each method has a distinct signature",
              "Works well for versioning APIs",
              "No risk of breaking changes when adding new overloads"
            ],
            "cons": [
              "More code to write",
              "Can lead to many similar method definitions"
            ],
            "tags": [
              "overloading",
              "api-design"
            ]
          },
          {
            "title": "Optional Parameters",
            "description": "Define a single method with parameters that have default values.",
            "pros": [
              "Less code duplication",
              "Easy to add new optional parameters",
              "Very readable at the call site"
            ],
            "cons": [
              "Risk of breaking changes if default values change",
              "Can lead to confusion with many optional params"
            ],
            "tags": [
              "optional",
              "defaults"
            ]
          }
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Method",
            "definition": "A named block of code that performs a specific task. Methods can accept parameters and return values."
          },
          {
            "term": "Parameters",
            "definition": "Variables declared in a method signature that receive values passed when the method is called."
          },
          {
            "term": "Return Type",
            "definition": "The data type of the value a method returns. Use `void` for methods that do not return a value."
          },
          {
            "term": "Method Overloading",
            "definition": "Defining multiple methods with the same name but different parameter lists, allowing the same operation to work with different inputs."
          },
          {
            "term": "ref Parameter",
            "definition": "A parameter passed by reference, allowing the method to read and modify the caller's original variable."
          },
          {
            "term": "out Parameter",
            "definition": "A parameter passed by reference used to return additional values. The method must assign a value before returning."
          },
          {
            "term": "in Parameter",
            "definition": "A parameter passed by reference for read-only access, useful for avoiding copies of large structs."
          },
          {
            "term": "Recursion",
            "definition": "When a method calls itself to solve a smaller version of the same problem, requiring a base case to stop."
          },
          {
            "term": "Local Function",
            "definition": "A function defined inside another method's body, visible only within that containing method."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Calculator with Overloaded Methods",
        "description": "Create a Calculator class demonstrating method overloading, optional parameters, and return types.",
        "steps": [
          "Create a new console project: `dotnet new console -n CalcMethods`",
          "Create a `Calculator` class in the same file or a separate one",
          "Add an overloaded `Add` method: one that takes 2 ints, one that takes 3 ints, and one that takes a `params int[]`",
          "Add a `Divide` method that returns a double and handles division by zero",
          "Add an optional parameter method `Power(double base, int exponent = 2)` that calculates base^exponent",
          "Add an `out` parameter method `TryDivide(double a, double b, out double result)` that returns false if b is zero",
          "In Program.cs, test all methods and print the results"
        ],
        "expectedOutcome": "A Calculator class with multiple overloaded Add methods, safe division, exponentiation, and TryDivide with out parameters — all tested in the main program."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Methods Mastery Checklist",
        "items": [
          {
            "text": "Defined methods with return types and void methods",
            "hint": "Use void when no value is returned"
          },
          {
            "text": "Used required and optional parameters with default values",
            "hint": "Optional params go at the end of the parameter list"
          },
          {
            "text": "Called methods using named arguments",
            "hint": "methodName(paramName: value)"
          },
          {
            "text": "Used params for variable argument counts",
            "hint": "params must be the last parameter"
          },
          {
            "text": "Created method overloads with different signatures",
            "hint": "Overloads differ by parameter count or types"
          },
          {
            "text": "Used ref and out parameters correctly",
            "hint": "ref requires initialization; out requires assignment"
          },
          {
            "text": "Wrote a recursive method with a base case",
            "hint": "Always define the stopping condition first"
          },
          {
            "text": "Used expression-bodied members for simple methods",
            "hint": "Use => for single-expression methods"
          },
          {
            "text": "Created a local function inside a method",
            "hint": "Local functions can capture outer variables"
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "When should I use a method vs inline code?",
            "answer": "Use a method when the logic is reused, complex, or needs a meaningful name. Inline code is fine for trivial one-off operations. Methods improve readability, enable reuse, and make testing easier."
          },
          {
            "question": "What's the difference between ref and out?",
            "answer": "Both pass by reference, but `ref` requires the variable to be initialized before the call, and the method can read the initial value. `out` does not require initialization, but the method MUST assign a value before returning — it's designed for returning multiple values."
          },
          {
            "question": "When should I use recursion?",
            "answer": "Recursion is ideal for problems with a natural recursive structure: tree traversal, factorial, Fibonacci, and divide-and-conquer algorithms. For simple iteration (like looping over a list), a `for` or `foreach` loop is clearer and more efficient."
          },
          {
            "question": "Are local functions better than lambdas?",
            "answer": "Local functions are generally better when you need: generics, `ref`/`out` parameters, better stack traces, or a named function. Lambdas are better for short, inline delegates passed to methods like LINQ's `Where()`, `Select()`, etc."
          },
          {
            "question": "Can I overload by return type alone?",
            "answer": "No. C# does not allow overloading methods based on return type alone. The parameter list must differ. For example, you cannot have `int GetValue()` and `string GetValue()` — the compiler cannot determine which to call based on return type."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Methods encapsulate reusable logic with parameters and return types",
          "Use `void` for methods that don't return a value",
          "Optional parameters use default values; named arguments improve readability",
          "`params` allows a variable number of arguments",
          "Method overloading provides flexibility with different parameter signatures",
          "`ref` modifies caller's variable; `out` returns multiple values; `in` passes read-only by reference",
          "Recursion solves problems by breaking them into smaller sub-problems",
          "Expression-bodied members (`=>`) simplify single-expression methods",
          "Local functions are scoped helpers defined inside another method"
        ],
        "takeaway": "Methods are the core of C# programming — mastering parameters, overloading, and reference semantics makes you a more effective developer."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
