{
  "id": "lesson-csharp-fundamentals-advanced-async-concurrency-threading-lock-and-monitor",
  "slug": "lock-and-monitor",
  "moduleSlug": "advanced-async-concurrency-threading",
  "courseSlug": "csharp-fundamentals",
  "title": "lock and Monitor",
  "description": "Use the lock statement and Monitor class to protect shared state from concurrent access.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The `lock` keyword is the simplest way to prevent two threads from executing the same block of code at the same time. Under the hood, it uses the `Monitor` class to acquire an exclusive lock on an object."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How lock works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a thread enters a `lock` block, it calls `Monitor.Enter` on the lock object. If another thread already holds that lock, the requesting thread blocks until the lock is released. When execution leaves the `lock` block (normally or via exception), `Monitor.Exit` is called automatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic lock usage",
        "language": "csharp",
        "code": "private readonly object _lock = new object();\nprivate int _value;\n\npublic void UpdateValue(int newValue)\n{\n    lock (_lock)\n    {\n        _value = newValue;\n        LogUpdate(_value);\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Pitfalls of lock"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "**Lock on public objects** — If external code can access your lock object, it can cause deadlocks or denial of service.",
          "**Reentrancy** — The same thread can re-enter a `lock` it already holds. This is safe in single-threaded contexts but can mask design problems.",
          "**Lock ordering** — Acquiring multiple locks in inconsistent order across threads is a recipe for deadlock."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Deadlock caused by inconsistent lock ordering",
        "language": "csharp",
        "code": "private readonly object _a = new object();\nprivate readonly object _b = new object();\n\npublic void FirstThenSecond()\n{\n    lock (_a) { lock (_b) { DoWork(); } }\n}\n\npublic void SecondThenFirst()\n{\n    lock (_b) { lock (_a) { DoWork(); } }\n}\n\n// If Thread 1 calls FirstThenSecond and Thread 2 calls SecondThenFirst\n// simultaneously, both threads wait forever for the other to release a lock."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Keep lock objects private",
        "text": "Always use a dedicated, private object for locking. Locking on `this`, `typeof(MyClass)`, or string literals exposes the lock to external code."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "`lock` is syntactic sugar over `Monitor.Enter`/`Monitor.Exit`.",
          "Lock objects should be private and dedicated.",
          "Inconsistent lock ordering causes deadlocks.",
          "Prefer fine-grained locks to reduce contention."
        ]
      }
    }
  ],
  "relatedLessons": [
    "race-conditions-and-thread-safety",
    "semaphoreslim-and-bounded-concurrency",
    "deadlocks-starvation-and-livelocks"
  ],
  "furtherReading": [
    { "title": "lock Statement (C# Reference)", "url": "https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/lock" },
    { "title": "Monitor Class (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/api/system.threading.monitor" }
  ]
}