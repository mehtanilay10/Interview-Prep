{
  "id": "lesson-csharp-fundamentals-advanced-async-concurrency-threading-async-io-vs-cpu-bound-work",
  "slug": "async-io-vs-cpu-bound-work",
  "moduleSlug": "advanced-async-concurrency-threading",
  "courseSlug": "csharp-fundamentals",
  "title": "Async I/O vs CPU-bound work",
  "description": "Learn when to use async I/O, when to offload CPU-bound work to the ThreadPool, and how to choose the right pattern.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Not all slow operations should be made `async`. Knowing the difference between I/O-bound and CPU-bound work is one of the most important skills for writing scalable .NET applications."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "I/O-bound vs CPU-bound"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**I/O-bound** work spends most of its time waiting for an external system: a database query, an HTTP call, or a file read. The CPU is mostly idle during this wait. Making I/O-bound work `async` frees the current thread to do other things while the OS waits for the external response."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**CPU-bound** work keeps the CPU busy: image processing, encryption, complex calculations, or parsing large files. There is no external wait. To keep the UI responsive or to parallelize CPU-bound work, you offload it to the ThreadPool with `Task.Run`."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Characteristic", "I/O-bound", "CPU-bound"],
        "rows": [
          ["Bottleneck", "External system latency", "CPU speed"],
          ["Typical examples", "HTTP requests, DB queries, file I/O", "Encryption, calculations, parsing"],
          ["Async pattern", "Use `async`/`await` with I/O APIs", "Wrap in `Task.Run`"],
          ["Thread usage", "Frees thread during wait", "Occupies thread while computing"]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Async I/O — HTTP request",
        "language": "csharp",
        "code": "public async Task<string> FetchDataAsync(CancellationToken ct)\n{\n    // I/O-bound: thread is released while waiting for the network\n    using var response = await _httpClient.GetAsync(\"/api/data\", ct);\n    response.EnsureSuccessStatusCode();\n    return await response.Content.ReadAsStringAsync(ct);\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "CPU-bound work — heavy calculation",
        "language": "csharp",
        "code": "public Task<int> ComputeHashAsync(string input)\n{\n    // CPU-bound: move work to the ThreadPool\n    return Task.Run(() => ComputeSha256Hash(input));\n}\n\nprivate int ComputeSha256Hash(string input)\n{\n    using var sha = SHA256.Create();\n    byte[] bytes = Encoding.UTF8.GetBytes(input);\n    byte[] hash = sha.ComputeHash(bytes);\n    return BitConverter.ToInt32(hash, 0);\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Do not wrap I/O in Task.Run",
        "text": "Wrapping an already-async I/O operation in `Task.Run` adds an unnecessary ThreadPool thread and increases latency. Use the native async API directly."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "I/O-bound work uses `async`/`await` with native async APIs.",
          "CPU-bound work uses `Task.Run` to offload to the ThreadPool.",
          "Wrapping I/O in `Task.Run` wastes threads and hurts scalability."
        ]
      }
    }
  ],
  "relatedLessons": [
    "threadpool-internals-and-thread-starvation",
    "task-scheduling-and-continuations",
    "valuetask-and-when-it-is-appropriate"
  ],
  "furtherReading": [
    { "title": "Async I/O vs CPU-bound (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/standard/asynchronous-programming-patterns/async-scenarios" },
    { "title": "Stephen Cleary — CPU-bound vs I/O-bound", "url": "https://blog.stephencleary.com/2013/11/there-is-no-thread.html" }
  ]
}