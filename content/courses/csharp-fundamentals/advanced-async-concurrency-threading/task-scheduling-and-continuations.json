{
  "id": "lesson-csharp-fundamentals-advanced-async-concurrency-threading-task-scheduling-and-continuations",
  "slug": "task-scheduling-and-continuations",
  "moduleSlug": "advanced-async-concurrency-threading",
  "courseSlug": "csharp-fundamentals",
  "title": "Task scheduling and continuations",
  "description": "Understand how the TPL schedules tasks and how continuations let you compose asynchronous workflows.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When you call `Task.Run`, you are asking the Task Parallel Library to schedule work. The TPL decides *where* and *when* that work runs using a scheduler. Continuations then let you chain follow-up work that runs after a task completes."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Task schedulers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A `TaskScheduler` determines how and where queued tasks execute. The default scheduler runs tasks on the ThreadPool. You can create custom schedulers for specialized behavior, such as limiting concurrency or running tasks on a dedicated thread."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "**ThreadPoolTaskScheduler** — The default. Uses the ThreadPool and balances load across cores.",
          "**SynchronizationContextTaskScheduler** — Used in UI and ASP.NET contexts. Posts work back to the original context.",
          "**Custom schedulers** — Built by subclassing `TaskScheduler` for custom queuing logic."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using a custom TaskScheduler",
        "language": "csharp",
        "code": "public class LimitedConcurrencyLevelTaskScheduler : TaskScheduler\n{\n    [ThreadStatic]\n    private static bool _currentThreadIsProcessing;\n\n    private readonly int _maxDegreeOfParallelism;\n    private readonly Queue<Task> _tasks = new Queue<Task>();\n\n    public LimitedConcurrencyLevelTaskScheduler(int maxDegreeOfParallelism)\n    {\n        _maxDegreeOfParallelism = maxDegreeOfParallelism;\n    }\n\n    protected override IEnumerable<Task> GetScheduledTasks()\n    {\n        return _tasks.ToArray();\n    }\n\n    protected override void QueueTask(Task task)\n    {\n        lock (_tasks)\n        {\n            _tasks.Enqueue(task);\n        }\n        ThreadPool.QueueUserWorkItem(ProcessNext);\n    }\n\n    private void ProcessNext(object _)\n    {\n        if (_currentThreadIsProcessing)\n            return;\n\n        _currentThreadIsProcessing = true;\n        try\n        {\n            while (_tasks.Count > 0)\n            {\n                Task task;\n                lock (_tasks)\n                {\n                    task = _tasks.Dequeue();\n                }\n                TryExecuteTask(task);\n            }\n        }\n        finally\n        {\n            _currentThreadIsProcessing = false;\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Continuations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A continuation is work that runs automatically when a task finishes. You create continuations with `ContinueWith` or by simply `await`-ing the task. Continuations capture the current `SynchronizationContext` by default, which means they may run on a specific thread (like the UI thread) unless told otherwise."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Continuation with ContinueWith",
        "language": "csharp",
        "code": "Task<int> computeTask = Task.Run(() => ComputeValue());\n\ncomputeTask.ContinueWith(antecedent =>\n{\n    Console.WriteLine($\"Result: {antecedent.Result}\");\n}, TaskScheduler.Default); // Runs on ThreadPool"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer async/await over ContinueWith",
        "text": "`ContinueWith` is powerful but easy to misuse. It does not catch exceptions automatically and can lead to unobserved exceptions. `async`/`await` composes continuations safely and reads more naturally."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "The default TaskScheduler uses the ThreadPool.",
          "Custom schedulers can limit concurrency or change execution context.",
          "Continuations run after a task completes.",
          "`async`/`await` is generally safer than `ContinueWith`."
        ]
      }
    }
  ],
  "relatedLessons": [
    "threadpool-internals-and-thread-starvation",
    "async-io-vs-cpu-bound-work",
    "diagnosing-async-performance-problems"
  ],
  "furtherReading": [
    { "title": "TaskScheduler Class (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/api/system.threading.tasks.taskscheduler" },
    { "title": "Chaining tasks by using continuations (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/standard-async/continuation-logic" }
  ]
}