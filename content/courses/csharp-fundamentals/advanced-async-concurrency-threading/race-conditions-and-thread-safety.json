{
  "id": "lesson-csharp-fundamentals-advanced-async-concurrency-threading-race-conditions-and-thread-safety",
  "slug": "race-conditions-and-thread-safety",
  "moduleSlug": "advanced-async-concurrency-threading",
  "courseSlug": "csharp-fundamentals",
  "title": "Race conditions and thread safety",
  "description": "Identify race conditions, understand memory ordering, and apply thread-safe patterns in C#.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When multiple threads access shared data without coordination, the result depends on the exact timing of each thread. This is a *race condition* — and it is one of the most common sources of elusive production bugs."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What a race condition looks like"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Imagine two threads incrementing the same counter. The increment operation is not atomic: it reads the value, adds one, and writes back. If both threads read the same value before either writes back, one increment is silently lost."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Race condition on a shared counter",
        "language": "csharp",
        "code": "private int _counter = 0;\n\npublic void Increment()\n{\n    int current = _counter; // Thread A reads 5\n    current++;               // Thread B also reads 5\n    _counter = current;      // Thread A writes 6\n                            // Thread B writes 6 (A's increment lost)\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Memory ordering and visibility"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Modern CPUs and compilers reorder instructions for performance. Without synchronization, one thread may not see changes made by another thread in time. The `volatile` keyword and `Thread.MemoryBarrier()` provide limited ordering guarantees, but they do not make compound operations atomic."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Synchronization tool", "Use case", "Atomicity guarantee"],
        "rows": [
          ["`lock`", "Protect a code block or object", "Yes — mutual exclusion"],
          ["`Interlocked`", "Single read-modify-write on a variable", "Yes — atomic operation"],
          ["`volatile`", "Prevent certain reordering", "No — does not make compound ops atomic"],
          ["`Thread.MemoryBarrier()`", "Low-level fence", "Yes — ordering only"]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Thread-safe increment with Interlocked",
        "language": "csharp",
        "code": "private int _counter = 0;\n\npublic void Increment()\n{\n    // Atomically reads, increments, and writes in one CPU instruction\n    Interlocked.Increment(ref _counter);\n}\n\npublic int GetAndAdd(int delta)\n{\n    // Atomically returns current value and adds delta\n    return Interlocked.Add(ref _counter, delta);\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "volatile is not a lock",
        "text": "`volatile` prevents certain compiler and CPU reordering, but it does not make `counter++` safe. For compound operations like read-modify-write, use `lock` or `Interlocked`."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Race conditions happen when unsynchronized threads share mutable state.",
          "`Interlocked` makes simple operations atomic without a lock.",
          "`volatile` controls visibility but does not make operations atomic.",
          "Use `lock` when multiple operations must happen together as a unit."
        ]
      }
    }
  ],
  "relatedLessons": [
    "lock-and-monitor",
    "concurrent-collections",
    "deadlocks-starvation-and-livelocks"
  ],
  "furtherReading": [
    { "title": "Threading (C# Programming Guide)", "url": "https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/concepts/threading/" },
    { "title": "Interlocked Class (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/api/system.threading.interlocked" }
  ]
}