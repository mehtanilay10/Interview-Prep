{
  "id": "lesson-csharp-fundamentals-advanced-async-concurrency-threading-concurrent-collections",
  "slug": "concurrent-collections",
  "moduleSlug": "advanced-async-concurrency-threading",
  "courseSlug": "csharp-fundamentals",
  "title": "Concurrent collections",
  "description": "Choose the right concurrent collection for lock-free or low-contention scenarios in multithreaded code.",
  "order": 8,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Standard collections like `List<T>` and `Dictionary<TKey, TValue>` are not thread-safe. Adding or removing items while another thread reads or writes can corrupt data or throw exceptions. The `System.Collections.Concurrent` namespace provides collections designed for concurrent access."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common concurrent collections"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Each concurrent collection is optimized for a specific access pattern. Choosing the wrong one can lead to unnecessary contention or poor performance."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Collection", "Best for", "Notes"],
        "rows": [
          ["`ConcurrentDictionary<TKey, TValue>`", "Key-value lookups and updates", "Lock-free reads; fine-grained locking for writes"],
          ["`ConcurrentQueue<T>`", "First-in-first-out producer-consumer", "Fully lock-free for most operations"],
          ["`ConcurrentStack<T>`", "Last-in-first-out work items", "Fully lock-free for most operations"],
          ["`ConcurrentBag<T>`", "Unordered work items", "Fast for adds/removes; iteration is a snapshot"],
          ["`BlockingCollection<T>`", "Bounded producer-consumer with blocking", "Wraps any `IProducerConsumerCollection`"]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Thread-safe cache with ConcurrentDictionary",
        "language": "csharp",
        "code": "private readonly ConcurrentDictionary<string, User> _cache = new();\n\npublic User GetOrAddUser(string userId)\n{\n    return _cache.GetOrAdd(userId, id =>\n    {\n        // Factory runs only if key is missing\n        return LoadUserFromDatabase(id);\n    });\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Producer-consumer with BlockingCollection",
        "language": "csharp",
        "code": "private readonly BlockingCollection<string> _queue = new();\n\npublic void StartProcessing(CancellationToken ct)\n{\n    Task.Run(() =>\n    {\n        foreach (var item in _queue.GetConsumingEnumerable(ct))\n        {\n            ProcessItem(item);\n        }\n    }, ct);\n}\n\npublic void EnqueueWork(string workItem)\n{\n    _queue.Add(workItem);\n}\n\npublic void Complete()\n{\n    _queue.CompleteAdding();\n}"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Do not iterate a `ConcurrentBag` expecting a consistent snapshot — the collection may change during enumeration.",
          "`GetConsumingEnumerable` blocks when the collection is empty and ends when `CompleteAdding` is called.",
          "Concurrent collections reduce lock contention but do not eliminate the need for correct design."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer concurrent collections over manual locking",
        "text": "If a standard concurrent collection fits your scenario, use it. Hand-rolled locking is error-prone and often slower than well-tested framework code."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Use `ConcurrentDictionary` for key-value caches and lookups.",
          "Use `BlockingCollection` for bounded producer-consumer pipelines.",
          "Concurrent collections reduce but do not eliminate synchronization concerns."
        ]
      }
    }
  ],
  "relatedLessons": [
    "race-conditions-and-thread-safety",
    "lock-and-monitor",
    "channels-and-producer-consumer-pipelines"
  ],
  "furtherReading": [
    { "title": "Concurrent Collections (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/standard/collections/thread-safe/" },
    { "title": "BlockingCollection<T> Class (Microsoft Docs)", "url": "https://learn.microsoft.com/en-us/dotnet/api/system.collections.concurrent.blockingcollection-1" }
  ]
}