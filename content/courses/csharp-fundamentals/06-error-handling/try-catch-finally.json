{
  "id": "lesson-csharp-022",
  "slug": "try-catch-finally",
  "moduleSlug": "error-handling",
  "title": "Try/Catch/Finally",
  "description": "Master C#'s core exception-handling constructs — try, catch, finally, and using — and understand the precise execution flow when exceptions occur.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 32,
  "tags": [
    "csharp",
    "try",
    "catch",
    "finally",
    "using-statement",
    "exception-handling"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "C# provides structured exception handling through the `try`, `catch`, and `finally` blocks. Together, they let you write code that handles errors gracefully, performs mandatory cleanup, and keeps your application running when things go wrong."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Three Blocks Explained"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **try** block wraps code that might throw an exception. One or more **catch** blocks follow it, each handling a specific type of error. An optional **finally** block runs regardless of whether an exception occurred — it is the place for cleanup code that must always execute, such as closing a file or releasing a database connection."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "try-catch-finally-flow",
        "caption": "Execution flow through try, catch, and finally blocks",
        "definition": "flowchart TD\n    Start([Start]) --> Try[Try block executes]\n    Try --> Success{Did exception occur?}\n    Success -->|No| Finally[Finally block runs]\n    Success -->|Yes| Catch{Catch block matches?}\n    Catch -->|Yes| Handle[Caught block runs]\n    Catch -->|No| Propagate[Exception propagates up stack]\n    Handle --> Finally\n    Finally --> End([End])\n    Propagate --> End"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Try/Catch"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The simplest form wraps risky code in a `try` block and catches any exception with a general `catch (Exception)` block. This prevents crashes, but it is imprecise — you won't know which specific error occurred unless you inspect the exception's `Message` property."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Try/Catch Structure",
        "content": "The fundamental pattern: wrap risky code in try, handle errors in catch.",
        "language": "csharp",
        "code": "using System;\n\nclass BasicTryCatch\n{\n    static void Main()\n    {\n        try\n        {\n            Console.Write(\"Enter a number: \");\n            string input = Console.ReadLine();\n            int number = int.Parse(input);\n            Console.WriteLine($\"You entered: {number}\");\n        }\n        catch (Exception ex)\n        {\n            Console.WriteLine($\"Error: {ex.Message}\");\n            Console.WriteLine(\"Please try again and enter a valid number.\");\n        }\n\n        Console.WriteLine(\"Program continues running...\");\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Catching Specific Exception Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Multiple `catch` blocks can follow a single `try`. The runtime evaluates them top to bottom, executing the first one whose exception type matches. Because `Exception` is the base of all exceptions, it must come last — otherwise it would catch every exception before the more specific handlers get a chance."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple Catch Blocks",
        "content": "Handling different exceptions with specific catch blocks and a general fallback.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass MultipleCatches\n{\n    static void ProcessFile(string path)\n    {\n        try\n        {\n            string content = File.ReadAllText(path);\n            int number = int.Parse(content);\n            int result = 100 / number;\n            Console.WriteLine($\"Result: {result}\");\n        }\n        catch (FileNotFoundException ex)\n        {\n            Console.WriteLine($\"File not found: {ex.FileName}\");\n        }\n        catch (DirectoryNotFoundException ex)\n        {\n            Console.WriteLine($\"Directory not found: {ex.Message}\");\n        }\n        catch (FormatException ex)\n        {\n            Console.WriteLine(\"File content is not a valid number\");\n        }\n        catch (DivideByZeroException ex)\n        {\n            Console.WriteLine(\"Cannot divide by zero\");\n        }\n        catch (IOException ex)\n        {\n            Console.WriteLine($\"I/O error: {ex.Message}\");\n        }\n        catch (Exception ex)\n        {\n            Console.WriteLine($\"Unexpected error: {ex.Message}\");\n        }\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Order Matters",
        "text": "Always place the most specific catch blocks first and the most general (`Exception`) last. C# requires more specific types to appear before their base types. Reversing the order causes a compile error."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accessing the Exception Object"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Each `catch` block can optionally name a variable that holds the thrown exception. You can use this variable to read the `Message`, `StackTrace`, or any custom properties the exception provides. Naming it (e.g., `ex` or `exception`) is a convention that improves readability."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using the Exception Variable",
        "content": "Reading exception properties to provide useful feedback and log diagnostic details.",
        "language": "csharp",
        "code": "using System;\n\nclass ExceptionVariableDemo\n{\n    static void ParseAge(string input)\n    {\n        try\n        {\n            int age = int.Parse(input);\n            Console.WriteLine($\"Age: {age}\");\n        }\n        catch (FormatException ex)\n        {\n            // ex is the actual exception object\n            Console.WriteLine($\"Invalid format: '{ex.Message}'\");\n            Console.WriteLine($\"Param name in message: '{ex.ParamName}'\");\n        }\n        catch (OverflowException ex)\n        {\n            Console.WriteLine($\"Number too large: {ex.Message}\");\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Finally Block"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `finally` block runs whether or not an exception occurred. It is the right place for cleanup code that must execute no matter what — closing database connections, releasing file handles, resetting application state, or disposing resources."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "Finally Block Guarantees",
        "items": [
          "Runs after the try block if no exception was thrown",
          "Runs after the catch block if an exception was caught",
          "Runs even if the try or catch contains a return statement",
          "Runs even if you call Environment.Exit (in most cases)"
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Finally for Resource Cleanup",
        "content": "Using finally to ensure a StreamReader is always closed, even if reading fails.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass FinallyDemo\n{\n    static void ReadFirstLine(string path)\n    {\n        StreamReader? reader = null;\n\n        try\n        {\n            reader = new StreamReader(path);\n            string? line = reader.ReadLine();\n            Console.WriteLine($\"First line: {line}\");\n        }\n        catch (FileNotFoundException ex)\n        {\n            Console.WriteLine($\"File not found: {ex.FileName}\");\n        }\n        catch (IOException ex)\n        {\n            Console.WriteLine($\"Read error: {ex.Message}\");\n        }\n        finally\n        {\n            // This ALWAYS runs — even if an exception occurred\n            reader?.Dispose();\n            Console.WriteLine(\"Reader closed in finally block\");\n        }\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer the using Statement",
        "text": "While finally works, C# provides a cleaner alternative. The `using` statement (or `using var` declaration) automatically calls `Dispose()` at the end of the scope. It is equivalent to try/finally with manual disposal, but more readable and less error-prone."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using Statement — Automatic Disposal",
        "content": "The using statement automatically disposes resources, making try/finally unnecessary for disposable objects.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass UsingStatementDemo\n{\n    static void ReadFirstLineClean(string path)\n    {\n        // 'using' ensures Dispose() is called automatically\n        try\n        {\n            using var reader = new StreamReader(path);\n            string? line = reader.ReadLine();\n            Console.WriteLine($\"First line: {line}\");\n        }\n        catch (FileNotFoundException ex)\n        {\n            Console.WriteLine($\"File not found: {ex.FileName}\");\n        }\n    }\n\n    static void ProcessMultipleFiles(string[] paths)\n    {\n        // Classic using statement with braces\n        foreach (string path in paths)\n        {\n            using (var reader = new StreamReader(path))\n            {\n                string content = reader.ReadToEnd();\n                Console.WriteLine($\"{path}: {content.Length} chars\");\n            }\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Exception Filters (C# 6+)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# 6 introduced **exception filters** — a condition added after the `catch` type using the `when` keyword. The catch block only executes if the condition is true. When the condition is false, the runtime continues checking the next catch block. Unlike putting logic inside the catch body, filters don't consume the exception — the stack trace remains intact."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Exception Filters with 'when'",
        "content": "Using exception filters to catch exceptions conditionally without modifying the stack trace.",
        "language": "csharp",
        "code": "using System;\n\nclass ExceptionFilters\n{\n    static void Divide(int a, int b)\n    {\n        try\n        {\n            int result = a / b;\n            Console.WriteLine($\"{a} / {b} = {result}\");\n        }\n        catch (DivideByZeroException ex) when (b == 0)\n        {\n            Console.WriteLine(\"The divisor was zero — caught by filter\");\n        }\n        catch (DivideByZeroException)\n        {\n            Console.WriteLine(\"Generic divide-by-zero handler\");\n        }\n    }\n\n    static void LogErrors(Exception ex)\n    {\n        if (ex is IOException)\n        {\n            Console.WriteLine(\"File or stream error occurred\");\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Rethrowing Exceptions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Sometimes you catch an exception to log it or perform partial cleanup, but you still want it to propagate up the stack. You have two options: `throw;` (preserves the original stack trace) and `throw ex;` (resets the stack trace to the current location). Always use `throw;` to preserve debugging information."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "throw; vs throw ex;",
        "cards": [
          {
            "title": "throw;",
            "description": "Rethrows the original exception, preserving its stack trace",
            "pros": [
              "Original stack trace is preserved",
              "You can find where the error originally occurred",
              "Recommended for rethrowing"
            ],
            "cons": [
              "Cannot add new information to the exception"
            ],
            "tags": ["preferred"]
          },
          {
            "title": "throw ex;",
            "description": "Rethrows the exception but resets the stack trace to this location",
            "pros": [
              "Can assign a new exception object"
            ],
            "cons": [
              "Original stack trace is lost",
              "Harder to debug — origin point becomes the rethrow line",
              "Generally not recommended"
            ],
            "tags": ["avoid"]
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Rethrowing with Stack Trace Preservation",
        "content": "Logging an exception and rethrowing it correctly to preserve the original stack trace.",
        "language": "csharp",
        "code": "using System;\n\nclass RethrowDemo\n{\n    // CORRECT: preserves original stack trace\n    static void CorrectRethrow(string path)\n    {\n        try\n        {\n            ProcessFile(path);\n        }\n        catch (Exception ex)\n        {\n            Console.WriteLine($\"Logging error: {ex.Message}\");\n            throw; // Re-throws with ORIGINAL stack trace intact\n        }\n    }\n\n    // WRONG: resets stack trace\n    static void WrongRethrow(string path)\n    {\n        try\n        {\n            ProcessFile(path);\n        }\n        catch (Exception ex)\n        {\n            Console.WriteLine($\"Logging error: {ex.Message}\");\n            throw ex; // Stack trace now points HERE — bad for debugging!\n        }\n    }\n\n    static void ProcessFile(string path)\n    {\n        // This is where the error actually happens\n        throw new FileNotFoundException(path);\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When NOT to Use try/catch"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Anti-patterns to Avoid",
        "items": [
          "Catching Exception just to swallow it silently — always log or rethrow",
          "Using exceptions for normal control flow (e.g., checking if a user exists)",
          "Catching and doing nothing — this hides bugs",
          "Putting the entire method body inside a try block — be specific about what might fail",
          "Rethrowing with `throw ex;` — always use `throw;` instead"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Try Block",
            "definition": "A block of code that is monitored for exceptions. If an exception occurs inside it, control is passed to a matching catch block."
          },
          {
            "term": "Catch Block",
            "definition": "A block that handles exceptions of a specific type. Multiple catch blocks can follow one try block."
          },
          {
            "term": "Finally Block",
            "definition": "A block that always executes after try (and catch), regardless of whether an exception occurred. Used for mandatory cleanup."
          },
          {
            "term": "Using Statement",
            "definition": "A C# construct that ensures a disposable object's Dispose() method is called when the scope ends. Syntactic sugar for try/finally."
          },
          {
            "term": "Exception Filter",
            "definition": "A `when` condition on a catch block that must evaluate to true for that block to execute. Does not consume the exception."
          },
          {
            "term": "Rethrow",
            "definition": "Re-emitting a caught exception so it propagates further up the call stack. Use `throw;` to preserve the stack trace."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Practice: Build a Robust File Reader",
        "description": "Write a method that reads a text file and returns its contents. Handle FileNotFoundException, DirectoryNotFoundException, UnauthorizedAccessException, and general IOException with appropriate user messages. Use a using statement for the file handle.",
        "steps": [
          "Create a method ReadFile(string path) that returns string",
          "Wrap the file reading logic in a try block",
          "Add specific catch blocks for each exception type listed above",
          "Use a using statement for the StreamReader",
          "In each catch block, print a clear message to the user explaining what went wrong",
          "Test with a valid file, a missing file, and an unreadable file"
        ],
        "expectedOutcome": "You will write production-quality error-handling code with specific catch blocks and proper resource cleanup, and understand when each exception type is triggered."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Try/Catch/Finally Checklist",
        "items": [
          {
            "text": "I understand what each block (try, catch, finally) does",
            "hint": "try = risky code, catch = error handling, finally = cleanup"
          },
          {
            "text": "I can write multiple catch blocks for different exception types",
            "hint": "Specific types first, Exception last"
          },
          {
            "text": "I know what the finally block guarantees",
            "hint": "It runs whether or not an exception occurs, even after return"
          },
          {
            "text": "I can use the using statement instead of manual try/finally",
            "hint": "using var reader = new StreamReader(path);"
          },
          {
            "text": "I know the difference between throw; and throw ex;",
            "hint": "throw; preserves stack trace; throw ex; does not"
          },
          {
            "text": "I can use exception filters with the when keyword",
            "hint": "catch (IOException ex) when (ex.HResult == -2147024894)"
          },
          {
            "text": "I avoid swallowing exceptions silently",
            "hint": "Always log or rethrow — never catch and do nothing"
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I use try/catch without catch?",
            "answer": "No, not directly. If you only want a finally block, you can use try/finally without catch. For try/catch you must have at least one catch block."
          },
          {
            "question": "What happens if I have a return inside the try block?",
            "answer": "The finally block still runs. The return value is held, finally executes, and then the method returns. This is why finally is the right place for cleanup — it runs even if you return early."
          },
          {
            "question": "Should I catch all exceptions with one generic catch block?",
            "answer": "No. Catch specific exception types that you can actually handle. Use a generic `catch (Exception ex)` only as a last-resort safety net near the top of your stack."
          },
          {
            "question": "Does using replace try/catch entirely?",
            "answer": "No. using only replaces try/finally for disposing resources. You still need try/catch to handle errors. using is about cleanup, not error handling."
          },
          {
            "question": "What is the performance cost of try/catch?",
            "answer": "There is essentially no performance cost when no exception occurs. The cost comes when an exception is thrown and the stack is unwound, which is already an expensive operation by nature."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Lesson Summary",
        "points": [
          "The try block wraps code that might throw; catch handles the exception; finally always runs for cleanup",
          "Multiple catch blocks can handle different exception types — place the most specific first",
          "The exception object (commonly named 'ex') gives you Message, StackTrace, and more",
          "Finally is guaranteed to run — even after a return statement or an unhandled exception",
          "The using statement is syntactic sugar for try/finally and should be preferred for disposable resources",
          "Exception filters (when keyword) let catch blocks fire conditionally without consuming the exception",
          "Use throw; to rethrow — it preserves the original stack trace for debugging"
        ],
        "takeaway": "Mastering try/catch/finally is essential for building C# applications that are resilient, debuggable, and safe to run in production."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
