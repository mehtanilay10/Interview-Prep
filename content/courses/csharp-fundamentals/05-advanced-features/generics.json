{
  "id": "lesson-csharp-017",
  "slug": "generics",
  "moduleSlug": "advanced-features",
  "title": "Generics",
  "description": "Learn how to write flexible, type-safe, reusable code with C# generics.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 26,
  "tags": [
    "csharp",
    "generics",
    "type-parameters",
    "constraints",
    "reusable"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Generics allow you to write classes, methods, and interfaces that work with any type while preserving full compile-time type safety. Instead of boxing value types into object or casting back from object, generics let the compiler enforce the correct type from the start."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Problem Generics Solve"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Before generics were introduced in C# 2.0, developers used the non-generic System.Collections namespace. An ArrayList could store any object, which meant the compiler could not help you catch mistakes. Worse, value types like int and double were boxed into object on every add and unboxed on every read — adding both type-safety risks and significant performance overhead."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Non-generic ArrayList — the old way",
        "content": "Demonstrates boxing and the risk of runtime cast errors",
        "language": "csharp",
        "code": "using System;\nusing System.Collections;  // Legacy non-generic namespace\n\npublic class NonGenericDemo\n{\n    public static void Main()\n    {\n        ArrayList items = new ArrayList();\n        items.Add(1);          // int boxed to object\n        items.Add(\"Hello\");   // string (already a reference type)\n        items.Add(2.5);        // double boxed to object\n\n        // Must cast back — and risk InvalidCastException\n        foreach (object item in items)\n        {\n            // int i = item; // Compile error — cannot convert object to int\n            int i = (int)item; // Throws at runtime for \"Hello\" and 2.5\n            Console.WriteLine(i);\n        }\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "No More Casting",
        "text": "With generics, the compiler guarantees type correctness at compile time. A List<int> only accepts int — no boxing, no casting, no surprises at runtime."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generic Methods"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A generic method declares its type parameters on the method itself, between angle brackets. The caller specifies the actual type when invoking the method, or the compiler can infer it from the arguments passed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generic Method — returning the default of any type",
        "content": "Shows explicit type specification and compiler inference",
        "language": "csharp",
        "code": "using System;\n\npublic class GenericMethodDemo\n{\n    // T is the type parameter — it stands in for any type\n    static T GetDefault<T>()\n    {\n        // default(T) returns the zero-initialized value for any type:\n        // null for reference types, 0 for numeric types, false for bool\n        return default(T);\n    }\n\n    public static void Main()\n    {\n        // Explicit type argument\n        int defaultInt = GetDefault<int>();    // 0\n        string? defaultStr = GetDefault<string>(); // null\n        bool defaultBool = GetDefault<bool>(); // false\n\n        // Compiler can infer the type from the context\n        double inferred = GetDefault<double>(); // 0.0\n\n        // Generic method with multiple type parameters\n        static void Swap<T>(ref T a, ref T b)\n        {\n            T temp = a;\n            a = b;\n            b = temp;\n        }\n\n        int x = 10, y = 20;\n        Swap(ref x, ref y);\n        Console.WriteLine($\"x={x}, y={y}\"); // x=20, y=10\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generic Classes"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A generic class declares its type parameter at the class level, making the entire class type-safe for whatever type T is supplied. The most common real-world example is the generic Repository<T> pattern used in data access layers."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generic Repository Pattern",
        "content": "A reusable data store that works with any entity type",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\nusing System.Linq;\n\n// T is the type of entity this repository manages\npublic class Repository<T> where T : class\n{\n    private List<T> _items = new List<T>();\n\n    public void Add(T item)\n    {\n        if (item == null) throw new ArgumentNullException(nameof(item));\n        _items.Add(item);\n    }\n\n    public T? GetById(int id)\n    {\n        // Simple index-based retrieval for demo purposes\n        return id >= 0 && id < _items.Count ? _items[id] : null;\n    }\n\n    public List<T> GetAll()\n    {\n        // Return a copy to prevent external mutation\n        return new List<T>(_items);\n    }\n\n    public bool Remove(T item)\n    {\n        return _items.Remove(item);\n    }\n\n    public int Count => _items.Count;\n}\n\n// Domain models\npublic class User\n{\n    public int Id { get; set; }\n    public string Name { get; set; } = string.Empty;\n}\n\npublic class Product\n{\n    public int Id { get; set; }\n    public string Name { get; set; } = string.Empty;\n    public decimal Price { get; set; }\n}\n\npublic class RepositoryDemo\n{\n    public static void Main()\n    {\n        // Same Repository class, two different types\n        Repository<User> userRepo = new Repository<User>();\n        userRepo.Add(new User { Id = 0, Name = \"Alice\" });\n        userRepo.Add(new User { Id = 1, Name = \"Bob\" });\n\n        Repository<Product> productRepo = new Repository<Product>();\n        productRepo.Add(new Product { Id = 0, Name = \"Laptop\", Price = 999 });\n\n        User? user = userRepo.GetById(0);\n        Console.WriteLine(user?.Name); // Alice\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generic Interfaces"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Generic interfaces let you define contracts that work with any type. The built-in IEnumerable<T> is the most important generic interface in C# — it enables foreach iteration and powers LINQ. Defining your own generic interface lets multiple types share the same contract."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generic Interface — IRepository<T>",
        "content": "Shows an interface contract consumed by multiple implementations",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic interface IRepository<T>\n{\n    void Add(T item);\n    T? GetById(int id);\n    List<T> GetAll();\n    bool Remove(T item);\n}\n\n// In-memory implementation\npublic class InMemoryRepository<T> : IRepository<T> where T : class\n{\n    private List<T> _items = new List<T>();\n    public void Add(T item) => _items.Add(item ?? throw new ArgumentNullException());\n    public T? GetById(int id) => id >= 0 && id < _items.Count ? _items[id] : null;\n    public List<T> GetAll() => new List<T>(_items);\n    public bool Remove(T item) => _items.Remove(item);\n}\n\n// Usage — same interface, different types\nIRepository<User> users = new InMemoryRepository<User>();\nIRepository<Product> products = new InMemoryRepository<Product>();\n"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Constraints — Restricting Allowed Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, a type parameter T can be replaced with any type. The where keyword lets you restrict T to types that meet certain criteria, such as being a class, a struct, having a parameterless constructor, or implementing a specific interface. Constraints unlock methods that would otherwise be unavailable on an unknown type."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Constraint",
          "Meaning",
          "What it Unlocks"
        ],
        "rows": [
          [
            "where T : class",
            "T must be a reference type",
            "Can check for null, use reference-type equality"
          ],
          [
            "where T : struct",
            "T must be a value (non-nullable) type",
            "Guarantees no null; can use default without nullability warning"
          ],
          [
            "where T : new()",
            "T must have a public parameterless constructor",
            "Can write new T() inside the generic class/method"
          ],
          [
            "where T : BaseClass",
            "T must inherit from BaseClass",
            "Can use BaseClass methods and properties on T"
          ],
          [
            "where T : IComparable<T>",
            "T must implement IComparable<T>",
            "Can call CompareTo on instances of T"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple Constraints Applied",
        "content": "Demonstrates class + new() + interface constraints together",
        "language": "csharp",
        "code": "using System;\n\n// Constraints: class (reference type), new() (parameterless ctor),\n// IComparable<T> (can be sorted/comparison operated)\npublic class SortedCache<T> where T : class, IComparable<T>, new()\n{\n    private List<T> _items = new List<T>();\n\n    public void Add(T item)\n    {\n        if (item == null) throw new ArgumentNullException();\n        _items.Add(item);\n        _items.Sort(); // IComparable<T>.CompareTo is now available\n    }\n\n    public T CreateDefault()\n    {\n        return new T(); // new() constraint makes this legal\n    }\n}\n\npublic class Employee : IComparable<Employee>\n{\n    public string Name { get; set; } = string.Empty;\n    public int Salary { get; set; }\n\n    public int CompareTo(Employee? other)\n    {\n        if (other == null) return 1;\n        return Salary.CompareTo(other.Salary);\n    }\n}\n\npublic class ConstraintDemo\n{\n    public static void Main()\n    {\n        var cache = new SortedCache<Employee>();\n        cache.Add(new Employee { Name = \"Alice\", Salary = 90000 });\n        cache.Add(new Employee { Name = \"Bob\", Salary = 75000 });\n\n        Employee defaultEmp = cache.CreateDefault();\n        Console.WriteLine($\"Default: {defaultEmp.Name}\"); // empty string\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Variance — Covariance and Contravariance"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# supports variance on interface and delegate type parameters using the out (covariant) and in (contravariant) keywords. Covariance allows a more derived type to be used where a base type is expected (output positions). Contravariance allows a less derived type (base) to be used where a more derived type is expected (input positions)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Covariance with IEnumerable<out T>",
        "content": "Assigning a derived-type sequence to a base-type sequence variable",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic class Animal { public string Name { get; set; } = string.Empty; }\npublic class Dog : Animal { public void Bark() => Console.WriteLine(\"Woof!\"); }\npublic class Cat : Animal { public void Meow() => Console.WriteLine(\"Meow!\"); }\n\npublic class VarianceDemo\n{\n    public static void Main()\n    {\n        List<Dog> dogs = new List<Dog>\n        {\n            new Dog { Name = \"Rex\" },\n            new Dog { Name = \"Buddy\" }\n        };\n\n        // IEnumerable<out T> is covariant — List<Dog> can be treated as IEnumerable<Animal>\n        IEnumerable<Animal> animals = dogs;\n        foreach (Animal a in animals)\n        {\n            Console.WriteLine(a.Name); // Rex, Buddy\n        }\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Real-World Generics in .NET",
        "text": "Generics are everywhere in .NET: List<T>, Dictionary<TKey, TValue>, Task<T>, IEnumerable<T>, IQueryable<T>, Nullable<T>, and your own Repository<T> pattern all rely on them."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Generic Pair<T>",
        "description": "Create a reusable generic class that stores two values of the same type and a method to swap them.",
        "steps": [
          "Define a class Pair<T> with two fields: First and Second.",
          "Write a constructor that accepts two T values.",
          "Add a method Swap() that exchanges First and Second using a temporary variable.",
          "Write a method BothEqual() that returns true if both values are equal (use EqualityComparer<T>.Default.Equals).",
          "Test your Pair<T> with int, string, and a custom class."
        ],
        "expectedOutcome": "A fully functional generic Pair<T> class that demonstrates type parameter usage, EqualityComparer<T>.Default, and ref-swap logic."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Generics Checklist",
        "items": [
          { "text": "I can explain why generics are better than using object", "hint": "Think about boxing, casting, and compile-time safety." },
          { "text": "I can write a generic method with type parameter T", "hint": "static T Method<T>(T input) — angle brackets on the method." },
          { "text": "I can write a generic class", "hint": "class Box<T> with T used for fields and methods." },
          { "text": "I understand and can apply at least one constraint", "hint": "where T : class, new(), struct, or an interface." },
          { "text": "I know where to find generics in the .NET framework", "hint": "List<T>, Dictionary<TKey, TValue>, Task<T>, IEnumerable<T>." }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can generics have more than one type parameter?",
            "answer": "Yes — Dictionary<TKey, TValue> is the most common example with two parameters. You can have as many as needed: Tuple<T1, T2, T3, T4, T5, T6, T7, TRest>."
          },
          {
            "question": "Are generics slower than non-generic code?",
            "answer": "No — the JIT compiler generates a specialized native version for each type argument at runtime. Value types avoid boxing entirely, so generics are often significantly faster than non-generic alternatives."
          },
          {
            "question": "What is the difference between where T : class and where T : class?",
            "answer": "where T : class means T must be a reference type (string, classes, interfaces). In a nullable-enabled context, it also means T may be null. Use where T : notnull for types that must never be null."
          },
          {
            "question": "Can I create a generic static class?",
            "answer": "Yes — a static generic class creates a separate static field instance for each closed type. This is useful for per-type caches or counters: static class Counter<T> { public static int Count; }"
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Generics provide compile-time type safety without boxing",
          "Type parameters use the <T> syntax",
          "Constraints (where T :) restrict which types can be used",
          "Common constraints: class, struct, new(), base class, interface",
          "Generics power List<T>, Dictionary<TKey, TValue>, Task<T>, and LINQ",
          "Variance (out/in) allows safe subtype assignment in interface types",
          "Generic interfaces enable flexible, testable code"
        ],
        "takeaway": "Generics are the foundation of type-safe, reusable code in C#. Once you master them, you will see them everywhere in the .NET ecosystem."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
