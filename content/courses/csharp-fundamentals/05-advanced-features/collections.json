{
  "id": "lesson-csharp-016",
  "slug": "collections",
  "moduleSlug": "advanced-features",
  "title": "Collections",
  "description": "Explore C# collection types — List, Dictionary, HashSet, Queue, and Stack — and learn when to use each one.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "collections",
    "list",
    "dictionary",
    "hashset",
    "queue",
    "stack",
    "generic"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Collections are specialized classes for storing and managing groups of related objects. Unlike plain arrays, most collections in C# are dynamic — they grow and shrink automatically as you add or remove items. They live in the System.Collections.Generic namespace and are the backbone of almost every C# application."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Use Collections Instead of Arrays?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Arrays have a fixed size set when they are created. If you need more slots later, you must create a brand-new array and copy everything over — a process that is both slow and error-prone. Collections solve this with internal resizing logic that handles growth transparently. They also provide rich methods for searching, sorting, and manipulating data, making your code far more expressive than manual array manipulation."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Always Prefer Generic Collections",
        "text": "Always use generic collections (List<T>, Dictionary<TKey, TValue>) from System.Collections.Generic. Non-generic collections like ArrayList and Hashtable require boxing for value types, lose compile-time type safety, and are considered legacy."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "List<T> — Dynamic Array"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "List<T> is the most commonly used collection in C#. It stores elements of a single type in a contiguous, zero-indexed sequence. Internally it uses a resizable array — when the array fills up, it allocates a larger one and copies items over, a process that happens infrequently enough to be efficient for most workloads."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating and Populating a List",
        "content": "Demonstrates initialization, adding, accessing, and removing items",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic class ListDemo\n{\n    public static void Main()\n    {\n        // Empty list\n        List<string> names = new List<string>();\n\n        // Add items one at a time\n        names.Add(\"Alice\");\n        names.Add(\"Bob\");\n        names.Add(\"Charlie\");\n\n        // Initialize with values\n        List<int> scores = new List<int> { 95, 87, 72, 91 };\n\n        // Index access — zero-based, same as arrays\n        string first = names[0];        // \"Alice\"\n        names[1] = \"Barbara\";          // Replaces \"Bob\"\n\n        // Count property\n        int totalNames = names.Count;    // 3\n\n        // Contains — checks for presence\n        bool hasAlice = names.Contains(\"Alice\"); // true\n\n        // Remove by value (returns true if found and removed)\n        bool removed = names.Remove(\"Barbara\");  // true\n\n        // RemoveAt — remove by index\n        names.RemoveAt(0);              // Removes \"Alice\"\n\n        // Insert — add at a specific position\n        names.Insert(1, \"David\");\n\n        // Iterate with foreach\n        foreach (string name in names)\n        {\n            Console.WriteLine(name);\n        }\n\n        // Convert to array\n        string[] nameArray = names.ToArray();\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Dictionary<TKey, TValue> — Key-Value Pairs"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Dictionary<TKey, TValue> stores data as key-value pairs, giving you lightning-fast lookups by key. Under the hood it uses a hash table, which means average-case lookup, insertion, and removal are all O(1). The only constraint is that keys must be unique — adding a duplicate key will throw an exception."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Working with a Dictionary",
        "content": "Shows adding, accessing, checking, and iterating key-value pairs",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic class DictionaryDemo\n{\n    public static void Main()\n    {\n        // Mapping student IDs to names\n        Dictionary<int, string> students = new Dictionary<int, string>\n        {\n            { 101, \"Alice\" },\n            { 102, \"Bob\" },\n            { 103, \"Charlie\" }\n        };\n\n        // Indexer — throws KeyNotFoundException if key missing\n        string name = students[102];   // \"Bob\"\n\n        // Safe access with TryGetValue — preferred in production\n        if (students.TryGetValue(104, out string? result))\n        {\n            Console.WriteLine(result);\n        }\n        else\n        {\n            Console.WriteLine(\"Student 104 not found.\");\n        }\n\n        // ContainsKey — check before accessing by indexer\n        if (students.ContainsKey(101))\n        {\n            Console.WriteLine(students[101]); // \"Alice\"\n        }\n\n        // Add or update\n        students[104] = \"Diana\";      // Add (or overwrite)\n        students.Add(105, \"Eve\");      // Add only — throws if key exists\n\n        // Remove by key\n        students.Remove(103);            // Removes Charlie\n\n        // Iterate over all pairs\n        foreach (KeyValuePair<int, string> entry in students)\n        {\n            Console.WriteLine($\"ID: {entry.Key}, Name: {entry.Value}\");\n        }\n\n        // Get just the keys or values\n        List<int> allIds = new List<int>(students.Keys);\n        List<string> allNames = new List<string>(students.Values);\n\n        Console.WriteLine($\"Total students: {students.Count}\");\n    }\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-dict-lookup",
        "caption": "How Dictionary key lookup works via hashing",
        "definition": "flowchart LR\n    A[\"Input Key: 102\"] --> B[\"Compute Hash\"]\n    B --> C[\"Find Bucket\"]\n    C --> D{\"Key Exists?\"}\n    D -->|Yes| E[\"Return Value\"]\n    D -->|No| F[\"Throw / Return Default\"]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "HashSet<T> — Unique Items"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "HashSet<T> is a collection of unique elements with no defined order. It is backed by a hash table just like Dictionary, so Add, Remove, and Contains all run in O(1) on average. Use it when you need to eliminate duplicates or test membership quickly, such as tracking which users have already been processed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "HashSet for Deduplication",
        "content": "Removes duplicates and performs fast membership checks",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic class HashSetDemo\n{\n    public static void Main()\n    {\n        HashSet<string> uniqueTags = new HashSet<string>();\n\n        // Add returns false if the item already exists\n        uniqueTags.Add(\"csharp\");   // true — added\n        uniqueTags.Add(\"dotnet\");   // true — added\n        uniqueTags.Add(\"csharp\");   // false — duplicate ignored\n\n        // Contains — O(1) membership check\n        bool isTagged = uniqueTags.Contains(\"dotnet\"); // true\n\n        // UnionWith adds all items from another set (deduplicating)\n        HashSet<string> moreTags = new() { \"web\", \"api\", \"csharp\" };\n        uniqueTags.UnionWith(moreTags);\n        // uniqueTags now has: csharp, dotnet, web, api\n\n        // Remove\n        uniqueTags.Remove(\"web\");\n\n        foreach (string tag in uniqueTags)\n        {\n            Console.WriteLine(tag);\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Queue<T> — First In, First Out (FIFO)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Queue<T> models a real-world line or waiting list. The first element added (enqueued) is the first one removed (dequeued). It is perfect for task scheduling, breadth-first search algorithms, and any scenario where order of processing must match order of arrival."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Queue for Task Processing",
        "content": "Simulates a print job queue",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic class QueueDemo\n{\n    public static void Main()\n    {\n        Queue<string> printQueue = new Queue<string>();\n\n        // Enqueue — add to the end of the line\n        printQueue.Enqueue(\"Report.pdf\");\n        printQueue.Enqueue(\"Invoice.docx\");\n        printQueue.Enqueue(\"Photo.jpg\");\n\n        // Peek — see who is next without removing them\n        string nextJob = printQueue.Peek(); // \"Report.pdf\"\n\n        // Dequeue — remove and return the front item\n        while (printQueue.Count > 0)\n        {\n            string job = printQueue.Dequeue();\n            Console.WriteLine($\"Printing: {job}\");\n        }\n        // Prints: Report.pdf, Invoice.docx, Photo.jpg\n\n        // Count is now 0\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Stack<T> — Last In, First Out (LIFO)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Stack<T> models a stack of plates or the browser's back button history. The last element pushed onto the stack is the first one popped off. It is used in undo-redo systems, expression evaluation, depth-first search, and call-stack simulation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Stack for Undo/Redo",
        "content": "Simple undo mechanism using a Stack",
        "language": "csharp",
        "code": "using System;\nusing System.Collections.Generic;\n\npublic class UndoDemo\n{\n    static Stack<string> undoStack = new Stack<string>();\n\n    public static void Main()\n    {\n        // Simulate user typing — each action is pushed\n        TypeText(\"Hello\");\n        TypeText(\" World\");\n        TypeText(\"!\");\n\n        // Undo the last action\n        Undo(); // Removes \"!\"\n        Undo(); // Removes \" World\"\n    }\n\n    static void TypeText(string text)\n    {\n        undoStack.Push(text);\n        Console.WriteLine($\"Typed: {text}\");\n    }\n\n    static void Undo()\n    {\n        if (undoStack.Count > 0)\n        {\n            string removed = undoStack.Pop();\n            Console.WriteLine($\"Undid: {removed}\");\n        }\n    }\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-collections-comparison",
        "caption": "Decision guide: which collection to use",
        "definition": "flowchart TD\n    A[\"Need a collection?\"] --> B{\"Need key-based lookup?\"}\n    B -->|Yes| C[\"Dictionary&lt;TKey, TValue&gt;\"]\n    B -->|No| D{\"Need unique items?\"}\n    D -->|Yes| E[\"HashSet&lt;T&gt;\"]\n    D -->|No| F{\"Need ordering?\"}\n    F -->|Yes| G[\"List&lt;T&gt;\"]\n    F -->|No| H{\"FIFO or LIFO?\"}\n    H -->|FIFO| I[\"Queue&lt;T&gt;\"]\n    H -->|LIFO| J[\"Stack&lt;T&gt;\"]"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Collection",
          "Order",
          "Duplicates",
          "Lookup Speed",
          "Best Use Case"
        ],
        "rows": [
          [
            "List<T>",
            "Indexed (0-based)",
            "Yes",
            "O(1) by index",
            "Dynamic arrays, general-purpose lists"
          ],
          [
            "Dictionary<TKey,TValue>",
            "N/A (hash order)",
            "Keys unique",
            "O(1) by key",
            "Fast lookups by key"
          ],
          [
            "HashSet<T>",
            "N/A (hash order)",
            "No",
            "O(1) Contains",
            "Unique items, set operations"
          ],
          [
            "Queue<T>",
            "FIFO",
            "Yes",
            "O(1) Enqueue/Dequeue",
            "Task processing, BFS"
          ],
          [
            "Stack<T>",
            "LIFO",
            "Yes",
            "O(1) Push/Pop",
            "Undo systems, DFS"
          ]
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "List<T>",
            "definition": "A dynamically-sized, ordered collection of elements of type T, backed by a resizable array."
          },
          {
            "term": "Dictionary<TKey, TValue>",
            "definition": "A hash-table-backed collection mapping unique keys to values with O(1) average lookup."
          },
          {
            "term": "HashSet<T>",
            "definition": "An unordered collection that enforces uniqueness; ideal for set operations and deduplication."
          },
          {
            "term": "Queue<T>",
            "definition": "A FIFO collection: the first item enqueued is the first dequeued."
          },
          {
            "term": "Stack<T>",
            "definition": "A LIFO collection: the last item pushed is the first popped."
          },
          {
            "term": "Enqueue / Dequeue",
            "definition": "Add to the back / remove from the front of a Queue<T>."
          },
          {
            "term": "Push / Pop",
            "definition": "Add to the top / remove from the top of a Stack<T>."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Shopping Cart",
        "description": "Build a simple shopping cart using the collections you have learned about.",
        "steps": [
          "Create a Dictionary<string, int> to map product names to their quantities.",
          "Add at least four products with quantities using the Add method.",
          "Update the quantity of one product using the indexer.",
          "Use ContainsKey to check if a product exists before accessing it.",
          "Use TryGetValue to safely look up a product that does not exist.",
          "Iterate through the dictionary and print each product and its quantity.",
          "Remove one product and print the updated total count."
        ],
        "expectedOutcome": "A console application that manages products in a cart, safely handles missing keys, and demonstrates the full lifecycle of a Dictionary."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Collections Checklist",
        "items": [
          {
            "text": "I can create and populate a List<T>",
            "hint": "Use new List<T>() or new List<T>{...} initialization."
          },
          {
            "text": "I can access, add, and remove items from a Dictionary",
            "hint": "TryGetValue for safe reads, Remove to delete."
          },
          {
            "text": "I understand when to use HashSet<T>",
            "hint": "When you need unique items and fast Contains checks."
          },
          {
            "text": "I can model a FIFO process with Queue<T>",
            "hint": "Enqueue to add, Dequeue to remove in order."
          },
          {
            "text": "I can model a LIFO process with Stack<T>",
            "hint": "Push to add, Pop to remove from the top."
          },
          {
            "text": "I can read the comparison table and choose the right collection",
            "hint": "Think about: ordering, duplicates, and lookup speed."
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can a List<T> hold null values?",
            "answer": "Yes — if T is a reference type, List<T> can store null entries. If T is a value type (like int), null is not allowed because value types cannot be null by default."
          },
          {
            "question": "What happens if I add a duplicate key to a Dictionary?",
            "answer": "An ArgumentException is thrown at runtime. Use ContainsKey or TryAdd before calling Add if duplicates are possible."
          },
          {
            "question": "Is List<T> thread-safe?",
            "answer": "No. Concurrent reads are fine, but any write operation (Add, Remove, etc.) must be synchronized externally. Use ConcurrentBag<T> or lock statements for multi-threaded scenarios."
          },
          {
            "question": "When should I use an array instead of a List<T>?",
            "answer": "Use an array when the size is truly fixed and known at compile time, or when interoperability with APIs that require arrays is needed. For all other cases, List<T> is preferred."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "List<T> is your go-to for dynamic, ordered collections",
          "Dictionary<TKey, TValue> provides O(1) lookups by key",
          "HashSet<T> guarantees uniqueness with fast Contains checks",
          "Queue<T> is FIFO — first in, first out",
          "Stack<T> is LIFO — last in, first out",
          "Always use generic collections from System.Collections.Generic",
          "Use TryGetValue over indexer access when the key might be missing"
        ],
        "takeaway": "Choosing the right collection makes your code more efficient and easier to read. Start with List<T> and Dictionary when you are unsure."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
