{
  "id": "lesson-csharp-031",
  "slug": "extension-methods",
  "moduleSlug": "advanced-features",
  "courseSlug": "csharp-fundamentals",
  "title": "Extension Methods",
  "description": "Learn how to add methods to existing types without modifying them.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "extension-methods",
    "static",
    "LINQ"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Extension methods** allow you to add new methods to existing types without modifying the original type or creating a new derived type. They are static methods defined in static classes, but they appear as instance methods on the extended type."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Extension Methods?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Sometimes you want to add behavior to a type you don't control — like `string`, `int`, or `List<T>`. Extension methods let you do this without inheritance, composition, or modifying the original source code. LINQ is a famous example: every LINQ method is an extension method on `IEnumerable<T>`."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Extension Method",
            "definition": "A static method defined in a static class, with `this` on the first parameter, that extends an existing type."
          },
          {
            "term": "this Parameter",
            "definition": "The first parameter prefixed with `this` tells the compiler which type is being extended."
          },
          {
            "term": "LINQ",
            "definition": "Language Integrated Query — a set of extension methods on IEnumerable<T> and IQueryable<T> for querying data."
          },
          {
            "term": "Static Class",
            "definition": "A class that cannot be instantiated and can only contain static members."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating Extension Methods"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "To create an extension method, follow these rules: define a static class, define a static method, and use the `this` keyword on the first parameter. Once imported with `using`, the method becomes available on instances of the extended type."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Extension Method",
        "content": "Extending the string type with a WordCount method",
        "language": "csharp",
        "code": "// 1. Static class\npublic static class StringExtensions\n{\n    // 2. Static method with 'this' on first parameter\n    public static int WordCount(this string text)\n    {\n        if (string.IsNullOrWhiteSpace(text))\n            return 0;\n\n        return text.Split(' ', StringSplitOptions.RemoveEmptyEntries).Length;\n    }\n}\n\n// 3. Usage (after adding 'using' for the namespace)\nstring sentence = \"Hello world from C#\";\nint count = sentence.WordCount(); // 4\nConsole.WriteLine(count);"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Must Be in a Static Class",
        "text": "Extension methods must live inside a non-generic static class. They cannot be defined in a non-static class or a generic static class."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Extending Generic Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can also extend generic types like `List<T>`, `IEnumerable<T>`, or even your own generic classes. The extended type can be a generic parameter as long as it is the first parameter."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generic Extension",
        "content": "Adding a method to IEnumerable<T>",
        "language": "csharp",
        "code": "public static class EnumerableExtensions\n{\n    // Extends any IEnumerable<T>\n    public static bool IsEmpty<T>(this IEnumerable<T> source)\n    {\n        return source == null || !source.Any();\n    }\n\n    public static IEnumerable<T> DistinctBy<T, TKey>(\n        this IEnumerable<T> source,\n        Func<T, TKey> keySelector)\n    {\n        var seen = new HashSet<TKey>();\n        foreach (var item in source)\n        {\n            if (seen.Add(keySelector(item)))\n                yield return item;\n        }\n    }\n}\n\n// Usage\nList<int> numbers = new List<int>();\nbool empty = numbers.IsEmpty(); // True\n\nvar people = new[]\n{\n    new { Name = \"Alice\", Dept = \"IT\" },\n    new { Name = \"Bob\", Dept = \"HR\" },\n    new { Name = \"Charlie\", Dept = \"IT\" }\n};\n\nvar distinct = people.DistinctBy(p => p.Dept);\n// Returns Alice (IT) and Bob (HR)"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Extension Methods and LINQ"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "LINQ is the most prominent use of extension methods. Methods like `Where`, `Select`, `OrderBy`, and `GroupBy` are all extension methods on `IEnumerable<T>`. This is why you can call them on arrays, lists, and any collection that implements `IEnumerable<T>`."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "extension-method-flow",
        "caption": "How extension methods appear on existing types",
        "definition": "graph TD\n    A[Static Class] -->|defines| B[Extension Method]\n    B -->|extends| C[Target Type]\n    C -->|appears as instance method on| D[Instance of Target Type]\n    D -->|calls| B\n    style B fill:#4CAF50,color:#fff\n    style D fill:#2196F3,color:#fff"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "LINQ-style Extension",
        "content": "Creating a custom WhereIf extension method",
        "language": "csharp",
        "code": "public static class QueryExtensions\n{\n    public static IEnumerable<T> WhereIf<T>(\n        this IEnumerable<T> source,\n        bool condition,\n        Func<T, bool> predicate)\n    {\n        if (condition)\n            return source.Where(predicate);\n        return source;\n    }\n}\n\n// Usage\nbool filterByAge = true;\nvar results = people\n    .WhereIf(filterByAge, p => p.Age >= 18)\n    .ToList();\n\n// If filterByAge is false, WhereIf is a no-op"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Rules and Limitations"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Rules for Extension Methods",
        "items": [
          "Defined in a static, non-generic class",
          "The method itself must be static",
          "The first parameter uses the `this` keyword",
          "The extended type cannot be a pointer type",
          "Extension methods cannot access private members of the extended type",
          "Instance methods on the actual type always take precedence over extension methods with the same signature",
          "You must import the namespace containing the static class with `using`"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Practical Examples"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "DateTime Helpers",
        "content": "Extending DateTime with common calculations",
        "language": "csharp",
        "code": "public static class DateTimeExtensions\n{\n    public static bool IsWeekend(this DateTime date)\n    {\n        return date.DayOfWeek is DayOfWeek.Saturday or DayOfWeek.Sunday;\n    }\n\n    public static DateTime StartOfWeek(this DateTime dt, DayOfWeek startOfWeek = DayOfWeek.Monday)\n    {\n        int diff = (7 + (dt.DayOfWeek - startOfWeek)) % 7;\n        return dt.Date.AddDays(-1 * diff).Date;\n    }\n\n    public static int Age(this DateTime birthDate)\n    {\n        var today = DateTime.Today;\n        int age = today.Year - birthDate.Year;\n        if (birthDate.Date > today.AddYears(-age)) age--;\n        return age;\n    }\n}\n\n// Usage\nDateTime birthday = new DateTime(1990, 5, 20);\nConsole.WriteLine(birthday.Age()); // e.g., 34\nConsole.WriteLine(DateTime.Today.IsWeekend()); // False"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Numeric Extensions",
        "content": "Extending numeric types with useful helpers",
        "language": "csharp",
        "code": "public static class NumericExtensions\n{\n    public static bool IsEven(this int number) => number % 2 == 0;\n    public static bool IsPositive(this double value) => value > 0;\n    public static double ToPercentage(this double value, double total) =>\n        total == 0 ? 0 : (value / total) * 100;\n}\n\n// Usage\nint n = 42;\nConsole.WriteLine(n.IsEven()); // True\n\ndouble sales = 75_000;\ndouble target = 100_000;\nConsole.WriteLine(sales.ToPercentage(target)); // 75"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Use Judiciously",
        "text": "Extension methods should feel natural and discoverable. Avoid adding methods with ambiguous names, or polluting types like `object` or `string` with unrelated behaviors."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a String Utilities Extension Class",
        "description": "Build a StringExtensions static class with these extension methods on string: IsNullOrEmpty (wrapper around string.IsNullOrEmpty), ToTitleCase (capitalizes the first letter of each word), Reverse (returns the string reversed), and Truncate(maxLength) (returns the string cut to maxLength with '...' if truncated).",
        "steps": [
          "Create a static class StringExtensions",
          "Implement IsNullOrEmpty, ToTitleCase, Reverse, and Truncate",
          "Add unit tests or a small console app to verify each method",
          "Test edge cases: empty strings, null inputs, and long strings"
        ],
        "expectedOutcome": "All extension methods compile and behave correctly on string instances."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Extension Methods Checklist",
        "items": [
          {
            "text": "Static class with a clear, descriptive name",
            "hint": "e.g., StringExtensions, DateTimeExtensions"
          },
          {
            "text": "Static method with 'this' on the first parameter",
            "hint": "This is the type being extended"
          },
          {
            "text": "Import the namespace with 'using'",
            "hint": "Without this, the extension method won't appear"
          },
          {
            "text": "Method name is intuitive and follows the extended type's conventions",
            "hint": "Avoid generic names like 'Do' or 'Handle'"
          }
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Extending Types: Approaches Compared",
        "cards": [
          {
            "title": "Inheritance",
            "description": "Create a derived class to add or override behavior.",
            "pros": [
              "Strongly typed",
              "Access to protected members"
            ],
            "cons": [
              "Requires inheritance support",
              "Single inheritance limit",
              "Cannot extend sealed classes"
            ],
            "tags": [
              "oop",
              "inheritance"
            ]
          },
          {
            "title": "Composition",
            "description": "Wrap the original type and delegate calls to it.",
            "pros": [
              "Works with any type",
              "Full control over behavior"
            ],
            "cons": [
              "More boilerplate",
              "Not transparent to callers"
            ],
            "tags": [
              "wrapper",
              "delegation"
            ]
          },
          {
            "title": "Extension Methods",
            "description": "Add static methods that appear as instance methods.",
            "pros": [
              "No wrapper needed",
              "Works on sealed types",
              "Clean call syntax"
            ],
            "cons": [
              "Cannot access private state",
              "Static-only behavior",
              "Namespace imports required"
            ],
            "tags": [
              "static",
              "syntactic-sugar"
            ]
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Do extension methods replace inheritance?",
            "answer": "No. Extension methods add behavior without access to private state or virtual dispatch. Use inheritance when you need to override or access protected members."
          },
          {
            "question": "Why does LINQ use extension methods?",
            "answer": "LINQ needs to work on any IEnumerable<T> without modifying each collection type. Extension methods let LINQ query methods appear on every enumerable type transparently."
          },
          {
            "question": "Can extension methods be overridden?",
            "answer": "No. Extension methods are static and cannot be overridden. If a real instance method exists with the same signature, the instance method always wins."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Extension methods are static methods with 'this' on the first parameter",
          "They make static methods appear as instance methods",
          "Must be defined in a static, non-generic class",
          "Cannot access private members of the extended type",
          "LINQ is built entirely on extension methods",
          "Use them to add clean, discoverable utility methods to existing types"
        ],
        "takeaway": "Extension methods let you extend types you don't control, making APIs feel natural and fluent."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
