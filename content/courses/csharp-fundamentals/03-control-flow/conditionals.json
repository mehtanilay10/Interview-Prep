{
  "id": "lesson-csharp-009",
  "slug": "conditionals",
  "moduleSlug": "control-flow",
  "title": "Conditional Statements",
  "description": "Master C# conditional statements including if, else if, else, ternary operators, logical operators, and complex boolean expressions. Learn how programs make decisions and branch execution paths based on runtime conditions.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 30,
  "tags": [
    "csharp",
    "if",
    "else",
    "ternary",
    "conditionals",
    "boolean",
    "logical-operators"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A computer program would not be very useful if it ran the same way every single time, no matter what happened. Real programs need to make decisions: should a user be allowed to log in? Is a number positive or negative? Did the user enter a valid choice? Conditional statements are the mechanism that allows your code to answer these questions and choose different paths at runtime."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In C#, all conditional logic is built on the concept of a boolean value. A boolean can only be true or false. Every condition you write in an if statement must ultimately evaluate to true or false. The compiler enforces this strictly — if you try to use a number or string directly as a condition in older C# versions, you will get a compile error. Understanding how conditions are formed and how they combine is the foundation of all program logic."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Are Conditional Statements"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Conditional statements are branching structures that tell the computer: 'if this condition is true, run this block of code; otherwise, run that block of code.' They are sometimes called selection statements because the program selects which code path to follow."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Boolean",
            "definition": "A data type with only two possible values: true or false. Named after mathematician George Boole."
          },
          {
            "term": "Condition",
            "definition": "An expression that evaluates to true or false, used inside if statements and loops."
          },
          {
            "term": "Branch",
            "definition": "A path of execution chosen by a conditional statement."
          },
          {
            "term": "Fall-through",
            "definition": "When execution continues to the next case or block because no break or return statement was encountered."
          },
          {
            "term": "Ternary Operator",
            "definition": "A shorthand conditional expression with three parts: condition ? valueIfTrue : valueIfFalse."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "conditional-flow-diagram",
        "caption": "How an if-else conditional branch works at runtime",
        "definition": "flowchart TD\n    Start([Program reaches if statement]) --> Condition{Is condition true?}\n    Condition -- Yes --> TrueBlock[Execute 'if' block]\n    Condition -- No --> HasElse{Has else?}\n    HasElse -- Yes --> ElseBlock[Execute 'else' block]\n    HasElse -- No --> End([Continue after if statement])\n    TrueBlock --> End\n    ElseBlock --> End"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The if Statement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The if statement is the simplest and most fundamental conditional structure. It evaluates a single boolean expression inside parentheses. If that expression is true, the code block immediately following the if is executed. If the expression is false, that block is skipped entirely and execution continues after the closing brace."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Here are several common ways to form conditions using comparison operators. The == operator checks equality, != checks inequality, < checks less than, > checks greater than, <= checks less than or equal to, and >= checks greater than or equal to. All of these return a boolean result."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "if statement with multiple conditions",
        "content": "Demonstrates basic if statement usage with comparison operators",
        "language": "csharp",
        "code": "int score = 85;\n\n// Check if score is passing\nif (score >= 60)\n{\n    Console.WriteLine(\"You passed!\");\n}\n\n// Check equality\nstring userName = \"Alice\";\nif (userName == \"Alice\")\n{\n    Console.WriteLine(\"Welcome back, Alice!\");\n}\n\n// Check inequality\nif (userName != \"Bob\")\n{\n    Console.WriteLine(\"Access granted for non-Bob users.\");\n}\n\n// Combine multiple conditions\nint age = 25;\nint income = 50000;\nif (age >= 18 && income > 30000)\n{\n    Console.WriteLine(\"You qualify for the loan.\");\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Block Scope in C#",
        "text": "In C#, variables declared inside an if block are scoped to that block only. They are not accessible outside the braces. This prevents accidental misuse and is a key part of C#'s design for safer code."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Comparison Operators Reference"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Operator",
          "Meaning",
          "Example",
          "Result"
        ],
        "rows": [
          [
            "==",
            "Equal to",
            "5 == 5",
            "true"
          ],
          [
            "!=",
            "Not equal to",
            "5 != 3",
            "true"
          ],
          [
            ">",
            "Greater than",
            "5 > 3",
            "true"
          ],
          [
            "<",
            "Less than",
            "5 < 3",
            "false"
          ],
          [
            ">=",
            "Greater than or equal to",
            "5 >= 5",
            "true"
          ],
          [
            "<=",
            "Less than or equal to",
            "5 <= 3",
            "false"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The else and else if Clauses"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A single if statement only handles one possibility. To handle additional cases, you add an else if clause. Each else if provides another condition to check, evaluated only if all previous conditions were false. You can chain as many else if clauses as needed. Finally, you can add an else clause at the end, which acts as a catch-all that runs when none of the preceding conditions are true. The else clause has no condition of its own."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The order of else if clauses matters. The first condition that evaluates to true wins, and the rest are ignored. This means you should order your conditions from most specific to most general to avoid unintended behavior."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "if-else-if chain for grading",
        "content": "Assigning letter grades using an if-else-if chain",
        "language": "csharp",
        "code": "int examScore = 78;\nstring grade;\n\nif (examScore >= 90)\n{\n    grade = \"A\";\n}\nelse if (examScore >= 80)\n{\n    grade = \"B\";\n}\nelse if (examScore >= 70)\n{\n    grade = \"C\";\n}\nelse if (examScore >= 60)\n{\n    grade = \"D\";\n}\nelse\n{\n    grade = \"F\";\n}\n\nConsole.WriteLine($\"Score {examScore} = Grade {grade}\");\n// Output: Score 78 = Grade C"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "if vs else if vs else",
        "cards": [
          {
            "title": "if",
            "description": "Starts a conditional block. Always checked first. You can use it standalone or as the first in a chain.",
            "tags": [
              "required"
            ]
          },
          {
            "title": "else if",
            "description": "Adds an additional condition to check. Only evaluated if all previous conditions were false. Optional and repeatable.",
            "tags": [
              "optional"
            ]
          },
          {
            "title": "else",
            "description": "The default fallback block. Runs when every preceding condition is false. Has no condition of its own. Optional but recommended for exhaustive handling.",
            "tags": [
              "optional"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Logical Operators"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Real-world conditions are rarely simple. You often need to check multiple things at once. C# provides three logical operators that let you combine or invert conditions: AND (&&), OR (||), and NOT (!). Understanding how these operators work — especially short-circuit evaluation — is critical for writing correct and efficient conditionals."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The && operator requires both sides to be true. If the left side is false, C# does not even evaluate the right side (this is called short-circuiting). The || operator requires at least one side to be true. If the left side is true, the right side is skipped. The ! operator flips a boolean: true becomes false, false becomes true."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Logical operators in practice",
        "content": "Using &&, ||, and ! to combine conditions",
        "language": "csharp",
        "code": "bool isMember = true;\nbool hasCoupon = false;\nint age = 22;\n\n// AND: both must be true\nif (isMember && hasCoupon)\n{\n    Console.WriteLine(\"Both conditions met: 20% discount\");\n}\nelse\n{\n    Console.WriteLine(\"Discount not applied.\");\n}\n\n// OR: at least one must be true\nif (isMember || age >= 65)\n{\n    Console.WriteLine(\"Eligible for senior/member discount.\");\n}\n\n// NOT: inverts the condition\nbool isBanned = false;\nif (!isBanned)\n{\n    Console.WriteLine(\"User can access the system.\");\n}\n\n// Combining logical operators\nif ((age >= 18 && age <= 65) && (isMember || hasCoupon))\n{\n    Console.WriteLine(\"Complex condition satisfied.\");\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Short-Circuit Evaluation Side Effects",
        "text": "Because && and || short-circuit, any method calls or expressions on the right side may never execute. Do not place side effects like Console.WriteLine() or variable assignments inside conditions — the behavior may be surprising."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Nested Conditionals"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can place an if statement inside another if statement. This is called nesting. While nesting is sometimes necessary, be careful: deeply nested conditionals become hard to read and are often called 'arrow code.' When you find yourself nesting more than two or three levels, consider refactoring with early returns or guard clauses."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nested conditionals vs guard clauses",
        "content": "Comparing deeply nested code with cleaner guard clauses",
        "language": "csharp",
        "code": "// Avoid this: deeply nested\nstring ProcessUser(User user)\n{\n    if (user != null)\n    {\n        if (user.IsActive)\n        {\n            if (user.Age >= 18)\n            {\n                return \"Access granted\";\n            }\n            else\n            {\n                return \"Too young\";\n            }\n        }\n        else\n        {\n            return \"Account inactive\";\n        }\n    }\n    else\n    {\n        return \"User is null\";\n    }\n}\n\n// Prefer this: guard clauses\nstring ProcessUser(User user)\n{\n    if (user == null) return \"User is null\";\n    if (!user.IsActive) return \"Account inactive\";\n    if (user.Age < 18) return \"Too young\";\n    return \"Access granted\";\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Ternary Conditional Operator"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The ternary operator is a compact way to write simple if-else logic in a single expression. It has three parts: a condition, a result when the condition is true, and a result when the condition is false. The syntax is condition ? valueIfTrue : valueIfFalse. Because it is an expression (not a statement), it produces a value that you can assign to a variable or return from a method directly."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use the ternary operator for simple assignments where the if-else would span multiple lines. If the logic is complex, or the branches are long statements rather than simple values, stick with a full if-else block for readability."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Ternary operator examples",
        "content": "Using ?: for concise conditional assignments",
        "language": "csharp",
        "code": "int temperature = 28;\n\n// Basic ternary\nstring weather = temperature > 25 ? \"Warm\" : \"Cool\";\nConsole.WriteLine(weather); // Warm\n\n// Ternary with method return\npublic string GetStatus(bool isOnline) => isOnline ? \"Online\" : \"Offline\";\n\n// Nested ternary (use carefully — can become hard to read)\nstring category = temperature > 30 ? \"Hot\" : temperature > 20 ? \"Warm\" : temperature > 10 ? \"Mild\" : \"Cold\";"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Ternary Best Practice",
        "text": "Use the ternary operator for simple value selection only. Avoid nested ternaries or using them as replacements for if blocks with multiple statements. If you need more than a simple true/false value, use if-else."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Pitfalls"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Frequent mistakes beginners make",
        "items": [
          "Using = (assignment) instead of == (comparison) inside conditions",
          "Forgetting break statements when converting conditionals to switch later",
          "Placing the broader condition before the narrower one in else if chains",
          "Using assignment inside a condition accidentally: if (x = 5) instead of if (x == 5)",
          "Not covering all cases, leaving logic gaps"
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Temperature Category Classifier",
        "description": "Write a method called GetTemperatureCategory that accepts an integer temperature and returns a string. Use if-else-if logic: 'Freezing' for below 0, 'Cold' for 0-14, 'Cool' for 15-24, 'Warm' for 25-34, 'Hot' for 35 and above. Include a guard clause for null input.",
        "steps": [
          "Create a method with an int parameter named temperature",
          "Add a guard clause if temperature is outside -100 to 100 range",
          "Use if-else-if chain to return the correct category string",
          "Test with temperatures: -5, 10, 20, 30, 40"
        ],
        "expectedOutcome": "The method correctly classifies each temperature and handles out-of-range values with a descriptive error message."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Conditional Mastery Checklist",
        "items": [
          {
            "text": "I can write a basic if statement with a boolean condition",
            "hint": "Try: if (score >= 60) { Console.WriteLine(\"Pass\"); }"
          },
          {
            "text": "I can chain else if clauses for multiple conditions",
            "hint": "Remember: first true condition wins; order matters."
          },
          {
            "text": "I understand short-circuit evaluation with && and ||",
            "hint": "&& stops on false, || stops on true."
          },
          {
            "text": "I can use the ternary operator for simple assignments",
            "hint": "Format: condition ? trueValue : falseValue"
          },
          {
            "text": "I can identify when to use if-else vs ternary",
            "hint": "Simple value selection = ternary. Complex logic = if-else."
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I use == to compare strings in C#?",
            "answer": "Yes. In C#, the == operator on strings compares the actual content (value equality), not the memory reference. This is different from some other languages. Under the hood, C# calls string.Equals() for you when you use == on strings."
          },
          {
            "question": "What happens if I forget the else clause?",
            "answer": "Nothing bad happens — the program simply continues after the if block. However, if your logic requires handling every possible case (for example, setting a variable in all branches), omitting else can leave variables unassigned, which causes a compile error."
          },
          {
            "question": "Is the ternary operator faster than if-else?",
            "answer": "The performance difference is negligible. The ternary operator is syntactic sugar that the compiler translates into equivalent IL. Choose between them based on readability, not performance."
          },
          {
            "question": "Can I return from inside an if block?",
            "answer": "Yes. Returning from inside an if block is a common pattern called a guard clause. It exits the method early when a condition is met, which often leads to cleaner, flatter code."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "if evaluates a boolean condition and executes a block when true",
          "else if adds additional conditions in sequence; first true wins",
          "else is the final fallback when no prior condition matched",
          "&& and || combine conditions; they short-circuit for efficiency",
          "! inverts a boolean value",
          "The ternary operator ?: is a concise expression-based if-else",
          "Guard clauses flatten nested conditionals and improve readability"
        ],
        "takeaway": "Conditional statements are the decision-making engine of every program. Mastering if, else if, else, logical operators, and ternary expressions gives you complete control over execution flow."
      }
    }
  ],
  "relatedLessons": [
    "loops",
    "switch-statements"
  ],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
