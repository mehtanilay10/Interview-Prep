{
  "id": "lesson-csharp-023",
  "slug": "file-io",
  "moduleSlug": "modern-csharp",
  "title": "File I/O",
  "description": "Master reading from and writing to files in C# using the File class, StreamReader, StreamWriter, and async I/O patterns for real-world applications.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "file-io",
    "streams",
    "filesystem",
    "async"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**File I/O (Input/Output)** is how your programs communicate with the file system. Every time your app saves a user's progress, exports a report, or loads a configuration file, it uses file I/O. C# provides several approaches ranging from simple one-liners to efficient streaming for large files."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Is File I/O?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "File I/O stands for **Input/Output** — it describes any operation where a program reads data from an external source (input) or writes data to an external destination (output). In C#, that external destination is typically the file system on disk. Without file I/O, your program would lose everything when it closes, because variables and objects only exist in memory (RAM) while the program runs."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "File I/O",
            "definition": "Operations that read from or write to files stored on disk."
          },
          {
            "term": "Stream",
            "definition": "A sequence of bytes that can be read from or written to sequentially."
          },
          {
            "term": "Dispose",
            "definition": "Releases unmanaged resources like file handles when a stream is no longer needed."
          },
          {
            "term": "Path",
            "definition": "A string representing the location of a file or directory on disk."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The File Class — Simple Read and Write"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `System.IO.File` class provides static methods that are perfect for small to medium-sized files. These methods are the simplest to use because you don't need to create or manage any objects. They load the entire file into memory at once, which is convenient but means they're not ideal for very large files."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Method",
          "Purpose",
          "Returns",
          "Overwrites?"
        ],
        "rows": [
          [
            "ReadAllText(path)",
            "Read entire file as one string",
            "string",
            "No"
          ],
          [
            "ReadAllLines(path)",
            "Read all lines into a string array",
            "string[]",
            "No"
          ],
          [
            "WriteAllText(path, text)",
            "Write a string to a file",
            "void",
            "Yes"
          ],
          [
            "AppendAllText(path, text)",
            "Append a string to an existing file",
            "void",
            "No"
          ],
          [
            "WriteAllLines(path, lines)",
            "Write a string array to a file",
            "void",
            "Yes"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Reading Files with File.ReadAllText"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Read All Text",
        "content": "Use ReadAllText when you need the entire file as a single string, such as a short configuration file or a poem.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass Program\n{\n    static void Main()\n    {\n        // Ensure the file exists before reading\n        string filePath = \"notes.txt\";\n\n        if (File.Exists(filePath))\n        {\n            // Read the entire file content as one string\n            string content = File.ReadAllText(filePath);\n            Console.WriteLine(\"File contents:\");\n            Console.WriteLine(content);\n        }\n        else\n        {\n            Console.WriteLine(\"File not found. Creating a sample file...\");\n            File.WriteAllText(filePath, \"Hello from File I/O!\");\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Reading Line by Line with ReadAllLines"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Read All Lines",
        "content": "Use ReadAllLines when you want to process each line individually, such as parsing a CSV or log file.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass Program\n{\n    static void Main()\n    {\n        string filePath = \"todo.txt\";\n\n        // Check if file exists before reading\n        if (!File.Exists(filePath))\n        {\n            Console.WriteLine(\"todo.txt not found. Creating sample file...\");\n            string[] sampleLines = { \"Buy groceries\", \"Finish homework\", \"Walk the dog\" };\n            File.WriteAllLines(filePath, sampleLines);\n        }\n\n        // Read all lines into an array\n        string[] lines = File.ReadAllLines(filePath);\n\n        Console.WriteLine($\"Found {lines.Length} tasks:\");\n\n        int taskNumber = 1;\n        foreach (string line in lines)\n        {\n            // Trim whitespace and skip empty lines\n            string trimmed = line.Trim();\n            if (!string.IsNullOrEmpty(trimmed))\n            {\n                Console.WriteLine($\"{taskNumber}. {trimmed}\");\n                taskNumber++;\n            }\n        }\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Writing and Appending Files"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Write and Append",
        "content": "WriteAllText overwrites the file, while AppendAllText adds to the end. WriteAllLines creates a fresh file with multiple lines.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass Program\n{\n    static void Main()\n    {\n        string filePath = \"journal.txt\";\n\n        // Write initial content (overwrites any existing file)\n        File.WriteAllText(filePath, \"Day 1: Started learning C#!\\n\");\n        Console.WriteLine(\"Created journal.txt with initial entry.\");\n\n        // Append a new line without removing existing content\n        File.AppendAllText(filePath, \"Day 2: Learned about File I/O.\\n\");\n        Console.WriteLine(\"Appended Day 2 entry.\");\n\n        // Write multiple lines at once (overwrites file)\n        string[] allEntries =\n        {\n            \"Day 1: Started learning C#!\",\n            \"Day 2: Learned about File I/O.\",\n            \"Day 3: Mastered streams!\"\n        };\n        File.WriteAllLines(filePath, allEntries);\n        Console.WriteLine(\"Rewrote journal.txt with all three entries.\");\n\n        // Read and display final file\n        Console.WriteLine(\"\\nFinal journal contents:\");\n        Console.WriteLine(File.ReadAllText(filePath));\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Streams for Efficient I/O"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `File` class methods load the entire file into memory at once. For large files — such as a 500 MB log file or a multi-gigabyte dataset — this can exhaust available memory and slow down or even crash your program. **Streams** solve this by reading and writing data incrementally, one piece at a time, keeping memory usage constant regardless of file size."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "file-io-stream-flow",
        "caption": "How streams process data incrementally from disk to memory",
        "definition": "flowchart LR\n    A[File on Disk] -->|Open Stream| B[StreamReader]\n    B -->|Read chunk| C[Memory Buffer]\n    C -->|Process| D[Your Program]\n    D -->|Write chunk| E[StreamWriter]\n    E -->|Flush to disk| F[Output File]\n    style A fill:#e1f5fe\n    style F fill:#e8f5e9\n    style D fill:#fff3e0"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "StreamReader — Reading Line by Line Efficiently"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`StreamReader` reads data character by character or line by line from a file. It is wrapped in a `using` statement so the file handle is automatically closed when reading is complete — even if an error occurs partway through."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "StreamReader for Large Files",
        "content": "StreamReader is ideal when you need to process one line at a time without loading the whole file into memory.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass Program\n{\n    static void Main()\n    {\n        string inputPath = \"large-data.csv\";\n        string outputPath = \"summary.txt\";\n\n        // Create a sample large file for demonstration\n        if (!File.Exists(inputPath))\n        {\n            using StreamWriter writer = new StreamWriter(inputPath);\n            for (int i = 1; i <= 10; i++)\n            {\n                writer.WriteLine($\"Row {i},Value {i * 10}\");\n            }\n        }\n\n        // Use StreamReader to read line by line\n        using StreamReader reader = new StreamReader(inputPath);\n        using StreamWriter writer = new StreamWriter(outputPath);\n\n        string line;\n        int lineCount = 0;\n\n        // ReadLine returns null when end of file is reached\n        while ((line = reader.ReadLine()) != null)\n        {\n            lineCount++;\n            Console.WriteLine($\"Line {lineCount}: {line}\");\n            writer.WriteLine($\"Processed: {line}\");\n        }\n\n        Console.WriteLine($\"\\nProcessed {lineCount} lines.\");\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "StreamWriter — Writing Efficiently"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "StreamWriter for Bulk Writes",
        "content": "StreamWriter buffers writes for efficiency. Call Flush or Close when done to ensure all data reaches the disk.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass Program\n{\n    static void Main()\n    {\n        string filePath = \"inventory.csv\";\n\n        // StreamWriter uses a buffer to batch writes efficiently\n        // The 'using' statement ensures Dispose is called automatically\n        using StreamWriter writer = new StreamWriter(filePath);\n\n        // Write a header row\n        writer.WriteLine(\"Product,Price,Stock\");\n\n        // Write data rows\n        string[,] products =\n        {\n            { \"Laptop\", \"999.99\", \"15\" },\n            { \"Mouse\", \"25.50\", \"100\" },\n            { \"Keyboard\", \"79.99\", \"45\" }\n        };\n\n        for (int row = 0; row < products.GetLength(0); row++)\n        {\n            writer.WriteLine($\"{products[row, 0]},{products[row, 1]},{products[row, 2]}\");\n        }\n\n        Console.WriteLine(\"Inventory saved to inventory.csv\");\n\n        // StreamWriter auto-flushes on Dispose when the 'using' block ends\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Working with Directories and Paths"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Real applications rarely deal with a single flat file. They organize files into directories, need to build paths dynamically, and must work on Windows, Mac, and Linux. The `System.IO.Path` and `System.IO.Directory` classes handle this cross-platform complexity for you."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Directory and Path Operations",
        "content": "Create directories, build paths safely, and enumerate files using Directory and Path helpers.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\n\nclass Program\n{\n    static void Main()\n    {\n        string baseFolder = \"MyAppData\";\n        string subFolder = \"Logs\";\n\n        // Build the full path safely (cross-platform)\n        string fullPath = Path.Combine(baseFolder, subFolder, \"app.log\");\n        Console.WriteLine($\"Full path: {fullPath}\");\n\n        // Get folder name and file name from a path\n        string directory = Path.GetDirectoryName(fullPath);\n        string fileName = Path.GetFileName(fullPath);\n        string extension = Path.GetExtension(fullPath);\n        Console.WriteLine($\"Directory: {directory}\");\n        Console.WriteLine($\"File: {fileName}\");\n        Console.WriteLine($\"Extension: {extension}\");\n\n        // Create directory if it doesn't exist\n        if (!Directory.Exists(baseFolder))\n        {\n            Directory.CreateDirectory(Path.Combine(baseFolder, subFolder));\n            Console.WriteLine($\"Created directory: {baseFolder}/{subFolder}\");\n        }\n\n        // List all .txt files in a directory\n        if (Directory.Exists(baseFolder))\n        {\n            string[] txtFiles = Directory.GetFiles(baseFolder, \"*.txt\", SearchOption.AllDirectories);\n            Console.WriteLine($\"\\n.txt files found: {txtFiles.Length}\");\n            foreach (string file in txtFiles)\n            {\n                Console.WriteLine(file);\n            }\n        }\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Cross-Platform Path Safety",
        "text": "Always use Path.Combine() instead of string concatenation for paths. Windows uses backslashes (\\), while macOS and Linux use forward slashes (/). Path.Combine handles the correct separator automatically. Similarly, use Environment.NewLine for line breaks so your app works on every OS."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Async File I/O"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For applications with user interfaces or web servers, blocking the main thread with file operations makes the app feel sluggish. **Async file I/O** uses the Task-based Asynchronous Pattern (TAP) to keep your application responsive. The `await` keyword lets the thread serve other work while the file operation completes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Async File Read and Write",
        "content": "Use ReadAllTextAsync and WriteAllTextAsync for non-blocking file operations.",
        "language": "csharp",
        "code": "using System;\nusing System.IO;\nusing System.Threading.Tasks;\n\nclass Program\n{\n    // Mark the method as async so we can await inside it\n    static async Task Main()\n    {\n        string filePath = \"async-demo.txt\";\n        string largeContent = new string('*', 1_000_000); // 1 MB of asterisks\n\n        // Write asynchronously — the thread is free to do other work\n        await File.WriteAllTextAsync(filePath, largeContent);\n        Console.WriteLine(\"File written asynchronously.\");\n\n        // Read asynchronously\n        string readBack = await File.ReadAllTextAsync(filePath);\n        Console.WriteLine($\"Read back {readBack.Length:N0} characters asynchronously.\");\n\n        // StreamReader async example\n        using StreamReader reader = new StreamReader(filePath);\n        char[] buffer = new char[4096];\n        int bytesRead = await reader.ReadAsync(buffer, 0, buffer.Length);\n        Console.WriteLine($\"First chunk: {new string(buffer, 0, bytesRead)}\");\n    }\n}"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "File I/O Checklist",
        "items": [
          {
            "text": "Check File.Exists before reading a file",
            "hint": "Use File.Exists(path) to avoid FileNotFoundException"
          },
          {
            "text": "Wrap streams in using statements",
            "hint": "The using block calls Dispose() automatically"
          },
          {
            "text": "Use Path.Combine() for all file paths",
            "hint": "Never concatenate paths with + or string interpolation"
          },
          {
            "text": "Prefer StreamReader for large files",
            "hint": "ReadAllText loads everything into memory at once"
          },
          {
            "text": "Use async methods for I/O-bound applications",
            "hint": "ReadAllTextAsync keeps UI and web servers responsive"
          },
          {
            "text": "Handle exceptions with try/catch",
            "hint": "IOException, UnauthorizedAccessException, and DirectoryNotFoundException are common"
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Simple Todo File Manager",
        "description": "Create a console app that manages a todo list stored in a text file. Each todo is one line. The app should add, list, and delete todos.",
        "steps": [
          "Create a text file named todos.txt with a few sample items.",
          "Write a method AddTodo(string task) that appends a new task to the file.",
          "Write a method ListTodos() that reads all lines and displays them with numbers.",
          "Write a method DeleteTodo(int number) that removes the task at the given line number.",
          "Wrap all file operations in try/catch blocks and print friendly error messages.",
          "Call each method from Main() to verify the behavior."
        ],
        "expectedOutcome": "A working todo manager that persists tasks to disk between runs, with clear user prompts and error handling."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "What is the difference between File.ReadAllText and StreamReader?",
            "answer": "ReadAllText loads the entire file into memory at once and returns a single string. StreamReader reads incrementally (one line or one character at a time), using far less memory for large files."
          },
          {
            "question": "Do I need to call Close() on streams?",
            "answer": "No, if you wrap the stream in a using statement, the Dispose() method (which Close() calls internally) is invoked automatically when the block ends."
          },
          {
            "question": "What exceptions can file I/O throw?",
            "answer": "Common exceptions include FileNotFoundException, DirectoryNotFoundException, UnauthorizedAccessException, IOException, and PathTooLongException. Always use try/catch around file operations in production code."
          },
          {
            "question": "When should I use async file I/O?",
            "answer": "Use async I/O for UI applications, ASP.NET web servers, or anywhere blocking the main thread would degrade user experience. For console scripts that run quickly, synchronous I/O is fine."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "File.ReadAllText and ReadAllLines load whole files into memory",
          "WriteAllText overwrites; AppendAllText adds to the end",
          "StreamReader/StreamWriter process data incrementally",
          "using ensures streams are disposed correctly",
          "Path.Combine builds cross-platform paths safely",
          "Async I/O keeps applications responsive"
        ],
        "takeaway": "File I/O enables data persistence. Choose the right tool: simple File methods for small files, streams for large ones, and async for responsive apps."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
