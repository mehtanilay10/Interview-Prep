{
  "id": "lesson-csharp-025",
  "slug": "nullable-types",
  "moduleSlug": "modern-csharp",
  "title": "Nullable Types",
  "description": "Understand nullable value types, the null-coalescing operators, and C# 8+ nullable reference types for writing safer, null-aware code.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "csharp",
    "nullable",
    "null-checks",
    "reference-types",
    "nrt"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Nullable types** let you represent the absence of a value. For value types like `int` and `bool`, C# requires the `?` suffix. For reference types like `string`, C# 8 introduced **Nullable Reference Types (NRT)**, which warn you at compile time when null could slip through. Together, these features dramatically reduce the risk of null reference exceptions."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Nullability Matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Tony Hoare, the computer scientist who introduced null references in 1965, later called it his \"billion-dollar mistake.\" Null reference exceptions — where your program crashes because it tried to use a variable that points to nothing — are among the most common runtime errors in programming. C# gives you tools to make null explicit, trackable, and safe."
      }
    },
    {
      "type": "quote",
      "data": {
        "text": "I call it my billion-dollar mistake. It was the invention of the null reference in 1965.",
        "attribution": "Tony Hoare"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Nullable Value Type",
            "definition": "A value type (int, bool, DateTime) that can also hold null, written as int? or bool?."
          },
          {
            "term": "HasValue",
            "definition": "A Boolean property on Nullable<T> that is true when the variable contains a real value."
          },
          {
            "term": "Value",
            "definition": "Returns the underlying value from a Nullable<T>. Throws InvalidOperationException if the value is null."
          },
          {
            "term": "Null-Coalescing Operator (??)",
            "definition": "Returns the left-hand operand if it is not null; otherwise returns the right-hand operand."
          },
          {
            "term": "Null-Coalescing Assignment (??=)",
            "definition": "Assigns the right-hand value only if the left-hand variable is currently null."
          },
          {
            "term": "Nullable Reference Type (NRT)",
            "definition": "A reference type annotated with ? to indicate it may be null. The compiler warns when nulls are assigned to non-nullable reference types."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Nullable Value Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Value types in C# — such as `int`, `double`, `bool`, and `DateTime` — are designed to always have a value. An `int` cannot be null by default; it defaults to 0. But in real data, a value might genuinely be missing. A database column for \"Age\" might be empty. A calculation might not apply. Nullable value types solve this."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Creating and Using Nullable Value Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nullable Value Types",
        "content": "Use the ? suffix to allow a value type to hold null. Use HasValue and Value to inspect it.",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        // int x = null; // Compile error: value types cannot be null\n\n        // Add '?' to make it nullable\n        int? age = null;\n        double? price = 29.99;\n        bool? isMember = null;\n        DateTime? lastLogin = null;\n\n        // Check if a value is present using HasValue\n        if (age.HasValue)\n        {\n            Console.WriteLine($\"Age is {age.Value}\");\n        }\n        else\n        {\n            Console.WriteLine(\"Age is unknown (null)\");\n        }\n\n        // The Value property throws if the value is null\n        // age.Value; // Throws InvalidOperationException!\n\n        // GetValueOrDefault returns a fallback when null\n        int safeAge = age.GetValueOrDefault();        // Returns 0 for int\n        int defaultAge = age.GetValueOrDefault(18);    // Returns 18\n\n        Console.WriteLine($\"safeAge = {safeAge}, defaultAge = {defaultAge}\");\n    }\n}"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Operator",
          "Name",
          "Description",
          "Example"
        ],
        "rows": [
          [
            "??",
            "Null-coalescing",
            "Return left if not null, else right",
            "int x = age ?? 0;"
          ],
          [
            "??=",
            "Null-coalescing assignment",
            "Assign right only if left is null",
            "age ??= 18;"
          ],
          [
            "?. ",
            "Null-conditional",
            "Access member only if not null",
            "name?.Length"
          ],
          [
            "?[]",
            "Null-index",
            "Index only if array is not null",
            "items?[0]"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Null-Coalescing Operators"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The null-coalescing operators let you provide fallback values cleanly. The `??` operator checks the left side; if it's not null, it returns it. If it is null, it evaluates and returns the right side. The `??=` operator only assigns if the left side is currently null, which is useful for lazy initialization."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Null-Coalescing and Null-Conditional Operators",
        "content": "Combine ??, ??=, and ?. to write concise null-safe code.",
        "language": "csharp",
        "code": "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        string? input = GetUserInput(); // Might be null\n\n        // Null-coalescing: provide a default if null\n        string displayName = input ?? \"Anonymous\";\n        Console.WriteLine($\"Hello, {displayName}!\");\n\n        // Null-conditional: safely access Length without a crash\n        int? nameLength = input?.Length;\n        Console.WriteLine($\"Name length: {nameLength}\");\n\n        // Null-coalescing assignment: only set if currently null\n        string? cached = null;\n        cached ??= LoadFromCache(); // Loads only if null\n        Console.WriteLine($\"Cached value: {cached}\");\n\n        // Chaining null-conditional and coalescing\n        string? city = null;\n        int population = city?.Length ?? 0; // If city is null, use 0\n        Console.WriteLine($\"Population placeholder: {population}\");\n    }\n\n    static string? GetUserInput() => null; // Simulating missing input\n\n    static string LoadFromCache() => \"Cached Data\";\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Nullable Reference Types (NRT)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "C# 8 introduced **Nullable Reference Types**, a compile-time feature that changes how the compiler treats reference types like `string`, `object`, and custom classes. Before C# 8, every reference type was implicitly nullable — the compiler gave no warning when you assigned null. With NRT enabled, you must explicitly mark nullable references with `?`, and the compiler warns about unsafe null usage."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "nrt-context-flow",
        "caption": "Flow of a nullable-aware method from input to safe output",
        "definition": "flowchart TD\n    A[Caller passes string?] --> B{Null check?}\n    B -->|Yes| C[Proceed safely]\n    B -->|No| D[Throw / return default]\n    C --> E[Compiler knows it's non-null]\n    E --> F[Safe to access .Length]\n    style A fill:#e1f5fe\n    style F fill:#e8f5e9\n    style D fill:#ffebee"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Enabling Nullable Reference Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can enable NRT at the project level in your `.csproj` file, or at the top of a single file with the `#nullable enable` directive. Project-level enablement is recommended because it provides consistent protection across your entire codebase."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Enable in Your .csproj",
        "text": "Add <Nullable>enable</Nullable> inside a <PropertyGroup> in your .csproj file. This turns on NRT for every file in the project, giving you compile-time warnings for potential null bugs."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nullable Reference Types in Action",
        "content": "With NRT enabled, the compiler distinguishes between non-nullable and nullable reference types.",
        "language": "csharp",
        "code": "#nullable enable\n\nusing System;\n\nclass UserProfile\n{\n    // 'string' without '?' means this must never be null\n    public string UserName { get; set; }\n\n    // 'string?' explicitly allows null\n    public string? MiddleName { get; set; }\n\n    // Return type is non-nullable — caller expects a real value\n    public string GetDisplayName()\n    {\n        // Compiler knows UserName is non-null here\n        return MiddleName != null\n            ? $\"{UserName} {MiddleName}\"\n            : UserName;\n    }\n\n    // Null-forgiving operator: suppress warning when you know it's not null\n    public int GetNameLength(string? name)\n    {\n        // name?.Length returns int? (nullable)\n        // name!.Length tells compiler: I know it's not null here\n        return name!.Length;\n    }\n}\n\nclass Program\n{\n    static void Main()\n    {\n        UserProfile profile = new UserProfile\n        {\n            UserName = \"Alice\",\n            MiddleName = \"Marie\"\n        };\n\n        Console.WriteLine(profile.GetDisplayName());\n        Console.WriteLine($\"Name length: {profile.GetNameLength(profile.UserName)}\");\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "The Null-Forgiving Operator (!)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The null-forgiving operator `!` tells the compiler to stop warning about a possible null reference. Use it sparingly — only when you have a strong guarantee that the value is not null, such as after an explicit null check or when working with dependency-injected services that the framework initializes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Null-Forgiving Operator",
        "content": "Suppress NRT warnings when you have external knowledge that a value is non-null.",
        "language": "csharp",
        "code": "#nullable enable\n\nusing System;\n\nclass Program\n{\n    static void Main()\n    {\n        string? input = LoadConfigValue();\n\n        // input?.Length returns int? — we need a plain int\n        // The ! suppresses the nullable warning because we validated it\n        if (input != null)\n        {\n            int length = input!.Length; // Safe: we just checked\n            Console.WriteLine($\"Config value length: {length}\");\n        }\n        else\n        {\n            Console.WriteLine(\"Config value was null.\");\n        }\n    }\n\n    static string? LoadConfigValue()\n    {\n        // Simulating a config file that might be missing\n        return Environment.GetEnvironmentVariable(\"APP_NAME\");\n    }\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Nullable Reference Types: On vs Off",
        "cards": [
          {
            "title": "NRT Disabled (Default in Older Projects)",
            "description": "All reference types are implicitly nullable. The compiler never warns about null assignments or dereferences.",
            "pros": [
              "No compilation warnings",
              "Legacy code works without changes"
            ],
            "cons": [
              "Null bugs reach production",
              "No compile-time safety"
            ],
            "tags": [
              "legacy",
              "unsafe"
            ]
          },
          {
            "title": "NRT Enabled (Modern C#)",
            "description": "Reference types are non-nullable by default. Use ? to explicitly allow null. Compiler warns about unsafe null usage.",
            "pros": [
              "Catch null bugs at compile time",
              "Self-documenting code",
              "Better IntelliSense"
            ],
            "cons": [
              "Initial warnings in legacy code",
              "Requires discipline"
            ],
            "tags": [
              "modern",
              "safe"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Gradual Adoption Strategy",
        "text": "If you're working on a large existing codebase, enable NRT in the .csproj with <Nullable>enable</Nullable> and treat warnings as suggestions first (<Nullable>annotations</Nullable> or <Nullable>warnings</Nullable>). Fix warnings incrementally rather than all at once."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Add Null Safety to a Person Class",
        "description": "Refactor a simple Person class to use nullable types and NRT properly.",
        "steps": [
          "Create a Person class with string FirstName, string LastName, and int? Age properties.",
          "Add a method GetFullName() that returns a string. Use the null-coalescing operator to handle a null LastName.",
          "Add a method GetAgeDescription() that returns a string. If Age is null, return 'Age not provided'.",
          "Create a Main method that creates two Person instances: one with all fields and one with a null LastName.",
          "Call GetFullName() and GetAgeDescription() on both and print the results."
        ],
        "expectedOutcome": "A Person class that handles nulls gracefully without crashing, demonstrating nullable value types, the null-coalescing operator, and NRT syntax."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "What is the difference between int? and int? vs nullable reference types?",
            "answer": "int? (nullable value type) has existed since C# 2.0 and uses System.Nullable<int> under the hood. Nullable reference types (string?) are a C# 8+ compile-time feature that doesn't change runtime behavior — it only adds warnings."
          },
          {
            "question": "Does enabling NRT change my code at runtime?",
            "answer": "No. NRT annotations are stripped at compile time. The runtime behavior of reference types is unchanged. NRT only adds compile-time warnings and affects IDE hints."
          },
          {
            "question": "What does the ? after an array type mean?",
            "answer": "string[]? means the array itself may be null (not the elements inside it). string?[] means the array cannot be null, but individual elements may be. This distinction matters for null-checking."
          },
          {
            "question": "When should I use ?? over if/else null checks?",
            "answer": "Use ?? for simple fallback values. Use if/else when you need different logic paths, logging, or when the fallback is expensive and should be lazy."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "int? makes value types nullable; HasValue checks for null",
          "?? provides a fallback when a value is null",
          "??= assigns only if the left side is currently null",
          "?. safely accesses members on possibly-null references",
          "NRT (C# 8+) warns at compile time about null bugs",
          "! suppresses a null warning when you know the value is safe"
        ],
        "takeaway": "Nullable types turn null from a silent crash source into an explicit, trackable part of your type system. Always prefer nullable-aware code."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "csharp-fundamentals"
}
