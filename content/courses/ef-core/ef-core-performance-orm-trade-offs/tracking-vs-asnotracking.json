{
  "id": "lesson-ef-core-ef-core-performance-orm-trade-offs-tracking-vs-asnotracking",
  "slug": "tracking-vs-asnotracking",
  "moduleSlug": "ef-core-performance-orm-trade-offs",
  "courseSlug": "ef-core",
  "title": "Tracking vs AsNoTracking",
  "description": "Understand EF Core change tracking behavior and learn how AsNoTracking can dramatically improve query performance for read-only scenarios.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["EF Core", "tracking", "AsNoTracking", "performance"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "By default, EF Core tracks every entity it materializes from the database. This tracking enables features like change detection and automatic `SaveChanges()`, but it also adds memory overhead and CPU work. For read-only queries, turning off tracking with `AsNoTracking()` is one of the simplest and most effective performance optimizations available."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is change tracking?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When EF Core reads a row from the database, it creates an entity instance and stores it in its change tracker — an internal dictionary keyed by primary key. On every `SaveChanges()`, EF Core compares the current values against the originals to determine what SQL UPDATE or DELETE statements to generate. This is powerful, but it means EF Core must allocate memory and maintain state for every tracked entity."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Snapshot vs. notification tracking",
        "text": "EF Core 6+ uses snapshot-based change tracking by default, which compares current and original values at SaveChanges time. With proxy-based notification tracking, entities raise PropertyChanged events immediately, which can be faster for frequent changes but requires lazy-loading proxies."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using AsNoTracking"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`AsNoTracking()` tells EF Core to materialize entities without adding them to the change tracker. The entities are still fully populated, but EF Core will not monitor them for changes. This reduces memory usage, skips snapshot creation, and speeds up query execution — sometimes by 20-40% for large result sets."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Default tracking vs AsNoTracking",
        "code": "// Tracked: EF Core allocates and monitors each Product entity\nvar trackedProducts = await dbContext.Products\n    .Where(p => p.Category.Name == \"Electronics\")\n    .ToListAsync();\n\ntrackedProducts[0].Price = 99.99m;\nawait dbContext.SaveChangesAsync(); // Produces an UPDATE\n\n// No tracking: faster, lighter, but changes are ignored\nvar readOnlyProducts = await dbContext.Products\n    .AsNoTracking()\n    .Where(p => p.Category.Name == \"Electronics\")\n    .ToListAsync();\n\nreadOnlyProducts[0].Price = 99.99m;\nawait dbContext.SaveChangesAsync(); // No UPDATE — entity was never tracked",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to use tracking"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "You plan to modify entities and call SaveChanges() in the same DbContext scope.",
          "You are updating related entities through navigation properties.",
          "You need concurrency token handling with automatic conflict detection."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to use AsNoTracking"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Read-only API endpoints that return data without modifications.",
          "Large list or search results that are immediately serialized and returned.",
          "Queries used for reporting, analytics, or dashboard data.",
          "Any query where the result set is large and entities will not be updated."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro Tip",
        "text": "In ASP.NET Core, consider configuring a default no-tracking query filter if your DbContext is scoped per-request. This prevents accidental tracking on GET endpoints without requiring AsNoTracking() on every query."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Configuring default no-tracking behavior",
        "code": "protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)\n{\n    optionsBuilder.UseSqlServer(connectionString)\n        .UseQueryTrackingBehavior(QueryTrackingBehavior.NoTracking);\n}",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "AsNoTracking with projections"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you project into a DTO or anonymous type, EF Core does not track the result regardless of `AsNoTracking()`. The projection itself avoids tracking because the result is not an entity type. However, adding `AsNoTracking()` explicitly can still improve performance slightly by telling EF Core to skip certain query-planning steps."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Projection with AsNoTracking for maximum read-only performance",
        "code": "var summaries = await dbContext.Orders\n    .AsNoTracking()\n    .Where(o => o.OrderDate >= DateTime.UtcNow.AddMonths(-1))\n    .Select(o => new OrderSummaryDto\n    {\n        Id = o.Id,\n        CustomerName = o.Customer.FullName,\n        Total = o.OrderItems.Sum(oi => oi.Quantity * oi.UnitPrice)\n    })\n    .ToListAsync();",
        "language": "csharp"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "items": [
          "Change tracking adds memory and CPU overhead for every materialized entity.",
          "Use AsNoTracking() for read-only queries to eliminate tracking overhead.",
          "Projected DTOs are never tracked, even without AsNoTracking().",
          "Consider setting QueryTrackingBehavior.NoTracking as a default for read-heavy applications."
        ]
      }
    }
  ],
  "relatedLessons": ["linq-expression-trees-and-sql-translation", "projection-vs-include"],
  "furtherReading": [
    { "title": "EF Core change tracking overview", "url": "https://learn.microsoft.com/en-us/ef/core/change-tracking/", "type": "article" },
    { "title": "No-tracking queries", "url": "https://learn.microsoft.com/en-us/ef/core/querying/no-tracking", "type": "article" }
  ]
}
