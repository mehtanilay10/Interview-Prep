{
  "id": "lesson-ef-core-ef-core-performance-orm-trade-offs-bulk-operations-and-large-data-changes",
  "slug": "bulk-operations-and-large-data-changes",
  "moduleSlug": "ef-core-performance-orm-trade-offs",
  "courseSlug": "ef-core",
  "title": "Bulk operations and large data changes",
  "description": "Understand why EF Core SaveChanges is not designed for bulk operations and learn practical strategies for high-volume inserts, updates, and deletes.",
  "order": 10,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": ["EF Core", "bulk operations", "SaveChanges", "performance", "batch"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "EF Core's `SaveChanges()` method is designed for transactional, row-by-row data modifications. It runs change detection, validates constraints, and generates individual INSERT, UPDATE, or DELETE statements for each tracked entity. This is ideal for normal application operations, but it becomes a severe bottleneck when you need to modify thousands or millions of rows."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The SaveChanges bottleneck"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every call to `SaveChanges()` opens a transaction, detects changes on every tracked entity, and sends SQL to the database in sequence. For 10,000 new entities, this means 10,000 individual INSERT statements, each with its own round-trip cost. Even with batching enabled, the change detection and statement generation overhead scales linearly with the row count."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Standard SaveChanges for bulk insert",
        "code": "// Loading 10,000 products into the change tracker\nforeach (var product in products)\n{\n    dbContext.Products.Add(product);\n}\n\nawait dbContext.SaveChangesAsync();\n\n// Behind the scenes: 10,000 individual INSERT statements\n// each with round-trip overhead and change detection.",
        "language": "csharp"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Memory usage warning",
        "text": "Adding 10,000 entities to the change tracker consumes significant memory. Each entity stores its original and current values for change detection. For very large imports, consider processing in smaller batches to keep memory bounded."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Batching SaveChanges"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "EF Core 7+ supports batching of INSERT, UPDATE, and DELETE statements in a single round-trip. You can configure a batch size, and EF Core groups operations into batches of that size. This reduces round-trips but does not eliminate the change-tracking overhead."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Configuring batch size",
        "code": "protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)\n{\n    optionsBuilder.UseSqlServer(connectionString)\n        .UseSqlBatching(options => options.MaxBatchSize = 100);\n}",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Bulk insert libraries"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Community libraries like EFCore.BulkExtensions provide bulk operations that bypass change tracking and send data to the database in bulk. `BulkInsert`, `BulkUpdate`, and `BulkDelete` use `SqlBulkCopy` or equivalent mechanisms under the hood, achieving near-raw-SQL performance while still working with your DbContext and entity types."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Bulk insert with EFCore.BulkExtensions",
        "code": "await dbContext.BulkInsertAsync(products, new BulkConfig\n{\n    BatchSize = 5000,\n    PreserveInsertOrder = true,\n    SetOutputIdentity = true\n});\n\n// 10,000 products inserted in 2 batches\n// with minimal change-tracking overhead.",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Raw bulk with SqlBulkCopy"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For absolute maximum throughput on SQL Server, `SqlBulkCopy` streams data directly to the database in the native bulk-copy format. It requires a `DataTable` or `IDataReader` source and a destination table. This approach is the fastest but also the most manual — you lose DbContext conveniences and must handle identity columns and constraints yourself."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "SqlBulkCopy for maximum throughput",
        "code": "using var bulkCopy = new SqlBulkCopy(_connection);\nbulkCopy.DestinationTableName = \"dbo.Products\";\nbulkCopy.BatchSize = 5000;\n\nvar dataTable = new DataTable();\ndataTable.Columns.Add(\"Name\", typeof(string));\ndataTable.Columns.Add(\"Price\", typeof(decimal));\n\nforeach (var product in products)\n{\n    dataTable.Rows.Add(product.Name, product.Price);\n}\n\nawait bulkCopy.WriteToServerAsync(dataTable);",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Strategies for large data changes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Use `ExecuteUpdate` and `ExecuteDelete` (EF Core 7+) for set-based updates without loading entities.",
          "Process large imports in batches of 500-5000 rows to bound memory usage.",
          "Disable change tracking during bulk operations by using a fresh DbContext instance per batch.",
          "Use `AsNoTracking()` for read operations that feed into bulk-write logic.",
          "Consider `SqlBulkCopy` for initial data loads or nightly batch imports where speed is critical."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Set-based update with ExecuteUpdate",
        "code": "// Update 10,000 rows in a single SQL statement\n// without loading any entities\nawait dbContext.Products\n    .Where(p => p.CategoryId == oldCategoryId)\n    .ExecuteUpdateAsync(s => s\n        .SetProperty(p => p.CategoryId, newCategoryId)\n        .SetProperty(p => p.UpdatedAt, DateTime.UtcNow));",
        "language": "csharp"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "items": [
          "SaveChanges is designed for small-to-medium transactional writes, not bulk operations.",
          "Use ExecuteUpdate/ExecuteDelete for set-based changes without loading entities.",
          "Bulk libraries like EFCore.BulkExtensions provide a productivity sweet spot for large imports.",
          "SqlBulkCopy delivers maximum throughput for SQL Server bulk loads at the cost of more manual setup."
        ]
      }
    }
  ],
  "relatedLessons": ["ef-core-vs-dapper-vs-raw-sql", "when-to-bypass-the-orm"],
  "furtherReading": [
    { "title": "EF Core bulk operations", "url": "https://learn.microsoft.com/en-us/ef/core/performance/bulk-operations", "type": "article" },
    { "title": "EFCore.BulkExtensions", "url": "https://github.com/borisdj/EFCore.BulkExtensions", "type": "article" }
  ]
}
