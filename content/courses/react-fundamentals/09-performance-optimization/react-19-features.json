{
  "id": "lesson-react-037",
  "slug": "react-19-features",
  "moduleSlug": "performance-optimization",
  "courseSlug": "react-fundamentals",
  "title": "React 19 Features",
  "description": "Explore React 19 features including Actions, the use() hook, Server Components, and compiler optimizations.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 22,
  "tags": [
    "react",
    "react-19",
    "actions",
    "use-hook",
    "server-components"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React 19 introduces features that simplify data mutations, reduce client-side JavaScript, and automate optimizations. Understanding these features keeps your code modern and maintainable."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Compiler"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The React Compiler automatically memoizes components, values, and effects at build time. In many cases you can remove manual React.memo, useMemo, and useCallback calls because the compiler handles them for you."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Before and After Compiler",
        "content": "Code the compiler can optimize automatically",
        "language": "jsx",
        "code": "// Before React 19 - manual memoization\nconst MemoizedList = React.memo(function List({ items, filter }) {\n  const visible = useMemo(\n    () => items.filter(filter),\n    [items, filter]\n  );\n  return <ul>{visible.map(mapItem)}</ul>;\n});\n\n// With React 19 compiler - cleaner\nfunction List({ items, filter }) {\n  const visible = items.filter(filter);\n  return <ul>{visible.map(mapItem)}</ul>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Opt In Gradually",
        "text": "The React Compiler is opt-in. You can adopt it incrementally and keep existing manual memoization while migrating."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Actions and useActionState"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Actions are functions that handle form submissions and other mutations. useActionState lets you read the result of an action inside your component, eliminating manual loading and error state management."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useActionState",
        "content": "Using an action with form state",
        "language": "jsx",
        "code": "import { useActionState } from 'react';\n\nasync function createItem(prevState, formData) {\n  'use server';\n  const name = formData.get('name');\n  // Save item...\n  return { id: crypto.randomUUID(), name };\n}\n\nfunction CreateItemForm() {\n  const [state, formAction, isPending] = useActionState(createItem, null);\n\n  return (\n    <form action={formAction}>\n      <input name=\"name\" required />\n      <button disabled={isPending}>\n        {isPending ? 'Saving...' : 'Create'}\n      </button>\n      {state?.id && <p>Created {state.id}</p>}\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The use() Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "use() is a new hook that reads a Promise or context directly inside render. It unwraps promises and lets you use context without a separate hook when you only need it in one place."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "use() with Promises",
        "content": "Reading a promise directly in render",
        "language": "jsx",
        "code": "import { use, Suspense } from 'react';\n\nfunction Comments({ commentsPromise }) {\n  // Suspends until the promise resolves\n  const comments = use(commentsPromise);\n  return comments.map((c) => <p key={c.id}>{c.text}</p>);\n}\n\nfunction Post({ id }) {\n  const promise = fetch(`/api/posts/${id}/comments`).then((r) => r.json());\n  return (\n    <Suspense fallback={<p>Loading comments...</p>}>\n      <Comments commentsPromise={promise} />\n    </Suspense>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Server Components Basics"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React Server Components run only on the server. They can read databases, access filesystems, and keep secrets safe, then stream HTML to the client without adding JavaScript bundle size."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Server Component Rules",
        "items": [
          "Can use async/await directly in the component body.",
          "Cannot use hooks like useState or useEffect.",
          "Cannot attach browser event listeners.",
          "Data stays on the server and never ships to the client."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Ref as a Prop"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In React 19, ref is a regular prop for function components. You no longer need forwardRef to pass a ref through a component."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Ref as Prop",
        "content": "Passing ref without forwardRef",
        "language": "jsx",
        "code": "function Input({ ref, ...props }) {\n  return <input ref={ref} {...props} />;\n}\n\nfunction Form() {\n  const inputRef = useRef();\n  return <Input ref={inputRef} placeholder=\"Name\" />;\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Action",
            "definition": "An async function that handles a mutation and returns a result."
          },
          {
            "term": "useActionState",
            "definition": "A hook that tracks the state of an action, including pending status and result."
          },
          {
            "term": "use()",
            "definition": "A hook that unwraps a Promise or reads context during render."
          },
          {
            "term": "Server Component",
            "definition": "A component that renders on the server and streams HTML to the client."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-react-19",
        "caption": "How React 19 features fit together across server and client.",
        "definition": "flowchart TD\n    A[Server Components] -->|stream HTML| B[Client]\n    B --> C[use() reads Promise]\n    B --> D[Actions handle mutations]\n    D -->|revalidates| A\n    E[Compiler] -->|optimizes at build| A"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Migrate a Form to Actions",
        "description": "Convert a basic contact form to use useActionState with a simulated server action.",
        "steps": [
          "Create an async function createContact that accepts formData and returns an object.",
          "Use useActionState in the form component.",
          "Replace the onSubmit handler with an action attribute.",
          "Show a pending state while the action runs."
        ],
        "expectedOutcome": "A form that manages submission state without manual useState for loading or error."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Do I need the React Compiler to use React 19?",
            "answer": "No. The compiler is optional. You can upgrade React 19 and add the compiler later, or never."
          },
          {
            "question": "Are Server Components ready for production?",
            "answer": "They are stable in frameworks like Next.js App Router. If you use plain Vite or CRA, you will need a framework that supports them."
          },
          {
            "question": "What replaces forwardRef?",
            "answer": "Ref is now a standard prop on function components. You can pass ref directly without forwardRef, though forwardRef still works for backward compatibility."
          },
          {
            "question": "Is use() the same as useEffect?",
            "answer": "No. use() reads values during render. useEffect runs after render for side effects. Use use() for promises and context; use useEffect for subscriptions and DOM mutations."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "The React Compiler automates memoization at build time.",
          "Actions and useActionState simplify form mutations and state tracking.",
          "use() unwraps promises and context directly in render.",
          "Server Components reduce client bundle size and secure data access.",
          "Ref is now a regular prop, simplifying component APIs."
        ],
        "takeaway": "React 19 shifts complexity from developer to tooling. Embrace the compiler and Server Components to write less code with better performance."
      }
    }
  ]
}