{
  "id": "lesson-react-036",
  "slug": "performance-optimization",
  "moduleSlug": "performance-optimization",
  "courseSlug": "react-fundamentals",
  "title": "Performance Optimization",
  "description": "Make React apps faster with memoization, lazy loading, and profiling.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "performance",
    "memo",
    "useMemo",
    "useCallback"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React is fast by default, but large apps can still suffer from unnecessary re-renders, expensive calculations, and prop-drilled callback instability. This module teaches you when and how to optimize with React.memo, useMemo, and useCallback."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Performance Matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Performance is a feature. Slow interactions frustrate users and hurt retention. React's virtual DOM is efficient, but it still re-renders components whenever their props or state change. Optimization reduces wasted work."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React.memo for Component Memoization"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React.memo wraps a component and skips re-rendering when props haven't changed. It does a shallow comparison of props by default."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React.memo",
        "content": "Wrapping a component to skip re-renders",
        "language": "jsx",
        "code": "const UserCard = React.memo(function UserCard({ user }) {\n  console.log('Rendered:', user.name);\n  return (\n    <div className=\"card\">\n      <h3>{user.name}</h3>\n      <p>{user.email}</p>\n    </div>\n  );\n});\n\n// Parent re-renders, but UserCard only re-renders\n// when the user prop object reference changes."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Memoize at the Right Level",
        "text": "Don't wrap every component in memo. Start by profiling, then memoize components that receive complex objects or arrays as props and re-render often."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useMemo for Expensive Calculations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useMemo caches the result of a calculation between renders. It recomputes only when its dependencies change."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useMemo",
        "content": "Caching an expensive filter operation",
        "language": "jsx",
        "code": "function SearchResults({ items, query }) {\n  const filteredItems = useMemo(() => {\n    console.log('Filtering...');\n    return items.filter((item) =>\n      item.name.toLowerCase().includes(query.toLowerCase())\n    );\n  }, [items, query]);\n\n  return (\n    <ul>\n      {filteredItems.map((item) => (\n        <li key={item.id}>{item.name}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useCallback for Stable References"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useCallback wraps a function and returns a memoized version. This is useful when passing callbacks to optimized child components that rely on reference equality."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useCallback",
        "content": "Stabilizing a callback reference",
        "language": "jsx",
        "code": "function Parent() {\n  const [count, setCount] = useState(0);\n\n  const handleRemove = useCallback((id) => {\n    setItems((prev) => prev.filter((item) => item.id !== id));\n  }, []);\n\n  return (\n    <div>\n      <button onClick={() => setCount((c) => c + 1)}>\n        Re-render parent ({count})\n      </button>\n      <ItemList items={items} onRemove={handleRemove} />\n    </div>\n  );\n}\n\nconst ItemList = React.memo(({ items, onRemove }) => {\n  return items.map((item) => (\n    <Item key={item.id} item={item} onRemove={onRemove} />\n  ));\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Pitfalls"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Optimization Mistakes",
        "items": [
          "Memoizing everything adds overhead. Only optimize after profiling.",
          "useMemo and useCallback return new values when dependencies change, not when you want them to.",
          "Forgetting dependencies causes stale closures and bugs.",
          "Wrapping components in memo when they receive new object/array props every render still causes re-renders."
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-perf-flow",
        "caption": "How memoization reduces unnecessary renders in React.",
        "definition": "flowchart LR\n    A[Props Change?] -->|no| B[Skip Re-render]\n    A -->|yes| C[Re-render Component]\n    C --> D[Update DOM]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Optimize a Slow List",
        "description": "Profile and optimize a list component that re-renders unnecessarily.",
        "steps": [
          "Create a Parent component with a counter state and a list of 100 items.",
          "Notice the list logs 'render' on every counter increment.",
          "Wrap the list in React.memo.",
          "Wrap the remove callback in useCallback.",
          "Verify the list no longer re-renders when the counter changes."
        ],
        "expectedOutcome": "A list that only re-renders when its own data changes, not when unrelated parent state changes."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "When should I use React.memo?",
            "answer": "Use it on components that render often with the same props, or that receive complex props like objects or arrays. Always profile first with React DevTools."
          },
          {
            "question": "Is useMemo required for performance?",
            "answer": "No. React re-renders are already fast. Use useMemo only for genuinely expensive calculations that you want to cache."
          },
          {
            "question": "What is the React Compiler?",
            "answer": "React 19 includes an optional compiler that automatically memoizes values and callbacks at build time, removing the need for manual useMemo and useCallback in many cases."
          },
          {
            "question": "Can memoization cause bugs?",
            "answer": "Yes. If you depend on a function being recreated every render, memoizing it can cause stale references. Only memoize when you are sure the reference should be stable."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "React.memo skips re-renders when props are shallowly equal.",
          "useMemo caches expensive calculations.",
          "useCallback stabilizes callback references for memoized children.",
          "Profile first, optimize second — don't prematurely optimize."
        ],
        "takeaway": "Performance optimization in React is about reducing unnecessary work, not adding complexity everywhere."
      }
    }
  ]
}
