{
  "id": "lesson-react-fundamentals-react-internals-modern-rendering-render-phase-vs-commit-phase",
  "slug": "render-phase-vs-commit-phase",
  "moduleSlug": "react-internals-modern-rendering",
  "courseSlug": "react-fundamentals",
  "title": "Render phase vs commit phase",
  "description": "Understand how React splits work into the render phase and the commit phase, and why this split matters for performance, correctness, and user experience.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "react",
    "rendering",
    "fiber",
    "internals"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React updates your UI in two distinct stages: the **render phase** and the **commit phase**. Knowing what happens in each stage helps you predict when side effects run, why certain code must be pure, and how React keeps the interface responsive during heavy updates."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What the render phase does"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In the render phase, React calls your component functions and figures out what the UI should look like. It walks the component tree, collects the new elements, and compares them with the previous result. This work is **pure** — it should not touch the DOM, read layout, or cause side effects like API calls."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Component functions execute and return JSX",
          "React reconciles the new tree against the previous tree",
          "The browser is not updated yet",
          "This work can be paused, abandoned, or restarted"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What the commit phase does"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Once React knows exactly what changed, it enters the commit phase. Here React mutates the DOM, inserts new nodes, removes old ones, and runs layout effects. This phase is **synchronous and uninterruptible** — the user sees every change as it happens."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "DOM mutations are applied",
          "`useLayoutEffect` callbacks run immediately after mutations",
          "The browser paints the updated frame",
          "`useEffect` callbacks run after the paint"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why the split matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Because the render phase can be interrupted, React can split large updates across multiple frames. If the user types while React is rendering, React can pause, let the browser handle the input, and resume later. This is the foundation of concurrent rendering."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Render phase vs commit phase in practice",
        "content": "The following example shows how state updates trigger both phases, and where side effects belong.",
        "code": "import { useState, useEffect, useLayoutEffect } from 'react';\n\nfunction SearchResults({ query }) {\n  const [results, setResults] = useState([]);\n\n  // Render phase: pure calculation only\n  const visibleResults = results.filter((item) =>\n    item.title.toLowerCase().includes(query.toLowerCase())\n  );\n\n  // Commit phase: side effects after DOM is updated\n  useEffect(() => {\n    document.title = `${visibleResults.length} results for \"${query}\"`;\n    analytics.track('search', { query, count: visibleResults.length });\n  }, [query, visibleResults.length]);\n\n  return (\n    <ul>\n      {visibleResults.map((item) => (\n        <li key={item.id}>{item.title}</li>\n      ))}\n    </ul>\n  );\n}",
        "language": "typescript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key terms"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Render phase",
            "definition": "The phase where React calculates the next UI tree by calling components and reconciling changes. It is pure and interruptible."
          },
          {
            "term": "Commit phase",
            "definition": "The phase where React applies changes to the DOM and runs effects. It is synchronous and cannot be interrupted."
          },
          {
            "term": "Side effect",
            "definition": "Any operation that interacts with the outside world: DOM reads, API requests, subscriptions, or logging."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Keep the render phase pure",
        "text": "Never read layout, make API calls, or schedule timeouts during the render phase. Doing so can cause inconsistent UI, tearing, or lost updates when React pauses and resumes rendering."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "The render phase is pure and interruptible; the commit phase is synchronous and side-effect heavy",
          "Effects run after the commit, not during render",
          "Keeping render pure lets React pause work without corrupting state"
        ],
        "takeaway": "If your component crashes or behaves oddly during updates, first check whether you are accidentally mixing side effects into the render phase."
      }
    },
    {
      "type": "relatedLessons",
      "data": {
        "slugs": [
          "reconciliation-and-component-identity",
          "fiber-and-scheduling-concepts"
        ]
      }
    },
    {
      "type": "furtherReading",
      "data": [
        {
          "title": "React Docs: How React Works",
          "url": "https://react.dev/learn/render-and-commit",
          "type": "article"
        },
        {
          "title": "React Docs: Synchronous vs Concurrent Rendering",
          "url": "https://react.dev/reference/react#concurrent-rendering",
          "type": "article"
        }
      ]
    }
  ],
  "relatedLessons": [
    "reconciliation-and-component-identity",
    "fiber-and-scheduling-concepts"
  ],
  "furtherReading": [
    {
      "title": "React Docs: Render and Commit",
      "url": "https://react.dev/learn/render-and-commit",
      "type": "article"
    },
    {
      "title": "React Docs: Synchronous vs Concurrent Rendering",
      "url": "https://react.dev/reference/react#concurrent-rendering",
      "type": "article"
    }
  ]
}
