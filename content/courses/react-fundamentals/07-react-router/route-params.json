{
  "id": "lesson-react-027",
  "slug": "route-params",
  "moduleSlug": "react-router",
  "courseSlug": "react-fundamentals",
  "title": "Route Parameters",
  "description": "Learn how to use dynamic segments and query parameters.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "router",
    "params",
    "dynamic"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Route parameters** let you create dynamic URLs that match changing values, such as user IDs or product slugs. React Router extracts these values so your component can fetch or display the right data."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "URL Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Define a parameter by prefixing a segment with a colon. Access it inside the component with the `useParams` hook."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "URL Params",
        "content": "Defining and reading dynamic segments",
        "language": "jsx",
        "code": "import { useParams } from 'react-router-dom';\n\n// Define route with parameter\n<Route path=\"/users/:userId\" element={<UserProfile />} />\n\n// Access parameter\nfunction UserProfile() {\n  const { userId } = useParams();\n\n  return (\n    <div>\n      <h1>User {userId}</h1>\n      {/* Fetch and display user data */}\n    </div>\n  );\n}\n\n// URL /users/123 renders: User 123"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can define multiple parameters in a single path. Destructure them all from `useParams()`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple Params",
        "content": "Using two dynamic segments",
        "language": "jsx",
        "code": "<Route path=\"/posts/:postId/comments/:commentId\" element={<Comment />} />\n\nfunction Comment() {\n  const { postId, commentId } = useParams();\n  return (\n    <div>\n      <p>Post: {postId}</p>\n      <p>Comment: {commentId}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Query parameters live after the `?` in a URL. Use `useSearchParams` to read and update them."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Query Params",
        "content": "Reading and setting query parameters",
        "language": "jsx",
        "code": "import { useSearchParams } from 'react-router-dom';\n\nfunction SearchResults() {\n  const [searchParams, setSearchParams] = useSearchParams();\n  const query = searchParams.get('q') || '';\n  const page = Number(searchParams.get('page')) || 1;\n\n  const goToPage = (newPage) => {\n    setSearchParams({ q: query, page: String(newPage) });\n  };\n\n  return (\n    <div>\n      <p>Search: {query}</p>\n      <p>Page: {page}</p>\n      <button onClick={() => goToPage(page + 1)}>Next</button>\n    </div>\n  );\n}\n\n// URL /search?q=react&page=2"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Optional Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Make a parameter optional by appending a question mark: `/users/:userId?`. The value will be undefined when omitted."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Optional Param",
        "content": "Optional userId parameter",
        "language": "jsx",
        "code": "<Route path=\"/users/:userId?\" element={<UserProfile />} />\n\nfunction UserProfile() {\n  const { userId } = useParams();\n  // userId is a string or undefined\n  return <h1>{userId ? `User ${userId}` : 'All Users'}</h1>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Params Are Strings",
        "text": "useParams always returns strings. Convert to numbers with Number() or parseInt() when needed."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "URL Parameter",
            "definition": "A dynamic segment of a URL path, defined with a colon prefix."
          },
          {
            "term": "Query Parameter",
            "definition": "Key-value pairs after the ? in a URL, used for filtering, pagination, and search."
          },
          {
            "term": "useParams",
            "definition": "A hook that returns an object of URL parameters."
          },
          {
            "term": "useSearchParams",
            "definition": "A hook for reading and writing query parameters."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-route-params",
        "caption": "How React Router extracts dynamic parameters from URLs.",
        "definition": "flowchart LR\n    A[URL: /users/42] --> B[Route: /users/:userId]\n    B --> C[useParams]\n    C --> D[{ userId: '42' }]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Product Page",
        "description": "Create a dynamic product page using route parameters.",
        "steps": [
          "Define a route /products/:productId.",
          "Create a ProductPage component that reads productId from useParams.",
          "Fetch product data from a mock API using the ID.",
          "Display the product name, price, and description."
        ],
        "expectedOutcome": "A product detail page that loads data based on the URL parameter."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I use regex in route params?",
            "answer": "React Router v6 supports path patterns like /users/:userId(\\d+) to restrict param formats."
          },
          {
            "question": "What happens if the parameter is missing?",
            "answer": "useParams returns undefined. Always handle missing values gracefully."
          },
          {
            "question": "How do I parse query params manually?",
            "answer": "Use URLSearchParams: `new URLSearchParams(window.location.search).get('q')`. useSearchParams is preferred because it integrates with React Router."
          },
          {
            "question": "Can params change without unmounting the component?",
            "answer": "Yes. When the same component matches a new param value, it receives new params in the same render. Include the param in useEffect dependencies to refetch data."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Define params with :paramName in route paths.",
          "Access params with useParams and query params with useSearchParams.",
          "Params are always strings; convert when needed.",
          "Make params optional with a trailing question mark."
        ],
        "takeaway": "Route parameters turn static routes into dynamic, data-driven pages. Next, you will learn programmatic navigation."
      }
    }
  ]
}
