{
  "id": "lesson-react-024",
  "slug": "forwarding-refs",
  "moduleSlug": "context-refs",
  "courseSlug": "react-fundamentals",
  "title": "Forwarding Refs",
  "description": "Learn how to pass refs through components to their children.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "refs",
    "forwardRef",
    "components"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Ref forwarding** lets a component receive a ref from its parent and pass it down to a child DOM element. This is essential for building reusable component libraries where the parent needs direct access to a nested element."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why forwardRef?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, refs are not passed through components like props. If a parent gives a ref to a functional component, it receives null unless the component explicitly forwards it."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Without forwardRef",
        "content": "Ref does not reach the child DOM element",
        "language": "jsx",
        "code": "function CustomInput(props) {\n  return <input {...props} />;\n}\n\nfunction Form() {\n  const ref = useRef();\n\n  return <CustomInput ref={ref} />; // ref is null\n}\n\n// Clicking the button does nothing because ref.current is null"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using forwardRef"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Wrap the component with `forwardRef`. The ref becomes the second argument. Assign it to the child DOM element."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "forwardRef",
        "content": "Passing refs through components",
        "language": "jsx",
        "code": "import { forwardRef, useRef } from 'react';\n\nconst CustomInput = forwardRef(function CustomInput(props, ref) {\n  return <input ref={ref} {...props} />;\n});\n\nfunction Form() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <>\n      <CustomInput ref={inputRef} placeholder=\"Name\" />\n      <button onClick={focusInput}>Focus</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useImperativeHandle"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For more control, use `useImperativeHandle` with forwardRef. It lets you expose only specific methods to the parent, hiding the rest of the child implementation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useImperativeHandle",
        "content": "Exposing a limited API to the parent",
        "language": "jsx",
        "code": "import { forwardRef, useImperativeHandle, useRef } from 'react';\n\nconst VideoPlayer = forwardRef(function VideoPlayer(props, ref) {\n  const internalRef = useRef();\n\n  useImperativeHandle(ref, () => ({\n    play() { internalRef.current.play(); },\n    pause() { internalRef.current.pause(); },\n    // Parent cannot call internal methods directly\n  }));\n\n  return <video ref={internalRef} src=\"/video.mp4\" />;\n});\n\nfunction App() {\n  const playerRef = useRef();\n\n  return (\n    <>\n      <VideoPlayer ref={playerRef} />\n      <button onClick={() => playerRef.current.play()}>Play</button>\n      <button onClick={() => playerRef.current.pause()}>Pause</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Use Cases"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "When to Forward Refs",
        "items": [
          "Building reusable input components that need focus management.",
          "Creating button components that need to trigger animations.",
          "Wrapping third-party components that accept refs.",
          "Exposing specific methods from a child to a parent."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Avoid Overusing forwardRef",
        "text": "Only forward refs when the parent genuinely needs DOM access. If the child can handle the behavior internally, keep the ref private."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "forwardRef",
            "definition": "A React utility that passes a ref through a component to a child."
          },
          {
            "term": "useImperativeHandle",
            "definition": "A hook that customizes the instance value exposed to parent refs."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-forward-ref",
        "caption": "How forwardRef passes a parent ref through to a child DOM node.",
        "definition": "flowchart LR\n    A[Parent ref] --> B[forwardRef]\n    B --> C[Child Component]\n    C --> D[DOM Element]\n    D --> E[Parent can focus/measure]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Focusable Button",
        "description": "Create a custom button that forwards its ref and exposes focus.",
        "steps": [
          "Create a CustomButton component using forwardRef.",
          "Forward the ref to the underlying <button>.",
          "Create a parent component that holds a ref to the custom button.",
          "Add a 'Focus Button' button that calls ref.current.focus()."
        ],
        "expectedOutcome": "A custom button that can be focused programmatically through a forwarded ref."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Do I need forwardRef for class components?",
            "answer": "No. Class components automatically receive refs as instance properties. forwardRef is only needed for function components."
          },
          {
            "question": "Can I pass a ref to any component?",
            "answer": "To DOM elements and class components, yes. To function components, only if they are wrapped in forwardRef."
          },
          {
            "question": "Why use useImperativeHandle instead of forwarding the raw ref?",
            "answer": "It protects the child's internals by exposing only the methods you choose. The parent cannot access private state or methods."
          },
          {
            "question": "Is forwardRef used in production libraries?",
            "answer": "Yes. Material UI, Chakra UI, React Bootstrap, and most component libraries use forwardRef to let consumers access underlying DOM nodes."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "forwardRef passes parent refs through function components.",
          "The ref is the second argument to the forwarded component.",
          "useImperativeHandle exposes a limited API to the parent.",
          "Use forwarding when building reusable component libraries."
        ],
        "takeaway": "Forwarding refs is essential for building flexible, accessible component libraries. You have now completed the Context and Refs module."
      }
    }
  ]
}
