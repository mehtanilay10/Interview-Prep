{
  "id": "lesson-react-022",
  "slug": "usecontext-hook",
  "moduleSlug": "context-refs",
  "courseSlug": "react-fundamentals",
  "title": "The useContext Hook",
  "description": "Master consuming context values with the useContext hook.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "react",
    "usecontext",
    "context",
    "hooks"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useContext** is the hook for consuming context values. It accepts a context object and returns the current context value from the nearest Provider above the calling component."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Usage"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Call `useContext` at the top level of your component. It returns whatever value the nearest Provider supplied."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useContext",
        "content": "Consuming a theme context",
        "language": "jsx",
        "code": "const ThemeContext = createContext('light');\n\nfunction ThemedButton() {\n  const theme = useContext(ThemeContext);\n  return <button className={theme}>Styled by theme</button>;\n}\n\n// Usage\nfunction App() {\n  return (\n    <ThemeContext.Provider value=\"dark\">\n      <ThemedButton />\n    </ThemeContext.Provider>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Reading Complex Values"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Context values are often objects that include both data and updater functions. Destructure them for readability."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Theme Provider with Toggle",
        "content": "A complete theme context implementation",
        "language": "jsx",
        "code": "const ThemeContext = createContext();\n\nfunction ThemeProvider({ children }) {\n  const [theme, setTheme] = useState('light');\n  const toggleTheme = () => setTheme((t) => (t === 'light' ? 'dark' : 'light'));\n\n  return (\n    <ThemeContext.Provider value={{ theme, toggleTheme }}>\n      {children}\n    </ThemeContext.Provider>\n  );\n}\n\nfunction Header() {\n  const { theme, toggleTheme } = useContext(ThemeContext);\n  return (\n    <header className={theme}>\n      <button onClick={toggleTheme}>Toggle Theme</button>\n    </header>\n  );\n}\n\nfunction App() {\n  return (\n    <ThemeProvider>\n      <Header />\n      <Main />\n    </ThemeProvider>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple Contexts"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use multiple useContext calls in one component to consume different contexts. This keeps components decoupled and testable."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple Contexts",
        "content": "Consuming theme, user, and locale contexts",
        "language": "jsx",
        "code": "function Header() {\n  const { theme } = useContext(ThemeContext);\n  const { user } = useContext(UserContext);\n  const { locale } = useContext(LocaleContext);\n\n  return (\n    <header className={theme}>\n      <p>Hello, {user.name}!</p>\n      <p>Language: {locale}</p>\n    </header>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Custom Hook for Context"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a custom hook to consume context with a helpful error message if used outside the Provider."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useTheme Hook",
        "content": "Wrapping useContext with validation",
        "language": "jsx",
        "code": "function useTheme() {\n  const context = useContext(ThemeContext);\n  if (!context) {\n    throw new Error('useTheme must be used within a ThemeProvider');\n  }\n  return context;\n}\n\n// Usage — cleaner and safer\nfunction Header() {\n  const { theme, toggleTheme } = useTheme();\n  return <button onClick={toggleTheme}>{theme}</button>;\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "useContext",
            "definition": "A hook that reads the current value from a context."
          },
          {
            "term": "Provider Value",
            "definition": "The data supplied by a Provider to all consuming descendants."
          },
          {
            "term": "Custom Context Hook",
            "definition": "A wrapper around useContext that provides error checking and a cleaner API."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-usecontext",
        "caption": "useContext reads the nearest Provider value in the component tree.",
        "definition": "flowchart TD\n    A[Provider value: dark] --> B[Child]\n    B --> C[useContext]\n    C --> D[Receives 'dark']"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build an Auth Context",
        "description": "Create an auth context to manage user login state across the app.",
        "steps": [
          "Create AuthContext with createContext.",
          "Build AuthProvider that manages a user object and login/logout functions.",
          "Create a useAuth custom hook.",
          "Build a Header that shows 'Login' or the user's name depending on auth state."
        ],
        "expectedOutcome": "An auth context that lets any component read and update the current user."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "What if I forget to wrap a component in a Provider?",
            "answer": "useContext returns the default value passed to createContext. Use a custom hook with an error check to catch this early."
          },
          {
            "question": "Does useContext cause re-renders?",
            "answer": "Yes. Any component that calls useContext re-renders when the Provider value changes. Split contexts to limit the blast radius."
          },
          {
            "question": "Can I update context from a consumer?",
            "answer": "Yes. Include updater functions in the Provider value, or use the useState setter inside the Provider and pass it down."
          },
          {
            "question": "Should I memoize the Provider value?",
            "answer": "If the value is an object or array created inline, wrap it in useMemo to avoid unnecessary re-renders of consumers."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useContext reads the nearest Provider value.",
          "Wrap context consumption in a custom hook for error safety.",
          "Multiple contexts can be consumed in the same component.",
          "Memoize Provider values to avoid extra re-renders."
        ],
        "takeaway": "useContext is the cleanest way to consume global data. Next, you will learn useRef for DOM access and mutable values."
      }
    }
  ]
}