{
  "id": "lesson-react-023",
  "slug": "useref-hook",
  "moduleSlug": "context-refs",
  "courseSlug": "react-fundamentals",
  "title": "The useRef Hook",
  "description": "Learn how to access DOM elements and persist values across renders.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "useref",
    "refs",
    "dom"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useRef** serves two purposes: accessing DOM elements directly and storing mutable values that persist across renders without causing re-renders when changed."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accessing DOM Elements"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Attach a ref to any JSX element with the `ref` attribute. After React mounts the component, `ref.current` points to the DOM node."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Focus an Input",
        "content": "Using useRef to focus an input programmatically",
        "language": "jsx",
        "code": "import { useRef } from 'react';\n\nfunction TextInputWithFocusButton() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <>\n      <input ref={inputRef} type=\"text\" placeholder=\"Type here\" />\n      <button onClick={focusInput}>Focus the input</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Persisting Values Across Renders"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useRef values survive across renders but do not trigger re-renders when mutated. This is useful for storing previous values, IDs, or any mutable value you don't want in state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Previous Value Tracker",
        "content": "Tracking the previous state value with useRef",
        "language": "jsx",
        "code": "function Counter() {\n  const [count, setCount] = useState(0);\n  const prevCountRef = useRef();\n\n  useEffect(() => {\n    prevCountRef.current = count;\n  }, [count]);\n\n  const prevCount = prevCountRef.current;\n\n  return (\n    <div>\n      <p>Now: {count}, Before: {prevCount}</p>\n      <button onClick={() => setCount((c) => c + 1)}>+</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useRef vs useState"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "useRef",
          "useState"
        ],
        "rows": [
          [
            "Does not trigger re-render",
            "Triggers re-render"
          ],
          [
            "Same object across renders",
            "New value each render"
          ],
          [
            "For mutable values and DOM refs",
            "For reactive state"
          ]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Ref Object",
        "text": "useRef returns `{ current: initialValue }`. You can read and write `ref.current` at any time without triggering React updates."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common useRef Patterns"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "What Developers Use useRef For",
        "items": [
          "Focusing inputs, buttons, or other focusable elements.",
          "Measuring DOM dimensions or positions.",
          "Storing interval or timeout IDs for cleanup.",
          "Tracking previous values without causing re-renders.",
          "Integrating with third-party DOM libraries."
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Ref",
            "definition": "An object with a current property that persists across renders."
          },
          {
            "term": "Mutable",
            "definition": "A value that can be changed without notifying React."
          },
          {
            "term": "DOM Node",
            "definition": "An element in the browser's Document Object Model."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-useref",
        "caption": "useRef creates a stable container that survives re-renders.",
        "definition": "flowchart LR\n    A[useRef] --> B[Ref Object]\n    B --> C[Persists across renders]\n    C --> D[No re-render triggered]\n    D --> E[Mutable .current]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Stopwatch",
        "description": "Create a stopwatch that starts, stops, and resets using useRef.",
        "steps": [
          "Create a Stopwatch component.",
          "Use useState for the displayed time.",
          "Use useRef to store the interval ID so you can clear it on stop or unmount.",
          "Add Start, Stop, and Reset buttons."
        ],
        "expectedOutcome": "A working stopwatch that cleans up its interval correctly."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I read ref.current during render?",
            "answer": "Yes, but only for values that do not depend on the DOM layout. For measurements that need the painted DOM, use useLayoutEffect."
          },
          {
            "question": "Why doesn't changing ref.current re-render the component?",
            "answer": "That is by design. Refs are for values that should not trigger UI updates. If you need a re-render, use state."
          },
          {
            "question": "Can I useRef inside a conditional?",
            "answer": "No. Like all hooks, useRef must be called unconditionally at the top level."
          },
          {
            "question": "What is the difference between useRef and createRef?",
            "answer": "createRef creates a new ref object on every render. useRef returns the same ref object across renders, making it suitable for persisting values."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useRef persists values across renders without triggering re-renders.",
          "Attach refs to DOM elements with the ref attribute.",
          "Use refs for focus, measurements, timers, and previous values.",
          "Prefer useRef over createRef in functional components."
        ],
        "takeaway": "useRef gives you escape hatches for DOM access and mutable values. Next, you will learn to pass refs through components with forwardRef."
      }
    }
  ]
}
