{
  "id": "lesson-react-021",
  "slug": "react-context",
  "moduleSlug": "context-refs",
  "courseSlug": "react-fundamentals",
  "title": "React Context",
  "description": "Learn how to share data across components without prop drilling.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "context",
    "state",
    "prop-drilling"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Context** provides a way to pass data through the component tree without manually threading props through every intermediate level. It solves the **prop drilling** problem cleanly."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Prop Drilling Problem"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a deeply nested child needs data from a top-level parent, you must pass that data through every component in between — even if those intermediate components do not use it."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Prop Drilling",
        "content": "Passing props through components that don't need them",
        "language": "jsx",
        "code": "// Without context — props pass through every level\nfunction App() {\n  const [user, setUser] = useState({ name: 'Alice' });\n  return <Layout user={user} />;\n}\n\nfunction Layout({ user }) {\n  return <Header user={user} />; // Doesn't use user\n}\n\nfunction Header({ user }) {\n  return <UserMenu user={user} />; // Doesn't use user\n}\n\nfunction UserMenu({ user }) {\n  return <p>{user.name}</p>; // Finally uses user\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating Context"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a context object with `createContext()`. Wrap your tree with a Provider that supplies the value. Any descendant can then read that value."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create and Provide Context",
        "content": "Setting up a user context",
        "language": "jsx",
        "code": "import { createContext, useContext, useState } from 'react';\n\n// 1. Create context with a default value\nconst UserContext = createContext(null);\n\n// 2. Provide context at a high level\nfunction App() {\n  const [user, setUser] = useState({ name: 'Alice' });\n\n  return (\n    <UserContext.Provider value={{ user, setUser }}>\n      <Layout />\n    </UserContext.Provider>\n  );\n}\n\n// 3. Consume anywhere in the tree\nfunction UserMenu() {\n  const { user } = useContext(UserContext);\n  return <p>{user.name}</p>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Context"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Good Use Cases",
        "items": [
          "**Theming** — dark/light mode across the app.",
          "**Authentication** — current user data.",
          "**Language / locale** — internationalization.",
          "**Notifications** — global toast or alert system.",
          "**Settings** — user preferences."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Don't Overuse Context",
        "text": "Context is great for truly global data. For component-specific communication, props are simpler. Overusing context makes components harder to reuse and test."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Default Values"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The value passed to `createContext` is a fallback used only when a component reads the context outside a Provider. Always provide a sensible default."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Default Value",
        "content": "Using a default value in createContext",
        "language": "jsx",
        "code": "const ThemeContext = createContext('light');\n\n// Outside any provider, this returns 'light'\nfunction SomeRandomComponent() {\n  const theme = useContext(ThemeContext); // 'light'\n  return <p>{theme}</p>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple Contexts"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can nest multiple Providers. Components consume whichever contexts they need. For complex apps, consider splitting contexts by concern (theme, auth, locale) rather than one mega-context."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-context-tree",
        "caption": "Context flows from Provider to any descendant, skipping intermediate components.",
        "definition": "flowchart TD\n    A[Provider] --> B[Intermediate Component]\n    B --> C[Intermediate Component]\n    C --> D[Consumer]\n    A -.->|context skips B & C| D"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Context",
            "definition": "A mechanism for passing data through the component tree without prop drilling."
          },
          {
            "term": "Provider",
            "definition": "A component that accepts a value prop and makes it available to descendants."
          },
          {
            "term": "Prop Drilling",
            "definition": "Passing props through components that do not need them."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Theme Context",
        "description": "Create a theme context and consume it in deeply nested components.",
        "steps": [
          "Create ThemeContext with createContext.",
          "Wrap the app with ThemeProvider that holds a theme state.",
          "Create a deeply nested Button component that reads the theme from context.",
          "Add a toggle in the header that updates the theme."
        ],
        "expectedOutcome": "A theme toggle that updates the entire app without prop drilling."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "How many contexts should I create?",
            "answer": "Create one per concern. Separate theme, auth, and locale into different contexts. One mega-context causes unnecessary re-renders."
          },
          {
            "question": "Does Context replace Redux?",
            "answer": "For simple global state, yes. For complex state with time-travel debugging, middleware, or fine-grained subscriptions, Redux or Zustand may still be preferable."
          },
          {
            "question": "Do all consumers re-render when context changes?",
            "answer": "Yes, by default. All components using useContext re-render when the Provider value changes. Split contexts to minimize re-renders."
          },
          {
            "question": "Can I update context from a consumer?",
            "answer": "Yes. Pass both the value and an updater function in the context value object, or use useState inside the Provider."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Context eliminates prop drilling by broadcasting data to descendants.",
          "Create with createContext, provide with Provider, consume with useContext.",
          "Use context for truly global data, not for component-local sharing.",
          "Split contexts by concern to avoid unnecessary re-renders."
        ],
        "takeaway": "Context is a powerful tool for escaping prop drilling. Next, you will master the useContext hook."
      }
    }
  ]
}