{
  "id": "lesson-react-033",
  "slug": "conditional-rendering",
  "moduleSlug": "components-props",
  "courseSlug": "react-fundamentals",
  "title": "Conditional Rendering",
  "description": "Learn how to show different UI based on conditions using ternary operators, logical &&, if statements, and switch.",
  "order": 5,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "conditional",
    "rendering",
    "ternary",
    "logical-and"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Conditional rendering lets your UI adapt based on state, props, or any JavaScript expression. React doesn't have built-in if/else tags, but JavaScript gives you several clean patterns inside JSX."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Conditional Rendering?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Real-world apps rarely show the same UI to every user. A logged-in user sees a profile menu; a guest sees a login button. A loading state shows a spinner; an error shows a message. Conditional rendering is how you express these differences in React."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Element Variables"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Store JSX in variables and include them in your return. This keeps the JSX readable and avoids deeply nested ternaries."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Element Variables",
        "content": "Assigning JSX to variables for conditional rendering",
        "language": "jsx",
        "code": "function UserGreeting({ isLoggedIn }) {\n  let message;\n\n  if (isLoggedIn) {\n    message = <h1>Welcome back!</h1>;\n  } else {\n    message = <h1>Please sign in.</h1>;\n  }\n\n  return (\n    <div>\n      {message}\n      {isLoggedIn && <UserAvatar />}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Ternary Operator"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use the ternary operator for inline if/else. It returns one of two values based on a condition."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Ternary in JSX",
        "content": "Inline conditional with ternary",
        "language": "jsx",
        "code": "function Greeting({ isLoggedIn }) {\n  return (\n    <div>\n      {isLoggedIn ? (\n        <h1>Welcome back!</h1>\n      ) : (\n        <h1>Please sign in.</h1>\n      )}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Keep Ternaries Simple",
        "text": "If the condition logic grows beyond a simple variable, extract it into a function or use element variables instead. Deeply nested ternaries are hard to read."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Logical && Operator"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you only need to render something when a condition is true (and render nothing otherwise), use the logical && operator."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Logical &&",
        "content": "Rendering only when condition is true",
        "language": "jsx",
        "code": "function Mailbox({ unreadMessages }) {\n  return (\n    <div>\n      <h1>Hello!</h1>\n      {unreadMessages.length > 0 && (\n        <p>You have {unreadMessages.length} unread messages.</p>\n      )}\n    </div>\n  );\n}\n\n// unreadMessages = [] renders nothing\n// unreadMessages = [msg1] renders the <p>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Rendering Null for Nothing"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Returning null from a component tells React to render nothing. Components that return null are useful as wrappers that only render children under certain conditions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Null Return",
        "content": "Component that renders nothing",
        "language": "jsx",
        "code": "function WarningBanner({ showWarning }) {\n  if (!showWarning) {\n    return null;\n  }\n\n  return (\n    <div className=\"warning\">\n      Warning!\n    </div>\n  );\n}\n\n// Usage\n<WarningBanner showWarning={false} /> // renders nothing"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Inline If with Logical && and Return "
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Combine element variables with early returns for maximum clarity."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Early Return Pattern",
        "content": "Returning null early to avoid nesting",
        "language": "jsx",
        "code": "function UserProfile({ user }) {\n  if (!user) {\n    return <p>Loading user...</p>;\n  }\n\n  if (user.error) {\n    return <div className=\"error\">Failed to load user.</div>;\n  }\n\n  return (\n    <div className=\"profile\">\n      <h1>{user.name}</h1>\n      <p>{user.email}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Switch Statements for Multiple Conditions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you have more than two branches, extract the logic into a function or use a switch outside the JSX."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Switch Statement",
        "content": "Multiple conditional branches",
        "language": "jsx",
        "code": "function StatusMessage({ status }) {\n  let message;\n\n  switch (status) {\n    case 'loading':\n      message = <Spinner />;\n      break;\n    case 'success':\n      message = <SuccessCheckmark />;\n      break;\n    case 'error':\n      message = <ErrorAlert />;\n      break;\n    default:\n      message = null;\n  }\n\n  return <div className=\"status\">{message}</div>;\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Conditional Rendering",
            "definition": "Displaying different UI based on conditions like state, props, or permissions."
          },
          {
            "term": "Ternary Operator",
            "definition": "A concise if/else expression: condition ? trueValue : falseValue."
          },
          {
            "term": "Logical AND (&&)",
            "definition": "Renders the right side only when the left side is truthy."
          },
          {
            "term": "Early Return",
            "definition": "Returning null or an alternate component before the main JSX to avoid rendering."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Best Practices"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Conditional Rendering Tips",
        "items": [
          "Prefer element variables and early returns over deeply nested ternaries.",
          "Use && for 'show something if true' cases.",
          "Use ternary when you need both true and false branches.",
          "Extract complex conditions into helper functions with descriptive names.",
          "Always handle loading and error states for async data."
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-conditional-rendering",
        "caption": "How conditional rendering decides which UI branch to display.",
        "definition": "flowchart LR\n    A[Condition] -->|true| B[Render Component A]\n    A -->|false| C[Render Component B]\n    A -->|null/undefined| D[Render Nothing]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Notification Bell",
        "description": "Create a notification bell that shows a badge only when there are unread notifications.",
        "steps": [
          "Create a NotificationBell component that accepts a notifications array prop.",
          "Use conditional rendering to show a red badge with the count when notifications.length > 0.",
          "Show a plain bell icon when there are zero notifications.",
          "Use the && operator for the badge and a ternary for the icon text."
        ],
        "expectedOutcome": "A bell icon with a count badge that appears only when notifications exist."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I use if/else directly inside JSX?",
            "answer": "No. JSX is JavaScript expressions, not statements. Use ternary (condition ? A : B), && (condition && A), or move logic outside the return."
          },
          {
            "question": "What happens if the && left side is 0 or an empty string?",
            "answer": "React renders 0 or the empty string, not null. Only false, null, undefined, and true are ignored. Be careful with numbers and empty strings."
          },
          {
            "question": "Should I create a separate component for each conditional view?",
            "answer": "Yes, when the conditional UI is complex. Keep the parent clean by delegating to smaller components like Loading, ErrorMessage, or UserCard."
          },
          {
            "question": "How do I handle loading, error, and empty states together?",
            "answer": "Use early returns at the top of your component: if (loading) return <Spinner />; if (error) return <ErrorMessage />; if (!data) return <EmptyState />; Then render the main UI."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Conditional rendering adapts the UI to state, props, or user permissions.",
          "Use element variables and early returns for clarity.",
          "Ternary handles true/false branches; && handles show-or-nothing.",
          "Return null to render nothing from a component.",
          "Extract complex conditions into helper functions."
        ],
        "takeaway": "Conditional rendering turns static templates into adaptive user interfaces. Next, you'll learn to render dynamic lists from data."
      }
    }
  ]
}
