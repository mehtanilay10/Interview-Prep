{
  "id": "lesson-react-006",
  "slug": "props-basics",
  "moduleSlug": "components-props",
  "courseSlug": "react-fundamentals",
  "title": "Props Basics",
  "description": "Learn how to pass data to components using props.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 18,
  "tags": [
    "react",
    "props",
    "data",
    "components"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Props** (short for properties) let you pass data from a parent component to a child component. Props are read-only and make components reusable — the same component can display different data depending on the props it receives."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Passing Props"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Props look like HTML attributes. You can pass strings, numbers, booleans, objects, arrays, and functions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Passing Different Types",
        "content": "Strings, numbers, booleans, objects, and functions as props",
        "language": "jsx",
        "code": "// String\n<Welcome name=\"Alice\" />\n\n// Number\n<Age value={25} />\n\n// Boolean\n<IsActive isActive={true} />\n\n// Object\n<User info={{ name: 'Alice', age: 25 }} />\n\n// Array\n<Colors palette={['red', 'green', 'blue']} />\n\n// Function\n<Button onClick={() => alert('Clicked!')} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Receiving Props"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Inside a functional component, props arrive as a single object. You can access properties with dot notation or destructure them directly in the parameter list."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Accessing Props",
        "content": "Dot notation and destructuring",
        "language": "jsx",
        "code": "// Dot notation\nfunction UserCard(props) {\n  return <h2>{props.name}</h2>;\n}\n\n// Destructuring in parameters\nfunction UserCard({ name, email }) {\n  return (\n    <div>\n      <h2>{name}</h2>\n      <p>{email}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Destructure for Readability",
        "text": "Destructuring props in the function signature keeps JSX clean and makes it obvious which props the component expects."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Props Are Read-Only"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Never modify props inside a child component. Props are owned by the parent. If a child needs to change a value, the parent should pass an updater function as a prop instead."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Do Not Mutate Props",
        "content": "Correct and incorrect prop usage",
        "language": "jsx",
        "code": "// WRONG — mutating props\nfunction Counter(props) {\n  props.count = props.count + 1; // Do not do this\n  return <p>{props.count}</p>;\n}\n\n// CORRECT — props are read-only data\nfunction Counter({ count }) {\n  return <p>Count: {count}</p>;\n}\n\n// Parent passes count as a prop and updates it with its own state\n<Counter count={5} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "One-Way Data Flow"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React follows a strict **one-way data flow**: data moves down from parent to child via props. This makes the app predictable — you can trace where any value came from by following the props."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-one-way-data",
        "caption": "Data flows down from parent to child via props.",
        "definition": "flowchart TD\n    A[Parent State] -->|props| B[Child A]\n    A -->|props| C[Child B]\n    B -->|no reverse flow| A"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Props",
            "definition": "Read-only inputs passed from parent to child components."
          },
          {
            "term": "One-Way Data Flow",
            "definition": "Data flows from parent to child; children cannot modify parent data directly."
          },
          {
            "term": "Destructuring",
            "definition": "Extracting values from an object into individual variables."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Pass Props Through Three Levels",
        "description": "Practice passing props through multiple component layers.",
        "steps": [
          "Create a GrandParent component with a 'title' state.",
          "Create a Parent component that receives title and a subtitle as props.",
          "Create a Child component that receives both and renders them in an h1 and h2.",
          "Render GrandParent in App.jsx and verify the data flows correctly."
        ],
        "expectedOutcome": "Data passes from grandparent to child without breaking the one-way flow rule."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I change props inside a child?",
            "answer": "No. Props are read-only. If a child needs to modify a value, the parent should lift the state up and pass a setter function as a prop."
          },
          {
            "question": "What happens if I pass a prop that the child does not use?",
            "answer": "React ignores extra props. They do not cause errors, but they increase memory usage slightly."
          },
          {
            "question": "Can props be any JavaScript type?",
            "answer": "Yes. Props can be strings, numbers, booleans, objects, arrays, functions, and even other React elements."
          },
          {
            "question": "Are props shared across all instances of a component?",
            "answer": "No. Each instance receives its own props object from its parent. Changes to one instance's props do not affect others."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Props pass data from parent to child components.",
          "Props are read-only — children cannot modify them.",
          "Use destructuring for cleaner component signatures.",
          "React enforces one-way data flow for predictability."
        ],
        "takeaway": "Props are the foundation of component communication. Next, you will explore advanced prop patterns."
      }
    }
  ]
}