{
  "id": "lesson-react-034",
  "slug": "lists-and-keys",
  "moduleSlug": "components-props",
  "courseSlug": "react-fundamentals",
  "title": "Lists and Keys",
  "description": "Learn how to render arrays of data using map, assign stable keys, and filter or sort lists.",
  "order": 6,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "lists",
    "keys",
    "map",
    "rendering"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Rendering lists of items is one of the most common tasks in React. You'll use JavaScript's map() method to turn arrays into elements, assign keys for stable updates, and learn when using an array index is acceptable."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Rendering Lists with map()"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The map() method transforms each item in an array into a React element. Because JSX expressions must have one root, the result of map() is usually wrapped in a container element or a Fragment."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic List Rendering",
        "content": "Using map to render a list of items",
        "language": "jsx",
        "code": "function NumberList({ numbers }) {\n  return (\n    <ul>\n      {numbers.map((number) => (\n        <li key={number.toString()}>{number}</li>\n      ))}\n    </ul>\n  );\n}\n\n// Usage\n<NumberList numbers={[1, 2, 3, 4, 5]} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Keys Help React Identify Items"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Keys are special React attributes that help the reconciler match items between renders. A stable key lets React reuse DOM nodes, move elements efficiently, and preserve component state."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Key",
            "definition": "A string or number that uniquely identifies an item among its siblings."
          },
          {
            "term": "Reconciliation",
            "definition": "React's algorithm for comparing the previous tree with the next tree and deciding what to update."
          },
          {
            "term": "Stable Key",
            "definition": "A key that does not change between renders for the same logical item."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Rendering Product Cards",
        "content": "Keys with complex data objects",
        "language": "jsx",
        "code": "function ProductList({ products }) {\n  return (\n    <div className=\"grid\">\n      {products.map((product) => (\n        <ProductCard key={product.id} product={product} />\n      ))}\n    </div>\n  );\n}\n\nconst products = [\n  { id: 'p1', name: 'Laptop', price: 999 },\n  { id: 'p2', name: 'Phone', price: 699 },\n  { id: 'p3', name: 'Tablet', price: 449 },\n];"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Index as Key"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Using the array index as a key is only safe when the list is static — it never reorders, filters, or inserts items. If the list changes, index keys cause subtle bugs: state can leak to the wrong item, animations break, and performance suffers."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Key Strategies",
        "cards": [
          {
            "title": "Stable ID (Recommended)",
            "description": "Use a unique ID from your data.",
            "pros": [
              "Stable across renders",
              "Correctly preserves component state",
              "Efficient reordering and filtering"
            ],
            "tags": [
              "recommended"
            ]
          },
          {
            "title": "Array Index",
            "description": "Use the item's position in the array.",
            "pros": [
              "Works for static lists"
            ],
            "cons": [
              "Breaks with reordering or filtering",
              "Can leak state between items",
              "Poor performance for large dynamic lists"
            ],
            "tags": [
              "avoid-for-dynamic-lists"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Never Use Index for Dynamic Lists",
        "text": "If your list supports sorting, filtering, adding, or removing items, always use a stable ID from your data as the key."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Extracting List Items as Components"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When each list item has its own logic, extract it into a separate component. The parent still owns the data and passes each item via props."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ListItem Component",
        "content": "Separating list items into their own component",
        "language": "jsx",
        "code": "function ListItem({ item, onRemove }) {\n  return (\n    <li className=\"list-item\">\n      <span>{item.name}</span>\n      <button onClick={() => onRemove(item.id)}>Remove</button>\n    </li>\n  );\n}\n\nfunction PackingList({ items, onRemove }) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <ListItem key={item.id} item={item} onRemove={onRemove} />\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Filtering and Transforming Lists"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Combine map with filter and sort when the displayed list differs from the source data."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Filter and Map",
        "content": "Filtering a list before rendering",
        "language": "jsx",
        "code": "function ActiveUsers({ users }) {\n  const activeUsers = users\n    .filter((user) => user.isActive)\n    .sort((a, b) => a.name.localeCompare(b.name));\n\n  return (\n    <ul>\n      {activeUsers.map((user) => (\n        <li key={user.id}>{user.name}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-list-rendering",
        "caption": "The map-filter-render flow for displaying lists in React.",
        "definition": "flowchart LR\n    A[Raw Array] --> B[filter]\n    B --> C[map to Elements]\n    C --> D[Assign Stable Keys]\n    D --> E[Render to DOM]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Todo List",
        "description": "Render a list of todo items from an array and allow filtering by completion status.",
        "steps": [
          "Create a TodoList component that receives a todos array.",
          "Render each todo as an <li> with its text and a checkbox.",
          "Use the todo.id as the key.",
          "Add a filter button that shows only incomplete todos using .filter() before .map()."
        ],
        "expectedOutcome": "A list of todos that updates correctly when filtered, without losing checkbox state."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Do keys have to be globally unique?",
            "answer": "No. Keys only need to be unique among siblings. Two different lists can both use key='1' without conflict."
          },
          {
            "question": "Can keys be numbers?",
            "answer": "Yes. React accepts strings or numbers as keys. Numbers are automatically converted to strings internally."
          },
          {
            "question": "Why does React warn about keys?",
            "answer": "Keys help React identify which items changed, were added, or were removed. Without keys, React re-renders the entire list by default, which hurts performance and can break component state."
          },
          {
            "question": "Should I pass the key to the child component as a prop?",
            "answer": "No. Keys are reserved by React and are not available inside the child component via props. If the child needs an ID, pass it separately as a prop like id={item.id}."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use map() to transform arrays into JSX elements.",
          "Keys must be stable and unique among siblings.",
          "Prefer data IDs over array indexes for dynamic lists.",
          "Extract complex list items into separate components.",
          "Filter and sort data before mapping to JSX."
        ],
        "takeaway": "Lists and keys are the bridge between raw data and rendered UI. Master these and you can display any dataset in React."
      }
    }
  ]
}
