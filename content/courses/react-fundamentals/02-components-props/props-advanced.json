{
  "id": "lesson-react-007",
  "slug": "props-advanced",
  "moduleSlug": "components-props",
  "courseSlug": "react-fundamentals",
  "title": "Advanced Props Patterns",
  "description": "Learn default props, prop types, spread attributes, and children prop.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "props",
    "default-props",
    "children",
    "spread"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Once you know basic props, you can use advanced patterns to make components more flexible, safer, and easier to compose. This lesson covers default values, the special children prop, spread attributes, and render props."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Default Props"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Provide fallback values so components still work when a prop is omitted. In functional components, use JavaScript default parameters."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Default Parameters",
        "content": "Setting fallback values for props",
        "language": "jsx",
        "code": "function Button({ text = 'Click me', variant = 'primary' }) {\n  return <button className={`btn btn-${variant}`}>{text}</button>;\n}\n\n// Uses defaults\n<Button />\n\n// Overrides\n<Button text=\"Submit\" variant=\"success\" />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Children Prop"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `children` prop contains everything placed between the opening and closing tags of a component. It is the foundation of component composition and layout wrappers."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using Children",
        "content": "A layout wrapper that renders children",
        "language": "jsx",
        "code": "function Card({ title, children }) {\n  return (\n    <div className=\"card\">\n      {title && <h2 className=\"card-title\">{title}</h2>}\n      <div className=\"card-body\">{children}</div>\n    </div>\n  );\n}\n\n// Usage\n<Card title=\"Welcome\">\n  <p>This content goes inside the card body.</p>\n  <button>Action</button>\n</Card>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Spread Attributes"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Spread an object's keys as individual props. This is useful when forwarding props to a child or when passing many props from a data object."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Spread Operator",
        "content": "Passing an object's properties as props",
        "language": "jsx",
        "code": "const user = {\n  firstName: 'Alice',\n  lastName: 'Smith',\n  email: 'alice@example.com',\n  role: 'admin'\n};\n\n// Without spread\n<UserProfile\n  firstName={user.firstName}\n  lastName={user.lastName}\n  email={user.email}\n  role={user.role}\n/>\n\n// With spread\n<UserProfile {...user} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Render Props Pattern"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A render prop is a function prop that returns JSX. The parent controls what to render while the child component controls the logic. This is powerful for sharing behavior."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Render Prop",
        "content": "A component that shares mouse position via a render prop",
        "language": "jsx",
        "code": "function MouseTracker({ render }) {\n  const [pos, setPos] = useState({ x: 0, y: 0 });\n\n  useEffect(() => {\n    const move = (e) => setPos({ x: e.clientX, y: e.clientY });\n    window.addEventListener('mousemove', move);\n    return () => window.removeEventListener('mousemove', move);\n  }, []);\n\n  return <div>{render(pos)}</div>;\n}\n\n// Usage — parent decides what to render\n<MouseTracker\n  render={({ x, y }) => (\n    <p>Mouse at ({x}, {y})</p>\n  )}\n/>"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Render Props vs Custom Hooks",
        "text": "Render props share UI logic by wrapping JSX. Custom hooks share non-visual logic by returning values. Prefer hooks when you don't need JSX composition."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Children Prop",
            "definition": "A special prop that captures content placed between a component's opening and closing tags."
          },
          {
            "term": "Spread Attributes",
            "definition": "Using ...obj to pass all properties of an object as individual props."
          },
          {
            "term": "Render Prop",
            "definition": "A function prop that returns JSX, giving the parent control over rendering."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Modal",
        "description": "Create a Modal component using the children prop and a controlled isOpen prop.",
        "steps": [
          "Create a Modal component that accepts isOpen, onClose, title, and children.",
          "Render a semi-transparent overlay and a centered box when isOpen is true.",
          "Close the modal when the overlay is clicked or the Escape key is pressed.",
          "Use the modal in App.jsx and pass custom children."
        ],
        "expectedOutcome": "A reusable modal that opens and closes with overlay and Escape key support."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I use children with any component?",
            "answer": "Yes. Every component implicitly receives a children prop if content is placed between its tags. You do not need to declare it in the function signature."
          },
          {
            "question": "Is spreading props a security risk?",
            "answer": "Only if the source object contains unexpected properties. Spread only objects you control and avoid passing user input directly."
          },
          {
            "question": "When should I use render props?",
            "answer": "When a child component needs to share logic or data-driven rendering with many different parents. For non-visual logic, custom hooks are usually cleaner."
          },
          {
            "question": "How do I type children in TypeScript?",
            "answer": "Use React.ReactNode for the children prop type: `children: React.ReactNode`."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use default parameters to provide fallback prop values.",
          "children enables component composition and layout wrappers.",
          "Spread attributes simplify passing many props from an object.",
          "Render props share rendering logic between components."
        ],
        "takeaway": "Advanced prop patterns make components flexible and composable. Next, you'll learn how to combine components into full page layouts."
      }
    }
  ]
}