{
  "id": "lesson-react-009",
  "slug": "useState-hook",
  "moduleSlug": "state-events",
  "courseSlug": "react-fundamentals",
  "title": "The useState Hook",
  "description": "Learn how to add state to functional components using the useState hook.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "state",
    "useState",
    "hooks"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**State** is data that changes over time in your application. The `useState` hook is the most fundamental React hook — it lets you add reactive state to functional components so the UI updates automatically when the data changes."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How useState Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Call `useState` with an initial value. It returns an array with two elements: the current state value and a function to update it. By convention we destructure these into meaningful variable names."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useState Basics",
        "content": "Adding a counter to a component",
        "language": "jsx",
        "code": "import { useState } from 'react';\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Increment\n      </button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple State Variables"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Call `useState` as many times as you need. Each call manages an independent piece of state. Group related values into one object only if they always change together."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple States",
        "content": "Managing separate state variables",
        "language": "jsx",
        "code": "function Form() {\n  const [username, setUsername] = useState('');\n  const [email, setEmail] = useState('');\n  const [age, setAge] = useState(0);\n\n  return (\n    <form>\n      <input\n        value={username}\n        onChange={(e) => setUsername(e.target.value)}\n      />\n      <input\n        value={email}\n        onChange={(e) => setEmail(e.target.value)}\n      />\n      <input\n        type=\"number\"\n        value={age}\n        onChange={(e) => setAge(Number(e.target.value))}\n      />\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "State with Objects"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When storing an object in state, always create a new object when updating. React uses reference equality to detect changes, so mutating the existing object will not trigger a re-render."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Object State",
        "content": "Updating object state without mutation",
        "language": "jsx",
        "code": "function UserProfile() {\n  const [user, setUser] = useState({\n    firstName: 'John',\n    lastName: 'Doe',\n    age: 30\n  });\n\n  const updateAge = () => {\n    // WRONG — mutates state directly\n    // user.age = 31;\n\n    // CORRECT — creates a new object\n    setUser({ ...user, age: 31 });\n  };\n\n  return (\n    <div>\n      <p>{user.firstName} {user.lastName}</p>\n      <p>Age: {user.age}</p>\n      <button onClick={updateAge}>Birthday!</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Never Mutate State Directly",
        "text": "Direct mutation is invisible to React. Always create a new object or array when updating state."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Functional Updates"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When the new state depends on the previous state, pass a function to the setter. This guarantees you always work with the latest value, even if multiple updates happen in quick succession."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Functional Updates",
        "content": "Using a function form for safe state updates",
        "language": "jsx",
        "code": "// Risky — may use a stale count\n<button onClick={() => setCount(count + 1)}>+</button>\n\n// Safer — always uses the latest state\n<button onClick={() => setCount(prev => prev + 1)}>+</button>\n\n// Multiple updates — each sees the previous result\nfunction incrementThreeTimes() {\n  setCount(prev => prev + 1);\n  setCount(prev => prev + 1);\n  setCount(prev => prev + 1);\n  // Final count increases by 3"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Lazy Initialization"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If the initial state requires an expensive computation, pass a function to `useState`. React calls that function once during the initial render."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Lazy Initial State",
        "content": "Deferring expensive initial computation",
        "language": "jsx",
        "code": "const [items, setItems] = useState(() => {\n  const stored = localStorage.getItem('items');\n  return stored ? JSON.parse(stored) : [];\n});"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "State",
            "definition": "Mutable data inside a component that triggers re-renders when changed."
          },
          {
            "term": "Setter Function",
            "definition": "The second value returned by useState; updates state and schedules a re-render."
          },
          {
            "term": "Functional Update",
            "definition": "Passing a function to the setter to compute new state from the previous state."
          },
          {
            "term": "Re-render",
            "definition": "React calling the component function again to produce new UI."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-useState",
        "caption": "How useState manages state and triggers re-renders.",
        "definition": "flowchart LR\n    A[setState called] --> B[Schedule re-render]\n    B --> C[Component function runs]\n    C --> D[New JSX returned]\n    D --> E[Virtual DOM diffed]\n    E --> F[Real DOM updated]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Counter",
        "description": "Create a counter with increment, decrement, and reset buttons.",
        "steps": [
          "Create a Counter component in src/components/Counter.jsx.",
          "Initialize count to 0 with useState.",
          "Add three buttons: Increment, Decrement, Reset.",
          "Use functional updates for increment and decrement.",
          "Use direct value update for reset."
        ],
        "expectedOutcome": "A working counter that updates correctly and resets to zero."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why is the setter function named setCount and not updateCount?",
            "answer": "Naming is a convention. `set` + capitalized state name (setCount, setUser) is the React community standard."
          },
          {
            "question": "Can I use useState without importing it?",
            "answer": "No. Hooks must be imported from 'react'. React relies on call order, so do not conditionally call hooks."
          },
          {
            "question": "Does calling setState immediately update the variable?",
            "answer": "No. State updates are asynchronous. React batches updates and applies them during the next render."
          },
          {
            "question": "When should I use useReducer instead of useState?",
            "answer": "Use useReducer when state logic is complex, involves multiple sub-values, or when the next state depends on the previous state in a non-trivial way."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useState adds reactive state to functional components.",
          "It returns [value, setter] — destructure both.",
          "Never mutate state directly; always create new values.",
          "Use functional updates when new state depends on old state.",
          "Lazy initialization avoids expensive work on every render."
        ],
        "takeaway": "State powers dynamic, interactive UIs. Next, you will learn how user actions trigger state changes through events."
      }
    }
  ]
}