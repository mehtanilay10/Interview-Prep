{
  "id": "lesson-react-011",
  "slug": "state-best-practices",
  "moduleSlug": "state-events",
  "courseSlug": "react-fundamentals",
  "title": "State Best Practices",
  "description": "Learn patterns and best practices for managing state effectively.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "state",
    "best-practices",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Good state management makes apps predictable and easy to debug. This lesson teaches you where to put state, when to lift it up, how to group related values, and when to avoid storing derived data."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Keep State as Local as Possible"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Only keep state in a component if no other component needs that data. Local state reduces re-renders and makes components self-contained and reusable."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Local vs Shared State",
        "content": "Deciding where to put state",
        "language": "jsx",
        "code": "// Local state — only this component needs it\nfunction Toggle() {\n  const [isOn, setIsOn] = useState(false);\n  return <button onClick={() => setIsOn(!isOn)}>{isOn ? 'ON' : 'OFF'}</button>;\n}\n\n// Shared state — lift to the nearest common parent\nfunction Parent() {\n  const [count, setCount] = useState(0);\n  return (\n    <div>\n      <Display count={count} />\n      <Incrementor onIncrement={() => setCount(count + 1)} />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Lifting State Up"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When multiple components need the same changing data, move it to their closest common parent. This is called **lifting state up**."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Lifting State Up",
        "content": "Sharing state between siblings",
        "language": "jsx",
        "code": "// Before: state trapped in one child\nfunction TempInput({ scale }) {\n  const [temp, setTemp] = useState(0);\n  // Sibling cannot read this state!\n}\n\n// After: state lifted to parent\nfunction Calculator() {\n  const [temp, setTemp] = useState(0);\n  const [scale, setScale] = useState('c');\n\n  return (\n    <>\n      <TempInput temp={temp} scale=\"c\" onChange={setTemp} />\n      <TempInput temp={temp} scale=\"f\" onChange={setTemp} />\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Grouping Related State"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If two state values always change together, group them into a single object. This keeps related data synchronized and reduces the chance of inconsistent UI."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Grouped State",
        "content": "Combining related values into one state object",
        "language": "jsx",
        "code": "// Separate states — must update both manually\nconst [x, setX] = useState(0);\nconst [y, setY] = useState(0);\n\n// Grouped state — update atomically\nconst [position, setPosition] = useState({ x: 0, y: 0 });\n\nconst move = (dx, dy) => {\n  setPosition((prev) => ({ x: prev.x + dx, y: prev.y + dy }));\n};"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Avoid Derived State"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Do not store values in state that can be calculated from other state or props during render. Duplicating data creates a second source of truth and invites bugs."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Derived Values",
        "content": "Calculating values instead of duplicating them in state",
        "language": "jsx",
        "code": "// WRONG — duplicated state\nfunction Cart({ items }) {\n  const [total, setTotal] = useState(0);\n  useEffect(() => {\n    setTotal(items.reduce((sum, i) => sum + i.price, 0));\n  }, [items]);\n}\n\n// CORRECT — calculate during render\nfunction Cart({ items }) {\n  const total = items.reduce((sum, i) => sum + i.price, 0);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Single Source of Truth"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Each piece of data should have exactly one owner. If two components need the same value, lift it to their nearest common ancestor or use Context."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-state-lift",
        "caption": "Lifting state up to the nearest common parent.",
        "definition": "flowchart TD\n    A[Parent] -->|prop| B[Child 1]\n    A -->|prop| C[Child 2]\n    A -->|state| D[Shared Value]\n    D --> B\n    D --> C"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Lifting State Up",
            "definition": "Moving state to a common parent so multiple children can share it."
          },
          {
            "term": "Derived State",
            "definition": "A value computed from props or other state; should not be duplicated in state."
          },
          {
            "term": "Single Source of Truth",
            "definition": "Each piece of data lives in exactly one place."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Refactor a Temperature Converter",
        "description": "Identify derived state and lift shared state to the parent.",
        "steps": [
          "Create a TemperatureConverter with Celsius and Fahrenheit inputs.",
          "Initially, put state in each input separately.",
          "Notice the inputs get out of sync.",
          "Lift the temperature state to the parent and pass it down.",
          "Verify both inputs now stay in sync."
        ],
        "expectedOutcome": "A converter where both inputs reflect the same temperature because they share a single state owner."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "When should I use Context instead of lifting state up?",
            "answer": "Use Context when prop drilling through 3+ levels becomes painful. For sibling communication, lifting state up is simpler and more explicit."
          },
          {
            "question": "Should every piece of state be at the top level?",
            "answer": "No. Keep state as low as possible. Only lift it when multiple components genuinely need the same live data."
          },
          {
            "question": "Is useEffect required for derived values?",
            "answer": "No. Calculate derived values during render. useEffect adds complexity and can cause stale closures."
          },
          {
            "question": "What if my state object becomes too large?",
            "answer": "Consider splitting it into multiple useState calls, using useReducer, or normalizing the shape with an ID map."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Keep state local unless multiple components need it.",
          "Lift shared state to the nearest common parent.",
          "Group values that always change together.",
          "Never store derived values in state — calculate them during render.",
          "Maintain a single source of truth for every piece of data."
        ],
        "takeaway": "Good state architecture prevents bugs before they happen. Next, you'll apply these patterns to forms with controlled components."
      }
    }
  ]
}