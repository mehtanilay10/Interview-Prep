{
  "id": "lesson-react-013",
  "slug": "form-validation",
  "moduleSlug": "forms-input",
  "courseSlug": "react-fundamentals",
  "title": "Form Validation",
  "description": "Learn how to validate form inputs and display error messages.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "forms",
    "validation",
    "errors"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Form validation ensures users enter correct data before submission. React makes it easy to validate inputs and provide immediate feedback by storing errors in state and conditionally rendering messages."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Validation Strategies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can validate on every keystroke, when the user leaves a field (on blur), or only when the form is submitted. For the best user experience, combine real-time and submit-time validation."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "When to Validate",
        "items": [
          "**On change** — instant feedback, but can be annoying if too aggressive.",
          "**On blur** — validates when the user leaves the field; a great middle ground.",
          "**On submit** — catches all errors at once; required as a safety net."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Single Field Validation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Store the error message in state alongside the value. Clear the error once the input becomes valid."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Email Validation",
        "content": "Validating an email field",
        "language": "jsx",
        "code": "function EmailField() {\n  const [email, setEmail] = useState('');\n  const [error, setError] = useState('');\n\n  const validate = (value) => {\n    if (!value.trim()) return 'Email is required';\n    if (!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(value)) return 'Invalid email format';\n    return '';\n  };\n\n  const handleChange = (e) => {\n    const value = e.target.value;\n    setEmail(value);\n    setError(validate(value));\n  };\n\n  const handleBlur = () => {\n    setError(validate(email));\n  };\n\n  return (\n    <div>\n      <label>Email</label>\n      <input\n        value={email}\n        onChange={handleChange}\n        onBlur={handleBlur}\n        className={error ? 'input-error' : ''}\n      />\n      {error && <span className=\"error-text\">{error}</span>}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multi-Field Form Validation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For forms with many fields, keep errors in an object where each key matches a field name."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Registration Validation",
        "content": "Validating multiple fields together",
        "language": "jsx",
        "code": "function RegisterForm() {\n  const [form, setForm] = useState({ username: '', email: '', password: '' });\n  const [errors, setErrors] = useState({});\n\n  const validate = () => {\n    const newErrors = {};\n\n    if (!form.username.trim()) newErrors.username = 'Username is required';\n\n    if (!form.email.trim()) {\n      newErrors.email = 'Email is required';\n    } else if (!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(form.email)) {\n      newErrors.email = 'Enter a valid email';\n    }\n\n    if (!form.password) {\n      newErrors.password = 'Password is required';\n    } else if (form.password.length < 8) {\n      newErrors.password = 'Password must be at least 8 characters';\n    }\n\n    return newErrors;\n  };\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    const newErrors = validate();\n    setErrors(newErrors);\n\n    if (Object.keys(newErrors).length === 0) {\n      console.log('Submit:', form);\n    }\n  };\n\n  // ... render form with error display\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Validation on Blur"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Validate when the user leaves a field rather than on every keystroke. This avoids frustration while still providing timely feedback."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Blur Validation",
        "content": "Validating when the user leaves a field",
        "language": "jsx",
        "code": "function ValidatedInput({ name, label, error, onBlur, onChange, value }) {\n  return (\n    <div className=\"form-group\">\n      <label htmlFor={name}>{label}</label>\n      <input\n        id={name}\n        name={name}\n        value={value}\n        onChange={onChange}\n        onBlur={onBlur}\n        className={error ? 'input-error' : ''}\n        aria-invalid={!!error}\n      />\n      {error && <span className=\"error-message\" role=\"alert\">{error}</span>}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Accessibility Matters",
        "text": "Use aria-invalid and role=\"alert\" for error messages so screen readers announce them. Always link labels to inputs with htmlFor and id."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Validation",
            "definition": "Checking that user input meets defined rules before processing."
          },
          {
            "term": "On Blur",
            "definition": "Validating when the user leaves a field, balancing feedback speed with annoyance."
          },
          {
            "term": "Error Object",
            "definition": "A state object that maps field names to error messages."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Validate a Contact Form",
        "description": "Add validation to a contact form with name, email, and message fields.",
        "steps": [
          "Create a ContactForm component with three controlled inputs.",
          "Validate that name is not empty, email matches a regex, and message is at least 10 characters.",
          "Show errors on blur and on submit.",
          "Disable the submit button when errors exist."
        ],
        "expectedOutcome": "A contact form that prevents invalid submissions and guides the user with clear error messages."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Should I validate on the client, server, or both?",
            "answer": "Always both. Client validation improves UX, but server validation is mandatory for security because client code can be bypassed."
          },
          {
            "question": "Can I use a regex for all validation?",
            "answer": "Regex is great for format checks (email, phone). For business rules (password strength, uniqueness), combine regex with custom logic."
          },
          {
            "question": "What is debouncing in validation?",
            "answer": "Delaying validation slightly after the user stops typing to avoid excessive re-renders and annoying feedback."
          },
          {
            "question": "Should I use a form library?",
            "answer": "For simple forms, manual validation is fine. For complex forms with many fields, consider React Hook Form or Formik for built-in validation and performance."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Validate on change, blur, and submit for the best UX.",
          "Store errors in state and render them conditionally.",
          "Use accessible markup: labels, htmlFor, aria-invalid, role=alert.",
          "Client validation is for UX; server validation is for security."
        ],
        "takeaway": "Good validation catches errors early and guides users toward correct input. Next, you'll learn how to submit the form data to a server."
      }
    }
  ]
}
