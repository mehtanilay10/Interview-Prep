{
  "id": "lesson-react-016",
  "slug": "file-uploads",
  "moduleSlug": "forms-input",
  "courseSlug": "react-fundamentals",
  "title": "File Uploads",
  "description": "Learn how to handle file uploads in React forms.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "react",
    "forms",
    "files",
    "upload"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "File uploads are a special case in React because `<input type=\"file\">` is an **uncontrolled** component — React cannot set its value for security reasons. Instead, you read the selected file from the DOM and send it to the server using FormData."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic File Input"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Access the selected file via `e.target.files`. This FileList contains File objects with metadata like name, size, and type."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Single File Upload",
        "content": "Uploading a single file",
        "language": "jsx",
        "code": "function FileUpload() {\n  const [file, setFile] = useState(null);\n  const [status, setStatus] = useState('idle');\n\n  const handleChange = (e) => {\n    setFile(e.target.files[0]);\n  };\n\n  const handleSubmit = async () => {\n    if (!file) return;\n\n    const formData = new FormData();\n    formData.append('file', file);\n\n    setStatus('uploading');\n    try {\n      const res = await fetch('/api/upload', { method: 'POST', body: formData });\n      if (!res.ok) throw new Error('Upload failed');\n      setStatus('success');\n    } catch (err) {\n      setStatus('error');\n    }\n  };\n\n  return (\n    <div>\n      <input type=\"file\" onChange={handleChange} />\n      {file && <p>Selected: {file.name}</p>}\n      <button onClick={handleSubmit} disabled={!file}>Upload</button>\n      {status === 'success' && <p>Upload complete!</p>}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple Files"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Add the `multiple` attribute to accept multiple files. Convert the FileList to an array with `Array.from()`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple File Upload",
        "content": "Handling multiple selected files",
        "language": "jsx",
        "code": "function MultiUpload() {\n  const [files, setFiles] = useState([]);\n\n  const handleChange = (e) => {\n    setFiles(Array.from(e.target.files));\n  };\n\n  return (\n    <div>\n      <input type=\"file\" multiple onChange={handleChange} />\n      <ul>\n        {files.map((file, i) => (\n          <li key={i}>\n            {file.name} ({(file.size / 1024).toFixed(1)} KB)\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Drag and Drop"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use the native drag and drop API to let users drop files into a zone. Handle dragOver, dragLeave, and drop events."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Drag and Drop Zone",
        "content": "A reusable drop zone component",
        "language": "jsx",
        "code": "function DropZone({ onFiles }) {\n  const [isDragging, setIsDragging] = useState(false);\n\n  const handleDragOver = (e) => {\n    e.preventDefault();\n    setIsDragging(true);\n  };\n\n  const handleDrop = (e) => {\n    e.preventDefault();\n    setIsDragging(false);\n    const dropped = Array.from(e.dataTransfer.files);\n    onFiles(dropped);\n  };\n\n  return (\n    <div\n      onDragOver={handleDragOver}\n      onDragLeave={() => setIsDragging(false)}\n      onDrop={handleDrop}\n      className={isDragging ? 'drag-over' : 'drag-idle'}\n    >\n      <p>Drop files here</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "File Validation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Always validate file type, size, and count on both client and server. Client validation improves UX; server validation is essential for security."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Validation Checklist",
        "items": [
          "Check `file.type` against an allowlist (e.g., image/png, application/pdf).",
          "Check `file.size` against a maximum (e.g., 5 MB).",
          "Limit the number of files with `files.length`.",
          "Show clear error messages for rejected files."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Never Trust the Client",
        "text": "Client-side validation can be bypassed. Always validate files again on the server before storing or processing them."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "FormData",
            "definition": "A browser API for constructing multipart/form-data requests, used for file uploads."
          },
          {
            "term": "Uncontrolled Input",
            "definition": "An input whose value is managed by the DOM, not React state."
          },
          {
            "term": "Drag and Drop",
            "definition": "A native browser API for accepting dragged files onto a drop zone."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-file-upload",
        "caption": "The file upload flow from selection to server.",
        "definition": "flowchart LR\n    A[User selects file] --> B[Read File from input]\n    B --> C[Validate type and size]\n    C -->|valid| D[Create FormData]\n    D --> E[POST to server]\n    C -->|invalid| F[Show error]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Image Uploader",
        "description": "Build an image uploader with preview and validation.",
        "steps": [
          "Create an ImageUploader component with a file input and drag-and-drop zone.",
          "Accept only image files under 2 MB.",
          "Show a preview using URL.createObjectURL.",
          "Simulate an upload with setTimeout and show success or error."
        ],
        "expectedOutcome": "An image uploader that validates files, shows a preview, and simulates server upload."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why can't I control the file input value?",
            "answer": "Browsers block setting file input values for security. If JavaScript could set the file path, it could upload files without the user's knowledge."
          },
          {
            "question": "What is the maximum file size I should allow?",
            "answer": "It depends on your server, but 5–10 MB is common for images. Check your hosting and CDN limits."
          },
          {
            "question": "How do I upload files with additional fields?",
            "answer": "Append both files and text fields to the same FormData object before sending."
          },
          {
            "question": "Can I upload files with fetch?",
            "answer": "Yes. Set method to POST and body to a FormData instance. Do not set Content-Type; the browser sets the multipart boundary automatically."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "File inputs are uncontrolled; read values from the DOM.",
          "Use FormData to send files to the server.",
          "Validate file type and size before uploading.",
          "Drag and drop improves UX for file selection."
        ],
        "takeaway": "File uploads are a special case in React, but with the right patterns they are straightforward to implement."
      }
    }
  ]
}