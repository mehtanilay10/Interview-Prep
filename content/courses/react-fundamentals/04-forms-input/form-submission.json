{
  "id": "lesson-react-014",
  "slug": "form-submission",
  "moduleSlug": "forms-input",
  "courseSlug": "react-fundamentals",
  "title": "Form Submission",
  "description": "Learn how to handle form submission and API calls.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "forms",
    "submission",
    "api",
    "async"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Form submission connects the frontend to the backend. You must prevent the browser's default behavior, validate the data, make an async request, and provide clear feedback during and after the submission."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Form Submission"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every form handler starts by calling `e.preventDefault()` to stop the page from reloading. Then gather the data from state and send it to an API endpoint."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Form Submit Handler",
        "content": "Submitting form data with fetch",
        "language": "jsx",
        "code": "function ContactForm() {\n  const [name, setName] = useState('');\n  const [message, setMessage] = useState('');\n\n  const handleSubmit = async (e) => {\n    e.preventDefault();\n\n    try {\n      const response = await fetch('/api/contact', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ name, message })\n      });\n\n      if (!response.ok) throw new Error('Network error');\n\n      const data = await response.json();\n      console.log('Success:', data);\n      setName('');\n      setMessage('');\n    } catch (err) {\n      console.error('Submission failed:', err);\n    }\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input value={name} onChange={(e) => setName(e.target.value)} />\n      <textarea value={message} onChange={(e) => setMessage(e.target.value)} />\n      <button type=\"submit\">Send</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Loading and Error States"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Track whether the request is in progress and whether it failed. Display these states in the UI so the user knows what is happening."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Loading and Error State",
        "content": "Showing feedback during submission",
        "language": "jsx",
        "code": "function SubmitForm() {\n  const [isLoading, setIsLoading] = useState(false);\n  const [error, setError] = useState(null);\n\n  const handleSubmit = async (e) => {\n    e.preventDefault();\n    setIsLoading(true);\n    setError(null);\n\n    try {\n      await submitData();\n    } catch (err) {\n      setError(err.message);\n    } finally {\n      setIsLoading(false);\n    }\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      {/* form fields */}\n      <button type=\"submit\" disabled={isLoading}>\n        {isLoading ? 'Submitting...' : 'Submit'}\n      </button>\n      {error && <div className=\"error\">{error}</div>}\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Disabling the Submit Button"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Disable the button while loading to prevent duplicate submissions. Re-enable it when the request finishes, whether it succeeded or failed."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Optimistic Updates",
        "text": "For snappy UX, update the UI immediately and rollback if the request fails. This makes the app feel instant while keeping data consistent."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Success Feedback"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Show a success message, clear the form, or redirect the user after a successful submission."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Success State",
        "content": "Displaying success feedback",
        "language": "jsx",
        "code": "function ContactForm() {\n  const [status, setStatus] = useState('idle'); // idle | loading | success | error\n\n  const handleSubmit = async (e) => {\n    e.preventDefault();\n    setStatus('loading');\n\n    try {\n      await fetch('/api/contact', { method: 'POST', body: JSON.stringify(form) });\n      setStatus('success');\n    } catch {\n      setStatus('error');\n    }\n  };\n\n  if (status === 'success') {\n    return <p className=\"success\">Message sent!</p>;\n  }\n\n  return (\n    <form onSubmit={handleSubmit}>\n      {/* fields */}\n      <button type=\"submit\" disabled={status === 'loading'}>Send</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "e.preventDefault()",
            "definition": "Stops the browser's default form submission behavior (page reload)."
          },
          {
            "term": "Fetch API",
            "definition": "A modern browser API for making HTTP requests."
          },
          {
            "term": "Optimistic Update",
            "definition": "Updating the UI before the server confirms the change."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-form-submit",
        "caption": "The form submission lifecycle from user action to server response.",
        "definition": "flowchart LR\n    A[User submits] --> B[preventDefault]\n    B --> C[Validate]\n    C -->|invalid| D[Show errors]\n    C -->|valid| E[API Request]\n    E -->|loading| F[Disable button]\n    E -->|success| G[Show success]\n    E -->|error| H[Show error message]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Submit a Contact Form",
        "description": "Build a contact form with loading and error states.",
        "steps": [
          "Create a ContactForm with name, email, and message fields.",
          "Add isLoading and error state.",
          "On submit, simulate an API call with setTimeout (2 seconds).",
          "Show 'Sending...' while loading and 'Sent!' on success."
        ],
        "expectedOutcome": "A form that simulates submission with realistic loading and success feedback."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why does my page reload after submitting?",
            "answer": "You forgot e.preventDefault(). Always call it at the start of an onSubmit handler."
          },
          {
            "question": "Should I use fetch or axios?",
            "answer": "fetch is built into the browser and requires no library. axios offers automatic JSON parsing and request cancellation, which some teams prefer."
          },
          {
            "question": "How do I handle timeouts?",
            "answer": "Use AbortController with a setTimeout to cancel the fetch if it takes too long."
          },
          {
            "question": "Should I show a success message?",
            "answer": "Yes. Users need confirmation that their action succeeded. A brief message or toast notification is standard."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Always prevent default form submission behavior.",
          "Track loading and error states for async requests.",
          "Disable the submit button while loading.",
          "Show clear success or error feedback after submission."
        ],
        "takeaway": "Form submission ties the frontend to real data. Next, you will break long forms into multi-step wizards."
      }
    }
  ]
}
