{
  "id": "lesson-react-020",
  "slug": "custom-hooks",
  "moduleSlug": "lifecycle-effects",
  "courseSlug": "react-fundamentals",
  "title": "Custom Hooks",
  "description": "Learn how to create reusable logic with custom hooks.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "hooks",
    "custom-hooks",
    "reusable"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Custom hooks** let you extract component logic into reusable functions. They start with `use`, can call other hooks, and let you share stateful logic across components without repeating code."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating a Custom Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A custom hook is just a JavaScript function whose name starts with `use` and that calls other hooks inside. It can return anything: a value, an object, or an array."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useCounter Hook",
        "content": "A simple reusable counter hook",
        "language": "jsx",
        "code": "import { useState } from 'react';\n\nfunction useCounter(initialValue = 0) {\n  const [count, setCount] = useState(initialValue);\n\n  const increment = () => setCount((prev) => prev + 1);\n  const decrement = () => setCount((prev) => prev - 1);\n  const reset = () => setCount(initialValue);\n\n  return { count, increment, decrement, reset };\n}\n\n// Usage\nfunction Counter() {\n  const { count, increment, decrement, reset } = useCounter(0);\n\n  return (\n    <div>\n      <p>{count}</p>\n      <button onClick={increment}>+</button>\n      <button onClick={decrement}>-</button>\n      <button onClick={reset}>Reset</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Rules of Hooks"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Hook Rules",
        "items": [
          "Only call hooks at the **top level** — not inside loops, conditions, or nested functions.",
          "Only call hooks from **React functions** — components or other hooks.",
          "Start custom hook names with `use` so linters can enforce the rules."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Why the Rules?",
        "text": "React relies on the order of hook calls to associate state with the correct hook. Breaking the rules causes state to become corrupted or lost."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useFetch Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Encapsulate the entire fetch pattern — loading, error, data — into a single reusable hook."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useFetch",
        "content": "Reusable data fetching hook",
        "language": "jsx",
        "code": "import { useState, useEffect } from 'react';\n\nfunction useFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    const controller = new AbortController();\n\n    fetch(url, { signal: controller.signal })\n      .then((res) => {\n        if (!res.ok) throw new Error('Network error');\n        return res.json();\n      })\n      .then((data) => { setData(data); setLoading(false); })\n      .catch((err) => {\n        if (err.name !== 'AbortError') {\n          setError(err);\n          setLoading(false);\n        }\n      });\n\n    return () => controller.abort();\n  }, [url]);\n\n  return { data, loading, error };\n}\n\n// Usage\nfunction Posts() {\n  const { data, loading, error } = useFetch('/api/posts');\n\n  if (loading) return <p>Loading...</p>;\n  if (error) return <p>{error.message}</p>;\n\n  return <ul>{data.map((p) => <li key={p.id}>{p.title}</li>)}</ul>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useLocalStorage Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Persist state to localStorage so it survives page refreshes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useLocalStorage",
        "content": "Synchronizing state with localStorage",
        "language": "jsx",
        "code": "import { useState, useEffect } from 'react';\n\nfunction useLocalStorage(key, initialValue) {\n  const [value, setValue] = useState(() => {\n    const stored = localStorage.getItem(key);\n    return stored ? JSON.parse(stored) : initialValue;\n  });\n\n  useEffect(() => {\n    localStorage.setItem(key, JSON.stringify(value));\n  }, [key, value]);\n\n  return [value, setValue];\n}\n\n// Usage\nfunction ThemeToggle() {\n  const [theme, setTheme] = useLocalStorage('theme', 'light');\n  const toggle = () => setTheme((t) => (t === 'light' ? 'dark' : 'light'));\n\n  return (\n    <button onClick={toggle}>Current: {theme}</button>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Create Custom Hooks"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Good Candidates for Custom Hooks",
        "items": [
          "Data fetching with loading and error handling.",
          "Browser APIs: localStorage, sessionStorage, geolocation.",
          "Timers, intervals, and animation loops.",
          "Form field tracking and validation.",
          "Any logic duplicated across multiple components."
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Custom Hook",
            "definition": "A function starting with `use` that encapsulates reusable stateful logic."
          },
          {
            "term": "Rules of Hooks",
            "definition": "Constraints ensuring hooks work correctly: top-level calls only, inside React functions."
          },
          {
            "term": "Composition",
            "definition": "Combining multiple hooks inside a custom hook to build complex behavior."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-custom-hooks",
        "caption": "Custom hooks compose built-in hooks into reusable logic.",
        "definition": "flowchart LR\n    A[Custom Hook] --> B[useState]\n    A --> C[useEffect]\n    A --> D[useCallback]\n    B --> E[Component A]\n    B --> F[Component B]\n    C --> E\n    C --> F"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build useToggle",
        "description": "Create a custom hook that toggles a boolean value.",
        "steps": [
          "Create useToggle(initialValue) that returns [value, toggle, setTrue, setFalse].",
          "Use useState inside the hook.",
          "Use it in a component to toggle a modal open and closed.",
          "Add a reset function that returns to the initial value."
        ],
        "expectedOutcome": "A reusable toggle hook that simplifies boolean state management across components."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can custom hooks share state between components?",
            "answer": "No. Each call to a custom hook creates its own isolated state. To share state, use Context or a state management library."
          },
          {
            "question": "Do custom hooks need to start with use?",
            "answer": "Yes. The `use` prefix signals to React and linters that the function follows the Rules of Hooks."
          },
          {
            "question": "Can custom hooks call other custom hooks?",
            "answer": "Yes. Hooks can be composed. You can build useLocalStorage using useState and useEffect, then use it inside another custom hook."
          },
          {
            "question": "Where should I store custom hooks?",
            "answer": "In a `src/hooks/` folder, one file per hook (e.g., `src/hooks/useFetch.jsx`)."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Custom hooks extract reusable logic into functions starting with `use`.",
          "They can call any other hook, including other custom hooks.",
          "Follow the Rules of Hooks: top level, inside React functions only.",
          "Common hooks: useFetch, useLocalStorage, useToggle, useWindowSize."
        ],
        "takeaway": "Custom hooks are the secret to clean, reusable React code. Next, you will learn about Context and Refs to manage global data and DOM access."
      }
    }
  ]
}
