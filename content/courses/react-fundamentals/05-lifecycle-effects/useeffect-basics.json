{
  "id": "lesson-react-017",
  "slug": "useeffect-basics",
  "moduleSlug": "lifecycle-effects",
  "courseSlug": "react-fundamentals",
  "title": "The useEffect Hook",
  "description": "Learn how to perform side effects in functional components.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 22,
  "tags": [
    "react",
    "useeffect",
    "hooks",
    "side-effects"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useEffect** is React's hook for performing side effects in functional components. Side effects are actions that reach outside the component: fetching data, setting timers, subscribing to events, or manually changing the DOM."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When Does useEffect Run?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, useEffect runs after every render — both the initial mount and every update. You control this with a **dependency array**."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useEffect Syntax",
        "content": "Three common dependency patterns",
        "language": "jsx",
        "code": "import { useEffect } from 'react';\n\nfunction MyComponent({ value }) {\n  // Runs after every render\n  useEffect(() => {\n    console.log('Rendered');\n  });\n\n  // Runs only once — after the initial mount\n  useEffect(() => {\n    console.log('Mounted');\n  }, []);\n\n  // Runs when value changes\n  useEffect(() => {\n    console.log('Value changed:', value);\n  }, [value]);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Dependency Array"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Dependency Array",
          "When It Runs"
        ],
        "rows": [
          [
            "No array",
            "After every render"
          ],
          [
            "[] (empty array)",
            "Only once after the initial render"
          ],
          [
            "[value1, value2]",
            "When any dependency changes"
          ]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Exhaustive Deps",
        "text": "Include every value from the component scope (props, state, functions) that the effect uses. ESLint plugins can help catch missing dependencies."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Use Cases"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "What useEffect Handles",
        "items": [
          "**Data fetching** — load data when the component mounts.",
          "**Subscriptions** — set up event listeners or WebSocket connections.",
          "**Timers** — start intervals or timeouts.",
          "**DOM manipulation** — update the document title or scroll position.",
          "**Logging and analytics** — track page views or events."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Document Title",
        "content": "Updating the browser tab title",
        "language": "jsx",
        "code": "function Page({ title }) {\n  useEffect(() => {\n    document.title = title;\n  }, [title]);\n\n  return <h1>{title}</h1>;\n}\n\n// Usage\n<Page title=\"Dashboard\" />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Stale Closures"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If an effect captures an old value of a variable because it is not in the dependency array, you have a stale closure. The fix is to add the missing dependency or use a functional update."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Stale Closure Example",
        "content": "A timer that captures an outdated count value",
        "language": "jsx",
        "code": "function Timer() {\n  const [count, setCount] = useState(0);\n\n  // Stale closure — count is always 0 inside here\n  useEffect(() => {\n    const id = setInterval(() => {\n      console.log(count); // Logs 0 forever\n      setCount(count + 1); // Also uses stale count\n    }, 1000);\n    return () => clearInterval(id);\n  }, []); // Missing count in deps\n\n  return <p>{count}</p>;\n}\n\n// Fixed with functional update\nuseEffect(() => {\n  const id = setInterval(() => {\n    setCount((prev) => prev + 1);\n  }, 1000);\n  return () => clearInterval(id);\n}, []);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Side Effect",
            "definition": "Any operation that reaches outside the component: fetch, timer, subscription, DOM change."
          },
          {
            "term": "Dependency Array",
            "definition": "An optional array that tells React when to re-run the effect."
          },
          {
            "term": "Stale Closure",
            "definition": "A function that captures outdated values because its dependencies are missing."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-useEffect",
        "caption": "The useEffect execution flow based on the dependency array.",
        "definition": "flowchart TD\n    A[Component renders] --> B{Dependency array?}\n    B -->|no array| C[Run after every render]\n    B -->|empty| D[Run once after mount]\n    B -->|[deps]| E[Run only when deps change]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Document Title Updater",
        "description": "Create a component that updates the document title when a prop changes.",
        "steps": [
          "Create a PageTitle component that accepts a title prop.",
          "Use useEffect with [title] as the dependency.",
          "Update document.title inside the effect.",
          "Render two PageTitle components with different titles and verify the tab title updates."
        ],
        "expectedOutcome": "A component that keeps the browser tab title in sync with React state."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Can I call useEffect inside a condition?",
            "answer": "No. Hooks must always be called at the top level of your component, in the same order on every render."
          },
          {
            "question": "Why does my effect run twice in development?",
            "answer": "React 18 StrictMode intentionally double-invokes effects in development to help you find bugs. It does not happen in production."
          },
          {
            "question": "Should I put all logic in useEffect?",
            "answer": "No. Keep logic inside the render body when possible. useEffect is for things that must happen after render, like data fetching."
          },
          {
            "question": "What is the difference between useEffect and useLayoutEffect?",
            "answer": "useLayoutEffect fires synchronously after all DOM mutations but before paint. Use it only for measuring DOM or preventing flicker. useEffect is preferred for almost everything else."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useEffect handles side effects after render.",
          "No dependency array runs after every render.",
          "Empty array runs once after mount.",
          "Include all referenced values in the dependency array.",
          "Watch out for stale closures when dependencies are missing."
        ],
        "takeaway": "useEffect is your bridge between React and the imperative world. Next, you will learn how to clean up side effects to prevent memory leaks."
      }
    }
  ]
}