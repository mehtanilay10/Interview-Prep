{
  "id": "lesson-react-018",
  "slug": "cleanup-functions",
  "moduleSlug": "lifecycle-effects",
  "courseSlug": "react-fundamentals",
  "title": "Cleanup Functions",
  "description": "Learn how to clean up side effects to prevent memory leaks.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "react",
    "useeffect",
    "cleanup",
    "memory-leaks"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Some side effects need to be undone when the component unmounts or before the effect runs again. `useEffect` supports this by returning a cleanup function."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When Cleanup Is Needed"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Effects That Require Cleanup",
        "items": [
          "**Subscriptions** — WebSocket, EventSource, or store listeners.",
          "**Timers** — setInterval and setTimeout that keep running.",
          "**Event listeners** — window resize or scroll listeners.",
          "**Fetch requests** — AbortController to cancel pending requests."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Cleanup Syntax"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Return a function from your effect. React calls it when the component unmounts and before the next effect run if dependencies changed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Cleanup",
        "content": "Returning a cleanup function from useEffect",
        "language": "jsx",
        "code": "useEffect(() => {\n  // Setup\n  const subscription = subscribeToChannel();\n\n  // Cleanup\n  return () => {\n    subscription.unsubscribe();\n  };\n}, []);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Timer Cleanup"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Always clear intervals and timeouts in cleanup. Otherwise they continue running after the component unmounts and cause memory leaks or state updates on unmounted components."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Interval Cleanup",
        "content": "Clearing a timer on unmount",
        "language": "jsx",
        "code": "function Timer() {\n  const [seconds, setSeconds] = useState(0);\n\n  useEffect(() => {\n    const id = setInterval(() => {\n      setSeconds((s) => s + 1);\n    }, 1000);\n\n    // Cleanup: clear interval when component unmounts\n    return () => clearInterval(id);\n  }, []);\n\n  return <p>Seconds: {seconds}</p>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Event Listener Cleanup"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Remove event listeners in cleanup to avoid duplicate handlers if the component re-mounts."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Window Resize Listener",
        "content": "Adding and removing a resize listener",
        "language": "jsx",
        "code": "function WindowSize() {\n  const [size, setSize] = useState({\n    width: window.innerWidth,\n    height: window.innerHeight\n  });\n\n  useEffect(() => {\n    const handleResize = () => {\n      setSize({\n        width: window.innerWidth,\n        height: window.innerHeight\n      });\n    };\n\n    window.addEventListener('resize', handleResize);\n\n    // Cleanup: remove listener on unmount\n    return () => window.removeEventListener('resize', handleResize);\n  }, []);\n\n  return <p>{size.width} x {size.height}</p>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Aborting Fetch Requests"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use AbortController to cancel pending fetch requests when the component unmounts. This prevents state updates on unmounted components."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Abort Controller",
        "content": "Cancelling a fetch on unmount",
        "language": "jsx",
        "code": "useEffect(() => {\n  const controller = new AbortController();\n\n  const fetchData = async () => {\n    try {\n      const response = await fetch('/api/data', {\n        signal: controller.signal\n      });\n      const data = await response.json();\n      setData(data);\n    } catch (err) {\n      if (err.name !== 'AbortError') {\n        setError(err.message);\n      }\n    }\n  };\n\n  fetchData();\n\n  // Cleanup: abort fetch on unmount\n  return () => controller.abort();\n}, []);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Cleanup Function",
            "definition": "A function returned from useEffect that runs before the component unmounts or before the next effect run."
          },
          {
            "term": "Memory Leak",
            "definition": "Resources (timers, listeners, subscriptions) that are not released when a component unmounts."
          },
          {
            "term": "AbortController",
            "definition": "A browser API for canceling fetch requests and other async operations."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Memory Leaks Are Silent",
        "text": "Forgetting cleanup does not throw errors. The app may work fine for a while, then slow down, crash, or behave unpredictably. Always clean up subscriptions, timers, and listeners."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-cleanup",
        "caption": "Cleanup runs before unmount and before re-running the effect.",
        "definition": "flowchart LR\n    A[Effect runs] --> B[Setup resource]\n    B --> C[Return cleanup function]\n    C --> D[Component unmounts OR deps change]\n    D --> E[Cleanup runs]\n    E --> F[Effect runs again]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Add Cleanup to a Timer",
        "description": "Fix a timer component that leaks memory when unmounted.",
        "steps": [
          "Create a Clock component that shows the current time every second.",
          "Verify the timer keeps running after navigating away (check console logs).\n  - Add a cleanup function that clears the interval.",
          "Verify the timer stops when the component unmounts."
        ],
        "expectedOutcome": "A clock component that starts and stops cleanly without memory leaks."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Does cleanup run before the first effect run?",
            "answer": "No. React runs the setup, stores the cleanup, and runs cleanup only before the next effect run or on unmount."
          },
          {
            "question": "Can I run async code in cleanup?",
            "answer": "Avoid it. Cleanup should be synchronous. If you need async teardown, consider a separate flag or a dedicated hook."
          },
          {
            "question": "What if I return nothing from useEffect?",
            "answer": "That is fine. Only effects that create subscriptions or timers need cleanup."
          },
          {
            "question": "Does cleanup run in production?",
            "answer": "Yes. Cleanup is essential for correctness in both development and production."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Return a function from useEffect to clean up side effects.",
          "Cleanup runs before unmount and before the next effect run.",
          "Clear timers, remove listeners, and abort fetches.",
          "Cleanup prevents memory leaks and stale state updates."
        ],
        "takeaway": "Cleanup is the safety net of useEffect. Next, you will apply it to real-world data fetching."
      }
    }
  ]
}