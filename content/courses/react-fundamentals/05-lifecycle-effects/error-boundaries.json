{
  "id": "lesson-react-035",
  "slug": "error-boundaries",
  "moduleSlug": "lifecycle-effects",
  "courseSlug": "react-fundamentals",
  "title": "Error Boundaries",
  "description": "Learn how to catch JavaScript errors in component trees with error boundaries.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "errors",
    "error-boundaries",
    "fallback",
    "class-components"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Error boundaries are React components that catch JavaScript errors anywhere in their child component tree, log those errors, and display a fallback UI instead of crashing the whole app."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Problem: Uncaught Errors Break the UI"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Before error boundaries, a single uncaught error in one component would unmount the entire React tree. The user would see a blank screen with no way to recover without refreshing."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Error Without Boundary",
        "content": "An error in one component crashes everything",
        "language": "jsx",
        "code": "function BuggyComponent() {\n  throw new Error('I crashed!');\n  return <h1>Hello</h1>;\n}\n\nfunction App() {\n  return (\n    <div>\n      <Header />\n      <BuggyComponent /> {/* Crashes the whole tree */}\n      <Footer />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating an Error Boundary"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Error boundaries are class components that define either or both of these lifecycle methods: getDerivedStateFromError() and componentDidCatch()."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Error Boundary",
            "definition": "A class component that catches errors in its children and renders a fallback UI."
          },
          {
            "term": "getDerivedStateFromError",
            "definition": "A static lifecycle method that updates state when an error is caught, triggering a fallback render."
          },
          {
            "term": "componentDidCatch",
            "definition": "A lifecycle method for side effects like logging the error to an external service."
          },
          {
            "term": "Fallback UI",
            "definition": "The alternate UI shown when an error is caught, e.g. a friendly error message and retry button."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ErrorBoundary Class Component",
        "content": "A reusable error boundary",
        "language": "jsx",
        "code": "import { Component } from 'react';\n\nclass ErrorBoundary extends Component {\n  constructor(props) {\n    super(props);\n    this.state = { hasError: false, error: null };\n  }\n\n  static getDerivedStateFromError(error) {\n    return { hasError: true, error };\n  }\n\n  componentDidCatch(error, errorInfo) {\n    console.error('Error caught:', error, errorInfo);\n    // Send to logging service here\n  }\n\n  render() {\n    if (this.state.hasError) {\n      return (\n        <div className=\"error-fallback\">\n          <h2>Something went wrong.</h2>\n          <p>{this.state.error?.message}</p>\n          <button onClick={() => this.setState({ hasError: false })}>\n            Try again\n          </button>\n        </div>\n      );\n    }\n\n    return this.props.children;\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using the Boundary"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Wrap any part of your app that might throw in an ErrorBoundary. You can nest boundaries to isolate failures to specific sections."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Wrapping Risky Components",
        "content": "Isolating failures with multiple boundaries",
        "language": "jsx",
        "code": "function App() {\n  return (\n    <div>\n      <Header />\n      <ErrorBoundary>\n        <Dashboard />\n      </ErrorBoundary>\n      <ErrorBoundary>\n        <Widget />\n      </ErrorBoundary>\n      <Footer />\n    </div>\n  );\n}\n\n// If Dashboard crashes, Header and Footer stay visible."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Limitations"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "What Error Boundaries Catch",
        "items": [
          "Rendering errors (inside render or lifecycle methods)",
          "Errors in constructors of the whole tree",
          "Errors in the initial mount and updates"
        ]
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "What They Do NOT Catch",
        "items": [
          "Event handlers (use try/catch inside handlers)",
          "Async code (setTimeout, promises, async/await)",
          "Server-side rendering errors",
          "Errors thrown in the boundary itself"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Event Handlers Need Try/Catch",
        "text": "Error boundaries do not catch errors inside event handlers. Wrap handler logic in try/catch and set error state manually."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-error-boundary",
        "caption": "How an error boundary isolates a failing component from the rest of the app.",
        "definition": "flowchart TD\n    A[App Tree] --> B[ErrorBoundary]\n    B -->|no error| C[Normal Child UI]\n    B -->|error caught| D[Fallback UI]\n    E[Sibling Component] --> F[Still Visible]"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Add an Error Boundary",
        "description": "Wrap a data-heavy component with an error boundary and display a friendly fallback.",
        "steps": [
          "Create an ErrorBoundary class component with getDerivedStateFromError and componentDidCatch.",
          "Wrap a UserProfile component that fetches and renders user data.",
          "Throw an error inside UserProfile if the data is malformed.",
          "Verify that the fallback UI appears and the rest of the app stays visible."
        ],
        "expectedOutcome": "A page where a broken component shows a fallback without taking down the whole app."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why are error boundaries class components?",
            "answer": "Error boundaries require lifecycle methods (getDerivedStateFromError, componentDidCatch) that are not yet available in hooks. React may add a useErrorBoundary hook in the future."
          },
          {
            "question": "Can I share state between the boundary and its children?",
            "answer": "Not directly. The boundary controls the fallback UI. If children need to recover, expose a reset function via context or lift the state up."
          },
          {
            "question": "Should every component have its own boundary?",
            "answer": "No. Place boundaries strategically around sections that might fail independently, such as a dashboard, a widget, or a third-party embed."
          },
          {
            "question": "Do error boundaries catch errors in the boundary itself?",
            "answer": "No. If the boundary's render or lifecycle throws, the nearest boundary above it catches it. If there is no boundary above it, the app unmounts."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Error boundaries catch rendering errors in the component tree.",
          "They are class components using getDerivedStateFromError and componentDidCatch.",
          "Wrap risky sections to isolate failures and keep the rest of the app alive.",
          "They do not catch event handlers, async code, or SSR errors."
        ],
        "takeaway": "Error boundaries make your app resilient. Combined with proper logging, they turn crashes into recoverable moments."
      }
    }
  ]
}
