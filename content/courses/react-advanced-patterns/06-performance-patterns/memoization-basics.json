{
  "id": "lesson-react-adv-014",
  "slug": "memoization-basics",
  "moduleSlug": "performance-patterns",
  "courseSlug": "react-advanced-patterns",
  "title": "Memoization Basics",
  "description": "Understand when and how to use React.memo, useMemo, and useCallback to optimize React rendering performance.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": ["memo", "useMemo", "useCallback", "performance", "re-renders"],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "Why React Re-renders",
      "anchor": "why-react-re-renders"
    },
    {
      "type": "paragraph",
      "text": "React components re-render whenever their state or props change. While this is the core of React's declarative model, unnecessary re-renders can degrade performance, especially in large applications. Understanding when re-renders happen is the first step toward writing high-performance React code."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Common Causes of Unnecessary Re-renders",
      "anchor": "common-causes"
    },
    {
      "type": "bullet-list",
      "title": "Typical Triggers",
      "items": [
        "Parent component re-renders, passing new prop objects or arrays to children",
        "Inline function definitions in JSX, which create new references on every render",
        "Lack of memoization on expensive pure components that receive unchanged props",
        "State updates in components that do not affect the rendered output"
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "What Memoization Means",
      "anchor": "what-memoization-means"
    },
    {
      "type": "paragraph",
      "text": "Memoization is a technique to cache the result of expensive computations so they are only recomputed when dependencies change. React provides three primary APIs for memoization: React.memo for components, useMemo for values, and useCallback for functions."
    },
    {
      "type": "mermaid",
      "id": "memo-flow",
      "caption": "How memoization reduces re-renders",
      "definition": "graph LR\n    A[Parent Re-renders] --> B{Props Changed?}\n    B -->|Yes| C[Re-render Child]\n    B -->|No| D[Skip Render]\n    C --> E[Expensive Work]\n    D --> F[Reuse Cached Output]"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "React.memo",
      "anchor": "react-memo"
    },
    {
      "type": "paragraph",
      "text": "React.memo is a higher-order component that wraps a functional component and prevents it from re-rendering if its props have not changed. It performs a shallow comparison of props by default."
    },
    {
      "type": "example",
      "title": "Wrapping a Component with React.memo",
      "content": "Use React.memo when a component renders often with the same props.",
      "code": "import React from 'react';\n\ninterface Props {\n  userId: number;\n  name: string;\n}\n\nconst UserCard = ({ userId, name }: Props) => {\n  console.log('Rendering UserCard');\n  return (\n    <div className=\"user-card\">\n      <h3>{name}</h3>\n      <p>User ID: {userId}</p>\n    </div>\n  );\n};\n\nexport default React.memo(UserCard);\n\n// Parent passes the same props -> no re-render\n// Parent passes new props -> re-render",
      "language": "tsx"
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "Shallow Comparison Limit",
      "text": "React.memo only shallow-compares props. If you pass objects, arrays, or functions, it will still re-render when the reference changes, even if the internal data is identical. Use useMemo or useCallback to stabilize those references."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "useMemo",
      "anchor": "usememo"
    },
    {
      "type": "paragraph",
      "text": "useMemo caches the result of a calculation between renders. It only recomputes the cached value when one of its dependencies changes. This is useful for expensive calculations like filtering large arrays, sorting data, or parsing JSON."
    },
    {
      "type": "example",
      "title": "Memoizing an Expensive Calculation",
      "content": "Suppose we filter a large list of products on every render.",
      "code": "import { useMemo, useState } from 'react';\n\ninterface Product {\n  id: number;\n  name: string;\n  price: number;\n}\n\nfunction ProductList({ products, filter }: { products: Product[]; filter: string }) {\n  const [count, setCount] = useState(0);\n\n  const visibleProducts = useMemo(() => {\n    console.log('Filtering products...');\n    return products.filter(p =>\n      p.name.toLowerCase().includes(filter.toLowerCase())\n    );\n  }, [products, filter]);\n\n  return (\n    <div>\n      <button onClick={() => setCount(c => c + 1)}>\n        Count: {count}\n      </button>\n      <ul>\n        {visibleProducts.map(p => (\n          <li key={p.id}>{p.name} - ${p.price}</li>\n        ))}\n      </ul>\n    </div>\n  );\n}",
      "language": "tsx"
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "When to Use useMemo",
      "text": "Only use useMemo for expensive computations. For simple values, the overhead of memoization can outweigh the benefit. Profile first, optimize second."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "useCallback",
      "anchor": "usecallback"
    },
    {
      "type": "paragraph",
      "text": "useCallback returns a memoized callback function. Like useMemo, it only changes the function reference when its dependencies change. This is essential when passing callbacks to optimized child components wrapped in React.memo."
    },
    {
      "type": "example",
      "title": "Stabilizing Event Handlers",
      "content": "Without useCallback, the handleRemove reference changes on every render and breaks React.memo optimization.",
      "code": "import { useCallback, useState } from 'react';\n\nconst ItemList = React.memo(({ items, onRemove }: any) => {\n  console.log('Rendering ItemList');\n  return items.map(item => (\n    <li key={item.id}>\n      {item.name}\n      <button onClick={() => onRemove(item.id)}>Remove</button>\n    </li>\n  ));\n});\n\nfunction Parent() {\n  const [items, setItems] = useState([\n    { id: 1, name: 'First' },\n    { id: 2, name: 'Second' }\n  ]);\n  const [count, setCount] = useState(0);\n\n  const handleRemove = useCallback((id: number) => {\n    setItems(prev => prev.filter(item => item.id !== id));\n  }, []);\n\n  return (\n    <div>\n      <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>\n      <ItemList items={items} onRemove={handleRemove} />\n    </div>\n  );\n}",
      "language": "tsx"
    },
    {
      "type": "table",
      "headers": ["Hook", "Memoizes", "Typical Use"],
      "rows": [
        ["React.memo", "Component output", "Prevent re-renders when props are unchanged"],
        ["useMemo", "Computed value", "Expensive calculations, derived data"],
        ["useCallback", "Function reference", "Event handlers passed to memoized children"]
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "When NOT to Optimize Prematurely",
      "anchor": "when-not-to-optimize"
    },
    {
      "type": "paragraph",
      "text": "Not every component needs memoization. Overusing these hooks adds mental overhead and can sometimes hurt performance due to the cost of comparison and cache management. Always measure with React DevTools Profiler before and after applying memoization."
    },
    {
      "type": "faq-block",
      "title": "Common Questions",
      "items": [
        {
          "question": "Does React.memo guarantee a component will never re-render?",
          "answer": "No. React.memo only skips re-renders when props are shallowly equal. It does not prevent re-renders caused by local state changes, context changes, or parent re-renders with new prop references."
        },
        {
          "question": "Should I wrap every component in React.memo?",
          "answer": "No. Apply it selectively to components that are expensive to render and receive stable props. Simple presentational components usually do not benefit."
        },
        {
          "question": "Can useMemo and useCallback slow down my app?",
          "answer": "Yes, if overused. They add overhead for storing values in memory and comparing dependencies. Use them for expensive work or when passing callbacks to memoized children."
        }
      ]
    },
    {
      "type": "summary-box",
      "title": "Key Takeaways",
      "points": [
        "React.memo skips re-renders when props are shallowly equal",
        "useMemo caches computed values for expensive operations",
        "useCallback stabilizes function references for memoized children",
        "Measure with React DevTools before applying optimizations",
        "Avoid premature optimization on simple, low-frequency components"
      ]
    }
  ]
}
