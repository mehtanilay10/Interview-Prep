{
  "id": "lesson-react-ap-013",
  "slug": "control-props-examples",
  "moduleSlug": "control-props",
  "title": "Control Props Examples",
  "description": "Explore practical controlled props implementations.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "control-props",
    "examples",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Let's explore practical controlled props implementations."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Toggle Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Controlled Toggle",
        "content": "Toggle supporting both modes",
        "language": "jsx",
        "code": "function Toggle({ on, onChange, defaultOn = false }) {\n  const isControlled = on !== undefined;\n  const [internalOn, setInternalOn] = useState(defaultOn);\n\n  const currentOn = isControlled ? on : internalOn;\n\n  const toggle = () => {\n    if (!isControlled) {\n      setInternalOn(!currentOn);\n    }\n    if (onChange) {\n      onChange(!currentOn);\n    }\n  };\n\n  return (\n    <button onClick={toggle} className={currentOn ? 'on' : 'off'}>\n      {currentOn ? 'ON' : 'OFF'}\n    </button>\n  );\n}\n\n// Uncontrolled\n<Toggle defaultOn={false} />\n\n// Controlled\n<Toggle on={isOn} onChange={setIsOn} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Modal Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Controlled Modal",
        "content": "Modal supporting both modes",
        "language": "jsx",
        "code": "function Modal({ isOpen, onClose, defaultOpen = false, children }) {\n  const isControlled = isOpen !== undefined;\n  const [internalOpen, setInternalOpen] = useState(defaultOpen);\n\n  const currentOpen = isControlled ? isOpen : internalOpen;\n\n  const open = () => {\n    if (!isControlled) setInternalOpen(true);\n    // No onOpen callback needed for uncontrolled\n  };\n\n  const close = () => {\n    if (!isControlled) setInternalOpen(false);\n    if (onClose) onClose();\n  };\n\n  if (!currentOpen) return null;\n\n  return (\n    <div className=\"modal-overlay\" onClick={close}>\n      <div className=\"modal-content\" onClick={(e) => e.stopPropagation()}>\n        {children}\n        <button onClick={close}>Close</button>\n      </div>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Best Practice",
        "text": "Always support both modes in reusable components. This makes them flexible for different use cases."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Toggle can be controlled or uncontrolled",
          "Modal supports both modes",
          "Check if prop is undefined to detect mode",
          "Always support both modes in reusable components"
        ],
        "takeaway": "Control props make components flexible and reusable."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "react-advanced-patterns"
}