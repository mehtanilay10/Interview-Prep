{
  "id": "lesson-react-ap-012",
  "slug": "control-props-basics",
  "moduleSlug": "control-props",
  "title": "Control Props Basics",
  "description": "Understand the controlled props pattern for dual-mode components.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "control-props",
    "patterns",
    "controlled"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **control props pattern** allows components to work in both controlled and uncontrolled modes. If a prop is provided, the component is controlled; if not, it manages its own state."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Controlled vs Uncontrolled"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Controlled",
          "Uncontrolled"
        ],
        "rows": [
          [
            "Parent manages state",
            "Component manages state"
          ],
          [
            "Value passed as prop",
            "Value tracked internally"
          ],
          [
            "onChange callback",
            "Works independently"
          ],
          [
            "More flexible",
            "Simpler to use"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Implementation"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Controlled Input",
        "content": "Input that supports both modes",
        "language": "jsx",
        "code": "function Input({ value, onChange, defaultValue = '' }) {\n  // If value prop is provided, component is controlled\n  const isControlled = value !== undefined;\n  const [internalValue, setInternalValue] = useState(defaultValue);\n\n  const currentValue = isControlled ? value : internalValue;\n\n  const handleChange = (e) => {\n    if (!isControlled) {\n      setInternalValue(e.target.value);\n    }\n    if (onChange) {\n      onChange(e);\n    }\n  };\n\n  return <input value={currentValue} onChange={handleChange} />;\n}\n\n// Uncontrolled usage\n<Input defaultValue=\"hello\" />\n\n// Controlled usage\n<Input value={name} onChange={handleNameChange} />"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Warning",
        "text": "Warn developers if they switch between controlled and uncontrolled modes, as this can cause bugs."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Components can work in both modes",
          "Controlled: parent manages state",
          "Uncontrolled: component manages state",
          "Detect mode by checking if prop is undefined"
        ],
        "takeaway": "Control props provide flexibility for component consumers."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "react-advanced-patterns"
}