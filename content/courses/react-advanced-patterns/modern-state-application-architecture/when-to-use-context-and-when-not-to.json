{
  "id": "lesson-react-advanced-patterns-modern-state-application-architecture-when-to-use-context-and-when-not-to",
  "slug": "when-to-use-context-and-when-not-to",
  "moduleSlug": "modern-state-application-architecture",
  "courseSlug": "react-advanced-patterns",
  "title": "When to use Context and when not to",
  "description": "Learn how to decide whether React Context is the right tool for a given problem. This lesson covers what Context solves, where it becomes a liability, and which patterns avoid common pitfalls.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": [
    "context",
    "react",
    "architecture"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React Context is often introduced as the solution to prop drilling, and it does solve that problem. But Context is not a replacement for a full state-management library, and using it for the wrong kind of state can cause hidden re-renders, performance problems, and logic that is difficult to test."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "This lesson gives you a practical decision framework for when Context is appropriate and when you should reach for a different tool."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Context is designed for"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Context shines when you need to pass a value through a component tree without manually threading it through every intermediate component. It is especially useful for values that are \"global\" in the sense that most of the tree needs access to them."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Good candidates for Context",
        "items": [
          "Theme values used across many components",
          "Current user or authentication session",
          "Preferred language or locale",
          "A layout or navigation state shared by many pages"
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "A typical theme Context",
        "content": "Theme Context avoids passing `theme` and `toggleTheme` through every intermediate component.",
        "language": "tsx",
        "code": "const ThemeContext = createContext<{\n  theme: 'light' | 'dark';\n  toggleTheme: () => void;\n} | undefined>(undefined);\n\nfunction ThemeProvider({ children }: { children: ReactNode }) {\n  const [theme, setTheme] = useState<'light' | 'dark'>('light');\n  const toggleTheme = () => setTheme((prev) => (prev === 'light' ? 'dark' : 'light'));\n\n  return (\n    <ThemeContext.Provider value={{ theme, toggleTheme }}>\n      {children}\n    </ThemeContext.Provider>\n  );\n}\n\nfunction useTheme() {\n  const context = useContext(ThemeContext);\n  if (!context) {\n    throw new Error('useTheme must be used within a ThemeProvider');\n  }\n  return context;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When Context becomes a problem"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Context is not a general-purpose state-management solution. Because every consumer re-renders whenever the provider value changes, large or frequently updating contexts can create performance bottlenecks. Additionally, co-locating logic with Context providers often hides dependencies and makes components harder to reuse."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Warning signs that Context is the wrong tool",
        "items": [
          "The context value changes multiple times per second.",
          "Only a small portion of the tree actually consumes the value.",
          "You find yourself building complex selector logic inside `useContext`.",
          "Components that use the context become impossible to reuse outside the provider tree."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Common anti-pattern",
        "text": "Putting an entire Redux-like store into a single Context will cause every consumer to re-render on every dispatch. Split contexts by concern or use a dedicated state-management library instead."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Alternatives and complementary tools"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Sometimes the best fix for prop drilling is not Context but better component composition. Passing JSX as children or rendering props keeps data flow explicit and avoids the re-render behavior of Context. For more complex global state, libraries such as Zustand, Jotai, or Redux Toolkit provide more granular subscriptions and better debugging."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "Reach for these tools first",
        "items": [
          "Lift state up to the nearest common ancestor and pass handlers down.",
          "Use composition (`children` or render props) for shared UI behavior.",
          "Use Context for truly global, relatively static values such as theme or auth.",
          "Use a dedicated state library when you need selectors, time-travel debugging, or complex asynchronous workflows."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Composition instead of Context",
        "content": "A reusable button that needs an onClick handler does not need Context. It simply receives the handler as a prop from its parent.",
        "language": "tsx",
        "code": "function Parent() {\n  const handleAction = () => doSomething();\n  return <ActionButton onClick={handleAction} />;\n}\n\n// ActionButton stays reusable because it has no dependency on Context.\nfunction ActionButton({ onClick }: { onClick: () => void }) {\n  return <button onClick={onClick}>Perform action</button>;\n}"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Context is best for relatively static, global values such as theme or auth.",
          "Avoid Context for high-frequency or large shared stores.",
          "Prefer composition, lifting state up, or a dedicated library when Context causes performance or reusability problems."
        ],
        "takeaway": "Use Context when the value is shared, global, and slow-changing; reach for another tool when the value is dynamic, large, or consumed by only a few components."
      }
    }
  ],
  "relatedLessons": [
    "local-state-vs-server-state-vs-url-state",
    "choosing-state-ownership-boundaries"
  ],
  "furtherReading": [
    {
      "title": "React Context documentation",
      "url": "https://react.dev/learn/passing-data-deeply-with-context",
      "type": "article"
    },
    {
      "title": "How to safely use React Context",
      "url": "https://www.joshwcomeau.com/react/contexthook/",
      "type": "article"
    },
    {
      "title": "Zustand - small, fast state-management",
      "url": "https://docs.pmnd.rs/zustand",
      "type": "tool"
    }
  ]
}
