{
  "id": "lesson-react-ap-005",
  "slug": "hoc-examples",
  "moduleSlug": "higher-order-components",
  "title": "HOC Examples",
  "description": "Explore practical HOC implementations.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 22,
  "tags": ["react", "hoc", "examples", "patterns"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Let's explore practical HOC implementations for common scenarios."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "withLoading HOC"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Loading State",
        "content": "Adding loading state to any component",
        "language": "jsx",
        "code": "function withLoading(WrappedComponent) {\n  return function WithLoading({ isLoading, ...props }) {\n    if (isLoading) {\n      return <div className=\"spinner\">Loading...</div>;\n    }\n    return <WrappedComponent {...props} />;\n  };\n}\n\n// Usage\nconst UserListWithLoading = withLoading(UserList);\n<UserListWithLoading isLoading={loading} users={users} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "withErrorBoundary HOC"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Error Boundary",
        "content": "Adding error handling to components",
        "language": "jsx",
        "code": "function withErrorBoundary(WrappedComponent) {\n  return class WithErrorBoundary extends React.Component {\n    state = { hasError: false, error: null };\n\n    static getDerivedStateFromError(error) {\n      return { hasError: true, error };\n    }\n\n    render() {\n      if (this.state.hasError) {\n        return <div>Error: {this.state.error.message}</div>;\n      }\n      return <WrappedComponent {...this.props} />;\n    }\n  };\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "withLogger HOC"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Logging",
        "content": "Adding logging to components",
        "language": "jsx",
        "code": "function withLogger(WrappedComponent) {\n  return function WithLogger(props) {\n    useEffect(() => {\n      console.log(`${WrappedComponent.name} mounted`);\n      return () => console.log(`${WrappedComponent.name} unmounted`);\n    }, []);\n\n    console.log(`${WrappedComponent.name} rendered with`, props);\n    return <WrappedComponent {...props} />;\n  };\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Static Methods",
        "text": "HOCs don't copy static methods automatically. Use hoist-non-react-statics to preserve them."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "withLoading adds loading states",
          "withErrorBoundary handles errors",
          "withLogger adds debugging logs",
          "HOCs wrap components with behavior"
        ],
        "takeaway": "HOCs are powerful for cross-cutting concerns."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
