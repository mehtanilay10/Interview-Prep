{
  "id": "lesson-react-ap-004",
  "slug": "hoc-basics",
  "moduleSlug": "higher-order-components",
  "courseSlug": "react-advanced-patterns",
  "title": "HOC Basics",
  "description": "Understand the fundamentals of Higher-Order Components and how to use them for logic reuse.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 33,
  "tags": [
    "react",
    "hoc",
    "higher-order-components",
    "patterns",
    "beginner"
  ],
  "prerequisites": [
    "render-props-basics"
  ],
  "relatedLessons": [
    "hoc-examples"
  ],
  "furtherReading": [
    {
      "title": "Higher-Order Components - React Documentation",
      "url": "https://react.dev/reference/react#higher-order-components",
      "type": "article",
      "author": "React Team",
      "description": "Official React documentation explaining the HOC pattern with examples."
    },
    {
      "title": "Compound Components with React",
      "url": "https://kentcdodds.com/blog/compound-components-with-react-hooks",
      "type": "article",
      "author": "Kent C. Dodds",
      "description": "An in-depth article on component patterns including HOCs and alternatives."
    },
    {
      "title": "React Patterns: Higher-Order Components",
      "url": "https://reactpatterns.com/",
      "type": "article",
      "author": "React Patterns",
      "description": "A collection of useful React patterns including HOCs for common use cases."
    }
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **Higher-Order Component (HOC)** is a function that takes a component and returns a new component with additional props or behavior. It is a powerful pattern for **reusing component logic** across multiple parts of your application without repeating code."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How HOCs Work"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Think of an HOC as a **wrapper** around your component. The HOC receives the original component, adds some functionality, and returns an enhanced version. The original component remains untouched, which keeps your code clean and predictable."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "hoc-basics-diagram",
        "caption": "How a Higher-Order Component wraps a component to add behavior",
        "definition": "graph LR\n    A[Original Component] --> B{HOC Function}\n    B --> C[Enhanced Component]\n    C --> D[Additional Props]\n    C --> E[New Behavior]\n    style A fill:#e1f5fe,stroke:#01579b,stroke-width:2px\n    style C fill:#f3e5f5,stroke:#4a148c,stroke-width:2px"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic HOC",
        "content": "A simple HOC that wraps a component to add authentication logic",
        "language": "tsx",
        "code": "import { useAuth } from './auth-context';\n\ninterface WithAuthProps {\n  redirectTo?: string;\n}\n\nfunction withAuth<P extends object>(\n  WrappedComponent: React.ComponentType<P>\n) {\n  const AuthenticatedComponent = (props: P & WithAuthProps) => {\n    const { isAuthenticated, redirect } = useAuth();\n\n    if (!isAuthenticated) {\n      redirect(props.redirectTo ?? '/login');\n      return null;\n    }\n\n    return <WrappedComponent {...props} />;\n  };\n\n  AuthenticatedComponent.displayName = `withAuth(${WrappedComponent.displayName ?? WrappedComponent.name ?? 'Component'})`;\n\n  return AuthenticatedComponent;\n}\n\n// Usage\nfunction Dashboard({ title }: { title: string }) {\n  return <h1>{title}</h1>;\n}\n\nDashboard.displayName = 'Dashboard';\n\nconst ProtectedDashboard = withAuth(Dashboard);\n\n// In JSX\n<ProtectedDashboard title=\"My Dashboard\" />;"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Higher-Order Component",
            "definition": "A function that takes a component and returns a new component with enhanced capabilities. It is not a component itself, but a component factory."
          },
          {
            "term": "Wrapping",
            "definition": "The process of placing a component inside an HOC to transform or augment its behavior and props."
          },
          {
            "term": "displayName",
            "definition": "A static property set on the enhanced component to help React DevTools show a meaningful name instead of 'Anonymous' or 'ForwardRef'."
          },
          {
            "term": "Prop Injection",
            "definition": "The technique of passing additional props from the HOC into the wrapped component that the original component did not receive directly."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "HOC vs Render Props"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Both HOCs and Render Props solve the same problem: **sharing logic between components**. However, they use very different approaches. Use the comparison below to decide which pattern fits your use case."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "HOC vs Render Props",
        "cards": [
          {
            "title": "Higher-Order Component",
            "description": "Wraps a component to inject props and behavior automatically.",
            "pros": [
              "Declarative usage - looks like a normal component",
              "Easy to apply to many components at once",
              "Keeps the wrapped component clean"
            ],
            "cons": [
              "Can cause wrapper hell when nesting many HOCs",
              "Static methods must be manually copied",
              "Props can be harder to trace in DevTools"
            ],
            "tags": [
              "reuse",
              "wrapper"
            ]
          },
          {
            "title": "Render Props",
            "description": "Passes a function as a prop to control rendering dynamically.",
            "pros": [
              "Explicit control over what gets rendered",
              "Easy to access shared state in render",
              "No wrapper nesting required"
            ],
            "cons": [
              "Can create deeply nested JSX",
              "Less natural for simple reuse",
              "Can be harder to read with many render props"
            ],
            "tags": [
              "explicit",
              "function"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Naming Conventions"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Naming Your HOC",
        "text": "Prefix HOC names with **with** followed by the behavior they add: `withAuth`, `withLogger`, `withTheme`. Always set a `displayName` so React DevTools can show meaningful names for your enhanced components."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "HOCs are functions that take a component and return an enhanced component",
          "They enable logic reuse without modifying the original component",
          "Use the `with` prefix for naming and always set `displayName`",
          "HOCs inject props and can add any cross-cutting concern"
        ],
        "takeaway": "HOCs are component factories that let you reuse logic across your app."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a withAuth HOC",
        "description": "Build a `withAuth` Higher-Order Component that wraps a protected page component and redirects unauthenticated users to the login page.",
        "steps": [
          "Create a `withAuth` function that accepts a component and returns a new component",
          "Use a mock `useAuth` hook inside the HOC to check authentication status",
          "If the user is not authenticated, redirect them to `/login`",
          "If authenticated, render the wrapped component with all original props",
          "Set a `displayName` on the returned component for debugging"
        ],
        "expectedOutcome": "A working `withAuth` HOC that protects any component from unauthenticated access."
      }
    }
  ]
}
