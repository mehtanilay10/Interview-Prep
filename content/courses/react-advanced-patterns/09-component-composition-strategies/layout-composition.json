{
  "id": "lesson-react-adv-029",
  "slug": "layout-composition",
  "moduleSlug": "component-composition-strategies",
  "courseSlug": "react-advanced-patterns",
  "title": "Layout Composition",
  "description": "Compose complex layouts using React children, clones, and higher-order wrappers to build flexible page structures.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "layout",
    "children",
    "cloneElement",
    "composition",
    "wrappers",
    "page-layout"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "Layout as Composition"
    },
    {
      "type": "paragraph",
      "text": "Layout composition is the practice of building page structures by combining small, single-responsibility wrapper components. Instead of a monolithic PageLayout with dozens of props, you compose the layout from discrete pieces: a Container for max-width, a Stack for vertical spacing, a Grid for columns, and a Section for semantic grouping. Each wrapper adds a single concern, and React's composition model makes the resulting JSX readable and maintainable."
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "Composition over Configuration",
      "text": "Prefer composing wrappers like <Container><Stack><Section>...</Section></Stack></Container> over configuring a single component with twenty props. Composition scales horizontally: you add new wrappers without changing existing ones."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "The Container Pattern"
    },
    {
      "type": "paragraph",
      "text": "A Container is a layout primitive that constrains width and centers content. It does not know what it contains. By accepting props.children, it can wrap any subtree. This makes it composable with any other layout component. You can nest a Container inside a Grid cell, inside a Modal, or inside a Card without coupling them together."
    },
    {
      "type": "example",
      "title": "Reusable Container Component",
      "content": "A container that constrains width and centers content horizontally.",
      "code": "interface ContainerProps {\n  maxWidth?: 'sm' | 'md' | 'lg' | 'xl' | 'full';\n  padding?: boolean;\n  center?: boolean;\n  className?: string;\n  children: React.ReactNode;\n}\n\nconst maxWidthMap = {\n  sm: '640px',\n  md: '768px',\n  lg: '1024px',\n  xl: '1280px',\n  full: '100%',\n};\n\nfunction Container({\n  maxWidth = 'lg',\n  padding = true,\n  center = true,\n  className,\n  children,\n}: ContainerProps) {\n  return (\n    <div\n      className={cn('container', className)}\n      style={{\n        maxWidth: maxWidthMap[maxWidth],\n        marginLeft: center ? 'auto' : undefined,\n        marginRight: center ? 'auto' : undefined,\n        paddingLeft: padding ? '1rem' : undefined,\n        paddingRight: padding ? '1rem' : undefined,\n      }}\n    >\n      {children}\n    </div>\n  );\n}\n\n// Compose layouts freely\n<Page>\n  <Container maxWidth=\"xl\">\n    <Stack gap=\"lg\">\n      <Hero />\n      <Section>\n        <Container maxWidth=\"md\">\n          <Article />\n        </Container>\n      </Section>\n    </Stack>\n  </Container>\n</Page>",
      "language": "tsx"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Stack for Spacing"
    },
    {
      "type": "paragraph",
      "text": "Spacing is one of the most repeated concerns in layout. A Stack component wraps children and applies consistent gap between them. Unlike CSS flexbox gap, which has varying browser support and cannot target specific children, a Stack gives you a declarative gap prop and works with any layout model. You can create horizontal stacks, vertical stacks, and even grid-like stacks by changing the direction."
    },
    {
      "type": "example",
      "title": "Stack Component for Consistent Spacing",
      "content": "A Stack that applies gap between children without wrapper elements.",
      "code": "type Direction = 'row' | 'column' | 'row-reverse' | 'column-reverse';\n\ntype StackProps = {\n  direction?: Direction;\n  gap?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';\n  align?: 'start' | 'center' | 'end' | 'stretch';\n  justify?: 'start' | 'center' | 'end' | 'between';\n  wrap?: boolean;\n  children: React.ReactNode;\n};\n\nconst gapMap = { xs: '0.25rem', sm: '0.5rem', md: '1rem', lg: '1.5rem', xl: '2rem' };\n\nfunction Stack({\n  direction = 'column',\n  gap = 'md',\n  align = 'stretch',\n  justify = 'start',\n  wrap = false,\n  children,\n}: StackProps) {\n  return (\n    <div\n      style={{\n        display: 'flex',\n        flexDirection: direction,\n        gap: gapMap[gap],\n        alignItems: align,\n        justifyContent: justify === 'between' ? 'space-between' : justify,\n        flexWrap: wrap ? 'wrap' : 'nowrap',\n      }}\n    >\n      {children}\n    </div>\n  );\n}\n\n// Usage\n<Stack direction=\"column\" gap=\"lg\">\n  <Card title=\"First\" />\n  <Card title=\"Second\" />\n  <Card title=\"Third\" />\n</Stack>",
      "language": "tsx"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "React.cloneElement for Layout Injection"
    },
    {
      "type": "paragraph",
      "text": "Sometimes a layout wrapper needs to inject props or wrapper elements into its children without changing the child's source code. React.cloneElement lets you clone a child element and merge new props into it. Use this sparingly, because it creates tight coupling between parent and child, but it is invaluable for patterns like tabs, lists, and grids where the parent needs to add data attributes, event handlers, or wrapper markup."
    },
    {
      "type": "example",
      "title": "Grid with cloneElement Injection",
      "content": "A grid that clones children and injects grid item props.",
      "code": "interface GridProps {\n  columns?: number;\n  gap?: number;\n  children: React.ReactNode;\n}\n\nfunction Grid({ columns = 3, gap = 16, children }: GridProps) {\n  return (\n    <div\n      style={{\n        display: 'grid',\n        gridTemplateColumns: `repeat(${columns}, 1fr)`,\n        gap,\n      }}\n    >\n      {React.Children.map(children, (child, index) => {\n        if (React.isValidElement(child)) {\n          return React.cloneElement(child, {\n            'data-grid-index': index,\n            className: cn(child.props.className, 'grid-item'),\n          });\n        }\n        return child;\n      })}\n    </div>\n  );\n}\n\n// Usage\n<Grid columns={3} gap={24}>\n  <Card title=\"A\" />\n  <Card title=\"B\" />\n  <Card title=\"C\" />\n</Grid>",
      "language": "tsx"
    },
    {
      "type": "callout",
      "variant": "warning",
      "text": "cloneElement breaks the normal unidirectional data flow. If you rely on it heavily, your components become harder to debug because props come from both the parent and the child's own declaration. Prefer composition or render props unless cloneElement is the only practical solution."
    },
    {
      "type": "mermaid",
      "id": "layout-composition-tree",
      "caption": "A typical layout composition tree using wrapper components.",
      "definition": "graph TD\n    A[Page] --> B[Container]\n    B --> C[Stack]\n    C --> D[Header]\n    C --> E[Main]\n    E --> F[Grid]\n    F --> G[Card]\n    F --> H[Card]\n    F --> I[Card]\n    B --> J[Footer]"
    },
    {
      "type": "bullet-list",
      "title": "Layout Composition Rules",
      "items": [
        "Each wrapper should do exactly one thing: constrain width, apply spacing, or change direction.",
        "Avoid layout components that require specific children, like Header and Main, unless you are building a page framework.",
        "Use CSS Grid and Flexbox primitives in the implementation, but hide them behind semantic wrapper names.",
        "Prefer composition over cloneElement because composition is easier to reason about and test.",
        "Document the default gap, direction, and responsive behavior so consumers know what to expect."
      ]
    },
    {
      "type": "summary-box",
      "title": "Key Takeaways",
      "points": [
        "Layout composition builds complex pages from small, focused wrapper components.",
        "Container, Stack, and Grid are the three most reusable layout primitives.",
        "props.children is the foundation of layout composition because it lets wrappers be transparent.",
        "React.cloneElement can inject props into children, but it creates tight coupling and should be used sparingly.",
        "Composition keeps JSX readable, props minimal, and each layout concern independently testable."
      ],
      "takeaway": "Good layout composition feels like building with LEGO blocks: each piece is simple, but the combinations are powerful."
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": [
    "compound-basics"
  ]
}
