{
  "id": "lesson-react-adv-028",
  "slug": "polymorphic-components",
  "moduleSlug": "component-composition-strategies",
  "courseSlug": "react-advanced-patterns",
  "title": "Polymorphic Components",
  "description": "Create polymorphic components using the 'as' pattern to render as different HTML elements or React components while preserving props and behavior.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": ["polymorphic", "as-pattern", "forwardRef", "component-prop", "generic-components"],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "What Is Polymorphism in React?"
    },
    {
      "type": "paragraph",
      "text": "A polymorphic component is a component that can render as different underlying elements depending on a prop, most commonly called as. Instead of hardcoding a div or button, the component delegates the root element to the consumer. This is extremely useful for design system primitives like Text, Heading, or Button where the semantic HTML element should change based on context, but the visual styling and behavior remain constant."
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "Real-World Example",
      "text": "A Heading component might render as an h1 on a landing page, an h2 inside a card, or a p when used as a caption. Without polymorphism, you would need Heading1, Heading2, and Caption components, all duplicating the same color, font, and spacing logic."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "The 'as' Prop"
    },
    {
      "type": "paragraph",
      "text": "The as prop accepts either a string representing an HTML tag or a React component type. When the component renders, it uses React.createElement to create the underlying element with all the forwarded props. The tricky part is TypeScript: you need the component's props to change based on the value of as so that consumers get proper autocomplete and type checking for href, type, disabled, and other element-specific attributes."
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Polymorphic Component",
          "definition": "A component that can render as different element types via an as prop while maintaining a consistent API."
        },
        {
          "term": "as Prop",
          "definition": "A prop that accepts a string tag name or component type, telling the polymorphic component what to render as."
        },
        {
          "term": "ElementProps",
          "definition": "A TypeScript utility type that extracts the props of a given HTML element or component."
        },
        {
          "term": "forwardRef",
          "definition": "A React API that lets a component pass a ref through to a DOM node or class component."
        }
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Basic Polymorphic Component"
    },
    {
      "type": "paragraph",
      "text": "The simplest polymorphic component uses a union type for the as prop and casts the element type at render time. You lose some type safety, but the pattern works well for internal components where the consumer is trusted. The key is to use React.ElementType as the type for as and then cast the rest of the props accordingly."
    },
    {
      "type": "example",
      "title": "Basic Polymorphic Text Component",
      "content": "A Text component that can render as any HTML element.",
      "code": "import React from 'react';\n\ninterface TextProps<E extends React.ElementType = 'span'> {\n  as?: E;\n  children: React.ReactNode;\n  className?: string;\n}\n\ntype TextComponent = <E extends React.ElementType = 'span'>(\n  props: TextProps<E> & Omit<React.ComponentProps<E>, keyof TextProps<E>>\n) => React.ReactElement | null;\n\nconst Text: TextComponent = ({ as, children, className, ...rest }) => {\n  const Component = as || 'span';\n  return (\n    <Component className={`text ${className || ''}`} {...rest}>\n      {children}\n    </Component>\n  );\n};\n\n// Usage\n<Text as=\"p\" className=\"lead\">\n  This renders as a paragraph.\n</Text>\n<Text as=\"a\" href=\"#\" className=\"link\">\n  This renders as a link.\n</Text>\n<Text as=\"label\" htmlFor=\"email\">\n  This renders as a label.\n</Text>",
      "language": "tsx"
    },
    {
      "type": "callout",
      "variant": "warning",
      "text": "When spreading rest props onto the dynamic Component, TypeScript cannot always infer which props are valid. Using Omit to remove overlapping keys from the intersection type is necessary but does not guarantee runtime safety. Validate critical props manually if needed."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Polymorphic Button with Variants"
    },
    {
      "type": "paragraph",
      "text": "A polymorphic Button component is one of the most common use cases. It should render as a button by default, but allow as='a' for link-like behavior. When rendered as an anchor, it should accept href, target, and rel. When rendered as a button, it should accept type and disabled. The component must merge its own variant styles with the consumer's className and event handlers."
    },
    {
      "type": "example",
      "title": "Polymorphic Button Component",
      "content": "A button that adapts its underlying element and props based on the as prop.",
      "code": "type ButtonProps<E extends React.ElementType = 'button'> = {\n  as?: E;\n  variant?: 'primary' | 'secondary' | 'ghost';\n  size?: 'sm' | 'md' | 'lg';\n  isLoading?: boolean;\n  children: React.ReactNode;\n  className?: string;\n} & Omit<React.ComponentProps<E>, keyof ButtonProps<E>>;\n\nfunction Button<E extends React.ElementType = 'button'>({\n  as,\n  variant = 'primary',\n  size = 'md',\n  isLoading = false,\n  children,\n  className,\n  ...rest\n}: ButtonProps<E>) {\n  const Component = as || 'button';\n\n  const classes = [\n    'btn',\n    `btn-${variant}`,\n    `btn-${size}`,\n    isLoading ? 'btn-loading' : '',\n    className || '',\n  ]\n    .filter(Boolean)\n    .join(' ');\n\n  return (\n    <Component className={classes} disabled={isLoading || (rest as { disabled?: boolean }).disabled} {...rest}>\n      {isLoading && <span className=\"spinner\" aria-hidden=\"true\" />}\n      {children}\n    </Component>\n  );\n}\n\n// Usage\n<Button variant=\"primary\" size=\"lg\" onClick={handleClick}>\n  Submit\n</Button>\n\n<Button as=\"a\" href=\"/login\" variant=\"secondary\">\n  Sign In\n</Button>\n\n<Button as={Link} to=\"/dashboard\" variant=\"ghost\">\n  Dashboard\n</Button>",
      "language": "tsx"
    },
    {
      "type": "mermaid",
      "id": "polymorphic-flow",
      "caption": "How the as prop routes rendering to different element types.",
      "definition": "flowchart LR\n    A[Button Props] -->|as='button'| B[HTMLButtonElement]\n    A -->|as='a'| C[HTMLAnchorElement]\n    A -->|as=Link| D[React Component]\n    B --> E[Rendered Output]\n    C --> E\n    D --> E\n    E -->|preserves| F[variant & size styles]"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Forwarding Refs in Polymorphic Components"
    },
    {
      "type": "paragraph",
      "text": "Polymorphic components almost always need to forward refs so consumers can measure DOM nodes, focus elements, or integrate with third-party libraries. Because the underlying element type is dynamic, the ref type must also be generic. React.forwardRef combined with a generic type parameter gives you a ref that resolves to the correct DOM element or component instance."
    },
    {
      "type": "example",
      "title": "Polymorphic Component with forwardRef",
      "content": "Forward refs through a polymorphic component with correct typing.",
      "code": "import React from 'react';\n\ntype PolymorphicProps<E extends React.ElementType = 'button'> = {\n  as?: E;\n  children: React.ReactNode;\n} & Omit<React.ComponentProps<E>, keyof PolymorphicProps<E>>;\n\nfunction PolymorphicComponent<E extends React.ElementType = 'button'>(\n  { as, children, ...rest }: PolymorphicProps<E>,\n  ref: React.Ref<React.ElementType<E>>\n) {\n  const Component = as || 'button';\n  return (\n    <Component ref={ref} {...rest}>\n      {children}\n    </Component>\n  );\n}\n\nconst ForwardedPolymorphic = React.forwardRef(\n  PolymorphicComponent\n) as <E extends React.ElementType = 'button'>(\n  props: PolymorphicProps<E> & { ref?: React.Ref<React.ElementType<E>> }\n) => React.ReactElement | null;\n\n// Usage with ref\nconst btnRef = React.useRef<HTMLButtonElement>(null);\n\n<ForwardedPolymorphic ref={btnRef} variant=\"primary\">\n  Click me\n</ForwardedPolymorphic>\n\n<ForwardedPolymorphic as=\"a\" href=\"#\" ref={btnRef as React.Ref<HTMLAnchorElement>}>\n  Link\n</ForwardedPolymorphic>",
      "language": "tsx"
    },
    {
      "type": "bullet-list",
      "title": "Polymorphic Component Best Practices",
      "items": [
        "Always default as to a semantically appropriate element like button or span.",
        "Type the as prop as React.ElementType or a constrained generic so consumers get autocomplete.",
        "Use Omit to remove overlapping keys between your component props and the underlying element props.",
        "Forward refs so the component works with focus management and DOM measurement libraries.",
        "Document which element-specific props are available for each possible as value."
      ]
    },
    {
      "type": "faq-block",
      "title": "Polymorphic Components FAQ",
      "items": [
        {
          "question": "Should I use a string 'as' prop or a component prop?",
          "answer": "Use a string for standard HTML elements and a component prop for React components like Link or RouterLink. Strings are easier to type and optimize. Component props are necessary when you need to pass complex props or context."
        },
        {
          "question": "How do I handle conflicting prop names?",
          "answer": "Use the Omit utility to remove conflicting keys from the intersection type. If your component defines an onClick prop and the underlying element also has onClick, rename one of them or merge them carefully."
        },
        {
          "question": "Is polymorphism worth the type complexity?",
          "answer": "Yes, for design system primitives. The type complexity pays off because consumers get a single component API instead of five nearly identical components. For one-off components, polymorphism is usually overkill."
        }
      ]
    },
    {
      "type": "summary-box",
      "title": "Key Takeaways",
      "points": [
        "Polymorphic components use the as prop to render as different elements while keeping the same API.",
        "TypeScript generics with React.ElementType let you infer the correct props for each underlying element.",
        "forwardRef is essential for polymorphic components so consumers can access the actual DOM node.",
        "The Omit utility removes prop name conflicts between your component API and the underlying element.",
        "Use polymorphism for design system primitives and avoid it for application-specific components."
      ],
      "takeaway": "Polymorphism lets one component wear many HTML masks, dramatically reducing API surface area in component libraries."
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": ["render-props-basics"]
}
