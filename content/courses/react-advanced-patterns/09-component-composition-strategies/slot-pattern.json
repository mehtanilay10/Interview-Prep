{
  "id": "lesson-react-adv-026",
  "slug": "slot-pattern",
  "moduleSlug": "component-composition-strategies",
  "courseSlug": "react-advanced-patterns",
  "title": "Slot Pattern",
  "description": "Learn how to use the slot pattern to create flexible, composable components that accept arbitrary content at named injection points.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "slots",
    "composition",
    "content-injection",
    "flexible-ui",
    "render-props"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Is the Slot Pattern?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The slot pattern is a composition technique where a parent component defines named regions—called slots—that consumers fill with arbitrary content. This concept originates from web components and Angular content projection, but in React it is implemented using props.children, named children props, or render functions. Slots let you build highly reusable layout and container components while giving downstream developers full control over what renders inside each region."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Why Slots Matter",
        "text": "Without slots, you either bake every possible variation into a prop explosion or force consumers to wrap your component in awkward extra elements. Slots give you the best of both worlds: a stable outer shell with customizable inner regions."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Named Slots with React Children"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React's props.children gives you a single unnamed default slot. To create named slots, you can pass additional render props or use a single composition object. Each slot receives exactly the data it needs to render independently. The parent component orchestrates layout and context, while each slot focuses only on its own content."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Default Slot",
            "definition": "The unnamed children prop that renders inside the primary content area of a component."
          },
          {
            "term": "Named Slot",
            "definition": "A labeled content region, such as header, sidebar, or footer, passed via a dedicated prop or composition object."
          },
          {
            "term": "Content Projection",
            "definition": "The act of passing JSX from a consumer into a component so the component decides where to render it."
          },
          {
            "term": "Slot Props",
            "definition": "Data passed from the parent down into each slot's render function, enabling slots to access context or state."
          },
          {
            "term": "Composition Object",
            "definition": "A single prop whose keys map to slot names and whose values are ReactNode or render functions, grouping multiple slots into one declarative argument."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Implementing Named Slots"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A Card component is a classic example. It has a header, body, and footer region. Instead of relying on consumers to nest separate Header, Body, and Footer components manually, you define one Card component with named slot props. This keeps JSX clean and prevents invalid nesting."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Card Component with Named Slots",
        "content": "A Card component that accepts header, body, and footer slots as separate props.",
        "code": "import React from 'react';\n\ntype CardSlots = {\n  header?: React.ReactNode;\n  body?: React.ReactNode;\n  footer?: React.ReactNode;\n};\n\nfunction Card({ header, body, footer }: CardSlots) {\n  return (\n    <div className=\"card\">\n      {header && <div className=\"card-header\">{header}</div>}\n      {body && <div className=\"card-body\">{body}</div>}\n      {footer && <div className=\"card-footer\">{footer}</div>}\n    </div>\n  );\n}\n\n// Usage\n<Card\n  header={<h2>Profile</h2>}\n  body={<p>User bio goes here.</p>}\n  footer={<button>Follow</button>}\n/>",
        "language": "tsx"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "text": "Use short-circuit evaluation when rendering slots. This prevents empty wrapper elements from appearing when a slot is not provided."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Render Props as Slots"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Sometimes a slot needs data from the parent component, such as loading state, error messages, or pagination controls. In those cases, the slot should be a function that receives the data as an argument. This is the render-prop variant of the slot pattern. It gives the slot full control over how to use the data without tightly coupling the child to the parent's state shape."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "DataList with Render Prop Slots",
        "content": "A list component that passes item data, loading state, and error information into slot render functions.",
        "code": "import React from 'react';\n\ntype DataListRenderProps<T> = {\n  data: T[];\n  loading: boolean;\n  error: Error | null;\n  renderItem: (item: T) => React.ReactNode;\n  renderEmpty?: () => React.ReactNode;\n  renderError?: (error: Error) => React.ReactNode;\n};\n\nfunction DataList<T>({\n  data,\n  loading,\n  error,\n  renderItem,\n  renderEmpty,\n  renderError,\n}: DataListRenderProps<T>) {\n  if (loading) return <div className=\"loading\">Loading...</div>;\n  if (error && renderError) return <>{renderError(error)}</>;\n  if (data.length === 0 && renderEmpty) return <>{renderEmpty()}</>;\n\n  return (\n    <ul className=\"data-list\">\n      {data.map((item, index) => (\n        <li key={index}>{renderItem(item)}</li>\n      ))}\n    </ul>\n  );\n}\n\n// Usage\n<DataList\n  data={users}\n  loading={isLoading}\n  error={error}\n  renderItem={(user) => (\n    <div className=\"user-card\">\n      <img src={user.avatar} alt={user.name} />\n      <span>{user.name}</span>\n    </div>\n  )}\n  renderEmpty={() => <p>No users found.</p>}\n/>",
        "language": "tsx"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Use Case",
          "Slot Type",
          "Benefit"
        ],
        "rows": [
          [
            "Page layout with header/body/footer",
            "Named props",
            "Explicit API, easy to type"
          ],
          [
            "Dashboard with dynamic regions",
            "Composition object",
            "Scales to many slots"
          ],
          [
            "Data-driven list rendering",
            "Render props",
            "Slots access internal state"
          ],
          [
            "Simple content wrapping",
            "props.children",
            "Idiomatic, no extra props"
          ],
          [
            "Tabbed interfaces",
            "Render props + state",
            "Slots share active state"
          ]
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "slot-pattern-flow",
        "caption": "The slot pattern separates a parent's layout shell from the consumer's content.",
        "definition": "graph LR\n    A[Parent Component] -->|passes| B[Slot: header]\n    A -->|passes| C[Slot: body]\n    A -->|passes| D[Slot: footer]\n    B -->|renders inside| E[Component Shell]\n    C -->|renders inside| E\n    D -->|renders inside| E\n    E -->|outputs| F[Composed UI]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Composition Object Pattern"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For components with many slots, a single composition-object prop can be cleaner than many individual props. You define a type that maps slot names to ReactNode or render functions, and the component iterates over the object to render each slot in its designated region. This pattern scales well when the number of slots is dynamic or when you want to support custom slot registration."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "DashboardLayout with Composition Object",
        "content": "A dashboard layout that accepts a single slots object containing header, sidebar, main, and footer.",
        "code": "import React from 'react';\n\ntype DashboardSlots = {\n  header?: React.ReactNode;\n  sidebar?: React.ReactNode;\n  main?: React.ReactNode;\n  footer?: React.ReactNode;\n};\n\nfunction DashboardLayout({ slots }: { slots: DashboardSlots }) {\n  return (\n    <div className=\"dashboard\">\n      {slots.header && (\n        <header className=\"dashboard-header\">{slots.header}</header>\n      )}\n      <div className=\"dashboard-body\">\n        {slots.sidebar && (\n          <aside className=\"dashboard-sidebar\">{slots.sidebar}</aside>\n        )}\n        <main className=\"dashboard-main\">{slots.main}</main>\n      </div>\n      {slots.footer && (\n        <footer className=\"dashboard-footer\">{slots.footer}</footer>\n      )}\n    </div>\n  );\n}\n\n// Usage\n<DashboardLayout\n  slots={{\n    header: <TopBar user={user} />,\n    sidebar: <NavMenu items={navItems} />,\n    main: <DashboardContent data={data} />,\n    footer: <popyright />,\n  }}\n/>",
        "language": "tsx"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Slot Implementation Approaches",
        "cards": [
          {
            "title": "props.children",
            "description": "Use a single default slot for the main content area.",
            "pros": [
              "Simple and idiomatic",
              "No extra props needed"
            ],
            "cons": [
              "Only one unnamed region",
              "Cannot target specific areas"
            ],
            "tags": [
              "default",
              "simple"
            ]
          },
          {
            "title": "Named Props",
            "description": "Declare each slot as a separate optional prop.",
            "pros": [
              "Explicit API",
              "IDE autocomplete",
              "Type-safe"
            ],
            "cons": [
              "More props to document",
              "Can feel verbose with many slots"
            ],
            "tags": [
              "explicit",
              "type-safe"
            ]
          },
          {
            "title": "Composition Object",
            "description": "Pass a single object mapping slot names to content.",
            "pros": [
              "Scales to many slots",
              "Single prop to document"
            ],
            "cons": [
              "Less IDE guidance",
              "Slightly more nested JSX"
            ],
            "tags": [
              "scalable",
              "flexible"
            ]
          },
          {
            "title": "Render Props",
            "description": "Pass functions as slots to receive parent data.",
            "pros": [
              "Slots get internal state",
              "Maximum flexibility"
            ],
            "cons": [
              "More complex API",
              "Can overfit if data shape changes"
            ],
            "tags": [
              "data-driven",
              "flexible"
            ]
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Card Component with Named Slots",
        "description": "Create a Card component that accepts header, body, and footer named slots. The component should render each slot inside a distinctly styled region and gracefully handle missing slots.",
        "steps": [
          "Define a CardSlots type with optional header, body, and footer props of type ReactNode.",
          "Render the header inside a styled div, the body inside the main content area, and the footer at the bottom.",
          "Use short-circuit evaluation so empty slots do not produce empty DOM elements.",
          "Add a named-slot prop for an optional 'action' region and render it in the header next to the title.",
          "Consume the component by passing JSX to each slot and verify the layout holds."
        ],
        "expectedOutcome": "A reusable Card component where consumers can inject custom content into named regions without touching the component's internal layout logic."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "The slot pattern lets consumers inject content into named regions of a component.",
          "Use props.children for the default slot and additional props for named slots.",
          "Render props turn slots into functions that receive data from the parent.",
          "A composition object groups all slots into a single prop, which scales better for large components.",
          "Named slots prevent prop drilling and keep JSX clean when a component has multiple customizable regions."
        ],
        "takeaway": "Slots shift control from the component author to the component consumer, making layouts reusable without sacrificing flexibility."
      }
    }
  ],
  "relatedLessons": [
    "headless-components"
  ],
  "furtherReading": [
    {
      "title": "Thinking in React: Composition",
      "url": "https://react.dev/learn/thinking-in-react#composition",
      "type": "article",
      "author": "React Team",
      "description": "Official React guidance on why composition is preferred over inheritance for building component hierarchies."
    },
    {
      "title": "Content Projection in React: Patterns and Best Practices",
      "url": "https://www.smashingmagazine.com/2020/05/composition-react-content-projection/",
      "type": "article",
      "author": "Sarah Drasner",
      "description": "A deep dive into slot and content projection patterns across React and other frameworks."
    },
    {
      "title": "Vue slots vs React children: A comparison",
      "url": "https://ui.dev/recipes/slots-in-react",
      "type": "article",
      "author": "Tyler McGinnis",
      "description": "Compares slot implementations between Vue and React, with practical code examples."
    }
  ],
  "prerequisites": [
    "render-props-basics"
  ]
}
