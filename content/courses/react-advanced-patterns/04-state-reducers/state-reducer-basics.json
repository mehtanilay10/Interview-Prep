{
  "id": "lesson-react-ap-010",
  "slug": "state-reducer-basics",
  "moduleSlug": "state-reducers",
  "title": "State Reducer Basics",
  "description": "Understand the state reducer pattern for flexible state management.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 22,
  "tags": ["react", "state-reducer", "patterns", "customization"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **state reducer pattern** allows consumers to intercept and customize state changes. It's inspired by Redux reducers and gives users full control over how state updates."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How It Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Instead of directly updating state, the component calls a reducer function. The consumer can provide a custom reducer to override default behavior."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Toggle with Reducer",
        "content": "Basic state reducer implementation",
        "language": "jsx",
        "code": "function toggleReducer(state, action) {\n  switch (action.type) {\n    case 'TOGGLE': return { on: !state.on };\n    case 'RESET': return { on: false };\n    default: return state;\n  }\n}\n\nfunction useToggle({ reducer = toggleReducer } = {}) {\n  const [state, dispatch] = useReducer(reducer, { on: false });\n\n  const toggle = () => dispatch({ type: 'TOGGLE' });\n  const reset = () => dispatch({ type: 'RESET' });\n\n  return { on: state.on, toggle, reset };\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Customizing Behavior"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Custom Reducer",
        "content": "Overriding default behavior",
        "language": "jsx",
        "code": "function App() {\n  const { on, toggle } = useToggle({\n    reducer: (state, action) => {\n      switch (action.type) {\n        case 'TOGGLE':\n          // Don't allow turning off\n          if (state.on) return state;\n          return { on: !state.on };\n        default:\n          return toggleReducer(state, action);\n      }\n    }\n  });\n\n  return <button onClick={toggle}>{on ? 'ON' : 'OFF'}</button>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Use Cases",
        "text": "State reducers are useful when building reusable component libraries where users need to customize behavior without modifying source code."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "State reducers intercept state changes",
          "Consumers can customize behavior",
          "Inspired by Redux reducers",
          "Great for reusable component libraries"
        ],
        "takeaway": "State reducers give users control over component behavior."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
