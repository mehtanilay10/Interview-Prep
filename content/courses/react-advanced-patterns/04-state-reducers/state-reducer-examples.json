{
  "id": "lesson-react-ap-011",
  "slug": "state-reducer-examples",
  "moduleSlug": "state-reducers",
  "title": "State Reducer Examples",
  "description": "Explore practical state reducer implementations.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 22,
  "tags": [
    "react",
    "state-reducer",
    "examples",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Let's explore practical state reducer implementations for common scenarios."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Counter with Limits"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Counter",
        "content": "Counter with customizable limits",
        "language": "jsx",
        "code": "function counterReducer(state, action) {\n  switch (action.type) {\n    case 'INCREMENT': return { count: state.count + 1 };\n    case 'DECREMENT': return { count: state.count - 1 };\n    case 'RESET': return { count: 0 };\n    default: return state;\n  }\n}\n\nfunction useCounter({ initialCount = 0, reducer = counterReducer, max = Infinity, min = -Infinity } = {}) {\n  const [state, dispatch] = useReducer(reducer, { count: initialCount });\n\n  const enhancedReducer = (state, action) => {\n    const newState = reducer(state, action);\n    if (newState.count > max || newState.count < min) {\n      return state; // Prevent change\n    }\n    return newState;\n  };\n\n  // ... expose actions\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Dropdown with Logging"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dropdown",
        "content": "Dropdown with logging reducer",
        "language": "jsx",
        "code": "function useDropdown({ reducer } = {}) {\n  const defaultReducer = (state, action) => {\n    switch (action.type) {\n      case 'OPEN': return { ...state, isOpen: true };\n      case 'CLOSE': return { ...state, isOpen: false };\n      default: return state;\n    }\n  };\n\n  const [state, dispatch] = useReducer(\n    reducer || defaultReducer,\n    { isOpen: false, selectedItem: null }\n  );\n\n  return { ...state, open: () => dispatch({ type: 'OPEN' }), close: () => dispatch({ type: 'CLOSE' }) };\n}\n\n// Consumer adds logging\nconst { isOpen, open, close } = useDropdown({\n  reducer: (state, action) => {\n    console.log('Action:', action.type, 'State:', state);\n    const newState = defaultReducer(state, action);\n    console.log('New State:', newState);\n    return newState;\n  }\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Action Types",
        "text": "Document action types so consumers know what actions they can intercept and customize."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Counters can have customizable limits",
          "Dropdowns can add logging via reducers",
          "Document action types for consumers",
          "Combine default behavior with custom logic"
        ],
        "takeaway": "State reducers enable powerful customization."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "react-advanced-patterns"
}