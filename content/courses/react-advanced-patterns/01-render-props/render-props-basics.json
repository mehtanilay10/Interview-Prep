{
  "id": "lesson-react-ap-001",
  "slug": "render-props-basics",
  "moduleSlug": "render-props",
  "title": "Render Props Basics",
  "description": "Understand the render props pattern for component composition.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "render-props",
    "composition",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **render props** pattern is a technique for sharing code between components using a prop whose value is a function. The component calls this function instead of rendering its own UI."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How Render Props Work"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A component receives a function as a prop (often called `render` or `children`) and calls it to determine what to render."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Render Prop",
        "content": "Component with render prop",
        "language": "jsx",
        "code": "function DataFetcher({ url, render }) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    fetch(url)\n      .then(res => res.json())\n      .then(data => {\n        setData(data);\n        setLoading(false);\n      });\n  }, [url]);\n\n  return render({ data, loading });\n}\n\n// Usage\n<DataFetcher\n  url=\"/api/users\"\n  render={({ data, loading }) => {\n    if (loading) return <p>Loading...</p>;\n    return <ul>{data.map(u => <li key={u.id}>{u.name}</li>)}</ul>;\n  }}\n/>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Children as Render Prop"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Instead of a named prop, you can use `children` as the render prop for a more natural JSX syntax."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Children Render Prop",
        "content": "Using children as render prop",
        "language": "jsx",
        "code": "function MouseTracker({ children }) {\n  const [position, setPosition] = useState({ x: 0, y: 0 });\n\n  const handleMouseMove = (e) => {\n    setPosition({ x: e.clientX, y: e.clientY });\n  };\n\n  return (\n    <div onMouseMove={handleMouseMove}>\n      {children(position)}\n    </div>\n  );\n}\n\n// Usage\n<MouseTracker>\n  {({ x, y }) => (\n    <p>Mouse is at ({x}, {y})</p>\n  )}\n</MouseTracker>"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "When to Use Render Props",
        "text": "Render props are useful when you need to share logic but want the consumer to control the rendering. However, custom hooks have largely replaced this pattern in modern React."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Render props share logic via a function prop",
          "Children can be used as render prop",
          "Consumer controls what to render",
          "Custom hooks are often a modern alternative"
        ],
        "takeaway": "Render props enable flexible component composition."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "react-advanced-patterns"
}