{
  "id": "lesson-react-ap-002",
  "slug": "render-props-examples",
  "moduleSlug": "render-props",
  "title": "Render Props Examples",
  "description": "Explore practical examples of the render props pattern.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 22,
  "tags": [
    "react",
    "render-props",
    "examples",
    "composition"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Let's explore practical examples of render props in action."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Toggle Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Toggle with Render Prop",
        "content": "Reusable toggle logic",
        "language": "jsx",
        "code": "function Toggle({ render }) {\n  const [on, setOn] = useState(false);\n  const toggle = () => setOn(!on);\n\n  return render({ on, toggle });\n}\n\n// Usage\n<Toggle>\n  {({ on, toggle }) => (\n    <div>\n      <p>The switch is {on ? 'ON' : 'OFF'}</p>\n      <button onClick={toggle}>Toggle</button>\n    </div>\n  )}\n</Toggle>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Form Input Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Form Input",
        "content": "Reusable input with validation",
        "language": "jsx",
        "code": "function FormInput({ name, validate, render }) {\n  const [value, setValue] = useState('');\n  const [touched, setTouched] = useState(false);\n\n  const error = validate ? validate(value) : null;\n\n  return render({\n    name,\n    value,\n    error: touched ? error : null,\n    onChange: (e) => setValue(e.target.value),\n    onBlur: () => setTouched(true)\n  });\n}\n\n// Usage\n<FormInput\n  name=\"email\"\n  validate={(v) => !v.includes('@') ? 'Invalid email' : null}\n  render={({ name, value, error, onChange, onBlur }) => (\n    <div>\n      <input name={name} value={value} onChange={onChange} onBlur={onBlur} />\n      {error && <span className=\"error\">{error}</span>}\n    </div>\n  )}\n/>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Copy to Clipboard"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Clipboard",
        "content": "Copy text to clipboard",
        "language": "jsx",
        "code": "function CopyToClipboard({ text, render }) {\n  const [copied, setCopied] = useState(false);\n\n  const copy = async () => {\n    await navigator.clipboard.writeText(text);\n    setCopied(true);\n    setTimeout(() => setCopied(false), 2000);\n  };\n\n  return render({ copied, copy });\n}\n\n// Usage\n<CopyToClipboard text=\"Hello World\">\n  {({ copied, copy }) => (\n    <button onClick={copy}>\n      {copied ? 'Copied!' : 'Copy'}\n    </button>\n  )}\n</CopyToClipboard>"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Benefits",
        "text": "Render props separate logic from presentation, making both reusable and testable independently."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Toggle logic can be shared via render props",
          "Form inputs benefit from reusable validation",
          "Utility features like clipboard can use render props",
          "Logic and presentation are separated"
        ],
        "takeaway": "Render props enable flexible, reusable component logic."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a Custom Hook",
        "description": "Build a useLocalStorage hook that syncs state with localStorage.",
        "steps": [
          "Accept key and initialValue",
          "Read from localStorage on mount",
          "Write to localStorage on change"
        ],
        "expectedOutcome": "Reusable useLocalStorage hook."
      }
    }
  ],
  "courseSlug": "react-advanced-patterns"
}
