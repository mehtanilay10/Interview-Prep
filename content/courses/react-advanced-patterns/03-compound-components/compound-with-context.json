{
  "id": "lesson-react-ap-009",
  "slug": "compound-with-context",
  "moduleSlug": "compound-components",
  "title": "Compound Components with Context",
  "description": "Master using context to share state in compound components.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": [
    "react",
    "compound",
    "context",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Context is essential for compound components. It allows the parent to share state with deeply nested children without explicit prop passing."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating Context"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Context Setup",
        "content": "Creating context for compound components",
        "language": "jsx",
        "code": "import { createContext, useContext, useState } from 'react';\n\n// Create context with default values\nconst MenuContext = createContext({\n  isOpen: false,\n  toggle: () => {},\n  selectedItem: null,\n  selectItem: () => {}\n});\n\n// Custom hook for consuming context\nfunction useMenu() {\n  const context = useContext(MenuContext);\n  if (!context) {\n    throw new Error('Menu components must be used within a Menu');\n  }\n  return context;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Menu Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Menu",
        "content": "Building a menu with context",
        "language": "jsx",
        "code": "function Menu({ children }) {\n  const [isOpen, setIsOpen] = useState(false);\n  const [selectedItem, setSelectedItem] = useState(null);\n\n  const toggle = () => setIsOpen(prev => !prev);\n  const selectItem = (item) => {\n    setSelectedItem(item);\n    setIsOpen(false);\n  };\n\n  return (\n    <MenuContext.Provider value={{ isOpen, toggle, selectedItem, selectItem }}>\n      <div className=\"menu\">{children}</div>\n    </MenuContext.Provider>\n  );\n}\n\nfunction MenuButton({ children }) {\n  const { isOpen, toggle } = useMenu();\n  return (\n    <button onClick={toggle}>\n      {children} {isOpen ? '▲' : '▼'}\n    </button>\n  );\n}\n\nfunction MenuList({ children }) {\n  const { isOpen } = useMenu();\n  return isOpen ? <ul className=\"menu-list\">{children}</ul> : null;\n}\n\nfunction MenuItem({ id, children }) {\n  const { selectItem, selectedItem } = useMenu();\n  const isSelected = selectedItem === id;\n  return (\n    <li\n      className={isSelected ? 'selected' : ''}\n      onClick={() => selectItem(id)}\n    >\n      {children}\n    </li>\n  );\n}\n\nMenu.Button = MenuButton;\nMenu.List = MenuList;\nMenu.Item = MenuItem;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Usage"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Menu Usage",
        "content": "Using the menu component",
        "language": "jsx",
        "code": "<Menu>\n  <MenuButton>Options</MenuButton>\n  <MenuList>\n    <MenuItem id=\"edit\">Edit</MenuItem>\n    <MenuItem id=\"delete\">Delete</MenuItem>\n    <MenuItem id=\"share\">Share</MenuItem>\n  </MenuList>\n</Menu>"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Error Handling",
        "text": "The useMenu hook throws an error if used outside Menu, helping developers catch mistakes early."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Context provides implicit state sharing",
          "Custom hooks encapsulate context consumption",
          "Error handling catches misuse",
          "Compound components + context = powerful APIs"
        ],
        "takeaway": "Context is the backbone of compound components."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a Custom Hook",
        "description": "Build a useLocalStorage hook that syncs state with localStorage.",
        "steps": [
          "Accept key and initialValue",
          "Read from localStorage on mount",
          "Write to localStorage on change"
        ],
        "expectedOutcome": "Reusable useLocalStorage hook."
      }
    }
  ],
  "courseSlug": "react-advanced-patterns"
}
