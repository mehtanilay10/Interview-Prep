{
  "id": "lesson-react-ap-008",
  "slug": "compound-examples",
  "moduleSlug": "compound-components",
  "title": "Compound Component Examples",
  "description": "Explore practical compound component implementations.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 28,
  "tags": [
    "react",
    "compound",
    "examples",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Let's explore practical compound component implementations."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Tabs Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Tabs",
        "content": "Building a tabs component",
        "language": "jsx",
        "code": "function Tabs({ children, defaultIndex = 0 }) {\n  const [activeIndex, setActiveIndex] = useState(defaultIndex);\n\n  return (\n    <TabsContext.Provider value={{ activeIndex, setActiveIndex }}>\n      {children}\n    </TabsContext.Provider>\n  );\n}\n\nfunction TabList({ children }) {\n  return <div className=\"tab-list\">{children}</div>;\n}\n\nfunction Tab({ index, children }) {\n  const { activeIndex, setActiveIndex } = useContext(TabsContext);\n  const isActive = activeIndex === index;\n\n  return (\n    <button\n      className={isActive ? 'tab active' : 'tab'}\n      onClick={() => setActiveIndex(index)}\n    >\n      {children}\n    </button>\n  );\n}\n\nfunction TabPanels({ children }) {\n  const { activeIndex } = useContext(TabsContext);\n  return <div className=\"tab-panels\">{children[activeIndex]}</div>;\n}\n\nfunction TabPanel({ children }) {\n  return <div className=\"tab-panel\">{children}</div>;\n}\n\nTabs.List = TabList;\nTabs.Tab = Tab;\nTabs.Panels = TabPanels;\nTabs.Panel = TabPanel;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Usage"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Tabs Usage",
        "content": "Using the tabs component",
        "language": "jsx",
        "code": "<Tabs defaultIndex={0}>\n  <Tabs.List>\n    <Tabs.Tab index={0}>First</Tabs.Tab>\n    <Tabs.Tab index={1}>Second</Tabs.Tab>\n    <Tabs.Tab index={2}>Third</Tabs.Tab>\n  </Tabs.List>\n  <Tabs.Panels>\n    <Tabs.Panel>First panel content</Tabs.Panel>\n    <Tabs.Panel>Second panel content</Tabs.Panel>\n    <Tabs.Panel>Third panel content</Tabs.Panel>\n  </Tabs.Panels>\n</Tabs>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accordion Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Accordion",
        "content": "Building an accordion component",
        "language": "jsx",
        "code": "function Accordion({ children, allowMultiple = false }) {\n  const [openIndices, setOpenIndices] = useState([]);\n\n  const toggle = (index) => {\n    setOpenIndices(prev =>\n      allowMultiple\n        ? prev.includes(index) ? prev.filter(i => i !== index) : [...prev, index]\n        : prev.includes(index) ? [] : [index]\n    );\n  };\n\n  return (\n    <AccordionContext.Provider value={{ openIndices, toggle }}>\n      <div className=\"accordion\">{children}</div>\n    </AccordionContext.Provider>\n  );\n}\n\nfunction AccordionItem({ index, children }) {\n  const { openIndices } = useContext(AccordionContext);\n  const isOpen = openIndices.includes(index);\n\n  return (\n    <div className={`accordion-item ${isOpen ? 'open' : ''}`}>\n      {children}\n    </div>\n  );\n}\n\nfunction AccordionHeader({ index, children }) {\n  const { toggle } = useContext(AccordionContext);\n  return (\n    <button className=\"accordion-header\" onClick={() => toggle(index)}>\n      {children}\n    </button>\n  );\n}\n\nfunction AccordionPanel({ children }) {\n  return <div className=\"accordion-panel\">{children}</div>;\n}\n\nAccordion.Item = AccordionItem;\nAccordion.Header = AccordionHeader;\nAccordion.Panel = AccordionPanel;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Flexibility",
        "text": "Compound components let users reorder, omit, or wrap parts as needed, making them highly flexible."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Tabs are a classic compound component example",
          "Accordions benefit from compound pattern",
          "Users control the composition",
          "Context provides implicit state sharing"
        ],
        "takeaway": "Compound components create powerful, flexible APIs."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a Custom Hook",
        "description": "Build a useLocalStorage hook that syncs state with localStorage.",
        "steps": [
          "Accept key and initialValue",
          "Read from localStorage on mount",
          "Write to localStorage on change"
        ],
        "expectedOutcome": "Reusable useLocalStorage hook."
      }
    }
  ],
  "courseSlug": "react-advanced-patterns"
}
