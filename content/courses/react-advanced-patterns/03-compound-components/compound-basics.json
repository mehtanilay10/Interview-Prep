{
  "id": "lesson-react-ap-007",
  "slug": "compound-basics",
  "moduleSlug": "compound-components",
  "title": "Compound Components Basics",
  "description": "Understand the compound components pattern for flexible APIs.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [
    "react",
    "compound",
    "components",
    "patterns"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Compound components** is a pattern where components share implicit state to work together. The parent component defines the structure, and child components handle specific parts."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How It Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The parent component provides context, and children consume it. This creates a flexible API where users can customize the composition."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Select Component",
        "content": "Basic compound component",
        "language": "jsx",
        "code": "function Select({ children }) {\n  const [isOpen, setIsOpen] = useState(false);\n  const [selected, setSelected] = useState(null);\n\n  return (\n    <SelectContext.Provider value={{ isOpen, setIsOpen, selected, setSelected }}>\n      <div className=\"select\">{children}</div>\n    </SelectContext.Provider>\n  );\n}\n\nfunction SelectTrigger({ children }) {\n  const { setIsOpen } = useContext(SelectContext);\n  return <button onClick={() => setIsOpen(true)}>{children}</button>;\n}\n\nfunction SelectOptions({ children }) {\n  const { isOpen } = useContext(SelectContext);\n  return isOpen ? <ul className=\"options\">{children}</ul> : null;\n}\n\nfunction SelectOption({ value, children }) {\n  const { setSelected, setIsOpen } = useContext(SelectContext);\n  return <li onClick={() => { setSelected(value); setIsOpen(false); }}>{children}</li>;\n}\n\n// Attach as properties\nSelect.Trigger = SelectTrigger;\nSelect.Options = SelectOptions;\nSelect.Option = SelectOption;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Usage"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using Compound Components",
        "content": "Flexible component composition",
        "language": "jsx",
        "code": "<Select>\n  <Select.Trigger>Choose an option</Select.Trigger>\n  <Select.Options>\n    <Select.Option value=\"react\">React</Select.Option>\n    <Select.Option value=\"vue\">Vue</Select.Option>\n    <Select.Option value=\"angular\">Angular</Select.Option>\n  </Select.Options>\n</Select>"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Benefits",
        "text": "Compound components provide flexibility (users control composition), readability (declarative API), and separation of concerns (each child handles one thing)."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Parent provides context, children consume it",
          "Users control the composition",
          "Creates flexible, expressive APIs",
          "Each child has a single responsibility"
        ],
        "takeaway": "Compound components enable flexible, declarative APIs."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Implement a Compound Component",
        "description": "Build a Tabs component using the compound component pattern.",
        "steps": [
          "Create Tab and TabList subcomponents",
          "Use React.Children to iterate",
          "Manage active tab in parent"
        ],
        "expectedOutcome": "Flexible Tabs component."
      }
    }
  ],
  "courseSlug": "react-advanced-patterns"
}
