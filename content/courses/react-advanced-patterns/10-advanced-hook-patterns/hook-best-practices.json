{
  "id": "lesson-react-adv-035",
  "slug": "hook-best-practices",
  "moduleSlug": "advanced-hook-patterns",
  "courseSlug": "react-advanced-patterns",
  "title": "Hook Best Practices",
  "description": "Follow best practices for hook design, naming, composition, and documentation.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": ["best-practices", "composition", "naming", "documentation", "rules-of-hooks"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Hooks are the building blocks of React's component model. Writing them well means understanding the rules of hooks, designing for composability, naming for discoverability, and documenting behavior so other developers can use them correctly."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Rules of Hooks"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Two Ironclad Rules",
        "text": "Only call hooks at the top level. Do not call them inside loops, conditions, or nested functions. Only call hooks from React function components or custom hooks."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React relies on the order in which hooks are called to associate state with the correct hook. Conditional or looped calls change that order between renders, corrupting state. The ESLint plugin `eslint-plugin-react-hooks` enforces these rules at build time."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Common Violations"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Incorrect: Hook Inside Condition",
        "content": "This violates the rules of hooks and will break state.",
        "language": "tsx",
        "code": "function UserCard({ user, showEmail }: Props) {\n  if (showEmail) {\n    // Violation: hook inside condition\n    const [copied, setCopied] = useState(false);\n  }\n\n  return <div>{user.name}</div>;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Correct: Move Condition Inside Hook",
        "content": "Keep hooks at the top level; put conditions inside them.",
        "language": "tsx",
        "code": "function useCopyEmail(email: string | null) {\n  const [copied, setCopied] = useState(false);\n\n  const copy = () => {\n    if (!email) return;\n    navigator.clipboard.writeText(email);\n    setCopied(true);\n    setTimeout(() => setCopied(false), 2000);\n  };\n\n  return { copied, copy };\n}\n\nfunction UserCard({ user, showEmail }: Props) {\n  const { copied, copy } = useCopyEmail(showEmail ? user.email : null);\n  // hooks are always called in the same order\n\n  return <div>{user.name}</div>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Naming and Discoverability"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Custom hooks should start with `use` so developers and tooling recognize them immediately. The name should describe the behavior, not the implementation. Prefer `useLocalStorage` over `useStorageWithJSONAndSSRHandling`."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Good Name", "Bad Name", "Reason"],
        "rows": [
          ["useWindowSize", "useDimensions", "Specific to what it tracks"],
          ["useDebounce", "useDelayEffect", "Describes the behavior"],
          ["useAuth", "useFirebaseAuth", "Keeps implementation agnostic"],
          ["usePrevious", "useOldValue", "Clear and conventional"]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Composition Over Abstraction"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Small, single-purpose hooks compose naturally. A `useMouse` hook that returns coordinates pairs well with a `useDrag` hook that consumes them. Avoid mega-hooks that bundle unrelated concerns—they are harder to test, harder to optimize, and harder to reuse."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Composing Small Hooks",
        "content": "Build a draggable component by composing useMouse and useState.",
        "language": "tsx",
        "code": "function useMousePosition() {\n  const [pos, setPos] = useState({ x: 0, y: 0 });\n\n  useEffect(() => {\n    const handler = (e: MouseEvent) => setPos({ x: e.clientX, y: e.clientY });\n    window.addEventListener('mousemove', handler);\n    return () => window.removeEventListener('mousemove', handler);\n  }, []);\n\n  return pos;\n}\n\nfunction useDraggable(initial = { x: 0, y: 0 }) {\n  const [offset, setOffset] = useState(initial);\n  const mouse = useMousePosition();\n\n  const startRef = useRef<typeof mouse | null>(null);\n\n  const onMouseDown = () => { startRef.current = mouse; };\n  const onMouseUp = () => { startRef.current = null; };\n\n  useEffect(() => {\n    if (!startRef.current) return;\n    const dx = mouse.x - startRef.current.x;\n    const dy = mouse.y - startRef.current.y;\n    setOffset((prev) => ({ x: prev.x + dx, y: prev.y + dy }));\n    startRef.current = mouse;\n  }, [mouse]);\n\n  return { ...offset, onMouseDown, onMouseUp };\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Extract When You Repeat",
        "text": "If three components share the same useEffect or state logic, extract it into a custom hook. If only one component uses it, keep it inline."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Documentation and Contracts"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Document inputs, outputs, and edge cases. Use TypeScript to make the contract explicit, and provide JSDoc for consumers who rely on IntelliSense. Note whether the hook is strict about dependencies, whether it cleans up after itself, and whether it supports SSR."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Well-Documented Hook Signature",
        "content": "TypeScript types and JSDoc describe the full API.",
        "language": "tsx",
        "code": "/**\n * Listens to the online/offline status of the browser.\n *\n * @returns {OnlineStatus} An object with an `online` boolean and\n *   an `eventType` string describing the last change.\n *\n * @example\n * const { online } = useOnlineStatus();\n */\nexport function useOnlineStatus(): { online: boolean; eventType: 'online' | 'offline' } {\n  const [online, setOnline] = useState(navigator.onLine);\n\n  useEffect(() => {\n    const goOnline = () => setOnline(true);\n    const goOffline = () => setOnline(false);\n\n    window.addEventListener('online', goOnline);\n    window.addEventListener('offline', goOffline);\n\n    return () => {\n      window.removeEventListener('online', goOnline);\n      window.removeEventListener('offline', goOffline);\n    };\n  }, []);\n\n  return { online, eventType: online ? 'online' : 'offline' };\n}"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Hook Quality Checklist",
        "items": [
          {
            "text": "Name starts with 'use' and describes behavior",
            "hint": "e.g., useLocalStorage, useDebounce"
          },
          {
            "text": "All hooks called unconditionally at the top level",
            "hint": "No loops, conditions, or nested functions around hooks"
          },
          {
            "text": "Dependencies arrays are exhaustive and minimal",
            "hint": "Use ESLint plugin to verify"
          },
          {
            "text": "Effects clean up after themselves",
            "hint": "Return a cleanup function from useEffect when subscribing"
          },
          {
            "text": "Types are exported for consumer inference",
            "hint": "Avoid 'as any' and internal-only shapes in the public API"
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Hooks must always be called in the same order on every render.",
          "Compose small, focused hooks rather than building large monolithic ones.",
          "Name hooks for behavior, not implementation details.",
          "Document contracts with TypeScript types and JSDoc comments.",
          "Always clean up subscriptions, timers, and event listeners."
        ],
        "takeaway": "Great hooks feel like native React features: predictable, composable, and well-documented."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": ["imperative-handle-patterns"]
}
