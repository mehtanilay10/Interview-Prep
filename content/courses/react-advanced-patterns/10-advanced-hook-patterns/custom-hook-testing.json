{
  "id": "lesson-react-adv-034",
  "slug": "custom-hook-testing",
  "moduleSlug": "advanced-hook-patterns",
  "courseSlug": "react-advanced-patterns",
  "title": "Custom Hook Testing",
  "description": "Learn strategies for testing custom hooks with React Testing Library and Jest.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "testing",
    "jest",
    "react-testing-library",
    "custom-hooks",
    "@testing-library/react"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Testing custom hooks is essential for maintaining reliable logic that is shared across components. React Testing Library provides utilities like `renderHook` and `act` that let you run hooks in a test environment, observe state changes, and assert behavior without rendering a full UI tree."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "renderHook Basics"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing a Simple useCounter Hook",
        "content": "Use renderHook to invoke the hook and access its result.",
        "language": "tsx",
        "code": "import { renderHook, act } from '@testing-library/react';\nimport { useCounter } from './useCounter';\n\nfunction useCounter(initial = 0) {\n  const [count, setCount] = useState(initial);\n  const increment = () => setCount((c) => c + 1);\n  const decrement = () => setCount((c) => c - 1);\n  return { count, increment, decrement };\n}\n\ndescribe('useCounter', () => {\n  it('starts at the initial value', () => {\n    const { result } = renderHook(() => useCounter(10));\n    expect(result.current.count).toBe(10);\n  });\n\n  it('increments the count', () => {\n    const { result } = renderHook(() => useCounter());\n\n    act(() => {\n      result.current.increment();\n    });\n\n    expect(result.current.count).toBe(1);\n  });\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Why act() Matters",
        "text": "State updates in React are batched and flushed asynchronously. Wrapping imperative calls in act() ensures all updates are processed before assertions run."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Testing Async Hooks"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Hooks that wrap async operations—such as data fetching or debouncing—require waiting for promises to resolve. Use `waitFor` to poll until the hook reaches the expected state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing useAsync with waitFor",
        "content": "Assert loading, success, and error states in sequence.",
        "language": "tsx",
        "code": "function useAsync<T>(asyncFn: () => Promise<T>) {\n  const [state, setState] = useState<{\n    data: T | null;\n    loading: boolean;\n    error: Error | null;\n  }>({ data: null, loading: true, error: null });\n\n  useEffect(() => {\n    asyncFn()\n      .then((data) => setState({ data, loading: false, error: null }))\n      .catch((error) => setState({ data: null, loading: false, error }));\n  }, [asyncFn]);\n\n  return state;\n}\n\ntest('useAsync resolves data', async () => {\n  const fetchData = () => Promise.resolve({ id: 1 });\n  const { result } = renderHook(() => useAsync(fetchData));\n\n  expect(result.current.loading).toBe(true);\n\n  await waitFor(() => expect(result.current.loading).toBe(false));\n  expect(result.current.data).toEqual({ id: 1 });\n  expect(result.current.error).toBeNull();\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Testing Hooks That Depend on Context"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If a hook consumes React Context, `renderHook` accepts a `wrapper` option. Provide the context provider so the hook receives the values it expects."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Wrapping with a Context Provider",
        "content": "Testing a theme-aware hook inside a provider.",
        "language": "tsx",
        "code": "const ThemeContext = createContext<'light' | 'dark'>('light');\n\nfunction useThemeClass() {\n  const theme = useContext(ThemeContext);\n  return theme === 'dark' ? 'bg-gray-900' : 'bg-white';\n}\n\ntest('useThemeClass returns dark class', () => {\n  const wrapper = ({ children }: { children: React.ReactNode }) => (\n    <ThemeContext.Provider value=\"dark\">{children}</ThemeContext.Provider>\n  );\n\n  const { result } = renderHook(() => useThemeClass(), { wrapper });\n  expect(result.current).toBe('bg-gray-900');\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Mocking and Spy Strategies"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Common mocking patterns:",
        "items": [
          "Use jest.spyOn to observe function calls inside the hook.",
          "Replace window or document APIs with jest.spyOn for DOM-dependent hooks.",
          "Create wrapper components that inject props to simulate parent behavior.",
          "Avoid testing implementation details; assert on the returned state and callbacks."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Beware Over-Mocking",
        "text": "If you find yourself mocking every internal dependency, the hook may be doing too much. Consider splitting logic into smaller, testable units."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Test a useDebounce Hook",
        "description": "Write tests for a hook that delays updating a value.",
        "steps": [
          "Create a useDebounce hook that takes a value and delayMs.",
          "Test the initial state returns the original value.",
          "Use jest.useFakeTimers() and act() to advance time.",
          "Assert the debounced value updates only after the delay."
        ],
        "expectedOutcome": "A fully tested debounce hook with mocked timers."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "FAQ",
        "items": [
          {
            "question": "Should I test hooks independently or through components?",
            "answer": "Test hooks independently when they contain non-trivial logic. Thin wrappers around built-in hooks can be tested indirectly through components."
          },
          {
            "question": "Why does my test fail with 'not wrapped in act()'?",
            "answer": "State updates are asynchronous. Wrap interactions in act() or use waitFor to let React flush pending updates before asserting."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "renderHook executes a hook inside a test and returns its current result.",
          "act() ensures all state updates are flushed before assertions.",
          "waitFor handles async transitions by polling until the condition is met.",
          "Context-dependent hooks need a wrapper provider in renderHook options."
        ],
        "takeaway": "Testing hooks directly gives you fast, focused feedback on logic that components reuse."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": [
    "reducer-hooks-patterns"
  ]
}
