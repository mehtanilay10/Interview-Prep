{
  "id": "lesson-react-adv-031",
  "slug": "reducer-hooks-patterns",
  "moduleSlug": "advanced-hook-patterns",
  "courseSlug": "react-advanced-patterns",
  "title": "Reducer Hooks Patterns",
  "description": "Master useReducer and createReducer patterns for managing complex state logic in React components.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "useReducer",
    "createReducer",
    "state-machine",
    "actions",
    "dispatch",
    "discriminated-union",
    "immutability"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React's **useReducer** hook is the foundation for predictable state management. It mirrors Redux's reducer pattern but lives inside your component. When state transitions become complex—involving multiple sub-values or dependent updates—`useReducer` outperforms `useState` by centralizing logic and decoupling the event from the state update."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Reach for useReducer"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Signs your component needs a reducer:",
        "items": [
          "The next state depends on the previous state (e.g., counters, form fields).",
          "State logic is scattered across multiple event handlers.",
          "You need to dispatch complex actions with payloads.",
          "A prop or custom hook needs to trigger state changes without exposing the setter."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro Tip",
        "text": "If you find yourself passing a function to setState that reads the previous state, consider switching to useReducer. It makes the dependency explicit."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Basic useReducer Setup"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Counter with useReducer",
        "content": "A simple reducer managing increment, decrement, and reset with TypeScript safety.",
        "language": "tsx",
        "code": "import { useReducer } from 'react';\n\ntype State = { count: number };\n\ntype Action =\n  | { type: 'increment'; payload?: number }\n  | { type: 'decrement'; payload?: number }\n  | { type: 'reset' };\n\nfunction counterReducer(state: State, action: Action): State {\n  switch (action.type) {\n    case 'increment':\n      return { count: state.count + (action.payload ?? 1) };\n    case 'decrement':\n      return { count: state.count - (action.payload ?? 1) };\n    case 'reset':\n      return { count: 0 };\n    default:\n      return state;\n  }\n}\n\nfunction Counter() {\n  const [state, dispatch] = useReducer(counterReducer, { count: 0 });\n\n  return (\n    <div className=\"flex items-center gap-4\">\n      <button onClick={() => dispatch({ type: 'decrement' })}>-</button>\n      <span>{state.count}</span>\n      <button onClick={() => dispatch({ type: 'increment', payload: 5 })}>+5</button>\n      <button onClick={() => dispatch({ type: 'reset' })}>Reset</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "reducer-state-transitions",
        "caption": "State transitions for a counter reducer with three action types.",
        "definition": "stateDiagram-v2\n    [*] --> Zero\n    Zero --> Positive: increment\n    Zero --> Negative: decrement\n    Positive --> Zero: reset\n    Negative --> Zero: reset\n    Positive --> Positive: increment\n    Negative --> Negative: decrement"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Reusable Reducer Factories"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Hardcoding reducers inside components limits reuse. A **reducer factory** is a function that returns a reducer pre-configured with initial state or extra behavior. This pattern lets you share reducer logic across components or extract it into a custom hook."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "createToggle Reducer Factory",
        "content": "A reusable reducer factory for toggle-like state with a configurable initial value.",
        "language": "tsx",
        "code": "import { useReducer } from 'react';\n\ntype ToggleState = { on: boolean };\n\ninterface ToggleAction {\n  type: 'TOGGLE' | 'SET' | 'RESET';\n  payload?: { value: boolean };\n}\n\nfunction createToggleReducer(initial = false) {\n  return function toggleReducer(state: ToggleState, action: ToggleAction): ToggleState {\n    switch (action.type) {\n      case 'TOGGLE':\n        return { on: !state.on };\n      case 'SET':\n        return { on: action.payload!.value };\n      case 'RESET':\n        return { on: initial };\n      default:\n        return state;\n    }\n  };\n}\n\nfunction useToggle(initial = false) {\n  const reducer = createToggleReducer(initial);\n  const [state, dispatch] = useReducer(reducer, { on: initial });\n\n  const toggle = () => dispatch({ type: 'TOGGLE' });\n  const set = (value: boolean) => dispatch({ type: 'SET', payload: { value } });\n  const reset = () => dispatch({ type: 'RESET' });\n\n  return { ...state, toggle, set, reset };\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Avoid Mutating State",
        "text": "Reducers must be pure. Never mutate the state object directly—always return a new object. React relies on immutability to detect changes and trigger re-renders."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "State Machine Patterns with Reducers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A reducer can enforce a strict state machine by returning the current state for unrecognized actions. Combined with discriminated unions, TypeScript can narrow the state shape at compile time, preventing impossible states from ever compiling."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Reducer",
            "definition": "A pure function (state, action) => newState that describes how state evolves in response to actions."
          },
          {
            "term": "Dispatch",
            "definition": "The function returned by useReducer that sends an action to the reducer."
          },
          {
            "term": "Discriminated Union",
            "definition": "A TypeScript union type where all members share a literal property (like type) used for type narrowing."
          },
          {
            "term": "Reducer Factory",
            "definition": "A higher-order function that returns a configured reducer, enabling reuse and composition."
          },
          {
            "term": "Immutability",
            "definition": "The practice of never modifying state directly, instead returning new objects so React can detect changes."
          }
        ]
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Action Type",
          "Payload",
          "Typical Use Case"
        ],
        "rows": [
          [
            "SET_VALUE",
            "{ value: T }",
            "Updating a form field or toggle"
          ],
          [
            "ADD_ITEM",
            "{ item: T }",
            "Appending to a list (todo, tags)"
          ],
          [
            "REMOVE_ITEM",
            "{ id: string }",
            "Deleting from a collection"
          ],
          [
            "UPDATE_ITEM",
            "{ id: string; changes: Partial<T> }",
            "Editing an existing item"
          ],
          [
            "RESET",
            "—",
            "Restoring initial state"
          ]
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "useState vs useReducer",
        "cards": [
          {
            "title": "useState",
            "description": "Simple scalar state updates with a setter function.",
            "pros": [
              "Less boilerplate for simple state",
              "Familiar API for beginners"
            ],
            "cons": [
              "Logic scattered across handlers",
              "Hard to test transitions in isolation"
            ],
            "tags": [
              "simple",
              "local-state"
            ]
          },
          {
            "title": "useReducer",
            "description": "Centralized state transitions via a reducer function.",
            "pros": [
              "Predictable state changes",
              "Testable pure function",
              "Easier to debug with action logs"
            ],
            "cons": [
              "More upfront code",
              "Can be overkill for trivial state"
            ],
            "tags": [
              "complex-state",
              "predictable"
            ]
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Form State Reducer",
        "description": "Create a useReducer-based form state manager that tracks values, touched fields, and errors.",
        "steps": [
          "Define a discriminated union for actions: SET_FIELD, TOUCH_FIELD, SET_ERRORS, RESET.",
          "Write a reducer that updates state immutably for each action type.",
          "Return dispatch helpers like setField, touchField, and reset from a custom hook.",
          "Test the reducer with a few action sequences to verify correctness."
        ],
        "expectedOutcome": "A reusable form state hook with centralized transition logic."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useReducer centralizes state logic, making complex transitions predictable.",
          "Reducer factories enable reuse by pre-configuring initial state and behavior.",
          "TypeScript discriminated unions make reducers type-safe and self-documenting.",
          "Reducers are pure functions, so they are easy to test in isolation.",
          "Always return new state objects—never mutate the existing state."
        ],
        "takeaway": "Reach for useReducer when state transitions outgrow scattered useState setters."
      }
    }
  ],
  "relatedLessons": [
    "hook-composition-patterns"
  ],
  "furtherReading": [
    {
      "title": "React Docs: useReducer",
      "url": "https://react.dev/reference/react/useReducer",
      "type": "article",
      "author": "React Team",
      "description": "Official documentation covering useReducer syntax, lazy initialization, and optimization patterns."
    },
    {
      "title": "Epic Stack: Advanced useReducer Patterns",
      "url": "https://www.epicreact.dev/blog/advanced-use-reducer-patterns/",
      "type": "article",
      "author": "Kent C. Dodds",
      "description": "A deep dive into reducer factories, action creators, and testing strategies."
    },
    {
      "title": "Redux Toolkit: createReducer",
      "url": "https://redux-toolkit.js.org/api/createReducer",
      "type": "article",
      "author": "Redux Team",
      "description": "Learn how the createReducer factory builds on these same patterns at scale."
    }
  ],
  "prerequisites": [
    "hook-composition-patterns"
  ]
}
