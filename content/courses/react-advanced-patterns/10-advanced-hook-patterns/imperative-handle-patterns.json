{
  "id": "lesson-react-adv-032",
  "slug": "imperative-handle-patterns",
  "moduleSlug": "advanced-hook-patterns",
  "courseSlug": "react-advanced-patterns",
  "title": "Imperative Handle Patterns",
  "description": "Learn how to use useImperativeHandle and forwardRef to expose imperative methods from child components to parents.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": ["useImperativeHandle", "forwardRef", "refs", "imperative", "parent-child", "typescript"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React's declarative model encourages parents to pass props down and children to render UI up. But some interactions—focusing an input, triggering an animation, or scrolling to an element—are inherently imperative. **useImperativeHandle** lets a child selectively expose methods to a parent ref without breaking encapsulation."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Not Just Use ref?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A DOM ref gives a parent direct access to a child's DOM node. That is powerful but brittle: the parent becomes coupled to the child's internal markup. If the child changes its structure, every parent that reached into it breaks. `useImperativeHandle` lets the child define a stable, intentional API surface."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Encapsulation Trade-off",
        "text": "Imperative handles are a deliberate escape hatch from declarative programming. Use them sparingly and document the exposed API clearly."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Basic Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Focusable Input with useImperativeHandle",
        "content": "A child component exposes focus and clear methods to the parent via a typed ref.",
        "language": "tsx",
        "code": "import { forwardRef, useImperativeHandle, useRef, useState } from 'react';\n\ninterface InputHandle {\n  focus: () => void;\n  clear: () => void;\n}\n\ninterface InputProps {\n  label: string;\n}\n\nconst Input = forwardRef<InputHandle, InputProps>(({ label }, ref) => {\n  const inputRef = useRef<HTMLInputElement>(null);\n  const [value, setValue] = useState('');\n\n  useImperativeHandle(ref, () => ({\n    focus() {\n      inputRef.current?.focus();\n    },\n    clear() {\n      setValue('');\n      inputRef.current?.focus();\n    },\n  }));\n\n  return (\n    <label className=\"flex flex-col gap-1\">\n      <span className=\"text-sm font-medium\">{label}</span>\n      <input\n        ref={inputRef}\n        value={value}\n        onChange={(e) => setValue(e.target.value)}\n        className=\"rounded border px-3 py-2\"\n      />\n    </label>\n  );\n});\n\nfunction Form() {\n  const inputRef = useRef<InputHandle>(null);\n\n  return (\n    <form>\n      <Input ref={inputRef} label=\"Email\" />\n      <button type=\"button\" onClick={() => inputRef.current?.focus()}>Focus</button>\n      <button type=\"button\" onClick={() => inputRef.current?.clear()}>Clear</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "imperative-handle-flow",
        "caption": "How useImperativeHandle bridges parent refs and child internal state.",
        "definition": "graph LR\n    A[Parent Component] --> B[forwardRef Component]\n    B --> C[Internal DOM Refs]\n    B --> D[useImperativeHandle]\n    D --> E[Exposed Handle Object]\n    E --> A\n    style D fill:#1e293b,stroke:#38bdf8,color:#f1f5f9\n    style E fill:#1e293b,stroke:#a855f7,color:#f1f5f9"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "forwardRef and TypeScript"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`forwardRef` takes two generic parameters: the handle type and the props type. TypeScript will enforce that parents only call methods defined on the handle, preventing accidental access to internal refs or private state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Type-Safe Imperative Handle",
        "content": "Defining a strict handle interface for compile-time safety.",
        "language": "tsx",
        "code": "import { forwardRef, useImperativeHandle, useState } from 'react';\n\ninterface ModalHandle {\n  open: () => void;\n  close: () => void;\n  isOpen: boolean;\n}\n\nconst Modal = forwardRef<ModalHandle, { title: string }>(({ title }, ref) => {\n  const [isOpen, setIsOpen] = useState(false);\n\n  useImperativeHandle(ref, () => ({\n    get isOpen() {\n      return isOpen;\n    },\n    open() {\n      setIsOpen(true);\n    },\n    close() {\n      setIsOpen(false);\n    },\n  }), [isOpen]);\n\n  if (!isOpen) return null;\n  return <div className=\"modal\">{title}</div>;\n});"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "useImperativeHandle",
            "definition": "A hook that customizes the instance value (ref) exposed to parent components when using ref."
          },
          {
            "term": "forwardRef",
            "definition": "A higher-order component that forwards refs from parent to child, enabling typed ref passing."
          },
          {
            "term": "Handle Interface",
            "definition": "The object type that defines which methods and properties a child exposes via its ref."
          },
          {
            "term": "Encapsulation",
            "definition": "The principle of hiding internal implementation details and exposing only a controlled API surface."
          },
          {
            "term": "Imperative API",
            "definition": "A set of methods that a consumer can call directly, rather than describing behavior through props."
          }
        ]
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Use Case", "Exposed API", "Benefit"],
        "rows": [
          ["Focus management", "focus(), blur()", "Parent controls input focus without DOM traversal"],
          ["Media playback", "play(), pause(), seek()", "Parent triggers video/animation"],
          ["Modal dialogs", "open(), close()", "Parent opens/closes overlay without lifting state"],
          ["Scroll containers", "scrollTo(top, behavior)", "Parent scrolls child to a position"]
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Imperative Handles vs Props",
        "cards": [
          {
            "title": "Props (Declarative)",
            "description": "Describe what the child should render; React diff updates the DOM.",
            "pros": ["Predictable rendering", "Easy to test and debug", "SSR-safe"],
            "cons": ["Parent must own all state", "Cannot trigger one-shot actions"],
            "tags": ["declarative", "reactive"]
          },
          {
            "title": "Imperative Handle",
            "description": "Expose imperative methods so the parent can trigger actions directly.",
            "pros": ["Direct control over child behavior", "Can fire one-shot actions", "Keeps state encapsulated"],
            "cons": ["Breaks rendering predictability", "Harder to test", "Escape hatch from declarative model"],
            "tags": ["imperative", "escape-hatch"]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Dependency Array",
        "text": "The third argument to useImperativeHandle is a dependency array. If your handle closes over changing values, include them so the handle stays in sync."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a Scrollable Container",
        "description": "Build a component that exposes a scrollTo method via useImperativeHandle.",
        "steps": [
          "Create a forwardRef component with a scrollable div.",
          "Use useImperativeHandle to expose scrollTo(top, behavior).",
          "Wire the internal ref to the div and call scrollTo on it.",
          "Consume it from a parent with a button that scrolls to a specific item."
        ],
        "expectedOutcome": "A reusable scrollable container with a clean imperative API."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useImperativeHandle lets children expose a curated API to parent refs.",
          "forwardRef bridges React's ref system with TypeScript generics.",
          "Always provide a dependency array to avoid stale closures.",
          "Prefer declarative data flow; use imperative handles only for truly imperative interactions."
        ],
        "takeaway": "Imperative handles give you escape valves when declarative props are not enough."
      }
    }
  ],
  "relatedLessons": ["reducer-hooks-patterns"],
  "furtherReading": [
    {
      "title": "React Docs: useImperativeHandle",
      "url": "https://react.dev/reference/react/useImperativeHandle",
      "type": "article",
      "author": "React Team",
      "description": "Official guide on when and how to use imperative handles with forwardRef."
    },
    {
      "title": "React Docs: Forwarding a Ref to a Component",
      "url": "https://react.dev/learn/refs-and-the-dom",
      "type": "article",
      "author": "React Team",
      "description": "A beginner-friendly tutorial on ref forwarding and imperative patterns."
    },
    {
      "title": "Why do we need forwardRef?",
      "url": "https://www.mattzein.dev/react-forwarding-refs/",
      "type": "video",
      "author": "Matt Zein",
      "description": "A practical walkthrough of forwardRef pitfalls in TypeScript."
    }
  ],
  "prerequisites": ["reducer-hooks-patterns"]
}
