{
  "id": "lesson-react-adv-033",
  "slug": "layout-effect-patterns",
  "moduleSlug": "advanced-hook-patterns",
  "courseSlug": "react-advanced-patterns",
  "title": "Layout Effect Patterns",
  "description": "Apply useLayoutEffect for DOM measurements, synchronous updates, and visual consistency.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "useLayoutEffect",
    "useEffect",
    "dom",
    "measurements",
    "synchronous",
    "isomorphic"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useLayoutEffect** fires synchronously after all DOM mutations but before the browser paints. This timing makes it ideal for reading layout from the DOM and synchronously re-rendering to avoid visual flicker. In contrast, useEffect runs asynchronously after paint, which can cause the user to see an intermediate frame."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useEffect vs useLayoutEffect"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "effect-timeline",
        "caption": "React render and commit timeline showing where layout and passive effects fire.",
        "definition": "sequenceDiagram\n    participant Render as Render phase\n    participant Commit as Commit phase\n    participant Layout as useLayoutEffect\n    participant Paint as Browser paint\n    participant Passive as useEffect\n    Render->>Commit: DOM mutations applied\n    Commit->>Layout: Synchronous flush\n    Layout->>Paint: Block until done\n    Paint->>Passive: Async flush"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `useEffect` for data fetching, subscriptions, and logging—anything that does not need to block painting. Use `useLayoutEffect` for DOM measurements, synchronously mutating styles, or any logic where a visual flash would degrade UX."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Server-Side Warning",
        "text": "useLayoutEffect is forbidden during server-side rendering because the DOM does not exist. If you need a universal solution, conditionally skip it on the server or use a custom hook like useIsomorphicLayoutEffect."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Measuring DOM Elements"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Auto-Height Textarea",
        "content": "Resize a textarea to fit its content without layout flicker.",
        "language": "tsx",
        "code": "import { useRef, useState, useLayoutEffect } from 'react';\n\nfunction AutoTextarea({ value, onChange }: { value: string; onChange: (v: string) => void }) {\n  const ref = useRef<HTMLTextAreaElement>(null);\n\n  useLayoutEffect(() => {\n    const el = ref.current;\n    if (!el) return;\n\n    // Reset height to get accurate scrollHeight\n    el.style.height = 'auto';\n    el.style.height = `${el.scrollHeight}px`;\n  }, [value]);\n\n  return (\n    <textarea\n      ref={ref}\n      value={value}\n      onChange={(e) => onChange(e.target.value)}\n      className=\"w-full resize-none overflow-hidden\"\n    />\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Synchronous Visual Updates"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a child renders before a parent measures it, `useLayoutEffect` can re-measure and update styles synchronously. A common pattern is a tooltip or dropdown that must position itself relative to a trigger without flickering between old and new positions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Positioning a Tooltip",
        "content": "Read the trigger's bounding rect and apply styles before paint.",
        "language": "tsx",
        "code": "import { useRef, useState, useLayoutEffect } from 'react';\n\nfunction Tooltip({ trigger, children }: { trigger: React.ReactNode; children: React.ReactNode }) {\n  const triggerRef = useRef<HTMLDivElement>(null);\n  const [coords, setCoords] = useState({ top: 0, left: 0 });\n\n  useLayoutEffect(() => {\n    const rect = triggerRef.current?.getBoundingClientRect();\n    if (rect) {\n      setCoords({\n        top: rect.bottom + window.scrollY + 8,\n        left: rect.left + window.scrollX,\n      });\n    }\n  }, []);\n\n  return (\n    <>\n      <span ref={triggerRef}>{trigger}</span>\n      <div\n        style={{ position: 'absolute', top: coords.top, left: coords.left }}\n        className=\"rounded bg-gray-900 px-2 py-1 text-white\"\n      >\n        {children}\n      </div>\n    </>\n  );\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "useLayoutEffect",
            "definition": "A useEffect variant that fires synchronously after DOM mutations but before the browser paints."
          },
          {
            "term": "useEffect",
            "definition": "A hook for managing side effects that run asynchronously after the browser paints."
          },
          {
            "term": "DOM Measurement",
            "definition": "Reading layout properties like offsetWidth, offsetHeight, or getBoundingClientRect before painting."
          },
          {
            "term": "Synchronous Update",
            "definition": "A state update that blocks the browser from painting until it is complete."
          },
          {
            "term": "Isomorphic Effect",
            "definition": "An effect that works safely in both server-side and client-side environments."
          }
        ]
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Scenario",
          "Recommended Hook",
          "Reason"
        ],
        "rows": [
          [
            "Data fetching",
            "useEffect",
            "Does not block rendering; network response is async"
          ],
          [
            "DOM measurements",
            "useLayoutEffect",
            "Need layout before paint to avoid flicker"
          ],
          [
            "Event listener cleanup",
            "useEffect",
            "Non-visual side effect; async is fine"
          ],
          [
            "Synchronous style mutation",
            "useLayoutEffect",
            "Must apply before the browser paints"
          ],
          [
            "Logging / analytics",
            "useEffect",
            "Should not block the user's view"
          ]
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "useEffect vs useLayoutEffect",
        "cards": [
          {
            "title": "useEffect",
            "description": "Fires after paint. Good for side effects that do not affect visual output.",
            "pros": [
              "Does not block rendering",
              "Works in SSR without warnings"
            ],
            "cons": [
              "Cannot prevent visual flicker",
              "DOM may be stale during execution"
            ],
            "tags": [
              "async",
              "painting"
            ]
          },
          {
            "title": "useLayoutEffect",
            "description": "Fires before paint. Good for reading layout and synchronously updating DOM.",
            "pros": [
              "Prevents flicker",
              "DOM is fully committed",
              "Can measure before paint"
            ],
            "cons": [
              "Blocks browser paint",
              "Forbidden in SSR by default"
            ],
            "tags": [
              "sync",
              "layout"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer CSS When Possible",
        "text": "Before reaching for useLayoutEffect, check whether CSS (position: absolute, transforms, or the Popover API) can solve the problem without JavaScript measurements."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "isomorphicLayoutEffect Shim"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Library authors often need effects that run in both SSR and CSR environments. A common pattern is aliasing `useLayoutEffect` to `useEffect` on the server to suppress React's hydration warning."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Cross-Environment Layout Effect",
        "content": "A safe layout effect for libraries and apps that supports SSR.",
        "language": "tsx",
        "code": "import { useEffect, useLayoutEffect, useRef, useState, type RefObject } from 'react';\n\nconst useIsomorphicLayoutEffect =\n  typeof window !== 'undefined' ? useLayoutEffect : useEffect;\n\nfunction useMeasure(ref: RefObject<HTMLElement>) {\n  const [bounds, setBounds] = useState({ width: 0, height: 0 });\n\n  useIsomorphicLayoutEffect(() => {\n    const el = ref.current;\n    if (!el) return;\n    setBounds({\n      width: el.offsetWidth,\n      height: el.offsetHeight,\n    });\n  }, [ref]);\n\n  return bounds;\n}"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build an Auto-Height Textarea",
        "description": "Create a textarea that automatically resizes to fit its content.",
        "steps": [
          "Create a textarea with a ref pointing to the DOM element.",
          "Use useLayoutEffect to read scrollHeight after every value change.",
          "Reset the row count or height to 'auto' before measuring.",
          "Set the new height synchronously so no flash occurs between renders."
        ],
        "expectedOutcome": "A smooth-resizing textarea that never shows a visual jump."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useLayoutEffect runs before the browser paints, blocking visual updates.",
          "useEffect runs after paint and should be used for non-visual side effects.",
          "Measure DOM nodes and apply synchronous styles in layout effects.",
          "Use an isomorphic shim when writing libraries that support SSR.",
          "Prefer CSS solutions when they avoid the need for measurements entirely."
        ],
        "takeaway": "Choose the effect hook based on whether your logic must block painting or can wait."
      }
    }
  ],
  "relatedLessons": [
    "imperative-handle-patterns"
  ],
  "furtherReading": [
    {
      "title": "React Docs: useLayoutEffect",
      "url": "https://react.dev/reference/react/useLayoutEffect",
      "type": "article",
      "author": "React Team",
      "description": "Official reference on timing, SSR considerations, and when to use layout effects."
    },
    {
      "title": "React Docs: You Might Not Need an Effect",
      "url": "https://react.dev/warnings/cannot-update-a-component-while-rendering",
      "type": "article",
      "author": "React Team",
      "description": "Guidance on avoiding unnecessary effects and choosing the right tool."
    },
    {
      "title": "Auren's Blog: useLayoutEffect vs useEffect",
      "url": "https://overreacted.io/you-dont-need-good-laptop-to-be-a-programmer/",
      "type": "article",
      "author": "Dan Abramov",
      "description": "Practical advice on when synchronous effects are justified."
    }
  ],
  "prerequisites": [
    "reducer-hooks-patterns"
  ]
}
