{
  "id": "lesson-tsr-026",
  "slug": "compound-components",
  "moduleSlug": "react-patterns",
  "title": "Compound Components",
  "description": "Use the compound component pattern with TypeScript to build flexible, expressive component APIs where parent and child components work together seamlessly.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": ["typescript", "react", "compound-components", "patterns"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Compound components** are a set of components that work together to form a complete UI. Think of a `<select>` and `<option>` — they are meaningless alone but powerful together. The compound component pattern lets you build this relationship in React with full TypeScript support."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Are Compound Components?"
      }
    },
    {
      "type": "quote",
      "data": {
        "text": "Compound components allow you to express a set of subcomponents that implicitly share state, making the API both flexible and intuitive.",
        "attribution": "Kent C. Dodds"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The pattern uses React Context under the hood to share state between a parent and its children. The parent manages the shared state, and each child component reads from context. TypeScript ensures each child receives exactly the props it needs."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Compound Component",
            "definition": "A group of components where a parent implicitly shares state with its children via context."
          },
          {
            "term": "Context Sharing",
            "definition": "Mechanism where child components access parent state without explicit prop drilling."
          },
          {
            "term": "Implicit State",
            "definition": "State managed by the parent and automatically available to children without manual prop passing."
          },
          {
            "term": "Component API Design",
            "definition": "The way components are composed and used — compound components create a declarative, HTML-like API."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-compound-023",
        "caption": "How compound components share state through context",
        "definition": "graph TD\n    A[Tabs parent] -->|creates context| B[TabsContext]\n    B -->|selected index| C[Tabs.List]\n    B -->|selected index| D[Tabs.Tab]\n    B -->|selected index| E[Tabs.Panel]\n    D -->|onClick updates| A\n    E -->|renders when selected| B\n    F[Consumer writes] -->|<Tabs><Tab/><Panel/></Tabs>| A\n    style B fill:#4f46e5,color:#fff"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Building a Tabs Component"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A Tabs component is the classic example. The parent holds the selected tab index. Each Tab knows whether it is active. Each Panel renders conditionally. All without passing any props explicitly between them."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Compound Tabs Component",
        "content": "Type-safe compound Tabs with Tab and Panel subcomponents",
        "language": "tsx",
        "code": "import { createContext, useContext, useState, ReactNode, HTMLAttributes } from 'react';\n\ninterface TabsContextValue {\n  activeIndex: number;\n  setActiveIndex: (index: number) => void;\n  items: { id: string; label: string }[];\n}\n\nconst TabsContext = createContext<TabsContextValue | null>(null);\n\nfunction useTabsContext() {\n  const ctx = useContext(TabsContext);\n  if (!ctx) throw new Error('Tabs compound components must be used inside <Tabs>');\n  return ctx;\n}\n\n// Parent component\ninterface TabsProps {\n  items: { id: string; label: string }[];\n  defaultIndex?: number;\n  children: ReactNode;\n}\n\nfunction Tabs({ items, defaultIndex = 0, children }: TabsProps) {\n  const [activeIndex, setActiveIndex] = useState(defaultIndex);\n\n  return (\n    <TabsContext.Provider value={{ activeIndex, setActiveIndex, items }}>\n      <div className=\"tabs-container\">{children}</div>\n    </TabsContext.Provider>\n  );\n}\n\n// Child: Tab\ninterface TabProps extends HTMLAttributes<HTMLElement> {\n  index: number;\n}\n\nfunction Tab({ index, children, ...rest }: TabProps) {\n  const { activeIndex, setActiveIndex } = useTabsContext();\n  const isActive = activeIndex === index;\n\n  return (\n    <button\n      role=\"tab\"\n      aria-selected={isActive}\n      onClick={() => setActiveIndex(index)}\n      className={`tab ${isActive ? 'tab-active' : ''}`}\n      {...rest}\n    >\n      {children}\n    </button>\n  );\n}\n\n// Child: Panel\ninterface PanelProps extends HTMLAttributes<HTMLElement> {\n  index: number;\n}\n\nfunction Panel({ index, children, ...rest }: PanelProps) {\n  const { activeIndex } = useTabsContext();\n  if (activeIndex !== index) return null;\n\n  return (\n    <div role=\"tabpanel\" className=\"tab-panel\" {...rest}>\n      {children}\n    </div>\n  );\n}\n\nTabs.Tab = Tab;\nTabs.Panel = Panel;\n\nexport { Tabs };"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using Compound Components"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The consumer writes JSX that mirrors the mental model of the UI. Tabs contains Tabs.Tab and Tabs.Panel — no prop passing required, no index management, no state handling outside the component."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using the Tabs Component",
        "content": "Consumer code using compound Tabs",
        "language": "tsx",
        "code": "const tabItems = [\n  { id: 'overview', label: 'Overview' },\n  { id: 'details', label: 'Details' },\n  { id: 'settings', label: 'Settings' },\n];\n\nfunction AccountSettings() {\n  return (\n    <Tabs items={tabItems} defaultIndex={0}>\n      <Tabs.Tab index={0}>Overview</Tabs.Tab>\n      <Tabs.Tab index={1}>Details</Tabs.Tab>\n      <Tabs.Tab index={2}>Settings</Tabs.Tab>\n\n      <Panel index={0}>\n        <h2>Account Overview</h2>\n        <p>View your account summary and activity.</p>\n      </Panel>\n\n      <Panel index={1}>\n        <h2>Details</h2>\n        <p>Edit your profile information and preferences.</p>\n      </Panel>\n\n      <Panel index={2}>\n        <h2>Settings</h2>\n        <p>Configure notifications, privacy, and security.</p>\n      </Panel>\n    </Tabs>\n  );\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Component Composition Patterns",
        "cards": [
          {
            "title": "Compound Components",
            "description": "Parent + children share implicit state via context",
            "pros": ["Declarative, HTML-like API", "State shared without prop drilling", "Highly flexible layout"],
            "cons": ["More components to export", "Context adds slight overhead"],
            "tags": ["declarative", "flexible"]
          },
          {
            "title": "Render Props",
            "description": "Child receives render function from parent",
            "pros": ["Full rendering control", "Explicit data flow", "Easy to type"],
            "cons": ["Callback nesting", "Less natural API"],
            "tags": ["explicit", "flexible"]
          },
          {
            "title": "Single Component with Config",
            "description": "All options passed as props to one component",
            "pros": ["Simple API surface", "Easy to document", "Fewer files"],
            "cons": ["Rigid structure", "Hard to customize layout", "Prop drilling inside"],
            "tags": ["simple", "rigid"]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "TypeScript Enforces Correct Usage",
        "text": "The useTabsContext hook throws an error if a Tab or Panel is used outside Tabs. TypeScript also ensures the index prop is a number, and HTMLAttributes are correctly typed on Tab and Panel."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Compound Component Checklist",
        "items": [
          {
            "text": "Create a context with the full shared state type",
            "hint": "Everything children need should be in the context value"
          },
          {
            "text": "Add a guard hook that throws if context is missing",
            "hint": "Prevents misuse when components are used standalone"
          },
          {
            "text": "Attach children to the parent as static properties",
            "hint": "Tabs.Tab = Tab enables <Tabs.Tab> syntax"
          },
          {
            "text": "Use HTMLAttributes for DOM-facing children",
            "hint": "Lets consumers pass className, onClick, aria-*, etc."
          },
          {
            "text": "Keep the context value minimal",
            "hint": "Only share what children actually need"
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Accordion Component",
        "description": "Build an Accordion compound component with Accordion, AccordionItem, AccordionTrigger, and AccordionContent. Each item shows its content when its trigger is clicked. Use context to track the open index, and allow only one item open at a time.",
        "steps": [
          "Define AccordionContextValue with openIndex and toggle function",
          "Build the Accordion parent that creates the context",
          "Build AccordionTrigger that renders a button with an arrow icon",
          "Build AccordionContent that shows/hides based on openIndex",
          "Attach Trigger and Content as Accordion.Trigger and Accordion.Content"
        ],
        "expectedOutcome": "A fully working accordion where the consumer writes declarative JSX with <Accordion><AccordionItem> blocks, and only the active item's content is visible."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why not just pass an openIndex prop to each item?",
            "answer": "You could — but the consumer would need to manage state and pass the same value to every child. Compound components hide that complexity by sharing state implicitly through context."
          },
          {
            "question": "Can I use compound components with data from an API?",
            "answer": "Yes. Fetch data in the parent, pass it through context, and children can render different states (loading, error, success) based on what they read from context."
          },
          {
            "question": "What about accessibility?",
            "answer": "Use proper ARIA attributes: role=tablist on the parent, role=tab on tabs, aria-selected on active tabs, and role=tabpanel on panels. TypeScript ensures these are strings when needed."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Compound components use context to share state implicitly",
          "The parent creates state, children consume it via a guard hook",
          "Static properties (Tabs.Tab) create a clean declarative API",
          "TypeScript enforces correct nesting and prop types"
        ],
        "takeaway": "Compound components give you an API that reads like HTML — intuitive for consumers, powerful under the hood."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "typescript-for-react"
}
