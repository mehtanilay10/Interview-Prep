{
  "id": "lesson-tsr-010",
  "slug": "typing-useref",
  "moduleSlug": "typing-hooks",
  "title": "Typing useRef",
  "description": "Learn how to type useRef for DOM access and mutable values.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": ["react", "typescript", "useref", "hooks"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "useRef has two use cases: accessing DOM elements and storing mutable values. Each requires different typing."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "DOM Element Refs"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "DOM Ref",
        "content": "Typing refs for DOM elements",
        "language": "tsx",
        "code": "function FocusInput() {\n  // Ref for input element\n  const inputRef = useRef<HTMLInputElement>(null);\n\n  const focusInput = () => {\n    inputRef.current?.focus();\n  };\n\n  return (\n    <>\n      <input ref={inputRef} />\n      <button onClick={focusInput}>Focus</button>\n    </>\n  );\n}\n\n// Ref for div element\nconst divRef = useRef<HTMLDivElement>(null);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Mutable Value Refs"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Mutable Ref",
        "content": "Typing refs for mutable values",
        "language": "tsx",
        "code": "function Timer() {\n  // Store interval ID\n  const intervalRef = useRef<number | null>(null);\n\n  const start = () => {\n    intervalRef.current = window.setInterval(() => {\n      console.log('Tick');\n    }, 1000);\n  };\n\n  const stop = () => {\n    if (intervalRef.current) {\n      clearInterval(intervalRef.current);\n    }\n  };\n\n  return (...);\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Null Initial Value",
        "text": "DOM refs are initialized with null because the element doesn't exist yet. Use optional chaining (ref.current?.method()) to safely access."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "DOM refs: useRef<HTMLElement>(null)",
          "Mutable refs: useRef<Type>(initialValue)",
          "Access with ref.current",
          "Use optional chaining for null safety"
        ],
        "takeaway": "useRef typing depends on whether it's for DOM or mutable values."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
