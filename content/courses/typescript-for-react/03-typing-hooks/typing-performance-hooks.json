{
  "id": "lesson-tsr-033",
  "slug": "typing-performance-hooks",
  "moduleSlug": "typing-hooks",
  "courseSlug": "typescript-for-react",
  "title": "Typing Performance Hooks",
  "description": "Learn how to type useMemo, useCallback, and memo for optimized React components.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "react",
    "typescript",
    "performance",
    "memo",
    "hooks"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Performance hooks like useMemo and useCallback help avoid unnecessary recalculations. TypeScript ensures the memoized values and callbacks have the correct types."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useMemo Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed useMemo",
        "content": "Memoizing computed values with types",
        "language": "tsx",
        "code": "interface User {\n  id: number;\n  name: string;\n  posts: Post[];\n}\n\nfunction UserStats({ users }: { users: User[] }) {\n  const totalUsers = useMemo(() => users.length, [users]);\n  const activeUsers = useMemo(\n    () => users.filter((u) => u.posts.length > 0).length,\n    [users]\n  );\n\n  return (\n    <div>\n      <p>Total: {totalUsers}</p>\n      <p>Active: {activeUsers}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useCallback Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed useCallback",
        "content": "Memoizing event handlers with types",
        "language": "tsx",
        "code": "function Search({ onSearch }: { onSearch: (query: string) => void }) {\n  const handleChange = useCallback(\n    (e: ChangeEvent<HTMLInputElement>) => {\n      onSearch(e.target.value);\n    },\n    [onSearch]\n  );\n\n  return <input onChange={handleChange} />;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "memo and React.memo"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React.memo wraps a component and prevents re-renders when props haven't changed. With TypeScript, props stay fully typed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React.memo",
        "content": "Memoizing a component with typed props",
        "language": "tsx",
        "code": "interface ExpensiveListProps {\n  items: string[];\n  filter: string;\n}\n\nconst ExpensiveList = memo(function ExpensiveList({\n  items,\n  filter,\n}: ExpensiveListProps) {\n  const visible = items.filter((item) =>\n    item.toLowerCase().includes(filter.toLowerCase())\n  );\n\n  return (\n    <ul>\n      {visible.map((item) => (\n        <li key={item}>{item}</li>\n      ))}\n    </ul>\n  );\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Don't Over-Optimize",
        "text": "useMemo and useCallback add overhead. Profile first, then optimize only when needed. TypeScript helps ensure the optimization is correct."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "useMemo",
            "definition": "Memoizes a computed value until dependencies change"
          },
          {
            "term": "useCallback",
            "definition": "Memoizes a function reference until dependencies change"
          },
          {
            "term": "memo",
            "definition": "Wraps a component to skip re-renders when props are shallow-equal"
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Common Questions",
        "items": [
          {
            "question": "Should I wrap every function in useCallback?",
            "answer": "No. Only wrap callbacks passed to optimized child components or used as hook dependencies."
          },
          {
            "question": "Does useMemo guarantee the same reference?",
            "answer": "Yes, as long as dependencies don't change. TypeScript ensures the returned type is consistent."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useMemo returns the same type as the computation",
          "useCallback preserves function signatures",
          "memo keeps component props typed",
          "Optimize only when profiling shows a benefit"
        ],
        "takeaway": "Typed performance hooks keep optimization safe and maintainable."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
