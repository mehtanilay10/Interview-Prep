{
  "id": "lesson-tsr-032",
  "slug": "typing-usecontext-and-redux",
  "moduleSlug": "typing-hooks",
  "courseSlug": "typescript-for-react",
  "title": "Typing useContext and Redux Hooks",
  "description": "Learn how to type React Context consumers and Redux hooks with TypeScript.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": ["react", "typescript", "context", "redux", "hooks"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React Context and Redux are common state management tools in React. TypeScript makes consuming them type-safe and self-documenting."
      }
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Type-Safe Context"}
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When creating a context, provide a default value that matches the context type. Always create a custom hook to consume the context so you get a clear error if the provider is missing."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed Context",
        "content": "Creating and consuming type-safe context",
        "language": "tsx",
        "code": "interface AuthContextValue {\n  user: { id: string; name: string } | null;\n  login: (name: string) => void;\n  logout: () => void;\n}\n\nconst AuthContext = createContext<AuthContextValue | undefined>(undefined);\n\nfunction AuthProvider({ children }: { children: ReactNode }) {\n  const [user, setUser] = useState<AuthContextValue['user']>(null);\n\n  const login = (name: string) => setUser({ id: crypto.randomUUID(), name });\n  const logout = () => setUser(null);\n\n  return (\n    <AuthContext.Provider value={{ user, login, logout }}>\n      {children}\n    </AuthContext.Provider>\n  );\n}\n\nfunction useAuth() {\n  const context = useContext(AuthContext);\n  if (!context) throw new Error('useAuth must be used within AuthProvider');\n  return context;\n}"
      }
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Typing Redux Hooks"}
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Redux Toolkit ships with typed hooks. Use RootState and AppDispatch types so selectors and dispatch are fully typed across your app."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed Redux Hooks",
        "content": "Using typed Redux Toolkit hooks",
        "language": "tsx",
        "code": "import { useDispatch, useSelector } from 'react-redux';\nimport type { RootState, AppDispatch } from './store';\n\nexport const useAppDispatch = useDispatch.withTypes<AppDispatch>();\nexport const useAppSelector = useSelector.withTypes<RootState>();\n\nfunction UserProfile() {\n  const dispatch = useAppDispatch();\n  const user = useAppSelector((state) => state.auth.user);\n\n  useEffect(() => {\n    if (user) dispatch(fetchUser(user.id));\n  }, [user, dispatch]);\n\n  return <div>{user?.name ?? 'Guest'}</div>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Typed Hooks Pattern",
        "text": "Export typed versions of useDispatch and useSelector from a separate file. This gives you autocomplete for state slices and catches invalid dispatch calls at compile time."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "createContext",
            "definition": "Creates a Context object that components can provide or read"
          },
          {
            "term": "useContext",
            "definition": "Hook that reads the nearest context value and triggers re-renders on change"
          },
          {
            "term": "RootState",
            "definition": "The inferred type of the entire Redux store state"
          }
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Context vs Redux",
        "cards": [
          {
            "title": "React Context",
            "description": "Built-in state sharing for small to medium apps",
            "pros": ["No extra library", "Simple API", "Great for theme/auth"],
            "cons": ["Can cause re-renders", "No devtools", "Harder to debug at scale"]
          },
          {
            "title": "Redux Toolkit",
            "description": "Predictable state container for complex apps",
            "pros": ["Devtools", "Middleware support", "Excellent TS inference"],
            "cons": ["More boilerplate", "Learning curve", "Overkill for simple state"]
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a ThemeContext",
        "description": "Create a fully typed theme context with light and dark modes.",
        "steps": [
          "Define a ThemeContextValue interface with theme and toggleTheme",
          "Create the context with a safe default",
          "Build a ThemeProvider that wraps children",
          "Export a useTheme custom hook",
          "Add a ThemeToggle component that uses the hook"
        ],
        "expectedOutcome": "A reusable, type-safe theme system that prevents misuse outside the provider."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Context needs a default value matching its type",
          "Always wrap useContext in a custom hook",
          "Redux Toolkit provides strong TypeScript inference",
          "Export typed dispatch and selector hooks for reuse"
        ],
        "takeaway": "Typed context and Redux hooks make global state predictable and self-documenting."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "typescript-for-react"
}
