{
  "id": "lesson-tsr-036",
  "slug": "mocking-and-test-utilities",
  "moduleSlug": "testing-best-practices",
  "courseSlug": "typescript-for-react",
  "title": "Mocking and Test Utilities",
  "description": "Mocking TypeScript React components, typing test utilities, and using jest.mock with types.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "typescript",
    "testing",
    "jest",
    "mocking",
    "test-utilities"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Mocking in TypeScript requires understanding both Jest's mocking mechanisms and TypeScript's type system. When you mock a module, TypeScript needs to know the shape of the mock so you get proper autocomplete and type safety."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Typing Mocks Matters"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Type Safety in Mocks",
        "text": "Untyped mocks can silently accept wrong arguments or return wrong shapes, leading to tests that pass but hide bugs. TypeScript catches these issues at compile time."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "jest.mock with Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed Mock Module",
        "content": "Mocking a module with TypeScript types",
        "language": "typescript",
        "code": "// api.ts\nexport interface User {\n  id: number;\n  name: string;\n}\n\nexport const fetchUser = async (id: number): Promise<User> => {\n  const response = await fetch(`/api/users/${id}`);\n  return response.json();\n};\n\n// __tests__/api.test.ts\nimport { fetchUser, User } from '../api';\n\n// Tell TypeScript this is a mock\njest.mock('../api');\n\n// The mock is typed with the original module's types\nconst mockFetchUser = fetchUser as jest.MockedFunction<typeof fetchUser>;\n\nmockFetchUser.mockResolvedValue({ id: 1, name: 'Alice' } as User);\n\n// Test uses the typed mock\nasync function testFetchUser() {\n  const user = await mockFetchUser(1);\n  expect(user.name).toBe('Alice'); // TypeScript knows user is User\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Mocking React Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Mock Component",
        "content": "Mocking a child component for testing",
        "language": "tsx",
        "code": "interface ButtonProps {\n  label: string;\n  onClick: () => void;\n}\n\nfunction Button({ label, onClick }: ButtonProps) {\n  return <button onClick={onClick}>{label}</button>;\n}\n\n// Parent component that uses Button\nfunction Form({ onSubmit }: { onSubmit: () => void }) {\n  return (\n    <form onSubmit={onSubmit}>\n      <Button label=\"Submit\" onClick={onSubmit} />\n    </form>\n  );\n}\n\n// Test with mocked Button\njest.mock('./Button', () => ({\n  __esModule: true,\n  default: jest.fn(({ label, onClick }: ButtonProps) => (\n    <button data-testid=\"mock-button\" onClick={onClick}>{label}</button>\n  ))\n}));\n\nfunction FormTest() {\n  const handleSubmit = jest.fn();\n  \n  render(<Form onSubmit={handleSubmit} />);\n  \n  const button = screen.getByTestId('mock-button');\n  fireEvent.click(button);\n  expect(handleSubmit).toHaveBeenCalled();\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Custom Render Utilities"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed Render Utility",
        "content": "Creating a typed render function with providers",
        "language": "tsx",
        "code": "import { render, RenderOptions } from '@testing-library/react';\nimport { ThemeProvider } from '../ThemeProvider';\nimport { QueryClient, QueryClientProvider } from '@tanstack/react-query';\n\ninterface CustomRenderOptions extends Omit<RenderOptions, 'wrapper'> {\n  theme?: 'light' | 'dark';\n}\n\nfunction customRender(ui: ReactElement, options?: CustomRenderOptions) {\n  const queryClient = new QueryClient({\n    defaultOptions: { queries: { retry: false } }\n  });\n\n  return render(\n    <QueryClientProvider client={queryClient}>\n      <ThemeProvider theme={options?.theme || 'light'}>\n        {ui}\n      </ThemeProvider>\n    </QueryClientProvider>,\n    options\n  );\n}\n\nexport * from '@testing-library/react';\nexport { customRender as render };"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Mocking Approaches",
        "cards": [
          {
            "title": "jest.mock()",
            "description": "Automatically mocks an entire module. Best for external dependencies like API clients.",
            "pros": ["Quick to set up", "Mocks entire module", "Good for integration tests"],
            "cons": ["Less control", "Can miss edge cases"]
          },
          {
            "title": "jest.fn()",
            "description": "Creates a manually controlled mock function. Best for functions you need to assert on.",
            "pros": ["Full control", "Easy to assert", "Clear intent"],
            "cons": ["More verbose", "Need manual setup"]
          },
          {
            "title": "MSW (Mock Service Worker)",
            "description": "Intercepts network requests at the service worker level. Best for API mocking.",
            "pros": ["Realistic", "Works with any test", "Declarative handlers"],
            "cons": ["Requires setup", "Extra dependency"]
          }
        ]
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Mocking Checklist",
        "items": [
          {
            "text": "Type the mock with jest.MockedFunction or jest.MockedClass",
            "hint": "Preserves the original type for type safety"
          },
          {
            "text": "Use __esModule: true for ES module interop",
            "hint": "Required when mocking ES modules with Jest"
          },
          {
            "text": "Reset mocks after each test",
            "hint": "Use afterEach(() => jest.clearAllMocks())"
          },
          {
            "text": "Type the return values of mockResolvedValue",
            "hint": "Cast to the expected interface or use 'as'"
          },
          {
            "text": "Avoid mocking too much",
            "hint": "Only mock external dependencies, not your own components"
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Mock a Custom Hook",
        "description": "Create a typed mock for the useCounter custom hook from the Typing Hooks module.",
        "steps": [
          "Define the useCounter return type",
          "Create a jest.MockedFunction for the hook",
          "Set up mockReturnValue to control counter behavior",
          "Write a test component that uses the mocked hook",
          "Assert the component renders the mocked counter values"
        ],
        "expectedOutcome": "A test suite where the custom hook is fully typed and controllable through mocks."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently Asked Questions",
        "items": [
          {
            "question": "Why does jest.mock need __esModule?",
            "answer": "ES modules use named exports while CommonJS uses default exports. The __esModule flag tells Jest to respect the original export structure."
          },
          {
            "question": "Should I type mocks with 'as any'?",
            "answer": "Avoid 'as any' when possible. Use jest.MockedFunction<typeof fn> to preserve types. Only fall back to 'as any' for third-party libraries with poor type definitions."
          },
          {
            "question": "How do I mock React Router?",
            "answer": "Use MemoryRouter with initialEntries for routing tests, or mock useNavigate and useParams with jest.fn() returning typed values."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use jest.MockedFunction to preserve types",
          "Mock components by typing their props",
          "Create custom render utilities for shared setup",
          "Reset mocks between tests"
        ],
        "takeaway": "Typed mocks make your tests safer and easier to debug."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "typescript-for-react"
}