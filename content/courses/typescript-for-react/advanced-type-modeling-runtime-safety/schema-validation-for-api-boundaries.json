{
  "id": "lesson-typescript-for-react-advanced-type-modeling-runtime-safety-schema-validation-for-api-boundaries",
  "slug": "schema-validation-for-api-boundaries",
  "moduleSlug": "advanced-type-modeling-runtime-safety",
  "courseSlug": "typescript-for-react",
  "title": "Schema validation for API boundaries",
  "description": "Learn how schema validation libraries bridge the gap between backend types and frontend TypeScript code.",
  "order": 9,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "typescript",
    "zod",
    "schema-validation",
    "api"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "APIs are contracts between the server and the browser. The server promises a shape, and the browser promises to use it correctly. But promises are not enough. Schema validation gives you a single source of truth that lives in both TypeScript types and runtime checks."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining a Schema"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A schema describes the shape of your data. Instead of writing an interface by hand and hoping the JSON matches, you describe the data once and derive both the type and the runtime check from it. If the server returns something different, you find out immediately."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Schema with Zod",
        "content": "Defining and inferring a type from a schema",
        "language": "typescript",
        "code": "import { z } from 'zod';\n\nconst PostSchema = z.object({\n  id: z.string().uuid(),\n  title: z.string().min(1),\n  content: z.string(),\n  authorId: z.string(),\n  tags: z.array(z.string()).default([]),\n  publishedAt: z.coerce.date()\n});\n\ntype Post = z.infer<typeof PostSchema>;\n\nconst post = PostSchema.parse(apiResponse);\n// post is now fully typed as Post"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Shared Types with the Backend"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The biggest benefit of schema validation is a single definition that both the backend and frontend understand. If you generate schemas from your database or API layer, the frontend automatically stays in sync when the backend changes."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Generate schemas from your database using Prisma, Drizzle, or tRPC",
          "Share schema packages between server and client",
          "Use schema inference to avoid duplicated interfaces",
          "Fail fast with clear error messages when data is wrong"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Form Validation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Schema validation is not only for APIs. Forms are another major boundary. User input is untrusted by definition, so validating it against a schema before submission prevents corrupt data from ever reaching your server."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Form Schema and Validation",
        "content": "Validating form input with a schema",
        "language": "typescript",
        "code": "import { z } from 'zod';\nimport { useForm } from 'react-hook-form';\nimport { zodResolver } from '@hookform/resolvers/zod';\n\nconst SignupSchema = z.object({\n  name: z.string().min(2),\n  email: z.string().email(),\n  password: z.string().min(8)\n});\n\ntype SignupInput = z.infer<typeof SignupSchema>;\n\nfunction SignupForm() {\n  const {\n    register,\n    handleSubmit,\n    formState: { errors }\n  } = useForm<SignupInput>({\n    resolver: zodResolver(SignupSchema)\n  });\n\n  const onSubmit = (data: SignupInput) => {\n    // data is validated and typed\n    api.signup(data);\n  };\n\n  return (\n    <form onSubmit={handleSubmit(onSubmit)}>\n      <input {...register('name')} />\n      {errors.name && <span>{errors.name.message}</span>}\n\n      <input type='email' {...register('email')} />\n      {errors.email && <span>{errors.email.message}</span>}\n\n      <input type='password' {...register('password')} />\n      {errors.password && <span>{errors.password.message}</span>}\n\n      <button type='submit'>Sign up</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Validate at Every Boundary",
        "text": "API responses, form submissions, cookies, localStorage, and WebSocket messages are all external inputs. Each one should be validated with a schema before it is trusted by your application."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Schemas describe data shape in one place",
          "Infer TypeScript types from schemas instead of duplicating them",
          "Use schemas for API responses, forms, and shared packages",
          "Validation libraries give clear errors when data is wrong"
        ],
        "takeaway": "Schema validation turns vague runtime errors into predictable, typed data flows across your entire stack."
      }
    }
  ],
  "relatedLessons": [
    "runtime-validation-vs-compile-time-types",
    "avoiding-unsafe-type-assertions"
  ],
  "furtherReading": [
    {
      "title": "Zod Documentation",
      "url": "https://zod.dev/",
      "type": "tool"
    },
    {
      "title": "tRPC: End-to-End Type Safety",
      "url": "https://trpc.io/",
      "type": "tool"
    },
    {
      "title": "React Hook Form with Zod",
      "url": "https://react-hook-form.com/get-started#SchemaValidation",
      "type": "article"
    }
  ]
}
