{
  "id": "lesson-typescript-for-react-advanced-type-modeling-runtime-safety-discriminated-unions",
  "slug": "discriminated-unions",
  "moduleSlug": "advanced-type-modeling-runtime-safety",
  "courseSlug": "typescript-for-react",
  "title": "Discriminated unions",
  "description": "Learn how discriminated unions make complex React state and API responses type-safe and easy to maintain.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "typescript",
    "discriminated-unions",
    "union-types",
    "react"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A discriminated union is a set of object types that share one common property with a literal type. That shared property, called the discriminant, tells TypeScript exactly which shape you are working with. It is one of the most useful patterns for modeling state in React."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Modeling Async States"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React components often have several loading states: idle, loading, success, and error. Each state carries different data. A discriminated union lets you express all of these states in one type and safely access the right fields."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Async State as a Discriminated Union",
        "content": "Modeling fetch states with a common kind field",
        "language": "typescript",
        "code": "type FetchState<\n  TSuccess,\n  TError = string\n> =\n  | { kind: 'idle' }\n  | { kind: 'loading' }\n  | { kind: 'success'; data: TSuccess }\n  | { kind: 'error'; error: TError };\n\nfunction UserCard({ state }: { state: FetchState<User> }) {\n  switch (state.kind) {\n    case 'idle':\n      return <p>Search for a user.</p>;\n    case 'loading':\n      return <Spinner />;\n    case 'success':\n      return <Profile user={state.data} />;\n    case 'error':\n      return <Alert message={state.error} />;\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why the Discriminant Matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Without a discriminant, TypeScript cannot tell which properties are available in a union. The `kind` property acts as a key that unlocks the rest of the object. In the example above, TypeScript knows that `state.data` only exists when `state.kind` is `'success'`, so accessing `state.data` in any other case would be a compile-time error."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Union Types with and without Discrimination",
        "cards": [
          {
            "title": "Without a Discriminant",
            "description": "TypeScript must check every property before it knows which fields exist. The code becomes harder to write and easier to misuse.",
            "pros": [
              "Works for ad-hoc unions"
            ],
            "cons": [
              "No safe narrowing",
              "Property access is fragile",
              "Easy to forget checks"
            ]
          },
          {
            "title": "With a Discriminant",
            "description": "A single literal field tells TypeScript exactly which shape is active. The compiler narrows automatically in switch statements and if checks.",
            "pros": [
              "Automatic narrowing",
              "Forces exhaustive handling",
              "Easy to read and maintain"
            ],
            "cons": [
              "Requires a consistent property across all variants"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Real-World React Patterns"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Discriminated unions appear everywhere in React: form submission states, route guards, modal dialogs, and even event handlers. Grouping related states under one type makes components predictable and self-documenting."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Form Submission State",
        "content": "Using a discriminated union for form status",
        "language": "typescript",
        "code": "type FormState<\n  TData,\n  TError = string\n> =\n  | { status: 'idle' }\n  | { status: 'submitting' }\n  | { status: 'success'; data: TData }\n  | { status: 'error'; error: TError };\n\nfunction SignupForm() {\n  const [state, setState] = useState<FormState<{ email: string }>>({\n    status: 'idle'\n  });\n\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault();\n    setState({ status: 'submitting' });\n    try {\n      const data = await api.signup(formData);\n      setState({ status: 'success', data });\n    } catch (err) {\n      setState({ status: 'error', error: 'Please try again.' });\n    }\n  };\n\n  if (state.status === 'success') {\n    return <Checkmark email={state.data.email} />;\n  }\n  return <form onSubmit={handleSubmit}>...</form>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Choose a Clear Discriminant Name",
        "text": "Common names include `kind`, `type`, `status`, and `variant`. Pick a name that describes what changes across variants, and keep it consistent across your codebase so other developers know what to expect."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "A discriminated union shares one literal property across all variants",
          "TypeScript narrows automatically when you check that property",
          "Use them for async states, forms, modals, and route guards",
          "Combine with exhaustive checking for compile-time safety"
        ],
        "takeaway": "Discriminated unions turn messy conditional logic into predictable, type-safe state machines."
      }
    }
  ],
  "relatedLessons": [
    "never-and-exhaustive-checking",
    "type-predicates"
  ],
  "furtherReading": [
    {
      "title": "TypeScript Handbook: Discriminated Unions",
      "url": "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#discriminated-unions",
      "type": "article"
    },
    {
      "title": "State Machines in React with TypeScript",
      "url": "https://xstate.dev/docs/guides/typescript/",
      "type": "article"
    }
  ]
}
