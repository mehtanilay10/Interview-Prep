{
  "id": "lesson-typescript-for-react-advanced-type-modeling-runtime-safety-avoiding-unsafe-type-assertions",
  "slug": "avoiding-unsafe-type-assertions",
  "moduleSlug": "advanced-type-modeling-runtime-safety",
  "courseSlug": "typescript-for-react",
  "title": "Avoiding unsafe type assertions",
  "description": "Learn why type assertions can be dangerous and how to replace them with safer patterns in React and TypeScript.",
  "order": 10,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "typescript",
    "type-assertions",
    "type-safety",
    "react"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A type assertion tells TypeScript to treat a value as a specific type, even when the compiler cannot verify it. It is like a promise you make to the compiler: 'Trust me, I know this is a string.' If your promise is wrong, the compiler will not catch it and the bug will appear at runtime."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Cost of Lying to the Compiler"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Type assertions remove safety. When you assert a value is a type without proving it, you bypass the narrowing logic that would otherwise catch mistakes. This is especially risky in React components where props or state might come from external sources."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Unsafe Assertion Example",
        "content": "Asserting without validation can crash your app",
        "language": "typescript",
        "code": "interface User {\n  id: string;\n  name: string;\n}\n\nfunction getUserName(userId: string): string {\n  const user = localStorage.getItem(`user:${userId}`);\n\n  // Dangerous: user might be null or not a valid JSON string\n  const parsed = JSON.parse(user) as User;\n\n  return parsed.name.toUpperCase();\n}\n\n// If the stored data is missing or malformed,\n// this function throws at runtime."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Safer Alternatives"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Instead of asserting, validate. A type guard or schema validation proves the shape of the data before you use it. The compiler stays honest, and invalid data produces a clear error instead of a silent crash."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Replace Assertion with Validation",
        "content": "Using a type guard instead of as",
        "language": "typescript",
        "code": "interface User {\n  id: string;\n  name: string;\n}\n\nfunction isUser(value: unknown): value is User {\n  return (\n    typeof value === 'object' &&\n    value !== null &&\n    'id' in value &&\n    typeof (value as { id: unknown }).id === 'string' &&\n    'name' in value &&\n    typeof (value as { name: unknown }).name === 'string'\n  );\n}\n\nfunction getUserName(userId: string): string {\n  const raw = localStorage.getItem(`user:${userId}`);\n\n  if (!raw) return 'Unknown';\n\n  const parsed = JSON.parse(raw);\n\n  if (!isUser(parsed)) {\n    throw new Error('Invalid user data');\n  }\n\n  return parsed.name.toUpperCase();\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When Assertions Are Acceptable"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Assertions are not evil. They are useful in three situations: when you have already validated the data earlier, when you interact with a library that TypeScript cannot understand, or when the assertion is trivial and obviously correct."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "After a successful schema parse, the data is already validated",
          "When DOM APIs return `any` and you know the element exists",
          "For simple literals that the compiler cannot infer"
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Acceptable Assertion After Validation",
        "content": "Asserting after a schema parse",
        "language": "typescript",
        "code": "const UserSchema = z.object({\n  id: z.string(),\n  name: z.string()\n});\n\ntype User = z.infer<typeof UserSchema>;\n\nconst validated = UserSchema.parse(rawData);\n// validated is already known to be User\n// No additional assertion needed here"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "danger",
        "title": "Avoid as Unknown as Any",
        "text": "The pattern `value as unknown as SomeType` bypasses all type checking. It is a code smell that usually means the types are wrong somewhere. Fix the types instead of forcing the compiler to ignore the mismatch."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Type assertions bypass compiler checks and can hide bugs",
          "Prefer type guards or schema validation over assertions",
          "Assertions are acceptable only after prior validation",
          "Avoid `as unknown as SomeType` as a workaround"
        ],
        "takeaway": "Replace assertions with validations and let TypeScript prove correctness instead of assuming it."
      }
    }
  ],
  "relatedLessons": [
    "schema-validation-for-api-boundaries",
    "unknown-vs-any"
  ],
  "furtherReading": [
    {
      "title": "TypeScript Handbook: Type Assertions",
      "url": "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#type-assertions",
      "type": "article"
    },
    {
      "title": "Effective TypeScript: Avoid Assertions",
      "url": "https://effectivetypescript.com/",
      "type": "course"
    }
  ]
}
