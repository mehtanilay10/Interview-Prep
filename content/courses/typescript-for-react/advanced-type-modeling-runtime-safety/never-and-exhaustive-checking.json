{
  "id": "lesson-typescript-for-react-advanced-type-modeling-runtime-safety-never-and-exhaustive-checking",
  "slug": "never-and-exhaustive-checking",
  "moduleSlug": "advanced-type-modeling-runtime-safety",
  "courseSlug": "typescript-for-react",
  "title": "never and exhaustive checking",
  "description": "Learn how the never type helps you catch missing cases and build truly exhaustive conditional logic in TypeScript.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "typescript",
    "never",
    "exhaustive-checking",
    "discriminated-unions"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The `never` type is one of the most misunderstood features in TypeScript. It represents the set of values that never occur. In practice, you rarely see `never` as the type of a variable. Instead, it becomes a powerful tool for enforcing exhaustive conditional logic, ensuring that every possible case is handled."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What never Means"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every value is assignable to `never`, but `never` is assignable to every other type. That sounds backwards, but it is useful: it lets the compiler verify that a function always throws, always loops forever, or that a switch statement covers every case."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Functions That Return never",
        "content": "Functions that never return normally",
        "language": "typescript",
        "code": "function fail(message: string): never {\n  throw new Error(message);\n}\n\nfunction infiniteLoop(): never {\n  while (true) {\n    // keeps running forever\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Exhaustive Checking in Practice"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Imagine a React component that renders a button differently depending on the button state. If you use a union type for the state, you want the compiler to warn you when a new state is added but you forget to handle it. That is where exhaustive checking shines."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Exhaustive Switch Statement",
        "content": "Using never to catch missing cases",
        "language": "typescript",
        "code": "type ButtonState = 'idle' | 'loading' | 'success' | 'error';\n\nfunction ButtonIcon({ state }: { state: ButtonState }) {\n  switch (state) {\n    case 'idle':\n      return <span>Idle</span>;\n    case 'loading':\n      return <span>Loading...</span>;\n    case 'success':\n      return <span>Done</span>;\n    case 'error':\n      return <span>Retry</span>;\n    default:\n      const _exhaustive: never = state;\n      return _exhaustive;\n  }\n}"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If someone later adds `'disabled'` to the `ButtonState` union but forgets to update the switch statement, TypeScript will show an error on the default branch because the new value can no longer be assigned to `never`."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pattern for Exhaustive Checks",
        "text": "Create a reusable helper that forces every case to be handled. Many libraries and codebases define a small function like `assertNever(value)` so the pattern is consistent across the project."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using assertNever"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reusable Exhaustive Check Helper",
        "content": "A helper function for exhaustive checks",
        "language": "typescript",
        "code": "function assertNever(value: never): never {\n  throw new Error(`Unexpected value: ${value}`);\n}\n\nfunction renderStatus(status: 'idle' | 'loading' | 'success') {\n  switch (status) {\n    case 'idle':\n      return 'Ready';\n    case 'loading':\n      return 'Working...';\n    case 'success':\n      return 'Complete';\n    default:\n      return assertNever(status);\n  }\n}"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`never` represents values that never occur",
          "It is most useful in the default branch of exhaustive checks",
          "Adding a helper like `assertNever` makes the pattern reusable",
          "Exhaustive checks prevent bugs when union types change"
        ],
        "takeaway": "never turns union type changes into compile-time errors instead of runtime surprises."
      }
    }
  ],
  "relatedLessons": [
    "discriminated-unions",
    "type-predicates"
  ],
  "furtherReading": [
    {
      "title": "TypeScript Handbook: never",
      "url": "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#never",
      "type": "article"
    },
    {
      "title": "Exhaustive Type Checks in TypeScript",
      "url": "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#exhaustiveness-checking",
      "type": "article"
    }
  ]
}
