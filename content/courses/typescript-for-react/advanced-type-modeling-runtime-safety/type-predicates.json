{
  "id": "lesson-typescript-for-react-advanced-type-modeling-runtime-safety-type-predicates",
  "slug": "type-predicates",
  "moduleSlug": "advanced-type-modeling-runtime-safety",
  "courseSlug": "typescript-for-react",
  "title": "Type predicates",
  "description": "Learn how user-defined type guards let you narrow types safely and make React components more robust.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "typescript",
    "type-guards",
    "type-predicates",
    "narrowing"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "TypeScript can narrow many types with built-in checks like `typeof` and `in`, but those only go so far. When you work with custom classes, union types, or objects from an API, you need your own narrowing logic. Type predicates, also called user-defined type guards, let you write a function that tells the compiler exactly which type a value is."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Basic Pattern"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A type predicate is a special return type in the form `value is Type`. When the function returns `true`, TypeScript treats the input as that type inside the calling code. It is one of the cleanest ways to move runtime checks into reusable helpers."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Simple Type Predicate",
        "content": "Checking if a value is a string",
        "language": "typescript",
        "code": "function isString(value: unknown): value is string {\n  return typeof value === 'string';\n}\n\nfunction printLength(value: unknown) {\n  if (isString(value)) {\n    // value is now string in this branch\n    console.log(value.length);\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Narrowing Complex Objects"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The real power of type predicates shows up with object shapes. You can check for a unique property and tell the compiler the full object type. This is especially useful for API responses that may return different shapes depending on status."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Checking API Response Shapes",
        "content": "Type predicate for API response discrimination",
        "language": "typescript",
        "code": "type SuccessResponse<\n  T\n> = { status: 'success'; data: T };\n\ntype ErrorResponse = { status: 'error'; message: string };\n\ntype ApiResponse<T> = SuccessResponse<T> | ErrorResponse;\n\nfunction isSuccess<T>(\n  response: ApiResponse<T>\n): response is SuccessResponse<T> {\n  return response.status === 'success';\n}\n\nfunction useUser(userId: string) {\n  const response = fetch(`/api/users/${userId}`);\n  if (isSuccess(response)) {\n    // response is SuccessResponse<User> here\n    console.log(response.data.name);\n  } else {\n    console.error(response.message);\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using Predicates in React"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React components frequently receive mixed data from context, props, or external APIs. Type predicates let you validate that data once and then use it safely without repeated `as` assertions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Guarding Event Payloads",
        "content": "Type predicate for a custom event payload",
        "language": "typescript",
        "code": "interface ClickPayload {\n  x: number;\n  y: number;\n  target: HTMLElement;\n}\n\nfunction isClickPayload(\n  payload: unknown\n): payload is ClickPayload {\n  return (\n    typeof payload === 'object' &&\n    payload !== null &&\n    'x' in payload &&\n    'y' in payload &&\n    'target' in payload\n  );\n}\n\nfunction handleCustomEvent(payload: unknown) {\n  if (isClickPayload(payload)) {\n    console.log(`Clicked at ${payload.x}, ${payload.y}`);\n  }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Keep Predicates Honest",
        "text": "A type predicate should always return true only when the value truly matches the claimed type. A bad predicate can create a false sense of safety and allow runtime errors to slip through."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Type predicates use the return type `value is Type`",
          "They let you write reusable runtime checks",
          "Use them for API responses, custom events, and complex unions",
          "Keep predicate logic simple and accurate"
        ],
        "takeaway": "Type predicates let you move validation logic out of components and into small, testable helpers."
      }
    }
  ],
  "relatedLessons": [
    "discriminated-unions",
    "unknown-vs-any"
  ],
  "furtherReading": [
    {
      "title": "TypeScript Handbook: User-Defined Type Guards",
      "url": "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#user-defined-type-guards",
      "type": "article"
    },
    {
      "title": "Advanced TypeScript: Type Guards and Predicates",
      "url": "https://www.typescriptlang.org/docs/handbook/2/typeof-types-guards.html",
      "type": "article"
    }
  ]
}
