{
  "id": "lesson-tsr-028",
  "slug": "type-assertions-and-as",
  "moduleSlug": "typescript-basics",
  "courseSlug": "typescript-for-react",
  "title": "Type Assertions and `as`",
  "description": "Learn to tell TypeScript what type a value really is using assertions, `as`, and `satisfies`.",
  "order": 6,
  "difficulty": "beginner",
  "estimatedMinutes": 14,
  "tags": [
    "typescript",
    "type-assertion",
    "as",
    "satisfies"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Sometimes TypeScript knows less about a value than you do. Type assertions let you tell the compiler to trust your judgment, without changing the runtime value."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The `as` Syntax"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Type Assertion with as",
        "content": "Asserting a more specific type",
        "language": "typescript",
        "code": "// TypeScript sees message as unknown\nconst message = JSON.parse(localStorage.getItem('msg') || 'null');\n\n// We know it is a string\nconst text = message as string;\n\n// From a DOM element\nconst input = document.getElementById('name') as HTMLInputElement;\nconst value = input.value;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Assertion Styles"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Two Assertion Styles",
        "content": "Angle-bracket vs as syntax",
        "language": "typescript",
        "code": "// Angle bracket style (JSX-free files)\nconst value = <string>someValue;\n\n// 'as' style (works in .tsx files)\nconst value = someValue as string;\n\n// Prefer 'as' in TypeScript projects\nconst element = document.querySelector('.card') as HTMLDivElement;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Assertions Do Not Convert",
        "text": "A type assertion is a compile-time instruction only. It does not change the value or perform any runtime conversion. Use it only when you have verified the type yourself."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Const Assertions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `as const` assertion tells TypeScript to treat a value as an immutable, literal type."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Const Assertion",
        "content": "Freezing literal types",
        "language": "typescript",
        "code": "// Without as const\nconst point = { x: 1, y: 2 }; // { x: number; y: number }\n\n// With as const\nconst frozen = { x: 1, y: 2 } as const; // { readonly x: 1; readonly y: 2 }\n\n// Array of literal strings\nconst routes = ['/home', '/about', '/contact'] as const;\n// type: readonly [\"/home\", \"/about\", \"/contact\"]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The `satisfies` Operator"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `satisfies` keyword checks that a value matches a type while preserving its original literal types. It is safer than `as` because it errors when the check fails."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "satisfies",
        "content": "Checking a value against a type",
        "language": "typescript",
        "code": "interface Config {\n  port: number;\n  host: string;\n}\n\n// Preserves literal types while checking the shape\nconst config = {\n  port: 3000,\n  host: 'localhost',\n} satisfies Config;\n\n// config.port is number, not widened\n// A missing field would be a compile error"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use What"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Tool",
          "Best For",
          "Risk"
        ],
        "rows": [
          [
            "as",
            "Trusted casts from unknown or DOM",
            "Silently bypasses checks"
          ],
          [
            "as const",
            "Freezing literal values",
            "Read-only values only"
          ],
          [
            "satisfies",
            "Checking a value against a shape",
            "Errors on mismatch"
          ]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer `satisfies`",
        "text": "Whenever you only need to check that a value matches a shape, use `satisfies` instead of `as`. It keeps literal types intact and fails loudly when the shape changes."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use `as` for trusted casts",
          "Use `as const` to freeze literal types",
          "Use `satisfies` to check shapes safely",
          "Assertions never change runtime values"
        ],
        "takeaway": "Type assertions bridge the gap between what TypeScript knows and what you know."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "typescript-for-react"
}