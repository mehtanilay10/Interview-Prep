{
  "id": "lesson-rq-006",
  "slug": "pagination",
  "moduleSlug": "query-hooks",
  "title": "Pagination",
  "description": "Learn how to implement paginated queries.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": ["react-query", "pagination", "pages", "data"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useQuery** can handle pagination by including the page number in the query key."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Paginated Query"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Pagination",
        "content": "Implementing pagination",
        "language": "jsx",
        "code": "function UserList() {\n  const [page, setPage] = useState(1);\n\n  const { data, isLoading, isPlaceholderData } = useQuery({\n    queryKey: ['users', page],\n    queryFn: () => fetchUsers(page),\n    placeholderData: keepPreviousData // Show old data while loading\n  });\n\n  return (\n    <div>\n      {isLoading && <p>Loading...</p>}\n      <ul>\n        {data.users.map(user => <li key={user.id}>{user.name}</li>)}\n      </ul>\n      <button onClick={() => setPage(p => Math.max(1, p - 1))} disabled={page === 1}>\n        Previous\n      </button>\n      <button onClick={() => setPage(p => p + 1)} disabled={isPlaceholderData}>\n        Next\n      </button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "keepPreviousData",
        "text": "Use keepPreviousData (or placeholderData) to show previous page data while loading the new page for a smoother UX."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Include page in query key",
          "Use keepPreviousData for smooth transitions",
          "Each page is cached separately",
          "Disable next button when loading"
        ],
        "takeaway": "Pagination is straightforward with React Query."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
