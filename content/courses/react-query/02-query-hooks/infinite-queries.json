{
  "id": "lesson-rq-007",
  "slug": "infinite-queries",
  "moduleSlug": "query-hooks",
  "title": "Infinite Queries",
  "description": "Learn how to implement infinite scrolling with useInfiniteQuery.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 22,
  "tags": ["react-query", "infinite", "scrolling", "pagination"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useInfiniteQuery** is designed for infinite scrolling and load-more patterns. It manages multiple pages of data automatically."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Infinite Query"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useInfiniteQuery",
        "content": "Implementing infinite scroll",
        "language": "jsx",
        "code": "import { useInfiniteQuery } from '@tanstack/react-query';\n\nfunction UserFeed() {\n  const {\n    data,\n    fetchNextPage,\n    hasNextPage,\n    isFetchingNextPage,\n    isLoading\n  } = useInfiniteQuery({\n    queryKey: ['users'],\n    queryFn: ({ pageParam = 1 }) => fetchUsers(pageParam),\n    initialPageParam: 1,\n    getNextPageParam: (lastPage) => lastPage.nextPage ?? undefined\n  });\n\n  if (isLoading) return <p>Loading...</p>;\n\n  return (\n    <div>\n      {data.pages.map((page, i) => (\n        <div key={i}>\n          {page.users.map(user => (\n            <div key={user.id}>{user.name}</div>\n          ))}\n        </div>\n      ))}\n      <button\n        onClick={() => fetchNextPage()}\n        disabled={!hasNextPage || isFetchingNextPage}\n      >\n        {isFetchingNextPage ? 'Loading...' : 'Load More'}\n      </button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "getNextPageParam",
        "text": "getNextPageParam returns the next page parameter or undefined if there are no more pages."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useInfiniteQuery for infinite scroll",
          "Manages multiple pages automatically",
          "fetchNextPage loads more data",
          "getNextPageParam determines next page"
        ],
        "takeaway": "Infinite queries simplify load-more patterns."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
