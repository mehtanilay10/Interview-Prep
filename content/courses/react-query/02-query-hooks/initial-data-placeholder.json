{
  "id": "lesson-react-query-042",
  "slug": "initial-data-placeholder",
  "moduleSlug": "query-hooks",
  "courseSlug": "react-query",
  "title": "Initial Data & Placeholder Data",
  "description": "Learn how to use initialData and placeholderData to provide instant UI feedback during data fetches.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "react-query",
    "placeholderData",
    "initialData",
    "caching",
    "ux",
    "options"
  ],
  "prerequisites": ["query-options-react-query"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React Query provides two powerful options to improve perceived performance: **initialData** and **placeholderData**. Both let you show something before the network request completes, but they work in very different ways."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is initialData?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**initialData** is a synchronous value used only on the very first render when no cached data exists yet. It lets the UI display data immediately instead of waiting for the first network response. Once the query completes, React Query replaces initialData with the real server response â€” and subsequent renders use the cache."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "initialData is One-Time",
        "text": "initialData is consumed only on the first render. After the query succeeds, the cache takes over. If you navigate away and back, the cache is used â€” not initialData."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is placeholderData?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**placeholderData** is used whenever cached data is missing or stale. Unlike initialData, it can be applied on every render where the cache is empty. It accepts either a static value or a function that returns the previous page's data â€” making it ideal for smooth pagination transitions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "initialData Example",
        "content": "Providing initial data so the UI never shows an empty state",
        "language": "tsx",
        "code": "import { useQuery, QueryClient } from '@tanstack/react-query';\n\n// Pre-seed the cache with initial data\nconst queryClient = new QueryClient({\n  defaultOptions: {\n    queries: {\n      initialData: () => {\n        const cached = localStorage.getItem('users');\n        return cached ? JSON.parse(cached) : undefined;\n      },\n    },\n  },\n});\n\nfunction UserList() {\n  const { data, isLoading } = useQuery({\n    queryKey: ['users'],\n    queryFn: async () => {\n      const res = await fetch('/api/users');\n      return res.json();\n    },\n  });\n\n  // data is available instantly from initialData!\n  // isLoading is true, but the UI renders with the placeholder\n  return (\n    <ul>\n      {data?.map(user => <li key={user.id}>{user.name}</li>)}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "placeholderData in Action"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "placeholderData shines in pagination. When the user clicks \"Next\", React Query fetches page 2. While that request is in flight, page 1 data remains visible â€” so the UI never feels broken. The data shown during the fetch is considered \"placeholder\" and not yet committed to the cache."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "placeholderData with keepPreviousData",
        "content": "Smooth pagination transitions using placeholderData",
        "language": "tsx",
        "code": "import { useState } from 'react';\nimport { useQuery, keepPreviousData } from '@tanstack/react-query';\n\nfunction PaginatedUsers() {\n  const [page, setPage] = useState(1);\n\n  const { data, isLoading, isPlaceholderData } = useQuery({\n    queryKey: ['users', page],\n    queryFn: () => fetchUsersPage(page),\n    placeholderData: keepPreviousData,\n  });\n\n  return (\n    <div>\n      <ul>\n        {data?.users.map(user => (\n          <li key={user.id}>{user.name}</li>\n        ))}\n      </ul>\n      <button\n        onClick={() => setPage(p => p - 1)}\n        disabled={page === 1 || isLoading}\n      >\n        Previous\n      </button>\n      <button\n        onClick={() => setPage(p => p + 1)}\n        disabled={!data?.hasMore || isPlaceholderData}\n      >\n        {isPlaceholderData ? 'Loading...' : 'Next'}\n      </button>\n      {isPlaceholderData && (\n        <p className=\"muted\">Loading next page...</p>\n      )}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Data Flow Comparison"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-placeholder-initial-flow",
        "caption": "How initialData and placeholderData are used at each stage of a query lifecycle",
        "definition": "flowchart LR\n    A[First Render\n    No Cache] --> B{initialData\n    provided?}\n    B -->|Yes| C[Show initialData\n    isLoading = true]\n    B -->|No| D[Show loading\n    skeleton]\n    C --> E[Network Request\n    in flight]\n    E --> F[Response arrives]\n    F --> G[Show real data\n    isLoading = false]\n\n    H[Subsequent Render\n    Cache exists] --> I{Data fresh?}\n    I -->|Yes| J[Show cached\n    data immediately]\n    I -->|No| K[placeholderData\n    shown if provided]\n    K --> L[Refetch in\n    background]\n    L --> M[Update cache\n    on response]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key Differences"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "initialData vs placeholderData",
        "cards": [
          {
            "title": "initialData",
            "description": "A static or computed value shown only on the very first render when the cache is empty.",
            "pros": [
              "Instant UI on first load",
              "Great for SSR or pre-fetched data",
              "Prevents flash-of-empty-state"
            ],
            "cons": [
              "Only used once â€” on first render",
              "Stale if the data changes before the response arrives",
              "Can give a false sense of freshness"
            ],
            "tags": ["first-render", "static", "one-time"]
          },
          {
            "title": "placeholderData",
            "description": "A fallback value (static or function) used whenever the cache is missing data. Can be reapplied on every relevant render.",
            "pros": [
              "Used every time data is missing from cache",
              "Smooth transitions during pagination",
              "Can derive from previous page data via function"
            ],
            "cons": [
              "Data is not committed to the cache",
              "Requires careful type handling for pagination",
              "isPlaceholderData flag adds complexity"
            ],
            "tags": ["fallback", "pagination", "reusable"]
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Each"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Use initialData when:",
        "items": [
          "You have pre-loaded data from SSR or a parent route loader",
          "You want to seed the cache from localStorage on first visit",
          "You want the UI to feel instant on first mount and don't care about slight staleness",
          "You pass data from a parent component that already fetched it"
        ]
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Use placeholderData when:",
        "items": [
          "You want a smooth UX during pagination or infinite scroll",
          "You need to show stale data while a refetch completes",
          "You want a fallback for any cache miss, not just the first render",
          "The user navigates between views that share query keys"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "initialData",
            "definition": "A static or computed value provided to useQuery that is used only on the first render when no cached data exists. Acts as a synchronous stand-in for the upcoming network response."
          },
          {
            "term": "placeholderData",
            "definition": "A fallback value (static or function) used by useQuery whenever the cache is empty. Unlike initialData, it can be reapplied on every render where data is missing, including during pagination transitions."
          },
          {
            "term": "keepPreviousData",
            "definition": "A special placeholderData value that returns the previous query's data. Used to keep the UI populated while a new page loads during pagination."
          },
          {
            "term": "stale-while-revalidate",
            "definition": "A caching strategy where stale (outdated) data is served immediately to the user while a background refetch updates the cache with fresh data. React Query implements this by default."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Combining Both Strategies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can use both options in the same query. initialData seeds the first render, and placeholderData covers subsequent renders where data might have been garbage-collected. This gives you instant loading on first visit and smooth transitions throughout the session."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Combined: initialData + placeholderData",
        "content": "Using both options together for the best UX",
        "language": "tsx",
        "code": "import { useQuery } from '@tanstack/react-query';\n\nfunction ArticleList({ categoryId }: { categoryId: string }) {\n  const { data, isLoading, isPlaceholderData } = useQuery({\n    queryKey: ['articles', categoryId],\n    queryFn: () => fetchArticles(categoryId),\n    initialData: () => {\n      // Pre-loaded from SSR â€” instant first paint\n      return window.__INITIAL_ARTICLES?.[categoryId];\n    },\n    placeholderData: (previousData) => {\n      // If cache was cleared, show old data while refetching\n      return previousData;\n    },\n  });\n\n  return (\n    <div>\n      {isLoading && <p>Loading articles...</p>}\n      {isPlaceholderData && (\n        <span className=\"badge\">Updating...</span>\n      )}\n      {data?.map(article => (\n        <article key={article.id}>\n          <h3>{article.title}</h3>\n        </article>\n      ))}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Type Safety with placeholderData",
        "text": "When using a function with placeholderData, TypeScript may require a type assertion. Use a type guard or cast carefully: placeholderData: (prev) => prev as Article[] | undefined"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Exercise"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Search with instantFeedback",
        "description": "Create a search component that fetches results from a mock API. Use initialData to show previous search results instantly when the component mounts. Then add placeholderData so that if the user clears the search and starts typing again, old results remain visible while the new request is in flight.",
        "steps": [
          "Create a useQuery that fetches results from /api/search?q={query}",
          "Set initialData to [] so the component never renders an empty list on first load",
          "Set placeholderData to keepPreviousData for smooth search transitions",
          "Display a 'Searching...' badge when isPlaceholderData is true",
          "Log to the console when initialData is used vs when real data arrives"
        ],
        "expectedOutcome": "A search component that feels instant: old results persist during new searches, and the first mount never shows an empty state. The console shows clear logs for initialData consumption vs cache hits."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Summary"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "initialData is used only on the first render when the cache is empty",
          "placeholderData is used every time cached data is missing or stale",
          "Use initialData for SSR data, localStorage seeds, or parent-provided props",
          "Use placeholderData for pagination, search transitions, and cache-rebuilding scenarios",
          "Both options dramatically improve perceived performance without extra network requests",
          "Combining both gives the best UX across the full query lifecycle"
        ],
        "takeaway": "initialData seeds the first impression; placeholderData keeps it smooth throughout the session."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Frequently Asked Questions"
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Initial Data & Placeholder Data â€” FAQ",
        "items": [
          {
            "question": "Does initialData trigger a network request?",
            "answer": "No. initialData is a local value â€” it does not trigger any fetch. The network request still fires normally; React Query simply shows initialData in the UI while waiting."
          },
          {
            "question": "Will placeholderData overwrite my cache?",
            "answer": "No. placeholderData is never written to the cache. It is only used for rendering. The real response is committed to the cache when it arrives."
          },
          {
            "question": "Can I use a function for initialData?",
            "answer": "Yes. initialData accepts a function that is called synchronously on the first render. This is useful for reading from localStorage or extracting data from window.__NEXT_DATA__ in SSR apps."
          },
          {
            "question": "What happens if initialData throws an error?",
            "answer": "If the initialData function throws, React Query falls back to the normal loading state. Wrap your logic in try/catch to be safe."
          },
          {
            "question": "How is this different from stale-while-revalidate?",
            "answer": "stale-while-revalidate is the overall caching strategy React Query uses. initialData and placeholderData are tools within that strategy: they provide the data that gets shown to the user while the revalidation request completes in the background."
          }
        ]
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
