{
  "id": "lesson-react-query-034",
  "slug": "polling-real-time-data",
  "moduleSlug": "real-world-patterns",
  "courseSlug": "react-query",
  "title": "Polling & Real-Time Data",
  "description": "Set up polling for real-time data updates and learn smart strategies for efficient data synchronization.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "polling",
    "real-time",
    "refetch-interval",
    "websocket",
    "live-data"
  ],
  "prerequisites": [
    "mutations"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Some applications need data that updates continuouslyâ€”stock prices, live notifications, or collaborative editing. React Query provides built-in polling capabilities that automatically refetch data at specified intervals, keeping your UI in sync with the server without manual effort."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Polling Fundamentals"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "When to Use Polling",
        "items": [
          "Dashboards showing live metrics or KPIs",
          "Stock prices or cryptocurrency tickers",
          "Real-time notifications and alerts",
          "Collaborative document status",
          "Server health monitoring"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Polling vs WebSockets",
        "text": "Polling is simpler to implement but less efficient than WebSockets. Use polling when data changes infrequently or when real-time infrastructure is unavailable."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "polling-lifecycle-034",
        "caption": "Polling lifecycle with React Query",
        "definition": "stateDiagram-v2\n    [*] --> Idle\n    Idle --> Fetching: Component mounts\n    Fetching --> Success: Data received\n    Success --> Waiting: Wait interval\n    Waiting --> Fetching: refetchInterval triggers\n    Success --> Error: Network failure\n    Error --> Waiting: Retry after delay\n    Waiting --> [*]: Component unmounts"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Polling Setup"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The simplest way to poll is to set `refetchInterval` on a query. This tells React Query to automatically refetch at the specified interval, regardless of whether the data has changed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Polling with refetchInterval",
        "content": "The simplest way to poll is to set refetchInterval on a query",
        "language": "tsx",
        "code": "const { data: ticker } = useQuery({\n  queryKey: ['btc-price'],\n  queryFn: fetchBitcoinPrice,\n  refetchInterval: 5000, // Poll every 5 seconds\n  refetchIntervalInBackground: false, // Don't poll when tab hidden\n  staleTime: 0 // Consider data stale immediately\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Background Polling",
        "text": "refetchIntervalInBackground controls whether polling continues when the user switches tabs. Set it to false to save bandwidth and battery."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Smart Polling Strategies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Not all data needs the same polling frequency. You can use a dynamic `refetchInterval` function to adjust the interval based on query state. For example, poll faster when there are unread notifications, slower when everything is read."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Conditional Polling",
        "content": "Poll faster when there are unread notifications, slower otherwise",
        "language": "tsx",
        "code": "const { data } = useQuery({\n  queryKey: ['notifications'],\n  queryFn: fetchNotifications,\n  refetchInterval: (query) => {\n    // Poll faster if there are unread notifications\n    if (query.state.data?.unreadCount > 0) {\n      return 3000; // 3 seconds\n    }\n    return 10000; // 10 seconds when all read\n  },\n  enabled: isUserActive // Only poll when user is active\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Pausing and Resuming Polling"
      }
    },
    {
      "type": "paragraph",
      "text": "Users often switch tabs or navigate away. Polling in the background wastes resources. You can pause polling when the page is hidden and resume when it becomes visible again.",
      "data": {
        "text": "Users often switch tabs or navigate away. Polling in the background wastes resources. You can pause polling when the page is hidden and resume when it becomes visible again."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Pause Polling When Hidden",
        "content": "Use the Page Visibility API to pause polling when the user is not looking",
        "language": "tsx",
        "code": "'use client';\n\nimport { useState, useEffect } from 'react';\nimport { useQuery } from '@tanstack/react-query';\n\nconst useVisibilityPolling = (\n  queryKey: unknown[]\n) => {\n  const [isVisible, setIsVisible] = useState(true);\n\n  useEffect(() => {\n    const handleVisibilityChange = () => {\n      setIsVisible(document.visibilityState === 'visible');\n    };\n\n    document.addEventListener('visibilitychange', handleVisibilityChange);\n    return () => {\n      document.removeEventListener('visibilitychange', handleVisibilityChange);\n    };\n  }, []);\n\n  return useQuery({\n    queryKey,\n    queryFn: fetchNotifications,\n    enabled: isVisible,\n    refetchInterval: isVisible ? 5000 : false\n  });\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Pause with External Control",
        "content": "Allow users or parent components to control polling state",
        "language": "tsx",
        "code": "const LiveDashboard = () => {\n  const [isLive, setIsLive] = useState(true);\n\n  const { data: metrics } = useQuery({\n    queryKey: ['metrics'],\n    queryFn: fetchMetrics,\n    refetchInterval: isLive ? 2000 : false,\n    enabled: true\n  });\n\n  return (\n    <div>\n      <button onClick={() => setIsLive(!isLive)}>\n        {isLive ? 'Pause Live Updates' : 'Resume Live Updates'}\n      </button>\n      <MetricsDisplay data={metrics} />\n    </div>\n  );\n};"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "WebSocket Integration"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you need true real-time updates, WebSockets are more efficient than polling. You can combine WebSockets for push updates with React Query for caching and state management. The WebSocket handler writes directly to the cache using `setQueryData`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "WebSocket + React Query",
        "content": "Combine WebSockets for push updates with React Query for caching",
        "language": "tsx",
        "code": "'use client';\n\nimport { useEffect } from 'react';\nimport { useQuery, useQueryClient } from '@tanstack/react-query';\n\ntype TickerData = {\n  price: number;\n  change: number;\n  lastUpdated: string;\n};\n\nconst useWebSocketTicker = (symbol: string) => {\n  const queryClient = useQueryClient();\n\n  useEffect(() => {\n    const ws = new WebSocket(`wss://stream.example.com/${symbol}`);\n\n    ws.onmessage = (event) => {\n      const tick = JSON.parse(event.data) as TickerData;\n\n      queryClient.setQueryData(['ticker', symbol], (old: TickerData | undefined) => ({\n        ...old,\n        price: tick.price,\n        change: tick.change,\n        lastUpdated: new Date().toISOString()\n      }));\n    };\n\n    ws.onclose = () => {\n      console.log('WebSocket disconnected');\n    };\n\n    return () => ws.close();\n  }, [symbol, queryClient]);\n\n  return useQuery({\n    queryKey: ['ticker', symbol],\n    queryFn: () => fetchTicker(symbol),\n    staleTime: Infinity\n  });\n};"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Polling vs WebSockets",
        "cards": [
          {
            "title": "Polling",
            "description": "Periodic HTTP requests to check for updates.",
            "pros": [
              "Simple to implement",
              "Works with any backend",
              "No persistent connection needed"
            ],
            "cons": [
              "Delayed updates (up to interval)",
              "Wastes requests when data hasn't changed",
              "Not truly real-time"
            ],
            "tags": [
              "simple",
              "http"
            ]
          },
          {
            "title": "WebSockets",
            "description": "Persistent bidirectional connection for instant push updates.",
            "pros": [
              "True real-time updates",
              "Low overhead after connection",
              "Bidirectional communication"
            ],
            "cons": [
              "Requires WebSocket server",
              "More complex connection management",
              "Reconnection logic needed"
            ],
            "tags": [
              "real-time",
              "push"
            ]
          }
        ]
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Real-Time Data Checklist",
        "items": [
          {
            "text": "Polling interval is appropriate for data volatility",
            "hint": "Fast-changing data needs shorter intervals"
          },
          {
            "text": "Background polling disabled by default",
            "hint": "Save resources when tab is hidden"
          },
          {
            "text": "Polling pauses when user navigates away",
            "hint": "Use enabled flag or visibility API"
          },
          {
            "text": "WebSocket updates write to React Query cache",
            "hint": "setQueryData keeps cache in sync"
          },
          {
            "text": "Stale time configured for data type",
            "hint": "Real-time data often needs staleTime: 0"
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Polling FAQ",
        "items": [
          {
            "question": "What interval should I use for polling?",
            "answer": "Match the interval to your data's change frequency. Stock tickers need 1-5 seconds. News feeds can use 30-60 seconds. Server status checks can use 5-10 minutes."
          },
          {
            "question": "Will polling drain the user's battery?",
            "answer": "Frequent polling on mobile can drain battery. Always use refetchIntervalInBackground: false and consider using the Network Information API to reduce intervals on slow connections."
          },
          {
            "question": "Can I mix polling and WebSockets?",
            "answer": "Yes. Use WebSockets for high-priority real-time data and polling for lower-priority data where WebSocket infrastructure is not available."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use refetchInterval for simple polling",
          "Control polling with enabled and refetchIntervalInBackground",
          "WebSocket updates integrate via setQueryData",
          "Always pause polling when not visible",
          "Choose intervals based on data change frequency"
        ],
        "takeaway": "Real-time data requires balancing freshness with performance. React Query makes it easy to configure smart polling strategies."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
