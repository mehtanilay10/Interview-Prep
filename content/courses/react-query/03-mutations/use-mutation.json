{
  "id": "lesson-rq-009",
  "slug": "use-mutation",
  "moduleSlug": "mutations",
  "title": "useMutation",
  "description": "Learn how to modify server data with useMutation.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": ["react-query", "mutation", "create", "update", "delete"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useMutation** is the hook for creating, updating, or deleting data on the server. Unlike queries, mutations are triggered manually."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Mutation"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useMutation",
        "content": "Creating a mutation",
        "language": "jsx",
        "code": "import { useMutation, useQueryClient } from '@tanstack/react-query';\n\nfunction AddUserForm() {\n  const queryClient = useQueryClient();\n\n  const mutation = useMutation({\n    mutationFn: async (newUser) => {\n      const response = await fetch('/api/users', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(newUser)\n      });\n      return response.json();\n    },\n    onSuccess: () => {\n      // Invalidate and refetch users query\n      queryClient.invalidateQueries({ queryKey: ['users'] });\n    }\n  });\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    mutation.mutate({ name: e.target.name.value });\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input name=\"name\" />\n      <button disabled={mutation.isPending}>\n        {mutation.isPending ? 'Adding...' : 'Add User'}\n      </button>\n      {mutation.isError && <p>Error: {mutation.error.message}</p>}\n      {mutation.isSuccess && <p>User added!</p>}\n    </form>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Mutation States",
        "text": "Mutations have states: isPending, isSuccess, isError. Use these to show loading indicators and feedback."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useMutation modifies server data",
          "Triggered manually with mutate()",
          "onSuccess callback for side effects",
          "Invalidate queries to refetch data"
        ],
        "takeaway": "useMutation handles data modifications."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
