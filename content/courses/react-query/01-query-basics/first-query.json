{
  "id": "lesson-rq-004",
  "slug": "first-query-react-query",
  "moduleSlug": "query-basics",
  "courseSlug": "react-query",
  "title": "Your First Query",
  "description": "Write your first useQuery hook, handle loading and error states, and understand query keys.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "react-query",
    "useQuery",
    "fetching",
    "data",
    "loading",
    "error-handling"
  ],
  "prerequisites": [
    "query-client",
    "setup"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useQuery** is the primary hook for fetching data in React Query. It accepts a configuration object with a unique queryKey and an async queryFn, and returns an object containing the data, loading state, error state, and several utility flags â€” all managed automatically."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The useQuery Signature"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In React Query v5, useQuery takes a single options object with queryKey and queryFn as the only required fields. This is a change from earlier versions where the hook accepted positional arguments."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic useQuery â€” v5 Syntax",
        "content": "The minimal useQuery call in React Query v5",
        "language": "tsx",
        "code": "import { useQuery } from '@tanstack/react-query';\n\ninterface User {\n  id: string;\n  name: string;\n  email: string;\n}\n\nasync function fetchUsers(): Promise<User[]> {\n  const response = await fetch('/api/users');\n  if (!response.ok) {\n    throw new Error(`HTTP ${response.status}: ${response.statusText}`);\n  }\n  return response.json();\n}\n\nfunction UserList() {\n  const { data, isLoading, isError, error } = useQuery({\n    queryKey: ['users'],\n    queryFn: fetchUsers,\n  });\n\n  if (isLoading) return <p>Loading users...</p>;\n  if (isError)   return <p className=\"error\">Error: {error.message}</p>;\n\n  return (\n    <ul>\n      {data?.map((user) => (\n        <li key={user.id}>{user.name} â€” {user.email}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "v5 Requires an Options Object",
        "text": "The old syntax useQuery(queryKey, queryFn) is no longer valid in v5. Always use useQuery({ queryKey, queryFn }) â€” this is a common migration pitfall."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Return Value Properties"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Property",
          "Type",
          "Description"
        ],
        "rows": [
          [
            "data",
            "T | undefined",
            "The resolved data, undefined until the query succeeds"
          ],
          [
            "isLoading",
            "boolean",
            "True on the very first render with no cache data"
          ],
          [
            "isPending",
            "boolean",
            "Alias for isLoading in v5 â€” same meaning"
          ],
          [
            "isError",
            "boolean",
            "True when the query has errored"
          ],
          [
            "error",
            "Error | null",
            "The error object if isError is true"
          ],
          [
            "isFetching",
            "boolean",
            "True whenever a fetch is in progress (including refetches)"
          ],
          [
            "isSuccess",
            "boolean",
            "True after the query succeeds at least once"
          ],
          [
            "dataUpdatedAt",
            "number",
            "Timestamp (ms) when data was last updated"
          ],
          [
            "isFetched",
            "boolean",
            "True after the query has been fetched at least once"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query Keys Explained"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Query keys are how React Query identifies and organizes cached data. They are simple arrays of values. Every value that changes the response should be included in the key â€” typically resource type plus identifying parameters."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Query Key Patterns",
        "items": [
          "['users'] â€” all users (no parameters)",
          "['users', userId] â€” one specific user by ID",
          "['posts', postId, 'comments'] â€” comments for a specific post",
          "['todos', 'filtered', filter, status] â€” filtered todos with multiple params",
          "['search', queryString, page] â€” search results with pagination"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "queryKey",
            "definition": "A unique array that identifies a query in the React Query cache. Must include all variables that affect the response. Used for caching, deduplication, and invalidation."
          },
          {
            "term": "queryFn",
            "definition": "An async function that returns a Promise resolving to the query data. Called by React Query when the cache is empty or stale."
          },
          {
            "term": "isLoading",
            "definition": "A boolean flag that is true only on the first render when no cached data exists. Distinct from isFetching, which is true during background refetches too."
          },
          {
            "term": "isError",
            "definition": "A boolean flag set to true when the query function throws an error. Paired with the error property containing the Error object."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query with Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Real queries almost always depend on variables â€” a user ID, a search term, a page number. Include all variables in the queryKey. React Query automatically creates separate cache entries for each unique key combination."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Parameterized Query",
        "content": "A query that depends on a dynamic userId prop",
        "language": "tsx",
        "code": "import { useQuery } from '@tanstack/react-query';\n\ninterface User {\n  id: string;\n  name: string;\n  role: string;\n}\n\nasync function fetchUser(userId: string): Promise<User> {\n  const response = await fetch(`/api/users/${encodeURIComponent(userId)}`);\n  if (!response.ok) throw new Error('Failed to fetch user');\n  return response.json();\n}\n\nfunction UserProfile({ userId }: { userId: string }) {\n  const { data: user, isLoading, isError, error } = useQuery({\n    queryKey: ['user', userId],\n    queryFn: () => fetchUser(userId),\n    enabled: !!userId,\n  });\n\n  if (isLoading) return <p>Loading profile...</p>;\n  if (isError)   return <p>Error: {error.message}</p>;\n  if (!user)     return null;\n\n  return (\n    <div>\n      <h1>{user.name}</h1>\n      <p>Role: {user.role}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-usequery-lifecycle",
        "caption": "The lifecycle of a useQuery call from mount to cache hit",
        "definition": "flowchart TD\n    A[Component mounts\n    queryKey: ['user', '42']] --> B{Cache hit\n    and fresh?}\n    B -->|Yes| C[Return cached data\n    isLoading: false\n    isFetching: false]\n    B -->|No| D[Start fetch]\n    D --> E[isLoading: true\n    isFetching: true]\n    E --> F{queryFn\n    resolves?}\n    F -->|Success| G[Cache data\n    isLoading: false\n    isFetching: false]\n    F -->|Error| H[Set error\n    isError: true]\n    G --> I[Component re-renders\n    with data]\n    H --> J[Retry based on\n    retry setting]\n    J -->|Exhausted| K[Error boundary\n    or fallback UI]\n    J -->|Retries remain| D"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The enabled Option"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The enabled option controls whether the query runs automatically. Set it to false when you want to fetch data only after a certain condition is met â€” such as when a userId becomes available. This is the foundation for dependent queries, covered in the next lesson."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Conditional Query with enabled",
        "content": "Preventing a query from running until required data is ready",
        "language": "tsx",
        "code": "function UserPosts({ userId }: { userId: string | null }) {\n  const { data: posts, isLoading } = useQuery({\n    queryKey: ['posts', userId],\n    queryFn: () => fetchUserPosts(userId!),\n    enabled: userId !== null, // Only fetch when userId is set\n  });\n\n  if (!userId) return <p>Select a user to view their posts.</p>;\n  if (isLoading) return <p>Loading posts...</p>;\n\n  return (\n    <ul>\n      {posts?.map(post => (\n        <li key={post.id}>{post.title}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Exercise"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a User Directory",
        "description": "Create a UserDirectory component that fetches and displays a list of users from a mock API. Practice writing a clean useQuery component with proper loading, error, and empty states.",
        "steps": [
          "Create a mock API function fetchUsers that returns a Promise<User[]> after a 500ms delay using setTimeout",
          "Write the UserDirectory component using useQuery with queryKey ['users'] and queryFn fetchUsers",
          "Show a loading spinner or text while isLoading is true",
          "Show a styled error message if isError is true",
          "Show a friendly message when data is an empty array",
          "Render each user as a card with name and email",
          "Add a second component UserProfile that fetches a single user by ID with queryKey ['user', userId]"
        ],
        "expectedOutcome": "A fully working user directory with smooth loading, error, and empty states. The UserProfile component correctly uses a parameterized query key and the enabled option."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Summary"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useQuery requires queryKey and queryFn in an options object (v5 syntax)",
          "Returns data, isLoading, isError, error, isFetching, and more",
          "queryKey must be unique and include all variables that affect the response",
          "queryFn must return a Promise â€” use async/await for clarity",
          "enabled controls whether the query runs automatically",
          "isLoading is true only on the first render with no cache data",
          "isFetching is true on every fetch, including background refetches"
        ],
        "takeaway": "useQuery is the fundamental hook of React Query. Mastering its return values and options is the foundation for everything else in this course."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
