{
  "id": "lesson-rq-004",
  "slug": "first-query-react-query",
  "moduleSlug": "query-basics",
  "title": "Your First Query",
  "description": "Learn how to fetch data with useQuery hook.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "react-query",
    "useQuery",
    "fetching",
    "data"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useQuery** is the primary hook for fetching data. It handles loading, error, and success states automatically."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Query"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useQuery",
        "content": "Fetching data with useQuery",
        "language": "jsx",
        "code": "import { useQuery } from '@tanstack/react-query';\n\nfunction UserList() {\n  const { data, isLoading, error } = useQuery({\n    queryKey: ['users'],\n    queryFn: async () => {\n      const response = await fetch('/api/users');\n      if (!response.ok) throw new Error('Network error');\n      return response.json();\n    }\n  });\n\n  if (isLoading) return <p>Loading...</p>;\n  if (error) return <p>Error: {error.message}</p>;\n\n  return (\n    <ul>\n      {data.map(user => <li key={user.id}>{user.name}</li>)}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query with Parameters"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Parameterized Query",
        "content": "Query that depends on variables",
        "language": "jsx",
        "code": "function UserProfile({ userId }) {\n  const { data: user, isLoading } = useQuery({\n    queryKey: ['user', userId],\n    queryFn: async () => {\n      const response = await fetch(`/api/users/${userId}`);\n      return response.json();\n    },\n    enabled: !!userId // Only run when userId exists\n  });\n\n  if (isLoading) return <p>Loading...</p>;\n  return <h1>{user.name}</h1>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Query Keys",
        "text": "Query keys must be unique per query. Include all variables that affect the data in the key array."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useQuery fetches and caches data",
          "Returns data, isLoading, error",
          "Query key must be unique",
          "enabled option controls when query runs"
        ],
        "takeaway": "useQuery is the foundation of React Query."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "react-query"
}
