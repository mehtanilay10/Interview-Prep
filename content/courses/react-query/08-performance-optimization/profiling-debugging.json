{
  "id": "lesson-rq-031",
  "slug": "profiling-debugging",
  "moduleSlug": "performance-optimization",
  "courseSlug": "react-query",
  "title": "Profiling and Debugging",
  "description": "Profile and debug React Query performance using DevTools, query inspectors, and network analysis.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "profiling",
    "debugging",
    "devtools",
    "performance",
    "network"
  ],
  "prerequisites": [
    "caching-strategies"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Before optimizing, you need to measure. React Query provides first-class DevTools that show query status, cache state, and performance timing. Combined with browser network inspection and React Profiler, you can identify slow queries, memory leaks, and unnecessary re-renders with precision."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Query DevTools"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The DevTools panel gives you a live view of every query and mutation in your application. You can inspect query keys, status, data, and timestamps. In development, DevTools automatically appear in the bottom-right corner. In production, you can disable them or enable them conditionally."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Conditional DevTools",
        "text": "Wrap the DevTools component in a process.env.NODE_ENV check or feature flag so it never ships to production accidentally."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Setting up React Query DevTools",
        "content": "Mounting the DevTools panel in your application root",
        "language": "tsx",
        "code": "import { QueryClientProvider } from '@tanstack/react-query';\nimport { ReactQueryDevtools } from '@tanstack/react-query-devtools';\n\nfunction App() {\n  const queryClient = new QueryClient({\n    defaultOptions: {\n      queries: {\n        staleTime: 1000 * 60 * 5,\n        retry: 1\n      }\n    }\n  });\n\n  return (\n    <QueryClientProvider client={queryClient}>\n      <YourRoutes />\n      {process.env.NODE_ENV === 'development' && (\n        <ReactQueryDevtools initialIsOpen={false} />\n      )}\n    </QueryClientProvider>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Inspecting Queries"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Each query in DevTools shows its current state: `pending`, `success`, or `error`. You can see the exact data object, the query hash, and the time since last fetch. If a query is stuck in `pending` or `error`, DevTools helps you identify whether the issue is network-related, a bad query key, or an exception in the query function."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "DevTools Panel",
          "What It Shows",
          "Common Use"
        ],
        "rows": [
          [
            "Queries",
            "All active queries with status, data, and timestamps",
            "Find stale or failing queries"
          ],
          [
            "Mutations",
            "Pending, success, and error states for mutations",
            "Debug optimistic update rollbacks"
          ],
          [
            "Cache",
            "Full query cache tree with query keys",
            "Verify cache invalidation behavior"
          ],
          [
            "Query Info",
            "Individual query details: observers, fetch times, state",
            "Deep-dive into a single slow query"
          ],
          [
            "Query Log",
            "Chronological event stream with fetch duration",
            "Trace request waterfalls"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Profiling with React DevTools"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React DevTools Profiler records render timings and commit data. Use it alongside React Query DevTools to see which components re-render when queries update. If a parent re-renders and passes new props to a memoized child, the profiler flags the mismatch. Common culprits are inline object literals, array spreads, and unstable callbacks."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-profiling-flow-031",
        "caption": "Systematic debugging flow for slow React Query applications",
        "definition": "graph TD\n    A[Symptom: Slow UI or Stale Data] --> B{React DevTools Profiler}\n    B -->|Excessive renders| C[Check props and memoization]\n    B -->|Normal renders| D{React Query DevTools}\n    D -->|Stale queries| E[Check staleTime and gcTime]\n    D -->|High refetch rate| F[Check query keys and refetchOnWindowFocus]\n    D -->|Cache misses| G[Check query key consistency]\n    C --> H[Apply useMemo / useCallback]\n    E --> I[Increase staleTime]\n    F --> J[Disable refetchOnWindowFocus for hidden tabs]\n    G --> K[Standardize query key factories]\n    H --> L[Measure again]\n    I --> L\n    J --> L\n    K --> L"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Network and Cache Debugging"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Browser DevTools Network tab reveals what React Query is actually requesting. Look for duplicate requests, missing cache headers, and slow response times. Combine this with `queryClient.getQueryData()` to inspect the cache programmatically and verify that data is stored under the expected keys."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Inspecting the cache programmatically",
        "content": "Using the QueryClient to inspect and manipulate cache during debugging",
        "language": "tsx",
        "code": "import { useQueryClient, useState } from 'react';\n\nfunction DebugPanel() {\n  const queryClient = useQueryClient();\n  const [debugInfo, setDebugInfo] = useState<Record<string, unknown>>({});\n\n  const inspectCache = () => {\n    const queries = queryClient.getQueriesData<any>({ type: 'all' });\n    const info: Record<string, unknown> = {};\n    queries.forEach(([queryKey, data]) => {\n      info[JSON.stringify(queryKey)] = {\n        dataSize: JSON.stringify(data).length,\n        state: queryClient.getQueryState(queryKey)?.status\n      };\n    });\n    setDebugInfo(info);\n  };\n\n  return (\n    <div>\n      <button onClick={inspectCache}>Inspect Cache</button>\n      <pre>{JSON.stringify(debugInfo, null, 2)}</pre>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Performance Debugging Checklist",
        "items": [
          {
            "text": "Open React Query DevTools and scan for queries stuck in pending or error state",
            "hint": "A high error count usually indicates bad query keys or network issues."
          },
          {
            "text": "Record a React Profiler session and look for components rendering more than expected",
            "hint": "Focus on the render count column, not just total time."
          },
          {
            "text": "Check Network tab for duplicate requests to the same endpoint",
            "hint": "If the same queryKey fires twice, check for missing dependencies in useEffect or useQuery."
          },
          {
            "text": "Verify staleTime is set appropriately for your data freshness requirements",
            "hint": "A staleTime of 0 means every window focus refetches."
          },
          {
            "text": "Log query cache size in development to catch memory leaks",
            "hint": "A growing cache with no gcTime limit can exhaust browser memory."
          }
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Common Profiling Questions",
        "items": [
          {
            "question": "Why does my query refetch when I switch tabs?",
            "answer": "By default, React Query refetches when the window regains focus. Set refetchOnWindowFocus: false in query options if your data does not need real-time freshness."
          },
          {
            "question": "How do I find which component triggers a refetch?",
            "answer": "Use the Query Log in React Query DevTools. Each log entry includes the component stack trace showing which useQuery instance triggered the fetch."
          },
          {
            "question": "Is the DevTools panel available in production?",
            "answer": "Yes, but you should only ship it when explicitly needed. Wrap it in process.env.NODE_ENV === 'development' or a feature flag."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Debug a Slow Query Using DevTools",
        "description": "A product listing page loads slowly. Use React Query DevTools and React Profiler to identify the bottleneck.",
        "steps": [
          "Open React Query DevTools and identify the slow query",
          "Check if the query is stuck in pending or refetching unnecessarily",
          "Open React Profiler and record a render while the page loads",
          "Identify which components re-render more than expected",
          "Check Network tab for duplicate or slow requests",
          "Apply the fix and verify in DevTools that the query status improves"
        ],
        "expectedOutcome": "Identify the root cause (e.g., missing staleTime, duplicate query key) and resolve it."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "React Query DevTools provides real-time visibility into queries, mutations, and cache state",
          "React Profiler identifies unnecessary re-renders caused by unstable props",
          "Browser Network tab confirms actual HTTP behavior",
          "Programmatic cache inspection with getQueriesData helps validate key consistency"
        ],
        "takeaway": "Profiling is not a one-time task. Build a habit of checking DevTools during development to catch performance regressions early."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
