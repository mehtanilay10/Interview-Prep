{
  "id": "lesson-rq-029",
  "slug": "windowing-large-lists",
  "moduleSlug": "performance-optimization",
  "courseSlug": "react-query",
  "title": "Windowing Large Lists",
  "description": "Master windowing and virtualization techniques to render large datasets efficiently with React Query.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": ["windowing", "virtualization", "large-lists", "performance", "react-window"],
  "prerequisites": ["caching-strategies"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Rendering thousands of rows in a table or list kills performance because the DOM has to manage every node. **Windowing** (also called virtualization) renders only the visible subset of rows and reuses DOM nodes as the user scrolls. When combined with React Query, windowing keeps the data cached and ready while the UI stays buttery smooth."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Windowing Matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A list of 10,000 items means 10,000 DOM elements. Each element carries event listeners, styles, and memory overhead. Even with React's reconciliation, the initial paint and subsequent scroll events become janky. Windowing solves this by rendering only ~20-30 items at a time, regardless of total data size."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Do not window small lists",
        "text": "Windowing adds complexity. Only apply it when you expect more than 100-200 visible items. For smaller lists, standard mapping is faster and simpler."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Virtualization",
            "definition": "Rendering only the visible portion of a list, recycling DOM nodes as the user scrolls."
          },
          {
            "term": "Windowing",
            "definition": "A virtualization technique that renders a sliding window of items matching the viewport size."
          },
          {
            "term": "DOM Node Recycling",
            "definition": "Reusing existing DOM elements for new data items instead of creating and destroying them."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How Windowing Works"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-windowing-concept-029",
        "caption": "How windowing reduces DOM nodes from thousands to a small viewport window",
        "definition": "graph TB\n    A[10,000 Items in Cache] --> B[Windowing Library]\n    B --> C[Viewport Calculation]\n    C --> D[Only 30 DOM Nodes Rendered]\n    D --> E[User Scrolls]\n    E --> F[Recycle DOM Nodes]\n    F --> D\n    G[User Sees Smooth Scroll] --> D"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Integrating react-window with React Query"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Libraries like `react-window` and `react-virtuoso` handle the math of viewport calculation and DOM recycling. The key integration point is feeding the cached query data into the windowed list. React Query fetches and caches the full dataset; the windowing library handles rendering."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Fixed-size list with react-window",
        "content": "Rendering a large list of users using React Query data and react-window",
        "language": "tsx",
        "code": "import { useQuery } from '@tanstack/react-query';\nimport { FixedSizeList as List } from 'react-window';\n\nfunction UserList() {\n  const { data: users = [] } = useQuery({\n    queryKey: ['users'],\n    queryFn: fetchUsers\n  });\n\n  const Row = ({ index, style }: { index: number; style: React.CSSProperties }) => (\n    <div style={style} className=\"flex items-center border-b px-4 py-2\">\n      <img src={users[index].avatar} className=\"h-8 w-8 rounded-full\" />\n      <span className=\"ml-3\">{users[index].name}</span>\n    </div>\n  );\n\n  return (\n    <List\n      height={500}\n      itemCount={users.length}\n      itemSize={50}\n      width=\"100%\"\n    >\n      {Row}\n    </List>\n  );\n}"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Variable-size Lists"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Some lists have rows of varying height. `react-window` provides `VariableSizeList` for this scenario. You must measure or estimate row heights upfront. Alternatively, `react-virtuoso` auto-detects row sizes, making it a better choice for dynamic content like chat messages or cards with multiline text."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Windowing Libraries",
        "cards": [
          {
            "title": "react-window",
            "description": "Lightweight, battle-tested windowing library by Brian Vaughn.",
            "pros": ["Small bundle size (~7KB)", "Fixed and variable size lists", "Works with any data source"],
            "cons": ["Requires manual row height for variable lists", "Less feature-rich than Virtuoso"],
            "tags": ["lightweight", "flexible"]
          },
          {
            "title": "react-virtuoso",
            "description": "Feature-rich virtualization with automatic sizing and follow-output.",
            "pros": ["Auto-measures row heights", "Follow-output for chat UIs", "Grouping support built-in"],
            "cons": ["Larger bundle (~20KB)", "More opinionated API"],
            "tags": ["auto-size", "chat-friendly"]
          },
          {
            "title": "tanstack-virtual",
            "description": "Headless virtualizer from TanStack. Framework-agnostic core.",
            "pros": ["Framework agnostic", "Headless - full control over rendering", "Growing ecosystem"],
            "cons": ["Requires more setup", "Smaller community than react-window"],
            "tags": ["headless", "tanstack"]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Infinite Scroll with Virtualization"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you combine infinite queries with windowing, you get an infinite scroll that stays performant. React Query's `useInfiniteQuery` fetches pages; the windowing library renders only what is visible. The integration requires calculating total item count from `data.pages` and flattening the nested page structure."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Infinite scroll with react-window",
        "content": "Combining useInfiniteQuery with a virtualized list for smooth infinite scroll",
        "language": "tsx",
        "code": "function InfiniteUserList() {\n  const {\n    data,\n    fetchNextPage,\n    hasNextPage,\n    isFetchingNextPage\n  } = useInfiniteQuery({\n    queryKey: ['users', 'infinite'],\n    queryFn: ({ pageParam = 0 }) => fetchUsersPage(pageParam),\n    getNextPageParam: (lastPage, allPages) => lastPage.nextCursor\n  });\n\n  const allUsers = data?.pages.flatMap(page => page.users) ?? [];\n  const itemCount = hasNextPage ? allUsers.length + 1 : allUsers.length;\n\n  const Row = ({ index, style }: { index: number; style: React.CSSProperties }) => {\n    if (index === allUsers.length) {\n      return (\n        <div style={style} className=\"flex justify-center p-4\">\n          {isFetchingNextPage ? 'Loading...' : 'Load More'}\n        </div>\n      );\n    }\n    const user = allUsers[index];\n    return (\n      <div style={style} className=\"flex items-center border-b px-4 py-2\">\n        <img src={user.avatar} className=\"h-8 w-8 rounded-full\" />\n        <span className=\"ml-3\">{user.name}</span>\n      </div>\n    );\n  };\n\n  return (\n    <List\n      height={500}\n      itemCount={itemCount}\n      itemSize={50}\n      width=\"100%\"\n      onItemsRendered={({ visibleStopIndex }) => {\n        if (visibleStopIndex >= allUsers.length - 5 && hasNextPage) {\n          fetchNextPage();\n        }\n      }}\n    >\n      {Row}\n    </List>\n  );\n}"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Windowing Implementation Checklist",
        "items": [
          {
            "text": "Measure or estimate row heights before choosing a library",
            "hint": "Variable-size lists need accurate measurements for smooth scrolling."
          },
          {
            "text": "Keep React Query fetching logic separate from windowing rendering logic",
            "hint": "Query fetches pages; windowing renders what is visible."
          },
          {
            "text": "Flatten infinite query pages before passing to the list component",
            "hint": "useInfiniteQuery returns nested pages; windowing expects a flat array."
          },
          {
            "text": "Trigger fetchNextPage when the user is within 5-10 items of the end",
            "hint": "Fetching too early wastes requests; too late causes visible loading."
          },
          {
            "text": "Show a loading indicator at the bottom when fetching the next page",
            "hint": "Users need feedback that more content is coming."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Windowing renders only visible items, not the full dataset",
          "Pair react-window or react-virtuoso with useQuery or useInfiniteQuery",
          "Flatten infinite query pages before feeding them to the list",
          "Choose react-virtuoso when row heights are dynamic"
        ],
        "takeaway": "Windowing keeps large lists performant by trading DOM nodes for scroll math."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
