{
  "id": "lesson-rq-028",
  "slug": "selective-fetching",
  "moduleSlug": "performance-optimization",
  "courseSlug": "react-query",
  "title": "Selective Fetching",
  "description": "Learn selective fetching strategies to reduce unnecessary network requests and improve application performance.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "selective-fetching",
    "performance",
    "network-optimization"
  ],
  "prerequisites": [
    "caching-strategies"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React applications often request more data than they actually need. **Selective fetching** lets you transform server responses on the client, fetching only the fields you require while keeping the full payload cached for other consumers. This reduces bandwidth, speeds up renders, and prevents your UI from being coupled to your API shape."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Problem with Over-Fetching"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Imagine an API endpoint that returns a 50-field user object. Your component only needs `name` and `avatar`. Without selective fetching, every consumer receives the full payload, parses it, and stores it in memory. Over time, this wastes bandwidth on slow networks and increases memory pressure in long-running single-page applications."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Hidden Cost of Raw Payloads",
        "text": "Every unnecessary field you transfer slows down initial page load, increases JSON parse time, and bloats the Query cache. On mobile networks, this difference is measurable."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Without Selective Fetching",
        "content": "Fetching the entire payload and discarding most of it in the UI.",
        "language": "tsx",
        "code": "function UserAvatar({ userId }: { userId: string }) {\n  const { data } = useQuery({\n    queryKey: ['user', userId],\n    queryFn: () => fetch(`/api/users/${userId}`).then(res => res.json())\n  });\n\n  // We only need name and avatar, but the cache holds 50 fields\n  const user = data;\n\n  return (\n    <img\n      src={user.avatar}\n      alt={user.name}\n      className=\"h-10 w-10 rounded-full\"\n    />\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "With Selective Fetching",
        "content": "Using the select option to extract only the fields your component needs.",
        "language": "tsx",
        "code": "function UserAvatar({ userId }: { userId: string }) {\n  const { data } = useQuery({\n    queryKey: ['user', userId],\n    queryFn: () => fetch(`/api/users/${userId}`).then(res => res.json()),\n    select: (user) => ({\n      name: user.name,\n      avatar: user.avatar\n    })\n  });\n\n  return (\n    <img\n      src={data.avatar}\n      alt={data.name}\n      className=\"h-10 w-10 rounded-full\"\n    />\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How the select Option Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `select` option receives the full server response and returns a transformed subset. React Query caches the **original** response under the query key, so other components using the same key still see the full data. Your component receives only the selected fields, making re-renders cheaper because the returned object is smaller and more stable."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Benefits of Selective Fetching",
        "items": [
          "Reduced payload size per component without changing the API",
          "Smaller transformed objects lead to faster equality checks in React.memo",
          "Full server data stays cached for other components that need more fields",
          "Decouples UI shape from API response shape",
          "Improves perceived performance on slow networks"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "select",
            "definition": "A query option that transforms the server response before it reaches your component."
          },
          {
            "term": "Structural Sharing",
            "definition": "React Query compares previous and new data by reference. Transforming data with select can improve sharing by returning stable object shapes."
          },
          {
            "term": "Over-fetching",
            "definition": "Downloading more data from the server than the current view actually needs."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-selective-fetching-028",
        "caption": "Data flow with selective fetching in React Query",
        "definition": "graph LR\n    A[API Response 50 Fields] --> B[Query Cache<br/>Full Payload]\n    B --> C{Component A needs 2 fields?}\n    B --> D{Component B needs 10 fields?}\n    C -->|Yes| E[select extracts 2 fields]\n    D -->|Yes| F[select extracts 10 fields]\n    E --> G[Smaller render object]\n    F --> H[Medium render object]\n    G --> I[Faster reconciliation]\n    H --> I"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dynamic select with useMemo",
        "content": "When the select logic depends on props, memoize it to avoid unnecessary recalculations.",
        "language": "tsx",
        "code": "import { useMemo } from 'react';\nimport { useQuery } from '@tanstack/react-query';\n\nfunction UserCard({ userId, showEmail }: { userId: string; showEmail: boolean }) {\n  const selectUser = useMemo(() => (user: User) => ({\n    name: user.name,\n    avatar: user.avatar,\n    ...(showEmail && { email: user.email })\n  }), [showEmail]);\n\n  const { data } = useQuery({\n    queryKey: ['user', userId],\n    queryFn: fetchUser,\n    select: selectUser\n  });\n\n  return (\n    <div>\n      <h2>{data.name}</h2>\n      <img src={data.avatar} alt={data.name} />\n      {showEmail && <p>{data.email}</p>}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Select Strategies",
        "cards": [
          {
            "title": "Inline select",
            "description": "Define the select function directly in the useQuery options.",
            "pros": [
              "Simple and readable",
              "No extra hooks needed"
            ],
            "cons": [
              "Recreated on every render",
              "Can break structural sharing"
            ],
            "tags": [
              "simple"
            ]
          },
          {
            "title": "Memoized select with useMemo",
            "description": "Wrap select in useMemo when it depends on props or state.",
            "pros": [
              "Stable reference across renders",
              "Works with dynamic dependencies"
            ],
            "cons": [
              "Requires useMemo import",
              "Slightly more code"
            ],
            "tags": [
              "dynamic",
              "recommended"
            ]
          },
          {
            "title": "Factory function",
            "description": "Create a reusable select function that takes configuration.",
            "pros": [
              "Reusable across components",
              "Encapsulates selection logic"
            ],
            "cons": [
              "Additional abstraction layer",
              "Harder to debug inline"
            ],
            "tags": [
              "reusable"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Combine select with memoization",
        "text": "Wrap the select function in useMemo when it depends on props or external values. This prevents React Query from re-running the selector on every render."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Apply Selective Fetching to a Table Component",
        "description": "You have a UserTable that displays name, email, and role. The API returns 30 fields. Apply selective fetching to extract only what the table needs.",
        "steps": [
          "Create a select function that extracts name, email, and role from the full user object",
          "Memoize the select function with useMemo if it depends on visible columns state",
          "Apply the select option to the useQuery call in UserTable",
          "Add a second component (UserAvatar) that uses the same query key but only needs name and avatar",
          "Verify both components receive correctly shaped data without duplicating fetches"
        ],
        "expectedOutcome": "Two components share one cached fetch but receive different data shapes via select."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use select to transform server responses without changing the API",
          "The full payload stays cached; only the component receives the subset",
          "Smaller objects mean faster React reconciliation",
          "Memoize select functions that depend on props or state",
          "Structural sharing works alongside select for optimal cache behavior"
        ],
        "takeaway": "Selective fetching decouples your UI from your API while keeping the network layer efficient."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
