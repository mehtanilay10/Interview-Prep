{
  "id": "lesson-rq-030",
  "slug": "memoization-strategies",
  "moduleSlug": "performance-optimization",
  "courseSlug": "react-query",
  "title": "Memoization Strategies",
  "description": "Apply memoization techniques to prevent redundant React Query executions and unnecessary re-renders.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": ["memoization", "performance", "useMemo", "useCallback", "structural-sharing"],
  "prerequisites": ["caching-strategies"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React Query does an impressive amount of memoization internally: it deduplicates requests, caches by reference, and uses structural sharing to avoid re-rendering when data has not changed. But in complex UIs, you still need to apply **memoization strategies** at the component and hook level to prevent expensive re-renders and redundant computations."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Query Built-in Memoization"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React Query memoizes query results by default. When a query resolves, the result is cached. If another component requests the same query key, React Query returns the cached reference. If a background refetch returns identical data, structural sharing ensures React sees the same object reference and skips re-rendering."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Structural Sharing by Default",
        "text": "React Query compares each key in the new response with the old one. If a value has not changed by reference, it reuses the old reference. This is why updating one field in a large object does not re-render every consumer."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-structural-sharing-030",
        "caption": "How React Query structural sharing prevents unnecessary re-renders",
        "definition": "graph LR\n    A[Old Cache Object] --> B{Compare keys by reference}\n    C[New Fetch Response] --> B\n    B -->|Same reference| D[Reuse old object]\n    B -->|Changed reference| E[Create new cached object]\n    D --> F[No re-render]\n    E --> G[Re-render only changed consumers]"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Memoizing Derived Data with useMemo"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you derive data from a query â€” sorting a list, filtering items, or computing aggregates â€” the derivation runs on every render by default. Wrap expensive derivations in `useMemo` so they only recalculate when the underlying query data changes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Memoizing filtered and sorted lists",
        "content": "Using useMemo to prevent expensive list transformations on every render",
        "language": "tsx",
        "code": "import { useMemo, useState } from 'react';\nimport { useQuery } from '@tanstack/react-query';\n\nfunction UserTable() {\n  const { data: users = [] } = useQuery({\n    queryKey: ['users'],\n    queryFn: fetchUsers\n  });\n\n  const [search, setSearch] = useState('');\n\n  const visibleUsers = useMemo(() => {\n    return users\n      .filter(user => user.name.toLowerCase().includes(search.toLowerCase()))\n      .sort((a, b) => a.name.localeCompare(b.name));\n  }, [users, search]);\n\n  return (\n    <div>\n      <input value={search} onChange={(e) => setSearch(e.target.value)} />\n      <table>\n        <tbody>\n          {visibleUsers.map(user => (\n            <tr key={user.id}>\n              <td>{user.name}</td>\n              <td>{user.email}</td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Memoizing Callbacks with useCallback"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Passing inline functions to memoized child components breaks their memoization because the function reference changes on every render. Use `useCallback` for event handlers and query functions that depend on props or state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Stable query keys with useCallback",
        "content": "Generating dynamic query keys without breaking referential equality",
        "language": "tsx",
        "code": "import { useCallback } from 'react';\nimport { useQuery, useQueryClient } from '@tanstack/react-query';\n\nfunction UserDashboard({ filters }: { filters: Filters }) {\n  const queryClient = useQueryClient();\n\n  const getQueryKey = useCallback(() => ['users', filters], [filters]);\n\n  const { data } = useQuery({\n    queryKey: getQueryKey(),\n    queryFn: () => fetchUsers(filters)\n  });\n\n  return (\n    <UserGrid\n      users={data}\n      onRefresh={() => queryClient.invalidateQueries(getQueryKey())}\n    />\n  );\n}"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When Memoization Helps vs Hurts"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Memoization Decision Guide",
        "items": [
          "Use useMemo for filtering, sorting, or mapping large arrays",
          "Use useCallback for handlers passed to memoized children (React.memo)",
          "Avoid memoizing simple values like strings or booleans",
          "Avoid wrapping everything in useMemo/useCallback by default",
          "Profile first to confirm a performance problem exists"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "useMemo",
            "definition": "React hook that caches the result of an expensive computation between renders."
          },
          {
            "term": "useCallback",
            "definition": "React hook that returns a memoized callback reference."
          },
          {
            "term": "Structural Sharing",
            "definition": "React Query compares old and new data by reference. Unchanged fields keep their old reference, preventing re-renders."
          },
          {
            "term": "Referential Equality",
            "definition": "Two values are referentially equal when they point to the same object in memory. React uses this to decide whether to re-render."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Optimize a Dashboard with Memoization",
        "description": "You have a dashboard with three widgets that all consume the same query. When the user types in a search box, the entire dashboard re-renders. Apply memoization so only the relevant widget updates.",
        "steps": [
          "Identify the expensive computations in each widget (filtering, sorting, formatting)",
          "Wrap each computation in useMemo with the correct dependency array",
          "Wrap the refresh callback in useCallback so the QueryClient reference is stable",
          "Verify with React DevTools Profiler that widgets outside the search scope do not re-render"
        ],
        "expectedOutcome": "Only the search widget re-renders on keystroke. Other widgets remain stable."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Profile before you memoize",
        "text": "Use React DevTools Profiler to confirm a component is re-rendering unnecessarily. Premature memoization adds complexity without measurable benefit."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "React Query structural sharing prevents re-renders when data is unchanged",
          "Use useMemo for expensive derivations like filtering and sorting",
          "Use useCallback for event handlers passed to memoized children",
          "Profile first to ensure memoization solves a real problem"
        ],
        "takeaway": "Memoization is most effective when applied deliberately to known bottlenecks, not everywhere by default."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
