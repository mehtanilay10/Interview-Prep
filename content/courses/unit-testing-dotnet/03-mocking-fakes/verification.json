{
  "id": "lesson-ut-012",
  "slug": "verification",
  "moduleSlug": "mocking-fakes",
  "title": "Verification",
  "description": "Verify that methods were called with expected parameters.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "verification",
    "mocking",
    "dotnet"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Verification** checks that specific methods were called during a test. This ensures your code interacts correctly with its dependencies."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Verification Examples",
        "content": "var mock = new Mock<IEmailService>();\nvar service = new OrderService(mock.Object);\n\nservice.ProcessOrder(new Order());\n\n// Verify method was called\nmock.Verify(x => x.Send(It.IsAny<string>()));\n\n// Verify called specific number of times\nmock.Verify(x => x.Send(It.IsAny<string>()), Times.Once);\nmock.Verify(x => x.Send(It.IsAny<string>()), Times.Exactly(3));\n\n// Verify with specific parameters\nmock.Verify(x => x.Send(\"test@example.com\"));\n\n// Verify with condition\nmock.Verify(x => x.Send(It.Is<string>(s => s.Contains(\"@\"))));",
        "language": "csharp"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Verify checks method calls",
          "Times specifies expected call count",
          "It.Is and It.IsAny for parameter matching"
        ],
        "takeaway": "Use verification to ensure your code interacts correctly with dependencies."
      }
    }
  ],
  "relatedLessons": [
    "stubs-fakes"
  ],
  "furtherReading": [],
  "courseSlug": "unit-testing-dotnet"
}