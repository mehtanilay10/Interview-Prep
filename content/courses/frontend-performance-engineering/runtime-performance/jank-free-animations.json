{
  "id": "lesson-frontend-performance-engineering-runtime-performance-jank-free-animations",
  "slug": "jank-free-animations",
  "moduleSlug": "runtime-performance",
  "courseSlug": "frontend-performance-engineering",
  "title": "Jank-Free Animations",
  "description": "Learn how to build animations that maintain 60fps by respecting the frame budget, avoiding main-thread work, and choosing the right animation APIs.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "animation",
    "fps",
    "rAF",
    "css-transitions",
    "accessibility"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Jank is the perception of stutter or dropped frames during animation or scrolling. At 60Hz displays the browser must render a frame every 16.67 ms. If JavaScript, layout, paint, or composite take longer than that, frames are dropped and the user notices."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Frame Budget"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every frame has a strict time budget. Understanding where that time goes helps you identify which work to remove or move off the main thread. Long tasks, layout thrashing, and heavy paint are the biggest frame budget killers."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Frame Budget Breakdown",
        "content": "Typical frame budget allocation at 60fps.",
        "language": "text",
        "code": "Frame Budget at 60Hz (16.67 ms per frame):\n\nJavaScript (app logic + rAF callbacks):   ~4 ms\nStyle recalculation:                        ~1 ms\nLayout (if triggered):                     ~3-8 ms\nPaint (if triggered):                      ~2-6 ms\nComposite (always):                        ~1 ms\n\nIf JavaScript alone exceeds ~10 ms, the frame is missed.\nCommon jank causes:\n- Long tasks (>50 ms)\n- Layout thrashing inside rAF\n- Expensive paint (large shadows, blur)\n- Synchronous XHR or heavy event handlers\n- Garbage collection during animation"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "CSS Transitions and Animations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "CSS transitions and keyframe animations are the cheapest way to animate because browsers can run them on the compositor thread. As long as you animate only transform and opacity, the main thread is free for JavaScript work."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Performant CSS Transitions",
        "content": "Use CSS transitions for smooth, GPU-friendly state changes.",
        "language": "css",
        "code": "/* Bad: animates layout properties */\n.bad {\n  transition: width 0.3s, height 0.3s, top 0.3s;\n}\n/* Triggers layout + paint + composite every frame */\n\n/* Good: animates compositor-only properties */\n.good {\n  transition: transform 0.3s ease-out, opacity 0.3s ease-out;\n}\n/* Only composite → smooth on low-end devices */\n\n.modal-backdrop {\n  opacity: 0;\n  transition: opacity 0.25s ease-out;\n}\n.modal-backdrop.open {\n  opacity: 1;\n}\n\n.tooltip {\n  transform: translateY(8px);\n  opacity: 0;\n  transition: transform 0.2s ease-out, opacity 0.2s ease-out;\n}\n.tooltip.visible {\n  transform: translateY(0);\n  opacity: 1;\n}\n\n/* Tip: add will-change only when animating */\n.animated {\n  will-change: transform, opacity;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "requestAnimationFrame for JavaScript Animations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For complex, interactive, or state-driven animations, use requestAnimationFrame. It synchronizes your callback with the browser's paint cycle, so you never write a DOM change that the browser cannot use."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Smooth rAF Animation",
        "content": "Build a custom animation loop with requestAnimationFrame.",
        "language": "javascript",
        "code": "function animateBounce(element, duration = 1000) {\n  const start = performance.now();\n  const startY = 0;\n  const distance = 200;\n\n  function step(now) {\n    const elapsed = now - start;\n    const progress = Math.min(elapsed / duration, 1);\n\n    // Ease-out cubic for natural feel\n    const eased = 1 - Math.pow(1 - progress, 3);\n\n    const currentY = startY + distance * eased;\n    element.style.transform = `translateY(${currentY}px)`;\n\n    if (progress < 1) {\n      requestAnimationFrame(step);\n    }\n  }\n\n  requestAnimationFrame(step);\n}\n\nconst box = document.querySelector('.box');\nanimateBounce(box, 800);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Interactive and Gesture Animations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When animating in response to user input — dragging, scrolling, swiping — keep the animation loop as thin as possible. Read input values, compute the next frame, and write only transform or opacity."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Drag Gesture with rAF",
        "content": "Keep drag animations inside the frame budget.",
        "language": "javascript",
        "code": "let currentX = 0;\nlet targetX = 0;\nlet rafId = null;\nconst slider = document.querySelector('.slider');\n\nfunction onPointerMove(e) {\n  // Update target from input — very fast\n  targetX = e.clientX - 100;\n\n  // Schedule animation frame only if needed\n  if (!rafId) {\n    rafId = requestAnimationFrame(updateSlider);\n  }\n}\n\nfunction updateSlider() {\n  rafId = null;\n\n  // Simple lerp toward target\n  currentX += (targetX - currentX) * 0.2;\n\n  // Single compositor-only write\n  slider.style.transform = `translateX(${currentX}px)`;\n\n  // Continue if not settled\n  if (Math.abs(targetX - currentX) > 0.1) {\n    rafId = requestAnimationFrame(updateSlider);\n  }\n}\n\nslider.addEventListener('pointermove', onPointerMove);\n\n// Key principle: read input, write transform, stop"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Animation Libraries"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Framer Motion, React Spring, and Motion One all default to transform and opacity, use rAF internally, and batch work to stay inside the frame budget. Prefer them over manual style manipulation for declarative animations."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Framer Motion List Animation",
        "content": "Animate list items with layout animations.",
        "language": "javascript",
        "code": "import { motion, AnimatePresence } from 'framer-motion';\n\nfunction TodoList({ items }) {\n  return (\n    <AnimatePresence>\n      {items.map(item => (\n        <motion.li\n          key={item.id}\n          initial={{ opacity: 0, y: 10 }}\n          animate={{ opacity: 1, y: 0 }}\n          exit={{ opacity: 0, scale: 0.95 }}\n          transition={{ duration: 0.2 }}\n          layout // enables smooth reordering without manual rAF\n        >\n          {item.text}\n        </motion.li>\n      ))}\n    </AnimatePresence>\n  );\n}\n\n// 'layout' prop animates position changes\n// automatically using transform + opacity"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accessibility: Respect Reduced Motion"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Some users prefer reduced motion due to vestibular disorders or personal preference. Use the prefers-reduced-motion media query to detect this and disable or simplify animations."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reduced Motion Support",
        "content": "Respect user motion preferences.",
        "language": "css",
        "code": "/* Default: smooth animation */\n.modal {\n  opacity: 0;\n  transform: scale(0.95);\n  transition: opacity 0.25s ease-out, transform 0.25s ease-out;\n}\n.modal.open {\n  opacity: 1;\n  transform: scale(1);\n}\n\n/* Reduced motion: instant state change */\n@media (prefers-reduced-motion: reduce) {\n  *,\n  *::before,\n  *::after {\n    animation-duration: 0.01ms !important;\n    animation-iteration-count: 1 !important;\n    transition-duration: 0.01ms !important;\n    scroll-behavior: auto !important;\n  }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Animation Jank in Production",
        "text": "Animation performance varies across devices. A smooth 60fps animation on a desktop may jank on a mid-range phone. Always test on real low-end hardware and use Chrome DevTools CPU throttling to simulate slower devices during development."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Frame Budget",
            "definition": "The time available to render one frame; 16.67 ms at 60Hz."
          },
          {
            "term": "requestAnimationFrame",
            "definition": "Browser API that schedules callbacks before the next paint, aligning JavaScript with the rendering cycle."
          },
          {
            "term": "prefers-reduced-motion",
            "definition": "A media query that indicates whether the user has requested fewer or no animations."
          }
        ]
      }
    },
    {
      "type": "relatedLessons",
      "data": {
        "lessons": [
          "painting-and-compositing",
          "layout-thrashing",
          "user-centric-metrics"
        ]
      }
    },
    {
      "type": "furtherReading",
      "data": {
        "items": [
          {
            "title": "High Performance Animations",
            "url": "https://web.dev/animations-guide/",
            "type": "article",
            "description": "Google's guide to compositor-thread animations and avoiding jank."
          },
          {
            "title": "Web Animations API",
            "url": "https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API",
            "type": "article",
            "description": "Native browser API for performant, scriptable animations."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "60fps requires each frame to finish within 16.67 ms",
          "Animating transform and opacity is compositor-only and safe for 60fps",
          "Animating layout properties forces layout + paint every frame",
          "requestAnimationFrame synchronizes JavaScript with the paint cycle",
          "Keep rAF callbacks small: read input, compute, write transform",
          "Respect prefers-reduced-motion for accessibility",
          "Test animations on real low-end devices and with CPU throttling"
        ],
        "takeaway": "Jank-free animations are built by choosing the right properties, batching DOM work, and always staying within the frame budget. Compositor-only properties and requestAnimationFrame are your primary tools."
      }
    }
  ],
  "relatedLessons": [
    "painting-and-compositing",
    "layout-thrashing",
    "user-centric-metrics"
  ],
  "furtherReading": [
    {
      "title": "High Performance Animations",
      "url": "https://web.dev/animations-guide/",
      "type": "article",
      "description": "Google's guide to compositor-thread animations and avoiding jank."
    },
    {
      "title": "Web Animations API",
      "url": "https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API",
      "type": "article",
      "description": "Native browser API for performant, scriptable animations."
    }
  ]
}
