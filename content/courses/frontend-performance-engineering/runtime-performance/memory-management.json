{
  "id": "lesson-frontend-performance-engineering-runtime-performance-memory-management",
  "slug": "memory-management",
  "moduleSlug": "runtime-performance",
  "courseSlug": "frontend-performance-engineering",
  "title": "Memory Management and Leak Prevention",
  "description": "Understand JavaScript garbage collection, identify common memory leak patterns, and apply memory-efficient coding practices in browser applications.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "gc",
    "memory-leaks",
    "heap",
    "weakref",
    "profiling"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "JavaScript garbage collection automatically reclaims memory from objects that are no longer reachable. But automatic does not mean free: leaks still happen, and memory pressure causes slow garbage-collection pauses, tab crashes, and degraded performance on low-memory devices."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Stack vs Heap Memory"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Primitives like numbers and booleans live on the stack, while objects, arrays, and functions live on the heap. The stack is fast and bounded; the heap is large but slower. Most performance problems come from how the heap is used, not the stack."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Memory Allocation in Practice",
        "content": "Understanding where JavaScript values are stored.",
        "language": "javascript",
        "code": "// Stack values (fast, limited)\nlet count = 42;\nlet active = true;\nlet name = 'Frontend';\n\n// Heap values (slower allocation, larger capacity)\nlet user = {\n  id: 1,\n  name: 'Alice',\n  preferences: { theme: 'dark' }\n}; // object on heap\n\nlet history = [];\nfor (let i = 0; i < 100_000; i++) {\n  history.push({ step: i }); // 100k objects on heap\n}\n\n// Releasing references\nuser = null; // user object becomes eligible for GC\nhistory = null; // entire array becomes eligible"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Garbage Collection Algorithms"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Modern engines use generational, incremental, and concurrent garbage collection. New objects go in the young generation and are collected frequently because most objects die young. Survivors are promoted to the old generation, where collection is slower but less frequent."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generational GC Overview",
        "content": "How modern engines organize and collect memory.",
        "language": "text",
        "code": "Generational Garbage Collection:\n\nYoung Generation (Nursery):\n- New objects allocated here\n- Frequent, fast minor GC\n- Most objects die here (generational hypothesis)\n\nOld Generation:\n- Objects that survived young-gen GC\n- Infrequent, expensive major GC\n- Contains long-lived caches, closures, DOM wrappers\n\nScavenge (Minor GC):\n- Copies live objects from young to young\n- Reclaims memory by wiping the from-space\n- Very fast (milliseconds)\n\nMark-and-Sweep (Major GC):\n- Marks all reachable objects from roots\n- Sweeps unmarked objects\n- May require compaction to reduce fragmentation\n\nConcurrent Marking:\n- Marks reachable objects on a background thread\n- Reduces main-thread pause time\n- Available in modern V8"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Memory Leak Patterns"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The most common leaks in web apps are accidental global references, uncleared timers, unreleased event listeners, closures capturing large objects, and detached DOM nodes kept alive by JavaScript references."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Leak Patterns and Fixes",
        "content": "Identify and fix common memory leaks.",
        "language": "javascript",
        "code": "// LEAK 1: Accidental global variable\nfunction init() {\n  cache = {}; // missing 'const' → global\n}\n\n// FIX: always declare variables\nfunction init() {\n  const cache = {};\n}\n\n// LEAK 2: Uncleared interval\nconst id = setInterval(() => check(), 1000);\n// Component unmounts → interval keeps running\n\n// FIX: clear on cleanup\nuseEffect(() => {\n  const id = setInterval(() => check(), 1000);\n  return () => clearInterval(id);\n}, []);\n\n// LEAK 3: Event listener on removed element\nconst btn = document.getElementById('btn');\nbtn.addEventListener('click', handler);\n// Later: btn.remove() — listener keeps btn in memory\n\n// FIX: remove listener before removing element\nbtn.removeEventListener('click', handler);\nbtn.remove();\n\n// LEAK 4: Closure over large object\nfunction createHandler() {\n  const hugeData = new Array(1_000_000).fill('x');\n  return () => console.log('still alive');\n}\n\n// FIX: extract only what is needed\nfunction createHandler() {\n  const count = 1_000_000;\n  return () => console.log(count);\n}\n\n// LEAK 5: Detached DOM node held by JS\nlet removed = null;\nfunction removeNode() {\n  const node = document.querySelector('.box');\n  removed = node; // JS keeps reference\n  node.remove(); // DOM removed, but JS reference prevents GC\n}\n\n// FIX: clear reference\nremoved = null;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Memory-Efficient Patterns"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Object pooling, typed arrays, and WeakMap/WeakSet caching reduce allocation pressure. For numeric-heavy workloads, typed arrays use contiguous memory and avoid boxing overhead."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Object Pool and Typed Arrays",
        "content": "Reduce allocation pressure in hot paths.",
        "language": "javascript",
        "code": "// Object pool for particles or frequent allocations\nclass Pool {\n  constructor(factory, reset, size = 50) {\n    this.factory = factory;\n    this.reset = reset;\n    this.items = Array.from({ length: size }, factory);\n  }\n  acquire() {\n    return this.items.length ? this.items.pop() : this.factory();\n  }\n  release(item) {\n    this.reset(item);\n    this.items.push(item);\n  }\n}\n\nconst particlePool = new Pool(\n  () => ({ x: 0, y: 0, vx: 0, vy: 0 }),\n  (p) => { p.x = 0; p.y = 0; p.vx = 0; p.vy = 0; }\n);\n\n// Typed arrays for numeric data\nfunction processSamples(samples) {\n  const out = new Float32Array(samples.length);\n  for (let i = 0; i < samples.length; i++) {\n    out[i] = samples[i] * 1.5;\n  }\n  return out;\n}\n\n// WeakMap cache for DOM metadata\nconst meta = new WeakMap();\nfunction setMeta(el, data) { meta.set(el, data); }\nfunction getMeta(el) { return meta.get(el); }\n// When element is removed from DOM, cache entry is GC'd automatically"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Monitoring Memory in DevTools"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use Chrome DevTools Memory panel to detect leaks. Take heap snapshots before and after an action. If the heap grows unexpectedly and retained size stays high, you likely have a leak. The Allocation Instrumentation timeline shows allocation stacks in real time."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "DevTools Memory Workflow",
        "content": "Systematic approach to finding memory leaks.",
        "language": "text",
        "code": "Memory leak investigation workflow:\n\n1. Open DevTools > Memory\n2. Select 'Heap snapshot' and click 'Take snapshot'\n3. Perform the suspected leak action (open/close, add/remove)\n4. Take another snapshot\n5. Compare snapshots\n\nWhat to look for:\n- Objects with increasing 'Distance' from GC roots\n- Detached DOM nodes growing between snapshots\n- Closures retaining large arrays or strings\n- Event listeners or timers not released\n\nAllocation timeline:\n- Record allocation over time\n- Interact with the app\n- Look for allocations that never get reclaimed"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Browsers Enforce Memory Limits",
        "text": "Most browsers enforce per-tab memory limits (often 1–4 GB on desktop, much less on mobile). Exceeding these limits crashes the tab. Long-running SPAs, image galleries, and offline apps are especially prone to leaks; build cleanup routines and test navigation cycles."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Generational GC",
            "definition": "A garbage collection strategy that divides objects into young and old generations and collects them at different frequencies."
          },
          {
            "term": "Detached DOM",
            "definition": "A DOM node removed from the document but still referenced by JavaScript, preventing garbage collection."
          },
          {
            "term": "WeakMap",
            "definition": "A map that does not prevent its keys from being garbage collected, useful for metadata caches."
          }
        ]
      }
    },
    {
      "type": "relatedLessons",
      "data": {
        "lessons": [
          "cpu-profiling",
          "js-engine-optimization",
          "debugging-performance"
        ]
      }
    },
    {
      "type": "furtherReading",
      "data": {
        "items": [
          {
            "title": "Memory Management on MDN",
            "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Memory_Management",
            "type": "article",
            "description": "Foundation concepts of JavaScript memory and garbage collection."
          },
          {
            "title": "Chrome DevTools Memory Panel",
            "url": "https://developer.chrome.com/blog/devtools-blog-archive-2023#memory",
            "type": "article",
            "description": "How to use heap snapshots and allocation timelines."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "JavaScript uses stack for primitives and heap for objects",
          "Generational GC collects young objects frequently and old objects rarely",
          "Common leaks: globals, timers, listeners, closures, detached DOM",
          "Clear timers and listeners on unmount or cleanup",
          "Use object pools for high-frequency allocations",
          "Use typed arrays for numeric-heavy workloads",
          "Use WeakMap/WeakSet for caches that should not prevent GC",
          "Detect leaks with heap snapshots in DevTools"
        ],
        "takeaway": "Memory leaks are not mysterious: they are references that live too long. Audit cleanup paths, use heap snapshots regularly, and keep long-lived references minimal."
      }
    }
  ],
  "relatedLessons": [
    "cpu-profiling",
    "js-engine-optimization",
    "debugging-performance"
  ],
  "furtherReading": [
    {
      "title": "Memory Management on MDN",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Memory_Management",
      "type": "article",
      "description": "Foundation concepts of JavaScript memory and garbage collection."
    },
    {
      "title": "Chrome DevTools Memory Panel",
      "url": "https://developer.chrome.com/blog/devtools-blog-archive-2023#memory",
      "type": "article",
      "description": "How to use heap snapshots and allocation timelines."
    }
  ]
}
