{
  "id": "lesson-frontend-performance-engineering-runtime-performance-performance-markers",
  "slug": "performance-markers",
  "moduleSlug": "runtime-performance",
  "courseSlug": "frontend-performance-engineering",
  "title": "Performance Markers and the User Timing API",
  "description": "Learn how to use the User Timing API, PerformanceObserver, and custom marks to measure and instrument real user interactions.",
  "order": 7,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [
    "performance-api",
    "user-timing",
    "marks",
    "measures",
    "instrumentation"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The User Timing API lets developers place markers and measures in their code to instrument real user flows. These markers appear in the DevTools Performance panel and can be aggregated to understand exactly where time is spent in complex interactions."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "marks() and measure()"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "performance.mark() records a named timestamp. performance.measure() computes the duration between two marks or between a mark and the navigation start. Use them to measure loading, rendering, and interaction milestones."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "User Timing API Examples",
        "content": "Instrument a checkout flow with marks and measures.",
        "language": "javascript",
        "code": "// Mark key milestones\nperformance.mark('checkout-start');\n\n// After data fetch\nawait fetch('/api/checkout');\nperformance.mark('checkout-data-ready');\n\n// After render\nrequestAnimationFrame(() => {\n  performance.mark('checkout-rendered');\n  \n  // Measure from start to render\n  performance.measure(\n    'checkout-render',\n    'checkout-start',\n    'checkout-rendered'\n  );\n  \n  // Measure data fetch duration\n  performance.measure(\n    'checkout-fetch',\n    'checkout-start',\n    'checkout-data-ready'\n  );\n});\n\n// Read results\nconst measures = performance.getEntriesByType('measure');\nfor (const m of measures) {\n  console.log(`${m.name}: ${m.duration.toFixed(2)}ms`);\n}\n\n// Clear when done\nperformance.clearMarks();\nperformance.clearMeasures();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "PerformanceObserver for Real-Time Data"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The PerformanceObserver API lets you observe entries as they are recorded. Use it to watch for long tasks, largest contentful paints, layout shifts, and custom marks in real time without polling."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "PerformanceObserver for Long Tasks and LCP",
        "content": "Observe runtime performance events as they happen.",
        "language": "javascript",
        "code": "// Observe long tasks\nconst longTaskObserver = new PerformanceObserver((list) => {\n  for (const entry of list.getEntries()) {\n    console.warn('Long task detected:', entry.duration, 'ms');\n    // Send to analytics\n    sendToAnalytics({ type: 'longtask', duration: entry.duration });\n  }\n});\nlongTaskObserver.observe({ type: 'longtask', buffered: true });\n\n// Observe LCP\nconst lcpObserver = new PerformanceObserver((list) => {\n  const entries = list.getEntries();\n  const lastEntry = entries[entries.length - 1];\n  console.log('LCP:', lastEntry.renderTime || lastEntry.loadTime);\n});\nlcpObserver.observe({ type: 'largest-contentful-paint', buffered: true });\n\n// Observe custom marks\nconst markObserver = new PerformanceObserver((list) => {\n  for (const entry of list.getEntries()) {\n    console.log('Mark:', entry.name, 'at', entry.startTime);\n  }\n});\nmarkObserver.observe({ type: 'mark', buffered: true });\n\n// Observe layout shifts\nconst clsObserver = new PerformanceObserver((list) => {\n  for (const entry of list.getEntries()) {\n    if (!entry.hadRecentInput) {\n      console.log('CLS candidate:', entry.value);\n    }\n  }\n});\nclsObserver.observe({ type: 'layout-shift', buffered: true });"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Navigation Timing and Resource Timing"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The Navigation Timing API provides high-resolution timestamps for every stage of page load. The Resource Timing API does the same for individual assets. Together they let you measure exactly where load time is spent: DNS, TCP, request, response, or DOM processing."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Navigation Timing Breakdown",
        "content": "Inspect page load timing.",
        "language": "javascript",
        "code": "const nav = performance.getEntriesByType('navigation')[0];\n\nconsole.log('DNS lookup:', nav.domainLookupEnd - nav.domainLookupStart);\nconsole.log('TCP connection:', nav.connectEnd - nav.connectStart);\nconsole.log('TLS negotiation:', nav.connectEnd - nav.secureConnectionStart);\nconsole.log('Request sent:', nav.responseStart - nav.requestStart);\nconsole.log('TTFB:', nav.responseStart - nav.requestStart);\nconsole.log('Response download:', nav.responseEnd - nav.responseStart);\nconsole.log('DOM processing:', nav.domInteractive - nav.responseEnd);\nconsole.log('DOM complete:', nav.domContentLoadedEventEnd - nav.domContentLoadedEventStart);\nconsole.log('Full load:', nav.loadEventEnd - nav.loadEventStart);\n\n// Resource timing\nconst resources = performance.getEntriesByType('resource');\nfor (const r of resources) {\n  console.log(r.name, 'duration:', r.duration.toFixed(2));\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Instrumenting React Interactions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Place marks around key user flows in React: clicks, form submissions, and navigation transitions. Measure from interaction start to paint to quantify perceived latency."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React Interaction Instrumentation",
        "content": "Measure interaction latency in a React component.",
        "language": "javascript",
        "code": "function CheckoutButton({ onCheckout }) {\n  const handleClick = () => {\n    performance.mark('checkout-click');\n    \n    onCheckout();\n    \n    requestAnimationFrame(() => {\n      performance.mark('checkout-paint');\n      \n      performance.measure(\n        'checkout-latency',\n        'checkout-click',\n        'checkout-paint'\n      );\n    });\n  };\n\n  return <button onClick={handleClick}>Checkout</button>;\n}\n\n// Custom hook for repeatable measurements\nfunction useInteractionMeasure(label) {\n  useEffect(() => {\n    const start = `${label}-start`;\n    const end = `${label}-end`;\n    \n    return () => {\n      if (performance.mark(start) && performance.mark(end)) {\n        performance.measure(label, start, end);\n      }\n    };\n  }, [label]);\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Clear Marks in Production",
        "text": "The performance buffer has a finite size. In production, periodically clear marks and measures, or rely on PerformanceObserver to consume entries as they arrive. Never leave thousands of unobserved entries in memory."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Performance Mark",
            "definition": "A named timestamp recorded with performance.mark() for later measurement."
          },
          {
            "term": "Performance Measure",
            "definition": "A duration calculated between two marks or between a mark and navigation start."
          },
          {
            "term": "PerformanceObserver",
            "definition": "An API for asynchronously observing performance entries as they are recorded."
          }
        ]
      }
    },
    {
      "type": "relatedLessons",
      "data": {
        "lessons": [
          "user-centric-metrics",
          "cpu-profiling",
          "debugging-performance"
        ]
      }
    },
    {
      "type": "furtherReading",
      "data": {
        "items": [
          {
            "title": "User Timing API on MDN",
            "url": "https://developer.mozilla.org/en-US/docs/Web/API/User_Timing_API",
            "type": "article",
            "description": "Reference for marks, measures, and PerformanceObserver."
          },
          {
            "title": "Navigation Timing Level 2",
            "url": "https://developer.mozilla.org/en-US/docs/Web/API/Navigation_Timing_API",
            "type": "article",
            "description": "Detailed breakdown of page-load timing properties."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use performance.mark() and performance.measure() to instrument real user flows",
          "PerformanceObserver lets you watch entries in real time",
          "Navigation Timing gives per-stage page-load breakdowns",
          "Resource Timing shows asset-level download and latency details",
          "Clear marks periodically to avoid exhausting the performance buffer",
          "Pair custom marks with field metrics to connect lab and production"
        ],
        "takeaway": "Custom performance markers turn vague intuition into hard numbers. Instrument the interactions that matter most, observe them in production, and use the data to prioritize optimization work."
      }
    }
  ],
  "relatedLessons": [
    "user-centric-metrics",
    "cpu-profiling",
    "debugging-performance"
  ],
  "furtherReading": [
    {
      "title": "User Timing API on MDN",
      "url": "https://developer.mozilla.org/en-US/docs/Web/API/User_Timing_API",
      "type": "article",
      "description": "Reference for marks, measures, and PerformanceObserver."
    },
    {
      "title": "Navigation Timing Level 2",
      "url": "https://developer.mozilla.org/en-US/docs/Web/API/Navigation_Timing_API",
      "type": "article",
      "description": "Detailed breakdown of page-load timing properties."
    }
  ]
}
