{
  "id": "lesson-react-testing-testing-strategy-for-senior-frontend-engineers-playwright-based-e2e-testing",
  "slug": "playwright-based-e2e-testing",
  "moduleSlug": "testing-strategy-for-senior-frontend-engineers",
  "courseSlug": "react-testing",
  "title": "Playwright-based E2E testing",
  "description": "Write reliable end-to-end tests for React apps with Playwright, focusing on user-centric assertions, test isolation, and CI-ready configuration.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "testing",
    "playwright",
    "e2e",
    "ci"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "End-to-end tests exercise your application the same way a real user does: they load the page, click buttons, fill forms, and check that the right content appears. Playwright is the most capable modern E2E framework for this job because it controls a real browser, handles flaky selectors better than its predecessors, and provides excellent debugging tools."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A senior frontend engineer uses E2E tests sparingly but strategically. They are the slowest and most brittle layer of the testing pyramid, so they should cover only the critical user journeys that cannot be verified at lower levels."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Writing a Playwright test for a React app"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A Playwright test starts from a URL and interacts with the page through accessible selectors. Here is a test for a login flow:"
      }
    },
    {
      "type": "example",
      "data": {
        "language": "typescript",
        "caption": "Playwright E2E test for a login flow",
        "code": "import { test, expect } from '@playwright/test'\n\ntest.describe('Login flow', () => {\n  test('successful login redirects to the dashboard', async ({ page }) => {\n    await page.goto('http://localhost:3000/login')\n\n    await page.getByLabel('Email').fill('user@example.com')\n    await page.getByLabel('Password').fill('secure-password')\n    await page.getByRole('button', { name: /sign in/i }).click()\n\n    await expect(page).toHaveURL('http://localhost:3000/dashboard')\n    await expect(page.getByRole('heading', { name: /dashboard/i }))\n      .toBeVisible()\n  })\n\n  test('shows an error message for invalid credentials', async ({ page }) => {\n    await page.goto('http://localhost:3000/login')\n\n    await page.getByLabel('Email').fill('wrong@example.com')\n    await page.getByLabel('Password').fill('wrong-password')\n    await page.getByRole('button', { name: /sign in/i }).click()\n\n    await expect(page.getByRole('alert')).toHaveText('Invalid email or password')\n  })\n})"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Notice the use of getByLabel, getByRole, and getByText — the same queries that React Testing Library encourages. Playwright supports these accessible selectors directly, which makes tests more resilient to DOM changes."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Test isolation and authentication"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "E2E tests must not depend on each other. If one test logs in and another expects a logged-out state, the suite becomes flaky. Playwright provides a fresh context per test, which naturally isolates cookies and local storage."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Use test fixtures or a beforeAll hook to seed the database with known test data.",
          "Avoid logging in through the UI for every test: use a storage state fixture or API call to set the auth cookie once per worker.",
          "Never assert on implementation details like CSS class names or data-testid attributes that can change during a redesign.",
          "Clean up database state after each test to prevent cross-test pollution."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Authentication in Playwright tests"
      }
    },
    {
      "type": "example",
      "data": {
        "language": "typescript",
        "caption": "Reusing auth state across tests with Playwright storage state",
        "code": "import { test as base } from '@playwright/test'\n\nexport const test = base.extend({\n  // Run once per worker, not once per test.\n  storageState: { path: 'playwright/.auth/admin.json' },\n})\n\ntest('admin can access the settings page', async ({ page, storageState }) => {\n  await page.goto('http://localhost:3000/admin/settings')\n  await expect(page.getByRole('heading', { name: /settings/i })).toBeVisible()\n})"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "CI strategy",
        "text": "Run E2E tests in a dedicated CI job after unit and integration tests pass. Use Playwright's built-in retries (retries: 2) for known-flaky tests while you investigate the root cause. Do not use retries as a permanent solution — they hide the problem."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "items": [
          "Use Playwright for critical user journeys that cannot be fully verified at lower test levels.",
          "Write tests with accessible selectors: getByRole, getByLabel, getByText.",
          "Isolate tests with fresh browser contexts and seeded database state.",
          "Run E2E tests in CI after faster unit and integration suites pass."
        ]
      }
    }
  ],
  "relatedLessons": [
    "testcontainers-and-realistic-backend-integration",
    "flaky-test-diagnosis"
  ],
  "furtherReading": [
    {
      "type": "article",
      "title": "Playwright Best Practices",
      "url": "https://playwright.dev/docs/best-practices"
    },
    {
      "type": "article",
      "title": "Playwright Auth Patterns",
      "url": "https://playwright.dev/docs/auth"
    },
    {
      "type": "article",
      "title": "Web-First Assertions in Playwright",
      "url": "https://playwright.dev/docs/test-assertions"
    }
  ]
}
