{
  "id": "lesson-auth-017",
  "slug": "client-credentials-flow",
  "moduleSlug": "oauth2-flows-deep-dive",
  "courseSlug": "authentication-authorization",
  "title": "Client Credentials Flow",
  "description": "Implement machine-to-machine authentication using the OAuth2 Client Credentials grant type.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 24,
  "tags": ["oauth2", "client-credentials", "m2m", "machine-to-machine", "service-account"],
  "prerequisites": ["oauth-overview"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The Client Credentials flow is the simplest OAuth 2.0 flow and is used exclusively for machine-to-machine (M2M) authentication. There is no user involved — the client authenticates with its own credentials (client_id and client_secret) and receives a token representing the service itself. This token is then used to access APIs or other services on behalf of the client, not any particular user."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Client Credentials"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Backend services calling other backend services (e.g., a billing service calling a payment API)",
          "Scheduled jobs or cron tasks that need API access",
          "Microservices authenticating to an internal API gateway",
          "CI/CD pipelines that need to authenticate to deployment APIs",
          "Service accounts in Kubernetes or cloud environments"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "No user context",
        "text": "Tokens issued through Client Credentials represent the client itself, not a user. Any scopes in the token are limited to what the client is authorized to access. This means you cannot use this flow to access user-specific data — only service-level or client-level resources."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Flow"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The client authenticates with the authorization server using its client credentials. The request is a POST to the token endpoint with grant_type=client_credentials. If valid, the server responds with an access token. The client then uses that token to call the protected API."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "client-credentials-flow-diagram",
        "caption": "Client Credentials flow — direct machine-to-machine token exchange",
        "definition": "sequenceDiagram\n    participant Client\n    participant AuthServer\n    participant ResourceServer\n    Client->>AuthServer: POST /token (grant_type=client_credentials, client_id, client_secret, scope)\n    AuthServer->>AuthServer: Validate client credentials\n    AuthServer->>Client: access_token + expires_in\n    Client->>ResourceServer: GET /api/resource (Authorization: Bearer access_token)\n    ResourceServer->>Client: Protected resource"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Node.js Client Credentials Request",
        "content": "A service requesting its own access token",
        "language": "javascript",
        "code": "const axios = require('axios');\n\nasync function getServiceToken() {\n  const tokenUrl = process.env.TOKEN_URL;\n  const clientId = process.env.CLIENT_ID;\n  const clientSecret = process.env.CLIENT_SECRET;\n  const scope = 'api.read api.write';\n\n  const params = new URLSearchParams();\n  params.append('grant_type', 'client_credentials');\n  params.append('client_id', clientId);\n  params.append('client_secret', clientSecret);\n  params.append('scope', scope);\n\n  try {\n    const response = await axios.post(tokenUrl, params, {\n      headers: {\n        'Content-Type': 'application/x-www-form-urlencoded',\n      },\n    });\n\n    const { access_token, expires_in } = response.data;\n\n    // Cache the token to avoid requesting a new one on every call\n    const tokenExpiry = Date.now() + expires_in * 1000 - 60000; // 1 min buffer\n    return { accessToken: access_token, expiresAt: tokenExpiry };\n  } catch (error) {\n    console.error('Failed to obtain client credentials token:', error.response?.data);\n    throw error;\n  }\n}\n\n// Usage: get token, cache it, refresh before expiry\nconst tokenCache = { accessToken: null, expiresAt: null };\n\nasync function callProtectedApi() {\n  if (!tokenCache.accessToken || Date.now() > tokenCache.expiresAt) {\n    const token = await getServiceToken();\n    tokenCache.accessToken = token.accessToken;\n    tokenCache.expiresAt = token.expiresAt;\n  }\n\n  const response = await axios.get('https://api.example.com/data', {\n    headers: { Authorization: `Bearer ${tokenCache.accessToken}` },\n  });\n\n  return response.data;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Backend Middleware for Client Credentials Token",
        "content": "Validating a Client Credentials token in an Express middleware",
        "language": "tsx",
        "code": "import { Request, Response, NextFunction } from 'express';\nimport jwt from 'jsonwebtoken';\nimport jwksClient from 'jwks-rsa';\n\nconst client = jwksClient({ jwksUri: process.env.JWKS_URI });\n\nconst REQUIRED_SCOPE = 'api.read';\n\nexport async function requireClientCredentials(\n  req: Request,\n  res: Response,\n  next: NextFunction\n) {\n  const authHeader = req.headers.authorization;\n  if (!authHeader?.startsWith('Bearer ')) {\n    return res.status(401).json({ error: 'Missing or invalid Authorization header' });\n  }\n\n  const token = authHeader.substring(7);\n\n  try {\n    // Verify the token signature using the JWKS endpoint\n    const decoded = jwt.decode(token, { complete: true }) as jwt.JwtPayload;\n    if (!decoded) {\n      return res.status(401).json({ error: 'Invalid token' });\n    }\n\n    // Fetch the signing key\n    const signingKey = await new Promise< string>((resolve, reject) => {\n      client.getSigningKey(decoded.header.kid, (err, key) => {\n        if (err) reject(err);\n        resolve(key.getPublicKey());\n      });\n    });\n\n    const payload = jwt.verify(token, signingKey, {\n      audience: process.env.CLIENT_ID,\n      issuer: process.env.ISSUER,\n      algorithms: ['RS256'],\n    }) as jwt.JwtPayload;\n\n    // Verify this token was issued via client_credentials (no user sub)\n    if (payload.sub) {\n      return res.status(403).json({ error: 'Token must be from client credentials' });\n    }\n\n    // Require the specific scope this endpoint needs\n    const scopes = payload.scope?.split(' ') || [];\n    if (!scopes.includes(REQUIRED_SCOPE)) {\n      return res.status(403).json({ error: `Insufficient scope, requires: ${REQUIRED_SCOPE}` });\n    }\n\n    req.clientId = payload.client_id;\n    next();\n  } catch (error) {\n    console.error('Token validation error:', error);\n    return res.status(401).json({ error: 'Invalid token' });\n  }\n}\n"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Token caching strategy",
        "text": "Client Credentials tokens are often used across many requests. Always cache the token and its expiry. Request a new token only when the cached one is about to expire — this reduces load on the authorization server and avoids rate limiting."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Client Credentials vs. Authorization Code",
        "cards": [
          {
            "title": "Client Credentials",
            "description": "Machine-to-machine, no user involved. Token represents the client service.",
            "pros": ["No user interaction required", "Simple token request", "Ideal for service accounts", "No refresh token needed"],
            "cons": ["No user identity in token", "Limited to client-level scopes", "Not for user data access"],
            "tags": ["m2m", "services"]
          },
          {
            "title": "Authorization Code",
            "description": "User-driven flow with code exchange. Token represents the authenticated user.",
            "pros": ["Full user identity", "Works for interactive apps", "Refresh tokens for long sessions", "Supports OIDC"],
            "cons": ["Requires browser redirect", "More complex", "Needs user interaction"],
            "tags": ["user", "interactive"]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Scope validation is critical",
        "text": "The authorization server must validate that the requested scopes are within the client's allowed scope set. Never let a client request arbitrary scopes or escalate privileges. The token should carry only the minimum scopes needed for the intended operation."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Client Credentials is the only OAuth flow with no user involvement",
          "Use it for service-to-service, scheduled jobs, and CI/CD pipelines",
          "Always cache tokens and request new ones only near expiration",
          "Validate scope — tokens carry only the client's allowed scopes",
          "The issued token has no 'sub' (user subject) claim",
          "Protect the client secret — rotate it regularly in production"
        ],
        "takeaway": "Client Credentials is the simplest OAuth flow, but it's only correct when no user identity is needed."
      }
    }
  ],
  "relatedLessons": ["authorization-code-flow"],
  "furtherReading": [
    {
      "title": "RFC 6749 Section 4.4 — Client Credentials Grant",
      "url": "https://datatracker.ietf.org/doc/html/rfc6749#section-4.4",
      "type": "article",
      "description": "The official specification for the Client Credentials grant."
    }
  ]
}
