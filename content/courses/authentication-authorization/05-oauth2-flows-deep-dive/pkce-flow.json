{
  "id": "lesson-auth-016",
  "slug": "pkce-flow",
  "moduleSlug": "oauth2-flows-deep-dive",
  "courseSlug": "authentication-authorization",
  "title": "PKCE Flow",
  "description": "Secure public client authentication using Proof Key for Code Exchange (PKCE) with OAuth2.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 28,
  "tags": ["oauth2", "pkce", "mobile", "public-client", "security"],
  "prerequisites": ["authorization-code-flow"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "PKCE (Proof Key for Code Exchange) is an extension to the OAuth 2.0 Authorization Code flow. It was originally designed for native applications (mobile apps, desktop apps, single-page applications) that cannot securely store a client secret. PKCE prevents authorization code interception attacks by requiring the client to prove it was the original party that initiated the flow before the authorization server will issue tokens."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why PKCE Exists"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In a plain Authorization Code flow, the authorization code is passed through the browser's front channel. An attacker who can intercept the code (via a malicious app registering a custom URL scheme, network eavesdropping, or log injection) can exchange it for tokens. PKCE eliminates this risk: the client generates a random secret (code_verifier) and its transformation (code_challenge) upfront. The authorization server stores the challenge with the code. When the client exchanges the code, it must present the original verifier, which only the legitimate client possesses."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "code_verifier: A high-entropy random string generated by the client.",
          "code_challenge: BASE64URL-ENCODE(SHA256(code_verifier)) — the transformed value sent to the authorization server.",
          "code_challenge_method: S256 is the only acceptable method. Avoid plain (plaintext) at all costs.",
          "transform: The mathematical operation used to derive the challenge (SHA256 with S256)."
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "pkce-flow-diagram",
        "caption": "PKCE Authorization Code Flow with code verifier and challenge",
        "definition": "sequenceDiagram\n    participant User\n    participant Client\n    participant AuthServer\n    participant API\n    Client->>Client: Generate code_verifier\n    Client->>Client: Generate code_challenge = BASE64URL(SHA256(verifier))\n    Client->>AuthServer: Redirect with code_challenge, method=S256\n    AuthServer->>User: Login & Consent\n    User->>AuthServer: Submit credentials\n    AuthServer->>Client: Redirect with authorization code\n    Client->>AuthServer: POST code + code_verifier to token endpoint\n    AuthServer->>AuthServer: Verify SHA256(code_verifier) == code_challenge\n    AuthServer->>Client: access_token + refresh_token + id_token\n    Client->>API: Request with access_token\n    API->>Client: Protected resource"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Never use plain PKCE method",
        "text": "If code_challenge_method is set to 'plain', the challenge is just the raw verifier in plaintext. An attacker who intercepts the challenge can use it directly as the verifier. Always use S256 (SHA-256 hashing). Most modern authorization servers reject plain challenges from non-legacy clients."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generating the PKCE Pair",
        "content": "Generating cryptographically secure code verifier and challenge",
        "language": "javascript",
        "code": "function base64URLEncode(arrayBuffer) {\n  return btoa(String.fromCharCode(...new Uint8Array(arrayBuffer)))\n    .replace(/=/g, '')\n    .replace(/\\+/g, '-')\n    .replace(/\\//g, '_');\n}\n\nasync function generateCodeChallenge(codeVerifier) {\n  const encoder = new TextEncoder();\n  const data = encoder.encode(codeVerifier);\n  const digest = await crypto.subtle.digest('SHA-256', data);\n  return base64URLEncode(digest);\n}\n\nfunction generateCodeVerifier() {\n  // Generate 32 bytes of random data (43-128 chars is valid)\n  const array = new Uint8Array(32);\n  crypto.getRandomValues(array);\n  return base64URLEncode(array);\n}\n\n// Usage: generate verifier up front and keep it until token exchange\nconst codeVerifier = generateCodeVerifier();\nconst codeChallenge = await generateCodeChallenge(codeVerifier);\nconsole.log({ codeVerifier, codeChallenge });"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "PKCE Authorization Code Exchange",
        "content": "Exchanging the authorization code with the PKCE verifier on the backend",
        "language": "tsx",
        "code": "import { Router, Request, Response } from 'express';\nimport axios from 'axios';\n\nconst router = Router();\nconst TOKEN_URL = process.env.TOKEN_URL;\nconst CLIENT_ID = process.env.CLIENT_ID;\nconst REDIRECT_URI = process.env.REDIRECT_URI;\n\nrouter.get('/callback', async (req: Request, res: Response) => {\n  const { code } = req.query;\n\n  if (!code) {\n    return res.status(400).send('Missing authorization code');\n  }\n\n  try {\n    // Retrieve the code_verifier that was generated at the start\n    // of this flow. It must be stored per-session or per-request.\n    const codeVerifier = req.session.pkceCodeVerifier;\n\n    if (!codeVerifier) {\n      return res.status(400).send('PKCE code verifier not found');\n    }\n\n    // Build the token exchange request with PKCE\n    const params = new URLSearchParams();\n    params.append('grant_type', 'authorization_code');\n    params.append('code', code as string);\n    params.append('client_id', CLIENT_ID);\n    params.append('redirect_uri', REDIRECT_URI);\n    params.append('code_verifier', codeVerifier);\n\n    const response = await axios.post(TOKEN_URL, params, {\n      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },\n    });\n\n    const { access_token, refresh_token, id_token } = response.data;\n\n    // Tokens are now safely exchanged — the code could have been\n    // intercepted, but without the verifier it is useless to an attacker\n    req.session.accessToken = access_token;\n    req.session.refreshToken = refresh_token;\n    req.session.idToken = id_token;\n\n    res.redirect('/dashboard');\n  } catch (error: any) {\n    console.error('PKCE token exchange failed:', error.response?.data);\n    res.status(500).send('Authentication failed');\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Step by Step: PKCE in Four Phases"
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "items": [
          "At the start of the login flow, generate a code_verifier (random string) and compute code_challenge = BASE64URL(SHA256(code_verifier)). Store verifier locally and pass only the challenge.",
          "Send the user to the authorization endpoint with code_challenge and code_challenge_method=S256 instead of (or in addition to) client_id.",
          "After the user authenticates, redirect back with the authorization code. The authorization server will have stored the code_challenge alongside the code.",
          "POST the authorization code AND the original code_verifier to the token endpoint. The server runs SHA256(code_verifier) and compares it to the stored code_challenge. If they match, it issues tokens."
        ]
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Challenge Method", "Security", "Recommended"],
        "rows": [
          ["S256 (SHA-256)", "Verifier is hashed before sending; intercepted codes are useless.", "Yes"],
          ["plain", "Challenge equals the verifier; defeating the protection.", "No — never use"]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Where to store the code_verifier",
        "text": "In a single-page app, store the verifier in memory (not localStorage) to reduce XSS exposure. In a mobile app, use the platform's secure keychain. The verifier must survive the redirect round-trip but should be single-use and short-lived."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "PKCE extends Authorization Code flow for public clients that cannot store a secret",
          "code_verifier is a random secret; code_challenge is its SHA-256 hash",
          "Always use code_challenge_method=S256 — never 'plain'",
          "The verifier proves the token exchange request is from the same client that started the flow",
          "Store the verifier in memory or a secure keychain, tied to the session",
          "PKCE is required for mobile apps and strongly recommended for all SPAs"
        ],
        "takeaway": "PKCE turns an interceptable authorization code into a useless string without the matching verifier, closing the main gap in the Authorization Code flow for public clients."
      }
    }
  ],
  "relatedLessons": ["authorization-code-flow", "mobile-patterns"],
  "furtherReading": [
    {
      "title": "RFC 7636 — Proof Key for Code Exchange by OAuth Public Clients",
      "url": "https://datatracker.ietf.org/doc/html/rfc7636",
      "type": "article",
      "description": "The official specification defining PKCE."
    }
  ]
}
