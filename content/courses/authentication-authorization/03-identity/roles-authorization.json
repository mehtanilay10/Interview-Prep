{
  "id": "lesson-auth-011",
  "slug": "roles-authorization",
  "moduleSlug": "identity",
  "title": "Roles and Authorization",
  "description": "Learn how to implement role-based authorization with Identity.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": ["identity", "roles", "authorization", "policies"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "ASP.NET Core Identity supports role-based authorization, allowing you to restrict access based on user roles."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Role-Based Authorization"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Roles",
        "content": "Using roles for authorization",
        "language": "csharp",
        "code": "// Create roles\nawait roleManager.CreateAsync(new IdentityRole(\"Admin\"));\nawait roleManager.CreateAsync(new IdentityRole(\"User\"));\n\n// Assign role to user\nawait userManager.AddToRoleAsync(user, \"Admin\");\n\n// Protect endpoints\n[Authorize(Roles = \"Admin\")]\npublic IActionResult AdminPanel()\n{\n    return Ok(\"Admin only\");\n}\n\n// Multiple roles\n[Authorize(Roles = \"Admin,Manager\")]\npublic IActionResult Manage()\n{\n    return Ok(\"Admins and Managers\");\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Policy-Based Authorization"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Policies",
        "content": "Creating custom authorization policies",
        "language": "csharp",
        "code": "builder.Services.AddAuthorization(options =>\n{\n    options.AddPolicy(\"AdminOnly\", policy =>\n        policy.RequireRole(\"Admin\"));\n    \n    options.AddPolicy(\"AtLeast18\", policy =>\n        policy.RequireClaim(\"Age\", \"18\", \"19\", \"20\"));\n    \n    options.AddPolicy(\"CanEdit\", policy =>\n        policy.RequireAssertion(context =>\n            context.User.IsInRole(\"Admin\") ||\n            context.User.HasClaim(\"Permission\", \"Edit\")));\n});\n\n// Usage\n[Authorize(Policy = \"AdminOnly\")]\npublic IActionResult AdminOnly() { }"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer Policies",
        "text": "Policy-based authorization is more flexible than role-based. Use policies for complex authorization rules."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Roles restrict access by user type",
          "Policies for complex rules",
          "Combine roles and claims",
          "Prefer policies over roles"
        ],
        "takeaway": "Role and policy-based authorization control access."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
