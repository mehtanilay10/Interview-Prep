{
  "id": "lesson-auth-013",
  "slug": "password-management",
  "moduleSlug": "identity",
  "title": "Password Management and Recovery",
  "description": "Implement password reset, change password, and account recovery features.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "identity",
    "password",
    "recovery",
    "security",
    "aspnet-core"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Password management is critical for user security. ASP.NET Core Identity provides built-in features for password reset, change password, and account recovery."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Password Reset Flow"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The password reset flow involves generating a token, sending it via email, and allowing the user to set a new password."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Password Reset Token Generation",
        "content": "Generate and send password reset token.",
        "language": "csharp",
        "code": "[HttpPost(\"forgot-password\")]\npublic async Task<IActionResult> ForgotPassword(ForgotPasswordModel model)\n{\n    var user = await _userManager.FindByEmailAsync(model.Email);\n    if (user == null || !(await _userManager.IsEmailConfirmedAsync(user)))\n    {\n        // Don't reveal that the user does not exist\n        return RedirectToAction(\"ForgotPasswordConfirmation\");\n    }\n\n    var token = await _userManager.GeneratePasswordResetTokenAsync(user);\n    var callbackUrl = Url.Action(\"ResetPassword\", \"Account\", \n        new { token, email = user.Email }, protocol: HttpContext.Request.Scheme);\n\n    await _emailService.SendEmailAsync(user.Email, \"Reset Password\",\n        $\"Reset your password by clicking here: <a href='{callbackUrl}'>link</a>\");\n\n    return RedirectToAction(\"ForgotPasswordConfirmation\");\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Password Reset Handler",
        "content": "Handle password reset with token validation.",
        "language": "csharp",
        "code": "[HttpPost]\n[ValidateAntiForgeryToken]\npublic async Task<IActionResult> ResetPassword(ResetPasswordModel model)\n{\n    if (!ModelState.IsValid)\n        return View(model);\n\n    var user = await _userManager.FindByEmailAsync(model.Email);\n    if (user == null)\n        return RedirectToAction(\"ResetPasswordConfirmation\");\n\n    var result = await _userManager.ResetPasswordAsync(user, model.Token, model.Password);\n    if (result.Succeeded)\n    {\n        return RedirectToAction(\"ResetPasswordConfirmation\");\n    }\n\n    foreach (var error in result.Errors)\n    {\n        ModelState.AddModelError(string.Empty, error.Description);\n    }\n    return View(model);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Change Password"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Authenticated users can change their password without requiring a reset token. This is a common security feature."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Change Password Implementation",
        "content": "Allow users to change their password.",
        "language": "csharp",
        "code": "[HttpPost]\n[ValidateAntiForgeryToken]\npublic async Task<IActionResult> ChangePassword(ChangePasswordModel model)\n{\n    if (!ModelState.IsValid)\n        return View(model);\n\n    var user = await _userManager.GetUserAsync(User);\n    if (user == null)\n        return RedirectToAction(\"Login\");\n\n    var result = await _userManager.ChangePasswordAsync(user, model.OldPassword, model.NewPassword);\n    if (result.Succeeded)\n    {\n        await _signInManager.RefreshSignInAsync(user);\n        return RedirectToAction(\"ChangePasswordConfirmation\");\n    }\n\n    foreach (var error in result.Errors)\n    {\n        ModelState.AddModelError(string.Empty, error.Description);\n    }\n    return View(model);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Account Lockout"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Identity automatically locks accounts after too many failed login attempts. This prevents brute force attacks."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Lockout Configuration",
        "content": "Configure account lockout settings.",
        "language": "csharp",
        "code": "builder.Services.Configure<IdentityOptions>(options =>\n{\n    options.Lockout.DefaultLockoutTimeSpan = TimeSpan.FromMinutes(5);\n    options.Lockout.MaxFailedAccessAttempts = 5;\n    options.Lockout.AllowedForNewUsers = true;\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Security Best Practices",
        "text": "Always use HTTPS for password-related operations. Password reset tokens should expire quickly (typically 1-24 hours). Never log passwords or reset tokens."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Password Policy Configuration"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Configure password requirements to enforce strong passwords across your application."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Strong Password Policy",
        "content": "Configure comprehensive password requirements.",
        "language": "csharp",
        "code": "builder.Services.Configure<IdentityOptions>(options =>\n{\n    options.Password.RequireDigit = true;\n    options.Password.RequireLowercase = true;\n    options.Password.RequireUppercase = true;\n    options.Password.RequireNonAlphanumeric = true;\n    options.Password.RequiredLength = 12;\n    options.Password.RequiredUniqueChars = 6;\n});"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Password Reset Token",
            "definition": "A security token generated by Identity that allows a user to reset their password without being logged in."
          },
          {
            "term": "Account Lockout",
            "definition": "A security feature that temporarily disables an account after multiple failed login attempts."
          },
          {
            "term": "Password Policy",
            "definition": "Rules that define what constitutes a valid password, such as length, complexity, and character requirements."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Password reset uses tokens sent via email",
          "Change password requires authentication",
          "Account lockout prevents brute force attacks",
          "Configure strong password policies",
          "Always use HTTPS for password operations"
        ],
        "takeaway": "Password management features are essential for user security and account recovery."
      }
    }
  ],
  "relatedLessons": [
    "identity-overview",
    "setup-identity"
  ],
  "courseSlug": "authentication-authorization"
}