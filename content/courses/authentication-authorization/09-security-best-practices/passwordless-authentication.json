{
  "id": "lesson-auth-035",
  "slug": "passwordless-authentication",
  "moduleSlug": "security-best-practices",
  "courseSlug": "authentication-authorization",
  "title": "Passwordless Authentication",
  "description": "Implement passwordless authentication methods including magic links, WebAuthn, and one-time codes.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "passwordless",
    "magic-links",
    "webauthn",
    "otp",
    "security"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Passwordless authentication** removes the need for users to remember and type passwords. Instead, users prove identity through something they have (email, phone, security key) or something they are (biometrics)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Passwords Are a Liability"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "**Reuse**: Users recycle passwords across sites, so one breach compromises many accounts",
          "**Phishing**: Passwords can be stolen via fake login pages",
          " **Brute force**: Weak passwords are guessable with enough attempts",
          "**Credential stuffing**: Leaked passwords are automatically tried on other sites",
          "**Helpdesk burden**: Password resets consume support resources"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "The Password Problem",
        "text": "According to Verizon's DBIR, over 80% of data breaches involve stolen or weak passwords. Passwordless auth eliminates this attack surface entirely."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Magic Links"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **magic link** is a single-use, time-limited URL sent to the user's email. Clicking it logs the user in without requiring a password. The flow is simple but must be implemented with strict security controls."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Magic Link Generation (C#)",
        "content": "Generating and validating a secure magic link token in ASP.NET Core",
        "language": "csharp",
        "code": "public async Task<string> GenerateMagicLinkAsync(string email)\n{\n    var token = Convert.ToBase64String(Guid.NewGuid().ToByteArray())\n        .Replace(\"/\", \"\").Replace(\"+\", \"\").Replace(\"=\", \"\");\n\n    var magicLink = new PasswordlessLogin\n    {\n        Email = email,\n        Token = token,\n        ExpiresAt = DateTime.UtcNow.AddMinutes(15),\n        Used = false\n    };\n\n    _context.PasswordlessLogins.Add(magicLink);\n    await _context.SaveChangesAsync();\n\n    var callbackUrl = $\"{_httpContextAccessor.HttpContext.Request.Scheme}://\" +\n        $\"{_httpContextAccessor.HttpContext.Request.Host}/\" +\n        $\"auth/magic-link?token={token}&email={Uri.EscapeDataString(email)}\";\n\n    await _emailSender.SendAsync(email, \"Sign in to your account\",\n        $\"Click <a href='{callbackUrl}'>here</a> to sign in. This link expires in 15 minutes.\");\n\n    return callbackUrl;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Security Requirements"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Token must be cryptographically random (use `RandomNumberGenerator`, not `Guid` alone)",
          "Token must expire within 15 minutes",
          "Token must be single-use (mark `Used = true` after first use)",
          "Link must be HTTPS only",
          "Rate limit magic link requests per email/IP"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "WebAuthn / FIDO2"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**WebAuthn** is the W3C standard for passwordless authentication using public-key cryptography. Users authenticate with a hardware security key (YubiKey), platform authenticator (Windows Hello, Touch ID), or roaming authenticator (phone)."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Why WebAuthn Is Secure",
        "text": "WebAuthn is phishing-resistant because the credential is bound to the origin (domain). An attacker cannot replay a credential on a fake site. It also never exposes a private key to the server."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "WebAuthn Registration (TypeScript)",
        "content": "Registering a WebAuthn credential on the client side",
        "language": "tsx",
        "code": "async function registerCredential() {\n  const challenge = new Uint8Array(32);\n  crypto.getRandomValues(challenge);\n\n  const response = await fetch('/api/webauthn/register-challenge', {\n    method: 'POST',\n    body: JSON.stringify({ userId: user.id, username: user.email })\n  });\n\n  const { attestation, userId } = await response.json();\n\n  const credential = await navigator.credentials.create({\n    publicKey: {\n      challenge,\n      rp: { name: 'Interview Prep', id: window.location.hostname },\n      user: {\n        id: new TextEncoder().encode(userId),\n        name: user.email,\n        displayName: user.name\n      },\n      pubKeyCredParams: [{ type: 'public-key', alg: -7 }], // ES256\n      authenticatorSelection: {\n        authenticatorAttachment: 'cross-platform',\n        userVerification: 'required'\n      },\n      timeout: 60000,\n      attestation: 'direct'\n    }\n  });\n\n  await fetch('/api/webauthn/register', {\n    method: 'POST',\n    body: JSON.stringify({\n      id: credential.id,\n      rawId: Array.from(new Uint8Array(credential.rawId)),\n      response: credential.response\n    })\n  });\n}"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "passwordless-flow",
        "caption": "Passwordless authentication flow using magic links",
        "definition": "sequenceDiagram\n    participant U as User\n    participant C as Client App\n    participant S as Server\n    participant E as Email Provider\n    U->>C: Enter email address\n    C->>S: Request magic link\n    S->>S: Generate secure token\n    S->>E: Send email with link\n    E->>U: Deliver email\n    U->>C: Click magic link\n    C->>S: Validate token\n    S->>S: Check expiry + single use\n    S->>C: Return session/JWT\n    C->>U: User is logged in"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "One-Time Codes (OTP)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "OTP codes are numeric or alphanumeric codes sent via email or SMS. They are simpler than magic links but require the user to manually copy-paste. For higher security, combine OTP with a secondary factor."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Passwordless Implementation Checklist",
        "items": [
          {
            "text": "Use cryptographically secure random tokens (not sequential or guessable)",
            "hint": "Prefer RandomNumberGenerator over Guid for token generation"
          },
          {
            "text": "Set token expiration to 15 minutes or less",
            "hint": "Longer windows increase the window for interception"
          },
          {
            "text": "Enforce single-use tokens",
            "hint": "Invalidate immediately after first successful use"
          },
          {
            "text": "Rate-limit requests per email/IP address",
            "hint": "Prevent email bombing attacks"
          },
          {
            "text": "Provide fallback authentication methods",
            "hint": "Users may lose access to their email or security keys"
          },
          {
            "text": "Log all passwordless authentication attempts",
            "hint": "Essential for forensic analysis after incidents"
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Passwordless auth eliminates password-related attacks",
          "Magic links require secure, expiring, single-use tokens",
          "WebAuthn is phishing-resistant and uses public-key cryptography",
          "Always provide fallback authentication methods",
          "Rate-limit to prevent email bombing"
        ],
        "takeaway": "Passwordless authentication is more secure and user-friendly than passwords when implemented correctly."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
