{
  "id": "lesson-auth-008",
  "slug": "react-integration",
  "moduleSlug": "jwt",
  "title": "React Integration",
  "description": "Learn how to integrate JWT authentication in React applications.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 22,
  "tags": ["jwt", "react", "integration", "frontend"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Integrating JWT auth in React involves storing tokens, attaching them to requests, and protecting routes."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Auth Context"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Auth Context",
        "content": "Creating auth context in React",
        "language": "jsx",
        "code": "const AuthContext = createContext(null);\n\nexport function AuthProvider({ children }) {\n  const [token, setToken] = useState(localStorage.getItem('token'));\n\n  const login = async (email, password) => {\n    const response = await fetch('/api/auth/login', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ email, password })\n    });\n    const data = await response.json();\n    localStorage.setItem('token', data.token);\n    setToken(data.token);\n  };\n\n  const logout = () => {\n    localStorage.removeItem('token');\n    setToken(null);\n  };\n\n  return (\n    <AuthContext.Provider value={{ token, login, logout }}>\n      {children}\n    </AuthContext.Provider>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Protected Routes"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Protected Route",
        "content": "Creating protected routes",
        "language": "jsx",
        "code": "function ProtectedRoute({ children }) {\n  const { token } = useAuth();\n\n  if (!token) {\n    return <Navigate to=\"/login\" />;\n  }\n\n  return children;\n}\n\n// Usage\n<Route path=\"/dashboard\" element={\n  <ProtectedRoute>\n    <Dashboard />\n  </ProtectedRoute>\n} />"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Axios Interceptor",
        "text": "Use Axios interceptors to automatically attach the JWT to every request."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Store token in localStorage",
          "Auth context provides auth state",
          "Protected routes guard pages",
          "Attach token to API requests"
        ],
        "takeaway": "React integration requires context and route protection."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
