{
  "id": "lesson-auth-007",
  "slug": "validating-jwt",
  "moduleSlug": "jwt",
  "title": "Validating JWTs",
  "description": "Learn how JWT validation works and how to protect endpoints.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "jwt",
    "validation",
    "authorization",
    "protect"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "JWT validation verifies the token's signature, expiration, and claims. ASP.NET Core handles this automatically with the [Authorize] attribute."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Protecting Endpoints"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Authorize",
        "content": "Protecting API endpoints",
        "language": "csharp",
        "code": "[ApiController]\n[Route(\"api/[controller]\")]\npublic class UsersController : ControllerBase\n{\n    [Authorize] // Requires valid JWT\n    [HttpGet]\n    public IActionResult Get()\n    {\n        return Ok(\"Protected data\");\n    }\n\n    [Authorize(Roles = \"Admin\")] // Requires Admin role\n    [HttpDelete(\"{id}\")]\n    public IActionResult Delete(int id)\n    {\n        return Ok(\"Deleted\");\n    }\n\n    [AllowAnonymous] // No auth required\n    [HttpPost(\"login\")]\n    public IActionResult Login(LoginModel model)\n    {\n        // Login logic\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accessing User Claims"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Claims",
        "content": "Reading user data from JWT",
        "language": "csharp",
        "code": "[HttpGet(\"profile\")]\npublic IActionResult GetProfile()\n{\n    var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;\n    var email = User.FindFirst(ClaimTypes.Email)?.Value;\n    var role = User.FindFirst(ClaimTypes.Role)?.Value;\n\n    return Ok(new { userId, email, role });\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "User Property",
        "text": "The User property on ControllerBase contains the authenticated user's claims from the JWT."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "[Authorize] protects endpoints",
          "[AllowAnonymous] bypasses auth",
          "Roles restrict access",
          "User property contains claims"
        ],
        "takeaway": "JWT validation is automatic with proper configuration."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "authentication-authorization"
}
