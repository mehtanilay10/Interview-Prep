{
  "id": "lesson-redux-015",
  "slug": "entity-adapter",
  "moduleSlug": "redux-patterns",
  "courseSlug": "redux",
  "title": "Entity Adapter",
  "description": "Learn how to use createEntityAdapter for automated normalized state management in Redux Toolkit.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "redux",
    "entity",
    "adapter",
    "normalization",
    "toolkit"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Manually normalizing state and writing all the update logic can become repetitive. Every time you add a new entity type, you write similar code for adding, updating, and deleting items. **Redux Toolkit's `createEntityAdapter`** solves this by generating all that boilerplate automatically."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An entity adapter provides pre-built reducers for common CRUD operations and memoized selectors for efficient data retrieval. Instead of writing normalization logic from scratch, you configure an adapter once and get everything you need."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating an Entity Adapter"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Entity Adapter",
        "content": "Setting up an entity adapter for users",
        "language": "javascript",
        "code": "import { createEntityAdapter, createSlice } from '@reduxjs/toolkit';\n\n// Create the adapter with optional configuration\nconst usersAdapter = createEntityAdapter({\n  // Tell the adapter how to get the ID (defaults to entity.id)\n  selectId: (user) => user.userId,\n  // Optional: keep entries sorted by this comparer function\n  sortComparer: (a, b) => a.name.localeCompare(b.name)\n});\n\n// Get the pre-built initial state\nconst initialState = usersAdapter.getInitialState({\n  // You can add additional fields to the slice state\n  loading: false,\n  error: null\n});\n\n// Use the adapter in a slice\nconst usersSlice = createSlice({\n  name: 'users',\n  initialState,\n  reducers: {\n    // These are pre-built adapter methods\n    addUser: usersAdapter.addOne,\n    addUsers: usersAdapter.addMany,\n    setAllUsers: usersAdapter.setAll,\n    updateUser: usersAdapter.updateOne,\n    removeUser: usersAdapter.removeOne,\n    removeAllUsers: usersAdapter.removeAll\n  }\n});\n\nexport const {\n  addUser,\n  updateUser,\n  removeUser\n} = usersSlice.actions;\n\nexport default usersSlice.reducer;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Adapter Methods"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Entity adapters provide a complete set of methods for managing normalized state. These methods handle the immutability updates automatically, so you don't have to worry about spreading objects correctly."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Method",
          "Parameters",
          "Description"
        ],
        "rows": [
          [
            "addOne",
            "state, entity",
            "Add a single entity to the state"
          ],
          [
            "addMany",
            "state, entities",
            "Add multiple entities (array or object)"
          ],
          [
            "setAll",
            "state, entities",
            "Replace all entities with new ones"
          ],
          [
            "updateOne",
            "state, changes",
            "Update one entity with partial changes"
          ],
          [
            "updateMany",
            "state, changesArray",
            "Update multiple entities at once"
          ],
          [
            "removeOne",
            "state, id",
            "Remove a single entity by ID"
          ],
          [
            "removeMany",
            "state, ids",
            "Remove multiple entities by ID array"
          ],
          [
            "removeAll",
            "state",
            "Remove all entities"
          ],
          [
            "upsertOne",
            "state, entity",
            "Insert or update an entity"
          ],
          [
            "upsertMany",
            "state, entities",
            "Insert or update multiple entities"
          ]
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "caption": "Entity adapter operations flow",
        "definition": "flowchart TD\n    A[\"API Response\n(entity array)\"] --> B[addMany]\n    B --> C[\"Normalized State\n{ byId: {...}, ids: [...] }\"]\n    C --> D[getSelectors]\n    D --> E[selectAll / selectById]\n    C --> F[updateOne]\n    F --> C\n    C --> G[removeOne]\n    G --> C"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using Entity Selectors"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Along with reducers, entity adapters generate memoized selectors. These selectors are optimized to only recompute when the relevant data actually changes, preventing unnecessary re-renders in your components."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generated Selectors",
        "content": "Using the auto-generated selectors",
        "language": "javascript",
        "code": "import { createSelector } from '@reduxjs/toolkit';\n\n// Create a selector factory for this entity slice\nconst usersSelectors = usersAdapter.getSelectors(\n  (state) => state.users\n);\n\n// These are the pre-built selectors\nexport const selectAllUsers = usersSelectors.selectAll;\nexport const selectUserById = usersSelectors.selectById;\nexport const selectUserIds = usersSelectors.selectIds;\nexport const selectTotalUserCount = usersSelectors.selectTotal;\nexport const selectUserEntities = usersSelectors.selectEntities;\n\n// Usage in a component\nconst users = useSelector(selectAllUsers);\nconst specificUser = useSelector((state) => selectUserById(state, 'user-123'));"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Updating Entities"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `updateOne` method uses an object with an `id` and `changes` property. This lets you update specific fields without replacing the entire entity. The changes object is shallowly merged with the existing entity."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Updating Entities",
        "content": "Different ways to update entity data",
        "language": "javascript",
        "code": "// Basic field update\ndispatch(updateUser({\n  id: 'user-123',\n  changes: { name: 'Alice Smith', email: 'alice@example.com' }\n}));\n\n// Update with a function for computed changes\ndispatch(updateUser({\n  id: 'user-123',\n  changes: {\n    lastLogin: new Date().toISOString(),\n    loginCount: (state, id) => state.users.byId[id].loginCount + 1\n  }\n}));\n\n// Multiple updates at once\ndispatch(usersAdapter.updateMany([\n  { id: 'user-1', changes: { role: 'admin' } },\n  { id: 'user-2', changes: { role: 'moderator' } }\n]));"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Manual vs Adapter Approach",
        "cards": [
          {
            "title": "Manual Normalization",
            "content": "Write byId and allIds state manually\nCreate custom reducer logic for each operation\nWrite selector functions from scratch\nHigher chance of bugs in immutability\nMore boilerplate per entity type"
          },
          {
            "title": "Entity Adapter",
            "content": "Auto-generates normalized state structure\nPre-built reducers for all CRUD operations\nMemoized selectors included\nBattle-tested immutability handling\nConsistent pattern across all entities"
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Consistent Patterns",
        "text": "Using entity adapters across your application creates a consistent pattern for handling all entity data, making your codebase easier to understand and maintain."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "title": "Key Terms",
        "terms": [
          {
            "term": "Entity Adapter",
            "definition": "A Redux Toolkit utility that generates reducers and selectors for normalized state management."
          },
          {
            "term": "selectId",
            "definition": "A function that extracts the unique ID from an entity object."
          },
          {
            "term": "sortComparer",
            "definition": "A function that determines the sort order of entities."
          },
          {
            "term": "Memoized Selectors",
            "definition": "Selectors that cache their results and only recompute when inputs change."
          },
          {
            "term": "updateOne",
            "definition": "An adapter method to partially update a single entity by ID."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Products Slice",
        "description": "Create an entity adapter for managing products. Implement all CRUD operations and create selectors for common use cases."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "createEntityAdapter generates normalized state management code",
          "Pre-built reducer methods handle all CRUD operations",
          "Auto-generated selectors are memoized for performance",
          "Configure selectId and sortComparer for custom entity types",
          "Combine with createSlice for complete entity state management",
          "Works great with RTK Query for data synchronization"
        ],
        "takeaway": "Entity adapters eliminate normalization boilerplate and provide battle-tested patterns for managing collections in Redux."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [
    {
      "title": "Redux Toolkit Entity Adapter API",
      "url": "https://redux-toolkit.js.org/api/createEntityAdapter"
    },
    {
      "title": "RTK Query Entity Cache",
      "url": "https://redux-toolkit.js.org/rtk-query/usage/optimistic-updates"
    }
  ]
}
