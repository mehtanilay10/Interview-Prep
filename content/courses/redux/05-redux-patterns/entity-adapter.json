{
  "id": "lesson-redux-015",
  "slug": "entity-adapter",
  "moduleSlug": "redux-patterns",
  "title": "Entity Adapter",
  "description": "Learn createEntityAdapter for normalized state management.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 22,
  "tags": [
    "redux",
    "entity",
    "adapter",
    "normalization"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**createEntityAdapter** generates reducers and selectors for normalized state. It handles common CRUD operations automatically."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Setup"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Entity Adapter",
        "content": "Creating an entity adapter",
        "language": "javascript",
        "code": "import { createEntityAdapter, createSlice } from '@reduxjs/toolkit';\n\nconst usersAdapter = createEntityAdapter({\n  selectId: (user) => user.id,\n  sortComparer: (a, b) => a.name.localeCompare(b.name)\n});\n\nconst usersSlice = createSlice({\n  name: 'users',\n  initialState: usersAdapter.getInitialState({\n    loading: false,\n    error: null\n  }),\n  reducers: {\n    userAdded: usersAdapter.addOne,\n    usersReceived: usersAdapter.setAll,\n    userUpdated: usersAdapter.updateOne,\n    userRemoved: usersAdapter.removeOne\n  }\n});\n\n// Auto-generated selectors\nconst selectors = usersAdapter.getSelectors((state) => state.users);\nexport const { selectAll: selectAllUsers, selectById: selectUserById } = selectors;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Benefits",
        "text": "Entity adapters handle normalization, provide CRUD reducers, and generate memoized selectors automatically."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "createEntityAdapter handles normalization",
          "Auto-generates CRUD reducers",
          "Provides memoized selectors",
          "Simplifies state management"
        ],
        "takeaway": "Entity adapters automate normalized state management."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "redux"
}