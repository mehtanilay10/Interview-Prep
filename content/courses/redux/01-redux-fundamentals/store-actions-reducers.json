{
  "id": "lesson-redux-002",
  "slug": "store-actions-reducers",
  "moduleSlug": "redux-fundamentals",
  "title": "Store, Actions, and Reducers",
  "description": "Learn the three core building blocks of Redux.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 22,
  "tags": [
    "redux",
    "store",
    "actions",
    "reducers"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Redux has three core concepts that work together: the **Store** holds your state, **Actions** describe what happened, and **Reducers** specify how state changes in response. Once you understand how these three pieces interact, you have the mental model for every Redux application."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-store-actions-reducers",
        "caption": "How the Store, Action, and Reducer work together",
        "definition": "flowchart LR\n    UI[Component] -- \"dispatch(action)\" --> S[Store]\n    S -- \"current state + action\" --> R[Reducer]\n    R -- \"new state\" --> S\n    S -- \"notifies\" --> UI\n    style UI fill:#e1f5ff\n    style S fill:#ffe1f5\n    style R fill:#e7ffe1"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Store"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The **store** is the single object that holds your application's state and provides the methods to read it, change it, and listen for changes. In modern Redux you create it with `configureStore` from Redux Toolkit, but the underlying `createStore` API exposes the same essential methods."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store",
        "content": "Using createStore (classic) and configureStore (modern)",
        "language": "javascript",
        "code": "// Classic Redux\nimport { createStore } from 'redux';\nconst store = createStore(counterReducer);\n\n// Modern Redux Toolkit (recommended)\nimport { configureStore } from '@reduxjs/toolkit';\nconst store = configureStore({\n  reducer: counterReducer\n});\n\n// Essential store methods\nstore.getState();          // read current state\nstore.dispatch(action);    // change state via an action\nstore.subscribe(listener); // listen for state changes"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Actions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An **action** is a plain JavaScript object that describes what happened in your application. The only requirement is that it has a `type` field, which is a string that uniquely identifies the action. Other fields carry the data needed to update state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Action Objects",
        "content": "Defining plain action objects",
        "language": "javascript",
        "code": "// Minimal action - just a type\n{ type: 'counter/increment' }\n\n// Action with payload\n{\n  type: 'todos/add',\n  payload: { id: 1, text: 'Learn Redux', completed: false }\n}\n\n// Action with error\n{ type: 'auth/loginFailed', error: 'Invalid credentials' }"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Most apps wrap action creation in **action creator** functions. This keeps your code DRY and makes refactoring easier because the action shape lives in one place."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Action Creators",
        "content": "Functions that return action objects",
        "language": "javascript",
        "code": "const addTodo = (text) => ({\n  type: 'todos/add',\n  payload: { id: nextId(), text, completed: false }\n});\n\nconst increment = () => ({ type: 'counter/increment' });\nconst incrementByAmount = (amount) => ({\n  type: 'counter/incrementByAmount',\n  payload: amount\n});\n\n// Usage\ndispatch(addTodo('Write docs'));\ndispatch(incrementByAmount(5));"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Reducers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **reducer** is a pure function that takes the current state and an action, and returns the next state. Reducers are the only place state transitions are defined, which makes them the heart of every Redux app."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "A Counter Reducer",
        "content": "A classic reducer written by hand",
        "language": "javascript",
        "code": "const initialState = { value: 0 };\n\nfunction counterReducer(state = initialState, action) {\n  switch (action.type) {\n    case 'counter/increment':\n      return { value: state.value + 1 };\n    case 'counter/decrement':\n      return { value: state.value - 1 };\n    case 'counter/incrementByAmount':\n      return { value: state.value + action.payload };\n    default:\n      return state;\n  }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Reducers Must Be Pure",
        "text": "Reducers must be pure functions - they cannot mutate state, perform side effects, or call non-pure functions like Date.now() or Math.random(). If you need side effects, use middleware or move them outside the reducer."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Putting It All Together"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Full Example",
        "content": "A complete classic Redux counter",
        "language": "javascript",
        "code": "import { createStore } from 'redux';\n\n// 1. Action creators\nconst increment = () => ({ type: 'counter/increment' });\nconst decrement = () => ({ type: 'counter/decrement' });\n\n// 2. Reducer\nfunction counter(state = { value: 0 }, action) {\n  switch (action.type) {\n    case 'counter/increment': return { value: state.value + 1 };\n    case 'counter/decrement': return { value: state.value - 1 };\n    default: return state;\n  }\n}\n\n// 3. Store\nconst store = createStore(counter);\n\n// 4. Subscribe and dispatch\nstore.subscribe(() => console.log('State:', store.getState()));\nstore.dispatch(increment()); // State: { value: 1 }\nstore.dispatch(increment()); // State: { value: 2 }\nstore.dispatch(decrement()); // State: { value: 1 }"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Comparing the Building Blocks"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Concept",
          "What It Is",
          "What It Does",
          "Key API"
        ],
        "rows": [
          [
            "Store",
            "Single state object",
            "Holds state, dispatches actions, notifies subscribers",
            "getState, dispatch, subscribe"
          ],
          [
            "Action",
            "Plain object with `type`",
            "Describes what happened",
            "Dispatched via store.dispatch"
          ],
          [
            "Reducer",
            "Pure function",
            "Computes the next state from previous state and action",
            "(state, action) => newState"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key Terms"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Action Type",
            "definition": "A string constant that uniquely identifies an action. Conventionally written as `domain/eventName` (e.g. `todos/add`)."
          },
          {
            "term": "Action Creator",
            "definition": "A function that returns an action object. Helps avoid typos and keeps the action shape in one place."
          },
          {
            "term": "Payload",
            "definition": "A conventional field on an action that carries the data needed to update state."
          },
          {
            "term": "Pure Function",
            "definition": "A function that always returns the same output for the same input and has no side effects."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Exercise"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Todo Reducer",
        "description": "Build a small todo reducer from scratch using only createStore, plain actions, and a switch statement.",
        "steps": [
          "Define action types: 'todos/add', 'todos/toggle', 'todos/remove'",
          "Write action creators for each",
          "Write a reducer that manages an array of todos with id, text, and completed fields",
          "Create a store, subscribe, and dispatch a few actions to verify behavior"
        ],
        "expectedOutcome": "A working classic Redux todo slice that you can dispatch actions against and observe state changes through the console."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "The store holds state and exposes dispatch, getState, and subscribe",
          "Actions are plain objects with a required `type` field",
          "Action creators wrap action creation for reuse and consistency",
          "Reducers are pure functions that return the next state",
          "Reducers must never mutate state or perform side effects"
        ],
        "takeaway": "The store, actions, and reducers are the three foundational building blocks of every Redux application."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "redux"
}
