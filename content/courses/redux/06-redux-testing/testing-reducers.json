{
  "id": "lesson-redux-017",
  "slug": "testing-reducers",
  "moduleSlug": "redux-testing",
  "courseSlug": "redux",
  "title": "Testing Reducers",
  "description": "Learn how to write comprehensive unit tests for Redux reducers using Jest and React Testing Library.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "redux",
    "testing",
    "reducers",
    "unit-tests",
    "jest"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Testing is crucial for maintaining reliable Redux applications. Reducers are particularly easy to test because they are **pure functions**. Given the same input state and action, a reducer always returns the same output. No mocking, no async complexity, just straightforward input-output verification."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "This lesson covers how to write comprehensive reducer tests that verify your state management logic works correctly for all possible cases."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Test Reducers?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Reducers contain your application's core logic for state transitions. Testing them thoroughly gives you confidence that your state changes work correctly, regardless of what components or side effects interact with them."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Unit vs Integration Tests",
        "cards": [
          {
            "title": "Unit Tests (Reducers)",
            "content": "Test state logic in isolation\nNo rendering or DOM needed\nFast execution\nEasy to pinpoint failures\nTest edge cases thoroughly"
          },
          {
            "title": "Integration Tests",
            "content": "Test full user flows\nIncludes components and store\nSlower to run\nTests real user behavior\nCatches integration bugs"
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Reducer Test Structure"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A good reducer test suite covers: the initial state, each action type, edge cases, and invalid action handling. Each test should be focused and readable."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducer Test Structure",
        "content": "A well-structured reducer test file",
        "language": "javascript",
        "code": "import counterReducer, {\n  increment,\n  decrement,\n  incrementByAmount,\n  reset\n} from './counterSlice';\n\ndescribe('counter reducer', () => {\n  // Always test the initial state\n  describe('initial state', () => {\n    it('should return the initial state when called with undefined', () => {\n      const initialState = { value: 0, status: 'idle' };\n      expect(counterReducer(undefined, { type: 'unknown' })).toEqual(initialState);\n    });\n  });\n\n  // Group tests by action\n  describe('increment', () => {\n    it('should increment the value by 1', () => {\n      const previousState = { value: 5, status: 'idle' };\n      expect(counterReducer(previousState, increment())).toEqual({\n        value: 6,\n        status: 'idle'\n      });\n    });\n\n    it('should handle increment from zero', () => {\n      const initialState = { value: 0, status: 'idle' };\n      expect(counterReducer(initialState, increment())).toEqual({\n        value: 1,\n        status: 'idle'\n      });\n    });\n  });\n\n  describe('decrement', () => {\n    it('should decrement the value by 1', () => {\n      const previousState = { value: 3, status: 'idle' };\n      expect(counterReducer(previousState, decrement())).toEqual({\n        value: 2,\n        status: 'idle'\n      });\n    });\n  });\n\n  describe('incrementByAmount', () => {\n    it('should increment by the specified amount', () => {\n      const previousState = { value: 10, status: 'idle' };\n      expect(counterReducer(previousState, incrementByAmount(5))).toEqual({\n        value: 15,\n        status: 'idle'\n      });\n    });\n\n    it('should handle zero increment', () => {\n      const previousState = { value: 7, status: 'idle' };\n      expect(counterReducer(previousState, incrementByAmount(0))).toEqual({\n        value: 7,\n        status: 'idle'\n      });\n    });\n  });\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Testing with Payload Data"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Many actions carry payload data. Your tests should verify that the reducer correctly uses this payload to update state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing Payload Actions",
        "content": "Testing actions with payload data",
        "language": "javascript",
        "code": "// Assuming a todo slice\nimport todosReducer, {\n  todoAdded,\n  todoToggled,\n  todoDeleted,\n  todoUpdated\n} from './todosSlice';\n\ndescribe('todos reducer', () => {\n  const initialState = {\n    items: [],\n    filter: 'all'\n  };\n\n  describe('todoAdded', () => {\n    it('should add a new todo with correct structure', () => {\n      const action = todoAdded({\n        id: 'todo-1',\n        text: 'Buy groceries',\n        completed: false\n      });\n      \n      const result = todosReducer(initialState, action);\n      \n      expect(result.items).toHaveLength(1);\n      expect(result.items[0]).toMatchObject({\n        id: 'todo-1',\n        text: 'Buy groceries',\n        completed: false\n      });\n    });\n  });\n\n  describe('todoUpdated', () => {\n    it('should update todo text', () => {\n      const stateWithTodo = {\n        items: [\n          { id: 'todo-1', text: 'Old text', completed: false }\n        ],\n        filter: 'all'\n      };\n      \n      const action = todoUpdated({\n        id: 'todo-1',\n        changes: { text: 'New text' }\n      });\n      \n      const result = todosReducer(stateWithTodo, action);\n      expect(result.items[0].text).toBe('New text');\n    });\n\n    it('should toggle todo completed status', () => {\n      const stateWithTodo = {\n        items: [\n          { id: 'todo-1', text: 'Task', completed: false }\n        ],\n        filter: 'all'\n      };\n      \n      const action = todoToggled('todo-1');\n      const result = todosReducer(stateWithTodo, action);\n      \n      expect(result.items[0].completed).toBe(true);\n    });\n  });\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Assertion Types"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Assertion",
          "Use Case",
          "Example"
        ],
        "rows": [
          [
            "toEqual",
            "Deep equality for objects/arrays",
            "toEqual({ value: 5 })"
          ],
          [
            "toStrictEqual",
            "Like toEqual but stricter (checks undefined)",
            "toStrictEqual({ value: 5 })"
          ],
          [
            "toMatchObject",
            "Partial matching, object contains",
            "toMatchObject({ id: '1' })"
          ],
          [
            "toHaveLength",
            "Array/string length",
            "toHaveLength(3)"
          ],
          [
            "toContain",
            "Array/string includes item",
            "toContain(todo)"
          ],
          [
            "toBeTruthy/toBeFalsy",
            "Boolean checks",
            "toBeTruthy()"
          ],
          [
            "toBe",
            "Same reference (primitives)",
            "toBe(5)"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Testing Entity Adapters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Reducers using entity adapters have consistent patterns. You can test the adapter operations directly without worrying about implementation details."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing Entity Adapter Reducers",
        "content": "Testing slices with entity adapters",
        "language": "javascript",
        "code": "import usersReducer, {\n  userAdded,\n  userUpdated,\n  userRemoved,\n  usersReceived\n} from './usersSlice';\n\ndescribe('users reducer', () => {\n  const initialState = {\n    ids: [],\n    entities: {},\n    loading: false\n  };\n\n  describe('userAdded', () => {\n    it('should add a user to empty state', () => {\n      const user = { id: 'user-1', name: 'Alice', email: 'alice@example.com' };\n      const result = usersReducer(initialState, userAdded(user));\n      \n      expect(result.ids).toContain('user-1');\n      expect(result.entities['user-1']).toEqual(user);\n    });\n\n    it('should add multiple users with addMany equivalent', () => {\n      // Using addOne multiple times works like addMany\n      let result = usersReducer(initialState, userAdded({ id: 'u1', name: 'A' }));\n      result = usersReducer(result, userAdded({ id: 'u2', name: 'B' }));\n      \n      expect(result.ids).toHaveLength(2);\n      expect(Object.keys(result.entities)).toHaveLength(2);\n    });\n  });\n\n  describe('usersReceived', () => {\n    it('should replace all users with new set', () => {\n      const stateWithUsers = {\n        ids: ['old-1'],\n        entities: { 'old-1': { id: 'old-1', name: 'Old' } },\n        loading: false\n      };\n      \n      const newUsers = [\n        { id: 'new-1', name: 'New One' },\n        { id: 'new-2', name: 'New Two' }\n      ];\n      \n      const result = usersReducer(stateWithUsers, usersReceived(newUsers));\n      \n      expect(result.ids).toHaveLength(2);\n      expect(result.entities['new-1']).toBeDefined();\n      expect(result.entities['old-1']).toBeUndefined();\n    });\n  });\n\n  describe('userUpdated', () => {\n    it('should partially update a user', () => {\n      const stateWithUser = {\n        ids: ['user-1'],\n        entities: { 'user-1': { id: 'user-1', name: 'Alice', role: 'user' } },\n        loading: false\n      };\n      \n      const result = usersReducer(\n        stateWithUser,\n        userUpdated({ id: 'user-1', changes: { role: 'admin' } })\n      );\n      \n      expect(result.entities['user-1'].role).toBe('admin');\n      expect(result.entities['user-1'].name).toBe('Alice'); // Unchanged\n    });\n  });\n\n  describe('userRemoved', () => {\n    it('should remove a user', () => {\n      const stateWithUser = {\n        ids: ['user-1'],\n        entities: { 'user-1': { id: 'user-1', name: 'Alice' } },\n        loading: false\n      };\n      \n      const result = usersReducer(stateWithUser, userRemoved('user-1'));\n      \n      expect(result.ids).not.toContain('user-1');\n      expect(result.entities['user-1']).toBeUndefined();\n    });\n  });\n});"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Reducer Test Checklist",
        "items": [
          "Test initial state when state is undefined",
          "Test each action type with typical input",
          "Test each action type with edge cases (empty, zero, max values)",
          "Test that original state is not mutated",
          "Test multiple sequential actions",
          "Test that extra state fields are preserved",
          "Use descriptive test names that explain what is being tested"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "title": "Key Terms",
        "terms": [
          {
            "term": "Pure Function",
            "definition": "A function that always returns the same output for the same input and has no side effects."
          },
          {
            "term": "Unit Test",
            "definition": "A test that verifies a single unit of code in isolation."
          },
          {
            "term": "Reducer",
            "definition": "A pure function that takes the current state and an action, returning the next state."
          },
          {
            "term": "Deep Equality",
            "definition": "Checking that all nested properties of objects are equal, not just the reference."
          },
          {
            "term": "Immutability",
            "definition": "Not modifying the original state, but returning a new state object."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Write Reducer Tests",
        "description": "Write comprehensive tests for a shopping cart slice with add, remove, update quantity, and clear operations."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Reducers are pure functions and easy to test",
          "Test initial state first with undefined input",
          "Cover each action type with typical and edge cases",
          "Use descriptive test names that explain the scenario",
          "Verify immutability by checking original state is unchanged",
          "Group related tests with describe blocks for organization"
        ],
        "takeaway": "Reducer tests are fast, reliable, and give you confidence in your state management logic."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [
    {
      "title": "Redux Testing Documentation",
      "url": "https://redux.js.org/tutorials/fundamentals/part-8-modern-redux"
    },
    {
      "title": "Jest Documentation",
      "url": "https://jestjs.io/docs/expect"
    }
  ]
}
