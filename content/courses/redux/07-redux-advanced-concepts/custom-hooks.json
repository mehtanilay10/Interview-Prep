{
  "id": "lesson-redux-019",
  "slug": "custom-hooks",
  "moduleSlug": "redux-advanced-concepts",
  "title": "Custom Redux Hooks",
  "description": "Learn how to create custom hooks for reusable Redux logic, including patterns for authentication, lists, and async actions.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "redux",
    "hooks",
    "reusable",
    "react"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Custom hooks are the React-idiomatic way to **encapsulate and reuse** Redux logic across many components. Instead of repeating `useSelector` and `useDispatch` calls in every component that needs the same data or actions, you build one hook that returns a clean API."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Why Custom Hooks?",
        "text": "A custom hook is just a JavaScript function whose name starts with `use`. It can call other hooks inside it and return any value you want — state, derived data, or action dispatchers."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Problem: Repetitive Boilerplate"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Imagine five components that all need to read the current user and dispatch `logout`. Without custom hooks, every component repeats the same `useSelector` and `useDispatch` calls — and any change to that logic has to be applied in five places."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Repeated boilerplate",
        "content": "Before custom hooks",
        "language": "jsx",
        "code": "function Header() {\n  const user = useSelector(selectUser);\n  const dispatch = useDispatch();\n  const isAuthenticated = !!user;\n  return (\n    <button onClick={() => dispatch(logout())}>Logout</button>\n  );\n}\n\nfunction Sidebar() {\n  const user = useSelector(selectUser);\n  const dispatch = useDispatch();\n  // ...same code again\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Building Your First Custom Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Let's build `useAuth`. It returns the user, an `isAuthenticated` boolean, a guard function, and a `logout` action — everything a component needs in one call."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useAuth hook",
        "content": "Encapsulating auth logic",
        "language": "javascript",
        "code": "import { useSelector, useDispatch } from 'react-redux';\nimport { selectUser, logout } from './authSlice';\n\nexport function useAuth() {\n  const dispatch = useDispatch();\n  const user = useSelector(selectUser);\n\n  const isAuthenticated = !!user;\n\n  const requireAuth = () => {\n    if (!isAuthenticated) {\n      throw new Error('Unauthorized — please log in');\n    }\n  };\n\n  return {\n    user,\n    isAuthenticated,\n    requireAuth,\n    logout: () => dispatch(logout()),\n  };\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using the Hook in Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Component usage",
        "content": "Reading auth state with one line",
        "language": "jsx",
        "code": "import { useAuth } from './hooks/useAuth';\n\nfunction Dashboard() {\n  const { user, isAuthenticated, logout } = useAuth();\n\n  if (!isAuthenticated) return <Login />;\n\n  return (\n    <div>\n      <h1>Welcome {user.name}</h1>\n      <button onClick={logout}>Log out</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Comparing Reuse Patterns"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Three ways to reuse Redux logic",
        "cards": [
          {
            "title": "Custom Hook",
            "description": "Plain function that calls `useSelector`/`useDispatch`. Returns any shape you want.",
            "pros": [
              "Idiomatic React",
              "Zero nesting — just call and destructure",
              "Tree-shakeable and easy to test"
            ],
            "cons": [
              "Must follow Rules of Hooks"
            ]
          },
          {
            "title": "Higher-Order Component",
            "description": "Wraps a component, passing store data as props.",
            "pros": [
              "Works in class components",
              "Familiar pattern from older React"
            ],
            "cons": [
              "Adds extra nesting (prop drilling through wrappers)",
              "Harder to compose multiple HOCs",
              "Verbose with TypeScript"
            ]
          },
          {
            "title": "Render Props",
            "description": "Component accepts a function as a child and calls it with store data.",
            "pros": [
              "Very flexible — full control of render",
              "Works without hooks"
            ],
            "cons": [
              "Awkward JSX",
              "Easy to overuse",
              "Mostly replaced by hooks"
            ]
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "custom-hooks-reuse",
        "caption": "One hook powers many components, each subscribing to the same Redux store.",
        "definition": "flowchart LR\n  A[Shared Logic\nuseAuth / useCart] -->|imported by| B[Header]\n  A -->|imported by| C[Dashboard]\n  A -->|imported by| D[Checkout]\n  A -->|imported by| E[Profile]\n  B -.uses.-> F[Redux Store]\n  C -.uses.-> F\n  D -.uses.-> F\n  E -.uses.-> F"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Hooks That Wrap Async Logic"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Custom hooks shine when wrapping `createAsyncThunk` calls. They can read the current request status and expose convenient `submit` functions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Hook for an async form",
        "content": "useLogin hides loading/error state",
        "language": "javascript",
        "code": "import { useDispatch, useSelector } from 'react-redux';\nimport { login, selectAuth } from './authSlice';\n\nexport function useLogin() {\n  const dispatch = useDispatch();\n  const { loading, error, token } = useSelector(selectAuth);\n\n  const submit = (credentials) => dispatch(login(credentials));\n\n  return { submit, loading, error, token };\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "title": "Key Terms",
        "terms": [
          {
            "term": "Custom Hook",
            "definition": "A JavaScript function starting with `use` that may call other hooks and returns any value."
          },
          {
            "term": "Rules of Hooks",
            "definition": "Only call hooks at the top level and from React functions or other hooks."
          },
          {
            "term": "Encapsulation",
            "definition": "Hiding internal Redux calls behind a clean API so components don't need to know about selectors or action creators."
          },
          {
            "term": "Selector",
            "definition": "A function that reads a slice of Redux state. Often passed to `useSelector`."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Best Practices"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Keep hooks focused — one hook, one concern (auth, cart, form)",
          "Return a stable, predictable shape (object with named keys)",
          "Memoize functions returned from hooks with `useCallback` if passed to memoized children",
          "Place hooks in a `src/hooks/` or `src/features/<feature>/hooks/` directory",
          "Write tests for hooks using `@testing-library/react`'s `renderHook`"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Naming",
        "text": "Always prefix with `use`. React will refuse to call your function if it doesn't follow this convention."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a `useTodos` hook",
        "description": "Create a custom hook called `useTodos` that:\n1. Reads the todos list from the store with `useSelector`.\n2. Returns `{ todos, addTodo, toggleTodo, removeTodo }`.\n3. The action functions should be wrapped in `useCallback` so they can be safely passed to memoized children.\n4. Write a tiny test with `renderHook` that confirms the hook returns an array.",
        "steps": [
          "Use `useDispatch()` once at the top of the hook.",
          "Import `useCallback` from React.",
          "`renderHook(() => useTodos())` returns a `result` you can read."
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Custom hooks encapsulate Redux reads and dispatches behind a clean API",
          "They replace HOCs and render props for modern React codebases",
          "One hook can be imported by many components, keeping logic DRY",
          "Combine `useSelector`, `useDispatch`, and `useCallback` for powerful hooks",
          "Always start hook names with `use`"
        ],
        "takeaway": "Custom hooks organize and reuse Redux logic in a React-native way."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "redux"
}
