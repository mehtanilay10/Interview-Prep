{
  "id": "lesson-redux-019",
  "slug": "custom-hooks",
  "moduleSlug": "redux-advanced-concepts",
  "title": "Custom Redux Hooks",
  "description": "Learn how to create custom hooks for reusable Redux logic.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 16,
  "tags": [
    "redux",
    "hooks",
    "reusable",
    "react"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Custom hooks let you encapsulate Redux logic and reuse it across components."
      }
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Creating Custom Hooks"
    },
    {
      "type": "example",
      "data": {
        "title": "Custom Hook",
        "content": "A custom hook for user authentication",
        "language": "javascript",
        "code": "import { useSelector, useDispatch } from 'react-redux';\nimport { selectUser, logout } from './authSlice';\n\nfunction useAuth() {\n  const dispatch = useDispatch();\n  const user = useSelector(selectUser);\n\n  const isAuthenticated = !!user;\n  const requireAuth = () => {\n    if (!isAuthenticated) {\n      throw new Error('Unauthorized');\n    }\n  };\n\n  return {\n    user,\n    isAuthenticated,\n    requireAuth,\n    logout: () => dispatch(logout())\n  };\n}\n\n// Usage in component\nfunction Dashboard() {\n  const { user, isAuthenticated, logout } = useAuth();\n  if (!isAuthenticated) return <Login />;\n  return <div>Welcome {user.name}</div>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Reusability",
        "text": "Custom hooks keep components clean and promote code reuse across your app."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Encapsulate Redux logic in hooks",
          "Combine selectors and actions",
          "Improve component readability",
          "Enable cross-component reuse"
        ],
        "takeaway": "Custom hooks organize and reuse Redux logic."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "redux"
}
