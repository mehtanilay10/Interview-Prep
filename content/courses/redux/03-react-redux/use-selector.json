{
  "id": "lesson-redux-009",
  "slug": "use-selector",
  "moduleSlug": "react-redux",
  "title": "useSelector",
  "description": "Learn how to read data from the Redux store.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "redux",
    "useSelector",
    "hooks"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useSelector** is your primary tool for reading data from the Redux store. It subscribes to the store and automatically updates your component whenever the selected data changes. Think of it as a query for your state."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How useSelector Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you call `useSelector`, you pass a function that receives the entire Redux state and returns the piece you need. The hook subscribes to store updates and triggers a re-render only when the selected value changes."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "subscription-flow",
        "caption": "useSelector Subscription Flow",
        "definition": "sequenceDiagram\n    participant Component\n    participant useSelector\n    participant Store\n    participant Reducer\n    Component->>useSelector: useSelector(selector)\n    useSelector->>Store: Subscribe to store\n    Store-->>useSelector: Initial state\n    useSelector-->>Component: Render with data\n    Note over Store: Action dispatched\n    Store->>Reducer: Pass action\n    Reducer->>Store: New state\n    Store->>useSelector: State changed\n    useSelector->>Component: Re-render with new data"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Usage"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic useSelector",
        "content": "Reading state from the store",
        "language": "jsx",
        "code": "import { useSelector } from 'react-redux';\n\nfunction UserProfile() {\n  const user = useSelector((state) => state.user);\n  const userName = useSelector((state) => state.user.name);\n  const userEmail = useSelector((state) => state.user.email);\n\n  return (\n    <div>\n      <h1>{userName}</h1>\n      <p>{userEmail}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Selector Patterns"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "There are several patterns for writing selectors. Each has trade-offs between simplicity, reusability, and performance."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Pattern",
          "Example",
          "Best For"
        ],
        "rows": [
          [
            "Inline Selector",
            "(state) => state.users.items",
            "Simple, one-off selections"
          ],
          [
            "Memoized Selector",
            "createSelector(...)",
            "Computed values from multiple slices"
          ],
          [
            "Parametric Selector",
            "(id) => (state) => state.items[id]",
            "Selecting items by ID"
          ],
          [
            "Root Selector",
            "(state) => state",
            "When you need full state access"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Memoized Selectors"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For expensive computations, use `createSelector` from Redux Toolkit. Memoized selectors only recompute when their inputs change, preventing unnecessary re-renders."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Memoized Selectors",
        "content": "Using createSelector for performance",
        "language": "javascript",
        "code": "import { createSelector } from '@reduxjs/toolkit';\n\n// Base selectors\nconst selectUsers = (state) => state.users.items;\nconst selectActiveFilter = (state) => state.users.activeFilter;\n\n// Memoized selector - only recomputes when users or filter changes\nconst selectFilteredUsers = createSelector(\n  [selectUsers, selectActiveFilter],\n  (users, filter) => {\n    console.log('Computing filtered users...');\n    if (filter === 'all') return users;\n    return users.filter(user => user.role === filter);\n  }\n);\n\n// In a component\nfunction UserList() {\n  const filteredUsers = useSelector(selectFilteredUsers);\n  return (\n    <ul>\n      {filteredUsers.map(user => (\n        <li key={user.id}>{user.name}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Selector",
            "definition": "A function that extracts a specific piece of data from the Redux state."
          },
          {
            "term": "Memoization",
            "definition": "Caching the result of a computation so it's only recalculated when inputs change."
          },
          {
            "term": "Subscription",
            "definition": "A mechanism where components register to be notified when store state changes."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useSelector vs connect"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "cards": [
          {
            "title": "useSelector (Hooks)",
            "content": "Modern approach. Clean, functional syntax. Inline selector or custom hook. Part of react-redux v7+.",
            "highlight": true
          },
          {
            "title": "connect (HOC)",
            "content": "Legacy approach. Uses Higher-Order Component pattern. Verbose but explicit. Maintained for backward compatibility.",
            "highlight": false
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "connect vs useSelector",
        "content": "Comparing both approaches",
        "language": "javascript",
        "code": "// Old approach with connect (verbose)\nimport { connect } from 'react-redux';\n\nconst UserProfile = ({ user, isLoading }) => (\n  <div>{isLoading ? 'Loading...' : user.name}</div>\n);\n\nconst mapStateToProps = (state) => ({\n  user: state.user.data,\n  isLoading: state.user.isLoading,\n});\n\nexport default connect(mapStateToProps)(UserProfile);\n\n// Modern approach with hooks (clean)\nimport { useSelector } from 'react-redux';\n\nexport function UserProfile() {\n  const user = useSelector((state) => state.user.data);\n  const isLoading = useSelector((state) => state.user.isLoading);\n  return <div>{isLoading ? 'Loading...' : user.name}</div>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Shallow Equality",
        "text": "useSelector uses strict equality (===) by default. If your selector returns an object or array, the component will re-render on every dispatch even if the values haven't changed. Return primitives or use shallowEqual."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Avoiding Unnecessary Re-renders"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Return primitive values (strings, numbers, booleans) when possible",
          "Use `createSelector` for memoized computed values",
          "Split selectors so each component only subscribes to what it needs",
          "Use `useShallowSelector` or pass `shallowEqual` as second argument"
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Create a Filterable User List",
        "description": "Build a user list component that filters users based on a selected role. Practice using memoized selectors and multiple useSelector calls."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multiple Selectors"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple useSelector Calls",
        "content": "Using multiple selectors in one component",
        "language": "jsx",
        "code": "import { useSelector } from 'react-redux';\nimport { selectUser, selectUserPermissions } from './selectors';\n\nfunction Dashboard() {\n  // Each useSelector call is independent\n  const user = useSelector(selectUser);\n  const permissions = useSelector(selectUserPermissions);\n  const theme = useSelector((state) => state.ui.theme);\n  const notifications = useSelector((state) => state.notifications.unread);\n\n  return (\n    <div className={theme}>\n      <h1>Welcome, {user.name}</h1>\n      <p>You have {notifications} new notifications</p>\n      {permissions.canEdit && <EditButton />}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useSelector reads data from the Redux store and subscribes to updates",
          "The selector function receives the entire state and returns the needed slice",
          "Memoized selectors with createSelector prevent unnecessary computations",
          "useSelector uses strict equality (===) - return primitives when possible",
          "Modern hooks approach is cleaner than the legacy connect HOC"
        ],
        "takeaway": "useSelector is the primary hook for reading Redux state in React components, enabling efficient and reactive state access."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [
    {
      "title": "Official useSelector Documentation",
      "url": "https://react-redux.js.org/api/hooks#useselector"
    },
    {
      "title": "Redux Toolkit createSelector",
      "url": "https://redux-toolkit.js.org/api/createSelector"
    }
  ],
  "courseSlug": "redux"
}
