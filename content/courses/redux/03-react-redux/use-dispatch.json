{
  "id": "lesson-redux-010",
  "slug": "use-dispatch",
  "moduleSlug": "react-redux",
  "title": "useDispatch",
  "description": "Learn how to dispatch actions to the Redux store.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": [
    "react",
    "redux",
    "useDispatch",
    "hooks"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useDispatch** returns the Redux store's `dispatch` function. This is how components send actions to update the state. Without dispatch, your components couldn't change anything in the store."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How dispatch Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you dispatch an action, Redux passes it through the middleware chain and then to the reducer. The reducer returns a new state based on the action type. The flow goes: **Component** → **dispatch(action)** → **Middleware** → **Reducer** → **New State**."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "dispatch-flow",
        "caption": "Action Dispatch Flow in Redux",
        "definition": "flowchart LR\n    A[Component] -->|dispatch| B[Middleware]\n    B -->|action| C[Reducer]\n    C -->|new state| D[Store]\n    D -->|notify| E[Subscribed Components]\n    style A fill:#e1eff6\n    style C fill:#d4edda\n    style D fill:#fff3cd"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Usage"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic useDispatch",
        "content": "Dispatching actions in a component",
        "language": "jsx",
        "code": "import { useDispatch } from 'react-redux';\nimport { increment, decrement, reset } from './counterSlice';\n\nfunction Counter() {\n  const dispatch = useDispatch();\n  const count = useSelector((state) => state.counter.value);\n\n  return (\n    <div>\n      <h2>Count: {count}</h2>\n      <button onClick={() => dispatch(increment())}>+</button>\n      <button onClick={() => dispatch(decrement())}>-</button>\n      <button onClick={() => dispatch(reset())}>Reset</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Dispatch Patterns"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Pattern",
          "Code",
          "When to Use"
        ],
        "rows": [
          [
            "Inline dispatch",
            "dispatch(increment())",
            "Simple, one-time actions"
          ],
          [
            "Callback dispatch",
            "onClick={() => dispatch(action())}",
            "Event handlers"
          ],
          [
            "useCallback + dispatch",
            "memoized callbacks",
            "Performance optimization"
          ],
          [
            "Typed dispatch",
            "const dispatch: AppDispatch = useDispatch()",
            "TypeScript projects"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Typed Dispatch (TypeScript)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Typed Dispatch",
        "content": "Using typed dispatch with Redux Toolkit",
        "language": "typescript",
        "code": "import { useDispatch } from 'react-redux';\nimport type { AppDispatch } from './store';\n\n// Option 1: Type the dispatch directly\nfunction MyComponent() {\n  const dispatch = useDispatch<AppDispatch>();\n  \n  const handleAdd = (item: string) => {\n    dispatch(addItem(item)); // Fully typed!\n  };\n}\n\n// Option 2: Create a typed hook\nimport { useDispatch, useSelector } from 'react-redux';\nimport type { RootState, AppDispatch } from './store';\n\nexport const useAppDispatch = () => useDispatch<AppDispatch>();\nexport const useAppSelector = <T>(selector: (state: RootState) => T) => \n  useSelector(selector);\n\n// Then use it like:\nfunction AnotherComponent() {\n  const dispatch = useAppDispatch();\n  dispatch(addItem('hello'));\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key Terms"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "dispatch",
            "definition": "The function that sends actions to the Redux store to trigger state updates."
          },
          {
            "term": "Action",
            "definition": "A plain JavaScript object with a 'type' field that describes what happened."
          },
          {
            "term": "Action Creator",
            "definition": "A function that creates and returns an action, making actions reusable."
          },
          {
            "term": "Reducer",
            "definition": "A pure function that takes the current state and an action, returning the next state."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Dispatch vs Action Creators"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "cards": [
          {
            "title": "Direct dispatch({ type: ... })",
            "content": "Quick and simple for one-off actions. Good for learning. Harder to test and reuse.",
            "highlight": false
          },
          {
            "title": "Action Creators (Recommended)",
            "content": "Functions that return actions. Reusable, testable, type-safe. Best practice for production apps.",
            "highlight": true
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Action Creators",
        "content": "Creating and using action creators",
        "language": "javascript",
        "code": "// slice.js - Define action creators with Redux Toolkit\nimport { createSlice } from '@reduxjs/toolkit';\n\nconst todosSlice = createSlice({\n  name: 'todos',\n  initialState: { items: [], filter: 'all' },\n  reducers: {\n    addTodo: (state, action) => {\n      state.items.push({\n        id: Date.now(),\n        text: action.payload,\n        completed: false,\n      });\n    },\n    toggleTodo: (state, action) => {\n      const todo = state.items.find(t => t.id === action.payload);\n      if (todo) todo.completed = !todo.completed;\n    },\n    setFilter: (state, action) => {\n      state.filter = action.payload;\n    },\n  },\n});\n\nexport const { addTodo, toggleTodo, setFilter } = todosSlice.actions;\nexport default todosSlice.reducer;\n\n// Component.jsx - Use the action creators\nimport { useDispatch } from 'react-redux';\nimport { addTodo, toggleTodo, setFilter } from './todosSlice';\n\nfunction TodoApp() {\n  const dispatch = useDispatch();\n  const [newTodo, setNewTodo] = useState('');\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    if (newTodo.trim()) {\n      dispatch(addTodo(newTodo)); // Action creator returns the action\n      setNewTodo('');\n    }\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input \n        value={newTodo} \n        onChange={(e) => setNewTodo(e.target.value)}\n        placeholder=\"Add a todo...\"\n      />\n      <button type=\"submit\">Add</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Stable Reference",
        "text": "The dispatch function returned by useDispatch is stable across renders. Unlike useSelector, storing it in a variable won't cause re-renders unless you use it in a dependency array."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Form with useDispatch"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Contact Form",
        "content": "Building a form with useDispatch",
        "language": "jsx",
        "code": "import { useState } from 'react';\nimport { useDispatch } from 'react-redux';\nimport { submitContactForm } from './contactSlice';\n\nfunction ContactForm() {\n  const dispatch = useDispatch();\n  const [formData, setFormData] = useState({\n    name: '',\n    email: '',\n    message: '',\n  });\n\n  const handleChange = (e) => {\n    const { name, value } = e.target;\n    setFormData(prev => ({ ...prev, [name]: value }));\n  };\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    // Dispatch the action with form data as payload\n    dispatch(submitContactForm(formData));\n    setFormData({ name: '', email: '', message: '' });\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        name=\"name\"\n        value={formData.name}\n        onChange={handleChange}\n        placeholder=\"Your name\"\n        required\n      />\n      <input\n        name=\"email\"\n        type=\"email\"\n        value={formData.email}\n        onChange={handleChange}\n        placeholder=\"your@email.com\"\n        required\n      />\n      <textarea\n        name=\"message\"\n        value={formData.message}\n        onChange={handleChange}\n        placeholder=\"Your message...\"\n        required\n      />\n      <button type=\"submit\">Send Message</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Shopping Cart",
        "description": "Create a shopping cart with add, remove, and quantity update functionality. Practice dispatching different action types and handling payloads."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Not importing the correct action creators from the slice",
          "Passing wrong payload structure to action creators",
          "Forgetting to wrap dispatch calls in event handlers",
          "Not handling async actions properly (use thunk middleware)"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useDispatch returns the store's dispatch function for sending actions",
          "Dispatch an action to trigger state updates through the reducer",
          "Always use action creators for reusable, testable actions",
          "Dispatch is stable across renders - store without causing re-renders",
          "Use typed dispatch (AppDispatch) in TypeScript projects"
        ],
        "takeaway": "useDispatch is how React components communicate with the Redux store to update application state."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [
    {
      "title": "Official useDispatch Documentation",
      "url": "https://react-redux.js.org/api/hooks#usedispatch"
    },
    {
      "title": "Redux Style Guide - dispatching Actions",
      "url": "https://redux.js.org/style-guide/style-guide#write-action-creators-as-functions"
    }
  ],
  "courseSlug": "redux"
}
