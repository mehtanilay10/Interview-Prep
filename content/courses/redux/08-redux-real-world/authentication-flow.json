{
  "id": "lesson-redux-023",
  "slug": "authentication-flow",
  "moduleSlug": "redux-real-world",
  "title": "Authentication Flow",
  "description": "Implement a complete authentication flow using Redux Toolkit.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 24,
  "tags": [
    "redux",
    "authentication",
    "auth",
    "middleware"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Authentication is a common real-world use case. Redux Toolkit makes managing auth state straightforward."
      }
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Auth Slice"
    },
    {
      "type": "example",
      "data": {
        "title": "Auth Slice",
        "content": "Authentication state management",
        "language": "javascript",
        "code": "import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';\n\nexport const login = createAsyncThunk(\n  'auth/login',\n  async (credentials, { rejectWithValue }) => {\n    try {\n      const response = await fetch('/api/login', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(credentials)\n      });\n      const data = await response.json();\n      if (!response.ok) throw data;\n      return data;\n    } catch (err) {\n      return rejectWithValue(err);\n    }\n  }\n);\n\nconst authSlice = createSlice({\n  name: 'auth',\n  initialState: { user: null, token: null, loading: false, error: null },\n  reducers: {\n    logout: (state) => {\n      state.user = null;\n      state.token = null;\n    }\n  },\n  extraReducers: (builder) => {\n    builder\n      .addCase(login.pending, (state) => {\n        state.loading = true;\n        state.error = null;\n      })\n      .addCase(login.fulfilled, (state, action) => {\n        state.loading = false;\n        state.user = action.payload.user;\n        state.token = action.payload.token;\n      })\n      .addCase(login.rejected, (state, action) => {\n        state.loading = false;\n        state.error = action.payload;\n      });\n  }\n});\n\nexport const { logout } = authSlice.actions;\nexport default authSlice.reducer;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Token Storage",
        "text": "Store tokens in memory or httpOnly cookies. Avoid localStorage for sensitive tokens."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use createAsyncThunk for auth requests",
          "Track loading and error states",
          "Clear state on logout",
          "Protect routes based on auth state"
        ],
        "takeaway": "Redux Toolkit simplifies auth state management."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "redux"
}
