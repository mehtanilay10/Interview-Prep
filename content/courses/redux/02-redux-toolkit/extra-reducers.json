{
  "id": "lesson-redux-027",
  "slug": "extra-reducers",
  "moduleSlug": "redux-toolkit",
  "courseSlug": "redux",
  "title": "extraReducers",
  "description": "Learn how to use extraReducers to handle actions from other slices and async thunks.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "redux",
    "toolkit",
    "extra-reducers",
    "slices"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**extraReducers** lets a slice respond to actions defined in other slices — including lifecycle actions from `createAsyncThunk`. This is the key mechanism for cross-slice communication in Redux Toolkit."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why extraReducers?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In classic Redux, reducers only respond to their own action types. But real apps need to react to actions from other features. For example, when a `fetchUser` thunk succeeds, the `todos` slice might want to load that user's todos. `extraReducers` makes this possible."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Builder Pattern"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`createSlice` provides an `extraReducers` field that accepts a builder function. The builder has an `addCase` method for registering handlers for specific action types. It also supports `addMatcher` and `addDefaultCase` for advanced scenarios."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic extraReducers",
        "content": "Responding to another slice's action",
        "language": "javascript",
        "code": "import { createSlice } from '@reduxjs/toolkit';\nimport { increment } from '../counter/counterSlice';\n\nconst todosSlice = createSlice({\n  name: 'todos',\n  initialState: { items: [] },\n  reducers: {\n    addTodo: (state, action) => {\n      state.items.push(action.payload);\n    }\n  },\n  extraReducers: (builder) => {\n    builder.addCase(increment, (state) => {\n      // React to the counter incrementing\n      state.lastIncrementedAt = Date.now();\n    });\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Handling Async Thunk Lifecycle Actions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The most common use case for `extraReducers` is handling actions from `createAsyncThunk`. Since thunks dispatch `pending`, `fulfilled`, and `rejected` actions automatically, other slices can react to those lifecycle events."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Thunk Lifecycle in extraReducers",
        "content": "Handling fetchUsers thunk actions in another slice",
        "language": "javascript",
        "code": "import { createAsyncThunk, createSlice } from '@reduxjs/toolkit';\n\nexport const fetchUsers = createAsyncThunk(\n  'users/fetchUsers',\n  async () => {\n    const response = await fetch('/api/users');\n    return response.json();\n  }\n);\n\nconst notificationsSlice = createSlice({\n  name: 'notifications',\n  initialState: { message: null },\n  reducers: {\n    clearNotification: (state) => {\n      state.message = null;\n    }\n  },\n  extraReducers: (builder) => {\n    builder\n      .addCase(fetchUsers.pending, (state) => {\n        state.message = 'Loading users...';\n      })\n      .addCase(fetchUsers.fulfilled, (state) => {\n        state.message = 'Users loaded successfully!';\n      })\n      .addCase(fetchUsers.rejected, (state) => {\n        state.message = 'Failed to load users.';\n      });\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use extraReducers vs Regular Reducers"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Scenario",
          "Use regular reducers?",
          "Use extraReducers?"
        ],
        "rows": [
          [
            "Your slice creates the action",
            "Yes",
            "No"
          ],
          [
            "Another slice creates the action",
            "No",
            "Yes"
          ],
          [
            "createAsyncThunk lifecycle action",
            "No",
            "Yes"
          ],
          [
            "Third-party library action",
            "No",
            "Yes"
          ],
          [
            "Multiple slices need to react",
            "Varies",
            "Yes (in each reacting slice)"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using addMatcher"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`addMatcher` lets you handle actions conditionally. It takes a predicate function and a reducer. This is useful for filtering multiple action types or responding to patterns rather than specific actions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "addMatcher",
        "content": "Filtering actions by predicate",
        "language": "javascript",
        "code": "import { createSlice } from '@reduxjs/toolkit';\n\nconst logsSlice = createSlice({\n  name: 'logs',\n  initialState: [],\n  reducers: {},\n  extraReducers: (builder) => {\n    builder\n      .addMatcher(\n        (action) => action.type.endsWith('/fulfilled'),\n        (state, action) => {\n          state.push({\n            type: action.type,\n            time: Date.now(),\n            payload: action.payload\n          });\n        }\n      );\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using addDefaultCase"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`addDefaultCase` is the catch-all for any action not matched by `addCase` or `addMatcher`. Use it sparingly — explicit `addCase` calls are usually preferred for clarity and debugging."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "addDefaultCase",
        "content": "Logging all unhandled actions",
        "language": "javascript",
        "code": "const logsSlice = createSlice({\n  name: 'logs',\n  initialState: [],\n  reducers: {},\n  extraReducers: (builder) => {\n    builder.addDefaultCase((state, action) => {\n      state.push({ action: action.type, time: Date.now() });\n    });\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "State Shape After extraReducers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When using `extraReducers`, the slice's state shape remains exactly the same. You're simply adding more reaction paths to the same reducer function. The `initialState` and `reducers` define the core behavior, while `extraReducers` handles external influences."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-extrareducers-flow",
        "definition": "graph LR\n    A[Action Dispatched] --> B{Source?}\n    B -->|same slice| C[regular reducers]\n    B -->|other slice/thunk| D[extraReducers]\n    D --> E[addCase handler]\n    D --> F[addMatcher handler]\n    D --> G[addDefaultCase handler]\n    C --> H[new state]\n    E --> H\n    F --> H\n    G --> H\n    style A fill:#61dafb,stroke:#333,stroke-width:2px\n    style H fill:#4caf50,stroke:#333,stroke-width:2px\n    style D fill:#ff9800,stroke:#333,stroke-width:2px",
        "caption": "How extraReducers intercepts cross-slice actions"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "extraReducers",
            "definition": "Field on createSlice that handles actions from other slices, thunks, or external sources."
          },
          {
            "term": "builder.addCase",
            "definition": "Registers a reducer for a specific action type, typically used for thunk lifecycle actions."
          },
          {
            "term": "builder.addMatcher",
            "definition": "Registers a reducer for actions matching a predicate function, useful for pattern-based handling."
          },
          {
            "term": "builder.addDefaultCase",
            "definition": "Registers a reducer for any unmatched action, acting as a fallback."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Combining Regular and Extra Reducers"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Full Slice",
        "content": "A complete slice using reducers and extraReducers together",
        "language": "javascript",
        "code": "import { createAsyncThunk, createSlice } from '@reduxjs/toolkit';\nimport { fetchPosts } from '../posts/postsSlice';\n\nexport const fetchComments = createAsyncThunk(\n  'comments/fetchComments',\n  async (postId) => {\n    const res = await fetch(`/api/posts/${postId}/comments`);\n    return res.json();\n  }\n);\n\nconst commentsSlice = createSlice({\n  name: 'comments',\n  initialState: { items: [], postId: null, status: 'idle' },\n  reducers: {\n    setPostId: (state, action) => {\n      state.postId = action.payload;\n    }\n  },\n  extraReducers: (builder) => {\n    builder\n      .addCase(fetchComments.pending, (state) => {\n        state.status = 'loading';\n      })\n      .addCase(fetchComments.fulfilled, (state, action) => {\n        state.status = 'succeeded';\n        state.items = action.payload;\n      })\n      .addCase(fetchComments.rejected, (state) => {\n        state.status = 'failed';\n      })\n      .addCase(fetchPosts.fulfilled, (state, action) => {\n        if (action.payload.length > 0) {\n          state.postId = action.payload[0].id;\n        }\n      });\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Object Notation Alternative"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Instead of the builder function, you can pass `extraReducers` as an object with action type strings as keys. This is less common but still valid. The builder pattern is preferred for readability and TypeScript support."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Object Notation",
        "content": "Using extraReducers as an object",
        "language": "javascript",
        "code": "const slice = createSlice({\n  name: 'ui',\n  initialState: { visible: false },\n  reducers: {},\n  extraReducers: {\n    [fetchUsers.pending]: (state) => {\n      state.visible = true;\n    },\n    [fetchUsers.fulfilled]: (state) => {\n      state.visible = false;\n    }\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Trying to handle another slice's action in regular reducers (it won't work — use extraReducers)",
          "Forgetting that extraReducers handlers still need to follow Immer rules",
          "Using addDefaultCase when explicit addCase is clearer",
          "Duplicating logic between slices instead of extracting shared logic"
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Cross-Slice Communication",
        "description": "You have a `postsSlice` that fetches posts. Create a `uiSlice` that shows a loading spinner while posts are loading and shows an error message if they fail. Use `extraReducers` with `addCase` for the thunk lifecycle actions.",
        "steps": [
          "Import the fetchPosts thunk from postsSlice",
          "Use fetchPosts.pending, fetchPosts.fulfilled, and fetchPosts.rejected",
          "uiSlice state needs loading, error, and visible fields"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "extraReducers handles actions from other slices and thunks",
          "Use the builder pattern with addCase for type safety",
          "addMatcher enables pattern-based handling",
          "Most useful for reacting to async thunk lifecycle actions"
        ],
        "takeaway": "extraReducers enables cross-slice communication, making slices work together instead of in isolation."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
