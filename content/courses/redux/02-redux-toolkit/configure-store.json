{
  "id": "lesson-redux-005",
  "slug": "configure-store",
  "moduleSlug": "redux-toolkit",
  "courseSlug": "redux",
  "title": "configureStore",
  "description": "Learn how to set up a Redux store with Redux Toolkit's configureStore.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "redux",
    "toolkit",
    "store",
    "configure"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**configureStore** is the modern, recommended way to create a Redux store. It wraps the classic `createStore` with sensible defaults: DevTools integration, middleware setup, and development checks that catch common mistakes early."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why configureStore?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The old `createStore` method from Redux required manual setup for almost everything. You had to manually add reducers, apply middleware, and enable DevTools. `configureStore` bundles all of that into one clean call."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Store Setup"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Store",
        "content": "Creating a store with configureStore",
        "language": "javascript",
        "code": "import { configureStore } from '@reduxjs/toolkit';\nimport counterReducer from './counterSlice';\nimport userReducer from './userSlice';\n\nconst store = configureStore({\n  reducer: {\n    counter: counterReducer,\n    user: userReducer\n  }\n});\n\n// Type inference helpers\nexport type RootState = ReturnType<typeof store.getState>;\nexport type AppDispatch = typeof store.dispatch;\n\nexport default store;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "configureStore Options"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Option",
          "Type",
          "Description"
        ],
        "rows": [
          [
            "reducer",
            "Object | Function",
            "Reducer(s) for the store. Pass an object to combine multiple reducers."
          ],
          [
            "middleware",
            "Function",
            "Custom middleware array or function. Defaults to DevTools, thunk, and immutable checks."
          ],
          [
            "devTools",
            "boolean",
            "Enable/disable Redux DevTools. Auto-detected in development."
          ],
          [
            "preloadedState",
            "Object",
            "Initial state for the store, useful for SSR or hydration."
          ],
          [
            "enhancers",
            "Array",
            "Store enhancers. Rarely needed with configureStore."
          ],
          [
            "reducerManagement",
            "Object",
            "Advanced options for reducer injection and replacement."
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Redux DevTools Integration"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "One of the biggest wins with `configureStore` is automatic Redux DevTools Extension support. Install the [Redux DevTools Extension](https://github.com/reduxjs/redux-devtools) in your browser and you'll see every dispatched action and state change in real time."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "DevTools Configuration",
        "content": "Controlling DevTools behavior",
        "language": "javascript",
        "code": "const store = configureStore({\n  reducer: {\n    counter: counterReducer,\n    user: userReducer\n  },\n  devTools: process.env.NODE_ENV !== 'production'\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "DevTools in Production",
        "text": "Always disable DevTools in production for security and performance. configureStore handles this automatically when you use the pattern above."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Middleware Setup"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, `configureStore` includes three middleware: **serializableCheck** (warns about non-serializable values), **immutableCheck** (warns about state mutations), and **thunk** (enables async logic). You can customize this with the `middleware` option."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Custom Middleware",
        "content": "Adding custom middleware with getDefaultMiddleware",
        "language": "javascript",
        "code": "import { configureStore, getDefaultMiddleware } from '@reduxjs/toolkit';\nimport { logger } from 'redux-logger';\n\nconst store = configureStore({\n  reducer: {\n    counter: counterReducer,\n    user: userReducer\n  },\n  middleware: (getDefaultMiddleware) =>\n    getDefaultMiddleware().concat(logger)\n});"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "configureStore",
            "definition": "The primary Redux Toolkit function to create a Redux store with sensible defaults."
          },
          {
            "term": "DevTools",
            "definition": "Browser extension that visualizes state changes, dispatched actions, and time-travel debugging."
          },
          {
            "term": "Middleware",
            "definition": "Functions that intercept dispatched actions before they reach the reducer, enabling logging, async logic, and more."
          },
          {
            "term": "preloadedState",
            "definition": "Initial state passed to the store at creation time, useful for server-side rendering hydration."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Store Anatomy"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-redux-store-anatomy",
        "definition": "graph LR\n    A[dispatch action] --> B[middleware pipeline]\n    B --> C{is async?}\n    C -->|yes| D[thunk middleware]\n    C -->|no| E[root reducer]\n    D --> E\n    E --> F[new state]\n    F --> G[notify subscribers]\n    G --> H[DevTools records]\n    style A fill:#61dafb,stroke:#333,stroke-width:2px\n    style F fill:#4caf50,stroke:#333,stroke-width:2px\n    style H fill:#ff9800,stroke:#333,stroke-width:2px",
        "caption": "Data flow through a Redux Toolkit store"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "configureStore vs createStore"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "cards": [
          {
            "title": "createStore (Legacy)",
            "description": "The original Redux store creator. Requires manual middleware application, DevTools setup, and reducer combination. Deprecated and will be removed in future Redux versions.",
            "pros": [
              "Works with older Redux patterns",
              "Fully customizable from scratch"
            ],
            "cons": [
              "Boilerplate-heavy",
              "Easy to misconfigure",
              "No built-in checks",
              "Deprecated in Redux 5"
            ]
          },
          {
            "title": "configureStore (Modern)",
            "description": "The current standard. Provides ready-to-use DevTools, thunk middleware, and development checks out of the box.",
            "pros": [
              "Zero-config setup",
              "Auto-enabled DevTools",
              "Built-in best practices",
              "TypeScript inference support"
            ],
            "cons": [
              "Less low-level control",
              "May need customization for edge cases"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "TypeScript Integration"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Redux Toolkit is built with TypeScript in mind. The `configureStore` return type makes it easy to infer `RootState` and `AppDispatch` types, which you can use throughout your app for type-safe `useSelector` and `useDispatch` calls."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "TypeScript Store",
        "content": "Typed Redux store setup",
        "language": "typescript",
        "code": "import { configureStore } from '@reduxjs/toolkit';\nimport counterReducer from './counterSlice';\nimport userReducer from './userSlice';\n\nconst store = configureStore({\n  reducer: {\n    counter: counterReducer,\n    user: userReducer\n  }\n});\n\n// Infer types from the store itself\nexport type RootState = ReturnType<typeof store.getState>;\nexport type AppDispatch = typeof store.dispatch;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Combining Reducers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you pass an object to the `reducer` option, `configureStore` automatically combines them using `combineReducers`. Each key becomes a top-level state property. You don't need to call `combineReducers` yourself."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Combined Reducers",
        "content": "Store state shape from reducer object",
        "language": "javascript",
        "code": "const store = configureStore({\n  reducer: {\n    posts: postsReducer,\n    comments: commentsReducer,\n    users: usersReducer\n  }\n});\n\n// State shape:\n// {\n//   posts: postsState,\n//   comments: commentsState,\n//   users: usersState\n// }"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Preloaded State"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If you already have state data (for example, from a server-side render), you can pass it as `preloadedState`. This is especially useful when hydrating a Next.js or SSR app."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Preloaded State",
        "content": "Hydrating a store with initial data",
        "language": "javascript",
        "code": "const preloadedState = {\n  counter: { value: 5 }\n};\n\nconst store = configureStore({\n  reducer: {\n    counter: counterReducer,\n    user: userReducer\n  },\n  preloadedState\n});\n\n// store.getState().counter.value === 5"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Using `createStore` instead of `configureStore` (avoid the legacy API)",
          "Mutating state outside of Immer-enabled reducers",
          "Forgetting to export the store for tests or SSR",
          "Not disabling DevTools in production"
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build Your First Store",
        "description": "Create a Redux store with configureStore that has two reducers: `todos` and `filter`. Export `RootState` and `AppDispatch` types. Add a custom logger middleware after the default middleware.",
        "steps": [
          "Import configureStore and getDefaultMiddleware from @reduxjs/toolkit",
          "Pass an object with todos and filter keys to reducer",
          "Use middleware option to getDefaultMiddleware().concat(logger)"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "configureStore is the modern, recommended approach",
          "Auto-combines reducers passed as an object",
          "DevTools and thunk middleware included by default",
          "Use preloadedState for SSR hydration",
          "Export RootState and AppDispatch for type-safe hooks"
        ],
        "takeaway": "configureStore eliminates Redux boilerplate by bundling best practices into a single function call."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
