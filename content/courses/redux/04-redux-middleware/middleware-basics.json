{
  "id": "lesson-redux-011",
  "slug": "middleware-basics-redux",
  "moduleSlug": "redux-middleware",
  "title": "Middleware Basics",
  "description": "Understand how Redux middleware works.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "redux",
    "middleware",
    "extension",
    "side-effects"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Middleware** extends Redux's capabilities by intercepting actions before they reach the reducer. It's the way to add custom logic like logging, crash reporting, API calls, and routing to your Redux application."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How Middleware Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Middleware forms a **pipeline** around the dispatch function. When you dispatch an action, it flows through each middleware in order, giving each one a chance to inspect, modify, or stop the action before it reaches the reducer."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "middleware-pipeline",
        "caption": "Redux Middleware Pipeline Flow",
        "definition": "flowchart LR\n    A[dispatch\n    action] --> B[Middleware 1\n    logger]\n    B --> C[Middleware 2\n    thunk]\n    C --> D[Middleware 3\n    crash reporter]\n    D --> E[Reducer\n    pure function]\n    E --> F[New State]\n    style B fill:#e1eff6\n    style C fill:#d4edda\n    style D fill:#fff3cd\n    style E fill:#f8d7da"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Middleware Structure"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A Redux middleware is a curried function with three parameters: `store`, `next`, and `action`. This pattern allows middleware to access the store, pass control to the next middleware, or intercept actions."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Middleware Anatomy",
        "content": "Understanding the three-argument pattern",
        "language": "javascript",
        "code": "// Middleware signature: (store) => (next) => (action) => next(action)\n\nconst logger = (store) => (next) => (action) => {\n  // 1. Store: access state, dispatch, getState\n  console.log('Current state before:', store.getState());\n  console.log('Action:', action);\n\n  // 2. Next: pass action to next middleware or reducer\n  const result = next(action);\n\n  // 3. After reducer runs\n  console.log('State after:', store.getState());\n\n  // Return result for chaining\n  return result;\n};\n\n// If middleware doesn't call next(), action is STOPPED\nconst blocker = (store) => (next) => (action) => {\n  if (action.type === 'BLOCKED_ACTION') {\n    console.log('Blocking this action!');\n    return; // Action never reaches reducer\n  }\n  return next(action);\n};"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Middleware Use Cases"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Use Case",
          "Example Middleware",
          "Purpose"
        ],
        "rows": [
          [
            "Logging",
            "redux-logger",
            "Log all actions and state changes"
          ],
          [
            "Async",
            "redux-thunk, redux-saga",
            "Handle async operations"
          ],
          [
            "Crash Reporting",
            "redux-error-overlay",
            "Capture and report errors"
          ],
          [
            "Routing",
            "redux-first-history",
            "Sync with URL/routing"
          ],
          [
            "Persistence",
            "redux-persist",
            "Save state to localStorage"
          ],
          [
            "Analytics",
            "Custom middleware",
            "Track user actions"
          ]
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Middleware Pipeline",
            "definition": "An ordered chain of middleware functions that each get a chance to process actions."
          },
          {
            "term": "next",
            "definition": "A function that passes the action to the next middleware in the pipeline."
          },
          {
            "term": "Enhancer",
            "definition": "A function that enhances the store itself, including middleware and other capabilities."
          },
          {
            "term": "Side Effect",
            "definition": "Any operation that reaches outside the pure function world: API calls, timers, DOM manipulation."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Middleware vs Store Enhancers"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "cards": [
          {
            "title": "Middleware",
            "content": "Intercepts actions in the dispatch pipeline. Applied with applyMiddleware(). Best for cross-cutting concerns like logging and async operations.",
            "highlight": true
          },
          {
            "title": "Store Enhancers",
            "content": "Wraps the entire store creation. Can modify how store methods work at a deeper level. More powerful but rarely needed.",
            "highlight": false
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Writing a Logger Middleware"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Logger Middleware",
        "content": "Building a custom logging middleware",
        "language": "javascript",
        "code": "// Creating a more complete logger middleware\nconst loggerMiddleware = (store) => {\n  // Return the inner middleware\n  return (next) => {\n    // Return the innermost function (handles actions)\n    return (action) => {\n      // Add timestamp\n      const timestamp = new Date().toISOString();\n      const prevState = store.getState();\n\n      console.group(`Action @ ${timestamp}`);\n      console.log('Action type:', action.type);\n      console.log('Payload:', action.payload);\n      console.log('Previous state:', prevState);\n\n      // Call next to let action flow through\n      const returnValue = next(action);\n\n      console.log('Next state:', store.getState());\n      console.groupEnd();\n\n      // Return value for redux-thunk compatibility\n      return returnValue;\n    };\n  };\n};\n\n// Usage in store configuration\nimport { configureStore } from '@reduxjs/toolkit';\nimport rootReducer from './rootReducer';\n\nconst store = configureStore({\n  reducer: rootReducer,\n  middleware: (getDefaultMiddleware) =>\n    getDefaultMiddleware().concat(loggerMiddleware),\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Middleware Order Matters",
        "text": "The order you list middleware in the array matters. For example, thunk should come before logger so async actions are resolved before logging."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Async Middleware Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Async-Aware Middleware",
        "content": "A middleware that handles promises",
        "language": "javascript",
        "code": "// Middleware that handles promise actions\nconst asyncMiddleware = (store) => (next) => (action) => {\n  // If action is a function (thunk), call it with store\n  if (typeof action === 'function') {\n    return action(store.dispatch, store.getState);\n  }\n\n  // If action is a promise, resolve it and dispatch result\n  if (action.payload && typeof action.payload.then === 'function') {\n    return action.payload\n      .then((result) => {\n        store.dispatch({ ...action, payload: result });\n      })\n      .catch((error) => {\n        store.dispatch({ ...action, payload: error, error: true });\n      });\n  }\n\n  // Regular action - just pass through\n  return next(action);\n};\n\n// Usage: dispatch returns a promise\nstore.dispatch(fetchUser(123)).then(() => {\n  console.log('User loaded!');\n});"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Timing Middleware",
        "description": "Create a middleware that measures how long actions take to process and logs performance metrics. This is useful for identifying slow reducers."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Forgetting to call next(action) - action never reaches reducer",
          "Forgetting to return the result of next(action)",
          "Middleware order causing issues (e.g., logging before async resolves)",
          "Creating new middleware instances on every render",
          "Not handling edge cases like non-standard action shapes"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Middleware intercepts actions in the dispatch pipeline",
          "Structure: (store) => (next) => (action) => next(action)",
          "Use applyMiddleware() to add middleware to the store",
          "Middleware order matters - sequential execution",
          "Great for logging, async operations, crash reporting, and analytics"
        ],
        "takeaway": "Middleware is the extensibility point in Redux that allows you to add cross-cutting concerns like logging, async handling, and error reporting."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [
    {
      "title": "Redux Middleware Documentation",
      "url": "https://redux.js.org/understanding/history-and-architecture/middleware"
    },
    {
      "title": "Writing Custom Middleware",
      "url": "https://redux.js.org/tutorials/fundamentals/part-4-store#middleware"
    }
  ],
  "courseSlug": "redux"
}
