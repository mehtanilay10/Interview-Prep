{
  "id": "lesson-dp-repo-001",
  "slug": "repository-pattern",
  "moduleSlug": "04-enterprise-patterns",
  "title": "Repository Pattern",
  "description": "Abstract data access with a repository layer.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": ["design-patterns", "repository", "csharp", "data-access"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Repository Pattern**\n\nMediates between the domain and data mapping layers, acting like an in-memory collection of domain objects."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Use a Repository"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Centralizes data access logic",
          "Makes unit testing easier with fake repositories",
          "Decouples business logic from EF Core or ADO.NET",
          "Provides a clean, domain-friendly API"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generic Repository"
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "csharp",
        "code": "public interface IRepository<T> where T : class\n{\n    Task<T?> GetByIdAsync(int id);\n    Task<IEnumerable<T>> GetAllAsync();\n    Task AddAsync(T entity);\n    Task UpdateAsync(T entity);\n    Task DeleteAsync(int id);\n}\n\npublic class Repository<T> : IRepository<T> where T : class\n{\n    private readonly AppDbContext _context;\n    private readonly DbSet<T> _dbSet;\n\n    public Repository(AppDbContext context)\n    {\n        _context = context;\n        _dbSet = context.Set<T>();\n    }\n\n    public async Task<T?> GetByIdAsync(int id)\n        => await _dbSet.FindAsync(id);\n\n    public async Task<IEnumerable<T>> GetAllAsync()\n        => await _dbSet.ToListAsync();\n\n    public async Task AddAsync(T entity)\n    {\n        await _dbSet.AddAsync(entity);\n        await _context.SaveChangesAsync();\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Keep It Simple",
        "text": "A generic repository adds little over DbSet. Use it when you need testability or shared query logic."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Repository abstracts the data source",
          "Generic repositories reduce duplication",
          "Pair with Unit of Work for transaction management",
          "Use interfaces for testability"
        ],
        "takeaway": "Next, we'll coordinate multiple repositories with Unit of Work."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "design-patterns",
  "estimatedMinutes": 15
}
