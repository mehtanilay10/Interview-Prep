{
  "id": "lesson-dp-cqrs-001",
  "slug": "cqrs-basics",
  "moduleSlug": "04-enterprise-patterns",
  "title": "CQRS Basics",
  "description": "Separate read and write models for better scalability and clarity.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 15,
  "tags": [
    "design-patterns",
    "cqrs",
    "csharp",
    "architecture"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**CQRS (Command Query Responsibility Segregation)**\n\nSplits operations that read data (queries) from operations that write data (commands). Each side can use a different model optimized for its job."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Command Side (Writes)"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Commands represent intent (e.g., PlaceOrder)",
          "Command handlers execute business logic",
          "Write model is domain-rich and consistent",
          "Usually uses a normalized relational database"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query Side (Reads)"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Queries return DTOs optimized for the UI",
          "Read model can be denormalized",
          "Often uses a separate read database or cache",
          "Eventual consistency between read and write models"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "CQRS with MediatR"
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "csharp",
        "code": "// Command\npublic record PlaceOrderCommand(int CustomerId, List<OrderItem> Items)\n    : IRequest<Guid>;\n\npublic class PlaceOrderHandler : IRequestHandler<PlaceOrderCommand, Guid>\n{\n    public async Task<Guid> Handle(PlaceOrderCommand request, CancellationToken ct)\n    {\n        // Validate, apply business rules, save\n        var order = new Order { CustomerId = request.CustomerId };\n        await _orderRepository.AddAsync(order);\n        await _unitOfWork.CommitAsync();\n        return order.Id;\n    }\n}\n\n// Query\npublic record GetOrderDetailsQuery(Guid OrderId) : IRequest<OrderDetailsDto>;\n\npublic class GetOrderDetailsHandler : IRequestHandler<GetOrderDetailsQuery, OrderDetailsDto>\n{\n    public async Task<OrderDetailsDto> Handle(GetOrderDetailsQuery request, CancellationToken ct)\n    {\n        return await _readDbContext.OrdersDetails\n            .Where(o => o.Id == request.OrderId)\n            .Select(o => new OrderDetailsDto { ... })\n            .FirstAsync(ct);\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "When to Use CQRS",
        "text": "CQRS adds complexity. Start with it when you have distinct read/write workloads, need independent scaling, or have complex domain logic."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Commands write, Queries read — separate models",
          "Write side focuses on consistency",
          "Read side focuses on performance",
          "MediatR makes CQRS easy to implement"
        ],
        "takeaway": "Enterprise patterns help build maintainable, scalable systems."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "design-patterns"
}
