{
  "id": "lesson-dp-uow-001",
  "slug": "unit-of-work",
  "moduleSlug": "04-enterprise-patterns",
  "title": "Unit of Work Pattern",
  "description": "Coordinate multiple repositories in a single transaction.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": ["design-patterns", "unit-of-work", "csharp", "transactions"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Unit of Work**\n\nMaintains a list of objects affected by a business transaction and coordinates writing changes and concurrency resolution."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "EF Core Already Implements It"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`DbContext` is itself a Unit of Work. `SaveChangesAsync()` commits all tracked changes in one transaction."
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "csharp",
        "code": "public interface IUnitOfWork : IDisposable\n{\n    IRepository<Customer> Customers { get; }\n    IRepository<Order> Orders { get; }\n    Task<int> CommitAsync();\n}\n\npublic class UnitOfWork : IUnitOfWork\n{\n    private readonly AppDbContext _context;\n    public IRepository<Customer> Customers { get; }\n    public IRepository<Order> Orders { get; }\n\n    public UnitOfWork(AppDbContext context)\n    {\n        _context = context;\n        Customers = new Repository<Customer>(context);\n        Orders = new Repository<Order>(context);\n    }\n\n    public async Task<int> CommitAsync()\n        => await _context.SaveChangesAsync();\n\n    public void Dispose() => _context.Dispose();\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using Unit of Work"
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "csharp",
        "code": "public async Task PlaceOrderAsync(Order order)\n{\n    await _unitOfWork.Customers.GetByIdAsync(order.CustomerId);\n    await _unitOfWork.Orders.AddAsync(order);\n\n    // Commit everything atomically\n    await _unitOfWork.CommitAsync();\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Over-Engineering",
        "text": "For simple apps, injecting DbContext directly is fine. Introduce Unit of Work when you have multiple repositories and complex transactions."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Unit of Work batches changes into one transaction",
          "DbContext already acts as a Unit of Work",
          "Helps coordinate multiple repositories",
          "Use when transaction boundaries matter"
        ],
        "takeaway": "Next, we'll reduce dependencies with the Mediator pattern."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "design-patterns",
  "estimatedMinutes": 15
}
