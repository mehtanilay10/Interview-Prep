{
  "id": "lesson-dp-observer-pattern",
  "slug": "observer-pattern",
  "moduleSlug": "03-behavioral-patterns",
  "title": "Observer Pattern",
  "description": "Define a subscription mechanism for object changes.",
  "order": 1,
  "difficulty": "intermediate",
  "tags": [
    "design-patterns",
    "csharp",
    "architecture"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Observer Pattern**\n\nDefines a one-to-many dependency between objects so that when one object changes state, all its dependents are notified and updated automatically."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Real-World Example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "YouTube channel subscriptions - when a creator uploads a video, all subscribers get notified."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Observer in C#",
        "content": "// Subject interface\npublic interface ISubject\n{\n    void Attach(IObserver observer);\n    void Detach(IObserver observer);\n    void Notify();\n}\n\n// Observer interface\npublic interface IObserver\n{\n    void Update(string message);\n}\n\n// Concrete subject\npublic class NewsAgency : ISubject\n{\n    private readonly List<IObserver> _observers = new();\n    private string _news = \"\";\n    \n    public void Attach(IObserver observer) => _observers.Add(observer);\n    public void Detach(IObserver observer) => _observers.Remove(observer);\n    \n    public void Notify()\n    {\n        foreach (var observer in _observers)\n            observer.Update(_news);\n    }\n    \n    public void SetNews(string news)\n    {\n        _news = news;\n        Notify();\n    }\n}",
        "language": "csharp"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "One-to-many dependency",
          "Loose coupling",
          "Event-driven architecture",
          "Built into C# events"
        ],
        "takeaway": "Observer is fundamental for event-driven and reactive programming."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "design-patterns",
  "estimatedMinutes": 10
}
