{
  "id": "lesson-apollo-014",
  "slug": "local-state",
  "moduleSlug": "advanced-patterns",
  "title": "Local State Management",
  "description": "Learn how to manage local state with Apollo Client.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "apollo",
    "local",
    "state",
    "reactive-variables"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Apollo Client can manage local state alongside remote data. This eliminates the need for separate state management libraries."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Reactive Variables"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "makeVar",
        "content": "Creating reactive variables",
        "language": "jsx",
        "code": "import { makeVar } from '@apollo/client';\n\n// Create reactive variable\nexport const isLoggedInVar = makeVar(false);\nexport const currentUserVar = makeVar<User | null>(null);\n\n// Use in component\nfunction UserStatus() {\n  const isLoggedIn = useReactiveVar(isLoggedInVar);\n  const user = useReactiveVar(currentUserVar);\n\n  return (\n    <div>\n      {isLoggedIn ? `Hello, ${user?.name}` : 'Please log in'}\n    </div>\n  );\n}\n\n// Update anywhere\nisLoggedInVar(true);\ncurrentUserVar({ id: '1', name: 'Alice' });"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Local Fields"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Local Fields",
        "content": "Adding local fields to queries",
        "language": "jsx",
        "code": "const GET_USER = gql`\n  query GetUser($id: ID!) {\n    user(id: $id) {\n      id\n      name\n      isFollowing @client # Local field\n    }\n  }\n`;\n\nfunction UserProfile({ userId }) {\n  const { data } = useQuery(GET_USER, {\n    variables: { id: userId }\n  });\n\n  return (\n    <div>\n      <p>{data.user.name}</p>\n      <p>{data.user.isFollowing ? 'Following' : 'Not following'}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "@client Directive",
        "text": "The @client directive tells Apollo to resolve the field locally instead of sending it to the server."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Reactive variables for local state",
          "useReactiveVar hook in components",
          "@client directive for local fields",
          "Eliminates need for Redux"
        ],
        "takeaway": "Apollo can manage both remote and local state."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "apollo"
}
