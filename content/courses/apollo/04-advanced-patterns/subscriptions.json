{
  "id": "lesson-apollo-012",
  "slug": "subscriptions",
  "moduleSlug": "advanced-patterns",
  "title": "Subscriptions",
  "description": "Learn how to implement real-time updates with GraphQL subscriptions.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 22,
  "tags": ["apollo", "subscriptions", "websocket", "real-time"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**GraphQL subscriptions** enable real-time updates from server to client. Unlike polling, subscriptions push data when events occur."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Setup"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Subscription Setup",
        "content": "Setting up WebSocket link",
        "language": "jsx",
        "code": "import { split, HttpLink } from '@apollo/client';\nimport { getMainDefinition } from '@apollo/client/utilities';\nimport { WebSocketLink } from '@apollo/client/link/ws';\n\nconst httpLink = new HttpLink({\n  uri: 'https://api.example.com/graphql'\n});\n\nconst wsLink = new WebSocketLink({\n  uri: 'wss://api.example.com/graphql',\n  options: {\n    reconnect: true\n  }\n});\n\n// Split traffic based on operation type\nconst link = split(\n  ({ query }) => {\n    const definition = getMainDefinition(query);\n    return definition.kind === 'OperationDefinition' && definition.operation === 'subscription';\n  },\n  wsLink,\n  httpLink\n);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useSubscription"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useSubscription",
        "content": "Using subscription in component",
        "language": "jsx",
        "code": "const ON_USER_ADDED = gql`\n  subscription OnUserAdded {\n    userAdded {\n      id\n      name\n      email\n    }\n  }\n`;\n\nfunction UserList() {\n  const { data } = useSubscription(ON_USER_ADDED);\n\n  // data contains new user when subscription fires\n  return (\n    <div>\n      {data && <p>New user: {data.userAdded.name}</p>}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Alternatives",
        "text": "Consider using GraphQL over SSE (Server-Sent Events) for simpler real-time setups without WebSocket complexity."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Subscriptions enable real-time updates",
          "WebSocketLink handles WebSocket connection",
          "split routes operations to correct link",
          "useSubscription hook in components"
        ],
        "takeaway": "Subscriptions provide true real-time updates."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
