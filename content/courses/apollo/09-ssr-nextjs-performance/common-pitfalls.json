{
  "id": "lesson-apollo-035",
  "slug": "common-pitfalls",
  "moduleSlug": "ssr-nextjs-performance",
  "courseSlug": "apollo",
  "title": "Common Pitfalls",
  "description": "Avoid common pitfalls when using Apollo Client with Next.js SSR, SSG, and hydration.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["pitfalls", "nextjs", "hydration", "ssr", "ssg", "apollo-client", "best-practices"],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Hydration Mismatches"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Hydration mismatches occur when server-rendered HTML differs from client-rendered HTML. With Apollo Client, this happens when queries return different data on the server vs client. Ensure consistent cache initialization and avoid time-sensitive data in SSR."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Avoiding hydration mismatches",
        "content": "Use consistent cache initialization and avoid client-only data in SSR.",
        "language": "tsx",
        "code": "// Create a single Apollo Client instance\nlet apolloClient;\n\nfunction createApolloClient() {\n  if (typeof window === 'undefined') {\n    // Server: create new client per request\n    return new ApolloClient({\n      uri: process.env.NEXT_PUBLIC_GRAPHQL_URL,\n      cache: new InMemoryCache(),\n    });\n  }\n  // Client: reuse existing client\n  if (!apolloClient) {\n    apolloClient = new ApolloClient({\n      uri: '/graphql',\n      cache: new InMemoryCache(),\n    });\n  }\n  return apolloClient;\n}"
      }
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Other Common Pitfalls"
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": ["performance-optimization"]
}
