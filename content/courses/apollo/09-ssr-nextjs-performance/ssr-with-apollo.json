{
  "id": "lesson-apollo-032",
  "slug": "ssr-with-apollo",
  "moduleSlug": "ssr-nextjs-performance",
  "courseSlug": "apollo",
  "title": "SSR with Apollo",
  "description": "Implement server-side rendering with Apollo Client in Next.js using getServerSideProps.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["ssr", "getServerSideProps", "server-side-rendering", "nextjs", "apollo-client"],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Server-Side Rendering"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Server-side rendering fetches data on the server before sending HTML to the client. With Apollo Client, you can use `getServerSideProps` to execute queries on the server, hydrate the cache, and send the initial state to the client."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "SSR with getServerSideProps",
        "content": "Fetch data on the server and hydrate the Apollo cache.",
        "language": "tsx",
        "code": "import { ApolloClient, InMemoryCache, gql } from '@apollo/client';\n\nexport async function getServerSideProps() {\n  const client = new ApolloClient({\n    uri: process.env.NEXT_PUBLIC_GRAPHQL_URL,\n    cache: new InMemoryCache(),\n  });\n\n  const { data } = await client.query({\n    query: gql`query GetProducts { products { id name } }`,\n  });\n\n  return {\n    props: {\n      initialApolloState: client.cache.extract(),\n      products: data.products,\n    },\n  };\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "SSR Performance",
        "text": "SSR adds server-side latency. Only use SSR for pages that require fresh data on every request or need SEO optimization. For static content, prefer SSG."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "SSR Key Points",
        "points": [
          "SSR fetches data on the server before rendering.",
          "Hydrate the cache with initialApolloState.",
          "Use for SEO-critical or frequently updated pages.",
          "Balance SSR with performance costs."
        ],
        "takeaway": "SSR with Apollo Client enables fresh, SEO-friendly pages while maintaining client-side interactivity."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": ["nextjs-integration"]
}
