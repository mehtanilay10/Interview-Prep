{
  "id": "lesson-apollo-034",
  "slug": "performance-optimization",
  "moduleSlug": "ssr-nextjs-performance",
  "courseSlug": "apollo",
  "title": "Performance Optimization",
  "description": "Optimize Apollo Client performance in Next.js applications with selective fetching and caching strategies.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["performance", "optimization", "selective-fetching", "caching", "nextjs", "apollo-client"],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Selective Fetching"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In Next.js, different pages may need different data. Instead of fetching everything in a layout, use selective fetching in each page component. Apollo Client cache is shared across the app, so data fetched on one page is available on others."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Selective fetching in page components",
        "content": "Each page fetches only what it needs.",
        "language": "tsx",
        "code": "// products page\nconst ProductsPage = () => {\n  const { data } = useQuery(GET_PRODUCTS);\n  return <ProductList products={data?.products} />;\n};\n\n// product detail page\nconst ProductPage = ({ id }) => {\n  const { data } = useQuery(GET_PRODUCT, {\n    variables: { id },\n  });\n  return <ProductDetail product={data?.product} />;\n};"
      }
    },
    {
      "type": "callout",
      "tip": "Use Apollo Client cache to avoid duplicate requests. If the products page already fetched a product, the detail page will use the cached data.",
      "data": {
        "variant": "tip",
        "title": "Cache Sharing",
        "text": "Use Apollo Client cache to avoid duplicate requests. If the products page already fetched a product, the detail page will use the cached data."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Performance Optimization Key Points",
        "points": [
          "Fetch only what each page needs.",
          "Share cache across pages for free deduplication.",
          "Use cache-and-network for dynamic content.",
          "Monitor bundle size with Apollo Client."
        ],
        "takeaway": "Selective fetching combined with Apollo cache sharing delivers optimal performance in Next.js apps."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": ["ssg-hydration"]
}
