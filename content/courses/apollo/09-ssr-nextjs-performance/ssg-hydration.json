{
  "id": "lesson-apollo-033",
  "slug": "ssg-hydration",
  "moduleSlug": "ssr-nextjs-performance",
  "courseSlug": "apollo",
  "title": "SSG Hydration",
  "description": "Use static site generation with Apollo Client cache hydration for optimal performance.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 13,
  "tags": [
    "ssg",
    "getStaticProps",
    "hydration",
    "nextjs",
    "apollo-client",
    "static-site-generation"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Static Site Generation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Static site generation pre-renders pages at build time. With Apollo Client, you can fetch data during `getStaticProps`, hydrate the cache, and serve fully static HTML. This provides the best of both worlds: fast static pages with client-side interactivity."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "SSG with getStaticProps",
        "content": "Fetch data at build time and hydrate the cache.",
        "language": "tsx",
        "code": "export async function getStaticProps() {\n  const client = new ApolloClient({\n    uri: process.env.NEXT_PUBLIC_GRAPHQL_URL,\n    cache: new InMemoryCache(),\n  });\n\n  const { data } = await client.query({\n    query: GET_PRODUCTS,\n  });\n\n  return {\n    props: {\n      initialApolloState: client.cache.extract(),\n    },\n    revalidate: 60, // Revalidate every 60 seconds\n  };\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "ISR with Apollo",
        "text": "Use `revalidate` in getStaticProps for incremental static regeneration. Apollo cache is hydrated at build time and revalidated in the background."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "SSG Hydration Key Points",
        "points": [
          "SSG pre-renders pages at build time.",
          "Hydrate cache with initialApolloState.",
          "Use ISR for periodic data updates.",
          "Best for content that changes infrequently."
        ],
        "takeaway": "SSG with Apollo Client delivers the fastest user experience with minimal server load."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": [
    "ssr-with-apollo"
  ]
}
