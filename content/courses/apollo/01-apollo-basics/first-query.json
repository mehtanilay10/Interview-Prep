{
  "id": "lesson-apollo-003",
  "slug": "first-query",
  "moduleSlug": "apollo-basics",
  "title": "Your First Query",
  "description": "Learn how to fetch data with useQuery hook.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 18,
  "tags": [
    "apollo",
    "query",
    "useQuery",
    "graphql"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**useQuery** is the primary React hook for executing GraphQL queries in Apollo Client components."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining a Query"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Query",
        "content": "Defining and using a GraphQL query",
        "language": "jsx",
        "code": "import { gql, useQuery } from '@apollo/client';\n\nconst GET_USERS = gql`\n  query GetUsers {\n    users {\n      id\n      name\n      email\n    }\n  }\n`;\n\nfunction UserList() {\n  const { loading, error, data } = useQuery(GET_USERS);\n\n  if (loading) return <p>Loading...</p>;\n  if (error) return <p>Error: {error.message}</p>;\n\n  return (\n    <ul>\n      {data.users.map(user => (\n        <li key={user.id}>{user.name}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query with Variables"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Variables",
        "content": "Query with parameters",
        "language": "jsx",
        "code": "const GET_USER = gql`\n  query GetUser($id: ID!) {\n    user(id: $id) {\n      id\n      name\n      email\n    }\n  }\n`;\n\nfunction UserProfile({ userId }) {\n  const { loading, error, data } = useQuery(GET_USER, {\n    variables: { id: userId }\n  });\n\n  if (loading) return <p>Loading...</p>;\n  if (error) return <p>Error: {error.message}</p>;\n\n  return <h1>{data.user.name}</h1>;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "gql Tag",
        "text": "The gql tag is a template literal tag that parses GraphQL query strings into AST format for Apollo Client."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "useQuery executes GraphQL queries",
          "gql tag defines queries",
          "Returns loading, error, data",
          "Pass variables for parameterized queries"
        ],
        "takeaway": "useQuery is the foundation of Apollo data fetching."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "apollo"
}
