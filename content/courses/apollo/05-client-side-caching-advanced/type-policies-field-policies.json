{
  "id": "lesson-apollo-016",
  "slug": "type-policies-field-policies",
  "moduleSlug": "client-side-caching-advanced",
  "courseSlug": "apollo",
  "title": "Type Policies & Field Policies",
  "description": "Control cache behavior with type policies and field policies in Apollo Client.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "type-policies",
    "field-policies",
    "cache-behavior",
    "key-fields",
    "apollo-client"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Type Policies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Type policies let you define how a specific GraphQL type is read from and written to the cache. The most important property is `keyFields`, which tells Apollo which field(s) uniquely identify an object. By default, Apollo uses `id` or `_id`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Type policy example",
        "code": "const cache = new InMemoryCache({\n  typePolicies: {\n    Product: {\n      keyFields: ['sku'], // Use SKU instead of id\n      fields: {\n        price: {\n          read(_, { readField }) {\n            // Modify price before returning\n            const price = readField('price');\n            return price ? price * 1.1 : 0; // Add tax\n          },\n        },\n      },\n    },\n  },\n});",
        "language": "tsx",
        "content": "Custom type policy with keyFields and field modifiers."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Field Policies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Field policies control how individual fields are cached and merged. You can use `read` functions to transform cached data, `merge` functions to combine paginated results, and `keyArgs` to control cache key generation."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "read: Transform or compute field values from cache.",
          "merge: Combine incoming data with existing cache data.",
          "keyArgs: Specify which arguments affect cache uniqueness."
        ],
        "title": "Field Policy Options"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "text": "When using `merge` for pagination, always include a `keyArgs` strategy or Apollo may merge unrelated queries together. Use `false` for keyArgs if the field should never be split by arguments.",
        "title": "Pagination Merge"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Type & Field Policies Key Points",
        "points": [
          "Type policies define cache behavior per GraphQL type.",
          "keyFields specifies the unique identifier for a type.",
          "Field policies allow read/merge transforms.",
          "keyArgs controls cache key generation."
        ],
        "takeaway": "Type and field policies give you fine-grained control over Apollo Client caching behavior."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": [
    "cache-configuration"
  ]
}
