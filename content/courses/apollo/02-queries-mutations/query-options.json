{
  "id": "lesson-apollo-005",
  "slug": "query-options",
  "moduleSlug": "queries-mutations",
  "title": "Query Options",
  "description": "Learn all the options available for configuring queries.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": ["apollo", "query", "options", "configuration"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "useQuery accepts many options to control caching, fetching, and behavior."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Options"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Options",
        "content": "Configuring query behavior",
        "language": "jsx",
        "code": "const { data, loading } = useQuery(GET_USERS, {\n  // Variables\n  variables: { limit: 10 },\n  \n  // Fetch policy\n  fetchPolicy: 'cache-first', // default\n  // 'network-only', 'cache-only', 'no-cache', 'cache-and-network'\n  \n  // Polling\n  pollInterval: 5000, // Poll every 5 seconds\n  \n  // Error handling\n  errorPolicy: 'all', // 'none' | 'ignore' | 'all'\n  \n  // Callbacks\n  onCompleted: (data) => console.log('Completed:', data),\n  onError: (error) => console.error('Error:', error),\n  \n  // Skip query\n  skip: !userId,\n  \n  // Context\n  context: { headers: { Authorization: `Bearer ${token}` } }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Fetch Policies"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Policy", "Behavior"],
        "rows": [
          ["cache-first", "Check cache first, then network"],
          ["network-only", "Always fetch from network"],
          ["cache-only", "Only check cache, no network"],
          ["no-cache", "Fetch but don't cache"],
          ["cache-and-network", "Return cache, then fetch"]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Default Policy",
        "text": "cache-first is the default. Use network-only for data that must be fresh."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "fetchPolicy controls caching behavior",
          "pollInterval for real-time updates",
          "skip to conditionally execute",
          "onCompleted/onError callbacks"
        ],
        "takeaway": "Query options give you fine-grained control."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
