{
  "id": "lesson-apollo-008",
  "slug": "polling",
  "moduleSlug": "queries-mutations",
  "title": "Polling",
  "description": "Learn how to implement real-time updates with polling.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": [
    "apollo",
    "polling",
    "real-time",
    "updates"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Polling** repeatedly executes a query at specified intervals to keep data fresh. It's a simple way to achieve near real-time updates."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Polling"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Polling",
        "content": "Implementing polling",
        "language": "jsx",
        "code": "function LiveData() {\n  const { data, loading } = useQuery(GET_DATA, {\n    pollInterval: 5000 // Poll every 5 seconds\n  });\n\n  return <div>{/* ... */}</div>;\n}\n\n// Start/stop polling dynamically\nfunction DynamicPolling() {\n  const [isPolling, setIsPolling] = useState(true);\n\n  const { data } = useQuery(GET_DATA, {\n    pollInterval: isPolling ? 5000 : 0\n  });\n\n  return (\n    <div>\n      <button onClick={() => setIsPolling(!isPolling)}>\n        {isPolling ? 'Stop' : 'Start'} Polling\n      </button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Subscriptions vs Polling",
        "text": "For true real-time updates, use GraphQL subscriptions instead of polling. Polling is simpler but less efficient."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "pollInterval enables polling",
          "Set to 0 to stop polling",
          "Good for near real-time updates",
          "Subscriptions are better for real-time"
        ],
        "takeaway": "Polling is simple but subscriptions are more efficient."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "apollo"
}