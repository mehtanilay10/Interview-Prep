{
  "id": "lesson-prisma-022",
  "slug": "prisma-migrate-dev",
  "moduleSlug": "05-prisma-migrations",
  "courseSlug": "prisma",
  "title": "prisma migrate dev",
  "description": "Create, iterate, customize, and apply development migrations using the Prisma CLI.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "migrate-dev",
    "cli",
    "development-workflow"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "`prisma migrate dev` is the development command that automates the migration loop during feature implementation. It detects changes made in `schema.prisma`, creates a new timestamped migration directory containing a `migration.sql` file, executes that SQL on your development database, and regenerates Prisma Client."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Command Usage and Flags"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Running migrate dev",
        "content": "Always provide a descriptive name for your migration.",
        "language": "bash",
        "code": "# Create and apply a new migration\nnpx prisma migrate dev --name add_user_avatar\n\n# Create migration without applying immediately (allows manual SQL editing)\nnpx prisma migrate dev --create-only --name add_gin_trgm_index\n\n# Skip client generation if desired\nnpx prisma migrate dev --skip-generate"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Customizing SQL with `--create-only`"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Sometimes you need advanced PostgreSQL features that Prisma schema syntax doesn't directly express (e.g. creating extensions like `pg_trgm`, defining database triggers, or adding specialized check constraints). Using `--create-only` creates the SQL file so you can append custom SQL before applying it."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "prisma/migrations/20240101_add_trgm/migration.sql",
        "content": "Adding custom PostgreSQL extensions and indexes.",
        "language": "sql",
        "code": "-- Custom SQL added manually:\nCREATE EXTENSION IF NOT EXISTS pg_trgm;\n\n-- Prisma generated column:\nALTER TABLE \"users\" ADD COLUMN \"bio\" TEXT;\n\n-- Custom trigram index for fuzzy text search:\nCREATE INDEX \"users_bio_trgm_idx\" ON \"users\" USING gin (\"bio\" gin_trgm_ops);"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Commit Migration Files to Git",
        "text": "All files generated under `prisma/migrations/` MUST be committed to Git. They are the authoritative ledger of your database schema history."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`prisma migrate dev` is intended strictly for local development.",
          "Use `--create-only` to write custom SQL statements like extensions and triggers.",
          "Always commit the `prisma/migrations` folder to version control."
        ]
      }
    }
  ],
  "relatedLessons": [
    "migrations-overview",
    "prisma-migrate-deploy"
  ]
}