{
  "id": "lesson-prisma-029",
  "slug": "query-logging-and-performance",
  "moduleSlug": "06-prisma-advanced-features",
  "courseSlug": "prisma",
  "title": "Query Logging and Performance",
  "description": "Monitor generated SQL queries, track execution durations, identify N+1 query patterns, and extract Prometheus metrics.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "logging",
    "performance",
    "metrics",
    "debugging"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "High-performance applications require deep visibility into the exact SQL statements generated by the ORM. Prisma provides structured event-based logging and built-in runtime metrics to diagnose slow queries, connection pool saturation, and query waterfalls."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Configuring Event-Based Query Logging"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Logging Queries with Duration Thresholds",
        "content": "Log every SQL query in development, and log only slow queries (>100ms) in staging/production.",
        "language": "typescript",
        "code": "import { PrismaClient } from '@prisma/client';\n\nexport const prisma = new PrismaClient({\n  log: [\n    { emit: 'event', level: 'query' },\n    { emit: 'stdout', level: 'error' },\n    { emit: 'stdout', level: 'warn' },\n  ],\n});\n\n// Subscribe to query events\nprisma.$on('query', (e) => {\n  if (e.duration > 100) {\n    console.warn(`⚠️ SLOW QUERY (${e.duration}ms): ${e.query}`);\n    console.warn(`Params: ${e.params}`);\n  } else if (process.env.NODE_ENV === 'development') {\n    console.log(`[${e.duration}ms] ${e.query}`);\n  }\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Extracting Runtime Metrics"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Prisma includes a metrics API that captures query durations, connection pool wait times, and active client instances in Prometheus format or JSON."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Prometheus Metrics Endpoint in Next.js",
        "content": "app/api/metrics/route.ts exposing Prisma performance counters.",
        "language": "typescript",
        "code": "import { NextResponse } from 'next/server';\nimport { prisma } from '@/lib/prisma';\n\nexport async function GET() {\n  const metrics = await prisma.$metrics.prometheus();\n  return new NextResponse(metrics, {\n    headers: { 'Content-Type': 'text/plain; version=0.0.4' },\n  });\n}"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use `{ emit: 'event', level: 'query' }` to capture query execution time.",
          "Set alerts for queries taking longer than 100-200ms.",
          "Use `prisma.$metrics.prometheus()` to feed metrics directly to Datadog or Prometheus."
        ]
      }
    }
  ],
  "relatedLessons": [
    "middleware-and-extensions",
    "error-handling"
  ]
}