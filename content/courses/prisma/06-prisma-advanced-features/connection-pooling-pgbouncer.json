{
  "id": "lesson-prisma-031",
  "slug": "connection-pooling-pgbouncer",
  "moduleSlug": "06-prisma-advanced-features",
  "courseSlug": "prisma",
  "title": "Connection Pooling with PgBouncer",
  "description": "Architect scalable PostgreSQL connection pooling using PgBouncer, handle transaction mode limitations, and configure connection limits.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "prisma",
    "pgbouncer",
    "connection-pooling",
    "postgresql",
    "serverless"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "PostgreSQL forks a separate operating system process for every connected client. Because each process consumes 5-10MB of RAM, connecting hundreds of serverless function instances directly to PostgreSQL will crash the database server. **PgBouncer** is a lightweight connection pooler that maintains a small pool of persistent database connections and reuses them across thousands of client requests."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Configuring Prisma with PgBouncer"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When connecting Prisma to PgBouncer in transaction mode (the standard mode in Supabase, AWS RDS, and Neon), you must append `?pgbouncer=true` to your connection string. This tells Prisma Client's query engine to disable PostgreSQL prepared statements, which are incompatible with transaction-level pooling across multiple connections."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "schema.prisma with Dual URLs for PgBouncer",
        "content": "Configuring url for runtime pooling and directUrl for migrations.",
        "language": "prisma",
        "code": "datasource db {\n  provider  = \"postgresql\"\n  // 1. Runtime URL points to PgBouncer pooler (port 6543) with pgbouncer=true\n  url       = env(\"DATABASE_URL\")\n  // 2. Direct URL points to PostgreSQL directly (port 5432) for migrations\n  directUrl = env(\"DIRECT_URL\")\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": ".env Connection Strings",
        "content": "Setting connection parameters in your environment configuration.",
        "language": "bash",
        "code": "# Pooled connection for Next.js App (PgBouncer port 6543)\nDATABASE_URL=\"postgres://user:pass@ep-pooler.region.neon.tech:6543/db?pgbouncer=true&connection_limit=10\"\n\n# Direct connection for Prisma Migrate (Direct port 5432)\nDIRECT_URL=\"postgres://user:pass@ep-direct.region.neon.tech:5432/db\""
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "The `connection_limit` Parameter",
        "text": "In serverless environments, set `connection_limit=1` or `connection_limit=5` in your connection string so that individual serverless lambdas do not hoard pooler slots."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "PgBouncer multiplexes thousands of incoming client requests over a small set of database connections.",
          "Always append `?pgbouncer=true` when using transaction mode to disable prepared statements.",
          "Use `directUrl` for migrations because advisory locks require direct session access."
        ]
      }
    }
  ],
  "relatedLessons": [
    "connection-management-serverless",
    "schema-prisma-structure"
  ]
}