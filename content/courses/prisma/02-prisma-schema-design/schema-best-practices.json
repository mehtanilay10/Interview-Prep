{
  "id": "lesson-prisma-010",
  "slug": "schema-best-practices",
  "moduleSlug": "02-prisma-schema-design",
  "courseSlug": "prisma",
  "title": "Schema Design Best Practices",
  "description": "Architect maintainable, scalable, and performant Prisma schemas with proper indexing, audit trails, soft deletes, and naming standards.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "prisma",
    "best-practices",
    "architecture",
    "indexing",
    "soft-delete"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Designing an elegant database schema requires balancing immediate development velocity with long-term query performance and operational stability. Following proven industry patterns prevents database rewrites as your data volume grows from thousands to millions of records."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Top 5 Schema Design Rules"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Core Principles",
        "items": [
          "**1. Always index foreign keys**: Prisma does not automatically create database indexes on relation scalar fields. If you query `post.findMany({ where: { authorId } })`, you must add `@@index([authorId])`.",
          "**2. Consistent Primary Keys**: Standardize on either UUIDs (`@default(uuid())`) or CUIDs (`@default(cuid())`) across distributed tables to avoid ID collision and sequential enumeration attacks.",
          "**3. Standardize Audit Timestamps**: Include `createdAt DateTime @default(now())` and `updatedAt DateTime @updatedAt` on all mutable business entities.",
          "**4. Explicit Table & Column Mappings**: Use `@@map` and `@map` to decouple TypeScript camelCase properties from PostgreSQL snake_case table names.",
          "**5. Design for Soft Deletes**: Add `deletedAt DateTime?` to critical models to preserve historical records and financial trails."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Production-Ready Model Blueprint"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "schema.prisma Golden Template",
        "content": "A comprehensive model showcasing foreign key indexing, soft deletion, and audit tracking.",
        "language": "prisma",
        "code": "model Organization {\n  id          String       @id @default(uuid()) @db.Uuid\n  name        String       @db.VarChar(120)\n  slug        String       @unique @db.VarChar(60)\n  createdAt   DateTime     @default(now()) @map(\"created_at\") @db.Timestamptz\n  updatedAt   DateTime     @updatedAt @map(\"updated_at\") @db.Timestamptz\n  deletedAt   DateTime?    @map(\"deleted_at\") @db.Timestamptz\n\n  memberships Membership[]\n\n  @@index([deletedAt])\n  @@map(\"organizations\")\n}\n\nmodel Membership {\n  id             String       @id @default(uuid()) @db.Uuid\n  organizationId String       @map(\"organization_id\") @db.Uuid\n  userId         String       @map(\"user_id\") @db.Uuid\n  role           Role         @default(MEMBER)\n  createdAt      DateTime     @default(now()) @map(\"created_at\") @db.Timestamptz\n\n  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)\n  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n  @@unique([organizationId, userId], name: \"org_user_unique\")\n  // CRITICAL: Explicitly index foreign key fields!\n  @@index([organizationId])\n  @@index([userId])\n  @@map(\"memberships\")\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Automate Formatting with `prisma format`",
        "text": "Always run `npx prisma format` before committing. It automatically aligns fields, cleans up whitespaces, and normalizes relation declarations."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Foreign keys require explicit `@@index` declarations in Prisma to prevent slow sequential table scans.",
          "Use `createdAt` and `updatedAt` on all operational entities.",
          "Adopt UUID or CUID identifiers for modern cloud-native systems.",
          "Format schemas with `npx prisma format` to maintain team consistency."
        ]
      }
    }
  ],
  "relatedLessons": [
    "schema-prisma-structure",
    "model-attributes-and-constraints"
  ]
}