{
  "id": "lesson-prisma-007",
  "slug": "model-attributes-and-constraints",
  "moduleSlug": "02-prisma-schema-design",
  "courseSlug": "prisma",
  "title": "Model Attributes and Constraints",
  "description": "Learn how to configure primary keys, unique constraints, compound indexes, defaults, and table mappings using field and block attributes.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "attributes",
    "constraints",
    "primary-keys",
    "indexes"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Attributes in Prisma modify models or fields. Prisma differentiates between **field attributes** (prefixed with `@`) that modify a single column, and **block attributes** (prefixed with `@@`) that apply to multiple columns or the entire model."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Field Attributes vs Block Attributes"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Attribute",
          "Scope",
          "Purpose",
          "SQL Equivalent"
        ],
        "rows": [
          [
            "`@id`",
            "Field",
            "Defines the primary key of the model",
            "`PRIMARY KEY`"
          ],
          [
            "`@default(...)`",
            "Field",
            "Sets default value (autoincrement, now, uuid, cuid)",
            "`DEFAULT ...`"
          ],
          [
            "`@unique`",
            "Field",
            "Enforces unique values on the column",
            "`UNIQUE`"
          ],
          [
            "`@updatedAt`",
            "Field",
            "Auto-updates timestamp on every write",
            "Trigger / application-level timestamp"
          ],
          [
            "`@map(\"col_name\")`",
            "Field",
            "Maps Prisma field to different database column name",
            "Column renaming"
          ],
          [
            "`@@id([col1, col2])`",
            "Block",
            "Defines composite/compound primary key",
            "`PRIMARY KEY (col1, col2)`"
          ],
          [
            "`@@unique([col1, col2])`",
            "Block",
            "Defines composite unique constraint",
            "`UNIQUE (col1, col2)`"
          ],
          [
            "`@@index([col1, col2])`",
            "Block",
            "Creates a database index for fast querying",
            "`CREATE INDEX ...`"
          ],
          [
            "`@@map(\"table_name\")`",
            "Block",
            "Maps model name to a different physical table name",
            "Table renaming"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Real-World Schema Example"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Comprehensive Constraints Example",
        "content": "Demonstrating composite unique constraints, indexes, and custom column mappings.",
        "language": "prisma",
        "code": "model OrderItem {\n  id        String   @id @default(uuid())\n  orderId   String   @map(\"order_id\")\n  productId String   @map(\"product_id\")\n  quantity  Int      @default(1)\n  unitPrice Decimal  @map(\"unit_price\") @db.Decimal(10, 2)\n  createdAt DateTime @default(now()) @map(\"created_at\")\n\n  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)\n  product   Product  @relation(fields: [productId], references: [id])\n\n  // Prevent duplicate items for the same product in a single order\n  @@unique([orderId, productId], name: \"order_product_unique\")\n\n  // Add composite index for order query performance\n  @@index([orderId, createdAt])\n  @@map(\"order_items\")\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Clean TypeScript with `@map` and `@@map`",
        "text": "In relational databases, snake_case (`order_items`, `created_at`) is the standard naming convention, while JavaScript and TypeScript prefer camelCase (`orderItems`, `createdAt`). Using `@map` and `@@map` lets you write clean TypeScript in code without violating database naming standards."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use `@id` for single primary keys and `@@id` for composite keys.",
          "`@default(now())` and `@updatedAt` simplify audit timestamp tracking.",
          "`@@index` ensures query performance on frequently filtered columns.",
          "`@map` bridges TypeScript camelCase and PostgreSQL snake_case."
        ]
      }
    }
  ],
  "relatedLessons": [
    "schema-prisma-structure",
    "field-types-mapping"
  ]
}