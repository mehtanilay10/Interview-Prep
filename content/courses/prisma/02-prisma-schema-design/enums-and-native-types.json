{
  "id": "lesson-prisma-009",
  "slug": "enums-and-native-types",
  "moduleSlug": "02-prisma-schema-design",
  "courseSlug": "prisma",
  "title": "Enums and Native Types",
  "description": "Define strongly-typed database enums, handle status transitions, and use PostgreSQL custom types safely in TypeScript.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "prisma",
    "enums",
    "native-types",
    "typescript",
    "postgresql"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Enums allow developers to restrict a column's value to a fixed set of predefined options (e.g., order statuses, user roles, billing tiers). In PostgreSQL, Prisma enums map directly to native database `CREATE TYPE ... AS ENUM` statements, providing database-level constraint enforcement and compile-time TypeScript type safety."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining and Using Enums"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Enum Definition in schema.prisma",
        "content": "Define enum blocks and reference them as field types in your models.",
        "language": "prisma",
        "code": "enum Role {\n  CUSTOMER\n  STAFF\n  ADMIN\n  SUPER_ADMIN\n}\n\nenum OrderStatus {\n  PENDING\n  PAYMENT_CONFIRMED\n  PROCESSING\n  SHIPPED\n  DELIVERED\n  CANCELLED\n}\n\nmodel User {\n  id    String @id @default(uuid())\n  email String @unique\n  role  Role   @default(CUSTOMER)\n}\n\nmodel Order {\n  id     String      @id @default(uuid())\n  status OrderStatus @default(PENDING)\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using Enums in Application Code"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you run `prisma generate`, Prisma exports TypeScript enums directly from `@prisma/client`. You can import and use them in server actions, API routes, and validation schemas (e.g. Zod)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "TypeScript Usage with Zod Validation",
        "content": "Validating input against Prisma-generated enums.",
        "language": "typescript",
        "code": "import { OrderStatus, Role } from '@prisma/client';\nimport { z } from 'zod';\nimport { prisma } from '@/lib/prisma';\n\n// Zod schema referencing Prisma enum\nconst UpdateOrderStatusSchema = z.object({\n  orderId: z.string().uuid(),\n  status: z.nativeEnum(OrderStatus)\n});\n\nexport async function updateOrderStatus(orderId: string, status: OrderStatus) {\n  return await prisma.order.update({\n    where: { id: orderId },\n    data: { status }\n  });\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Migration Caveat for Enums",
        "text": "In PostgreSQL, adding a new value to an existing enum (`ALTER TYPE ... ADD VALUE`) is fast and safe. However, removing or renaming enum values in PostgreSQL is non-trivial and often requires creating a new type, migrating columns, and dropping the old type."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Enums enforce valid values at both the database engine and TypeScript compiler levels.",
          "Import enums directly from `@prisma/client` for consistent runtime comparisons.",
          "Plan enum values carefully because renaming or dropping enum values in PostgreSQL requires explicit migration steps."
        ]
      }
    }
  ],
  "relatedLessons": [
    "field-types-mapping",
    "schema-best-practices"
  ]
}