{
  "id": "lesson-prisma-006",
  "slug": "schema-prisma-structure",
  "moduleSlug": "02-prisma-schema-design",
  "courseSlug": "prisma",
  "title": "schema.prisma Structure",
  "description": "Master the structure of the schema.prisma file, including datasource, generators, models, and environment variable configuration.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "schema",
    "datasource",
    "generator",
    "models"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The `schema.prisma` file is the central source of truth for your entire application's data tier. It uses Prisma's custom modeling language (Prisma Schema Language / PSL) to describe data sources, output generators, database models, enums, and relations in a clean, human-readable format."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Three Essential Blocks"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Top-Level Schema Components",
        "items": [
          "**`datasource` block**: Specifies which database provider to connect to (e.g. `postgresql`, `mysql`, `sqlite`) and passes the database connection URL.",
          "**`generator` block**: Defines what artifacts to generate when running `prisma generate` (most commonly `prisma-client-js`).",
          "**`model` blocks**: Define the structure of your application entities, mapping directly to tables or collections in your database."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Complete Annotated schema.prisma Example",
        "content": "A production schema file demonstrating datasource configuration, generator settings, and model definition.",
        "language": "prisma",
        "code": "// 1. Data Source Configuration\ndatasource db {\n  provider  = \"postgresql\"\n  url       = env(\"DATABASE_URL\")       // Direct or pooled connection\n  directUrl = env(\"DIRECT_URL\")         // Used for migrations in serverless setups\n}\n\n// 2. Client Code Generator\ngenerator client {\n  provider        = \"prisma-client-js\"\n  previewFeatures = [\"fullTextSearchPostgres\", \"metrics\"]\n}\n\n// 3. Application Data Models\nmodel Account {\n  id           String    @id @default(cuid())\n  name         String\n  slug         String    @unique\n  plan         PlanType  @default(FREE)\n  createdAt    DateTime  @default(now())\n  updatedAt    DateTime  @updatedAt\n  users        User[]\n\n  @@map(\"accounts\") // Maps model to 'accounts' table in PostgreSQL\n}\n\nenum PlanType {\n  FREE\n  PRO\n  ENTERPRISE\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Understanding `url` vs `directUrl`"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Modern cloud PostgreSQL providers like Neon and Supabase use connection poolers (such as PgBouncer) to support thousands of concurrent serverless functions. However, schema migrations (`prisma migrate dev` or `prisma migrate deploy`) require direct, session-level database access because PgBouncer in transaction mode does not support advisory locks or prepared statements. Prisma allows defining `directUrl` specifically for CLI migrations while using `url` for pooled application queries."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Setting",
          "Target",
          "Example Value",
          "Purpose"
        ],
        "rows": [
          [
            "`url`",
            "Connection Pooler (Port 6543 / PgBouncer)",
            "postgres://...@ep-pooler.neon.tech/db?pgbouncer=true",
            "Runtime queries in serverless Next.js functions"
          ],
          [
            "`directUrl`",
            "Direct PostgreSQL Port (5432)",
            "postgres://...@ep-direct.neon.tech/db",
            "Prisma CLI migrations & schema push requiring advisory locks"
          ]
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`schema.prisma` is the single source of truth for the database and TypeScript types.",
          "Use `env()` to inject connection secrets securely without hardcoding credentials.",
          "Configure `directUrl` alongside `url` when deploying on serverless architectures using PgBouncer or Neon."
        ]
      }
    }
  ],
  "relatedLessons": [
    "model-attributes-and-constraints",
    "field-types-mapping"
  ]
}