{
  "id": "lesson-prisma-018",
  "slug": "one-to-many-relations",
  "moduleSlug": "04-prisma-relations",
  "courseSlug": "prisma",
  "title": "One-to-Many Relations",
  "description": "Implement 1:N relations in Prisma, query child collections with filtering, and configure cascade deletions.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "relations",
    "one-to-many",
    "foreign-keys",
    "indexes"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **1-to-Many (1:N) relation** is the most common relational pattern. One record in the parent table links to zero, one, or multiple records in a child table (e.g., one `Author` has many `Books`, one `User` has many `Orders`)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "1-to-Many Schema Definition with Index",
        "content": "Note the @@index on customerId to ensure high-performance lookups.",
        "language": "prisma",
        "code": "model Customer {\n  id        String   @id @default(uuid())\n  name      String\n  email     String   @unique\n  orders    Order[]  // 1 Customer -> Many Orders\n  createdAt DateTime @default(now())\n}\n\nmodel Order {\n  id          String   @id @default(uuid())\n  totalAmount Decimal  @db.Decimal(10, 2)\n  status      String   @default(\"PENDING\")\n  customerId  String   @map(\"customer_id\")\n  customer    Customer @relation(fields: [customerId], references: [id], onDelete: Restrict)\n\n  @@index([customerId]) // CRITICAL: Always index the foreign key!\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Filtering and Aggregating 1:N Collections"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Advanced 1:N Querying with Relation Filters",
        "content": "Use `some`, `every`, or `none` to filter parent records based on child attributes.",
        "language": "typescript",
        "code": "import { prisma } from '@/lib/prisma';\n\n// Find all customers who have at least one completed order over $500\nconst vipCustomers = await prisma.customer.findMany({\n  where: {\n    orders: {\n      some: {\n        status: 'COMPLETED',\n        totalAmount: { gte: 500 }\n      }\n    }\n  },\n  include: {\n    _count: {\n      select: { orders: true } // Efficiently returns order count without loading rows\n    }\n  }\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Avoid Loading Huge Lists: Use `_count`",
        "text": "If you only need to show the badge number of orders or comments, use `_count: { select: { orders: true } }` instead of fetching thousands of records over the wire."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "The foreign key lives on the 'many' side of the relation.",
          "Always create an `@@index` on the foreign key column.",
          "Use `some`, `every`, and `none` relation filters to query parents based on child criteria.",
          "Use `_count` to fetch relation counts with optimal performance."
        ]
      }
    }
  ],
  "relatedLessons": [
    "relations-overview",
    "many-to-many-relations"
  ]
}