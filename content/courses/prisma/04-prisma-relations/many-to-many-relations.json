{
  "id": "lesson-prisma-019",
  "slug": "many-to-many-relations",
  "moduleSlug": "04-prisma-relations",
  "courseSlug": "prisma",
  "title": "Many-to-Many Relations",
  "description": "Compare implicit vs explicit many-to-many relations in Prisma and learn when to choose an explicit junction table.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "prisma",
    "relations",
    "many-to-many",
    "junction-table",
    "implicit"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **Many-to-Many (M:N) relation** connects records where each entity can link to multiple instances of the other (e.g. `Post` and `Tag`, `Student` and `Course`). Prisma supports two ways to model M:N relations: **implicit** and **explicit**."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Implicit vs Explicit M:N Relations"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Choosing M:N Strategy",
        "cards": [
          {
            "title": "Implicit Many-to-Many",
            "description": "Prisma automatically creates and manages a hidden join table (`_PostToTag`) behind the scenes without defining a model in schema.prisma.",
            "pros": [
              "Zero schema boilerplate",
              "Simple nested connect/disconnect syntax"
            ],
            "cons": [
              "Cannot store additional metadata on the relationship (e.g. `assignedAt`, `role`)"
            ],
            "tags": [
              "Tags",
              "Categories",
              "Simple Links"
            ]
          },
          {
            "title": "Explicit Many-to-Many",
            "description": "You define the junction table as an explicit model in schema.prisma with its own foreign keys and custom fields.",
            "pros": [
              "Can store timestamps, status, permissions on the join record",
              "Full control over physical database indexes"
            ],
            "cons": [
              "Slightly more verbose queries"
            ],
            "tags": [
              "Team Memberships",
              "Enrollments",
              "Enterprise"
            ]
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Implicit M:N Definition",
        "content": "Simply add array fields on both models.",
        "language": "prisma",
        "code": "model Post {\n  id   String @id @default(uuid())\n  tags Tag[]  // Prisma creates _PostToTag table automatically\n}\n\nmodel Tag {\n  id    String @id @default(uuid())\n  posts Post[]\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Explicit M:N Definition with Metadata",
        "content": "Useful for memberships where you need to track `role` and `joinedAt`.",
        "language": "prisma",
        "code": "model Team {\n  id          String           @id @default(uuid())\n  name        String\n  memberships TeamMembership[]\n}\n\nmodel User {\n  id          String           @id @default(uuid())\n  name        String\n  memberships TeamMembership[]\n}\n\nmodel TeamMembership {\n  id        String   @id @default(uuid())\n  teamId    String   @map(\"team_id\")\n  userId    String   @map(\"user_id\")\n  role      String   @default(\"MEMBER\") // Metadata field\n  joinedAt  DateTime @default(now())    // Metadata field\n\n  team      Team     @relation(fields: [teamId], references: [id], onDelete: Cascade)\n  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n  @@unique([teamId, userId])\n  @@index([teamId])\n  @@index([userId])\n}"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use implicit M:N when you only need to associate IDs without extra columns.",
          "Use explicit M:N as soon as the relationship requires metadata like `role`, `createdAt`, or `status`.",
          "Always include composite unique constraints on junction tables."
        ]
      }
    }
  ],
  "relatedLessons": [
    "one-to-many-relations",
    "relation-queries-and-pagination"
  ]
}