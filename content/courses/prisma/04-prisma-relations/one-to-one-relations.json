{
  "id": "lesson-prisma-017",
  "slug": "one-to-one-relations",
  "moduleSlug": "04-prisma-relations",
  "courseSlug": "prisma",
  "title": "One-to-One Relations",
  "description": "Design 1:1 relations in Prisma with unique foreign key constraints and optional navigation fields.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "prisma",
    "relations",
    "one-to-one",
    "unique-constraint"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **1-to-1 (1:1) relation** connects exactly one record of a model to at most one record of another model (e.g. `User` and `Profile`, `Store` and `StoreSettings`). In relational databases, a 1:1 relation is enforced by placing a `@unique` constraint on the foreign key column."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "schema.prisma 1-to-1 Definition",
        "content": "Notice the `@unique` constraint on `userId` in the Profile model.",
        "language": "prisma",
        "code": "model User {\n  id      String   @id @default(uuid())\n  email   String   @unique\n  profile Profile? // Optional 1:1 relation\n}\n\nmodel Profile {\n  id     String @id @default(uuid())\n  bio    String?\n  userId String @unique @map(\"user_id\") // @unique turns 1-N into 1-1\n  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Querying 1:1 Relations"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Fetching and Updating 1:1 Relations",
        "content": "TypeScript infers `profile` as either the object or null.",
        "language": "typescript",
        "code": "import { prisma } from '@/lib/prisma';\n\n// Query User with their profile\nconst userWithProfile = await prisma.user.findUnique({\n  where: { email: 'sarah@example.com' },\n  include: { profile: true }\n});\n\n// Upsert Profile directly from the User model\nconst updated = await prisma.user.update({\n  where: { id: userWithProfile?.id },\n  data: {\n    profile: {\n      upsert: {\n        create: { bio: 'Senior Engineer' },\n        update: { bio: 'Principal Architect' }\n      }\n    }\n  }\n});"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "A 1:1 relation requires a `@unique` constraint on the foreign key column.",
          "Either side can be optional in application logic, but one side must hold the physical foreign key.",
          "Use nested `upsert` or `update` to manage linked records cleanly."
        ]
      }
    }
  ],
  "relatedLessons": [
    "relations-overview",
    "one-to-many-relations"
  ]
}