{
  "id": "lesson-prisma-020",
  "slug": "relation-queries-and-pagination",
  "moduleSlug": "04-prisma-relations",
  "courseSlug": "prisma",
  "title": "Relation Queries and Pagination",
  "description": "Query nested relations, filter connected entities, and paginate inside related arrays without memory bloat.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "relations",
    "nested-pagination",
    "filtering"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When fetching models that have hundreds or thousands of child records (e.g. an author with 500 articles), querying the entire relationship naively causes severe latency and memory exhaustion. Prisma allows filtering, sorting, and paginating nested relations directly within `include` or `select` blocks."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Nested Pagination in Action"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Paginating Child Records",
        "content": "Notice how `take`, `skip`, and `orderBy` are applied directly inside the posts relation.",
        "language": "typescript",
        "code": "import { prisma } from '@/lib/prisma';\n\nexport async function getAuthorWithRecentPosts(authorId: string) {\n  return await prisma.user.findUnique({\n    where: { id: authorId },\n    select: {\n      id: true,\n      name: true,\n      email: true,\n      // Nested pagination and filtering on posts!\n      posts: {\n        where: { published: true },\n        orderBy: { createdAt: 'desc' },\n        take: 5,\n        select: {\n          id: true,\n          title: true,\n          createdAt: true\n        }\n      },\n      _count: {\n        select: { posts: true } // Returns total post count for pagination UI\n      }\n    }\n  });\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "How Prisma Executes Relation Queries",
        "text": "Prisma executes queries intelligently using separate batched queries (e.g. `SELECT * FROM posts WHERE authorId IN (...)`) rather than massive SQL JOINs with Cartesian row multiplication. This avoids duplicate column transfer over the network."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Always paginate child relations (`take`, `skip`) when collections can exceed 20 records.",
          "Use `_count` to calculate total items for frontend pagination controls.",
          "Prisma's query engine batches relation queries to eliminate duplicate data transfer."
        ]
      }
    }
  ],
  "relatedLessons": [
    "one-to-many-relations",
    "select-and-include"
  ]
}