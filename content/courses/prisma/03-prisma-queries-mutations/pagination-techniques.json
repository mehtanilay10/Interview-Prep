{
  "id": "lesson-prisma-014",
  "slug": "pagination-techniques",
  "moduleSlug": "03-prisma-queries-mutations",
  "courseSlug": "prisma",
  "title": "Pagination Techniques",
  "description": "Compare Offset-based vs Keyset (Cursor-based) pagination in Prisma and implement scalable infinite scrolling.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "prisma",
    "pagination",
    "cursor",
    "offset",
    "performance"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When serving large datasets to client applications, loading all rows at once overwhelms memory and network bandwidth. Prisma provides first-class support for both **Offset-based pagination** and **Cursor-based pagination**."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Offset vs Cursor Pagination Comparison"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Strategy",
          "Prisma Arguments",
          "Performance",
          "Best Used For"
        ],
        "rows": [
          [
            "Offset Pagination",
            "`skip: 20, take: 10`",
            "Degrades linearly `O(N)` on large offsets",
            "Admin dashboards with explicit page numbers (Page 1, 2, 3)"
          ],
          [
            "Cursor (Keyset) Pagination",
            "`cursor: { id: lastId }, skip: 1, take: 10`",
            "Constant `O(1)` index seek performance",
            "Infinite scroll feeds (Twitter, Instagram, mobile apps)"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Offset Pagination Degrades at Scale"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you execute `SELECT * FROM orders OFFSET 100000 LIMIT 10`, PostgreSQL must scan and count 100,000 index entries or table rows before discarding them and returning the 10 rows. As the offset increases, query latency spikes significantly."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Production Keyset / Cursor Pagination Implementation",
        "content": "High-performance infinite scrolling with cursor and skip.",
        "language": "typescript",
        "code": "import { prisma } from '@/lib/prisma';\n\ninterface FetchPostsParams {\n  limit?: number;\n  cursor?: string; // ID of the last item from previous fetch\n}\n\nexport async function getPaginatedPosts({ limit = 10, cursor }: FetchPostsParams) {\n  const posts = await prisma.post.findMany({\n    take: limit + 1, // Fetch 1 extra item to determine if hasNextPage is true\n    skip: cursor ? 1 : 0, // Skip the cursor itself\n    cursor: cursor ? { id: cursor } : undefined,\n    orderBy: { id: 'desc' }, // Must order by unique cursor field\n  });\n\n  let nextCursor: string | undefined = undefined;\n  if (posts.length > limit) {\n    const nextItem = posts.pop(); // Remove the extra probe item\n    nextCursor = nextItem?.id;\n  }\n\n  return {\n    items: posts,\n    nextCursor,\n    hasNextPage: Boolean(nextCursor)\n  };\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Cursor Rule",
        "text": "The field specified in `cursor` MUST be a unique column (e.g. `@id` or `@unique`). If you order by `createdAt`, you should sort by a composite unique key like `[createdAt, id]`."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use offset pagination (`skip`/`take`) only for small datasets or explicit page-number controls.",
          "Use cursor pagination for infinite scrolling and high-throughput feeds.",
          "Always sort by the cursor field (or a composite key including the cursor) to ensure deterministic results."
        ]
      }
    }
  ],
  "relatedLessons": [
    "filtering-and-sorting",
    "select-and-include"
  ]
}