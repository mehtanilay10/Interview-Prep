{
  "id": "lesson-prisma-015",
  "slug": "select-and-include",
  "moduleSlug": "03-prisma-queries-mutations",
  "courseSlug": "prisma",
  "title": "Select vs Include",
  "description": "Optimize query payloads by understanding the fundamental difference between select and include in Prisma Client.",
  "order": 5,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "prisma",
    "select",
    "include",
    "optimization",
    "payload"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In Prisma, controlling the shape of returned data is accomplished via `select` and `include`. Understanding how they differ and when to use each is crucial for writing efficient queries that minimize network payload size and memory footprint."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Core Difference: Select vs Include"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "How They Work",
        "items": [
          "**`select`**: Explicitly chooses which scalar and relational fields to return. Any field not listed is excluded from the query (`SELECT id, email FROM ...`).",
          "**`include`**: Returns **ALL** default scalar fields on the model, plus adds specified relational models (`SELECT * FROM users` plus relation join)."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Mutually Exclusive at the Root Level",
        "text": "You cannot use `select` and `include` simultaneously on the same level of a query! If you want to include relations while selecting only specific parent fields, nest `select` inside `select`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Wrong vs Right: Mixing Select and Include",
        "content": "How to properly select specific parent fields while loading related records.",
        "language": "typescript",
        "code": "// ❌ INVALID: Causes TypeScript error: Cannot use both select and include\n// const invalid = await prisma.user.findMany({\n//   select: { id: true },\n//   include: { posts: true }\n// });\n\n// ✅ VALID: Nest relations directly inside select!\nconst users = await prisma.user.findMany({\n  select: {\n    id: true,\n    name: true,\n    // Relation loaded directly within select\n    posts: {\n      select: {\n        id: true,\n        title: true\n      },\n      where: { published: true }\n    }\n  }\n});"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Prefer `select` in production APIs to prevent leaking sensitive fields like password hashes.",
          "Use `include` when you need all scalar fields of an entity plus related entities.",
          "Never mix `select` and `include` at the same object depth."
        ]
      }
    }
  ],
  "relatedLessons": [
    "prisma-client-queries",
    "pagination-techniques"
  ]
}