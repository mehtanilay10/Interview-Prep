{
  "id": "lesson-prisma-037",
  "slug": "prisma-with-authentication",
  "moduleSlug": "prisma-nextjs-integration",
  "courseSlug": "prisma",
  "title": "Prisma with Authentication",
  "description": "Integrate Prisma with Clerk, Auth.js, and session-based authentication in Next.js.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 10,
  "tags": [
    "prisma",
    "authentication",
    "clerk",
    "authjs",
    "nextjs"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Authentication providers like Clerk and Auth.js manage user sessions and identities. Prisma stores user-specific data and can link to authentication provider IDs."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "User Model Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "User Model for Auth",
        "content": "Define a User model compatible with Auth.js.",
        "code": "model User {\n  id            String    @id @default(cuid())\n  name          String?\n  email         String    @unique\n  emailVerified DateTime?\n  image         String?\n  accounts      Account[]\n  sessions      Session[]\n}\n\nmodel Account {\n  id                String  @id @default(cuid())\n  userId            String\n  type              String\n  provider          String\n  providerAccountId String\n  user              User    @relation(fields: [userId], references: [id])\n\n  @@unique([provider, providerAccountId])\n}\n\nmodel Session {\n  id           String   @id @default(cuid())\n  sessionToken String   @unique\n  userId       String\n  expires      DateTime\n  user         User     @relation(fields: [userId], references: [id])\n}",
        "language": "prisma"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Fetching the Authenticated User"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Server Action with Auth",
        "content": "Fetch the current user in a Server Action.",
        "code": "'use server';\n\nimport { auth } from '@/auth';\nimport prisma from '@/lib/prisma';\n\nexport async function getCurrentUserProfile() {\n  const session = await auth();\n  if (!session?.user?.email) return null;\n\n  return prisma.user.findUnique({\n    where: { email: session.user.email }\n  });\n}",
        "language": "typescript"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Use the Auth.js adapter for Prisma to automatically sync accounts and sessions. Alternatively, use Clerk's webhook handlers to sync user data to your Prisma database."
      }
    }
  ],
  "relatedLessons": [
    "prisma-in-nextjs-app-router",
    "prisma-security-best-practices"
  ],
  "courseSlug": "prisma"
}
