{
  "id": "lesson-prisma-036",
  "slug": "prisma-api-routes",
  "moduleSlug": "07-prisma-nextjs-integration",
  "courseSlug": "prisma",
  "title": "Prisma in Pages Router API Routes",
  "description": "Maintain legacy Next.js Pages Router API routes (pages/api/*) with Prisma, type-safe request handlers, and middleware.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "prisma",
    "nextjs",
    "pages-router",
    "api-routes",
    "legacy"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "While modern Next.js projects adopt the App Router, millions of existing production codebases still run on the **Pages Router** (`pages/api/...`). In the Pages Router, API endpoints are standard Node.js request/response handlers (`NextApiRequest`, `NextApiResponse`)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "pages/api/users/[id].ts",
        "content": "Pages router API endpoint handling GET and DELETE requests.",
        "language": "typescript",
        "code": "import type { NextApiRequest, NextApiResponse } from 'next';\nimport { prisma } from '@/lib/prisma';\n\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  const { id } = req.query;\n\n  if (typeof id !== 'string') {\n    return res.status(400).json({ error: 'Invalid user ID' });\n  }\n\n  if (req.method === 'GET') {\n    const user = await prisma.user.findUnique({\n      where: { id },\n      select: { id: true, name: true, email: true }\n    });\n    if (!user) return res.status(404).json({ error: 'User not found' });\n    return res.status(200).json(user);\n  }\n\n  if (req.method === 'DELETE') {\n    await prisma.user.delete({ where: { id } });\n    return res.status(204).end();\n  }\n\n  res.setHeader('Allow', ['GET', 'DELETE']);\n  return res.status(405).end(`Method ${req.method} Not Allowed`);\n}"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Pages Router API routes use `(req: NextApiRequest, res: NextApiResponse)`.",
          "Use the same `lib/prisma.ts` singleton across Pages Router and App Router.",
          "Explicitly handle HTTP method branching and 405 Method Not Allowed."
        ]
      }
    }
  ],
  "relatedLessons": [
    "prisma-route-handlers",
    "prisma-in-nextjs-app-router"
  ]
}