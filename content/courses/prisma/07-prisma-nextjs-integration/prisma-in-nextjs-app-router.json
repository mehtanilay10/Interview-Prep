{
  "id": "lesson-prisma-033",
  "slug": "prisma-in-nextjs-app-router",
  "moduleSlug": "07-prisma-nextjs-integration",
  "courseSlug": "prisma",
  "title": "Prisma in Next.js App Router",
  "description": "Architect direct database access inside React Server Components (RSC) with caching, streaming, and zero bundle size.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "prisma",
    "nextjs",
    "app-router",
    "server-components",
    "rsc"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The Next.js App Router introduces React Server Components (RSC). In RSC, components execute strictly on the server during the render phase and never ship JavaScript to the browser. This eliminates the need for separate internal REST API endpoints just to feed UI components: you can query Prisma directly inside your component!"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Direct Database Querying in React Server Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "app/dashboard/page.tsx (Async Server Component)",
        "content": "Clean, direct database query with TypeScript type inference and zero client bundle size.",
        "language": "typescript",
        "code": "import { prisma } from '@/lib/prisma';\nimport { Suspense } from 'react';\n\n// 1. Dedicated data fetching function\nasync function getRecentOrders() {\n  return await prisma.order.findMany({\n    where: { status: 'COMPLETED' },\n    include: { customer: { select: { name: true, email: true } } },\n    orderBy: { createdAt: 'desc' },\n    take: 5,\n  });\n}\n\n// 2. Server Component\nexport default async function DashboardPage() {\n  const orders = await getRecentOrders();\n\n  return (\n    <main className=\"p-8 max-w-4xl mx-auto\">\n      <h1 className=\"text-2xl font-bold mb-6\">Recent Orders</h1>\n      <div className=\"space-y-4\">\n        {orders.map((order) => (\n          <div key={order.id} className=\"p-4 border rounded shadow-sm flex justify-between\">\n            <div>\n              <p className=\"font-medium\">{order.customer.name}</p>\n              <p className=\"text-sm text-gray-500\">{order.customer.email}</p>\n            </div>\n            <span className=\"font-semibold\">${order.totalAmount.toString()}</span>\n          </div>\n        ))}\n      </div>\n    </main>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Zero Client JavaScript",
        "text": "Notice that `@prisma/client` and its database connection logic never leak into the browser bundle. Next.js streams the rendered HTML and React flight data directly to the client."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "React Server Components can query Prisma directly without `fetch()` or API boilerplate.",
          "Database credentials and query logic remain 100% secure on the server.",
          "Use React `Suspense` to stream slow queries without blocking the initial page render."
        ]
      }
    }
  ],
  "relatedLessons": [
    "prisma-with-server-actions",
    "prisma-route-handlers"
  ]
}