{
  "id": "lesson-pg-040",
  "slug": "foreign-key",
  "moduleSlug": "constraints",
  "courseSlug": "postgresql",
  "title": "FOREIGN KEY & Referential Integrity",
  "description": "Enforce relationships, configure ON DELETE CASCADE, RESTRICT, and SET NULL, and avoid unindexed foreign key table locks.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "foreign-key",
    "referential-integrity",
    "cascade",
    "locks",
    "indexes"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **FOREIGN KEY** (FK) constraint ensures that values in a child column match existing values in a parent table's primary or unique key. This guarantees referential integrity at the database storage engine layer."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Unindexed Foreign Key Trap"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "danger",
        "title": "Critical DBA Warning",
        "text": "PostgreSQL does NOT automatically create an index on foreign key columns! When deleting a parent row, PostgreSQL must perform a sequential full table scan on the child table to verify referential integrity, acquiring heavy share locks and killing production performance. ALWAYS index your foreign keys!"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Foreign Key Definition with Explicit Index",
        "content": "Configuring cascade rules and child index.",
        "language": "sql",
        "code": "CREATE TABLE comments (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    post_id UUID NOT NULL,\n    user_id UUID NOT NULL,\n    content TEXT NOT NULL,\n    CONSTRAINT fk_comments_post \n        FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,\n    CONSTRAINT fk_comments_user \n        FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT\n);\n\n-- CRITICAL: Always index foreign key columns!\nCREATE INDEX idx_comments_post_id ON comments (post_id);\nCREATE INDEX idx_comments_user_id ON comments (user_id);"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Foreign keys enforce relational consistency across tables.",
          "Always add indexes to foreign key columns to prevent sequential table lock scans.",
          "Choose `ON DELETE CASCADE` or `RESTRICT` consciously based on your domain cleanup policy."
        ]
      }
    }
  ],
  "relatedLessons": [
    "primary-key",
    "unique-check"
  ]
}