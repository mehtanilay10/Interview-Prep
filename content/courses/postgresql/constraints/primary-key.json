{
  "id": "lesson-pg-039",
  "slug": "primary-key",
  "moduleSlug": "constraints",
  "courseSlug": "postgresql",
  "title": "PRIMARY KEY: Serial, Identity & UUIDs",
  "description": "Compare auto-incrementing SERIAL, modern GENERATED ALWAYS AS IDENTITY, UUIDv4, and UUIDv7 for primary keys.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "primary-key",
    "identity",
    "uuid",
    "serial",
    "architecture"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **PRIMARY KEY** uniquely identifies every record in a table and enforces both `UNIQUE` and `NOT NULL` constraints. In modern PostgreSQL, developers must decide between auto-incrementing integers and universally unique identifiers (UUIDs)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Identity Columns vs Legacy SERIAL"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In PostgreSQL 10+, SQL-standard **`GENERATED ALWAYS AS IDENTITY`** completely replaces legacy `SERIAL`. Identity columns prevent accidental manual overwrites of sequence numbers and integrate cleanly with schema permissions."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Integer vs UUID Primary Keys"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Strategy",
          "Storage Size",
          "Collision Risk",
          "Security / Enumeration",
          "B-Tree Fragmentation"
        ],
        "rows": [
          [
            "`BIGINT IDENTITY`",
            "8 bytes",
            "Zero",
            "Vulnerable to enumeration (`/users/1`, `/users/2`)",
            "Zero fragmentation (strictly sequential)"
          ],
          [
            "`UUIDv4` (Random)",
            "16 bytes",
            "Virtually Zero",
            "Secure (unguessable)",
            "High B-tree index fragmentation on large tables"
          ],
          [
            "`UUIDv7` (Time-Ordered)",
            "16 bytes",
            "Virtually Zero",
            "Secure & unguessable",
            "Minimal fragmentation (time-ordered sequential prefix)"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Identity vs UUIDv4 Table Creation",
        "content": "Demonstrating modern syntax.",
        "language": "sql",
        "code": "-- 1. Modern SQL-Standard Identity Column\nCREATE TABLE invoices (\n    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    total_amount NUMERIC(10, 2)\n);\n\n-- 2. Native UUIDv4 Primary Key\nCREATE TABLE accounts (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    name VARCHAR(100)\n);"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use `GENERATED ALWAYS AS IDENTITY` instead of legacy `SERIAL`.",
          "Use UUIDs when primary keys are exposed to client APIs or distributed systems.",
          "`gen_random_uuid()` is built into PostgreSQL without requiring external extensions."
        ]
      }
    }
  ],
  "relatedLessons": [
    "foreign-key",
    "unique-check"
  ]
}