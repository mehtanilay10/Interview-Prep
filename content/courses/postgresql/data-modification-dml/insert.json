{
  "id": "lesson-pg-027",
  "slug": "insert",
  "moduleSlug": "data-modification-dml",
  "courseSlug": "postgresql",
  "title": "INSERT Statement & RETURNING Clause",
  "description": "Insert single and multiple rows, insert from queries, and retrieve generated keys using PostgreSQL's powerful RETURNING clause.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "insert",
    "returning",
    "dml",
    "multi-row-insert"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **`INSERT`** statement adds new rows to a table. While standard SQL requires issuing a secondary query to fetch auto-generated IDs, PostgreSQL pioneered the **`RETURNING`** clause, allowing you to return inserted column values, computed defaults, or full row tuples in a single atomic roundtrip."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Batch Insert with RETURNING"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multi-Row Insert Returning Generated UUIDs",
        "content": "Inserts multiple rows and immediately returns the generated primary keys and timestamps.",
        "language": "sql",
        "code": "INSERT INTO users (email, name, role)\nVALUES \n    ('alice@company.com', 'Alice Smith', 'ADMIN'),\n    ('bob@company.com', 'Bob Jones', 'STAFF'),\n    ('carol@company.com', 'Carol White', 'CUSTOMER')\nRETURNING id, email, created_at;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "INSERT INTO ... SELECT"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can populate a table directly from the result of a `SELECT` query, which executes entirely within the database engine without sending data across the network."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Archiving Stale Inactive Users",
        "content": "Copying data from an active table to an archive table.",
        "language": "sql",
        "code": "INSERT INTO archived_users (user_id, email, archived_at)\nSELECT id, email, NOW()\nFROM users\nWHERE last_login_at < NOW() - INTERVAL '365 days'\nRETURNING user_id;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use `INSERT INTO ... VALUES (...), (...)` for efficient batch insertion.",
          "Use `RETURNING *` or `RETURNING id` to eliminate secondary `SELECT` lookups.",
          "`INSERT INTO ... SELECT` allows high-speed server-side data transformation."
        ]
      }
    }
  ],
  "relatedLessons": [
    "insert-on-conflict",
    "update"
  ]
}