{
  "id": "lesson-pg-028",
  "slug": "insert-on-conflict",
  "moduleSlug": "data-modification-dml",
  "courseSlug": "postgresql",
  "title": "UPSERT: INSERT ... ON CONFLICT",
  "description": "Implement race-condition-free upserts with ON CONFLICT DO UPDATE and DO NOTHING targeting unique constraints or partial indexes.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "upsert",
    "on-conflict",
    "concurrency",
    "race-conditions"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In concurrent applications, doing a `SELECT` check before an `INSERT` leads to race conditions (two transactions check simultaneously, see no record, and both attempt to insert, causing unique constraint violations). PostgreSQL solves this with native **UPSERT**: `INSERT ... ON CONFLICT`."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "ON CONFLICT DO UPDATE vs DO NOTHING"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Atomic Counter Upsert with EXCLUDED Pseudo-Table",
        "content": "Increments page view count or inserts initial view record atomically.",
        "language": "sql",
        "code": "INSERT INTO page_views (page_url, view_count, last_visited_at)\nVALUES ('/courses/postgresql', 1, NOW())\nON CONFLICT (page_url) \nDO UPDATE SET \n    view_count = page_views.view_count + 1,\n    last_visited_at = EXCLUDED.last_visited_at\nRETURNING page_url, view_count;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "The `EXCLUDED` Pseudo-Table",
        "text": "`EXCLUDED` represents the row that was proposed for insertion. Referencing `EXCLUDED.col` lets you apply the incoming value to the existing row."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`ON CONFLICT` provides atomic, race-condition-free upsert behavior.",
          "Target column must have an active `UNIQUE` constraint or unique index.",
          "Use `EXCLUDED.column_name` to reference incoming values in the update statement."
        ]
      }
    }
  ],
  "relatedLessons": [
    "insert",
    "merge"
  ]
}