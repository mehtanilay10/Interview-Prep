{
  "id": "lesson-pg-031",
  "slug": "merge",
  "moduleSlug": "data-modification-dml",
  "courseSlug": "postgresql",
  "title": "MERGE Statement (PostgreSQL 15+)",
  "description": "Execute conditional INSERT, UPDATE, and DELETE operations in a single SQL standard MERGE statement.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "merge",
    "sql-standard",
    "upsert",
    "data-warehousing"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Introduced in PostgreSQL 15, the **`MERGE`** statement provides standard ANSI SQL compliance for reconciling two datasets. It allows you to specify `WHEN MATCHED THEN UPDATE / DELETE` and `WHEN NOT MATCHED THEN INSERT` in a single unified command."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reconciling Warehouse Staging Data with MERGE",
        "content": "Synchronizing staging customer data into production tables.",
        "language": "sql",
        "code": "MERGE INTO customers AS target\nUSING staging_customers AS source\nON target.email = source.email\nWHEN MATCHED AND source.is_deleted = true THEN\n    DELETE\nWHEN MATCHED THEN\n    UPDATE SET \n        name = source.name,\n        updated_at = NOW()\nWHEN NOT MATCHED THEN\n    INSERT (email, name, created_at)\n    VALUES (source.email, source.name, NOW());"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`MERGE` was added in PostgreSQL 15 as an ANSI SQL standard alternative to `INSERT ... ON CONFLICT`.",
          "Supports conditional `DELETE` when rows match specific criteria.",
          "Widely used in ETL pipelines and data warehousing reconciliations."
        ]
      }
    }
  ],
  "relatedLessons": [
    "insert-on-conflict",
    "delete"
  ]
}