{
  "id": "lesson-pg-029",
  "slug": "update",
  "moduleSlug": "data-modification-dml",
  "courseSlug": "postgresql",
  "title": "UPDATE Statement & Multi-Table Joins",
  "description": "Update records efficiently, mutate columns based on other tables using UPDATE ... FROM, and retrieve modified rows with RETURNING.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "update",
    "update-from",
    "dml",
    "returning"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **`UPDATE`** statement modifies existing column values in a table. In PostgreSQL, updates can reference columns from other tables using the non-standard but extremely useful **`UPDATE ... FROM`** syntax."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multi-Table UPDATE with FROM Clause"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Applying Tiered Discounts from Pricing Table",
        "content": "Updates product prices based on category discount rules.",
        "language": "sql",
        "code": "UPDATE products p\nSET price = p.price * (1 - c.discount_rate)\nFROM categories c\nWHERE p.category_id = c.id\n  AND c.discount_rate > 0\nRETURNING p.id, p.title, p.price;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "HOT (Heap-Only Tuples) Updates",
        "text": "In PostgreSQL, an UPDATE writes a new version of the row tuple (MVCC). If the updated columns are not indexed and there is space in the original heap page, PostgreSQL performs a **Heap-Only Tuple (HOT)** update, avoiding the massive cost of rewriting index pointers!"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Always include a `WHERE` clause in `UPDATE` to avoid modifying every row in the table.",
          "Use `UPDATE ... FROM` to join other tables during mutation.",
          "Avoid updating indexed columns unnecessarily to maximize HOT update optimization."
        ]
      }
    }
  ],
  "relatedLessons": [
    "insert",
    "delete"
  ]
}