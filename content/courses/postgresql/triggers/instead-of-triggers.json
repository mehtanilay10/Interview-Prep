{
  "id": "lesson-pg-079",
  "slug": "instead-of-triggers",
  "moduleSlug": "triggers",
  "courseSlug": "postgresql",
  "title": "INSTEAD OF Triggers on Complex Views",
  "description": "Make complex multi-table views fully updatable and intercept writes using INSTEAD OF triggers.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [
    "instead-of-triggers",
    "views",
    "updatable-views",
    "abstraction"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "While simple single-table views are automatically updatable, complex views containing `JOIN`s, aggregates, or `UNION`s are read-only. An **`INSTEAD OF` trigger** intercepts `INSERT`, `UPDATE`, or `DELETE` statements on the view and redirects mutations to the underlying base tables."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Making a User Profile View Updatable",
        "content": "Splitting writes to users and profiles tables.",
        "language": "sql",
        "code": "CREATE OR REPLACE FUNCTION insert_into_user_view()\nRETURNS TRIGGER AS $$\nDECLARE\n    v_user_id UUID;\nBEGIN\n    INSERT INTO users (email, name) VALUES (NEW.email, NEW.name) RETURNING id INTO v_user_id;\n    INSERT INTO profiles (user_id, bio) VALUES (v_user_id, NEW.bio);\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_user_view_insert\nINSTEAD OF INSERT ON user_directory_view\nFOR EACH ROW EXECUTE FUNCTION insert_into_user_view();"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`INSTEAD OF` triggers apply strictly to Views, not physical tables.",
          "They intercept mutations and redirect writes to underlying relational tables.",
          "Provides clean data abstraction for legacy systems."
        ]
      }
    }
  ],
  "relatedLessons": [
    "simple-views",
    "trigger-basics"
  ]
}