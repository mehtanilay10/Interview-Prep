{
  "id": "lesson-pg-054",
  "slug": "btree-indexes",
  "moduleSlug": "indexes",
  "courseSlug": "postgresql",
  "title": "B-Tree Indexes & Composite Index Ordering",
  "description": "Master B-tree structure, left-prefix rule for composite indexes, equality vs range column positioning.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "btree",
    "composite-index",
    "left-prefix",
    "sargability"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **B-tree (Balanced Tree)** index is the workhorse of PostgreSQL. It maintains sorted key order from root to leaf pages. For **composite indexes** (indexes across multiple columns), the order in which columns are declared dictates which queries can utilize the index."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Left-Prefix Rule for Composite Indexes"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If you create an index on `(tenant_id, status, created_at)`: queries filtering by `tenant_id` can use the index; queries filtering by `tenant_id` AND `status` can use the index; but queries filtering ONLY by `status` or `created_at` CANNOT use the index efficiently because they skip the leading column."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Golden Rule: Equality Columns First, Range Columns Last",
        "content": "Ordering columns in composite indexes for maximum seek efficiency.",
        "language": "sql",
        "code": "-- ❌ POOR: Range column (created_at) declared first halts index seek for status!\nCREATE INDEX idx_orders_bad ON orders (created_at, status, user_id);\n\n-- ✅ OPTIMAL: Equality columns first (user_id, status), range column last (created_at)\nCREATE INDEX idx_orders_good ON orders (user_id, status, created_at);"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Composite indexes must be searched from the leftmost column outward.",
          "Always place equality (`=`) filter columns first in composite index definitions.",
          "Place inequality / range (`>`, `<`, `BETWEEN`) filter columns at the end."
        ]
      }
    }
  ],
  "relatedLessons": [
    "covering-indexes",
    "index-fundamentals"
  ]
}