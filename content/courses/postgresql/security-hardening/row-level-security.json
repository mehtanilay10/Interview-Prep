{
  "id": "lesson-pg-082",
  "slug": "row-level-security",
  "moduleSlug": "security-hardening",
  "courseSlug": "postgresql",
  "title": "Row-Level Security (RLS) & Multi-Tenancy",
  "description": "Isolate tenant data at the database engine level using CREATE POLICY, current_setting(), and Row-Level Security.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "rls",
    "row-level-security",
    "multi-tenancy",
    "policies"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Row-Level Security (RLS)** restricts which rows are returned or modified based on the user executing the query. Even if an application query forgets a `WHERE tenant_id = $1` clause, PostgreSQL enforces the security policy at the storage layer, preventing cross-tenant data leaks."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multi-Tenant RLS Implementation with Session Settings"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Enabling RLS on Documents Table",
        "content": "Policy reads app.current_tenant_id from session configuration.",
        "language": "sql",
        "code": "-- 1. Enable RLS on the table\nALTER TABLE documents ENABLE ROW LEVEL SECURITY;\n\n-- 2. Define policy for all DML operations\nCREATE POLICY tenant_isolation_policy ON documents\nFOR ALL\nUSING (tenant_id = NULLIF(current_setting('app.current_tenant_id', true), '')::UUID);\n\n-- 3. Application sets tenant ID at connection start\nSET LOCAL app.current_tenant_id = '11111111-2222-3333-4444-555555555555';\n\n-- Returns ONLY rows matching tenant 11111111-2222...\nSELECT * FROM documents;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "RLS transparently filters rows for non-superusers based on defined policies.",
          "Combine `current_setting('app.tenant_id')` with RLS for multi-tenant SaaS.",
          "Table owners and superusers bypass RLS by default unless `FORCE ROW LEVEL SECURITY` is set."
        ]
      }
    }
  ],
  "relatedLessons": [
    "roles-privileges",
    "auditing"
  ]
}