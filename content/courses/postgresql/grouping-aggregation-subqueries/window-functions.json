{
  "id": "lesson-pg-021",
  "slug": "window-functions",
  "moduleSlug": "grouping-aggregation-subqueries",
  "courseSlug": "postgresql",
  "title": "Window Functions & Analytical Queries",
  "description": "Perform advanced analytics across row sets without collapsing rows using OVER, PARTITION BY, ROW_NUMBER, RANK, DENSE_RANK, and LAG/LEAD.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "window-functions",
    "over",
    "partition-by",
    "row-number",
    "lead-lag"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Unlike `GROUP BY`, which collapses multiple rows into a single row, a **Window Function** computes a calculation across a set of table rows related to the current row, while **retaining individual row identities** in the output."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The `OVER (PARTITION BY ... ORDER BY ...)` Anatomy"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Core Window Clauses",
        "items": [
          "**`PARTITION BY`**: Divides rows into distinct groups (windows) that share the same partition key.",
          "**`ORDER BY`**: Dictates the sorting of rows within each partition.",
          "**Frame Specification (`ROWS / RANGE BETWEEN ...`)**: Defines the subset of rows within the partition evaluated for each row."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Top Analytical Window Functions"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Function",
          "Description",
          "Tie Handling"
        ],
        "rows": [
          [
            "`ROW_NUMBER()`",
            "Assigns consecutive integer starting at 1 for each row in partition.",
            "Strictly unique numbers (1, 2, 3, 4)"
          ],
          [
            "`RANK()`",
            "Assigns rank with gaps on ties.",
            "Ties share rank; next rank is skipped (1, 2, 2, 4)"
          ],
          [
            "`DENSE_RANK()`",
            "Assigns rank without gaps on ties.",
            "Ties share rank; next rank is immediate (1, 2, 2, 3)"
          ],
          [
            "`LAG(col, offset)`",
            "Accesses data from the previous row in the partition.",
            "Useful for period-over-period growth"
          ],
          [
            "`LEAD(col, offset)`",
            "Accesses data from the subsequent row in the partition.",
            "Useful for duration calculation until next event"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Month-Over-Month Revenue Growth with LAG()",
        "content": "Comparing current month revenue against previous month revenue.",
        "language": "sql",
        "code": "SELECT \n    sales_month,\n    revenue,\n    LAG(revenue, 1) OVER (ORDER BY sales_month) AS prev_month_revenue,\n    ROUND(\n        (revenue - LAG(revenue, 1) OVER (ORDER BY sales_month)) * 100.0 / \n        NULLIF(LAG(revenue, 1) OVER (ORDER BY sales_month), 0), 2\n    ) AS growth_percentage\nFROM monthly_sales\nORDER BY sales_month DESC;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Window functions calculate metrics across partitions while keeping every original row.",
          "Use `ROW_NUMBER()` for deduplication and pagination.",
          "Use `LAG` and `LEAD` to inspect preceding or following rows without self-joins."
        ]
      }
    }
  ],
  "relatedLessons": [
    "group-by",
    "ctes"
  ]
}