{
  "id": "lesson-pg-023",
  "slug": "correlated-subqueries",
  "moduleSlug": "grouping-aggregation-subqueries",
  "courseSlug": "postgresql",
  "title": "Subqueries & Correlated Subqueries",
  "description": "Distinguish scalar subqueries, subqueries with IN / EXISTS, and master correlated subqueries with planner optimization.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "subqueries",
    "correlated-subqueries",
    "exists",
    "in"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **subquery** is a query nested inside another query. When a subquery executes independently of the outer query, it is an **uncorrelated subquery**. When a subquery references columns from the outer query, it is a **correlated subquery**."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "IN vs EXISTS: Performance Difference"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A frequent optimization question is whether to use `IN (SELECT ...)` or `EXISTS (SELECT ...)`. In PostgreSQL, `EXISTS` terminates as soon as the first matching row is found (short-circuit evaluation), making it significantly faster for checking presence across large tables."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Optimized Membership Check with EXISTS",
        "content": "Notice the SELECT 1 inside EXISTS; it stops scanning upon first match.",
        "language": "sql",
        "code": "-- Efficient check for customers with at least one high-value order\nSELECT c.id, c.name, c.email\nFROM customers c\nWHERE EXISTS (\n    SELECT 1 \n    FROM orders o \n    WHERE o.customer_id = c.id \n      AND o.total_amount > 1000\n);"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "The NOT IN with NULL Disaster",
        "text": "If any row in the subquery returns `NULL`, `NOT IN (SELECT col FROM ...)` evaluates to `UNKNOWN` for all outer rows and returns ZERO records! Always use `NOT EXISTS` instead of `NOT IN` to avoid this notorious SQL pitfall."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Correlated subqueries reference values from the outer query for each row.",
          "Use `EXISTS` instead of `IN` when testing for presence to leverage short-circuit seeks.",
          "Always prefer `NOT EXISTS` over `NOT IN` to prevent NULL-induced zero-result bugs."
        ]
      }
    }
  ],
  "relatedLessons": [
    "ctes",
    "lateral-join"
  ]
}