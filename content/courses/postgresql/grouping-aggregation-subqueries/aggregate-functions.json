{
  "id": "lesson-pg-018",
  "slug": "aggregate-functions",
  "moduleSlug": "grouping-aggregation-subqueries",
  "courseSlug": "postgresql",
  "title": "Aggregate Functions & Statistical Calculations",
  "description": "Compute summary statistics with COUNT, SUM, AVG, MIN, MAX, and PostgreSQL's FILTER clause.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "aggregates",
    "count",
    "sum",
    "filter-clause",
    "statistics"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Aggregate functions compute a single summary value across a collection of rows. Common functions include `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`, `STRING_AGG`, and `JSONB_AGG`."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "COUNT(*) vs COUNT(column)"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Syntax",
          "Behavior",
          "NULL Handling"
        ],
        "rows": [
          [
            "`COUNT(*)`",
            "Counts all rows in the group regardless of contents.",
            "Includes rows with NULLs."
          ],
          [
            "`COUNT(column)`",
            "Counts only rows where specified column is NOT NULL.",
            "Skips NULL values."
          ],
          [
            "`COUNT(DISTINCT column)`",
            "Counts unique non-null values.",
            "Skips duplicates and NULLs."
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Modern `FILTER (WHERE ...)` Clause"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "SQL:2003 introduced the `FILTER (WHERE condition)` clause for aggregate functions. PostgreSQL implements this cleanly, eliminating messy `CASE WHEN` statements when computing conditional metrics."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Conditional Aggregates with FILTER",
        "content": "Computing multiple metrics in a single pass over the table.",
        "language": "sql",
        "code": "SELECT \n    COUNT(*) AS total_orders,\n    SUM(total_amount) AS total_revenue,\n    -- Using FILTER instead of clumsy SUM(CASE WHEN ...)\n    SUM(total_amount) FILTER (WHERE status = 'PAID') AS paid_revenue,\n    COUNT(*) FILTER (WHERE status = 'REFUNDED') AS refunded_orders_count,\n    ROUND(AVG(total_amount) FILTER (WHERE status = 'PAID'), 2) AS avg_paid_order_value\nFROM orders;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`COUNT(*)` counts rows; `COUNT(column)` ignores NULL values.",
          "Use `FILTER (WHERE condition)` for clean, performant conditional aggregations.",
          "Aggregate functions can be combined with `STRING_AGG` and `JSONB_AGG` to group values into arrays."
        ]
      }
    }
  ],
  "relatedLessons": [
    "group-by",
    "having-clause"
  ]
}