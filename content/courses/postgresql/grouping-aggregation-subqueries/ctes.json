{
  "id": "lesson-pg-022",
  "slug": "ctes",
  "moduleSlug": "grouping-aggregation-subqueries",
  "courseSlug": "postgresql",
  "title": "Common Table Expressions (CTEs) & Recursive Queries",
  "description": "Write clean, modular SQL queries with WITH clauses, and traverse hierarchical trees and graphs with RECURSIVE CTEs.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "cte",
    "with",
    "recursive",
    "hierarchies",
    "subqueries"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **Common Table Expression (CTE)** is a temporary result set defined within the execution scope of a single `SELECT`, `INSERT`, `UPDATE`, or `DELETE` statement. Introduced via the `WITH` keyword, CTEs replace deeply nested subqueries with readable, sequential code."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Standard CTE Example"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multi-Step Data Preparation with CTEs",
        "content": "Structuring complex reporting into readable stages.",
        "language": "sql",
        "code": "WITH regional_metrics AS (\n    SELECT \n        region,\n        SUM(amount) AS total_sales,\n        COUNT(DISTINCT customer_id) AS active_customers\n    FROM orders\n    WHERE created_at >= NOW() - INTERVAL '30 days'\n    GROUP BY region\n),\nranked_regions AS (\n    SELECT \n        region,\n        total_sales,\n        active_customers,\n        DENSE_RANK() OVER (ORDER BY total_sales DESC) AS sales_rank\n    FROM regional_metrics\n)\nSELECT * FROM ranked_regions WHERE sales_rank <= 5;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Recursive CTEs for Tree & Graph Traversal"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **Recursive CTE** (`WITH RECURSIVE`) repeatedly evaluates a union between an initial base case (anchor member) and a recursive step (recursive member) until no new rows are produced. It is the gold standard for querying organizational hierarchies, category trees, and bill-of-materials."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Traversing an Employee Management Hierarchy",
        "content": "Recursively finding all subordinates under a specific director.",
        "language": "sql",
        "code": "WITH RECURSIVE org_chart AS (\n    -- 1. Anchor Member: Base case (CEO or Department Head)\n    SELECT id, name, manager_id, 1 AS depth\n    FROM employees\n    WHERE id = 'emp-ceo-001'\n\n    UNION ALL\n\n    -- 2. Recursive Member: Join employees to their managers in org_chart\n    SELECT e.id, e.name, e.manager_id, oc.depth + 1\n    FROM employees e\n    INNER JOIN org_chart oc ON oc.id = e.manager_id\n)\nSELECT * FROM org_chart ORDER BY depth, name;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "CTEs (`WITH`) make complex queries modular, readable, and maintainable.",
          "Since PostgreSQL 12, non-recursive CTEs can be inlined by the optimizer by default for high performance.",
          "Use `WITH RECURSIVE` to traverse organizational charts, folder hierarchies, and graph structures."
        ]
      }
    }
  ],
  "relatedLessons": [
    "correlated-subqueries",
    "window-functions"
  ]
}