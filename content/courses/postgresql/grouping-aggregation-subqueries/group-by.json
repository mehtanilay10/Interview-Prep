{
  "id": "lesson-pg-019",
  "slug": "group-by",
  "moduleSlug": "grouping-aggregation-subqueries",
  "courseSlug": "postgresql",
  "title": "GROUP BY Clause & Aggregation Buckets",
  "description": "Partition rows into groups, avoid non-aggregated column errors, and explore advanced ROLLUP and CUBE grouping sets.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "group-by",
    "aggregation",
    "grouping-sets",
    "rollup",
    "cube"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **`GROUP BY`** clause collapses multiple rows sharing identical values in designated columns into summary rows. Any column projected in the `SELECT` list that is not part of the `GROUP BY` clause must be enclosed within an aggregate function."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Grouping by Date Truncation"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Monthly Revenue Aggregation with DATE_TRUNC",
        "content": "Grouping sales data by month and payment method.",
        "language": "sql",
        "code": "SELECT \n    DATE_TRUNC('month', created_at) AS sales_month,\n    payment_method,\n    COUNT(*) AS total_transactions,\n    SUM(amount) AS gross_sales\nFROM transactions\nGROUP BY DATE_TRUNC('month', created_at), payment_method\nORDER BY sales_month DESC, gross_sales DESC;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Advanced: GROUPING SETS, ROLLUP, and CUBE"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When generating financial balance sheets and BI reports, you often need subtotals and grand totals in addition to grouped rows. `ROLLUP` produces hierarchical subtotals automatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Sales Report with ROLLUP Subtotals",
        "content": "Produces region subtotals, product subtotals, and overall grand totals.",
        "language": "sql",
        "code": "SELECT \n    region,\n    product_category,\n    SUM(revenue) AS total_revenue\nFROM regional_sales\nGROUP BY ROLLUP (region, product_category)\nORDER BY region NULLS LAST, product_category NULLS LAST;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Every column in `SELECT` must either appear in `GROUP BY` or be enclosed in an aggregate function.",
          "Use `DATE_TRUNC('day' | 'month' | 'year', timestamp)` to group by time periods.",
          "Use `ROLLUP` and `CUBE` to compute multi-level subtotals and grand totals in a single query."
        ]
      }
    }
  ],
  "relatedLessons": [
    "aggregate-functions",
    "having-clause"
  ]
}