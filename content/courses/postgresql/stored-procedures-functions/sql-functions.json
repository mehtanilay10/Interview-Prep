{
  "id": "lesson-pg-071",
  "slug": "sql-functions",
  "moduleSlug": "stored-procedures-functions",
  "courseSlug": "postgresql",
  "title": "User-Defined SQL Functions",
  "description": "Write lightweight, inlinable user-defined functions in plain SQL with parameter binding and volatility markers.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "sql-functions",
    "inlining",
    "volatility",
    "udf"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "PostgreSQL allows defining functions written in pure SQL (`LANGUAGE sql`). SQL functions can be **inlined** by the query planner, meaning the optimizer inserts the function query directly into the caller's query plan with zero function-call overhead!"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Function Volatility: IMMUTABLE, STABLE, VOLATILE"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Volatility",
          "Behavior",
          "Example"
        ],
        "rows": [
          [
            "`IMMUTABLE`",
            "Always returns same result for same inputs; can be pre-computed in index expressions.",
            "Mathematical calculations (`col1 * 1.25`)"
          ],
          [
            "`STABLE`",
            "Result stays constant within a single query execution; scans can cache results.",
            "`NOW()`, table lookups"
          ],
          [
            "`VOLATILE` (Default)",
            "Result can change at any moment; optimizer cannot cache calls.",
            "`random()`, data modifications"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Pure SQL Function with Inlining Capability",
        "content": "Computing tax-inclusive totals.",
        "language": "sql",
        "code": "CREATE OR REPLACE FUNCTION calculate_gross_total(subtotal NUMERIC, tax_rate NUMERIC)\nRETURNS NUMERIC AS $$\n    SELECT ROUND(subtotal * (1 + tax_rate), 2);\n$$ LANGUAGE sql IMMUTABLE PARALLEL SAFE;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Plain SQL functions can be inlined by the optimizer for maximum speed.",
          "Always mark deterministic functions as `IMMUTABLE`.",
          "Only `IMMUTABLE` functions can be used in Expression Indexes."
        ]
      }
    }
  ],
  "relatedLessons": [
    "plpgsql-basics",
    "plpgsql-functions"
  ]
}