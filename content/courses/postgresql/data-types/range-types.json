{
  "id": "lesson-pg-037",
  "slug": "range-types",
  "moduleSlug": "data-types",
  "courseSlug": "postgresql",
  "title": "Range Types & Overlap Operators",
  "description": "Model temporal spans and numeric ranges using TSRANGE, DATERANGE, INT4RANGE, and the overlap operator &&.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [
    "range-types",
    "tsrange",
    "overlap",
    "exclusion-constraints"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In traditional databases, modeling a date range requires two separate columns (`start_date` and `end_date`) and complex `WHERE (start1 <= end2 AND end1 >= start2)` logic. PostgreSQL includes built-in **Range Types** (`DATERANGE`, `TSRANGE`, `TSTZRANGE`, `NUMRANGE`) that represent contiguous ranges of values."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Hotel Booking Overlap Check with && Operator",
        "content": "Using the overlap operator && to detect conflicting room reservations.",
        "language": "sql",
        "code": "CREATE TABLE room_bookings (\n    id SERIAL PRIMARY KEY,\n    room_number INT,\n    stay_duration DATERANGE\n);\n\n-- Find bookings overlapping with Christmas week\nSELECT * FROM room_bookings\nWHERE stay_duration && daterange('2024-12-24', '2024-12-31');"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Range types encapsulate continuous spans with inclusivity markers `[)`, `[]`, etc.",
          "The `&&` operator checks if two ranges overlap.",
          "Range types can be paired with GiST indexes and exclusion constraints to guarantee non-overlapping reservations."
        ]
      }
    }
  ],
  "relatedLessons": [
    "date-time-types",
    "exclusion-constraints"
  ]
}