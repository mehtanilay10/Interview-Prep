{
  "id": "lesson-pg-034",
  "slug": "text-types",
  "moduleSlug": "data-types",
  "courseSlug": "postgresql",
  "title": "Text Types: VARCHAR vs CHAR vs TEXT",
  "description": "Understand character storage in PostgreSQL, debunk the VARCHAR performance myth, and utilize collations.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "text",
    "varchar",
    "char",
    "performance",
    "collations"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In many relational databases (like MySQL or Oracle), `VARCHAR(255)` is faster than `TEXT`. **In PostgreSQL, this is a myth!** Under the hood, `VARCHAR`, `CHAR`, and `TEXT` all use the exact same underlying `varlena` (variable-length array) C data structure."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Text Type Breakdown"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Type",
          "Length Limit",
          "Trailing Spaces",
          "Recommendation"
        ],
        "rows": [
          [
            "`TEXT`",
            "Unlimited (up to 1GB)",
            "Preserved as-is",
            "Recommended default for strings unless length check is a domain constraint"
          ],
          [
            "`VARCHAR(n)`",
            "Max `n` characters",
            "Preserved as-is",
            "Use when schema must enforce a length limit (e.g. phone numbers, zip codes)"
          ],
          [
            "`CHAR(n)`",
            "Fixed `n` characters",
            "Padded with whitespace!",
            "Avoid in modern schemas; whitespace padding wastes storage"
          ]
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "There is zero performance penalty for using `TEXT` instead of `VARCHAR` in PostgreSQL.",
          "Use `VARCHAR(n)` strictly to enforce business validation at the database layer.",
          "Never use `CHAR(n)` due to wasteful space padding behavior."
        ]
      }
    }
  ],
  "relatedLessons": [
    "numeric-types",
    "jsonb-arrays"
  ]
}