{
  "id": "lesson-pg-067",
  "slug": "locking-rows",
  "moduleSlug": "transactions-error-handling-backup",
  "courseSlug": "postgresql",
  "title": "Explicit Row Locking: FOR UPDATE & FOR SHARE",
  "description": "Prevent lost updates with SELECT FOR UPDATE, implement high-concurrency job queues with SKIP LOCKED, and handle deadlocks.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "locking",
    "for-update",
    "skip-locked",
    "deadlocks",
    "queues"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When building concurrent workflows (such as reservation booking or background job dispatching), multiple transactions reading the same row can cause **lost updates**. PostgreSQL provides explicit row-level locking via **`SELECT ... FOR UPDATE`**."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Building High-Throughput Job Queues with `SKIP LOCKED`"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In standard locking, worker processes wait on locked rows. Using **`SKIP LOCKED`**, workers skip already-locked rows and grab the next available unlocked job, providing linear scale without lock contention!"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Concurrent Background Worker Job Claiming",
        "content": "Multiple worker nodes can execute this simultaneously without blocking each other.",
        "language": "sql",
        "code": "BEGIN;\n\n-- Atomically select and lock the highest priority pending job\nSELECT id, payload\nFROM queue_jobs\nWHERE status = 'PENDING'\nORDER BY priority DESC, created_at ASC\nLIMIT 1\nFOR UPDATE SKIP LOCKED;\n\n-- Update status for the claimed job\nUPDATE queue_jobs\nSET status = 'PROCESSING', started_at = NOW()\nWHERE id = $claimed_id;\n\nCOMMIT;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`FOR UPDATE` acquires an exclusive lock on selected rows until transaction commit.",
          "Use `SKIP LOCKED` to build lock-free concurrent task queues.",
          "Always access resources in a consistent global order to prevent deadlocks."
        ]
      }
    }
  ],
  "relatedLessons": [
    "transaction-fundamentals",
    "isolation-levels"
  ]
}