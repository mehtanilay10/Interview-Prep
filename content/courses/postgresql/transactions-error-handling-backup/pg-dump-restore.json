{
  "id": "lesson-pg-069",
  "slug": "pg-dump-restore",
  "moduleSlug": "transactions-error-handling-backup",
  "courseSlug": "postgresql",
  "title": "Logical Backups: pg_dump & pg_restore",
  "description": "Export databases with pg_dump custom format (-Fc), perform parallel backups (-j), and restore selectively with pg_restore.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "pg-dump",
    "pg-restore",
    "backup",
    "disaster-recovery",
    "dba"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**`pg_dump`** is the standard logical backup utility for PostgreSQL. It takes a consistent snapshot of the database without blocking read or write operations."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Best Practice: Custom Compressed Format (`-Fc`)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Parallel Dump and Selective Restore",
        "content": "Exporting with compression and multi-core parallelism.",
        "language": "bash",
        "code": "# 1. Export in custom compressed format using 4 parallel CPU jobs\npg_dump -h localhost -U postgres -d production_db -Fc -j 4 -f prod_backup.dump\n\n# 2. Restore entire database using parallel restore\npg_restore -h localhost -U postgres -d target_db -j 4 --clean --if-exists prod_backup.dump\n\n# 3. Restore only a single specific table from the backup file\npg_restore -h localhost -U postgres -d target_db -t customers prod_backup.dump"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Always use `-Fc` (custom format) for pg_dump to allow selective table restores.",
          "Use `-j` (jobs) to parallelize dump and restore across multiple CPU cores.",
          "`pg_dump` takes a non-blocking snapshot using `REPEATABLE READ` transactions."
        ]
      }
    }
  ],
  "relatedLessons": [
    "point-in-time-recovery",
    "transaction-fundamentals"
  ]
}