{
  "id": "lesson-pg-005",
  "slug": "select-statement",
  "moduleSlug": "querying-data",
  "courseSlug": "postgresql",
  "title": "SELECT Statement & Projections",
  "description": "Query relational tables, project specific columns, avoid SELECT *, compute expressions, and filter duplicates with DISTINCT and DISTINCT ON.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "select",
    "projection",
    "distinct",
    "distinct-on",
    "sql"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **`SELECT`** statement is the primary retrieval command in SQL. It specifies the projection list (columns and expressions to return) and the data source (`FROM` clause)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why You Must Avoid `SELECT *` in Production"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Hidden Pitfalls of SELECT *",
        "items": [
          "**Bandwidth & Memory Bloat**: Fetches wide columns (such as `TEXT`, `JSONB`, `BYTEA`) even if the application UI only displays an ID and a Name.",
          "**Defeats Index-Only Scans**: PostgreSQL cannot satisfy a query using an index alone if columns outside the index are requested with `*`.",
          "**Breaks Schema Decoupling**: If a new column is added or column order changes, brittle client applications expecting positional arguments will fail."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "DISTINCT vs PostgreSQL-Specific DISTINCT ON"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Standard SQL provides `DISTINCT` to eliminate duplicate rows across all projected columns. However, PostgreSQL offers an invaluable extension: **`DISTINCT ON (expression)`**. It retains only the first row of each group evaluated according to the `ORDER BY` clause."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Finding the Latest Order for Every Customer with DISTINCT ON",
        "content": "PostgreSQL's DISTINCT ON simplifies what would otherwise require complex window functions or correlated subqueries.",
        "language": "sql",
        "code": "-- Retrieve each customer's most recent order\nSELECT DISTINCT ON (customer_id)\n    customer_id,\n    order_id,\n    order_date,\n    total_amount\nFROM orders\nORDER BY customer_id, order_date DESC;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "DISTINCT ON Rule",
        "text": "The leftmost expressions in `ORDER BY` must match the expressions inside `DISTINCT ON (...)` to ensure deterministic result selection."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Always project explicit columns instead of using `SELECT *`.",
          "Use expressions and aliases (`AS alias_name`) to format result sets.",
          "Use PostgreSQL's unique `DISTINCT ON (col)` to retrieve top-1 items per group effortlessly."
        ]
      }
    }
  ],
  "relatedLessons": [
    "where-clause",
    "order-by"
  ]
}