{
  "id": "lesson-pg-006",
  "slug": "where-clause",
  "moduleSlug": "querying-data",
  "courseSlug": "postgresql",
  "title": "WHERE Clause & Filtering Logic",
  "description": "Filter datasets with comparison operators, pattern matching, case-insensitive ILIKE, and understand how the planner utilizes index scans.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "where",
    "filtering",
    "ilike",
    "sargable",
    "operators"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **`WHERE`** clause specifies search conditions (predicates) that filter rows before aggregation or projection. Rows are included in the result set only if the search condition evaluates to `TRUE`."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "PostgreSQL Comparison & Pattern Operators"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Operator",
          "Description",
          "Example"
        ],
        "rows": [
          [
            "`=` / `<>` or `!=`",
            "Equality / Inequality",
            "`WHERE status = 'ACTIVE'`"
          ],
          [
            "`<`, `<=`, `>`, `>=`",
            "Comparison operators",
            "`WHERE price >= 49.99`"
          ],
          [
            "`LIKE`",
            "Case-sensitive pattern matching (`%` matches any sequence, `_` matches single char)",
            "`WHERE email LIKE '%@gmail.com'`"
          ],
          [
            "`ILIKE`",
            "PostgreSQL-exclusive case-insensitive pattern matching",
            "`WHERE username ILIKE 'john%'`"
          ],
          [
            "`~` / `~*`",
            "Regular expression match (case-sensitive / case-insensitive)",
            "`WHERE phone ~ '^\\d{3}-\\d{4}$'`"
          ],
          [
            "`IS NULL` / `IS NOT NULL`",
            "Three-valued logic check for missing/unknown data",
            "`WHERE deleted_at IS NULL`"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Writing SARGable (Index-Friendly) Predicates"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A query is **SARGable** (Search Argument Able) when the database query optimizer can leverage an index to locate rows. Wrapping indexed columns in functions disables B-tree index usage unless an expression index exists!"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Non-SARGable vs SARGable Filtering",
        "content": "Notice how modifying the column side invalidates standard indexes.",
        "language": "sql",
        "code": "-- ❌ NON-SARGABLE: Wraps created_at in DATE() function; forces full table scan!\nSELECT id FROM users \nWHERE DATE(created_at) = '2024-01-15';\n\n-- ✅ SARGABLE: Leaves column untouched; uses B-tree index on created_at\nSELECT id FROM users \nWHERE created_at >= '2024-01-15 00:00:00' \n  AND created_at <  '2024-01-16 00:00:00';"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`WHERE` filters rows based on predicates evaluating to boolean `TRUE`.",
          "Use `ILIKE` for case-insensitive matching without manual `LOWER()` calls.",
          "Keep columns free of function wrappers to maintain index seek capability (SARGability)."
        ]
      }
    }
  ],
  "relatedLessons": [
    "select-statement",
    "and-or-operators"
  ]
}