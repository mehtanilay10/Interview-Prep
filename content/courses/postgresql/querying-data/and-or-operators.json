{
  "id": "lesson-pg-007",
  "slug": "and-or-operators",
  "moduleSlug": "querying-data",
  "courseSlug": "postgresql",
  "title": "AND, OR, and NOT Boolean Operators",
  "description": "Combine compound boolean predicates, master operator precedence, avoid logic bugs, and inspect Bitmap Index Scans.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "boolean-logic",
    "and",
    "or",
    "not",
    "operator-precedence"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Complex filtering requires combining conditions using `AND`, `OR`, and `NOT`. In SQL, **`AND` takes precedence over `OR`**, which is one of the most common sources of logical bugs in SQL queries."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Operator Precedence Trap"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Why Parentheses are Critical",
        "content": "Demonstrating how operator precedence alters query semantics.",
        "language": "sql",
        "code": "-- ❌ BUG: Evaluates as: role = 'ADMIN' OR (role = 'MANAGER' AND department = 'FINANCE')\n-- Result: Returns ALL Admins regardless of department!\nSELECT * FROM employees \nWHERE role = 'ADMIN' OR role = 'MANAGER' AND department = 'FINANCE';\n\n-- ✅ CORRECT: Explicit parentheses group the OR condition first\nSELECT * FROM employees \nWHERE (role = 'ADMIN' OR role = 'MANAGER') AND department = 'FINANCE';"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How PostgreSQL Executes `OR` with Indexes (BitmapOr)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When filtering with `OR` on two separate indexed columns (`WHERE email = $1 OR phone = $2`), PostgreSQL builds in-memory bitmaps of matching physical page addresses for both indexes and performs a **BitmapOr** operation before fetching actual heap tuples."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`AND` has higher precedence than `OR`.",
          "Always wrap `OR` expressions in explicit parentheses when combining with `AND`.",
          "PostgreSQL uses Bitmap Index Scans to satisfy multi-column `OR` filters efficiently."
        ]
      }
    }
  ],
  "relatedLessons": [
    "where-clause",
    "between-in-like"
  ]
}