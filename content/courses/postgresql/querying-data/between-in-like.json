{
  "id": "lesson-pg-008",
  "slug": "between-in-like",
  "moduleSlug": "querying-data",
  "courseSlug": "postgresql",
  "title": "BETWEEN, IN, and Pattern Matching",
  "description": "Work with range filtering via BETWEEN, list matching with IN and ANY, and advanced pattern matching with LIKE and SIMILAR TO.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "between",
    "in",
    "any",
    "like",
    "pattern-matching"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "SQL provides dedicated operators to simplify queries: `BETWEEN` filters across continuous ranges, `IN` filters against discrete sets of values, and `LIKE` performs pattern matching against string data."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "1. The `BETWEEN` Operator is Inclusive"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`value BETWEEN low AND high` is shorthand for `value >= low AND value <= high`. Because it is inclusive of both boundary endpoints, be cautious when using `BETWEEN` on timestamp fields containing time components."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "2. `IN` vs `= ANY(ARRAY[...])` in PostgreSQL"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In PostgreSQL, filtering against an array parameter is often cleaner and faster using `= ANY($1)` rather than dynamically constructing a string of comma-separated values in an `IN (...)` clause."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Comparing IN with = ANY()",
        "content": "Using parameterized array filtering.",
        "language": "sql",
        "code": "-- Standard IN clause\nSELECT * FROM products \nWHERE status IN ('IN_STOCK', 'LOW_STOCK', 'BACKORDER');\n\n-- Parameterized ANY clause (preferred for prepared statements with array bindings)\nSELECT * FROM products \nWHERE status = ANY(ARRAY['IN_STOCK', 'LOW_STOCK', 'BACKORDER']);"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`BETWEEN` is inclusive on both boundaries (`>= a AND <= b`).",
          "`IN` tests membership against a literal list or subquery result.",
          "Use `= ANY(ARRAY[...])` in PostgreSQL when passing array parameters from application code."
        ]
      }
    }
  ],
  "relatedLessons": [
    "where-clause",
    "null-handling"
  ]
}