{
  "id": "lesson-pg-009",
  "slug": "null-handling",
  "moduleSlug": "querying-data",
  "courseSlug": "postgresql",
  "title": "NULL Handling & Three-Valued Logic",
  "description": "Master SQL's Three-Valued Logic (TRUE, FALSE, UNKNOWN), handle missing data with COALESCE, NULLIF, and safe equality operators.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "null",
    "coalesce",
    "nullif",
    "three-valued-logic",
    "sql"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In SQL, `NULL` does not mean zero, an empty string, or false. `NULL` represents an **unknown or missing value**. Because of this, SQL operates on **Three-Valued Logic (3VL)**, where boolean expressions can evaluate to `TRUE`, `FALSE`, or `UNKNOWN`."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The NULL Comparison Trap: Why `x = NULL` is Never True"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Comparing any value to `NULL` using the equality operator (`=`) yields `UNKNOWN`. Even `NULL = NULL` evaluates to `UNKNOWN`! Because the `WHERE` clause only keeps rows that evaluate to `TRUE`, writing `WHERE col = NULL` will always return zero rows."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Correct vs Incorrect NULL Checks",
        "content": "Using IS NULL, IS NOT NULL, and IS DISTINCT FROM.",
        "language": "sql",
        "code": "-- ❌ WRONG: Returns 0 rows because col = NULL is UNKNOWN\nSELECT * FROM users WHERE deleted_at = NULL;\n\n-- ✅ CORRECT: Use IS NULL or IS NOT NULL\nSELECT * FROM users WHERE deleted_at IS NULL;\n\n-- ✅ NULL-Safe Equality: Treats two NULLs as equal\nSELECT * FROM users WHERE status IS NOT DISTINCT FROM NULL;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Essential NULL Functions"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Function",
          "Behavior",
          "Example Usage"
        ],
        "rows": [
          [
            "`COALESCE(val1, val2, ...)`",
            "Returns the first non-null argument in the list.",
            "`COALESCE(nickname, full_name, 'Anonymous')`"
          ],
          [
            "`NULLIF(val1, val2)`",
            "Returns `NULL` if `val1 = val2`; otherwise returns `val1`. Useful for preventing division by zero.",
            "`100 / NULLIF(total_items, 0)`"
          ],
          [
            "`GREATEST / LEAST`",
            "Returns largest/smallest value, ignoring NULLs unless all arguments are NULL.",
            "`GREATEST(discount_a, discount_b)`"
          ]
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "SQL uses Three-Valued Logic: `TRUE`, `FALSE`, and `UNKNOWN`.",
          "Never use `=` or `!=` with `NULL`; always use `IS NULL` or `IS NOT NULL`.",
          "Use `COALESCE` to provide default fallbacks for nullable columns.",
          "Use `NULLIF(denominator, 0)` to eliminate division by zero exceptions."
        ]
      }
    }
  ],
  "relatedLessons": [
    "where-clause",
    "order-by"
  ]
}