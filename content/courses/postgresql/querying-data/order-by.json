{
  "id": "lesson-pg-010",
  "slug": "order-by",
  "moduleSlug": "querying-data",
  "courseSlug": "postgresql",
  "title": "ORDER BY & Sorting Control",
  "description": "Sort result sets deterministically, control NULL positioning with NULLS FIRST/LAST, and sort across collations.",
  "order": 6,
  "difficulty": "beginner",
  "estimatedMinutes": 20,
  "tags": [
    "order-by",
    "sorting",
    "nulls-last",
    "collations"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Relational tables are unordered mathematical sets. Without an explicit **`ORDER BY`** clause, PostgreSQL does not guarantee any deterministic order of returned rows. Physical table order can change at any time due to `VACUUM`, page reorganization, or parallel worker scanning."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Controlling NULL Placement with NULLS FIRST and NULLS LAST"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default in PostgreSQL, `ASC` puts NULLs last (`NULLS LAST`), while `DESC` puts NULLs first (`NULLS FIRST`). You can override this behavior explicitly using `NULLS FIRST` or `NULLS LAST`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Ordering Tasks with NULL Dates at the End",
        "content": "Ensuring completed tasks are sorted newest first, while uncompleted (NULL) dates appear at the end.",
        "language": "sql",
        "code": "-- Show completed tasks newest first, keeping incomplete tasks (NULL) at the bottom\nSELECT id, title, completed_at\nFROM tasks\nORDER BY completed_at DESC NULLS LAST, id ASC;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Index Matching for ORDER BY",
        "text": "If you create an index with `CREATE INDEX idx_tasks ON tasks (completed_at DESC NULLS LAST)`, PostgreSQL can perform a backward index scan to fulfill the query with zero in-memory sort overhead!"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Query results have no guaranteed order without an explicit `ORDER BY` clause.",
          "Use `NULLS FIRST` or `NULLS LAST` to control where null values appear in sorted results.",
          "Always include a unique tie-breaker column (e.g. `id`) for deterministic pagination."
        ]
      }
    }
  ],
  "relatedLessons": [
    "select-statement",
    "limit-offset-fetch"
  ]
}