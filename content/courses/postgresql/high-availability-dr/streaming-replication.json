{
  "id": "lesson-pg-086",
  "slug": "streaming-replication",
  "moduleSlug": "high-availability-dr",
  "courseSlug": "postgresql",
  "title": "Physical Streaming Replication",
  "description": "Architect primary-standby streaming replication, synchronous vs asynchronous replication, and replication slots.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "streaming-replication",
    "ha",
    "wal",
    "standby",
    "replication-slots"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Physical Streaming Replication** streams raw Write-Ahead Log (WAL) records byte-for-byte from a primary database to one or more standby read-replicas. Replicas replay the WAL stream, maintaining an exact binary replica of the primary cluster."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Synchronous vs Asynchronous Replication"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Mode",
          "Commit Guarantee",
          "RPO (Recovery Point Objective)",
          "Write Latency Impact"
        ],
        "rows": [
          [
            "Asynchronous (Default)",
            "Primary commits immediately; streams WAL in background.",
            "Small window of data loss if primary dies before replica receives WAL.",
            "Zero write latency overhead."
          ],
          [
            "Synchronous (`synchronous_commit = on`)",
            "Primary waits until at least one standby confirms WAL receipt before returning success to client.",
            "Zero data loss (RPO = 0).",
            "Adds network roundtrip latency to every transaction commit."
          ]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Always Use Replication Slots",
        "text": "A **Replication Slot** instructs the primary server not to delete archived WAL files until the replica has acknowledged replaying them, preventing 'WAL segment already removed' errors."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Streaming replication creates exact binary read replicas.",
          "Use replication slots to prevent replicas from falling behind and breaking.",
          "Synchronous replication guarantees zero data loss at the cost of commit latency."
        ]
      }
    }
  ],
  "relatedLessons": [
    "logical-replication-ha",
    "failover-switchover"
  ]
}