{
  "id": "lesson-pg-049",
  "slug": "object-management",
  "moduleSlug": "database-schema-table-objects",
  "courseSlug": "postgresql",
  "title": "Database Object Management & System Catalogs",
  "description": "Query pg_class, pg_attribute, and information_schema to inspect schemas, table sizes, and column definitions.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "catalogs",
    "pg-class",
    "information-schema",
    "dba",
    "introspection"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "PostgreSQL stores all database metadata in internal system catalog tables inside the `pg_catalog` schema. Querying catalogs like `pg_class`, `pg_attribute`, and `pg_tables` lets you inspect disk usage, dead tuples, and table structures programmatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Finding the Largest Tables in the Database",
        "content": "Inspects total table size including indexes.",
        "language": "sql",
        "code": "SELECT \n    relname AS table_name,\n    pg_size_pretty(pg_total_relation_size(c.oid)) AS total_size,\n    pg_size_pretty(pg_relation_size(c.oid)) AS heap_size,\n    pg_size_pretty(pg_total_relation_size(c.oid) - pg_relation_size(c.oid)) AS index_size\nFROM pg_class c\nJOIN pg_namespace n ON n.oid = c.relnamespace\nWHERE n.nspname = 'public' \n  AND c.relkind = 'r'\nORDER BY pg_total_relation_size(c.oid) DESC\nLIMIT 10;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "PostgreSQL's system catalogs (`pg_class`, `pg_stat_user_tables`) provide deep introspective metadata.",
          "Use `pg_size_pretty()` to format byte counts into human-readable MB/GB.",
          "System catalogs power database migration tools, ORM introspection, and DBA dashboards."
        ]
      }
    }
  ],
  "relatedLessons": [
    "create-table",
    "psql-basics"
  ]
}