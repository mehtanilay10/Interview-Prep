{
  "id": "lesson-pg-013",
  "slug": "inner-join",
  "moduleSlug": "joins",
  "courseSlug": "postgresql",
  "title": "INNER JOIN Deep Dive",
  "description": "Combine matching records across multiple tables with ANSI SQL-92 INNER JOIN, join filters, and multi-table relationships.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "inner-join",
    "join-conditions",
    "tables",
    "performance"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**`INNER JOIN`** returns rows when there is a match in both the left and right tables. If a row in Table A does not match any row in Table B according to the join condition, it is omitted from the result set."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multi-Table INNER JOIN Example"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Joining Customers, Orders, and Order Items",
        "content": "Combining three tables using clean table aliases.",
        "language": "sql",
        "code": "SELECT \n    c.id AS customer_id,\n    c.name AS customer_name,\n    o.id AS order_id,\n    o.ordered_at,\n    p.title AS product_name,\n    oi.quantity,\n    oi.unit_price\nFROM customers c\nINNER JOIN orders o \n    ON o.customer_id = c.id\nINNER JOIN order_items oi \n    ON oi.order_id = o.id\nINNER JOIN products p \n    ON p.id = oi.product_id\nWHERE o.status = 'COMPLETED'\nORDER BY o.ordered_at DESC\nLIMIT 50;"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "ON vs WHERE in INNER JOINs",
        "text": "In an `INNER JOIN`, placing non-join filters in the `ON` clause versus the `WHERE` clause produces identical results. However, putting join predicates in `ON` and filtering predicates in `WHERE` makes your SQL vastly more readable."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`INNER JOIN` excludes non-matching rows from both tables.",
          "Always use clear table aliases (`c`, `o`, `oi`) for readability.",
          "Chain multiple joins sequentially to traverse relational paths."
        ]
      }
    }
  ],
  "relatedLessons": [
    "visualization-joins",
    "left-right-joins"
  ]
}