{
  "id": "lesson-pg-017",
  "slug": "lateral-join",
  "moduleSlug": "joins",
  "courseSlug": "postgresql",
  "title": "LATERAL JOIN (Correlated Subqueries in FROM)",
  "description": "Execute subqueries and set-returning functions for each row of the preceding table using PostgreSQL's powerful LATERAL keyword.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "lateral-join",
    "correlated-join",
    "subqueries",
    "advanced-sql"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In standard SQL, subqueries inside the `FROM` clause cannot reference columns from preceding tables in the same `FROM` list. PostgreSQL's **`LATERAL`** keyword unlocks this capability, acting like a `foreach` loop that executes the inner subquery for every single row of the outer table."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Top-N Rows Per Category with LATERAL"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A classic SQL challenge is fetching the top 3 most recent articles for every department or category. Traditional window functions require scanning the entire table first. A `LATERAL` join performs an index seek per category, making it orders of magnitude faster on large datasets."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Top 3 Products Per Category via LATERAL JOIN",
        "content": "The inner query references c.id from the outer categories table.",
        "language": "sql",
        "code": "SELECT \n    c.id AS category_id,\n    c.name AS category_name,\n    top_products.id AS product_id,\n    top_products.title,\n    top_products.sales_count\nFROM categories c\nLEFT JOIN LATERAL (\n    SELECT p.id, p.title, p.sales_count\n    FROM products p\n    WHERE p.category_id = c.id\n    ORDER BY p.sales_count DESC\n    LIMIT 3\n) top_products ON true;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`LATERAL` allows subqueries in the `FROM` clause to reference columns from preceding tables.",
          "Solves the 'Top-N per group' problem with optimal index seek performance.",
          "Can be paired with `LEFT JOIN ... ON true` to preserve outer records with zero subquery matches."
        ]
      }
    }
  ],
  "relatedLessons": [
    "cross-join",
    "correlated-subqueries"
  ]
}