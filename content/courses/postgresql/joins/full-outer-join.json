{
  "id": "lesson-pg-015",
  "slug": "full-outer-join",
  "moduleSlug": "joins",
  "courseSlug": "postgresql",
  "title": "FULL OUTER JOIN",
  "description": "Combine datasets with FULL OUTER JOIN to retain all rows from both tables, reconcile accounting ledgers, and audit discrepancies.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "full-outer-join",
    "reconciliation",
    "audit",
    "outer-join"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A **`FULL OUTER JOIN`** returns all rows from both tables. Where rows match, columns are populated together; where either side lacks a match, `NULL` values are returned for the missing side. It is the union of a `LEFT JOIN` and a `RIGHT JOIN`."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Real-World Application: Ledger Reconciliation"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reconciling Internal Invoices with Stripe Payout Records",
        "content": "Detects invoices missing in Stripe and Stripe payments missing in internal database.",
        "language": "sql",
        "code": "SELECT \n    COALESCE(i.invoice_number, s.stripe_invoice_id) AS reference_id,\n    i.amount AS internal_amount,\n    s.amount_received AS stripe_amount,\n    CASE \n        WHEN i.id IS NULL THEN 'Missing in Internal DB'\n        WHEN s.id IS NULL THEN 'Missing in Stripe'\n        WHEN i.amount <> s.amount_received THEN 'Amount Mismatch'\n        ELSE 'Matched'\n    END AS audit_status\nFROM internal_invoices i\nFULL OUTER JOIN stripe_charges s \n    ON s.stripe_invoice_id = i.invoice_number\nWHERE i.amount <> s.amount_received \n   OR i.id IS NULL \n   OR s.id IS NULL;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`FULL OUTER JOIN` preserves every row from both tables.",
          "Use `COALESCE(a.key, b.key)` to produce a unified identifier.",
          "Ideal for auditing, data migrations, and financial reconciliations."
        ]
      }
    }
  ],
  "relatedLessons": [
    "left-right-joins",
    "cross-join"
  ]
}