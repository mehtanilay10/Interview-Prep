{
  "id": "lesson-next-js-full-stack-react-app-router-rendering-authentication-and-authorization-boundaries",
  "slug": "authentication-and-authorization-boundaries",
  "moduleSlug": "app-router-rendering",
  "courseSlug": "next-js-full-stack-react",
  "title": "Authentication and authorization boundaries",
  "description": "Place auth checks at the right layer — middleware, server components, server actions, or client components — so private data stays private.",
  "order": 8,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["auth", "authorization", "middleware", "security"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Authentication answers *'who are you?'*. Authorization answers *'what are you allowed to do?'*. In the App Router, you can enforce both at several layers: **middleware**, **server components**, **route handlers**, and **server actions**. The sooner you reject an unauthorized request, the less surface area you expose."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Middleware runs before every request hits your application code. It is the right place for redirects, cookie-based session checks, and geo or A/B rules. Server-side auth checks protect your data. Client-side checks improve UX by hiding UI, but they never protect secrets."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Layered defense"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Middleware: redirect unauthenticated users before any page renders.",
          "Server Component: fetch user-specific data and redirect if missing.",
          "Server Action / Route Handler: enforce role checks before mutations.",
          "Client Component: hide buttons or sections for UX, never for security."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "danger",
        "title": "Never rely on client-side auth",
        "text": "Any code shipped to the browser can be modified. Always validate sessions and permissions on the server. Client-side hiding is purely for user experience."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Middleware redirect",
        "content": "Protect dashboard routes before the page loads.",
        "code": "middleware.ts\n\nimport { NextResponse } from 'next/server';\nimport type { NextRequest } from 'next/server';\nimport { getToken } from 'next-auth/jwt';\n\nexport async function middleware(request: NextRequest) {\n  const token = await getToken({ req: request });\n\n  const isProtected = request.nextUrl.pathname.startsWith('/dashboard');\n\n  if (isProtected && !token) {\n    return NextResponse.redirect(new URL('/login', request.url));\n  }\n\n  return NextResponse.next();\n}\n\nexport const config = {\n  matcher: ['/dashboard/:path*'],\n};",
        "language": "tsx"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Server-side session check",
        "content": "Use getServerSession inside a Server Component.",
        "code": "app/dashboard/page.tsx\n\nimport { getServerSession } from 'next-auth';\nimport { redirect } from 'next/navigation';\n\nexport default async function Dashboard() {\n  const session = await getServerSession();\n\n  if (!session) {\n    redirect('/login');\n  }\n\n  const data = await db.dashboardData.findMany({\n    where: { userId: session.user.id },\n  });\n\n  return <DashboardView user={session.user} data={data} />;\n}",
        "language": "tsx"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Authentication",
            "definition": "Verifying who the user is, usually via a session, token, or cookie."
          },
          {
            "term": "Authorization",
            "definition": "Checking whether the authenticated user can perform a specific action."
          },
          {
            "term": "Middleware",
            "definition": "Code that runs before every request in Next.js, ideal for redirects and lightweight checks."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaway",
        "points": [
          "Middleware is the first gate for route protection.",
          "Server Components and Server Actions are the second and third gates for data protection.",
          "Client-side hiding is only a convenience layer."
        ],
        "takeaway": "Put auth checks as close to the data as possible, and remember that client-side checks are never a security boundary."
      }
    }
  ],
  "relatedLessons": ["server-actions-and-mutation-boundaries", "route-handlers-and-backend-for-frontend-patterns"],
  "furtherReading": [
    {
      "title": "Authentication",
      "url": "https://nextjs.org/docs/app/building-your-application/authentication",
      "type": "article"
    },
    {
      "title": "Middleware",
      "url": "https://nextjs.org/docs/app/building-your-application/routing/middleware",
      "type": "article"
    }
  ]
}