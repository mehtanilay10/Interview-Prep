{
  "id": "lesson-next-js-full-stack-react-app-router-rendering-route-handlers-and-backend-for-frontend-patterns",
  "slug": "route-handlers-and-backend-for-frontend-patterns",
  "moduleSlug": "app-router-rendering",
  "courseSlug": "next-js-full-stack-react",
  "title": "Route handlers and backend-for-frontend patterns",
  "description": "Build API endpoints inside the App Router using Route Handlers and the backend-for-frontend pattern.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["route-handlers", "api-routes", "bff"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In the App Router, **Route Handlers** replace API Routes from the Pages Router. They live in `app/api/` and use standard Web `Request` and `Response` objects. This makes them closer to frontend developers and easier to test in isolation."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **backend for frontend (BFF)** is an API layer shaped specifically for one client. Instead of exposing every database table to the mobile app, you build a thin route handler that knows exactly what the UI needs: the right shape, the right permissions, and the right error format."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Route handler basics"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Each file exports named functions: `GET`, `POST`, `PUT`, `DELETE`.",
          "You receive a `Request` object and return a `Response`.",
          "They run on the server by default, so you can import database clients safely."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Mental model",
        "text": "Think of Route Handlers as tiny serverless functions. They are isolated, cacheable by default, and can be deployed to Node.js or Edge runtimes depending on your configuration."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "GET and POST route handler",
        "content": "A simple posts API inside the App Router.",
        "code": "app/api/posts/route.ts\n\nimport { NextResponse } from 'next/server';\nimport { getServerSession } from 'next-auth';\n\n// GET /api/posts\nexport async function GET() {\n  const posts = await db.post.findMany({\n    select: { id: true, title: true, createdAt: true },\n  });\n\n  return NextResponse.json(posts);\n}\n\n// POST /api/posts\nexport async function POST(request: Request) {\n  const session = await getServerSession();\n  if (!session) {\n    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  const body = await request.json();\n  const post = await db.post.create({\n    data: { ...body, authorId: session.user.id },\n  });\n\n  return NextResponse.json(post, { status: 201 });\n}",
        "language": "tsx"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Edge runtime route handler",
        "content": "Run the handler at the edge for lower latency.",
        "code": "app/api/hello/route.ts\n\nexport const runtime = 'edge';\n\nexport async function GET(request: Request) {\n  const geo = request.geo;\n  return Response.json({ country: geo?.country });\n}",
        "language": "tsx"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaway",
        "points": [
          "Route Handlers are first-class API endpoints in the App Router.",
          "They use standard Web Request and Response objects.",
          "Use them as thin BFFs that shape data exactly for your UI."
        ],
        "takeaway": "Route Handlers let you keep backend logic inside your Next.js app without a separate API server."
      }
    }
  ],
  "relatedLessons": ["server-actions-and-mutation-boundaries", "edge-runtime-vs-node-runtime"],
  "furtherReading": [
    {
      "title": "Route Handlers",
      "url": "https://nextjs.org/docs/app/building-your-application/routing/route-handlers",
      "type": "article"
    },
    {
      "title": "API Routes migration guide",
      "url": "https://nextjs.org/docs/app/building-your-application/upgrading/version-13#api-routes",
      "type": "article"
    }
  ]
}