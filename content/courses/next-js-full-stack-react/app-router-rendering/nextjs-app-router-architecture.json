{
  "id": "lesson-next-js-full-stack-react-app-router-rendering-nextjs-app-router-architecture",
  "slug": "nextjs-app-router-architecture",
  "moduleSlug": "app-router-rendering",
  "courseSlug": "next-js-full-stack-react",
  "title": "Next.js App Router architecture",
  "description": "Explore how the Next.js App Router organizes your application using folders, layouts, and server-first rendering.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["app-router", "architecture", "routing"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The **App Router** is Next.js's modern way to build applications. Instead of one big file for each page, you organize your app inside folders where each folder represents a route. The `app` directory contains **layouts**, **pages**, and **templates** that wrap your content and share UI across routes."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Think of it like a set of nested boxes. The outer box is the root layout. Inside it, each folder adds another layer. Files named `page.tsx` define what users see at that URL. Files named `layout.tsx` define the shared chrome around it — navigation, sidebars, or footers. Files named `loading.tsx` show a fallback while content loads."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How routes are built"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "`app/page.tsx` renders at `/`",
          "`app/blog/[slug]/page.tsx` renders at `/blog/my-post`",
          "`app/layout.tsx` wraps every page in the same folder",
          "`app/loading.tsx` shows a fallback while a page or nested layout loads"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Mental model",
        "text": "The closest match is a file-system router. The key difference is that the App Router treats every route as a **Server Component** by default. That means the initial HTML is rendered on the server, which keeps the client bundle small."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nested route structure",
        "content": "A blog page with a shared sidebar layout.",
        "code": "// app/blog/[slug]/page.tsx\n\n// This file becomes the main content area.\n// The parent layout wraps it.\n\nexport default function BlogPost({ params }) {\n  return (\n    <article>\n      <h1>{params.slug}</h1>\n      <p>Post content goes here.</p>\n    </article>\n  );\n}",
        "language": "tsx"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Layout",
            "definition": "A UI shell that wraps one or more pages and preserves state during navigation."
          },
          {
            "term": "Page",
            "definition": "The unique UI for a route. Only one active page renders at a time per route segment."
          },
          {
            "term": "Template",
            "definition": "Like a layout, but it re-mounts on every navigation instead of preserving state."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaway",
        "points": [
          "The App Router is folder-based, not file-based like the Pages Router.",
          "Layouts share UI across routes without repeating markup.",
          "Every page is a Server Component by default."
        ],
        "takeaway": "Mastering the App Router means understanding how folders, layouts, and server-first rendering work together."
      }
    }
  ],
  "relatedLessons": ["server-components-and-client-components", "ssr-ssg-isr-and-dynamic-rendering"],
  "furtherReading": [
    {
      "title": "App Router fundamentals",
      "url": "https://nextjs.org/docs/app/building-your-application/routing",
      "type": "article"
    },
    {
      "title": "Defining routes",
      "url": "https://nextjs.org/docs/app/api-reference/file-conventions/page",
      "type": "article"
    }
  ]
}