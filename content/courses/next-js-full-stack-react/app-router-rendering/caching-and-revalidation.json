{
  "id": "lesson-next-js-full-stack-react-app-router-rendering-caching-and-revalidation",
  "slug": "caching-and-revalidation",
  "moduleSlug": "app-router-rendering",
  "courseSlug": "next-js-full-stack-react",
  "title": "Caching and revalidation",
  "description": "Understand Next.js caching layers, cache tags, and how to revalidate content when underlying data changes.",
  "order": 7,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["caching", "revalidation", "performance"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Next.js caches at multiple levels. The **Data Cache** stores the result of `fetch` calls and database queries. The **Full Route Cache** keeps rendered HTML for repeat visitors. The **Client Router Cache** preserves page state during in-app navigation. Each layer can be configured independently."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Revalidation is how you tell the cache that a piece of content is no longer fresh. You can revalidate by **time** (every 60 seconds), **tag** (when a specific event happens), or **on-demand** (after a mutation). Picking the right strategy depends on how stale the data can safely be."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Cache layers at a glance"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Layer",
          "Where it lives",
          "What it caches",
          "How to control"
        ],
        "rows": [
          [
            "Data Cache",
            "Server memory or CDN",
            "`fetch` and database query results",
            "`next.revalidate`, `next.tags`, `cache: 'no-store'`"
          ],
          [
            "Full Route Cache",
            "Server",
            "Rendered HTML and RSC payload",
            "`revalidate`, `dynamic = 'force-dynamic'`"
          ],
          [
            "Client Router Cache",
            "Browser memory",
            "Page state during navigation",
            "`router.refresh()`, `useRouter().refresh()`"
          ]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Cache tags",
        "text": "Tags let you group related cached content. When a post is updated, revalidate the `posts` tag, and every page or fetch that used that tag refreshes automatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Cache with tags and on-demand revalidation",
        "content": "Tag a fetch and revalidate after a mutation.",
        "code": "// app/posts/page.tsx\n\nexport const dynamic = 'force-static';\n\nexport default async function PostsPage() {\n  const posts = await fetch('https://api.example.com/posts', {\n    next: { tags: ['posts'] },\n  }).then((res) => res.json());\n\n  return <PostList posts={posts} />;\n}\n\n// app/posts/[id]/page.tsx\n\nexport const revalidate = 3600;\n\n// After a post is updated:\nimport { revalidateTag } from 'next/cache';\n\nrevalidateTag('posts');",
        "language": "tsx"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Force dynamic for personalized data",
        "content": "Skip the cache when data must be fresh per request.",
        "code": "// app/dashboard/page.tsx\n\nexport const dynamic = 'force-dynamic';\n\nexport default async function Dashboard() {\n  const user = await getServerSession();\n  const data = await db.dashboardData.findUnique({\n    where: { userId: user.id },\n  });\n\n  return <DashboardView data={data} />;\n}",
        "language": "tsx"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaway",
        "points": [
          "Data Cache, Full Route Cache, and Client Router Cache serve different purposes.",
          "Use time-based revalidation for content that changes predictably.",
          "Use cache tags for fine-grained invalidation after mutations."
        ],
        "takeaway": "Think of caching as a spectrum. Cache aggressively, but know how to invalidate precisely when data changes."
      }
    }
  ],
  "relatedLessons": ["ssr-ssg-isr-and-dynamic-rendering", "server-actions-and-mutation-boundaries"],
  "furtherReading": [
    {
      "title": "Caching, revalidation, and tags",
      "url": "https://nextjs.org/docs/app/building-your-application/caching",
      "type": "article"
    },
    {
      "title": "Data cache",
      "url": "https://nextjs.org/docs/app/building-your-application/caching#data-cache",
      "type": "article"
    }
  ]
}