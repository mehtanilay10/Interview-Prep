{
  "id": "lesson-next-js-full-stack-react-app-router-rendering-server-actions-and-mutation-boundaries",
  "slug": "server-actions-and-mutation-boundaries",
  "moduleSlug": "app-router-rendering",
  "courseSlug": "next-js-full-stack-react",
  "title": "Server Actions and mutation boundaries",
  "description": "Use Server Actions to handle mutations safely, validate inputs, and keep forms progressively enhanced.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["server-actions", "mutations", "forms", "validation"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Server Actions** let you run asynchronous code directly from a UI component without creating an API route. They execute on the server, can use database clients, and return typed results. Because they are normal async functions, you can call them from forms, buttons, or event handlers."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A mutation boundary is the line between a user action and the server-side state change. Crossing that boundary safely means validating inputs, checking permissions, and handling errors without exposing raw database errors."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Progressive enhancement"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Forms work even before JavaScript loads because the browser submits the form natively.",
          "Server Actions receive `FormData` automatically when used with `<form action={...}>`.",
          "If JavaScript is enabled, Next.js upgrades the form to an async submission without a full reload."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Security boundary",
        "text": "Server Actions run with full server privileges. Always validate and authorize inside the action body. Never trust data coming from the client."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Form with progressive enhancement",
        "content": "A name-update form using a Server Action.",
        "code": "app/profile/page.tsx\n\nasync function updateName(formData: FormData) {\n  'use server';\n\n  const name = String(formData.get('name') ?? '').trim();\n\n  if (name.length < 2) {\n    return { error: 'Name must be at least 2 characters' };\n  }\n\n  await db.user.update({\n    where: { id: session.user.id },\n    data: { name },\n  });\n\n  revalidatePath('/profile');\n}\n\nexport default function ProfilePage() {\n  return (\n    <form action={updateName}>\n      <label>\n        Name\n        <input name=\"name\" defaultValue={session.user.name} />\n      </label>\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}",
        "language": "tsx"
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "Safe mutation boundaries",
        "items": [
          "Validate every field server-side, even if the client validates too.",
          "Return typed result objects so the UI can show exact errors.",
          "Keep actions idempotent where possible so retries are safe.",
          "Use `revalidatePath` or `revalidateTag` to update cached UI after a mutation."
        ]
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "FAQ",
        "items": [
          {
            "question": "Should I call server actions from client components?",
            "answer": "Yes, but keep business logic and auth checks on the server. Pass only serializable data."
          },
          {
            "question": "Can server actions replace all API routes?",
            "answer": "Not always. Use API routes for streaming responses, webhooks, or non-form clients like mobile apps."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaway",
        "points": [
          "Server Actions move mutation logic to the server without extra API routes.",
          "They work out of the box with HTML forms for progressive enhancement.",
          "Always validate and authorize inside the action body."
        ],
        "takeaway": "Server Actions are the simplest path from UI to server mutation, but they still need the same security discipline as any backend endpoint."
      }
    }
  ],
  "relatedLessons": ["route-handlers-and-backend-for-frontend-patterns", "authentication-and-authorization-boundaries"],
  "furtherReading": [
    {
      "title": "Server Actions",
      "url": "https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations",
      "type": "article"
    },
    {
      "title": "Forms and mutations",
      "url": "https://nextjs.org/docs/app/building-your-application/forms",
      "type": "article"
    }
  ]
}