{
  "id": "lesson-next-js-full-stack-react-state-management-url-as-state",
  "slug": "url-as-state",
  "moduleSlug": "state-management",
  "courseSlug": "next-js-full-stack-react",
  "title": "URL as State",
  "description": "Learn to treat the URL as a first-class state container in Next.js, using query parameters and route segments to drive filtering, pagination, and navigation.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["Next.js", "URL State", "Routing"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The URL is one of the most underused state containers in web development. It is shareable, bookmarkable, survives page refreshes, and works with the browser's back and forward buttons. Treating the URL as state means encoding things like the active tab, search query, filters, and pagination directly in query parameters."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In Next.js, both the App Router and Pages Router give you hooks to read and write the URL. When your UI state lives in the URL, users can share filtered views with colleagues, return to a specific page after a refresh, and use browser history naturally."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Belongs in the URL?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Not every piece of state belongs in the URL. The URL is best for state that users should be able to bookmark, share, or navigate back to. Common candidates include: search queries, filter selections, active tabs, sort order, pagination, and modal open/closed state."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Search query: `/products?q=running+shoes`",
          "Price filter: `/products?minPrice=50&maxPrice=150`",
          "Active tab: `/docs?tab=installation`",
          "Page number: `/posts?page=3`",
          "Modal state: `/settings?modal=delete-account`"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Reading and Writing Query Parameters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In the Next.js App Router, `useSearchParams` reads the current query string, and `useRouter` with `push` or `replace` updates it. Using `replace` instead of `push` avoids polluting browser history with every keystroke."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "URL-Driven Search Filter (App Router)",
        "language": "typescript",
        "code": "'use client';\n\nimport { useSearchParams, useRouter, usePathname } from 'next/navigation';\nimport { useDebouncedCallback } from 'use-debounce';\n\nfunction ProductSearch() {\n  const searchParams = useSearchParams();\n  const router = useRouter();\n  const pathname = usePathname();\n  const currentQuery = searchParams.get('q') ?? '';\n\n  const updateQuery = useDebouncedCallback((value: string) => {\n    const params = new URLSearchParams(searchParams.toString());\n    if (value) {\n      params.set('q', value);\n    } else {\n      params.delete('q');\n    }\n    router.replace(`${pathname}?${params.toString()}`);\n  }, 300);\n\n  return (\n    <input\n      defaultValue={currentQuery}\n      onChange={(e) => updateQuery(e.target.value)}\n      placeholder=\"Search products...\"\n    />\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "URL State on the Server"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In Server Components, you can read the query string directly from the request. This means URL-driven filtering works without any client-side JavaScript at all. Combine server-side `searchParams` with React Server Components for a filtering experience that is fast, SEO-friendly, and shareable."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Server Component Reading Query Params",
        "language": "typescript",
        "code": "interface Props {\n  searchParams: Promise<{ q?: string; category?: string }>;\n}\n\nexport default async function ProductPage({ searchParams }: Props) {\n  const { q, category } = await searchParams;\n\n  const res = await fetch(\n    `/api/products?${new URLSearchParams({ q: q ?? '', category: category ?? '' })}`,\n    { cache: 'no-store' }\n  );\n  const products = await res.json();\n\n  return (\n    <section>\n      <h1>Products</h1>\n      {q && <p>Searching for: {q}</p>}\n      {category && <p>Category: {category}</p>}\n      <ul>\n        {products.map((p: Product) => (\n          <li key={p.id}>{p.name}</li>\n        ))}\n      </ul>\n    </section>\n  );\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Debounce Inputs",
        "text": "Never update the URL on every keystroke without debouncing. Each update creates a new history entry or replaces the current one. A 300ms debounce feels instant to users and dramatically reduces unnecessary renders and router work."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "items": [
          "URL state is shareable, bookmarkable, and survives navigation",
          "Use query parameters for filters, search, pagination, and active tabs",
          "useSearchParams + useRouter control URL state on the client",
          "Server Components can read query params directly without client code"
        ]
      }
    }
  ],
  "relatedLessons": ["local-state-vs-server-state", "state-synchronization", "performance-and-re-renders"],
  "furtherReading": [
    {
      "type": "article",
      "title": "Next.js Docs: useSearchParams",
      "url": "https://nextjs.org/docs/app/api-reference/functions/use-search-params"
    },
    {
      "type": "article",
      "title": "URL as State: A React Router Pattern",
      "url": "https://ui.dev/react-router-url-state"
    }
  ]
}
