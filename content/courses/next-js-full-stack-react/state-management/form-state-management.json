{
  "id": "lesson-next-js-full-stack-react-state-management-form-state-management",
  "slug": "form-state-management",
  "moduleSlug": "state-management",
  "courseSlug": "next-js-full-stack-react",
  "title": "Form State Management",
  "description": "Learn how to manage complex form state in React and Next.js using React Hook Form, validation libraries, and patterns that scale beyond simple inputs.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["Forms", "React Hook Form", "Zod"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Forms look simple, but they are deceptively complex. A production form needs to track input values, validate on change or on blur, show error messages per field, handle submission states, manage nested or array fields, and often integrate with server-side validation. Doing all of this with raw `useState` quickly becomes unmaintainable."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Modern React form libraries move state management into a performant, uncontrolled-by-default model. **React Hook Form** is the most popular choice in the React ecosystem today. It reduces re-renders, works natively with HTML inputs, and integrates cleanly with validation schemas."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why Not Just Use useState?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every keystroke in a controlled input calls the setter and triggers a re-render. For a form with twenty fields, that is twenty re-renders per character. React Hook Form uses uncontrolled inputs under the hood — the browser tracks the input value, and React only reads it when needed. This dramatically reduces re-renders without sacrificing validation or UX."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Controlled forms re-render on every keystroke",
          "Uncontrolled forms let the DOM track input values",
          "React Hook Form combines both: uncontrolled inputs with controlled validation",
          "Schema validation with Zod or Yuv keeps rules outside your component"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Hook Form with Zod"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Zod lets you define a schema that describes your data shape and rules. React Hook Form's `resolver` plugs Zod into the form so validation runs automatically. The schema is a single source of truth shared between client and server."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Contact Form with React Hook Form and Zod",
        "language": "typescript",
        "code": "import { useForm } from 'react-hook-form';\nimport { zodResolver } from '@hookform/resolvers/zod';\nimport { z } from 'zod';\n\nconst schema = z.object({\n  name: z.string().min(2, 'Name must be at least 2 characters'),\n  email: z.string().email('Enter a valid email address'),\n  message: z.string().min(10, 'Message must be at least 10 characters'),\n});\n\ntype FormData = z.infer<typeof schema>;\n\nfunction ContactForm() {\n  const {\n    register,\n    handleSubmit,\n    formState: { errors, isSubmitting },\n  } = useForm<FormData>({\n    resolver: zodResolver(schema),\n  });\n\n  const onSubmit = async (data: FormData) => {\n    const res = await fetch('/api/contact', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify(data),\n    });\n    if (!res.ok) throw new Error('Failed to send');\n  };\n\n  return (\n    <form onSubmit={handleSubmit(onSubmit)}>\n      <input {...register('name')} placeholder=\"Your name\" />\n      {errors.name && <p role=\"alert\">{errors.name.message}</p>}\n\n      <input {...register('email')} placeholder=\"your@email.com\" />\n      {errors.email && <p role=\"alert\">{errors.email.message}</p>}\n\n      <textarea {...register('message')} placeholder=\"How can we help?\" />\n      {errors.message && <p role=\"alert\">{errors.message.message}</p>}\n\n      <button type=\"submit\" disabled={isSubmitting}>\n        {isSubmitting ? 'Sending...' : 'Send Message'}\n      </button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Handling Arrays and Nested Fields"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Forms with dynamic fields — like adding multiple phone numbers or list items — need array field support. React Hook Form's `useFieldArray` hook lets you append, remove, and map over array fields without losing performance or validation."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Accessibility",
        "text": "Always link errors to their inputs using `aria-describedby` or by rendering the error inside the label. Screen readers need to announce validation messages in context. React Hook Form's `errors` object gives you the field name and message; use them together."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "items": [
          "React Hook Form uses uncontrolled inputs to minimize re-renders",
          "Zod schemas define validation rules in one place shared across client and server",
          "useFieldArray handles dynamic form fields cleanly",
          "Forms are state — but they are a specialized kind of state that deserves its own tool"
        ]
      }
    }
  ],
  "relatedLessons": ["local-state-vs-server-state", "react-query-for-server-state", "performance-and-re-renders"],
  "furtherReading": [
    {
      "type": "article",
      "title": "React Hook Form Docs",
      "url": "https://react-hook-form.com/"
    },
    {
      "type": "article",
      "title": "Zod Documentation",
      "url": "https://zod.dev/"
    }
  ]
}
