{
  "id": "lesson-next-js-full-stack-react-state-management-react-query-for-server-state",
  "slug": "react-query-for-server-state",
  "moduleSlug": "state-management",
  "courseSlug": "next-js-full-stack-react",
  "title": "React Query for Server State",
  "description": "Master TanStack Query (React Query) for managing server state, including caching, background refetching, mutations, and devtools integration.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["React Query", "Server State", "Data Fetching"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Server state is different from local state in ways that matter. It lives somewhere else. It can change without your app knowing about it. It needs to be cached, deduplicated, and kept in sync with the outside world. **TanStack Query** (formerly React Query) was built specifically for these problems."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Instead of storing API responses in `useState` and writing complex `useEffect` hooks to fetch and refetch data, React Query gives you a cache with sensible defaults: data is cached by query key, stale data is refetched in the background, and duplicate requests are automatically deduplicated."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Core Concepts"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Query keys uniquely identify each piece of server data in the cache",
          "Queries are functions that return a promise — React Query caches the result",
          "Stale time determines how long data is considered fresh before refetching",
          "Mutations handle creating, updating, and deleting data, then invalidate related queries"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Fetching Data with useQuery"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `useQuery` hook takes a unique query key and a function that fetches data. React Query handles the loading, error, and success states for you. It also caches the result so subsequent components using the same key do not trigger duplicate network requests."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Fetching Posts with useQuery",
        "language": "typescript",
        "code": "import { useQuery } from '@tanstack/react-query';\n\ninterface Post {\n  id: string;\n  title: string;\n  content: string;\n}\n\nfunction PostList() {\n  const { data, isLoading, error, refetch } = useQuery<Post[]>({\n    queryKey: ['posts'],\n    queryFn: async () => {\n      const res = await fetch('/api/posts');\n      if (!res.ok) throw new Error('Failed to load posts');\n      return res.json();\n    },\n    staleTime: 1000 * 60 * 5,\n  });\n\n  if (isLoading) return <p>Loading posts...</p>;\n  if (error) return <p>Error loading posts.</p>;\n\n  return (\n    <div>\n      <button onClick={() => refetch()}>Refresh</button>\n      <ul>\n        {data?.map((post) => (\n          <li key={post.id}>{post.title}</li>\n        ))}\n      </ul>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Mutating Data with useMutation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Mutations are actions that change data on the server: creating a new post, updating a comment, deleting an item. After a mutation succeeds, you typically want to update the cache so the UI reflects the change immediately. React Query makes this easy with `invalidateQueries`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Post with useMutation",
        "language": "typescript",
        "code": "function CreatePost() {\n  const queryClient = useQueryClient();\n\n  const mutation = useMutation({\n    mutationFn: async (title: string) => {\n      const res = await fetch('/api/posts', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ title }),\n      });\n      if (!res.ok) throw new Error('Failed to create post');\n      return res.json();\n    },\n    onSuccess: () => {\n      queryClient.invalidateQueries({ queryKey: ['posts'] });\n    },\n  });\n\n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    const formData = new FormData(e.target as HTMLFormElement);\n    mutation.mutate(formData.get('title') as string);\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input name=\"title\" placeholder=\"Post title\" />\n      <button type=\"submit\" disabled={mutation.isPending}>\n        {mutation.isPending ? 'Creating...' : 'Create Post'}\n      </button>\n      {mutation.isError && <p>Error: {mutation.error.message}</p>}\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Query Keys and Cache Structure"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Query keys are arrays that form a hierarchy. `['posts']` matches all posts. `['posts', postId]` matches a single post. `['posts', { filter: 'draft' }]` matches filtered posts. This hierarchy means you can invalidate broadly or precisely. Invalidating `['posts']` refetches all post-related queries; invalidating `['posts', '123']` refetches only that one post."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "DevTools",
        "text": "Install the React Query DevTools browser extension or add them in development mode. They show you every query in the cache, its status, when it last fetched, and what it is waiting on. This visibility alone will change how you debug data-fetching issues."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "items": [
          "React Query manages server state with caching, deduplication, and background refetching",
          "useQuery handles fetching; useMutation handles creating, updating, and deleting",
          "Query keys form a hierarchy that controls cache invalidation",
          "Prefer React Query over useState for any data that comes from an API"
        ]
      }
    }
  ],
  "relatedLessons": ["local-state-vs-server-state", "state-synchronization", "form-state-management"],
  "furtherReading": [
    {
      "type": "article",
      "title": "TanStack Query Docs: Overview",
      "url": "https://tanstack.com/query/latest/docs/framework/react/overview"
    },
    {
      "type": "article",
      "title": "Server State vs Client State",
      "url": "https://tkdodo.eu/blog/practical-react-query#server-state-vs-client-state"
    }
  ]
}
