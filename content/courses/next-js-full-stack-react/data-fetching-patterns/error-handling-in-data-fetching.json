{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-error-handling-in-data-fetching",
  "slug": "error-handling-in-data-fetching",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Error Handling in Data Fetching",
  "description": "Learn how to handle fetch failures in Next.js using error.tsx, not-found handling, and try/catch patterns in Server Components.",
  "order": 7,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["Next.js", "Error Handling", "error.tsx", "Server Components"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Data fetching inevitably fails: APIs go down, databases disconnect, and users request records that do not exist. Next.js provides structured error-handling patterns that let you recover gracefully without crashing the entire application. Understanding these patterns ensures users always see a helpful message instead of a blank page."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Error handling in Next.js operates at multiple levels: route-level error boundaries with error.tsx, not-found handling for missing data, and manual try/catch inside Server Components for controlled error recovery."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Error Boundaries with error.tsx"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An error.tsx file creates a route-level error boundary. If a Server Component or any of its children throw an error, Next.js renders the error boundary instead of crashing the entire layout. The boundary catches errors in that segment and all nested child segments."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Route-level error boundary with reset",
        "content": "An error.tsx file with a reset function that re-renders the route segment.",
        "language": "tsx",
        "code": "'use client'\n\nimport { useEffect } from 'react'\n\nexport default function Error({\n  error,\n  reset\n}: {\n  error: Error & { digest?: string }\n  reset: () => void\n}) {\n  useEffect(() => {\n    console.error('Page error:', error)\n  }, [error])\n\n  return (\n    <div className=\"rounded-lg border border-red-200 bg-red-50 p-6\">\n      <h2 className=\"text-lg font-semibold text-red-800\">Something went wrong</h2>\n      <p className=\"mt-2 text-red-600\">We could not load this page. Please try again.</p>\n      <button\n        onClick={reset}\n        className=\"mt-4 rounded bg-red-700 px-4 py-2 text-white\"\n      >\n        Try again\n      </button>\n    </div>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Handling Not-Found Data"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a fetch returns a 404 or a database query returns no results, you can use the not-found() function to trigger the not-found UI. Next.js looks for a not-found.tsx file in the route segment and renders it. This is the preferred way to handle missing resources."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Triggering not-found for missing data",
        "content": "Calling not-found() when a database query returns no result.",
        "language": "tsx",
        "code": "import { notFound } from 'next/navigation'\n\nasync function Post({ params }: { params: { slug: string } }) {\n  const post = await fetchPost(params.slug)\n\n  if (!post) {\n    notFound()\n  }\n\n  return (\n    <article>\n      <h1>{post.title}</h1>\n      <div dangerouslySetInnerHTML={{ __html: post.content }} />\n    </article>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Try/Catch in Server Components"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For controlled error recovery, wrap data fetches in try/catch inside Server Components. This lets you show a fallback UI for a single section without triggering the route-level error boundary. It is useful when only one part of the page is optional or prone to failure."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Graceful partial failure with try/catch",
        "content": "Using try/catch in a Server Component to gracefully handle a failed fetch without crashing the page.",
        "language": "tsx",
        "code": "async function ProductPage({ id }: { id: string }) {\n  const product = await fetchProduct(id)\n\n  let recommendations\n  try {\n    recommendations = await fetchRecommendations(id)\n  } catch {\n    recommendations = [] // graceful fallback\n  }\n\n  return (\n    <div>\n      <h1>{product.name}</h1>\n      <p>{product.description}</p>\n\n      {recommendations.length > 0 ? (\n        <Recommendations items={recommendations} />\n      ) : (\n        <p className=\"text-gray-500\">No recommendations available right now.</p>\n      )}\n    </div>\n  )\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Watch Out",
        "text": "error.tsx must be a Client Component because it uses React state for the reset function. Do not use 'use server' in error.tsx or the error boundary will not work correctly."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Next.js provides layered error handling: error.tsx boundaries for route-level failures.",
          "not-found.tsx handles missing resources with a dedicated UI.",
          "try/catch in Server Components enables graceful partial failures.",
          "Use each tool at the appropriate level to keep your application resilient."
        ]
      }
    }
  ],
  "relatedLessons": ["streaming-and-suspense", "loading-states", "server-components-data-fetching"],
  "furtherReading": [
    { "title": "Next.js Error Handling Documentation", "url": "https://nextjs.org/docs/app/building-your-application/routing/error-handling" },
    { "title": "Next.js Not-Found Handling", "url": "https://nextjs.org/docs/app/api-reference/functions/not-found" }
  ]
}
