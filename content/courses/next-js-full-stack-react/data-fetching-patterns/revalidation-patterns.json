{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-revalidation-patterns",
  "slug": "revalidation-patterns",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Revalidation Patterns",
  "description": "Learn how to invalidate cached data using time-based revalidation, on-demand revalidation with revalidatePath, and tag-based revalidation with revalidateTag.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["Next.js", "Revalidation", "Cache Invalidation", "ISR", "Tags"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Caching improves performance, but cached data eventually becomes stale. Revalidation is the process of refreshing cached data so users see the latest information without sacrificing the speed benefits of caching. Next.js supports time-based revalidation and on-demand revalidation so you can control exactly when data refreshes."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Choosing the right revalidation strategy depends on how often your data changes and how fresh it needs to be. Some data can tolerate minutes or hours of staleness, while other data must be updated immediately after a change."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Time-Based Revalidation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Time-based revalidation refreshes cached data after a set number of seconds. Use the revalidate option in fetch to specify how long Next.js should keep data before checking for updates. This is the simplest form of revalidation and works well for content that updates on a predictable schedule."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Revalidating fetch data every 60 seconds",
        "content": "Revalidating fetch data every 60 seconds with the revalidate option.",
        "language": "tsx",
        "code": "async function ProductsPage() {\n  // Revalidate every 60 seconds\n  const products = await fetch('https://api.example.com/products', {\n    next: { revalidate: 60 }\n  })\n\n  const data = await products.json()\n  return <ProductGrid items={data} />\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "On-Demand Revalidation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "On-demand revalidation lets you invalidate cached data immediately after a mutation. Instead of waiting for a timer, you trigger a revalidation from a Route Handler when data changes. This ensures users see the latest data right after a create, update, or delete operation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Revalidating routes on demand after a mutation",
        "content": "Revalidating routes on demand after a POST mutation in a Route Handler.",
        "language": "tsx",
        "code": "// app/api/posts/route.ts\nimport { revalidatePath } from 'next/cache'\nimport { NextResponse } from 'next/server'\n\nexport async function POST(request: Request) {\n  const body = await request.json()\n  await savePost(body)\n\n  // Invalidate the posts page and the specific post page\n  revalidatePath('/posts')\n  revalidatePath('/posts/[slug]')\n\n  return NextResponse.json({ success: true })\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Tag-Based Revalidation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Tags let you group related fetches and invalidate them together. Tag-based revalidation is more flexible than path-based invalidation because it can target fetches across multiple routes. Assign tags to fetch calls, then use revalidateTag to refresh all cached data with that tag."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Tagging fetch calls and invalidating by tag",
        "content": "Tagging fetch calls and invalidating them by tag after mutations.",
        "language": "tsx",
        "code": "// In your data-fetching Server Component\nconst posts = await fetch('https://api.example.com/posts', {\n  next: { tags: ['posts'], revalidate: 3600 }\n})\n\nconst authors = await fetch('https://api.example.com/authors', {\n  next: { tags: ['authors'], revalidate: 3600 }\n})\n\n// In your Route Handler after a mutation\nimport { revalidateTag } from 'next/cache'\n\nexport async function POST() {\n  await createPost()\n  revalidateTag('posts') // refreshes all 'posts' tagged fetches\n  revalidateTag('authors') // refreshes all 'authors' tagged fetches\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Comparing Strategies"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Strategy", "Best For", "Freshness", "Complexity"],
        "rows": [
          ["Time-based (revalidate)", "Blog posts, product catalogs", "Minutes to hours", "Low"],
          ["On-demand (revalidatePath)", "User-specific dashboards", "Immediate after change", "Medium"],
          ["Tag-based (revalidateTag)", "Shared data across many routes", "Immediate after change", "Medium"]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro Tip",
        "text": "Combine revalidation strategies for different data types. Use long time-based revalidation for stable content like blog posts and on-demand revalidation for user-generated content that changes frequently."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Revalidation keeps cached data fresh without losing the speed of caching.",
          "Time-based revalidation refreshes data on a fixed schedule.",
          "On-demand revalidation invalidates data immediately after mutations.",
          "Tag-based revalidation groups related fetches for targeted invalidation.",
          "Combining multiple strategies lets you optimize each data source independently."
        ]
      }
    }
  ],
  "relatedLessons": ["caching-strategies", "error-handling-in-data-fetching", "streaming-and-suspense"],
  "furtherReading": [
    { "title": "Next.js Revalidation Documentation", "url": "https://nextjs.org/docs/app/building-your-application/data-fetching/revalidating" },
    { "title": "Next.js Caching Documentation", "url": "https://nextjs.org/docs/app/building-your-application/caching" }
  ]
}
