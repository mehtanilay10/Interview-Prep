{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-server-components-data-fetching",
  "slug": "server-components-data-fetching",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Server Components Data Fetching",
  "description": "Learn how to fetch data directly in Server Components using async/await, access databases securely, and leverage Next.js automatic caching for fast page loads.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": ["Next.js", "Server Components", "Data Fetching", "Async Components"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Server Components changed how we think about data fetching in Next.js. Instead of fetching data inside client-side effects, you can fetch directly in your component body using standard async/await syntax. The data never leaves the server, so sensitive information like database credentials and API keys stay protected."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In this lesson, you'll learn how Server Components handle data fetching, when to use them, and how Next.js automatically caches the results to make your application faster."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Are Server Components?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Server Components are React components that render only on the server. Their code is never sent to the browser. This means you can read files directly, query databases, and call external APIs without exposing secrets to the client."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Run exclusively on the server — no JavaScript bundle is sent to the browser for these components",
          "Can directly access backend resources like databases and file systems",
          "Keep secrets like API keys and database credentials safe on the server",
          "Reduce the amount of JavaScript the browser needs to download and parse"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Async Server Components"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Next.js extends React to support async Server Components. You simply mark your component as async, then await your data-fetching calls directly in the component body. There is no need for useEffect, useState, or data-fetching libraries."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Async Server Component with fetch",
        "content": "An async Server Component fetching data with fetch and the Next.js extended fetch API.",
        "language": "tsx",
        "code": "async function BlogPost({ params }: { params: { slug: string } }) {\n  const post = await fetch('https://api.example.com/posts/' + params.slug, {\n    next: { revalidate: 3600 }\n  })\n\n  if (!post.ok) {\n    throw new Error('Failed to fetch post')\n  }\n\n  const data = await post.json()\n\n  return (\n    <article>\n      <h1>{data.title}</h1>\n      <p>{data.body}</p>\n    </article>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Direct Database Access"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Because Server Components run on the server, you can import your database client directly and query it without creating an API route. This eliminates an extra network hop and simplifies your architecture."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Querying Prisma in a Server Component",
        "content": "Querying Prisma directly inside an async Server Component.",
        "language": "tsx",
        "code": "import { prisma } from '@/lib/prisma'\n\nasync function UsersPage() {\n  const users = await prisma.user.findMany({\n    orderBy: { createdAt: 'desc' },\n    take: 10\n  })\n\n  return (\n    <ul>\n      {users.map((user) => (\n        <li key={user.id}>{user.name}</li>\n      ))}\n    </ul>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Request Memoization"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Next.js automatically memoizes fetch requests within a single render. If you call the same URL with the same options multiple times in the same component tree, Next.js only makes one network request. This works across nested components without any extra configuration."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro Tip",
        "text": "When you need to skip memoization for a specific fetch, set the signal option to AbortSignal.timeout(0). This forces Next.js to treat the request as unique even if the URL and options are otherwise identical."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Server Components"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Fetching data that does not require interactivity",
          "Reading from a database or filesystem",
          "Keeping secrets like API keys off the client",
          "Sending less JavaScript to the browser for better performance"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Server Components let you fetch data directly in your component using async/await.",
          "They run exclusively on the server and can access databases and secrets safely.",
          "Next.js automatically memoizes fetch requests and caches responses for fast repeat visits.",
          "Use Server Components for data that does not require client-side interactivity."
        ]
      }
    }
  ],
  "relatedLessons": ["client-side-data-fetching", "parallel-data-loading", "caching-strategies"],
  "furtherReading": [
    { "title": "Next.js Server Components Documentation", "url": "https://nextjs.org/docs/app/building-your-application/rendering/server-components" },
    { "title": "React Server Components RFC", "url": "https://github.com/reactjs/rfcs/blob/main/text/0188-server-components.md" }
  ]
}
