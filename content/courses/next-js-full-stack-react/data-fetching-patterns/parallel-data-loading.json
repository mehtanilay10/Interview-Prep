{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-parallel-data-loading",
  "slug": "parallel-data-loading",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Parallel Data Loading",
  "description": "Speed up your Next.js pages by loading independent data in parallel using Promise.all and parallel route segments.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["Next.js", "Performance", "Promise.all", "Parallel Fetching"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When a page needs multiple pieces of data, the order in which you load them matters. If you fetch them one after another, the total load time is the sum of every request. Parallel loading lets you start all requests at the same time and wait for them together, dramatically reducing wait time."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Next.js supports parallel loading at the framework level through route segments and Promise.all in Server Components. Understanding these patterns is essential for building fast, responsive full-stack applications."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Sequential Problem"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, if you await multiple promises one after another in an async Server Component, each request waits for the previous one to finish. If your page needs a user profile, recent orders, and notifications, sequential fetching means the slowest request blocks everything else."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Sequential fetching — slow",
        "content": "Fetching data sequentially — each request waits for the previous one to complete.",
        "language": "tsx",
        "code": "// SLOW: sequential fetching\nasync function Dashboard() {\n  const user = await fetchUser()      // 200ms\n  const orders = await fetchOrders()  // 300ms\n  const notifications = await fetchNotifications() // 150ms\n  // Total: ~650ms\n\n  return (\n    <div>\n      <Profile user={user} />\n      <OrderList orders={orders} />\n      <NotificationBell count={notifications.length} />\n    </div>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Promise.all for Parallel Loading"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Wrap independent promises in Promise.all to start them simultaneously. The component waits for all of them to resolve before rendering, but they all run at the same time. In the example above, the total time becomes the slowest single request, not the sum of all three."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Parallel fetching with Promise.all — fast",
        "content": "Fetching data in parallel with Promise.all — all requests start at the same time.",
        "language": "tsx",
        "code": "// FAST: parallel fetching\nasync function Dashboard() {\n  const [user, orders, notifications] = await Promise.all([\n    fetchUser(),\n    fetchOrders(),\n    fetchNotifications()\n  ])\n  // Total: ~300ms (the slowest request)\n\n  return (\n    <div>\n      <Profile user={user} />\n      <OrderList orders={orders} />\n      <NotificationBell count={notifications.length} />\n    </div>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Parallel Route Segments"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Next.js also supports parallel route segments using the @folder convention. Two or more slots in the same layout load independently and render simultaneously. This is especially useful for dashboards with multiple independent panels."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Parallel route segment file structure",
        "content": "Parallel route segments in Next.js file-system routing.",
        "language": "text",
        "code": "app/dashboard/\n  @team/\n  page.tsx      # renders the team slot\n  @analytics/\n  page.tsx      # renders the analytics slot\n  layout.tsx    # receives both props and renders them side by side"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Layout receiving parallel slots",
        "content": "A layout receiving two parallel slots and rendering them side by side.",
        "language": "tsx",
        "code": "// app/dashboard/layout.tsx\nexport default function DashboardLayout({\n  children,\n  team,\n  analytics\n}: {\n  children: React.ReactNode\n  team: React.ReactNode\n  analytics: React.ReactNode\n}) {\n  return (\n    <div className=\"grid grid-cols-2 gap-4\">\n      <section>{team}</section>\n      <section>{analytics}</section>\n      <div className=\"col-span-2\">{children}</div>\n    </div>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Interleaving with Loading States"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "One of the biggest benefits of parallel routes is that each slot can show its own loading state. Users see panels as they finish rather than waiting for the slowest panel. Combine parallel routes with loading.tsx files for instant, progressive dashboards."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro Tip",
        "text": "Only use Promise.all for independent data. If one request depends on the result of another, sequential fetching is correct. Measure performance before and after to confirm the optimization helps real users."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Parallel data loading reduces total wait time by fetching independent data simultaneously.",
          "Use Promise.all for parallel fetches in Server Components.",
          "Parallel route segments let you build dashboard layouts with independent panels.",
          "Each panel can stream in with its own loading state for a progressive experience."
        ]
      }
    }
  ],
  "relatedLessons": ["server-components-data-fetching", "streaming-and-suspense", "loading-states"],
  "furtherReading": [
    { "title": "Next.js Parallel Routes Documentation", "url": "https://nextjs.org/docs/app/building-your-application/routing/parallel-routes" },
    { "title": "Next.js Data Fetching Documentation", "url": "https://nextjs.org/docs/app/building-your-application/data-fetching" }
  ]
}
