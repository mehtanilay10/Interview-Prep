{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-offline-support",
  "slug": "offline-support",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Offline Support",
  "description": "Build resilient Next.js applications with offline support using service workers, background sync, offline queues, and progressive enhancement.",
  "order": 10,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": ["Next.js", "PWA", "Offline", "Service Worker", "Progressive Enhancement"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Users expect applications to work even when their internet connection is unstable or completely offline. Offline support means your application continues to function, queues user actions for later, and syncs data when connectivity returns. Building offline-capable applications makes your software accessible and resilient."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Next.js supports offline features through Progressive Web App (PWA) capabilities, service workers, and client-side queueing. The key is designing your application so the interface works without network access and mutations are preserved until they can reach the server."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Detecting Connectivity"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The browser provides navigator.onLine as a quick way to check connectivity status. For more reliable detection, listen for online and offline events. These events fire when the browser gains or loses network access, letting you update the UI to reflect the current state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Offline indicator component",
        "content": "An offline indicator component that listens for browser connectivity events.",
        "language": "tsx",
        "code": "'use client'\n\nimport { useState, useEffect } from 'react'\n\nexport function OfflineIndicator() {\n  const [isOnline, setIsOnline] = useState(true)\n\n  useEffect(() => {\n    function updateOnlineStatus() {\n      setIsOnline(navigator.onLine)\n    }\n\n    window.addEventListener('online', updateOnlineStatus)\n    window.addEventListener('offline', updateOnlineStatus)\n\n    return () => {\n      window.removeEventListener('online', updateOnlineStatus)\n      window.removeEventListener('offline', updateOnlineStatus)\n    }\n  }, [])\n\n  if (isOnline) return null\n\n  return (\n    <div className=\"fixed bottom-4 left-4 rounded-lg bg-yellow-100 px-4 py-2 text-yellow-800\">\n      You are offline. Changes will sync when you reconnect.\n    </div>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Offline Action Queue"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a user performs an action while offline, store the request in a queue rather than discarding it. When the browser detects it is back online, replay the queued requests. This pattern lets users continue working without interruption and ensures no mutations are lost."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Offline queue hook",
        "content": "A custom hook that queues offline actions and flushes them when connectivity returns.",
        "language": "tsx",
        "code": "'use client'\n\nimport { useState, useEffect } from 'react'\n\ninterface QueuedAction {\n  id: string\n  url: string\n  method: string\n  body: unknown\n}\n\nfunction useOfflineQueue() {\n  const [queue, setQueue] = useState<QueuedAction[]>([])\n\n  useEffect(() => {\n    async function flushQueue() {\n      if (!navigator.onLine) return\n\n      const pending = queue.filter((action) => !action.sent)\n      for (const action of pending) {\n        try {\n          await fetch(action.url, {\n            method: action.method,\n            headers: { 'Content-Type': 'application/json' },\n            body: JSON.stringify(action.body)\n          })\n          setQueue((prev) =>\n            prev.map((a) =>\n              a.id === action.id ? { ...a, sent: true } : a\n            )\n          )\n        } catch {\n          break // stop flushing if network fails again\n        }\n      }\n    }\n\n    window.addEventListener('online', flushQueue)\n    return () => window.removeEventListener('online', flushQueue)\n  }, [queue])\n\n  function enqueue(action: QueuedAction) {\n    setQueue((prev) => [...prev, action])\n    localStorage.setItem('offlineQueue', JSON.stringify([...queue, action]))\n  }\n\n  return { queue, enqueue }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Service Workers and Next.js PWA"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Service workers are scripts that run in the background and intercept network requests. They can serve cached content when the user is offline, prefetch assets for faster repeat visits, and push notifications. Next.js supports PWA features through the @ducanh2912/next-pwa plugin, which simplifies service worker setup."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Service workers cache static assets like JavaScript, CSS, and images",
          "Cached assets are served even when the user is offline",
          "Background sync lets you retry failed requests when connectivity returns",
          "Push notifications keep users engaged even when the app is closed"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Progressive Enhancement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Progressive enhancement means building your core experience so it works without JavaScript or network access, then adding enhancements on top. In Next.js, Server Components provide the core content, while Client Components add interactivity. This architecture naturally supports offline use because the initial page renders from the server cache or static output."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro Tip",
        "text": "Persist queued actions to IndexedDB instead of localStorage for larger queues and better performance. LocalStorage is synchronous and limited to 5MB, while IndexedDB handles more data without blocking the main thread."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Offline support requires detecting connectivity, queuing user actions, and replaying them when the network returns.",
          "Service workers provide low-level caching and background sync.",
          "Application-level queues handle mutations while offline.",
          "Progressive enhancement ensures your app remains usable regardless of network conditions.",
          "Use IndexedDB for larger offline queues to avoid blocking the main thread."
        ]
      }
    }
  ],
  "relatedLessons": ["error-handling-in-data-fetching", "loading-states", "client-side-data-fetching"],
  "furtherReading": [
    { "title": "Next.js PWA Documentation", "url": "https://nextjs.org/docs/app/guides/progressive-web-apps" },
    { "title": "MDN Service Worker Guide", "url": "https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API" }
  ]
}
