{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-optimistic-updates",
  "slug": "optimistic-updates",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Optimistic Updates",
  "description": "Make your Next.js app feel instant with optimistic updates using React's useOptimistic hook, rollback patterns, and practical examples for form submissions.",
  "order": 9,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["Next.js", "Optimistic UI", "useOptimistic", "React", "UX"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Optimistic updates make applications feel instant by updating the UI before the server confirms the change. Instead of waiting for a slow network request, the interface assumes the action will succeed, shows the new state immediately, and silently corrects itself if the request fails."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React 19 introduced the useOptimistic hook, which manages this pattern cleanly. Next.js applications can use it in Client Components to provide snappy feedback for likes, toasts, form submissions, and other actions."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How Optimistic Updates Work"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An optimistic update follows three steps: first, the user triggers an action. Second, the UI immediately updates to reflect the expected result. Third, the background request completes and the UI either confirms the optimistic state or rolls back to the original state if the request fails."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "User clicks a button or submits a form",
          "The UI updates instantly to show the expected result",
          "The background request completes in the background",
          "On failure, the UI rolls back and shows an error message"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using useOptimistic"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The useOptimistic hook takes a current state and an update function. When you call the update function, the hook returns a new optimistic state immediately. When the async action settles, the hook automatically returns to the real state."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Adding a todo with useOptimistic",
        "content": "Using useOptimistic to add a todo instantly while the server request runs in the background.",
        "language": "tsx",
        "code": "'use client'\n\nimport { useOptimistic } from 'react'\n\ninterface Todo {\n  id: number\n  title: string\n  completed: boolean\n}\n\nfunction TodoList({ todos, toggleTodo }: {\n  todos: Todo[]\n  toggleTodo: (id: number) => Promise<void>\n}) {\n  const [optimisticTodos, addOptimisticTodo] = useOptimistic(\n    todos,\n    (state, newTodo: Todo) => [...state, newTodo]\n  )\n\n  async function handleAdd(title: string) {\n    const tempTodo: Todo = {\n      id: Date.now(),\n      title,\n      completed: false\n    }\n\n    addOptimisticTodo(tempTodo)\n\n    try {\n      await createTodo(title)\n    } catch {\n      // The hook automatically rolls back on error\n    }\n  }\n\n  return (\n    <ul>\n      {optimisticTodos.map((todo) => (\n        <li key={todo.id}>{todo.title}</li>\n      ))}\n    </ul>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Handling Rollbacks"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a server request fails, useOptimistic automatically reverts to the real state. You should also display a toast notification so the user understands what happened. Showing the error close to the affected UI helps users correct the problem without losing context."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Rolling back a failed optimistic update",
        "content": "Handling a failed optimistic update with a toast notification and automatic rollback.",
        "language": "tsx",
        "code": "async function handleLike(postId: number) {\n  addOptimisticLike(postId)\n\n  try {\n    await likePost(postId)\n  } catch (error) {\n    toast.error('Could not save your like. Please try again.')\n    // useOptimistic automatically reverts the UI\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use Optimistic Updates"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Liking or favoriting content where the action is usually safe",
          "Adding items to a list or cart where failure is rare",
          "Toggling settings that rarely fail validation",
          "Any action where the user expects immediate visual feedback"
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Watch Out",
        "text": "Do not use optimistic updates for critical operations like payments or account deletion. If these actions fail, the user must see a clear confirmation before the UI changes. Optimistic updates work best for low-risk, high-frequency interactions."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Optimistic updates make applications feel fast by updating the UI before the server confirms.",
          "Use React's useOptimistic hook to manage the pattern cleanly.",
          "Always handle failures with clear error messages and automatic rollback.",
          "Reserve optimistic updates for low-risk interactions where instant feedback matters most."
        ]
      }
    }
  ],
  "relatedLessons": ["client-side-data-fetching", "error-handling-in-data-fetching", "offline-support"],
  "furtherReading": [
    { "title": "React useOptimistic Documentation", "url": "https://react.dev/reference/react/useOptimistic" },
    { "title": "Next.js Mutations and Server Actions", "url": "https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations" }
  ]
}
