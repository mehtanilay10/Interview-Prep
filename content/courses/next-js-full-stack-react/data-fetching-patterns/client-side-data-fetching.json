{
  "id": "lesson-next-js-full-stack-react-data-fetching-patterns-client-side-data-fetching",
  "slug": "client-side-data-fetching",
  "moduleSlug": "data-fetching-patterns",
  "courseSlug": "next-js-full-stack-react",
  "title": "Client-Side Data Fetching",
  "description": "Learn when and how to fetch data on the client using useEffect, the React use() hook, and modern patterns for interactive Next.js applications.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["Next.js", "Client Components", "useEffect", "Interactive Data"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Server Components handle most data fetching in Next.js, but some scenarios require fetching data on the client. When a component needs to respond to user input, browser events, or real-time updates, you need a Client Component that runs in the browser."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Client-side data fetching means requesting data after the page has already loaded. The browser downloads the page first, then the JavaScript runs and fetches additional data as needed."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Fetch on the Client"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "The data depends on user input, like search queries or form fields",
          "You need real-time updates from WebSockets or polling",
          "The component uses browser-only APIs like localStorage or geolocation",
          "The data updates frequently and does not need server-side caching"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Fetching with useEffect"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The most common pattern is using the useEffect hook with useState to manage loading and error states. You start with an empty state, trigger the fetch in useEffect, and update the state when the response arrives."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Fetching with useState and useEffect",
        "content": "Fetching data on the client with useState, useEffect, and an abort-like cleanup pattern.",
        "language": "tsx",
        "code": "'use client'\n\nimport { useState, useEffect } from 'react'\n\ninterface Todo {\n  id: number\n  title: string\n  completed: boolean\n}\n\nexport function TodoList() {\n  const [todos, setTodos] = useState<Todo[]>([])\n  const [isLoading, setIsLoading] = useState(true)\n  const [error, setError] = useState<string | null>(null)\n\n  useEffect(() => {\n    let ignore = false\n\n    async function loadTodos() {\n      try {\n        const response = await fetch('/api/todos')\n        if (!response.ok) throw new Error('Failed to load todos')\n        const data = await response.json()\n        if (!ignore) setTodos(data)\n      } catch (err) {\n        if (!ignore) setError('Something went wrong')\n      } finally {\n        if (!ignore) setIsLoading(false)\n      }\n    }\n\n    loadTodos()\n\n    return () => {\n      ignore = true\n    }\n  }, [])\n\n  if (isLoading) return <p>Loading...</p>\n  if (error) return <p className=\"text-red-500\">{error}</p>\n\n  return (\n    <ul>\n      {todos.map((todo) => (\n        <li key={todo.id}>{todo.title}</li>\n      ))}\n    </ul>\n  )\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The React use() Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React also provides a use() hook that lets you read promises and context directly in render. Next.js extends this so you can use it in Server Components alongside Suspense boundaries for streaming. On the client, use() unwraps a promise and triggers the closest Suspense boundary while waiting."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using the use() hook in a Client Component",
        "content": "Using React's use() hook to unwrap a promise inside a Client Component.",
        "language": "tsx",
        "code": "'use client'\n\nimport { use } from 'react'\n\nfunction Comments({ commentsPromise }: { commentsPromise: Promise<any> }) {\n  const comments = use(commentsPromise)\n\n  return (\n    <ul>\n      {comments.map((c: any) => (\n        <li key={c.id}>{c.text}</li>\n      ))}\n    </ul>\n  )\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Watch Out",
        "text": "Client Components increase the JavaScript sent to the browser. Always ask whether the data could be fetched in a Server Component before moving logic to the client."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Cleaning Up Requests"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a component unmounts before a fetch completes, the state update can cause a memory leak. Use a cleanup flag inside useEffect to ignore stale responses. For AbortController, call abort() in the cleanup function to cancel the in-flight request."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Aborting in-flight requests on unmount",
        "content": "Using AbortController to cancel in-flight requests when the component unmounts or query changes.",
        "language": "tsx",
        "code": "useEffect(() => {\n  const controller = new AbortController()\n\n  async function load() {\n    try {\n      const res = await fetch('/api/search', {\n        signal: controller.signal\n      })\n      // handle response\n    } catch (err) {\n      if (err.name !== 'AbortError') {\n        // handle real error\n      }\n    }\n  }\n\n  load()\n\n  return () => controller.abort()\n}, [query])"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Use client-side data fetching when the data depends on browser state or user input.",
          "The useEffect hook with useState remains the standard pattern for client fetches.",
          "React's use() hook offers a cleaner alternative when combined with Suspense.",
          "Always clean up requests to prevent memory leaks and state updates on unmounted components."
        ]
      }
    }
  ],
  "relatedLessons": ["server-components-data-fetching", "streaming-and-suspense", "optimistic-updates"],
  "furtherReading": [
    { "title": "Next.js Client Components Documentation", "url": "https://nextjs.org/docs/app/building-your-application/rendering/client-components" },
    { "title": "React use() Hook Documentation", "url": "https://react.dev/reference/react/use" }
  ]
}
