{
  "id": "lesson-fs-xss-csrf-004",
  "slug": "securing-react-apps",
  "moduleSlug": "03-xss-csrf-protection",
  "title": "Securing React Applications",
  "description": "Apply XSS and CSRF defenses in React components and routing.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": ["react", "xss", "csrf", "security"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "React provides some built-in safety, but you still need to be intentional about handling user input and external data."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Avoid dangerouslySetInnerHTML"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Never pass untrusted content to `dangerouslySetInnerHTML`. If you must render HTML, sanitize it first with a library like DOMPurify."
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "tsx",
        "code": "import DOMPurify from 'dompurify';\n\nfunction RichText({ html }) {\n  const clean = DOMPurify.sanitize(html, {\n    ALLOWED_TAGS: ['b', 'i', 'p', 'a'],\n    ALLOWED_ATTR: ['href']\n  });\n  return <div dangerouslySetInnerHTML={{ __html: clean }} />;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Handle User Input Safely"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Validate input length and type before sending",
          "Escape user-generated content before inserting into the DOM",
          "Use URL parsing instead of string concatenation for links",
          "Avoid inline event handlers in dynamically generated HTML"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Router and CSRF"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "SPAs often forget CSRF tokens on navigation. Use a global fetch wrapper or Axios interceptor to ensure every state-changing request includes the token."
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "tsx",
        "code": "// api.ts\nconst CSRF_HEADER = 'X-CSRF-TOKEN';\n\nexport async function apiFetch(url: string, opts: RequestInit = {}) {\n  const token = getCsrfToken();\n\n  const headers = new Headers(opts.headers);\n  if (token) headers.set(CSRF_HEADER, token);\n\n  return fetch(url, { ...opts, headers });\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Third-Party Scripts",
        "text": "Be careful with analytics or ads. Use Subresource Integrity (SRI) and CSP to limit damage if a third-party script is compromised."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use DOMPurify for any HTML sanitization",
          "Validate and escape user input",
          "Centralize CSRF token handling",
          "Treat third-party scripts as untrusted"
        ],
        "takeaway": "With XSS and CSRF covered, we can now harden the API itself."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "fullstack-security",
  "estimatedMinutes": 15
}
