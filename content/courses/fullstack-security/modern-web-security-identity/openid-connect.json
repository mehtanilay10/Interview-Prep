{
  "id": "lesson-full-stack-security-for-net-and-react-modern-web-security-identity-openid-connect",
  "slug": "openid-connect",
  "moduleSlug": "modern-web-security-identity",
  "courseSlug": "full-stack-security-for-net-and-react",
  "title": "OpenID Connect",
  "description": "Understand OpenID Connect as an identity layer on top of OAuth 2.0. Learn about ID tokens, the UserInfo endpoint, discovery documents, and how .NET and JavaScript applications consume OIDC for authentication.",
  "order": 13,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "openid-connect",
    "oidc",
    "id-token",
    "userinfo",
    "discovery",
    "authentication"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "OpenID Connect, or OIDC, is an identity layer built on top of OAuth 2.0. While OAuth 2.0 is an authorization framework — it tells you what an app is allowed to do — OIDC adds authentication: it tells you who the user is. OIDC introduces the ID token, a JWT that contains the user's identity, and the UserInfo endpoint, which returns profile claims. OIDC also defines discovery documents that let applications find endpoints automatically."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "OIDC Core Concepts"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An ID token is a JWT signed by the authorization server. It proves that the user authenticated successfully and carries claims like the user's subject ID (`sub`), name, email, and when the authentication happened (`auth_time`). The access token from OIDC is still an OAuth access token — it authorizes the client to call the UserInfo endpoint or other APIs. The critical rule is: never use an access token as proof of authentication; only the ID token can serve that role."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "ID token",
            "definition": "A JWT issued during authentication that proves the user's identity. Only the ID token authenticates; access tokens authorize."
          },
          {
            "term": "UserInfo endpoint",
            "definition": "An OIDC endpoint that returns claims about the authenticated user. Requires a valid access token."
          },
          {
            "term": "Discovery document",
            "definition": "A JSON document at /.well-known/openid-configuration that lists all OIDC endpoints, supported algorithms, and scopes."
          },
          {
            "term": "at_hash",
            "definition": "A hash of the access token included in the ID token to prevent token substitution attacks."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ID token payload (decoded JWT)",
        "content": "The ID token carries the user's identity and authentication context.",
        "code": "// ID token payload (base64-decoded)\n{\n  \"iss\": \"https://auth.example.com\",\n  \"sub\": \"e3b0c442-98fc-1c14-9a2b\",\n  \"aud\": \"my-spa-client-id\",\n  \"exp\": 1735689600,\n  \"iat\": 1735686000,\n  \"auth_time\": 1735685999,\n  \"nonce\": \"xyz789\",\n  \"name\": \"Jordan Smith\",\n  \"email\": \"jordan@example.com\",\n  \"email_verified\": true\n}",
        "language": "javascript"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Validate the ID token, do not just decode it",
        "text": "Anyone can create a JWT with any claims. The client must validate the signature, issuer, audience, expiration, and nonce. Libraries like Microsoft.IdentityModel.Tokens and oidc-client-ts handle this, but you must configure them correctly."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "OIDC Discovery"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Discovery lets applications automatically find the authorization, token, UserInfo, and JWKS endpoints by fetching a well-known URL. The discovery document also lists supported signing algorithms, scopes, and claims. This means your application does not need to hardcode endpoint URLs — it reads them from the provider at startup. Most OIDC libraries fetch and cache the discovery document automatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Discovery document URL",
        "content": "Fetch the discovery document from the authorization server's well-known path.",
        "code": "GET https://auth.example.com/.well-known/openid-configuration\n\n// Response:\n{\n  \"issuer\": \"https://auth.example.com\",\n  \"authorization_endpoint\": \"https://auth.example.com/authorize\",\n  \"token_endpoint\": \"https://auth.example.com/oauth/token\",\n  \"userinfo_endpoint\": \"https://auth.example.com/userinfo\",\n  \"jwks_uri\": \"https://auth.example.com/.well-known/jwks.json\",\n  \"scopes_supported\": [\"openid\", \"profile\", \"email\", \"offline_access\"],\n  \"response_types_supported\": [\"code\", \"token\", \"id_token\"],\n  \"subject_types_supported\": [\"public\", \"pairwise\"],\n  \"id_token_signing_alg_values_supported\": [\"RS256\"]\n}",
        "language": "javascript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "OIDC in ASP.NET Core"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "ASP.NET Core's OpenID Connect handler manages the full OIDC flow: redirecting to the authorization server, receiving the callback, validating the ID token, and creating the user's identity. It also supports automatic token management, refresh token handling, and remote sign-out. Configure it with the authority (the issuer URL), client ID, and required scopes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "OpenID Connect configuration in .NET",
        "content": "Add the OpenID Connect handler and configure it with discovery.",
        "code": "builder.Services.AddAuthentication(options =>\n{\n    options.DefaultScheme = CookieAuthenticationDefaults.AuthenticationScheme;\n    options.DefaultChallengeScheme = OpenIdConnectDefaults.AuthenticationScheme;\n})\n.AddCookie()\n.AddOpenIdConnect(options =>\n{\n    options.Authority = \"https://auth.example.com\";\n    options.ClientId = \"my-spa-proxy-client\";\n    options.ClientSecret = \"my-secret\";\n    options.ResponseType = \"code\";\n    options.SaveTokens = true;\n    options.Scope.Add(\"openid\");\n    options.Scope.Add(\"profile\");\n    options.Scope.Add(\"email\");\n    options.Scope.Add(\"offline_access\");\n    options.GetClaimsFromUserInfoEndpoint = true;\n    options.TokenValidationParameters.NameClaimType = \"name\";\n});",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "OIDC in JavaScript (SPA)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For SPAs, libraries like oidc-client-ts or @azure/msal-browser handle the authorization code flow with PKCE automatically. They manage the redirect, validate the ID token, store tokens securely, and refresh them before expiry. These libraries read the discovery document to find the correct endpoints and use the browser's crypto API for PKCE."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "OIDC login with oidc-client-ts",
        "content": "Configure the OIDC client and trigger a login redirect.",
        "code": "import { UserManager, WebStorageStateStore } from 'oidc-client-ts';\n\nconst mgr = new UserManager({\n  authority: 'https://auth.example.com',\n  client_id: 'my-spa-client',\n  redirect_uri: 'https://app.example.com/callback',\n  post_logout_redirect_uri: 'https://app.example.com/',\n  response_type: 'code',\n  scope: 'openid profile email offline_access',\n  userStore: new WebStorageStateStore({ store: sessionStorage })\n});\n\nasync function login() {\n  await mgr.signinRedirect();\n}\n\nasync function handleCallback() {\n  const user = await mgr.signinRedirectCallback();\n  console.log('Authenticated:', user.profile.name);\n  window.accessToken = user.access_token;\n}",
        "language": "javascript"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "OpenID Connect checklist",
        "items": [
          {
            "text": "Validate ID tokens on the client: issuer, audience, signature, expiration, and nonce.",
            "hint": "Use a maintained library; do not parse JWTs manually."
          },
          {
            "text": "Use the discovery document to find endpoints dynamically.",
            "hint": "Hardcoding URLs breaks when the provider rotates certificates or domains."
          },
          {
            "text": "Request offline_access to get a refresh token when you need long-lived sessions.",
            "hint": "The authorization server decides whether to grant refresh tokens based on policy."
          },
          {
            "text": "Never use an access token to prove authentication.",
            "hint": "Only the ID token proves who the user is. Access tokens authorize API access."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "OIDC adds identity on top of OAuth 2.0 using the ID token and UserInfo endpoint.",
          "The discovery document lets clients find endpoints automatically.",
          "The ID token authenticates; access tokens authorize. They serve different roles.",
          "SPA clients should use the authorization code flow with PKCE and a maintained OIDC library."
        ],
        "takeaway": "OAuth tells you what you can do; OIDC tells you who you are. Use the ID token to authenticate, the access token to authorize."
      }
    }
  ],
  "relatedLessons": [
    "oauth-20-roles-and-flows",
    "authorization-code-flow-with-pkce",
    "jwt-pitfalls-and-token-storage"
  ],
  "furtherReading": [
    {
      "title": "OpenID Foundation — OpenID Connect Core 1.0",
      "url": "https://openid.net/specs/openid-connect-core-1_0.html",
      "type": "article"
    },
    {
      "title": "Auth0 — What is OpenID Connect?",
      "url": "https://auth0.com/intro-to-iam/what-is-openid-connect-oidc",
      "type": "article"
    },
    {
      "title": "Microsoft Docs — Authentication in ASP.NET Core with OpenID Connect",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/security/authentication/openid-connect",
      "type": "article"
    }
  ]
}
