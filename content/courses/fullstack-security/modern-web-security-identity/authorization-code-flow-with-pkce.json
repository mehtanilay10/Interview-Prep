{
  "id": "lesson-full-stack-security-for-net-and-react-modern-web-security-identity-authorization-code-flow-with-pkce",
  "slug": "authorization-code-flow-with-pkce",
  "moduleSlug": "modern-web-security-identity",
  "courseSlug": "full-stack-security-for-net-and-react",
  "title": "Authorization Code Flow with PKCE",
  "description": "Learn how the Authorization Code flow with PKCE protects public clients like SPAs and mobile apps. Implement the proof key challenge exchange in JavaScript and validate it securely in .NET.",
  "order": 12,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "pkce",
    "authorization-code",
    "spa",
    "oauth",
    "code-verifier",
    "code-challenge"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "The Authorization Code flow with PKCE (Proof Key for Code Exchange) is the recommended OAuth 2.0 flow for public clients — applications that cannot keep a secret, such as single-page applications (SPAs) running in a browser or mobile apps. PKCE adds a dynamic secret created by the client for each authorization request, which the authorization server verifies before exchanging the code for tokens. This prevents authorization code interception attacks."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Problem PKCE Solves"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In the traditional authorization code flow, the client authenticates the token exchange with a client secret. Public clients cannot safely store a secret — a browser-based app's JavaScript is fully visible to users. An attacker who intercepts the authorization code can exchange it for tokens using the stolen client secret. PKCE solves this by requiring a one-time secret (the code verifier) that the client generates at runtime and that the server never sees until the exchange."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "PKCE",
            "definition": "Proof Key for Code Exchange: a mechanism that binds an authorization code to the client that requested it."
          },
          {
            "term": "Code verifier",
            "definition": "A high-entropy random string generated by the client for each authorization request."
          },
          {
            "term": "Code challenge",
            "definition": "A transformation of the code verifier (plain or hashed) sent in the authorization request."
          },
          {
            "term": "Public client",
            "definition": "An application that cannot keep a client secret, such as SPAs and mobile apps."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "PKCE flow step by step",
        "content": "The client generates a verifier, derives a challenge, sends the challenge in the authorize request, and proves possession of the verifier during the token exchange.",
        "code": "// Step 1: Generate a code verifier (43-128 characters, high entropy)\nconst verifier = base64UrlEncode(crypto.getRandomValues(new Uint8Array(32)));\n\n// Step 2: Create the code challenge (SHA256 + base64url)\nconst challenge = base64UrlEncode(\n  crypto.subtle.digest('SHA-256', new TextEncoder().encode(verifier))\n);\n\n// Step 3: Authorization request includes code_challenge\nGET https://auth.example.com/authorize?\n  response_type=code\n  &client_id=spa-client-id\n  &redirect_uri=https://app.example.com/callback\n  &code_challenge={challenge}\n  &code_challenge_method=S256\n  &state=abc123\n  &scope=openid profile\n\n// Step 4: Token exchange includes the verifier (not the challenge)\nPOST /oauth/token\n  grant_type=authorization_code\n  &code=AUTH_CODE\n  &redirect_uri=https://app.example.com/callback\n  &code_verifier={verifier}\n  // Server hashes verifier and checks it matches the stored challenge",
        "language": "javascript"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "PKCE is required for SPAs",
        "text": "The OAuth 2.0 Security BCP recommends (and many authorization servers now require) PKCE for all public clients, even confidential clients. It adds virtually no complexity and closes a serious attack vector."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Implementing PKCE in JavaScript"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Modern browsers include the Web Crypto API, so you can generate code verifiers and challenges entirely in client-side JavaScript. The verifier must be at least 43 characters and at most 128 characters, with high entropy. Store the verifier in sessionStorage — it must persist across the redirect but should be cleared after the exchange."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "PKCE helper in JavaScript",
        "content": "Generate a verifier and challenge using the Web Crypto API.",
        "code": "async function generatePKCEPair() {\n  const array = new Uint8Array(32);\n  crypto.getRandomValues(array);\n  const verifier = base64UrlEncode(array);\n\n  const challengeBuffer = await crypto.subtle.digest(\n    'SHA-256',\n    new TextEncoder().encode(verifier)\n  );\n  const challenge = base64UrlEncode(new Uint8Array(challengeBuffer));\n\n  return { verifier, challenge };\n}\n\nfunction base64UrlEncode(buffer) {\n  return btoa(String.fromCharCode(...buffer))\n    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');\n}",
        "language": "javascript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Validating PKCE on the Server"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The authorization server stores the code challenge during the authorization request. When the client sends the authorization code and the verifier in the token request, the server hashes the verifier with SHA-256 and compares it to the stored challenge. If they match, the exchange proceeds. If not, the server rejects the request with an invalid_grant error."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "PKCE validation in .NET",
        "content": "When receiving the token request, hash the verifier and compare it to the stored challenge.",
        "code": "app.MapPost(\"/oauth/token\", async (TokenRequest req) =>\n{\n    if (req.GrantType != \"authorization_code\")\n        return Results.BadRequest();\n\n    // Retrieve the stored challenge from the authorization request\n    var storedChallenge = await GetStoredChallengeAsync(req.Code);\n\n    var verifierHash = SHA256.HashData(Encoding.UTF8.GetBytes(req.CodeVerifier));\n    var computedChallenge = Convert.ToBase64String(verifierHash)\n        .Replace('+', '-').Replace('/', '_').Replace(\"=\", \"\");\n\n    if (computedChallenge != storedChallenge)\n        return Results.BadRequest(new { error = \"invalid_grant\" });\n\n    // Issue tokens...\n    var accessToken = GenerateAccessToken(req.Subject);\n    return Results.Ok(new { access_token = accessToken, expires_in = 900 });\n});",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "State Parameter for CSRF Protection"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every authorization request must include a `state` parameter — a random, unguessable value generated by the client. The client stores this value (in sessionStorage) and verifies it when the authorization server redirects back. If the state does not match, the client rejects the response. The state parameter prevents CSRF attacks where an attacker tricks the user's browser into starting an OAuth flow without their knowledge."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Authorization code + PKCE checklist",
        "items": [
          {
            "text": "Generate a fresh code verifier and challenge for every authorization request.",
            "hint": "Never reuse a verifier across multiple authorization requests."
          },
          {
            "text": "Use S256 as the code_challenge_method, not plain.",
            "hint": "Plain sends the verifier in the clear; S256 hashes it."
          },
          {
            "text": "Always include and validate the state parameter.",
            "hint": "State prevents CSRF on the authorization request."
          },
          {
            "text": "Store the verifier in sessionStorage and clear it after the exchange.",
            "hint": "The verifier must survive the redirect but should not persist beyond it."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "PKCE protects authorization codes from interception in public clients.",
          "The code verifier is a one-time secret; the challenge is its hashed form.",
          "The server stores the challenge and verifies the verifier during the token exchange.",
          "Always include the state parameter to prevent CSRF on the authorization flow."
        ],
        "takeaway": "PKCE turns the authorization code into a one-time pad: only the client that started the flow can finish it."
      }
    }
  ],
  "relatedLessons": [
    "oauth-20-roles-and-flows",
    "jwt-pitfalls-and-token-storage",
    "refresh-token-rotation"
  ],
  "furtherReading": [
    {
      "title": "IETF RFC 7636 — Proof Key for Code Exchange by OAuth Public Clients",
      "url": "https://datatracker.ietf.org/doc/html/rfc7636",
      "type": "article"
    },
    {
      "title": "Auth0 — Authorization Code Flow with PKCE",
      "url": "https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-proof-key-for-code-exchange-pkce",
      "type": "article"
    },
    {
      "title": "Microsoft Docs — MSAL browser and PKCE",
      "url": "https://learn.microsoft.com/en-us/azure/active-directory/develop/msal-js-pkce-flow",
      "type": "article"
    }
  ]
}
