{
  "id": "lesson-full-stack-security-for-net-and-react-modern-web-security-identity-sso-and-identity-provider-integration",
  "slug": "sso-and-identity-provider-integration",
  "moduleSlug": "modern-web-security-identity",
  "courseSlug": "full-stack-security-for-net-and-react",
  "title": "SSO and Identity Provider Integration",
  "description": "Learn how Single Sign-On (SSO) works, compare SAML and OIDC, and integrate external identity providers in .NET applications. Understand federation, user provisioning, and the trust boundaries between apps and identity providers.",
  "order": 16,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "sso",
    "identity-provider",
    "saml",
    "oidc",
    "federation",
    "azure-ad",
    "okta"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Single Sign-On, or SSO, lets users authenticate once and access multiple applications without re-entering credentials. Instead of each application managing its own user database and login form, SSO delegates authentication to a centralized **identity provider** (IdP). The identity provider handles login, multi-factor authentication, and lifecycle management. The applications trust the identity provider and accept the tokens it issues."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How SSO Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a user visits an application that uses SSO, the application redirects them to the identity provider. The user logs in once — possibly with MFA — and the identity provider issues a security token. The identity provider then redirects the user back to the application with the token. The application creates a local session and the user is signed in. When the user visits a second application in the same SSO federation, the identity provider sees the existing session and immediately redirects back with a new token, skipping the login prompt."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Single Sign-On (SSO)",
            "definition": "An authentication scheme where one login grants access to multiple applications."
          },
          {
            "term": "Identity Provider (IdP)",
            "definition": "A trusted service that authenticates users and issues security tokens, such as Azure AD, Okta, or Auth0."
          },
          {
            "term": "Federation",
            "definition": "A trust relationship between an application and an identity provider that enables SSO."
          },
          {
            "term": "SAML",
            "definition": "Security Assertion Markup Language: an XML-based protocol for exchanging authentication and authorization data between IdPs and service providers."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "SSO flow with OIDC",
        "content": "A simplified OIDC-based SSO flow between an app and an identity provider.",
        "code": "1. User visits https://app.example.com\n2. App redirects to https://login.idp.com/authorize?client_id=app&redirect_uri=...\n3. User authenticates at the IdP (maybe with MFA)\n4. IdP redirects back to https://app.example.com/callback?code=...\n5. App exchanges code for ID token and access token\n6. App creates a local session and the user is signed in\n7. User visits https://anotherapp.example.com\n8. Anotherapp redirects to https://login.idp.com/authorize?client_id=anotherapp\n9. IdP sees existing session, immediately redirects back with a new code\n10. Anotherapp exchanges code for tokens, user is signed in without re-entering password",
        "language": "javascript"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "SAML is not obsolete — it is enterprise-grade",
        "text": "SAML 2.0 remains the dominant protocol for enterprise SSO, especially in organizations with on-premises Active Directory and federated identity infrastructure. OIDC is preferred for cloud-native and mobile applications. Many IdPs support both, so choose the protocol that matches your application type."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "SAML vs OIDC"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "SAML uses XML assertions signed with XML signatures, and the browser relays these assertions through form posts. OIDC uses JSON Web Tokens and REST APIs. SAML is heavier but deeply entrenched in enterprise environments. OIDC is lighter, mobile-friendly, and designed for modern APIs. For new applications, OIDC is usually the better choice. For integration with existing enterprise identity infrastructure that uses SAML, choose the protocol your IdP supports best."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Feature",
          "SAML 2.0",
          "OpenID Connect"
        ],
        "rows": [
          [
            "Token format",
            "XML assertions",
            "JWT"
          ],
          [
            "Transport",
            "Browser form POST / redirect",
            "REST APIs + redirects"
          ],
          [
            "Mobile support",
            "Difficult (browser-dependent)",
            "Native (PKCE)"
          ],
          [
            "SPA support",
            "Poor",
            "Excellent"
          ],
          [
            "Enterprise adoption",
            "Very high",
            "Growing rapidly"
          ],
          [
            "Logout standard",
            "SAML logout profiles",
            "OIDC end_session_endpoint"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Integrating an Identity Provider in .NET"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "ASP.NET Core supports both OIDC and SAML through middleware. For OIDC, use the built-in OpenID Connect handler. For SAML, use a library like Sustainsys.Saml2. Both approaches create a local authentication cookie after validating the IdP's token, so the rest of your application works with the standard `[Authorize]` attribute."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Sustainsys.Saml2 configuration",
        "content": "Configure SAML2 authentication with metadata from the identity provider.",
        "code": "builder.Services.AddAuthentication(options =>\n{\n    options.DefaultScheme = CookieAuthenticationDefaults.AuthenticationScheme;\n    options.DefaultChallengeScheme = Saml2Defaults.Scheme;\n})\n.AddCookie()\n.AddSaml2(options =>\n{\n    options.SPOptions.EntityId = new EntityId(\"https://app.example.com\");\n    options.SPOptions.ReturnUrl = new Uri(\"https://app.example.com/\");\n\n    var identityProvider = options.IdentityProviders[\n        \"https://login.idp.com\"]\n        = new IdentityProvider(new EntityId(\"https://login.idp.com\"), options.SPOptions)\n    {\n        LoadMetadata = true,\n        MetadataLocation = \"https://login.idp.com/federationmetadata/2007-06/federationmetadata.xml\"\n    };\n});",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "User Provisioning"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When a user signs in through an identity provider for the first time, the application must create a local user record — a process called **just-in-time provisioning**. The application reads the claims from the ID token or UserInfo response, creates the user if they do not exist, and links the external identity to the local account. Most enterprise IdPs use SCIM (System for Cross-domain Identity Management) to automate user creation, updates, and deprovisioning."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Just-in-time user provisioning",
        "content": "Create a local user on first login if they do not already exist.",
        "code": "app.MapGet(\"/account/profile\", async (HttpContext ctx, AppDbContext db) =>\n{\n    var externalId = ctx.User.FindFirst(ClaimTypes.NameIdentifier)?.Value;\n    var email = ctx.User.FindFirst(ClaimTypes.Email)?.Value;\n\n    var user = await db.Users\n        .FirstOrDefaultAsync(u => u.ExternalId == externalId);\n\n    if (user == null)\n    {\n        user = new User\n        {\n            ExternalId = externalId,\n            Email = email,\n            DisplayName = ctx.User.Identity.Name,\n            CreatedAt = DateTime.UtcNow\n        };\n        db.Users.Add(user);\n        await db.SaveChangesAsync();\n    }\n\n    return Results.Ok(new { user.Id, user.Email, user.DisplayName });\n}).RequireAuthorization();",
        "language": "csharp"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "SSO integration checklist",
        "items": [
          {
            "text": "Register your application with the identity provider and configure redirect URIs exactly.",
            "hint": "Authorization servers reject mismatched redirect URIs by design."
          },
          {
            "text": "Use OIDC for new applications; use SAML only when required by enterprise policy.",
            "hint": "OIDC is simpler for SPAs and APIs; SAML is entrenched in enterprise SSO."
          },
          {
            "text": "Implement just-in-time provisioning with a local user table.",
            "hint": "Map external IdP claims to your internal user model."
          },
          {
            "text": "Support logout that terminates the session at both the app and the IdP.",
            "hint": "Front-channel or back-channel logout ensures the user is fully signed out everywhere."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "SSO delegates authentication to a centralized identity provider.",
          "OIDC is preferred for cloud and mobile apps; SAML remains important for enterprise SSO.",
          "Just-in-time provisioning creates local user accounts from IdP claims on first login.",
          "Front-channel logout must terminate both the app session and the IdP session."
        ],
        "takeaway": "Let identity providers handle authentication. Your application should focus on authorization and local user management."
      }
    }
  ],
  "relatedLessons": [
    "openid-connect",
    "oauth-20-roles-and-flows",
    "authorization-code-flow-with-pkce"
  ],
  "furtherReading": [
    {
      "title": "Microsoft Docs — Add authentication to your ASP.NET Core app",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/security/authentication/",
      "type": "article"
    },
    {
      "title": "Sustainsys.Saml2 — SAML2 for ASP.NET Core",
      "url": "https://github.com/Sustainsys/Saml2",
      "type": "tool"
    },
    {
      "title": "OASIS — SAML 2.0 Technical Overview",
      "url": "https://wiki.oasis-open.org/security/FrontPage",
      "type": "article"
    }
  ]
}
