{
  "id": "lesson-full-stack-security-for-net-and-react-modern-web-security-identity-client-credentials-for-service-to-service-calls",
  "slug": "client-credentials-for-service-to-service-calls",
  "moduleSlug": "modern-web-security-identity",
  "courseSlug": "full-stack-security-for-net-and-react",
  "title": "Client Credentials for Service-to-Service Calls",
  "description": "Learn how machine-to-machine authentication works using the OAuth 2.0 client credentials flow. Configure .NET services to authenticate with their own identities and call protected APIs without user interaction.",
  "order": 14,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": [
    "client-credentials",
    "service-to-service",
    "machine-to-machine",
    "oauth",
    "azure-ad"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "In a microservices or cloud architecture, services often need to call each other without a user present. For example, an order-processing service might need to call an inventory service to reserve stock. The OAuth 2.0 **client credentials flow** is designed exactly for this scenario: the service authenticates with its own identity — a client ID and client secret, or a certificate — and receives an access token it can use to call other APIs."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How Client Credentials Works"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Unlike the authorization code flow, there is no user involved. The service (the client) sends its client ID and secret directly to the authorization server's token endpoint. The authorization server verifies the credentials and issues an access token. The service then uses that token to call the resource API. Because the token represents the service itself, not a user, it should never be used to make decisions on behalf of a specific person."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Client credentials flow",
            "definition": "An OAuth 2.0 flow where a service authenticates with its own credentials to obtain an access token for machine-to-machine calls."
          },
          {
            "term": "App registration",
            "definition": "The record in the authorization server (e.g., Azure AD) that defines the service's client ID, secret, and allowed API permissions."
          },
          {
            "term": "Daemon application",
            "definition": "A service or background process that authenticates without a user, often using client credentials."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Client credentials token request",
        "content": "The service sends its credentials and receives an access token.",
        "code": "POST https://login.microsoftonline.com/tenant-id/oauth2/v2.0/token\nContent-Type: application/x-www-form-urlencoded\n\ngrant_type=client_credentials\n&client_id=inventory-service-client-id\n&client_secret=CLIENT_SECRET\n&scope=https://graph.microsoft.com/.default\n\n// Response:\n{\n  \"token_type\": \"Bearer\",\n  \"expires_in\": 3599,\n  \"ext_expires_in\": 3599,\n  \"access_token\": \"eyJ0eXAiOiJKV1QiLCJhbGc...\"\n}",
        "language": "javascript"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "The access token represents the service, not a user",
        "text": "Do not use client credentials when you need to act on behalf of a specific user. The access token issued by client credentials has no user context. If your API makes authorization decisions based on user identity, the client credentials flow cannot provide that information."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Configuring .NET for Client Credentials"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In ASP.NET Core, you can use Microsoft.Identity.Web or the generic OAuth handler to acquire tokens using client credentials. The token is cached by the library and automatically refreshed when it expires. The resource API then validates the token using the same JWT bearer authentication middleware used for user tokens."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Calling an API with client credentials in .NET",
        "content": "Use ConfidentialClientApplication to acquire a token and call a protected API.",
        "code": "var app = ConfidentialClientApplicationBuilder\n    .Create(clientId)\n    .WithClientSecret(clientSecret)\n    .WithAuthority(authority)\n    .Build();\n\nstring[] scopes = { \"api://inventory-service/.default\" };\nvar result = await app.AcquireTokenForClient(scopes)\n    .ExecuteAsync();\n\nvar httpClient = new HttpClient();\nhttpClient.DefaultRequestHeaders.Authorization =\n    new AuthenticationHeaderValue(\"Bearer\", result.AccessToken);\n\nvar inventory = await httpClient.GetAsync(\"https://inventory.api/reserve\");",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Securing Client Secrets"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Client secrets are the weakest form of service authentication. They can be stolen from configuration files, environment variables, or source control. For production services, prefer **certificate-based authentication** or **managed identities**. Azure Managed Identity lets a service authenticate to Azure resources without any secret at all — the cloud platform manages the credential lifecycle."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Certificate-based client credentials in .NET",
        "content": "Use a certificate stored in Azure Key Vault instead of a client secret.",
        "code": "var certificate = await GetCertificateFromKeyVaultAsync();\n\nvar app = ConfidentialClientApplicationBuilder\n    .Create(clientId)\n    .WithCertificate(certificate)\n    .WithAuthority(authority)\n    .Build();\n\nvar result = await app.AcquireTokenForClient(scopes)\n    .ExecuteAsync();",
        "language": "csharp"
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Service-to-service authentication checklist",
        "items": [
          {
            "text": "Use client credentials only for machine-to-machine calls.",
            "hint": "Never use it to impersonate a user or access user-specific data."
          },
          {
            "text": "Prefer certificate-based authentication over client secrets.",
            "hint": "Certificates are harder to steal and do not expire as frequently as secrets."
          },
          {
            "text": "Use managed identities when running in Azure.",
            "hint": "Azure Key Vault, Storage, and SQL all support managed identity authentication."
          },
          {
            "text": "Scope tokens to the minimum required permissions.",
            "hint": "The .default scope grants all app permissions; use specific scopes when possible."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Client credentials is the OAuth flow for service-to-service authentication.",
          "The access token represents the service, not a user — do not use it for user decisions.",
          "Prefer certificates or managed identities over client secrets for production.",
          "The resource API validates the token the same way it validates user tokens."
        ],
        "takeaway": "Services need identity too. Authenticate them with certificates or managed identities, not secrets."
      }
    }
  ],
  "relatedLessons": [
    "oauth-20-roles-and-flows",
    "secret-storage-and-rotation",
    "scopes-claims-roles-and-policies"
  ],
  "furtherReading": [
    {
      "title": "IETF RFC 6749 — Client Credentials Grant",
      "url": "https://datatracker.ietf.org/doc/html/rfc6749#section-4.4",
      "type": "article"
    },
    {
      "title": "Microsoft Docs — Microsoft identity platform and client credentials",
      "url": "https://learn.microsoft.com/en-us/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow",
      "type": "article"
    },
    {
      "title": "Azure Docs — Managed identities for Azure resources",
      "url": "https://learn.microsoft.com/en-us/entra/identity/managed-identities-azure-resources/overview",
      "type": "article"
    }
  ]
}
