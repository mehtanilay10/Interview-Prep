{
  "id": "lesson-nuget-008",
  "slug": "minio",
  "moduleSlug": "cloud-integration",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Minio",
  "description": "Run a local S3-compatible object storage server for development and testing.",
  "order": 8,
  "difficulty": "beginner",
  "estimatedMinutes": 10,
  "tags": ["minio", "s3", "storage", "local"],
  "blocks": [
    {
      "type": "heading",
      "data": {"level": 2, "text": "What is Minio?"}
    },
    {
      "type": "paragraph",
      "data": {"text": "Minio is an open-source, S3-compatible object storage server. It is commonly used in development and CI pipelines to avoid hitting real cloud storage."}
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Installation"}
    },
    {
      "type": "example",
      "data": {
        "title": "Run Minio with Docker",
        "language": "bash",
        "code": "docker run -p 9000:9000 -p 9001:9001 \\\n  -e MINIO_ROOT_USER=minioadmin \\\n  -e MINIO_ROOT_PASSWORD=minioadmin \\\n  quay.io/minio/minio server /data --console-address ':9001'"
      }
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Using the Minio .NET SDK"}
    },
    {
      "type": "paragraph",
      "data": {"text": "The `Minio` NuGet package provides an S3-compatible client that works with Minio, AWS S3, and other S3-compatible services."}
    },
    {
      "type": "example",
      "data": {
        "title": "Upload a File to Minio",
        "language": "csharp",
        "code": "using Minio;\nusing Minio.DataModel.Tags;\n\nvar minio = new MinioClient()\n    .WithEndpoint(\"localhost:9000\")\n    .WithCredentials(\"minioadmin\", \"minioadmin\")\n    .WithSSL(false)\n    .Build();\n\nstring bucketName = \"test-bucket\";\nstring objectName = \"test.txt\";\nstring filePath = \"./test.txt\";\n\n// Create bucket if it does not exist\nbool found = await minio.BucketExistsAsync(\"test-bucket\");\nif (!found)\n{\n    await minio.MakeBucketAsync(\"test-bucket\");\n}\n\n// Upload file\nawait minio.UploadFileAsync(\"test-bucket\", objectName, filePath);\nConsole.WriteLine(\"Uploaded to Minio.\");\n\n// List objects\nawait foreach (var item in minio.ListObjectsAsync(\"test-bucket\"))\n{\n    Console.WriteLine(item.Key);\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "MinioClient",
            "definition": "The S3-compatible client for interacting with Minio or any S3-compatible service."
          },
          {
            "term": "Bucket",
            "definition": "A top-level container for objects in object storage."
          },
          {
            "term": "Endpoint",
            "definition": "The host and port of the storage service, e.g., `localhost:9000`."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Use Minio in local development to avoid cloud costs and speed up tests."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Minio provides a local, S3-compatible storage server.",
          "The Minio .NET SDK uses the same API shape as AWSSDK.S3.",
          "Perfect for local development and CI/CD pipelines."
        ],
        "takeaway": "Minio lets you develop against S3 locally without any cloud costs."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Run Minio and Upload a File",
        "description": "Start Minio via Docker, create a bucket, and upload a file using the Minio .NET SDK.",
        "steps": [
          "Start Minio with Docker.",
          "Install the Minio NuGet package.",
          "Connect to Minio with MinioClient.",
          "Create a bucket and upload a file.",
          "List the objects in the bucket."
        ],
        "expectedOutcome": "You can use Minio as a local S3-compatible backend."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
