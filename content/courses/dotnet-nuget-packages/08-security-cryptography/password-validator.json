{
  "id": "lesson-nuget-807",
  "slug": "password-validator",
  "moduleSlug": "security-cryptography",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Password Validator",
  "description": "Validate password strength and enforce password policies in .NET applications.",
  "order": 7,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "security",
    "passwords",
    "validation",
    "identity"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is PasswordValidator?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A `PasswordValidator` ensures passwords meet complexity requirements before they are accepted. In ASP.NET Core Identity, `IPasswordValidator<TUser>` enforces custom password rules alongside built-in options."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install ASP.NET Core Identity if not already present.",
        "language": "bash",
        "code": "dotnet add package Microsoft.AspNetCore.Identity.EntityFrameworkCore"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Implementing a Custom Validator"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Implement `IPasswordValidator<TUser>` to add custom password rules such as minimum length, digit requirements, and forbidden words."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Custom Password Validator",
        "content": "Enforce custom password strength rules.",
        "language": "csharp",
        "code": "using Microsoft.AspNetCore.Identity;\n\npublic class CustomPasswordValidator<TUser> : IPasswordValidator<TUser>\n    where TUser : IdentityUser\n{\n    public Task<IdentityResult> ValidateAsync(\n        UserManager<TUser> manager,\n        TUser user,\n        string password)\n    {\n        var errors = new List<IdentityError>();\n\n        if (password.Length < 12)\n            errors.Add(new IdentityError\n            {\n                Code = \"PasswordTooShort\",\n                Description = \"Password must be at least 12 characters.\"\n            });\n\n        if (!password.Any(char.IsDigit))\n            errors.Add(new IdentityError\n            {\n                Code = \"PasswordRequiresDigit\",\n                Description = \"Password must contain at least one digit.\"\n            });\n\n        if (!password.Any(char.IsUpper))\n            errors.Add(new IdentityError\n            {\n                Code = \"PasswordRequiresUpper\",\n                Description = \"Password must contain an uppercase letter.\"\n            });\n\n        return Task.FromResult(\n            errors.Count == 0\n                ? IdentityResult.Success\n                : IdentityResult.Failed(errors.ToArray())\n        );\n    }\n}\n\n// Register\nbuilder.Services.AddScoped<IPasswordValidator<ApplicationUser>,\n    CustomPasswordValidator<ApplicationUser>>();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Using Built-in Identity Options"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "You can also configure password rules declaratively using `IdentityOptions` without writing a custom validator."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Configure IdentityOptions",
        "content": "Set password requirements via configuration.",
        "language": "csharp",
        "code": "builder.Services.Configure<IdentityOptions>(options =>\n{\n    options.Password.RequireDigit = true;\n    options.Password.RequiredLength = 12;\n    options.Password.RequireNonAlphanumeric = true;\n    options.Password.RequireUppercase = true;\n    options.Password.RequireLowercase = true;\n    options.Password.RequiredUniqueChars = 4;\n});"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "IPasswordValidator<TUser>",
            "definition": "An interface for implementing custom password validation logic."
          },
          {
            "term": "IdentityResult",
            "definition": "Returned by validators to indicate success or list errors."
          },
          {
            "term": "IdentityOptions",
            "definition": "Configuration class for built-in Identity password rules."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Combine both approaches: use IdentityOptions for standard rules and a custom validator for domain-specific checks like checking against breached passwords."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "IPasswordValidator<TUser> enables custom password rules",
          "IdentityResult.Success or Failed communicates validation results",
          "IdentityOptions configures built-in rules declaratively",
          "Combine both for comprehensive password policies"
        ],
        "takeaway": "Strong password validation is a critical first line of defense for user accounts."
      }
    }
  ],
  "relatedLessons": [
    "bcrypt",
    "identity-model"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
