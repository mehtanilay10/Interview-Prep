{
  "id": "lesson-nuget-805",
  "slug": "identity-model",
  "moduleSlug": "security-cryptography",
  "courseSlug": "dotnet-nuget-packages",
  "title": "IdentityModel",
  "description": "A helper library for OAuth 2.0 and OpenID Connect discovery, token requests, and introspection.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 15,
  "tags": [
    "security",
    "identitymodel",
    "oauth",
    "openidconnect"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is IdentityModel?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`IdentityModel` is a companion library to `IdentityServer`. It provides lightweight client utilities for OAuth 2.0 and OpenID Connect, including discovery, token requests, introspection, and revocation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install IdentityModel.",
        "language": "bash",
        "code": "dotnet add package IdentityModel"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Discovering an Endpoint"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `DiscoveryClient` to fetch the OpenID Connect metadata from an authority, removing the need to hardcode token and userinfo endpoints."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Discovery",
        "content": "Discover endpoints from an identity provider.",
        "language": "csharp",
        "code": "using IdentityModel.Client;\n\nvar client = new HttpClient();\nvar disco = await client.GetDiscoveryDocumentAsync(\"https://demo.identityserver.io\");\n\nif (disco.IsError)\n{\n    Console.WriteLine(disco.Error);\n    return;\n}\n\nConsole.WriteLine(\"Token endpoint: \" + disco.TokenEndpoint);\nConsole.WriteLine(\"UserInfo endpoint: \" + disco.UserInfoEndpoint);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Requesting a Token"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `TokenClient` to request access tokens using client credentials, authorization code, or resource owner password flows."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Client Credentials Token Request",
        "content": "Request an access token using client credentials.",
        "language": "csharp",
        "code": "var tokenClient = new TokenClient(client, disco.TokenEndpoint, \"client\", \"secret\");\nvar tokenResponse = await tokenClient.RequestClientCredentialsAsync(\"api1\");\n\nif (tokenResponse.IsError)\n{\n    Console.WriteLine(tokenResponse.Error);\n    return;\n}\n\nConsole.WriteLine(\"Access token: \" + tokenResponse.AccessToken);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "DiscoveryClient",
            "definition": "Retrieves OpenID Connect configuration from an authority."
          },
          {
            "term": "TokenClient",
            "definition": "Requests tokens from an OAuth 2.0 token endpoint."
          },
          {
            "term": "IntrospectionClient",
            "definition": "Validates reference tokens by calling an introspection endpoint."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "IdentityModel works well with both server-side and client-side .NET applications. It removes the boilerplate of building HTTP requests for OAuth flows."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "DiscoveryClient fetches OpenID Connect metadata",
          "TokenClient simplifies OAuth 2.0 token requests",
          "Supports client credentials, authorization code, and ROPC flows",
          "Lightweight alternative to full OAuth client libraries"
        ],
        "takeaway": "IdentityModel provides the minimal utilities needed to integrate with modern identity providers."
      }
    }
  ],
  "relatedLessons": [
    "jwt",
    "data-protection"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
