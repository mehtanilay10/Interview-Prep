{
  "id": "lesson-nuget-804",
  "slug": "jwt",
  "moduleSlug": "security-cryptography",
  "courseSlug": "dotnet-nuget-packages",
  "title": "JWT Authentication",
  "description": "Implement JSON Web Token authentication in ASP.NET Core APIs.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 18,
  "tags": [
    "security",
    "jwt",
    "authentication",
    "aspnetcore"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is JWT?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`JSON Web Tokens (JWT)` are compact, URL-safe tokens used for securely transmitting claims between parties. In ASP.NET Core, `Microsoft.AspNetCore.Authentication.JwtBearer` validates incoming JWT tokens and populates `HttpContext.User`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the JWT Bearer authentication package.",
        "language": "bash",
        "code": "dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Configuring JWT Authentication"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Register JWT Bearer authentication in `Program.cs` by providing the token validation parameters."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "JWT Configuration",
        "content": "Configure JWT Bearer authentication in Program.cs.",
        "language": "csharp",
        "code": "builder.Services\n    .AddAuthentication(JwtBearerDefaults.AuthenticationScheme)\n    .AddJwtBearer(options =>\n    {\n        options.TokenValidationParameters = new TokenValidationParameters\n        {\n            ValidateIssuer = true,\n            ValidateAudience = true,\n            ValidateLifetime = true,\n            ValidateIssuerSigningKey = true,\n            ValidIssuer = builder.Configuration[\"Jwt:Issuer\"],\n            ValidAudience = builder.Configuration[\"Jwt:Audience\"],\n            IssuerSigningKey = new SymmetricSecurityKey(\n                Encoding.UTF8.GetBytes(builder.Configuration[\"Jwt:Key\"])\n            )\n        };\n    });\n\nbuilder.Services.AddAuthorization();\nvar app = builder.Build();\napp.UseAuthentication();\napp.UseAuthorization();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generating a JWT"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a login endpoint that generates a JWT using `JwtSecurityTokenHandler` and signs it with your secret key."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Token Generation",
        "content": "Generate a JWT after validating user credentials.",
        "language": "csharp",
        "code": "using System.IdentityModel.Tokens.Jwt;\nusing Microsoft.IdentityModel.Tokens;\n\n[HttpPost(\"login\")]\npublic IActionResult Login([FromBody] LoginDto dto)\n{\n    if (!ValidateCredentials(dto.Username, dto.Password))\n        return Unauthorized();\n\n    var tokenHandler = new JwtSecurityTokenHandler();\n    var key = Encoding.UTF8.GetBytes(_configuration[\"Jwt:Key\"]);\n\n    var tokenDescriptor = new SecurityTokenDescriptor\n    {\n        Subject = new ClaimsIdentity(new[]\n        {\n            new Claim(ClaimTypes.Name, dto.Username),\n            new Claim(ClaimTypes.Role, \"Admin\")\n        }),\n        Expires = DateTime.UtcNow.AddHours(1),\n        Issuer = _configuration[\"Jwt:Issuer\"],\n        Audience = _configuration[\"Jwt:Audience\"],\n        SigningCredentials = new SigningCredentials(\n            new SymmetricSecurityKey(key),\n            SecurityAlgorithms.HmacSha256Signature\n        )\n    };\n\n    var token = tokenHandler.CreateToken(tokenDescriptor);\n    return Ok(new { token = tokenHandler.WriteToken(token) });\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "JwtBearerDefaults",
            "definition": "The default authentication scheme name for JWT Bearer."
          },
          {
            "term": "TokenValidationParameters",
            "definition": "Configuration for validating issuer, audience, lifetime, and signature."
          },
          {
            "term": "JwtSecurityTokenHandler",
            "definition": "The class used to create and write JWT tokens."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Warning",
        "text": "Never hardcode JWT secrets in code. Use environment variables or a secret manager. Use strong, randomly generated keys with at least 32 characters."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "AddJwtBearer configures token validation",
          "Use JwtSecurityTokenHandler to generate tokens",
          "Claims in the token become HttpContext.User claims",
          "Always validate issuer, audience, and signing key"
        ],
        "takeaway": "JWT Bearer authentication provides stateless, scalable API security for ASP.NET Core."
      }
    }
  ],
  "relatedLessons": [
    "data-protection",
    "identity-model"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
