{
  "id": "lesson-nuget-015",
  "slug": "memorystream",
  "moduleSlug": "advanced-patterns",
  "courseSlug": "dotnet-nuget-packages",
  "title": "MemoryStream",
  "description": "Work with in-memory byte streams for efficient I/O without temporary files.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 10,
  "tags": ["memorystream", "io", "streams", "performance"],
  "blocks": [
    {
      "type": "heading",
      "data": {"level": 2, "text": "What is MemoryStream?"}
    },
    {
      "type": "paragraph",
      "data": {"text": "`MemoryStream` is a stream backed by a byte array in memory. It is part of `System.IO` and is useful for processing data without touching the filesystem."}
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Installation"}
    },
    {
      "type": "paragraph",
      "data": {"text": "`MemoryStream` is included in the base class library. No extra package is needed."}
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Basic Usage"}
    },
    {
      "type": "paragraph",
      "data": {"text": "Write bytes to a `MemoryStream`, then read them back or convert to an array."}
    },
    {
      "type": "example",
      "data": {
        "title": "Read and Write with MemoryStream",
        "language": "csharp",
        "code": "using System.IO;\n\nbyte[] data = System.Text.Encoding.UTF8.GetBytes(\"Hello MemoryStream!\");\n\nusing MemoryStream ms = new MemoryStream();\n\n// Write bytes\nms.Write(data, 0, data.Length);\n\n// Reset position to read\nms.Position = 0;\n\nusing StreamReader reader = new StreamReader(ms);\nstring text = reader.ReadToEnd();\nConsole.WriteLine(text);\n\n// Get the underlying bytes\nbyte[] buffer = ms.ToArray();\nConsole.WriteLine($\"Bytes: {buffer.Length}\");"
      }
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Using with Streams"}
    },
    {
      "type": "paragraph",
      "data": {"text": "`MemoryStream` implements `Stream`, so you can pass it anywhere a stream is expected, such as JSON serialization or HTTP content."}
    },
    {
      "type": "example",
      "data": {
        "title": "Serialize to MemoryStream",
        "language": "csharp",
        "code": "using System.Text.Json;\n\nvar person = new { Name = \"Alice\", Age = 30 };\n\nusing MemoryStream ms = new MemoryStream();\nJsonSerializer.Serialize(ms, person);\n\nms.Position = 0;\nusing StreamReader reader = new StreamReader(ms);\nstring json = reader.ReadToEnd();\nConsole.WriteLine(json);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "MemoryStream",
            "definition": "A stream backed by an in-memory byte array."
          },
          {
            "term": "Position",
            "definition": "The current read/write cursor in the stream."
          },
          {
            "term": "ToArray",
            "definition": "Copies the stream contents to a new byte array."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Reuse MemoryStream instances with `SetLength(0)` to reduce allocations."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "MemoryStream is ideal for in-memory byte manipulation.",
          "It implements Stream, so it is interchangeable with file streams.",
          "Reset Position before reading after writing."
        ],
        "takeaway": "MemoryStream eliminates temporary files and reduces disk I/O."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Serialize to MemoryStream",
        "description": "Serialize an object to JSON using MemoryStream and print the result.",
        "steps": [
          "Create a simple class.",
          "Serialize it to a MemoryStream with JsonSerializer.",
          "Reset Position and read the JSON string.",
          "Print the JSON."
        ],
        "expectedOutcome": "You can use MemoryStream for efficient in-memory serialization."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
