{
  "id": "lesson-nuget-016",
  "slug": "system-io-pipelines",
  "moduleSlug": "advanced-patterns",
  "courseSlug": "dotnet-nuget-packages",
  "title": "System.IO.Pipelines",
  "description": "High-performance, composable pipelines for reading and writing data.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 20,
  "tags": ["pipelines", "io", "performance", "high-throughput"],
  "blocks": [
    {
      "type": "heading",
      "data": {"level": 2, "text": "What is System.IO.Pipelines?"}
    },
    {
      "type": "paragraph",
      "data": {"text": "`System.IO.Pipelines` is a high-performance library for building I/O pipelines. It minimizes memory copies and allocations by reusing memory buffers across read and write operations."}
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Installation"}
    },
    {
      "type": "example",
      "data": {
        "title": "Install Package",
        "language": "bash",
        "code": "dotnet add package System.IO.Pipelines"
      }
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Reading from a Pipe"}
    },
    {
      "type": "paragraph",
      "data": {"text": "A `Pipe` has a `Writer` and a `Reader`. The reader advances through data while the writer fills the buffer."}
    },
    {
      "type": "example",
      "data": {
        "title": "Pipe Basics",
        "language": "csharp",
        "code": "using System.IO.Pipelines;\n\nPipe pipe = Pipe.Create();\n\n// Write data\n_ = Task.Run(async () =>\n{\n    string message = \"Hello Pipelines!\";\n    byte[] bytes = System.Text.Encoding.UTF8.GetBytes(message);\n    Memory<byte> memory = pipe.Writer.GetMemory(bytes.Length);\n    bytes.CopyTo(memory);\n    await pipe.Writer.WriteAsync(bytes);\n    pipe.Writer.Complete();\n});\n\n// Read data\n_ = Task.Run(async () =>\n{\n    ReadResult result = await pipe.Reader.ReadAsync();\n    ReadOnlySequence<byte> buffer = result.Buffer;\n\n    string text = System.Text.Encoding.UTF8.GetString(buffer.ToArray());\n    Console.WriteLine(text);\n\n    pipe.Reader.AdvanceTo(buffer.End);\n    pipe.Reader.Complete();\n});\n\nawait Task.Delay(100);"
      }
    },
    {
      "type": "heading",
      "data": {"level": 2, "text": "Why Pipelines?"}
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Benefits",
        "items": [
          "Fewer memory allocations than Stream-based parsing.",
          "Built-in backpressure between reader and writer.",
          "Composable with `PipeReader` and `PipeWriter` adapters for streams."
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Pipe",
            "definition": "The core type that pairs a PipeReader and PipeWriter."
          },
          {
            "term": "ReadOnlySequence",
            "definition": "A high-performance representation of potentially discontiguous memory."
          },
          {
            "term": "AdvanceTo",
            "definition": "Tells the pipe which bytes have been consumed."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Important",
        "text": "Always call `AdvanceTo` and `Complete` to avoid memory leaks in long-running apps."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Pipelines minimize allocations by reusing buffers.",
          "Use `PipeReader` and `PipeWriter` for producer-consumer I/O.",
          "Pipelines are the foundation of ASP.NET Core's high-performance HTTP stack."
        ],
        "takeaway": "System.IO.Pipelines is the foundation for high-throughput I/O in modern .NET."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Read Text Through a Pipe",
        "description": "Write data through a PipeWriter and read it back through a PipeReader.",
        "steps": [
          "Install System.IO.Pipelines.",
          "Create a Pipe.",
          "Write a UTF-8 string to the PipeWriter.",
          "Read it back through the PipeReader.",
          "Print the decoded text."
        ],
        "expectedOutcome": "You understand the basic Pipe reader and writer workflow."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
