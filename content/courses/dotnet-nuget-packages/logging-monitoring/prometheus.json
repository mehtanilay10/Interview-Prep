{
  "id": "lesson-nuget-006",
  "slug": "prometheus",
  "moduleSlug": "logging-monitoring",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Prometheus",
  "description": "Prometheus is a time-series database and monitoring system. The Prometheus client for .NET exposes metrics that Prometheus scrapes and stores for alerting and visualization.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": [
    "monitoring",
    "prometheus",
    "metrics"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Prometheus?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Prometheus is an open-source monitoring system that collects time-series data from instrumented targets. It scrapes metrics from an HTTP endpoint exposed by your application and stores them for querying with PromQL."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the Prometheus .NET client library.",
        "language": "bash",
        "code": "dotnet add package prometheus-net"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Exposing Metrics",
        "content": "Add a /metrics endpoint and define custom metrics.",
        "language": "csharp",
        "code": "using Prometheus;\n\nclass Program\n{\n    static void Main()\n    {\n        var metricServer = new MetricServer(port: 9090);\n        metricServer.Start();\n\n        var requestCounter = Metrics.CreateCounter(\"app_requests_total\", \"Total number of requests.\");\n        var requestDuration = Metrics.CreateHistogram(\"app_request_duration_seconds\", \"Request duration in seconds.\",\n            new HistogramConfiguration { Buckets = Histogram.ExponentialBuckets(0.01, 2, 10) });\n\n        for (int i = 0; i < 5; i++)\n        {\n            requestCounter.Inc();\n            using (requestDuration.NewTimer())\n            {\n                Thread.Sleep(50);\n            }\n        }\n\n        Console.WriteLine(\"Metrics exposed at http://localhost:9090/metrics\");\n        Thread.Sleep(Timeout.Infinite);\n    }\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Metric",
            "definition": "A numerical measurement taken at runtime, such as a counter, gauge, or histogram."
          },
          {
            "term": "Scrape",
            "definition": "The act of Prometheus pulling metrics from an HTTP endpoint on a schedule."
          },
          {
            "term": "PromQL",
            "definition": "Prometheus Query Language, used to query time-series data for dashboards and alerts."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "ASP.NET Core Integration",
        "text": "Use prometheus-net.AspNetCore to automatically collect request metrics and health checks without manual instrumentation."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Prometheus scrapes metrics from an HTTP endpoint",
          "prometheus-net provides counters, gauges, and histograms",
          "Metrics are stored as time-series data"
        ],
        "takeaway": "Prometheus is the de facto standard for metrics collection and alerting in cloud-native .NET apps."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Expose Custom Metrics",
        "description": "Add Prometheus metrics to a web API and scrape them.",
        "steps": [
          "Create an ASP.NET Core Web API project",
          "Install prometheus-net and prometheus-net.AspNetCore",
          "Add a custom counter for OrdersReceived and a histogram for OrderProcessingDuration",
          "Increment the counter and observe the histogram in an order endpoint",
          "Run Prometheus and configure it to scrape http://localhost:5000/metrics"
        ],
        "expectedOutcome": "Prometheus collects and stores your custom metrics, visible in its expression browser."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
