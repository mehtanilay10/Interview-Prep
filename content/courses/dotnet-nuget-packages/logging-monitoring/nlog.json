{
  "id": "lesson-nuget-002",
  "slug": "nlog",
  "moduleSlug": "logging-monitoring",
  "courseSlug": "dotnet-nuget-packages",
  "title": "NLog",
  "description": "NLog is a flexible and high-performance logging library for .NET. It supports multiple targets (file, database, email, console) and allows configuration via XML or code.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "logging",
    "nlog"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is NLog?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "NLog is a mature logging framework that makes it easy to write logs to many different destinations called targets. You can configure NLog using an XML file or entirely in code, giving you flexibility for both simple and complex scenarios."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the NLog core package and targets.",
        "language": "bash",
        "code": "dotnet add package NLog\ndotnet add package NLog.Targets.Console\ndotnet add package NLog.Targets.File"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic NLog Setup",
        "content": "Configure NLog programmatically with console and file targets.",
        "language": "csharp",
        "code": "using NLog;\n\nclass Program\n{\n    static void Main()\n    {\n        var config = new NLog.Config.LoggingConfiguration();\n\n        var consoleTarget = new NLog.Targets.ConsoleTarget(\"console\");\n        var fileTarget = new NLog.Targets.FileTarget(\"file\")\n        {\n            FileName = \"app.log\"\n        };\n\n        config.AddRule(LogLevel.Info, LogLevel.Fatal, consoleTarget);\n        config.AddRule(LogLevel.Debug, LogLevel.Fatal, fileTarget);\n\n        LogManager.Configuration = config;\n        var logger = LogManager.GetCurrentClassLogger();\n\n        logger.Info(\"Application started\");\n        logger.Warn(\"Low memory detected\");\n        logger.Error(\"Failed to connect to database\");\n    }\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Target",
            "definition": "A destination where NLog writes log events, such as Console, File, Database, or Email."
          },
          {
            "term": "Logger",
            "definition": "The main object used to write log messages. Obtain one via LogManager.GetCurrentClassLogger()."
          },
          {
            "term": "Log Level",
            "definition": "The severity of a log event: Trace, Debug, Info, Warn, Error, or Fatal."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "XML Configuration",
        "text": "For production apps, you can move the configuration to NLog.config and use NLog.Extensions.Logging for seamless ASP.NET Core integration."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "NLog supports multiple targets out of the box",
          "Configuration can be done in code or XML",
          "Log levels let you filter messages by severity"
        ],
        "takeaway": "NLog is a flexible choice when you need logs in multiple places without changing your code."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Configure NLog Targets",
        "description": "Set up NLog to write to both the console and a file.",
        "steps": [
          "Create a new console project",
          "Install NLog and the Console and File targets",
          "Configure two targets: console for Info+ and file for Debug+",
          "Log messages at different levels (Debug, Info, Warn, Error)",
          "Open the log file and verify both targets received the expected messages"
        ],
        "expectedOutcome": "Console shows Info and above, while the log file captures Debug and all higher-severity messages."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
