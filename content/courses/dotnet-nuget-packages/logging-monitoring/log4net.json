{
  "id": "lesson-nuget-003",
  "slug": "log4net",
  "moduleSlug": "logging-monitoring",
  "courseSlug": "dotnet-nuget-packages",
  "title": "log4net",
  "description": "log4net is a port of the popular Java logging framework log4j. It provides flexible logging with configurable appenders and layouts.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 10,
  "tags": [
    "logging",
    "log4net"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is log4net?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "log4net is a proven logging library that has been used in .NET for over a decade. It follows a similar model to log4j, where you configure appenders (where logs go) and layouts (how logs look) through an XML configuration file."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the log4net package.",
        "language": "bash",
        "code": "dotnet add package log4net"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic log4net Setup",
        "content": "Configure log4net programmatically with a console appender.",
        "language": "csharp",
        "code": "using log4net;\nusing log4net.Appender;\nusing log4net.Layout;\nusing log4net.Core;\n\nclass Program\n{\n    private static readonly ILog log = LogManager.GetLogger(typeof(Program));\n\n    static void Main()\n    {\n        var appender = new ConsoleAppender\n        {\n            Layout = new PatternLayout(\"%date %-5level %logger - %message%newline\")\n        };\n        appender.ActivateOptions();\n\n        BasicConfigurator.Configure(appender);\n\n        log.Info(\"Application starting\");\n        log.Warn(\"Disk space below 10%\");\n        log.Error(\"Database connection failed\");\n    }\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Appender",
            "definition": "A destination that writes log events, such as ConsoleAppender, FileAppender, or AdoNetAppender."
          },
          {
            "term": "Layout",
            "definition": "Controls how each log event is formatted, using patterns like %date, %level, and %message."
          },
          {
            "term": "Logger",
            "definition": "The entry point for logging. Obtain one with LogManager.GetLogger(typeof(MyClass))."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "note",
        "title": "XML Configuration",
        "text": "Most log4net users configure appenders via log4net.config XML. The programmatic approach shown here is useful for understanding the underlying model."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "log4net uses appenders and layouts to control log output",
          "It is mature and widely used in legacy .NET applications",
          "XML configuration is the most common setup method"
        ],
        "takeaway": "log4net remains a solid choice, especially when maintaining existing applications."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Add a File Appender",
        "description": "Extend the basic log4net setup to also write logs to a file.",
        "steps": [
          "Create a new console project and install log4net",
          "Add a FileAppender alongside the ConsoleAppender",
          "Set the file path to app.log",
          "Log a mix of Info, Warn, and Error messages",
          "Verify both the console and the log file contain the expected entries"
        ],
        "expectedOutcome": "Both console and file outputs receive the same log events."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
