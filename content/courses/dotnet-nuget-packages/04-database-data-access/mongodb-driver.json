{
  "id": "lesson-nuget-008",
  "slug": "mongodb-driver",
  "moduleSlug": "database-data-access",
  "courseSlug": "dotnet-nuget-packages",
  "title": "MongoDB Driver",
  "description": "The official MongoDB driver for .NET, enabling document database operations.",
  "order": 8,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": [
    "mongodb",
    "nosql",
    "document",
    "database"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is the MongoDB Driver?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The MongoDB .NET Driver provides asynchronous access to MongoDB databases. It enables you to store, query, and update BSON documents using strongly-typed C# classes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the MongoDB .NET Driver.",
        "language": "bash",
        "code": "dotnet add package MongoDB.Driver"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic CRUD Operations",
        "content": "Connect to MongoDB and perform CRUD operations.",
        "language": "csharp",
        "code": "using MongoDB.Bson;\nusing MongoDB.Driver;\n\nvar client = new MongoClient(\"mongodb://localhost:27017\");\nvar database = client.GetDatabase(\"mydb\");\nvar collection = database.GetCollection<Product>(\"products\");\n\n// Insert\nawait collection.InsertOneAsync(new Product { Name = \"Laptop\", Price = 999.99m });\n\n// Find\nvar filter = Builders<Product>.Filter.Eq(p => p.Name, \"Laptop\");\nvar product = await collection.Find(filter).FirstOrDefaultAsync();\n\n// Update\nvar update = Builders<Product>.Update.Set(p => p.Price, 899.99m);\nawait collection.UpdateOneAsync(filter, update);\n\n// Delete\nawait collection.DeleteOneAsync(filter);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "MongoClient",
            "definition": "The main entry point for connecting to a MongoDB deployment."
          },
          {
            "term": "IMongoCollection",
            "definition": "Represents a MongoDB collection and provides access to documents."
          },
          {
            "term": "FilterDefinition",
            "definition": "A strongly-typed filter for querying documents."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Use `MongoDB.Bson.Serialization.Attributes` to map C# properties to BSON fields."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "MongoDB.Driver is the official .NET driver",
          "Supports async CRUD operations",
          "Provides LINQ-like query syntax"
        ],
        "takeaway": "The MongoDB Driver enables flexible document database access from .NET."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise",
        "description": "Perform CRUD operations on a MongoDB collection.",
        "steps": [
          "Start MongoDB locally",
          "Install MongoDB.Driver",
          "Connect to the database",
          "Insert, find, update, and delete documents"
        ],
        "expectedOutcome": "A working MongoDB integration with basic CRUD operations."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}