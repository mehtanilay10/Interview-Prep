{
  "id": "lesson-nuget-006",
  "slug": "redis",
  "moduleSlug": "database-data-access",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Redis",
  "description": "An in-memory data store used as a cache, message broker, and session store.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": [
    "redis",
    "cache",
    "session",
    "in-memory"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Redis?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Redis is an open-source, in-memory data store used as a cache, message broker, and streaming engine. It supports strings, hashes, lists, sets, sorted sets, and more."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the Redis package from NuGet.",
        "language": "bash",
        "code": "dotnet add package Microsoft.Extensions.Caching.StackExchangeRedis"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Configuring Redis Cache",
        "content": "Register Redis cache in Program.cs.",
        "language": "csharp",
        "code": "var builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddStackExchangeRedisCache(options =>\n{\n    options.Configuration = \"localhost:6379\";\n    options.InstanceName = \"SampleApp\";\n});\n\nvar app = builder.Build();\napp.MapGet(\"/cache/{key}\", async (string key, IDistributedCache cache) =>\n{\n    var cached = await cache.GetStringAsync(key);\n    if (cached is not null)\n        return cached;\n\n    var value = $\"Value for {key} at {DateTime.Now}\";\n    await cache.SetStringAsync(key, value, new DistributedCacheEntryOptions\n    {\n        AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)\n    });\n    return value;\n});\n\napp.Run();"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Redis",
            "definition": "An in-memory data store for caching, messaging, and streaming."
          },
          {
            "term": "IDistributedCache",
            "definition": "An abstraction for distributed caching in ASP.NET Core."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Redis is ideal for caching frequently accessed data and storing user sessions."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Redis is a fast in-memory cache",
          "Integrated via IDistributedCache",
          "Supports expiration and sliding windows"
        ],
        "takeaway": "Redis dramatically improves application performance by caching hot data."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise",
        "description": "Set up Redis caching in an ASP.NET Core app.",
        "steps": [
          "Run Redis via Docker",
          "Install the Redis package",
          "Register IDistributedCache",
          "Cache and retrieve a value"
        ],
        "expectedOutcome": "A working Redis cache integration in an ASP.NET Core app."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}