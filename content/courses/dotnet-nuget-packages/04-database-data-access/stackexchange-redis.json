{
  "id": "lesson-nuget-007",
  "slug": "stackexchange-redis",
  "moduleSlug": "database-data-access",
  "courseSlug": "dotnet-nuget-packages",
  "title": "StackExchange.Redis",
  "description": "A high-performance Redis client library for .NET.",
  "order": 7,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": [
    "redis",
    "stackexchange",
    "client",
    "cache"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is StackExchange.Redis?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "StackExchange.Redis is the Redis client library used by Stack Overflow. It provides a high-performance, thread-safe, and flexible API for interacting with Redis from .NET applications."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install StackExchange.Redis from NuGet.",
        "language": "bash",
        "code": "dotnet add package StackExchange.Redis"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Usage",
        "content": "Connect to Redis and perform operations.",
        "language": "csharp",
        "code": "using StackExchange.Redis;\n\nvar redis = ConnectionMultiplexer.Connect(\"localhost:6379\");\nvar db = redis.GetDatabase();\n\n// String operations\nawait db.StringSetAsync(\"user:1:name\", \"Alice\");\nvar name = await db.StringGetAsync(\"user:1:name\");\nConsole.WriteLine(name);\n\n// Hash operations\nawait db.HashSetAsync(\"user:1\", new HashEntry[]\n{\n    new HashEntry(\"name\", \"Alice\"),\n    new HashEntry(\"email\", \"alice@example.com\")\n});\n\n// Key expiration\nawait db.KeyExpireAsync(\"user:1\", TimeSpan.FromHours(1));"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "ConnectionMultiplexer",
            "definition": "The main entry point for interacting with Redis."
          },
          {
            "term": "IDatabase",
            "definition": "Represents a Redis database, providing access to keys and values."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Best Practice",
        "text": "Reuse `ConnectionMultiplexer` as a singleton. It is thread-safe and handles connection pooling internally."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "StackExchange.Redis is the fastest Redis client for .NET",
          "ConnectionMultiplexer should be a singleton",
          "Supports all Redis data types"
        ],
        "takeaway": "Use StackExchange.Redis for fine-grained control over Redis operations."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise",
        "description": "Perform Redis hash and string operations with StackExchange.Redis.",
        "steps": [
          "Install StackExchange.Redis",
          "Connect to Redis",
          "Store and retrieve a string value",
          "Store and retrieve a hash"
        ],
        "expectedOutcome": "A working Redis client integration."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}