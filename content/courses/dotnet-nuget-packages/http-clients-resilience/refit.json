{
  "id": "lesson-nuget-015",
  "slug": "refit",
  "moduleSlug": "http-clients-resilience",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Refit",
  "description": "Refit is a REST library for .NET that turns REST API definitions into type-safe interfaces. Instead of manually building HttpClient requests, you define an interface and Refit generates the implementation.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": [
    "http",
    "refit",
    "rest"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Refit?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Refit is a type-safe REST client for .NET. You define an interface with methods decorated by HTTP attributes, and Refit automatically generates the implementation using HttpClient under the hood."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the Refit package.",
        "language": "bash",
        "code": "dotnet add package Refit"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Defining a REST Interface",
        "content": "Create an interface for a JSONPlaceholder API.",
        "language": "csharp",
        "code": "using Refit;\n\npublic interface IJsonPlaceholderApi\n{\n    [Get(\"/posts\")]\n    Task<List<Post>> GetPostsAsync();\n\n    [Get(\"/posts/{id}\")]\n    Task<Post> GetPostAsync(int id);\n\n    [Post(\"/posts\")]\n    Task<Post> CreatePostAsync([Body] Post post);\n}\n\npublic class Post\n{\n    public int Id { get; set; }\n    public string Title { get; set; } = string.Empty;\n    public string Body { get; set; } = string.Empty;\n    public int UserId { get; set; }\n}\n\n// Usage\nvar api = RestService.For<IJsonPlaceholderApi>(\"https://jsonplaceholder.typicode.com\");\nvar posts = await api.GetPostsAsync();"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Type-Safe",
            "definition": "The compiler checks method signatures and return types, reducing runtime errors."
          },
          {
            "term": "RestService",
            "definition": "The Refit factory that creates an implementation of your interface."
          },
          {
            "term": "HttpClient",
            "definition": "The underlying HTTP client Refit uses to make requests."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "DI Integration",
        "text": "Register Refit interfaces in the DI container with services.AddRefitClient<IYourApi>().ConfigureHttpClient(c => c.BaseAddress = ...)."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Refit turns interfaces into REST clients",
          "HTTP attributes map methods to endpoints",
          "It removes boilerplate HttpClient code"
        ],
        "takeaway": "Refit is the fastest way to build a type-safe API client in .NET."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a GitHub Client",
        "description": "Create a Refit interface for the GitHub API.",
        "steps": [
          "Define an IGitHubApi interface with methods for GetUser(string username) and GetRepos(string username)",
          "Use RestService.For or AddRefitClient in DI",
          "Call GetUser for a known GitHub user and print their name and public repos count",
          "Add error handling for 404 responses"
        ],
        "expectedOutcome": "The app fetches and displays real GitHub user data with minimal boilerplate."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
