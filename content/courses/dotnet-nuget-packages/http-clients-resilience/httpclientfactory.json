{
  "id": "lesson-nuget-020",
  "slug": "httpclientfactory",
  "moduleSlug": "http-clients-resilience",
  "courseSlug": "dotnet-nuget-packages",
  "title": "HttpClientFactory",
  "description": "HttpClientFactory is a factory for creating HttpClient instances in ASP.NET Core. It manages DNS changes, socket exhaustion, and integrates with Polly for resilience.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": [
    "http",
    "httpclient",
    "aspnet-core"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is HttpClientFactory?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "HttpClientFactory manages the lifetime of HttpClient instances. It prevents socket exhaustion by pooling handlers, handles DNS refresh automatically, and integrates seamlessly with Polly policies."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Registering Typed Clients",
        "content": "Configure a typed GitHub client with Polly policies.",
        "language": "csharp",
        "code": "using Microsoft.Extensions.DependencyInjection;\nusing Polly;\n\nvar builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddHttpClient<IGitHubService, GitHubService>(client =>\n    {\n        client.BaseAddress = new Uri(\"https://api.github.com/\");\n        client.DefaultRequestHeaders.Add(\"User-Agent\", \"MyApp\");\n    })\n    .AddPolicyHandler(Policy.Handle<HttpRequestException>()\n        .RetryAsync(3));\n\nvar app = builder.Build();\napp.MapGet(\"/repos/{user}\", async (IGitHubService gitHub, string user) =>\n{\n    var repos = await gitHub.GetReposAsync(user);\n    return Results.Ok(repos);\n});\n\napp.Run();"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Typed Client",
            "definition": "A class that receives an HttpClient via constructor injection, encapsulating API logic."
          },
          {
            "term": "IHttpClientFactory",
            "definition": "The service that creates and manages HttpClient instances with pooling."
          },
          {
            "term": "Handler Lifetime",
            "definition": "HttpClientFactory reuses HttpMessageHandler instances to avoid socket exhaustion."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Avoid new HttpClient()",
        "text": "Creating a new HttpClient for every request can exhaust sockets. Use IHttpClientFactory or reuse a single HttpClient instance instead."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "HttpClientFactory manages handler lifetimes automatically",
          "Typed clients encapsulate API logic in dedicated classes",
          "Polly policies integrate via AddPolicyHandler"
        ],
        "takeaway": "HttpClientFactory is the recommended way to use HttpClient in modern .NET."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Build a Typed Client",
        "description": "Create a typed client for a weather API.",
        "steps": [
          "Create an IWeatherService interface with a GetForecastAsync method",
          "Implement WeatherService that accepts HttpClient in its constructor",
          "Register the typed client with AddHttpClient<IWeatherService, WeatherService>()",
          "Inject IWeatherService into a controller or minimal API endpoint",
          "Call the endpoint and display the forecast data"
        ],
        "expectedOutcome": "A working typed client that fetches weather data through HttpClientFactory."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
