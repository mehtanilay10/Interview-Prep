{
  "id": "lesson-nuget-018",
  "slug": "polly",
  "moduleSlug": "http-clients-resilience",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Polly",
  "description": "Polly is a resilience and transient-fault-handling library for .NET. It provides retry, circuit breaker, timeout, fallback, and bulkhead isolation policies to make distributed systems more robust.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": [
    "http",
    "polly",
    "resilience"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Polly?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Polly is a .NET library that implements resilience patterns. Instead of writing complex retry and error-handling logic manually, you compose policies that automatically handle transient failures like network drops, timeouts, and 500 errors."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install Polly.",
        "language": "bash",
        "code": "dotnet add package Polly"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Retry and Circuit Breaker",
        "content": "Wrap an HTTP call with retry and circuit breaker policies.",
        "language": "csharp",
        "code": "using Polly;\nusing System.Net.Http;\n\nclass Program\n{\n    static readonly HttpClient http = new();\n\n    static async Task Main()\n    {\n        var retry = Policy\n            .Handle<HttpRequestException>()\n            .WaitAndRetryAsync(3, retryAttempt =>\n                TimeSpan.FromSeconds(Math.Pow(2, retryAttempt)));\n\n        var circuitBreaker = Policy\n            .Handle<HttpRequestException>()\n            .CircuitBreakerAsync(2, TimeSpan.FromSeconds(30));\n\n        var combined = Policy.WrapAsync(retry, circuitBreaker);\n\n        await combined.ExecuteAsync(async () =>\n        {\n            var response = await http.GetAsync(\"https://api.example.com/data\");\n            response.EnsureSuccessStatusCode();\n            Console.WriteLine(\"Success!\");\n        });\n    }\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Retry",
            "definition": "Automatically re-executes an operation after a transient failure."
          },
          {
            "term": "Circuit Breaker",
            "definition": "Stops calling an operation after a threshold of failures, giving the downstream service time to recover."
          },
          {
            "term": "Policy",
            "definition": "A reusable rule that defines how to handle failures."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Combine Policies",
        "text": "Use Policy.WrapAsync() to compose multiple policies. A common stack is Retry + CircuitBreaker + Timeout."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Polly handles transient faults declaratively",
          "Policies are composable and reusable",
          "Circuit breakers prevent cascading failures"
        ],
        "takeaway": "Polly turns brittle HTTP calls into resilient operations with minimal code."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Add Retry to an API Call",
        "description": "Wrap a flaky API call with a Polly retry policy.",
        "steps": [
          "Create a method that calls https://httpstat.us/500 (returns 500)",
          "Add a retry policy that waits 2, 4, and 8 seconds between retries",
          "Log each retry attempt",
          "Observe the total number of attempts and the final outcome"
        ],
        "expectedOutcome": "The app retries the failing call three times before surfacing the error."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
