{
  "id": "lesson-nuget-016",
  "slug": "flurl",
  "moduleSlug": "http-clients-resilience",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Flurl",
  "description": "Flurl is a fluent URL builder and HTTP client library for .NET. It lets you build URLs and make HTTP requests using a chainable, readable syntax.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": [
    "http",
    "flurl",
    "fluent"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Flurl?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Flurl is a fluent library for building URLs and making HTTP calls. Its chainable API makes constructing complex URLs and reading JSON responses feel natural and concise."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install Flurl.Http.",
        "language": "bash",
        "code": "dotnet add package Flurl.Http"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Building URLs and Making Requests",
        "content": "Use Flurl to build a URL and POST JSON.",
        "language": "csharp",
        "code": "using Flurl;\nusing Flurl.Http;\n\nclass Program\n{\n    static async Task Main()\n    {\n        var response = await \"https://api.example.com\"\n            .AppendPathSegment(\"users\")\n            .WithOAuthBearerToken(\"token\")\n            .PostJsonAsync(new { Name = \"Alice\", Email = \"alice@example.com\" });\n\n        var user = await response.GetJsonAsync<User>();\n        Console.WriteLine($\"Created user: {user.Name}\");\n\n        var list = await \"https://api.example.com/users\"\n            .SetQueryParam(\"page\", 1)\n            .SetQueryParam(\"pageSize\", 10)\n            .GetJsonAsync<List<User>>();\n    }\n}\n\npublic class User { public string Name { get; set; } = string.Empty; }"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Fluent API",
            "definition": "A chainable interface where each method returns an object that can be chained to the next call."
          },
          {
            "term": "AppendPathSegment",
            "definition": "Adds a segment to a URL path, handling slashes correctly."
          },
          {
            "term": "SetQueryParam",
            "definition": "Adds or replaces a query string parameter."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Flurl Client Reuse",
        "text": "Use Flurl.Configure(settings => settings.HttpClientFactory = ...) to manage HttpClient lifetimes and avoid socket exhaustion."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Flurl uses a fluent, chainable syntax for URLs and HTTP",
          "JSON serialization is built in",
          "It integrates with HttpClient lifetimes via settings"
        ],
        "takeaway": "Flurl makes building and calling HTTP APIs feel effortless."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Query a Public API",
        "description": "Use Flurl to fetch and display data from a public API.",
        "steps": [
          "Create a console app and install Flurl.Http",
          "Call https://jsonplaceholder.typicode.com/posts with a userId query parameter",
          "Deserialize the response into a list of posts",
          "Print the title and body of each post"
        ],
        "expectedOutcome": "Console displays filtered posts fetched with a fluent Flurl request."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
