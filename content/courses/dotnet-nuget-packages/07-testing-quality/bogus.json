{
  "id": "lesson-nuget-708",
  "slug": "bogus",
  "moduleSlug": "testing-quality",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Bogus",
  "description": "A fake data generator for .NET that creates realistic test data on demand.",
  "order": 8,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": [
    "testing",
    "bogus",
    "fakedata",
    "generator"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Bogus?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`Bogus` is a fake data generator that produces realistic, locale-aware test data. Unlike AutoFixture's random data, Bogus generates data that looks real, such as valid email addresses, names, addresses, and company information."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install Bogus.",
        "language": "bash",
        "code": "dotnet add package Bogus"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generating Fake Data"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a `Faker<T>` to generate instances of your custom types with realistic data."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Bogus Faker",
        "content": "Generate fake users with Bogus.",
        "language": "csharp",
        "code": "using Bogus;\nusing Xunit;\n\npublic class User\n{\n    public int Id { get; set; }\n    public string FirstName { get; set; }\n    public string LastName { get; set; }\n    public string Email { get; set; }\n}\n\n[Fact]\npublic void Generate_Fake_Users()\n{\n    var userFaker = new Faker<User>()\n        .RuleFor(u => u.Id, f => f.UniqueIndex)\n        .RuleFor(u => u.FirstName, f => f.Name.FirstName())\n        .RuleFor(u => u.LastName, f => f.Name.LastName())\n        .RuleFor(u => u.Email, (f, u) => f.Internet.Email(u.FirstName, u.LastName));\n\n    var user = userFaker.Generate();\n\n    Assert.False(string.IsNullOrEmpty(user.FirstName));\n    Assert.Contains(\"@\", user.Email);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generating Collections"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `Generate(count)` to create multiple items. This is ideal for seeding databases in integration tests."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generate Multiple Users",
        "content": "Create a list of fake users for test seeding.",
        "language": "csharp",
        "code": "[Fact]\npublic void Seed_Test_Database_With_Fake_Users()\n{\n    var userFaker = new Faker<User>()\n        .RuleFor(u => u.Id, f => f.UniqueIndex)\n        .RuleFor(u => u.FirstName, f => f.Name.FirstName())\n        .RuleFor(u => u.LastName, f => f.Name.LastName())\n        .RuleFor(u => u.Email, (f, u) => f.Internet.Email(u.FirstName, u.LastName));\n\n    var users = userFaker.Generate(10);\n\n    Assert.Equal(10, users.Count);\n    Assert.All(users, u => Assert.NotNull(u.Email));\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Faker<T>",
            "definition": "A fluent builder that generates fake instances of type T."
          },
          {
            "term": "RuleFor",
            "definition": "Registers a rule that defines how a specific property is generated."
          },
          {
            "term": "Generate",
            "definition": "Produces one or many instances based on the configured rules."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Bogus supports many locales. Use `new Faker(\"fr\")` to generate French names and addresses for internationalization tests."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Bogus generates realistic, locale-aware fake data",
          "Faker<T> uses RuleFor to configure property generation",
          "Generate() creates single or multiple instances",
          "Ideal for seeding test databases and integration tests"
        ],
        "takeaway": "Bogus produces more realistic test data than random generators, improving test reliability."
      }
    }
  ],
  "relatedLessons": [
    "autofixture",
    "fluent-assertions"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
