{
  "id": "lesson-nuget-705",
  "slug": "moq",
  "moduleSlug": "testing-quality",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Moq",
  "description": "The most popular mocking library for .NET, using LINQ and lambda expressions.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "testing",
    "moq",
    "mocking"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Moq?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`Moq` is the most widely used mocking library for .NET. It uses LINQ and lambda expressions to create, configure, and verify mocks without requiring any mock generation tools or runtime proxies."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install Moq.",
        "language": "bash",
        "code": "dotnet add package Moq"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating and Using Mocks"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `Mock<T>` to create a mock of an interface or class. Configure behavior with `Setup` and verify interactions with `Verify`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Mocking an IRepository",
        "content": "Mock a repository and verify it was called.",
        "language": "csharp",
        "code": "using Moq;\nusing Xunit;\n\npublic interface IRepository\n{\n    Task<User> GetByIdAsync(int id);\n    Task SaveAsync(User user);\n}\n\n[Fact]\npublic async Task GetUser_Should_Call_Repository()\n{\n    // Arrange\n    var mockRepo = new Mock<IRepository>();\n    mockRepo.Setup(r => r.GetByIdAsync(1))\n            .ReturnsAsync(new User { Id = 1, Name = \"Alice\" });\n\n    var service = new UserService(mockRepo.Object);\n\n    // Act\n    var user = await service.GetUserAsync(1);\n\n    // Assert\n    Assert.Equal(\"Alice\", user.Name);\n    mockRepo.Verify(r => r.GetByIdAsync(1), Times.Once);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Throwing Exceptions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Configure mocks to throw exceptions to test error handling paths."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Mocking Exceptions",
        "content": "Test that your code handles repository failures.",
        "language": "csharp",
        "code": "[Fact]\npublic async Task GetUser_When_Repository_Fails_Throws()\n{\n    var mockRepo = new Mock<IRepository>();\n    mockRepo.Setup(r => r.GetByIdAsync(It.IsAny<int>()))\n            .ThrowsAsync(new InvalidOperationException(\"DB down\"));\n\n    var service = new UserService(mockRepo.Object);\n\n    await Assert.ThrowsAsync<InvalidOperationException>(\n        () => service.GetUserAsync(1));\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Mock<T>",
            "definition": "The primary type used to create a mock of an interface or class."
          },
          {
            "term": "Setup",
            "definition": "Configures the behavior of a mock for a specific method call."
          },
          {
            "term": "Verify",
            "definition": "Asserts that a specific method was called with expected arguments."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Use `It.IsAny<T>()` and `It.Is<T>(predicate)` to match flexible arguments in mock setups and verifications."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Moq uses Mock<T> to create mocks",
          "Setup configures return values and behaviors",
          "Verify confirms interactions occurred",
          "It.IsAny<T>() matches any argument of type T"
        ],
        "takeaway": "Moq makes mocking in .NET tests concise and strongly typed."
      }
    }
  ],
  "relatedLessons": [
    "xunit",
    "nsubstitute"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
