{
  "id": "lesson-nuget-706",
  "slug": "nsubstitute",
  "moduleSlug": "testing-quality",
  "courseSlug": "dotnet-nuget-packages",
  "title": "NSubstitute",
  "description": "A friendly mocking library with a simple, natural syntax for .NET.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 15,
  "tags": [
    "testing",
    "nsubstitute",
    "mocking"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is NSubstitute?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`NSubstitute` is a mocking library designed for readability. It avoids complex configuration and instead uses a straightforward syntax where calling a method on a substitute automatically creates the return value."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install NSubstitute.",
        "language": "bash",
        "code": "dotnet add package NSubstitute"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating Substitutes"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `Substitute.For<T>()` to create a substitute. Configure returns with `.Returns()` and received calls with `.Received()`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Substitute",
        "content": "Create and configure a substitute with NSubstitute.",
        "language": "csharp",
        "code": "using NSubstitute;\nusing Xunit;\n\npublic interface IWeatherService\n{\n    Task<string> GetForecastAsync(string city);\n}\n\n[Fact]\npublic async Task GetForecast_Returns_Expected_City()\n{\n    // Arrange\n    var weather = Substitute.For<IWeatherService>();\n    weather.GetForecastAsync(\"Paris\").Returns(\"Sunny\");\n\n    var app = new WeatherApp(weather);\n\n    // Act\n    var forecast = await app.GetLocalForecastAsync(\"Paris\");\n\n    // Assert\n    Assert.Equal(\"Sunny\", forecast);\n    await weather.Received(1).GetForecastAsync(\"Paris\");\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Raising Events and Exceptions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "NSubstitute can also raise events or throw exceptions to test event handlers and error handling."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Throwing Exceptions",
        "content": "Test error handling with substitutes.",
        "language": "csharp",
        "code": "[Fact]\npublic async Task GetForecast_When_Service_Fails_Throws()\n{\n    var weather = Substitute.For<IWeatherService>();\n    weather.GetForecastAsync(Arg.Any<string>())\n          .ThrowsAsync(new HttpRequestException());\n\n    var app = new WeatherApp(weather);\n\n    await Assert.ThrowsAsync<HttpRequestException>(\n        () => app.GetLocalForecastAsync(\"Tokyo\"));\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Substitute.For<T>",
            "definition": "Creates a mock-like object for the given interface or class."
          },
          {
            "term": "Returns",
            "definition": "Specifies the value a method call should return."
          },
          {
            "term": "Received",
            "definition": "Verifies that a method was called a specific number of times."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "NSubstitute does not require explicit Setup calls. If you call a method on a substitute without configuring it, it returns a default value automatically."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "NSubstitute uses Substitute.For<T>() to create mocks",
          "Returns configures return values",
          "Received verifies method calls",
          "No explicit Setup required for simple cases"
        ],
        "takeaway": "NSubstitute prioritizes readability with minimal ceremony for test setup."
      }
    }
  ],
  "relatedLessons": [
    "moq",
    "fluent-assertions"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
