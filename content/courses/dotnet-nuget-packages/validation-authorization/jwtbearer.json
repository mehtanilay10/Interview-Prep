{
  "id": "lesson-nuget-013",
  "slug": "jwtbearer",
  "moduleSlug": "validation-authorization",
  "courseSlug": "dotnet-nuget-packages",
  "title": "JWT Bearer Authentication",
  "description": "JWT Bearer authentication lets your API validate access tokens without cookies. It is the standard way to secure stateless APIs in modern .NET applications.",
  "order": 5,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "authorization",
    "jwt",
    "bearer"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is JWT Bearer Authentication?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "JSON Web Tokens (JWT) are compact, URL-safe tokens that carry claims about a user. With JWT Bearer authentication, the client sends the token in the Authorization header, and your API validates it without hitting a database or session store."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the JWT Bearer authentication handler.",
        "language": "bash",
        "code": "dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Configuring JWT Bearer",
        "content": "Add JWT Bearer authentication in Program.cs.",
        "language": "csharp",
        "code": "using Microsoft.AspNetCore.Authentication.JwtBearer;\nusing Microsoft.IdentityModel.Tokens;\nusing System.Text;\n\nvar builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)\n    .AddJwtBearer(options =>\n    {\n        options.TokenValidationParameters = new TokenValidationParameters\n        {\n            ValidateIssuer = true,\n            ValidateAudience = true,\n            ValidateLifetime = true,\n            ValidateIssuerSigningKey = true,\n            ValidIssuer = \"https://localhost:5001\",\n            ValidAudience = \"api1\",\n            IssuerSigningKey = new SymmetricSecurityKey(\n                Encoding.UTF8.GetBytes(\"supersecretkey_12345678901234567890\"))\n        };\n    });\n\nbuilder.Services.AddAuthorization();\n\nvar app = builder.Build();\napp.UseAuthentication();\napp.UseAuthorization();\napp.Run();"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "JWT",
            "definition": "A compact, signed token that contains claims and is transmitted in the Authorization header."
          },
          {
            "term": "Bearer Token",
            "definition": "A token presented in the Authorization header as 'Bearer <token>'."
          },
          {
            "term": "Token Validation Parameters",
            "definition": "Settings that define how the JWT handler validates the issuer, audience, signature, and expiration."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Secret Management",
        "text": "Never hard-code signing keys. Use Azure Key Vault, environment variables, or user secrets in development."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "JWT Bearer auth is stateless and ideal for APIs",
          "Tokens are validated using a signing key and metadata",
          "Use [Authorize] to protect endpoints"
        ],
        "takeaway": "JWT Bearer authentication is the standard way to secure modern .NET APIs."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Protect an API Endpoint",
        "description": "Require a valid JWT to access a weather forecast endpoint.",
        "steps": [
          "Create a minimal API project",
          "Add JWT Bearer authentication with a known signing key",
          "Generate a JWT using jwt.io or a small console utility",
          "Add a /weather endpoint decorated with [Authorize]",
          "Call the endpoint without a token (expect 401), then with a valid token (expect 200)"
        ],
        "expectedOutcome": "Only requests with a valid JWT receive the weather data."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
