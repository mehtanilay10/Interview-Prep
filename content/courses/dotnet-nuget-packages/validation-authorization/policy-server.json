{
  "id": "lesson-nuget-014",
  "slug": "policy-server",
  "moduleSlug": "validation-authorization",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Policy-Based Authorization",
  "description": "Policy-based authorization lets you define reusable authorization rules in ASP.NET Core. Policies combine requirements and handlers to enforce complex access rules cleanly.",
  "order": 6,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "authorization",
    "policy",
    "aspnet-core"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Policy-Based Authorization?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Policy-based authorization lets you define access rules as reusable policies. Each policy consists of requirements (what must be true) and handlers (how to evaluate those requirements). This keeps authorization logic out of your controllers."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Defining a Custom Policy",
        "content": "Create a minimum age requirement and policy.",
        "language": "csharp",
        "code": "using Microsoft.AspNetCore.Authorization;\n\npublic class MinimumAgeRequirement : IAuthorizationRequirement\n{\n    public int MinimumAge { get; }\n    public MinimumAgeRequirement(int minimumAge) => MinimumAge = minimumAge;\n}\n\npublic class MinimumAgeHandler : AuthorizationHandler<MinimumAgeRequirement>\n{\n    protected override Task HandleRequirementAsync(AuthorizationHandlerContext context, MinimumAgeRequirement requirement)\n    {\n        var dateOfBirth = context.User.FindFirst(\"date_of_birth\")?.Value;\n        if (dateOfBirth == null) return Task.CompletedTask;\n\n        if (DateTime.TryParse(dateOfBirth, out var dob) &&\n            DateTime.Today.Year - dob.Year >= requirement.MinimumAge)\n        {\n            context.Succeed(requirement);\n        }\n\n        return Task.CompletedTask;\n    }\n}\n\n// In Program.cs\nbuilder.Services.AddAuthorization(options =>\n{\n    options.AddPolicy(\"AdultsOnly\", policy =>\n        policy.Requirements.Add(new MinimumAgeRequirement(18)));\n});\n\nbuilder.Services.AddScoped<IAuthorizationHandler, MinimumAgeHandler>();"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Policy",
            "definition": "A named set of requirements that must be satisfied for authorization to succeed."
          },
          {
            "term": "Requirement",
            "definition": "A condition that the authorization system checks, represented by a class implementing IAuthorizationRequirement."
          },
          {
            "term": "Handler",
            "definition": "A class that evaluates a requirement and calls context.Succeed() or context.Fail()."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Built-In Policies",
        "text": "ASP.NET Core includes built-in policies like RequireAuthenticatedUser, RequireRole, and RequireClaim. Use custom policies only when built-in ones are not enough."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Policies separate authorization rules from controllers",
          "Requirements define what must be true",
          "Handlers contain the evaluation logic"
        ],
        "takeaway": "Policy-based authorization is the most maintainable way to enforce complex access rules."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Create a Department Policy",
        "description": "Require users to belong to a specific department to access an endpoint.",
        "steps": [
          "Create a DepartmentRequirement with a DepartmentName property",
          "Create a DepartmentHandler that checks the user's department claim",
          "Register the policy in Program.cs with department name 'HR'",
          "Protect a GET endpoint with [Authorize(Policy = \"HROnly\")]",
          "Test with tokens that have and lack the department claim"
        ],
        "expectedOutcome": "Only users with the matching department claim can access the protected endpoint."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
