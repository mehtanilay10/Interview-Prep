{
  "id": "lesson-nuget-010",
  "slug": "dataannotations",
  "moduleSlug": "validation-authorization",
  "courseSlug": "dotnet-nuget-packages",
  "title": "DataAnnotations",
  "description": "DataAnnotations provide a built-in way to add validation rules directly to model classes using attributes. They are the simplest way to add validation in ASP.NET Core applications.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "validation",
    "dataannotations"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What are DataAnnotations?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "DataAnnotations are attributes in the System.ComponentModel.DataAnnotations namespace. You apply them to model properties to declare rules such as required fields, string length limits, and range checks. ASP.NET Core automatically validates them during model binding."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Applying DataAnnotations",
        "content": "Add validation attributes to a Product model.",
        "language": "csharp",
        "code": "using System.ComponentModel.DataAnnotations;\n\npublic class Product\n{\n    [Required(ErrorMessage = \"Product name is required\")]\n    [StringLength(100, MinimumLength = 3)]\n    public string Name { get; set; } = string.Empty;\n\n    [Range(0.01, 9999.99)]\n    public decimal Price { get; set; }\n\n    [EmailAddress]\n    public string ContactEmail { get; set; } = string.Empty;\n\n    [RegularExpression(@\"^[A-Z]{2}-\\d{4}$\", ErrorMessage = \"SKU must match XX-1234\")]\n    public string Sku { get; set; } = string.Empty;\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Required",
            "definition": "Ensures a property has a non-null value."
          },
          {
            "term": "StringLength",
            "definition": "Specifies the maximum and optionally minimum length of a string."
          },
          {
            "term": "Range",
            "definition": "Validates that a numeric value falls within a specified range."
          },
          {
            "term": "RegularExpression",
            "definition": "Validates a value against a regex pattern."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Client-Side Validation",
        "text": "DataAnnotations work on the server by default. To enable client-side validation in MVC, include the necessary JavaScript and ensure jQuery Validation is loaded."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "DataAnnotations use attributes to declare validation rules",
          "ASP.NET Core validates them automatically during model binding",
          "ModelState.IsValid tells you whether validation passed"
        ],
        "takeaway": "DataAnnotations are the quickest way to add basic validation to your models."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Validate a Registration Form",
        "description": "Apply DataAnnotations to a user registration model.",
        "steps": [
          "Create a RegisterUserModel with Username, Email, Password, and ConfirmPassword",
          "Apply Required, StringLength, EmailAddress, and Compare attributes",
          "Create an API controller with a POST endpoint that accepts the model",
          "Return a 400 response with validation errors if ModelState is invalid",
          "Test with valid and invalid payloads"
        ],
        "expectedOutcome": "Invalid payloads return descriptive validation errors; valid payloads proceed normally."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
