{
  "id": "lesson-nuget-902",
  "slug": "mailkit",
  "moduleSlug": "utilities-helpers",
  "courseSlug": "dotnet-nuget-packages",
  "title": "MailKit",
  "description": "A cross-platform mail client library for IMAP, POP3, and SMTP in .NET.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "email",
    "mailkit",
    "smtp",
    "imap",
    "pop3"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is MailKit?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`MailKit` is a comprehensive mail client library for .NET. It supports IMAP, POP3, SMTP, and NNTP with full async support, SASL authentication, and modern security features."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install MailKit.",
        "language": "bash",
        "code": "dotnet add package MailKit"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Sending Email with SMTP"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `SmtpClient` to connect to an SMTP server and send messages. MailKit supports modern authentication methods like OAuth 2.0."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Send an Email",
        "content": "Send an email via SMTP with MailKit.",
        "language": "csharp",
        "code": "using MailKit.Net.Smtp;\nusing MimeKit;\n\npublic async Task SendAsync()\n{\n    var message = new MimeMessage();\n    message.From.Add(new MailboxAddress(\"MyApp\", \"noreply@myapp.com\"));\n    message.To.Add(new MailboxAddress(\"User\", \"user@example.com\"));\n    message.Subject = \"Account Created\";\n\n    message.Body = new TextPart(\"plain\")\n    {\n        Text = \"Your account has been created successfully.\"\n    };\n\n    using var client = new SmtpClient();\n    await client.ConnectAsync(\"smtp.example.com\", 587, MailKit.Security.SecureSocketOptions.StartTls);\n    await client.AuthenticateAsync(\"user\", \"password\");\n    await client.SendAsync(message);\n    await client.DisconnectAsync(true);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Receiving Email with IMAP"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `ImapClient` to connect to an IMAP server, fetch messages, and parse them."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Fetch Inbox Messages",
        "content": "Connect to IMAP and list recent subjects.",
        "language": "csharp",
        "code": "using MailKit.Net.Imap;\nusing MailKit.SearchSorter;\n\npublic async Task ListRecentAsync()\n{\n    using var client = new ImapClient();\n    await client.ConnectAsync(\"imap.example.com\", 993, true);\n    await client.AuthenticateAsync(\"user\", \"password\");\n\n    var inbox = client.Inbox;\n    await inbox.OpenAsync(MailKit.FolderAccess.ReadOnly);\n\n    var uids = await inbox.SearchAsync(MailKit.Search.SearchQuery.Recent);\n    foreach (var uid in uids)\n    {\n        var message = await inbox.GetMessageAsync(uid);\n        Console.WriteLine($\"{uid}: {message.Subject}\");\n    }\n\n    await client.DisconnectAsync(true);\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "SmtpClient",
            "definition": "Sends email messages via SMTP with async support."
          },
          {
            "term": "ImapClient",
            "definition": "Connects to IMAP servers to read and manage email."
          },
          {
            "term": "MimeMessage",
            "definition": "Represents an email message following the MIME standard."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "MailKit is created by the same author as MimeKit. Use MimeKit to build rich HTML emails with inline images and attachments."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "MailKit supports SMTP, IMAP, and POP3",
          "Full async API with cancellation support",
          "OAuth 2.0 and SASL authentication supported",
          "MimeKit builds rich MIME email content"
        ],
        "takeaway": "MailKit is the most complete mail client library available for .NET."
      }
    }
  ],
  "relatedLessons": [
    "fluent-email",
    "sendgrid"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
