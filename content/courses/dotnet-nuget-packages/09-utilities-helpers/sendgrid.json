{
  "id": "lesson-nuget-903",
  "slug": "sendgrid",
  "moduleSlug": "utilities-helpers",
  "courseSlug": "dotnet-nuget-packages",
  "title": "SendGrid",
  "description": "Send transactional and marketing emails via the SendGrid cloud service.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "email",
    "sendgrid",
    "transactional",
    "cloud"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is SendGrid?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`SendGrid` is a cloud-based email delivery service. The `SendGrid` NuGet package provides a simple API to send emails through SendGrid's infrastructure, handling deliverability, bounces, and analytics."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install the SendGrid package.",
        "language": "bash",
        "code": "dotnet add package SendGrid"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Sending an Email"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a `SendGridMessage`, set the sender, recipients, subject, and body, then send it with the client."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Send Email via SendGrid",
        "content": "Send a transactional email using SendGrid.",
        "language": "csharp",
        "code": "using SendGrid;\nusing SendGrid.Helpers.Mail;\n\npublic async Task SendWelcomeEmailAsync(string toEmail, string name)\n{\n    var apiKey = Environment.GetEnvironmentVariable(\"SENDGRID_API_KEY\");\n    var client = new SendGridClient(apiKey);\n\n    var from = new EmailAddress(\"no-reply@myapp.com\", \"MyApp\");\n    var to = new EmailAddress(toEmail, name);\n    var msg = MailHelper.CreateSingleEmail(\n        from,\n        to,\n        \"Welcome to MyApp\",\n        \"Thanks for joining, \" + name + \"!\",\n        \"<h1>Welcome, \" + name + \"!</h1>\"\n    );\n\n    var response = await client.SendEmailAsync(msg);\n    Console.WriteLine(response.StatusCode);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Attachments and Templates"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "SendGrid supports attachments and dynamic transactional templates for rich, personalized emails."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dynamic Template",
        "content": "Send using a SendGrid dynamic template.",
        "language": "csharp",
        "code": "var msg = new SendGridMessage();\nmsg.SetFrom(new EmailAddress(\"no-reply@myapp.com\", \"MyApp\"));\nmsg.AddTo(new EmailAddress(toEmail));\nmsg.SetTemplateId(\"d-1234567890abcdef\");\n\nmsg.SetTemplateData(new\n{\n    Name = name,\n    ConfirmationLink = \"https://myapp.com/confirm?token=abc123\"\n});\n\nawait client.SendEmailAsync(msg);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "SendGridClient",
            "definition": "The main client for interacting with the SendGrid API."
          },
          {
            "term": "MailHelper",
            "definition": "Helper class for creating standard single emails."
          },
          {
            "term": "Dynamic Template",
            "definition": "A template managed in SendGrid that uses Handlebars for personalization."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "Store your SendGrid API key in an environment variable or secret manager. Never commit it to source control."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "SendGrid delivers email via a cloud API",
          "Use SendGridClient and MailHelper for simple emails",
          "Dynamic templates support Handlebars personalization",
          "API keys should be stored securely"
        ],
        "takeaway": "SendGrid is ideal for transactional emails when you want to offload deliverability to a cloud service."
      }
    }
  ],
  "relatedLessons": [
    "fluent-email",
    "mailkit"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
