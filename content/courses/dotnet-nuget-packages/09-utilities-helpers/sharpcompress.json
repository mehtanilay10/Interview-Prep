{
  "id": "lesson-nuget-906",
  "slug": "sharpcompress",
  "moduleSlug": "utilities-helpers",
  "courseSlug": "dotnet-nuget-packages",
  "title": "SharpCompress",
  "description": "Stream and compress archive files in .NET with support for ZIP, RAR, TAR, GZIP, and 7z.",
  "order": 6,
  "difficulty": "intermediate",
  "estimatedMinutes": 14,
  "tags": [
    "utilities",
    "sharpcompress",
    "compression",
    "zip"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is SharpCompress?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`SharpCompress` is a streaming archive library that reads and writes `ZIP`, `RAR`, `TAR`, `GZIP`, `BZIP2`, `XZ`, and `7z` files. It is fully managed and well-suited for both small scripts and large-scale file processing."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install SharpCompress.",
        "language": "bash",
        "code": "dotnet add package SharpCompress"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Reading an Archive"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `ArchiveFactory.Open` to detect the archive type and iterate entries."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "List Archive Contents",
        "content": "Read and list entries in a ZIP file.",
        "language": "csharp",
        "code": "using SharpCompress.Archives;\n\npublic void ListZipContents(string zipPath)\n{\n    using var archive = ArchiveFactory.Open(zipPath);\n    foreach (var entry in archive.Entries.Where(e => !e.IsDirectory))\n    {\n        Console.WriteLine($\"{entry.Key} - {entry.Size} bytes\");\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Writing an Archive"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `ArchiveFactory.Create` to build a new archive from files."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create a ZIP Archive",
        "content": "Compress files into a ZIP archive.",
        "language": "csharp",
        "code": "using SharpCompress.Writers;\nusing SharpCompress.Writers.Zip;\n\npublic void CreateZip(string outputPath, IEnumerable<string> files)\n{\n    using var stream = File.Create(outputPath);\n    using var writer = WriterFactory.Open(stream, ArchiveType.Zip, CompressionType.Deflate);\n\n    foreach (var file in files)\n    {\n        writer.Write(Path.GetFileName(file), File.OpenRead(file));\n    }\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "ArchiveFactory",
            "definition": "Opens and creates archives while auto-detecting the format."
          },
          {
            "term": "IArchiveEntry",
            "definition": "Represents a single entry inside an archive."
          },
          {
            "term": "CompressionType",
            "definition": "Specifies the compression algorithm, such as Deflate or BZip2."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Tip",
        "text": "SharpCompress streams entries, so it uses constant memory regardless of archive size. This makes it safe for server applications."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "SharpCompress supports ZIP, RAR, TAR, GZIP, 7z, and more",
          "ArchiveFactory auto-detects archive types",
          "Streaming API keeps memory usage low",
          "Fully managed with no native dependencies"
        ],
        "takeaway": "SharpCompress is the standard choice for archive operations in .NET."
      }
    }
  ],
  "relatedLessons": [
    "sharpcompress"
  ],
  "courseSlug": "dotnet-nuget-packages"
}
