{
  "id": "lesson-nuget-018",
  "slug": "rabbitmq-client",
  "moduleSlug": "messaging-events",
  "courseSlug": "dotnet-nuget-packages",
  "title": "RabbitMQ.Client",
  "description": "The official .NET client library for RabbitMQ message broker.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 12,
  "tags": [
    "rabbitmq",
    "amqp",
    "messaging",
    "message-broker"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is RabbitMQ.Client?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "RabbitMQ.Client is the official .NET client for RabbitMQ. It provides full access to the AMQP 0-9-1 protocol, enabling fine-grained control over exchanges, queues, and message routing."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install RabbitMQ.Client from NuGet.",
        "language": "bash",
        "code": "dotnet add package RabbitMQ.Client"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Sending and Receiving",
        "content": "Publish and consume messages with RabbitMQ.",
        "language": "csharp",
        "code": "var factory = new ConnectionFactory()\n{\n    HostName = \"localhost\",\n    UserName = \"guest\",\n    Password = \"guest\"\n};\n\nusing var connection = factory.CreateConnection();\nusing var channel = connection.CreateModel();\n\n// Declare a queue\nchannel.QueueDeclare(queue: \"hello\", durable: false, exclusive: false, autoDelete: false, arguments: null);\n\n// Send\nvar body = Encoding.UTF8.GetBytes(\"Hello World!\");\nchannel.BasicPublish(exchange: \"\", routingKey: \"hello\", basicProperties: null, body: body);\n\n// Receive\nvar consumer = new EventingBasicConsumer(channel);\nconsumer.Received += (model, ea) =>\n{\n    var message = Encoding.UTF8.GetString(ea.Body.ToArray());\n    Console.WriteLine($\"Received: {message}\");\n};\nchannel.BasicConsume(queue: \"hello\", autoAck: true, consumer: consumer);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Exchange",
            "definition": "A routing mechanism that receives messages from producers and routes them to queues."
          },
          {
            "term": "Queue",
            "definition": "A buffer that stores messages until they are consumed."
          },
          {
            "term": "Binding",
            "definition": "A relationship between an exchange and a queue."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Warning",
        "text": "Always handle connection and channel exceptions. RabbitMQ connections can be lost due to network issues."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "RabbitMQ.Client provides low-level AMQP access",
          "Supports all RabbitMQ features",
          "Requires manual connection management"
        ],
        "takeaway": "Use RabbitMQ.Client when you need full control over AMQP messaging."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise",
        "description": "Publish and consume a message with RabbitMQ.",
        "steps": [
          "Start RabbitMQ via Docker",
          "Install RabbitMQ.Client",
          "Create a connection and channel",
          "Publish a message",
          "Consume from a queue"
        ],
        "expectedOutcome": "A message successfully sent and received via RabbitMQ."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}