{
  "id": "lesson-nuget-022",
  "slug": "quartz",
  "moduleSlug": "messaging-events",
  "courseSlug": "dotnet-nuget-packages",
  "title": "Quartz",
  "description": "A powerful, open-source job scheduling library for .NET.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 12,
  "tags": [
    "quartz",
    "scheduling",
    "jobs",
    "cron"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Quartz?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Quartz.NET is a powerful, open-source job scheduling library for .NET. It supports complex scheduling requirements including cron-like expressions, calendars, and job persistence."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Installation",
        "content": "Install Quartz from NuGet.",
        "language": "bash",
        "code": "dotnet add package Quartz"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Scheduled Job",
        "content": "Define and schedule a job with Quartz.",
        "language": "csharp",
        "code": "public class HelloJob : IJob\n{\n    public async Task Execute(IJobExecutionContext context)\n    {\n        Console.WriteLine(\"Hello from Quartz!\");\n    }\n}\n\n// Scheduling\nvar scheduler = await StdSchedulerFactory.GetDefaultScheduler();\nawait scheduler.Start();\n\nvar job = JobBuilder.Create<HelloJob>()\n    .WithIdentity(\"myJob\", \"group1\")\n    .Build();\n\nvar trigger = TriggerBuilder.Create()\n    .WithIdentity(\"myTrigger\", \"group1\")\n    .WithCronSchedule(\"0 0/5 * 1/1 * ? *\") // every 5 minutes\n    .Build();\n\nawait scheduler.ScheduleJob(job, trigger);"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "IJob",
            "definition": "The interface that all Quartz jobs must implement."
          },
          {
            "term": "Trigger",
            "definition": "Defines when a job should execute (e.g., cron, interval)."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Integration",
        "text": "Quartz integrates well with ASP.NET Core and supports persistent job stores (e.g., SQL Server)."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Quartz supports complex scheduling",
          "Jobs persist across application restarts",
          "Supports cron-like expressions"
        ],
        "takeaway": "Quartz is ideal for complex scheduling requirements in .NET."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise",
        "description": "Create a Quartz job that runs every minute.",
        "steps": [
          "Install Quartz",
          "Implement IJob",
          "Configure a trigger",
          "Start the scheduler",
          "Observe job execution"
        ],
        "expectedOutcome": "A job executing on a schedule using Quartz."
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}