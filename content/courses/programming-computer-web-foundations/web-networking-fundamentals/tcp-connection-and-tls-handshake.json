{
  "id": "lesson-programming-computer-web-foundations-web-networking-fundamentals-tcp-connection-and-tls-handshake",
  "slug": "tcp-connection-and-tls-handshake",
  "moduleSlug": "web-networking-fundamentals",
  "courseSlug": "programming-computer-web-foundations",
  "title": "TCP connection and TLS handshake",
  "description": "Learn how TCP establishes connections, how the TLS handshake secures them, and why both stages affect page load speed and security.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["tcp", "tls", "networking", "security"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Once DNS gives your browser an IP address, the next step is opening a connection. On the public internet, that almost always means **TCP**, and almost always means **TLS** on top of it. Together, these two handshakes can add hundreds of milliseconds before a single byte of application data is sent."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "TCP: the reliable delivery layer"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "TCP guarantees that bytes arrive in order and without corruption. It does this using sequence numbers, acknowledgments, and retransmissions. For web traffic, TCP is the default transport protocol."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "TCP three-way handshake",
        "items": [
          "Client sends SYN (synchronize).",
          "Server replies with SYN-ACK (synchronize-acknowledge).",
          "Client sends ACK (acknowledge).",
          "Connection is open; application data can flow."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Latency note",
        "text": "A full TCP handshake requires one round-trip before data can start. On high-latency networks, that cost is noticeable."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "TLS: securing the channel"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "TLS (Transport Layer Security) encrypts traffic between client and server. Without TLS, anyone on the network path can read or modify requests and responses. Modern browsers mark non-HTTPS sites as 'Not secure'."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "TLS 1.2 is still common but considered aging",
          "TLS 1.3 is faster and more secure; most production servers should use it",
          "Certificates bind a public key to a domain name",
          "Certificate Authorities (CAs) issue and verify certificates"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "TLS 1.3 handshake"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "TLS 1.3 reduces the handshake to a single round-trip for new connections and zero round-trips for resumed connections. It also removes older, insecure cipher suites and features."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "TLS 1.3 abbreviated handshake",
        "items": [
          "Client sends ClientHello with supported cipher suites.",
          "Server replies with ServerHello, certificate, and Finished.",
          "Client verifies and sends its Finished.",
          "Encrypted application data begins."
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "tls-handshake",
        "caption": "TCP plus TLS handshake combined timeline",
        "mermaid": "sequenceDiagram\n    participant C as Browser\n    participant S as Server\n    C->>S: TCP SYN\n    S->>C: SYN-ACK\n    C->>S: ACK\n    C->>S: TLS ClientHello\n    S->>C: TLS ServerHello + Certificate\n    C->>S: TLS Finished\n    S->>C: TLS Finished\n    Note over C,S: Encrypted HTTP request begins"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "TLS pitfalls",
        "text": "Expired certificates, mismatched domain names, or incomplete certificate chains will cause browsers to block the connection. Always validate your certificate chain with tools like SSL Labs."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why connection reuse matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Browsers reuse connections through HTTP keep-alive. Opening a new TCP+TLS connection for every request is expensive; persistent connections let multiple requests share the same handshake cost."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "TCP guarantees ordered, reliable delivery via a three-way handshake.",
          "TLS 1.3 reduces the handshake to one round-trip for new connections.",
          "Certificate issues are a common cause of hard-to-debug connection failures.",
          "Connection reuse through keep-alive is essential for performance."
        ],
        "takeaway": "TCP and TLS are the foundation of every secure web request. Understanding their handshakes explains much of the latency and security behavior you observe in production."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently asked questions",
        "items": [
          {
            "question": "Why does HTTPS feel slower than HTTP sometimes?",
            "answer": "TLS adds handshake latency and encryption overhead. With HTTP/2 or HTTP/3 and TLS 1.3, the difference is usually small. Often the real cause is server performance, not encryption."
          },
          {
            "question": "What is session resumption in TLS?",
            "answer": "TLS can reuse earlier cryptographic parameters to skip part or all of the handshake. Session tickets and session IDs let clients reconnect faster without a full handshake."
          },
          {
            "question": "Can I use TCP without TLS?",
            "answer": "Yes, but browsers enforce HTTPS for most production features. HTTP over plain TCP is still used internally or in controlled environments."
          }
        ]
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "quote",
      "data": {
        "text": "TLS 1.3 is not just more secure; it is measurably faster because it removes obsolete negotiation steps.",
        "attribution": "IETF TLS Working Group"
      }
    }
  ],
  "relatedLessons": ["dns-resolution-from-browser-to-server", "http-request-and-response-lifecycle", "http11-vs-http2-vs-http3"],
  "furtherReading": [
    {
      "type": "article",
      "title": "TLS 1.3 specification",
      "url": "https://datatracker.ietf.org/doc/html/rfc8446",
      "description": "The official RFC describing TLS 1.3."
    },
    {
      "type": "article",
      "title": "SSL Labs Server Test",
      "url": "https://www.ssllabs.com/ssltest/",
      "description": "Test your server's TLS configuration for security and compatibility."
    }
  ]
}
