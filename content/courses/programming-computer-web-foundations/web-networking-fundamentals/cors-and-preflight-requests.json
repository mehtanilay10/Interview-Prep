{
  "id": "lesson-programming-computer-web-foundations-web-networking-fundamentals-cors-and-preflight-requests",
  "slug": "cors-and-preflight-requests",
  "moduleSlug": "web-networking-fundamentals",
  "courseSlug": "programming-computer-web-foundations",
  "title": "CORS and preflight requests",
  "description": "Understand Cross-Origin Resource Sharing, why browsers enforce it, and how preflight requests affect API design.",
  "order": 7,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": ["cors", "security", "api", "web"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Browsers restrict web pages from making requests to a different origin than the page itself. This is called the **same-origin policy**. CORS (Cross-Origin Resource Sharing) is the controlled escape hatch. When misconfigured, CORS causes errors that look like network failures but are actually browser security checks."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Same-origin policy"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Two URLs have the same origin only if their protocol, host, and port are identical. A page at `https://app.example.com` cannot freely read responses from `https://api.example.com` or `https://example.com:3000`."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Protocol: HTTPS and HTTP are different origins",
          "Host: subdomains are different origins",
          "Port: default ports are implied, but explicit ports create new origins"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Simple vs preflight requests"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **simple request** uses GET, HEAD, or POST with only safe headers. It bypasses preflight. A **preflight request** is an automatic OPTIONS request the browser sends first to check whether the actual request is safe to send."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "Preflight triggers",
        "items": [
          "Methods other than GET, HEAD, or POST",
          "POST with Content-Type other than application/x-www-form-urlencoded, multipart/form-data, or text/plain",
          "Custom headers such as Authorization or X-Custom-Header"
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "cors-flow",
        "caption": "CORS preflight sequence for a custom API call",
        "mermaid": "sequenceDiagram\n    participant B as Browser\n    participant A as API Server\n    B->>A: OPTIONS /api/data (preflight)\n    A->>B: 204 + Access-Control-Allow-Origin\n    B->>A: GET /api/data (actual request)\n    A->>B: 200 + JSON body"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Preflight is not a bug",
        "text": "Preflight adds latency, but it protects users. If an API is meant to be called from browsers, configure CORS intentionally instead of trying to eliminate preflight."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Essential CORS headers"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Header", "Purpose"],
        "rows": [
          ["Access-Control-Allow-Origin", "Which origins may read the response"],
          ["Access-Control-Allow-Methods", "HTTP methods allowed in actual requests"],
          ["Access-Control-Allow-Headers", "Headers the browser may send"],
          ["Access-Control-Allow-Credentials", "Whether cookies or Authorization headers are permitted"],
          ["Access-Control-Max-Age", "How long the preflight result can be cached"]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Wildcard and credentials conflict",
        "text": "You cannot use Access-Control-Allow-Origin: * together with Access-Control-Allow-Credentials: true. The server must echo back the exact requesting origin."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Returning a wildcard origin while also accepting credentials",
          "Forgetting to handle OPTIONS routes in server frameworks",
          "Using CORS to authenticate requests instead of proper tokens",
          "Opening CORS to all origins in production APIs without rate limiting"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "CORS is a browser security mechanism, not a server authentication system.",
          "Preflight requests happen automatically for non-simple requests.",
          "CORS headers must be configured on the server, not in frontend code.",
          "Misconfigured CORS is a common cause of cryptic API failures in development."
        ],
        "takeaway": "Treat CORS as a deliberate API contract. Explicit allowed origins and methods are safer and easier to debug than broad wildcards."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently asked questions",
        "items": [
          {
            "question": "Does CORS protect the server?",
            "answer": "Not directly. CORS protects the user's browser from reading data from another origin. The server still receives the request and must authenticate and authorize it independently."
          },
          {
            "question": "Why does the Network tab show the response but JavaScript cannot read it?",
            "answer": "The browser blocked the response because the CORS headers were missing or did not match. The request still reached the server; the browser simply prevented the script from accessing the body."
          },
          {
            "question": "Can CORS prevent CSRF?",
            "answer": "No. CORS is about reading cross-origin responses, not about preventing unwanted state-changing requests. Use SameSite cookies, CSRF tokens, or custom headers for CSRF protection."
          }
        ]
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "quote",
      "data": {
        "text": "CORS is the browser enforcing a contract. If the contract is unclear, debugging feels like arguing with the browser instead of fixing the server.",
        "attribution": "Frontend security practice"
      }
    }
  ],
  "relatedLessons": ["http-methods-status-codes-and-headers", "http-request-and-response-lifecycle", "websockets-vs-server-sent-events"],
  "furtherReading": [
    {
      "type": "article",
      "title": "CORS specification",
      "url": "https://fetch.spec.whatwg.org/#http-cors-protocol",
      "description": "The authoritative specification for CORS behavior."
    },
    {
      "type": "article",
      "title": "MDN CORS guide",
      "url": "https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS",
      "description": "A practical guide to understanding and fixing CORS errors."
    }
  ]
}
