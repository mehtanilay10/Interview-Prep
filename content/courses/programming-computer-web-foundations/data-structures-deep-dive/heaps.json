{
  "id": "lesson-programming-computer-web-foundations-data-structures-deep-dive-heaps",
  "slug": "heaps",
  "moduleSlug": "data-structures-deep-dive",
  "courseSlug": "programming-computer-web-foundations",
  "title": "Heaps",
  "description": "Understand binary heaps, heap operations, and how they power priority queues and efficient sorting.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["data-structures", "heaps", "priority-queues"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "A heap is a specialized tree that satisfies the heap property: in a max-heap, every parent is greater than or equal to its children; in a min-heap, every parent is less than or equal to its children. Because the extreme value always sits at the root, heaps are ideal for priority queues and for building efficient sorting algorithms."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Binary Heap Structure"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A binary heap is usually stored in an array. For a node at index i, its left child sits at 2i + 1 and its right child at 2i + 2. This compact layout avoids pointer overhead and keeps the heap cache-friendly."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Array representation of a max-heap",
        "content": "The max-heap [90, 70, 60, 50, 40] stored as an array satisfies the heap property because every parent is larger than its children.",
        "code": "// Index:    0    1   2   3   4\n// Values: [90, 70, 60, 50, 40]\n\n// parent(1) = index 0 -> 90\n// left(0)   = index 1 -> 70\n// right(0)  = index 2 -> 60\n// Both children are smaller than the parent.",
        "language": "javascript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Core Operations"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Heap operations and their costs",
        "items": [
          "insert: add at the end, then bubble up to restore the heap property — O(log n).",
          "extract max or min: swap root with the last element, remove it, then bubble down — O(log n).",
          "peek: look at the root — O(1).",
          "heapify: build a heap from an unordered array in O(n)."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Min-heap operations in JavaScript",
        "content": "A small class showing insertion and extraction.",
        "code": "class MinHeap {\n  constructor() {\n    this.heap = [];\n  }\n\n  insert(value) {\n    this.heap.push(value);\n    this.bubbleUp(this.heap.length - 1);\n  }\n\n  extractMin() {\n    const min = this.heap[0];\n    const end = this.heap.pop();\n    if (this.heap.length) {\n      this.heap[0] = end;\n      this.sinkDown(0);\n    }\n    return min;\n  }\n\n  bubbleUp(index) {\n    while (index > 0) {\n      const parent = Math.floor((index - 1) / 2);\n      if (this.heap[parent] <= this.heap[index]) break;\n      [this.heap[parent], this.heap[index]] = [this.heap[index], this.heap[parent]];\n      index = parent;\n    }\n  }\n\n  sinkDown(index) {\n    const length = this.heap.length;\n    while (true) {\n      let swap = index;\n      const left = 2 * index + 1;\n      const right = 2 * index + 2;\n      if (left < length && this.heap[left] < this.heap[swap]) swap = left;\n      if (right < length && this.heap[right] < this.heap[swap]) swap = right;\n      if (swap === index) break;\n      [this.heap[swap], this.heap[index]] = [this.heap[index], this.heap[swap]];\n      index = swap;\n    }\n  }\n}",
        "language": "javascript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "When to Use a Heap"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Scenario", "Why a heap"],
        "rows": [
          ["Priority queue", "Always returns the highest-priority item in O(log n)"],
          ["Kth largest element", "Maintain only k items and peek at the top"],
          ["Heapsort", "In-place sorting with O(n log n) worst-case guarantee"],
          ["Median maintenance", "Two heaps can track a running median efficiently"]
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Heapsort trade-off",
        "text": "Heapsort guarantees O(n log n) time and O(1) extra space, but it is not stable. If you need a stable sort, use merge sort or Timsort instead."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Heaps are complete binary trees stored in arrays.",
          "Insert and extract run in O(log n).",
          "Heaps power priority queues and efficient selection algorithms.",
          "Heapsort is fast and in-place but not stable."
        ],
        "takeaway": "Heaps are the right choice whenever you need quick access to the most important item in a changing collection."
      }
    }
  ],
  "relatedLessons": ["stacks-and-queues", "choosing-the-right-structure", "interview-problems"],
  "furtherReading": [
    {
      "title": "Heap Data Structure",
      "url": "https://www.programiz.com/dsa/heap-data-structure",
      "type": "article",
      "author": "Programiz",
      "description": "Step-by-step explanation of heap operations and heap sort."
    },
    {
      "title": "Binary Heap Visualization",
      "url": "https://www.cs.usfca.edu/~galles/visualization/Heap.html",
      "type": "tool",
      "description": "Interactive visualization of heap insert and extract operations."
    }
  ]
}
