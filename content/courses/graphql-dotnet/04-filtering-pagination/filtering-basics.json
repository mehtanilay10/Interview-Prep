{
  "id": "lesson-gql-401",
  "slug": "filtering-basics",
  "moduleSlug": "04-filtering-pagination",
  "title": "Filtering Basics",
  "description": "Add filtering capabilities to your GraphQL API.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "graphql",
    "filtering"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Filtering lets clients narrow results to exactly what they need. Hot Chocolate provides built-in filtering with the `[UseFiltering]` attribute."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Enabling Filtering"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Apply `[UseFiltering]` to a query method. Hot Chocolate automatically generates filter input types based on your return type."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Enable Filtering",
        "content": "Let clients filter books by any field.",
        "language": "csharp",
        "code": "public class Query\n{\n    [Query]\n    [UseFiltering]\n    public IQueryable<Book> GetBooks([Service] AppDbContext db)\n    {\n        return db.Books;\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generated Filter Input"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Hot Chocolate generates a `BookFilterInput` type automatically. Clients can use equality, comparison, and list operators."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Client Query",
        "content": "Filter books by price and author name.",
        "language": "graphql",
        "code": "query {\n  getBooks(filter: { price: { ge: 20 }, author: { contains: \"Smith\" } }) {\n    id\n    title\n    price\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Custom Filter Input"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For full control, define a custom filter input type with `[FilterInputType]`."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Custom Filter",
        "content": "Define a custom filter with specific fields.",
        "language": "csharp",
        "code": "[FilterInputType]\npublic class BookFilterInput\n{\n    public string Title { get; set; }\n    public decimal? MinPrice { get; set; }\n    public decimal? MaxPrice { get; set; }\n    public Genre? Genre { get; set; }\n}\n\npublic class Query\n{\n    [Query]\n    public IQueryable<Book> GetBooks(\n        BookFilterInput filter,\n        [Service] AppDbContext db)\n    {\n        var query = db.Books.AsQueryable();\n\n        if (!string.IsNullOrEmpty(filter.Title))\n            query = query.Where(b => b.Title.Contains(filter.Title));\n        if (filter.MinPrice.HasValue)\n            query = query.Where(b => b.Price >= filter.MinPrice.Value);\n\n        return query;\n    }\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "[UseFiltering]",
            "definition": "Attribute that enables automatic filter input generation on a query field."
          },
          {
            "term": "Filter Input",
            "definition": "An input type that defines available filter fields and operators."
          },
          {
            "term": "Comparison Operator",
            "definition": "Operators like eq, ne, gt, lt, ge, le for numeric and date comparisons."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Add Custom Filtering",
        "description": "Create a BookFilterInput with Title, MinPrice, MaxPrice, and Genre fields, then wire it into the GetBooks query.",
        "steps": [
          "Create BookFilterInput class with [FilterInputType]",
          "Add Title, MinPrice, MaxPrice, Genre properties",
          "Update GetBooks to accept BookFilterInput",
          "Apply Where clauses for each filter"
        ],
        "expectedOutcome": "Clients can filter books by title, price range, and genre in a single query."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "[UseFiltering] auto-generates filter inputs",
          "Custom filters give you full control",
          "Filtering works best with IQueryable",
          "Clients can combine multiple filters"
        ],
        "takeaway": "Next, we'll add pagination to handle large datasets."
      }
    }
  ],
  "relatedLessons": [
    "pagination-basics",
    "complex-filters"
  ],
  "courseSlug": "graphql-dotnet"
}
