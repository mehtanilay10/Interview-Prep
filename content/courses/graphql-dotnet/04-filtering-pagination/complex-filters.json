{
  "id": "lesson-gql-403",
  "slug": "complex-filters",
  "moduleSlug": "04-filtering-pagination",
  "title": "Complex Filters",
  "description": "Build multi-field and nested filters.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "graphql",
    "filtering"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Real-world APIs often need advanced filtering: date ranges, text search, nested object filters, and OR conditions. Hot Chocolate's filtering system supports all of these."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Date Range Filtering"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Filter by date ranges using comparison operators. This is common for events, orders, or published content."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Date Range Filter",
        "content": "Filter events by date range.",
        "language": "csharp",
        "code": "[FilterInputType]\npublic class EventFilterInput\n{\n    public string Name { get; set; }\n    public DateTime? StartDate { get; set; }\n    public DateTime? EndDate { get; set; }\n    public string Location { get; set; }\n}\n\npublic class Query\n{\n    [Query]\n    public IQueryable<Event> GetEvents(\n        EventFilterInput filter,\n        [Service] AppDbContext db)\n    {\n        var query = db.Events.AsQueryable();\n\n        if (!string.IsNullOrEmpty(filter.Name))\n            query = query.Where(e => e.Name.Contains(filter.Name));\n        if (filter.StartDate.HasValue)\n            query = query.Where(e => e.Date >= filter.StartDate.Value);\n        if (filter.EndDate.HasValue)\n            query = query.Where(e => e.Date <= filter.EndDate.Value);\n\n        return query;\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Nested Filters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Filter on related types by nesting filter inputs. This works well with `[UseFiltering]` on related types."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nested Filtering",
        "content": "Filter books and their authors.",
        "language": "graphql",
        "code": "query {\n  getBooks(filter: {\n    author: { name: { contains: \"Smith\" } }\n  }) {\n    title\n    author {\n      name\n    }\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "OR Conditions with Any"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `Any` for OR conditions inside lists. This lets clients search across multiple fields at once."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Any Operator",
        "content": "Search books where title or author contains a term.",
        "language": "graphql",
        "code": "query {\n  getBooks(filter: {\n    _or: [\n      { title: { contains: \"GraphQL\" } }\n      { author: { contains: \"GraphQL\" } }\n    ]\n  }) {\n    title\n    author\n  }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Performance",
        "text": "Complex filters on large datasets can generate inefficient SQL. Always test generated queries and add database indexes on filtered columns."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Filter Input",
            "definition": "An input type that defines available filter fields and operators."
          },
          {
            "term": "Nested Filter",
            "definition": "A filter that applies to a related type through a relationship."
          },
          {
            "term": "_or Operator",
            "definition": "A special operator that combines multiple filter conditions with OR logic."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build an Advanced Event Filter",
        "description": "Create an EventFilterInput that filters by name (contains), date range (start and end), and location (exact match). Wire it into the GetEvents query.",
        "steps": [
          "Define EventFilterInput with Name, StartDate, EndDate, Location",
          "Update GetEvents to accept the filter",
          "Apply each filter condition",
          "Order results by Date descending"
        ],
        "expectedOutcome": "Clients can run complex filtered queries like getEvents(filter: {startDate: \"2024-01-01\", location: \"NYC\"})."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Date ranges use comparison operators",
          "Nested filters traverse relationships",
          "_or combines conditions with OR logic",
          "Always profile generated SQL for complex filters"
        ],
        "takeaway": "Next, we'll optimize pagination for large datasets."
      }
    }
  ],
  "relatedLessons": [
    "filtering-basics",
    "performance-pagination"
  ],
  "courseSlug": "graphql-dotnet"
}
