{
  "id": "lesson-gql-404",
  "slug": "performance-pagination",
  "moduleSlug": "04-filtering-pagination",
  "title": "Performance with Pagination",
  "description": "Optimize pagination for large datasets.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "graphql",
    "pagination"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Pagination is not just about splitting results — it's about doing so efficiently. Without proper indexes and query shaping, pagination can become a performance bottleneck."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Indexing Strategies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Database indexes are the most important factor in pagination performance. Ensure your ORDER BY columns are indexed."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "SQL Index",
        "content": "Create an index for cursor pagination on Id.",
        "language": "sql",
        "code": "CREATE INDEX IX_Books_Id\nON Books (Id);\n\n-- For filtered + paginated queries\nCREATE INDEX IX_Books_Genre_Id\nON Books (Genre, Id);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Keyset Pagination"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Keyset pagination (a form of cursor pagination) uses the last seen values instead of OFFSET. It's much faster for deep pages."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Keyset Query",
        "content": "Use WHERE with the last seen values.",
        "language": "csharp",
        "code": "[Query]\n[UsePaging]\npublic IQueryable<Book> GetBooks(\n    int? lastId,\n    [Service] AppDbContext db)\n{\n    var query = db.Books.AsQueryable();\n\n    if (lastId.HasValue)\n        query = query.Where(b => b.Id > lastId.Value);\n\n    return query.OrderBy(b => b.Id);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Avoiding N+1 with Pagination"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When paginating related data, use `[UseProjection]` or `[UseSelection]` to let Hot Chocolate optimize the query."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Projection with Pagination",
        "content": "Combine projection and paging.",
        "language": "csharp",
        "code": "[Query]\n[UseProjection]\n[UsePaging]\npublic IQueryable<Book> GetBooks([Service] AppDbContext db)\n{\n    return db.Books.Include(b => b.Author);\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Watch Out For",
        "text": "OFFSET becomes slower as the page number increases because the database must still scan and discard rows. Keyset pagination avoids this by using indexed WHERE clauses."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Keyset Pagination",
            "definition": "Pagination based on the values of the last row seen, not on a page number."
          },
          {
            "term": "Database Index",
            "definition": "A data structure that improves query speed on database columns."
          },
          {
            "term": "N+1 Problem",
            "definition": "When fetching a list and then fetching each item's relations one by one, causing N+1 queries."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Index ORDER BY columns for fast pagination",
          "Keyset pagination avoids OFFSET overhead",
          "Use projection to reduce data transfer",
          "Profile queries with real data volumes"
        ],
        "takeaway": "Next, we'll secure our API with authentication and authorization."
      }
    }
  ],
  "relatedLessons": [
    "pagination-basics",
    "complex-filters"
  ],
  "courseSlug": "graphql-dotnet"
}
