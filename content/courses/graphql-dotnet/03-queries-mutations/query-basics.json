{
  "id": "lesson-gql-301",
  "slug": "query-basics",
  "moduleSlug": "03-queries-mutations",
  "title": "Query Basics",
  "description": "Define and execute queries in GraphQL.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "graphql",
    "queries"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Queries are how clients read data from your GraphQL API. In Hot Chocolate, any method on a class registered with `AddQueryType` becomes a query field."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining a Query"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a `Query` class and decorate methods with `[Query]`. The method name becomes the field name unless you override it."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Query Class",
        "content": "Expose a list of books and a single book lookup.",
        "language": "csharp",
        "code": "public class Query\n{\n    private readonly IBookRepository _repository;\n\n    public Query(IBookRepository repository)\n    {\n        _repository = repository;\n    }\n\n    [Query]\n    public IEnumerable<Book> GetBooks() => _repository.GetAll();\n\n    [Query(Name = \"book\")]\n    public Book? GetBookById(int id) => _repository.GetById(id);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Arguments and Filtering"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Query methods can accept arguments. The parameter name becomes the argument name in the schema."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Query with Arguments",
        "content": "Filter books by genre.",
        "language": "csharp",
        "code": "[Query]\npublic IEnumerable<Book> GetBooksByGenre(Genre genre)\n{\n    return _repository.GetAll().Where(b => b.Genre == genre);\n}\n\n// Client query:\n// query {\n//   getBooksByGenre(genre: SCI_FI) {\n//     title\n//     author\n//   }\n// }"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Dependency Injection",
        "text": "Hot Chocolate resolves constructor dependencies from the ASP.NET Core DI container. Inject repositories instead of using static data."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Async Queries"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Always use async methods when querying a database. Hot Chocolate supports `Task<T>` and `IAsyncEnumerable<T>` return types."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Async Query",
        "content": "Return Task for async database calls.",
        "language": "csharp",
        "code": "[Query]\npublic async Task<IEnumerable<Book>> GetBooksAsync(\n    CancellationToken ct)\n{\n    return await _repository.GetAllAsync(ct);\n}"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Query Field",
            "definition": "An entry point on the Query type that clients can call."
          },
          {
            "term": "Argument",
            "definition": "A value passed to a query field to filter or modify the result."
          },
          {
            "term": "Async Resolver",
            "definition": "A query method that returns Task<T> for non-blocking I/O."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Search Query",
        "description": "Create a search query that accepts a string and returns matching books by title or author (case-insensitive).",
        "steps": [
          "Create a Search method with a string query parameter",
          "Use _repository.GetAll() to get all books",
          "Filter where Title or Author contains the query",
          "Return the filtered results"
        ],
        "expectedOutcome": "Running search(query: \"graphql\") returns only books with \"graphql\" in the title or author name."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Methods on Query classes become query fields",
          "Parameters become arguments in the schema",
          "Always use async for database access",
          "DI keeps your code testable"
        ],
        "takeaway": "Next, we'll add mutations to modify data."
      }
    }
  ],
  "relatedLessons": [
    "mutation-basics",
    "resolvers"
  ],
  "courseSlug": "graphql-dotnet"
}
