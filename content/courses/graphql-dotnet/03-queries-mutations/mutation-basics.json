{
  "id": "lesson-gql-302",
  "slug": "mutation-basics",
  "moduleSlug": "03-queries-mutations",
  "title": "Mutation Basics",
  "description": "Define mutations to modify data.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "graphql",
    "mutations"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Mutations are how clients modify data — create, update, or delete. In Hot Chocolate, mutation methods are defined on a `Mutation` class."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Defining a Mutation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a `Mutation` class and decorate methods with `[Mutation]`. The return type becomes the GraphQL return type."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Mutation Class",
        "content": "Add a new book to the catalog.",
        "language": "csharp",
        "code": "public class Mutation\n{\n    private readonly IBookRepository _repository;\n\n    public Mutation(IBookRepository repository)\n    {\n        _repository = repository;\n    }\n\n    [Mutation]\n    public async Task<Book> AddBookAsync(CreateBookInput input)\n    {\n        var book = new Book\n        {\n            Title = input.Title,\n            Author = input.Author,\n            Price = input.Price,\n            Genre = input.Genre\n        };\n\n        await _repository.AddAsync(book);\n        return book;\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Input Types for Mutations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use input types to pass complex data. Hot Chocolate validates input types automatically before the resolver runs."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Input Type",
        "content": "Define the input for creating a book.",
        "language": "csharp",
        "code": "[InputType]\npublic class CreateBookInput\n{\n    public string Title { get; set; }\n    public string Author { get; set; }\n    public decimal Price { get; set; }\n    public Genre Genre { get; set; }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Update and Delete Mutations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Update mutations typically accept an ID and an input type. Delete mutations return a boolean or the deleted object."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Update and Delete",
        "content": "Modify and remove books.",
        "language": "csharp",
        "code": "[Mutation]\npublic async Task<Book> UpdateBookAsync(int id, UpdateBookInput input)\n{\n    var book = await _repository.GetByIdAsync(id);\n    if (book == null) throw new NotFoundException(\"Book not found.\");\n\n    book.Title = input.Title;\n    book.Price = input.Price;\n    await _repository.UpdateAsync(book);\n    return book;\n}\n\n[Mutation]\npublic async Task<bool> DeleteBookAsync(int id)\n{\n    var book = await _repository.GetByIdAsync(id);\n    if (book == null) return false;\n\n    await _repository.DeleteAsync(book);\n    return true;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Naming Conventions",
        "text": "GraphQL mutations traditionally start with a verb (addBook, not just book). Hot Chocolate preserves your method name by default."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Mutation",
            "definition": "A GraphQL operation that modifies data on the server."
          },
          {
            "term": "Input Type",
            "definition": "A type used to pass structured arguments into mutations."
          },
          {
            "term": "Payload",
            "definition": "The data returned by a mutation, often including the modified object."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Add a Delete Mutation",
        "description": "Add a deleteBook mutation that accepts an ID and returns a boolean indicating success.",
        "steps": [
          "Create a DeleteBook method with [Mutation]",
          "Accept int id as parameter",
          "Look up the book and return false if not found",
          "Delete and return true if found"
        ],
        "expectedOutcome": "Running deleteBook(id: 1) returns true and removes the book from the repository."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Mutations modify server-side data",
          "Use input types for complex arguments",
          "Return the modified object for client convenience",
          "Throw exceptions to signal errors"
        ],
        "takeaway": "Next, we'll add real-time subscriptions."
      }
    }
  ],
  "relatedLessons": [
    "query-basics",
    "subscriptions"
  ],
  "courseSlug": "graphql-dotnet"
}
