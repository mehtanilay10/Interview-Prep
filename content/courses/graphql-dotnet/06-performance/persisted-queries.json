{
  "id": "lesson-gql-604",
  "slug": "persisted-queries",
  "moduleSlug": "06-performance",
  "title": "Persisted Queries",
  "description": "Use persisted queries to improve performance and security.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "graphql",
    "persisted-queries"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Persisted queries (also called automatic persisted queries or APQ) let clients send a hash of the query instead of the full query text. The server looks up the query by hash, saving bandwidth and enabling query whitelisting."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How Persisted Queries Work"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The client sends a SHA-256 hash of the query. If the server has seen it, it executes from cache. If not, the client sends the full query, and the server stores it."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-pq",
        "caption": "Persisted query flow.",
        "definition": "sequenceDiagram\n    participant Client\n    participant Server\n    Client->>Server: Send query hash\n    alt Hash exists\n        Server-->>Client: Execute cached query\n    else Hash missing\n        Client->>Server: Send full query\n        Server->>Server: Store by hash\n        Server-->>Client: Execute and return result\n    end"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Enabling Persisted Queries"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Hot Chocolate supports persisted queries through the `AddDefaultQueryStorage` and client-side libraries."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Server Setup",
        "content": "Enable persisted queries in Program.cs.",
        "language": "csharp",
        "code": "builder.Services\n    .AddGraphQLServer()\n    .AddQueryType<Query>()\n    .AddDefaultQueryStorage();\n\n// In production, replace with a persistent store\nbuilder.Services.AddSingleton<IQueryStorage, InMemoryQueryStorage>();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Client Side"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use a GraphQL client that supports APQ, like Apollo Client or Relay. The client computes the hash and sends it automatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Apollo Client",
        "content": "Configure APQ in Apollo Client.",
        "language": "javascript",
        "code": "import { ApolloClient, InMemoryCache, HttpLink, ApolloLink } from '@apollo/client';\n\nconst apqLink = new ApolloLink((operation) => {\n  const queryHash = createHash('sha256')\n    .update(operation.query)\n    .digest('hex');\n\n  operation.setContext({\n    headers: {\n      'x-graphql-query-hash': queryHash\n    }\n  });\n\n  return toPromise(httpLink.request(operation));\n});\n\nconst client = new ApolloClient({\n  link: apqLink,\n  cache: new InMemoryCache()\n});"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Security Benefit",
        "text": "Persisted queries also act as a whitelist. Only queries that exist on the server can be executed, preventing malicious or accidental expensive queries."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Persisted Query",
            "definition": "A query stored on the server and referenced by its hash."
          },
          {
            "term": "APQ",
            "definition": "Automatic Persisted Queries — the protocol where clients send hashes and the server stores unknown queries."
          },
          {
            "term": "Query Whitelist",
            "definition": "A list of allowed queries, enforced by persisted queries."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Persisted queries reduce bandwidth",
          "Hashes let the server cache and lookup queries",
          "APQ auto-stores new queries on first use",
          "Persisted queries act as a security whitelist"
        ],
        "takeaway": "Next, we'll handle errors gracefully."
      }
    }
  ],
  "relatedLessons": [
    "dataloader",
    "caching"
  ],
  "courseSlug": "graphql-dotnet"
}
