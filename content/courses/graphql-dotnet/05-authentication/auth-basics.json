{
  "id": "lesson-gql-501",
  "slug": "auth-basics",
  "moduleSlug": "05-authentication",
  "title": "Authentication Basics",
  "description": "Add authentication to your GraphQL API.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "graphql",
    "authentication"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Authentication verifies who the client is. In GraphQL, you typically authenticate once at the transport layer (HTTP or WebSocket) and reuse that identity throughout the request."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Authentication at the Transport Layer"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Hot Chocolate integrates with ASP.NET Core authentication. Configure JWT, cookies, or OAuth in Program.cs, and Hot Chocolate picks up the authenticated user automatically."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Configure JWT Auth",
        "content": "Add JWT authentication to Program.cs.",
        "language": "csharp",
        "code": "builder.Services\n    .AddAuthentication(JwtBearerDefaults.AuthenticationScheme)\n    .AddJwtBearer(options =>\n    {\n        options.TokenValidationParameters = new TokenValidationParameters\n        {\n            ValidateIssuer = true,\n            ValidateAudience = true,\n            ValidateLifetime = true,\n            ValidateIssuerSigningKey = true,\n            ValidIssuer = builder.Configuration[\"Jwt:Issuer\"],\n            ValidAudience = builder.Configuration[\"Jwt:Audience\"],\n            IssuerSigningKey = new SymmetricSecurityKey(\n                Encoding.UTF8.GetBytes(builder.Configuration[\"Jwt:Key\"]))\n        };\n    });\n\nbuilder.Services\n    .AddGraphQLServer()\n    .AddAuthorization();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accessing the User"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Inject `IHttpContextAccessor` or use the `[Service]` attribute to get the current `ClaimsPrincipal` inside resolvers."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Get Current User",
        "content": "Access the authenticated user in a resolver.",
        "language": "csharp",
        "code": "public class Query\n{\n    [Query]\n    public string GetCurrentUser(\n        [Service] IHttpContextAccessor httpContext)\n    {\n        var user = httpContext.HttpContext.User;\n        return user.Identity?.Name ?? \"Anonymous\";\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Stateless by Design",
        "text": "GraphQL is typically stateless. Authenticate with a token (JWT) on each request rather than relying on server-side sessions."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Authentication",
            "definition": "The process of verifying who a user is."
          },
          {
            "term": "JWT",
            "definition": "JSON Web Token — a compact, URL-safe token that carries claims about the user."
          },
          {
            "term": "ClaimsPrincipal",
            "definition": "The .NET representation of the authenticated user and their claims."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Configure auth in ASP.NET Core, not GraphQL",
          "Hot Chocolate integrates with AddAuthorization()",
          "Inject IHttpContextAccessor to get the current user",
          "JWT is the standard token format for GraphQL APIs"
        ],
        "takeaway": "Next, we'll control access with authorization rules."
      }
    }
  ],
  "relatedLessons": [
    "authorization",
    "jwt-integration"
  ],
  "courseSlug": "graphql-dotnet"
}
