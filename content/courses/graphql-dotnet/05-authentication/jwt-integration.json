{
  "id": "lesson-gql-503",
  "slug": "jwt-integration",
  "moduleSlug": "05-authentication",
  "title": "JWT Integration",
  "description": "Use JWT tokens for authentication in GraphQL.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "graphql",
    "jwt"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "JSON Web Tokens (JWT) are the most common way to authenticate GraphQL clients. The client sends a bearer token in the Authorization header, and Hot Chocolate validates it via ASP.NET Core."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generating JWT Tokens"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Create a mutation or endpoint that generates a JWT after validating the user's credentials."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generate JWT",
        "content": "Create a login mutation that returns a token.",
        "language": "csharp",
        "code": "public class Mutation\n{\n    private readonly IUserService _userService;\n    private readonly IConfiguration _config;\n\n    public Mutation(IUserService userService, IConfiguration config)\n    {\n        _userService = userService;\n        _config = config;\n    }\n\n    [Mutation]\n    public async Task<string> Login(string email, string password)\n    {\n        var user = await _userService.ValidateAsync(email, password);\n        if (user == null) throw new GraphQLException(\"Invalid credentials.\");\n\n        var token = new JwtSecurityToken(\n            issuer: _config[\"Jwt:Issuer\"],\n            audience: _config[\"Jwt:Audience\"],\n            claims: new[] { new Claim(ClaimTypes.Name, user.Email) },\n            expires: DateTime.UtcNow.AddHours(2),\n            signingCredentials: new SigningCredentials(\n                new SymmetricSecurityKey(\n                    Encoding.UTF8.GetBytes(_config[\"Jwt:Key\"])),\n                SecurityAlgorithms.HmacSha256));\n\n        return new JwtSecurityTokenHandler().WriteToken(token);\n    }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Client Usage"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Clients store the token (usually in localStorage) and send it in the Authorization header on every request."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Client Request",
        "content": "Send JWT in the Authorization header.",
        "language": "graphql",
        "code": "query GetCurrentUser {\n  currentUser {\n    name\n    email\n  }\n}\n\n// HTTP Headers\n// Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Token Security",
        "text": "Always use HTTPS. Store JWTs in httpOnly cookies for web apps to prevent XSS. Set reasonable expiration times and use refresh tokens for long-lived sessions."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "JWT",
            "definition": "JSON Web Token — a compact, signed token that carries identity claims."
          },
          {
            "term": "Bearer Token",
            "definition": "A token presented in the Authorization header as \"Bearer <token>\"."
          },
          {
            "term": "Refresh Token",
            "definition": "A long-lived token used to obtain new access tokens without re-authenticating."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Generate JWTs after credential validation",
          "Store JWT settings in configuration",
          "Clients send tokens in Authorization header",
          "Use refresh tokens for better UX"
        ],
        "takeaway": "Next, we'll integrate with ASP.NET Core Identity."
      }
    }
  ],
  "relatedLessons": [
    "auth-basics",
    "identity-integration"
  ],
  "courseSlug": "graphql-dotnet"
}
