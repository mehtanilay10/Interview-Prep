{
  "id": "lesson-gql-504",
  "slug": "identity-integration",
  "moduleSlug": "05-authentication",
  "title": "ASP.NET Core Identity",
  "description": "Integrate GraphQL with ASP.NET Core Identity.",
  "order": 4,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "graphql",
    "identity"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "ASP.NET Core Identity provides a complete user management system. Integrating it with Hot Chocolate gives you registration, login, password reset, and role management out of the box."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Setting Up Identity"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Register Identity services and configure the user store (EF Core, InMemory, etc.)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Identity Setup",
        "content": "Configure Identity in Program.cs.",
        "language": "csharp",
        "code": "builder.Services\n    .AddDbContext<AppDbContext>(options =>\n        options.UseSqlServer(builder.Configuration.GetConnectionString(\"Default\")))\n    .AddIdentity<ApplicationUser, IdentityRole>(options =>\n    {\n        options.Password.RequireDigit = true;\n        options.Password.RequiredLength = 8;\n    })\n    .AddEntityFrameworkStores<AppDbContext>()\n    .AddDefaultTokenProviders();\n\nbuilder.Services\n    .AddGraphQLServer()\n    .AddAuthorization()\n    .AddQueryType<Query>()\n    .AddMutationType<Mutation>();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Registration Mutation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use `UserManager<TUser>` to create users from a GraphQL mutation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Register User",
        "content": "Create a user account mutation.",
        "language": "csharp",
        "code": "public class Mutation\n{\n    private readonly UserManager<ApplicationUser> _userManager;\n\n    public Mutation(UserManager<ApplicationUser> userManager)\n    {\n        _userManager = userManager;\n    }\n\n    [Mutation]\n    public async Task<string> Register(\n        string email,\n        string password,\n        string confirmPassword)\n    {\n        if (password != confirmPassword)\n            throw new GraphQLException(\"Passwords do not match.\");\n\n        var user = new ApplicationUser { UserName = email, Email = email };\n        var result = await _userManager.CreateAsync(user, password);\n\n        if (!result.Succeeded)\n            throw new GraphQLException(result.Errors.Select(e => e.Description));\n\n        return \"Registration successful.\";\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Best Practice",
        "text": "Return the user object (minus password) after registration so the client can navigate to the profile immediately."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "ASP.NET Core Identity",
            "definition": "A membership system for managing users, roles, and claims."
          },
          {
            "term": "UserManager",
            "definition": "The Identity service for creating, finding, and managing users."
          },
          {
            "term": "SignInManager",
            "definition": "The Identity service for signing users in and managing cookies/tokens."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Identity provides full user management",
          "UserManager handles registration and validation",
          "Combine Identity with JWT for stateless auth",
          "Role and claim-based auth work seamlessly"
        ],
        "takeaway": "Next, we'll optimize performance with DataLoader and caching."
      }
    }
  ],
  "relatedLessons": [
    "auth-basics",
    "authorization"
  ],
  "courseSlug": "graphql-dotnet"
}
