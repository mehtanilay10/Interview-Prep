{
  "id": "lesson-aspnet-core-web-api-production-api-design-reliability-etags-and-conditional-requests",
  "slug": "etags-and-conditional-requests",
  "moduleSlug": "production-api-design-reliability",
  "courseSlug": "aspnet-core-web-api",
  "title": "ETags and conditional requests",
  "description": "Reduce bandwidth and avoid lost updates with ETags. Learn conditional GET and PUT using ETags, Last-Modified, and the ASP.NET Core ETag infrastructure.",
  "order": 5,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Every time a client downloads a resource, it may already have the latest copy. ETags (entity tags) are lightweight identifiers for a specific version of a resource. The client sends the ETag back in subsequent requests, and the server replies with 304 Not Modified when nothing has changed. This saves bandwidth, reduces database load, and keeps mobile apps responsive."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Beyond caching, ETags also enable safe conditional writes. A client that includes `If-Match` on a PUT ensures it only overwrites the version it originally fetched, preventing the lost-update problem."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How ETags work"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "An ETag is an opaque string generated by the server that changes whenever the resource changes. The most common strategy is a hash of the response body, but a rowversion, updated timestamp, or incremental counter also works. Weak ETags (`W/\"abc\"`) signal that the representation could change byte-for-byte while the semantic entity is unchanged. Strong ETags (`\"abc\"`) guarantee byte-level identity."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Conditional GET with ETag"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "On the first GET, the server returns the body and an `ETag` header. The client caches both. On the next GET, the client sends `If-None-Match: \"abc\"`. If the resource is unchanged, the server returns 304 with no body. If it changed, the server returns 200 with the new body and a new ETag."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ETag filter for GET responses",
        "code": "public class ETagFilter : IResultFilter\n{\n    private readonly IETagGenerator _generator;\n\n    public ETagFilter(IETagGenerator generator) => _generator = generator;\n\n    public override void OnResultExecuting(ResultExecutingContext context)\n    {\n        if (context.Result is not ObjectResult result ||\n            context.HttpContext.Request.Method is not (\"GET\" or \"HEAD\"))\n        {\n            return;\n        }\n\n        var etag = _generator.Generate(result.Value);\n        context.HttpContext.Response.Headers.ETag = etag;\n\n        if (context.HttpContext.Request.Headers.TryGetValue(\"If-None-Match\", out var inm) &&\n            inm == etag)\n        {\n            context.Result = new StatusCodeResult(StatusCodes.Status304NotModified);\n        }\n    }\n}",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Conditional PUT with If-Match"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Conditional writes prevent overwriting newer data. When the client fetched the resource, it received an ETag. It includes `If-Match: \"abc\"` on PUT. The server compares the header with the current ETag. If they match, the update succeeds and the server issues a new ETag. If they differ, the server returns 412 Precondition Failed so the client can reconcile."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Conditional update endpoint",
        "code": "[HttpPut(\"{id:guid}\")]\npublic async Task<IActionResult> Update(Guid id, [FromBody] UpdateOrderDto dto)\n{\n    var order = await _db.Orders.FindAsync(id);\n    if (order is null) return NotFound();\n\n    var currentEtag = GenerateEtag(order);\n    var clientEtag = Request.Headers.IfMatch.FirstOrDefault()?.Trim('\"');\n\n    if (!string.Equals(clientEtag, currentEtag.Trim('\"'), StringComparison.Ordinal))\n        return StatusCode(StatusCodes.Status412PreconditionFailed);\n\n    order.ShipToName = dto.ShipToName;\n    order.ShippedAt = DateTime.UtcNow;\n    await _db.SaveChangesAsync();\n\n    var newEtag = GenerateEtag(order);\n    Response.Headers.ETag = newEtag;\n    return NoContent();\n}",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generating stable ETags"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Use a database `rowversion` column or a hash of the persisted representation. Avoid hashing in-memory objects before persistence because transient fields or serialization differences can change the hash even when the underlying data did not. A rowversion is cheap, unique per row, and guaranteed to change on every update."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Performance note",
        "text": "ETag generation should be fast. Cache the ETag value with the entity in the data layer so you do not recompute it on every request."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Summary"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "items": [
          "ETags let clients skip downloads when content has not changed, returning 304 instead of 200.",
          "Use `If-None-Match` for conditional GET and `If-Match` for conditional writes.",
          "Generate ETags from stable data like rowversion or persisted hashes, not transient object state.",
          "412 Precondition Failed is the correct response when a conditional write target has changed."
        ]
      }
    }
  ],
  "relatedLessons": [
    "optimistic-concurrency-for-apis",
    "pagination-filtering-and-sorting-design"
  ],
  "furtherReading": [
    {
      "title": "RFC 7232: HTTP Conditional Requests",
      "url": "https://tools.ietf.org/html/rfc7232",
      "type": "article"
    },
    {
      "title": "RFC 7233: HTTP Range Requests",
      "url": "https://tools.ietf.org/html/rfc7233",
      "type": "article"
    },
    {
      "title": "Microsoft Docs: Response caching in ASP.NET Core",
      "url": "https://learn.microsoft.com/aspnet/core/performance/caching/response",
      "type": "article"
    }
  ]
}
