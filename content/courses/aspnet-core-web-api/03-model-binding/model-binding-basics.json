{
  "id": "lesson-webapi-008",
  "slug": "model-binding-basics",
  "moduleSlug": "model-binding",
  "title": "Model Binding Basics",
  "description": "Understand how model binding works in ASP.NET Core.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "web-api",
    "model-binding",
    "binding",
    "parameters"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Model binding** automatically maps request data to action method parameters. It can bind from the route, query string, request body, and headers."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When an HTTP request hits your API, ASP.NET Core inspects the route data, query string, headers, and body. It then matches those values to your action parameters by name and type, converting strings to integers, decimals, GUIDs, and complex objects automatically."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Model Binding",
            "definition": "The process that converts incoming request data into strongly typed .NET objects that are passed to controller actions."
          },
          {
            "term": "Binding Source",
            "definition": "The origin of data used for binding, such as route, query string, request body, or headers."
          },
          {
            "term": "Action Parameter",
            "definition": "A parameter defined on a controller action method that receives data from model binding."
          },
          {
            "term": "Value Provider",
            "definition": "Services that fetch data from specific locations. For example, RouteValueProvider, QueryStringValueProvider, and FormValueProvider."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-binding-flow",
        "caption": "How an ASP.NET Core request reaches an action parameter through model binding.",
        "definition": "flowchart LR\n    A[HTTP Request] --> B{Model Binding}\n    B -->|Route data| C[FromRoute]\n    B -->|Query string| D[FromQuery]\n    B -->|JSON body| E[FromBody]\n    B -->|Headers| F[FromHeader]\n    C --> G[Action Parameter]\n    D --> G\n    E --> G\n    F --> G"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Binding Sources"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Attribute",
          "Source",
          "Typical Use"
        ],
        "rows": [
          [
            "[FromRoute]",
            "URL route parameters",
            "Identifying a resource by ID"
          ],
          [
            "[FromQuery]",
            "Query string parameters",
            "Filtering, sorting, pagination"
          ],
          [
            "[FromBody]",
            "Request body (JSON)",
            "Creating or updating resources"
          ],
          [
            "[FromHeader]",
            "HTTP headers",
            "Correlation IDs, API keys"
          ],
          [
            "[FromForm]",
            "Form data",
            "HTML form posts"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Complex vs Simple Types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "ASP.NET Core applies different default rules based on parameter type. Simple types like int, string, and bool default to [FromQuery]. Complex types such as classes and records default to [FromBody]. You can override either default by adding an explicit attribute."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multi-Source Binding",
        "content": "Combining route, query, and header parameters in a single action.",
        "language": "csharp",
        "code": "[ApiController]\n[Route(\"api/[controller]\")]\npublic class ProductsController : ControllerBase\n{\n    [HttpGet(\"{id}\")]\n    public IActionResult Get(\n        [FromRoute] int id,\n        [FromQuery] string search,\n        [FromHeader(Name = \"X-Correlation-Id\")] string correlationId)\n    {\n        var product = _repository.GetById(id);\n\n        if (!string.IsNullOrEmpty(search))\n        {\n            product = product\n                .Where(p => p.Name.Contains(search))\n                .ToList();\n        }\n\n        Response.Headers[\"X-Correlation-Id\"] = correlationId;\n        return Ok(product);\n    }\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Default Binding Behavior",
        "cards": [
          {
            "title": "Simple Types",
            "description": "int, string, bool, Guid, DateTime, and enums.",
            "pros": [
              "Default to [FromQuery]",
              "No extra attribute needed",
              "Works with default values"
            ],
            "cons": [
              "Cannot bind complex logic",
              "Limited to primitive values"
            ],
            "tags": [
              "default"
            ]
          },
          {
            "title": "Complex Types",
            "description": "Classes, records, and structs with multiple properties.",
            "pros": [
              "Default to [FromBody]",
              "Nested binding supported",
              "Works with JSON deserializers"
            ],
            "cons": [
              "Only one complex type per action unless attributed",
              "Requires readable JSON body"
            ]
          }
        ]
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Model Binding Checklist",
        "items": [
          {
            "text": "Use [FromRoute] for resource identifiers in the URL path"
          },
          {
            "text": "Use [FromQuery] for optional filters, page numbers, and search terms"
          },
          {
            "text": "Use [FromBody] for create and update payloads"
          },
          {
            "text": "Avoid binding more than one complex parameter from the body per action"
          },
          {
            "text": "Rely on [ApiController] automatic 400 responses when ModelState is invalid"
          },
          {
            "text": "Test with missing and malformed values to confirm expected behavior"
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Practice Multi-Source Binding",
        "description": "Create a controller action that binds a route ID, a query search term, and a custom header simultaneously.",
        "steps": [
          "Add a new GET endpoint at api/categories/{id}.",
          "Bind id from route using [FromRoute].",
          "Bind a query string parameter named includeInactive using [FromQuery] with a default of false.",
          "Bind a header named X-Tenant-Id using [FromHeader].",
          "Return a 200 OK with the filtered result, or 404 if the category is missing."
        ],
        "expectedOutcome": "A working endpoint that accepts data from three different binding sources and returns the correct response."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Model binding maps request data to action parameters automatically",
          "Attributes like [FromRoute], [FromQuery], and [FromBody] control the source",
          "Simple types default to query string; complex types default to body",
          "[ApiController] short-circuits invalid ModelState with a 400 response"
        ],
        "takeaway": "Model binding removes manual parsing and keeps your actions clean."
      }
    }
  ],
  "courseSlug": "aspnet-core-web-api"
}
