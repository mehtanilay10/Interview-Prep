{
  "id": "lesson-webapi-013",
  "slug": "custom-middleware",
  "moduleSlug": "middleware",
  "title": "Custom Middleware",
  "description": "Learn how to create your own middleware.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 20,
  "tags": [
    "web-api",
    "middleware",
    "custom",
    "logging"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "You can create custom middleware for application-specific logic like logging, timing, or request modification."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating Middleware"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Custom Middleware",
        "content": "Creating request timing middleware with proper imports",
        "language": "csharp",
        "code": "using System.Diagnostics;\n\npublic class RequestTimingMiddleware\n{\n    private readonly RequestDelegate _next;\n    private readonly ILogger<RequestTimingMiddleware> _logger;\n\n    public RequestTimingMiddleware(RequestDelegate next, ILogger<RequestTimingMiddleware> logger)\n    {\n        _next = next;\n        _logger = logger;\n    }\n\n    public async Task InvokeAsync(HttpContext context)\n    {\n        var sw = Stopwatch.StartNew();\n        await _next(context);\n        sw.Stop();\n        \n        _logger.LogInformation(\"{Method} {Path} took {Elapsed}ms\",\n            context.Request.Method,\n            context.Request.Path,\n            sw.ElapsedMilliseconds);\n    }\n}\n\n// Extension method\npublic static class MiddlewareExtensions\n{\n    public static IApplicationBuilder UseRequestTiming(this IApplicationBuilder builder)\n    {\n        return builder.UseMiddleware<RequestTimingMiddleware>();\n    }\n}\n\n// Usage in Program.cs\napp.UseRequestTiming();"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Extension Methods",
        "text": "Create extension methods for cleaner registration: app.UseRequestTiming() instead of app.UseMiddleware<T>()."
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "RequestDelegate",
            "definition": "A delegate that represents the next middleware in the pipeline."
          },
          {
            "term": "InvokeAsync",
            "definition": "The method custom middleware must implement to process requests asynchronously."
          },
          {
            "term": "UseMiddleware",
            "definition": "The extension method that registers custom middleware in the pipeline."
          },
          {
            "term": "Factory-based activation",
            "definition": "A pattern where middleware dependencies are resolved from DI instead of constructor parameters."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "custom-middleware-insertion",
        "caption": "Inserting custom middleware into the pipeline",
        "definition": "flowchart LR\n    A[Request] --> B[Custom Timing Middleware]\n    B --> C{Next available?}\n    C -->|Yes| D[Downstream Middleware]\n    C -->|No| E[Endpoint]\n    D --> F[Response]\n    E --> F\n    F --> B\n    B --> G[Response]\n    style B fill:#4ade80\n    style C fill:#facc15"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": ["Pattern", "Registration", "DI Support", "Use Case"],
        "rows": [
          ["Inline", "app.Use(async (ctx, next) => { ... })", "No", "Quick inline logic"],
          ["Class-based", "app.UseMiddleware<MyMiddleware>()", "Yes", "Reusable middleware"],
          ["Factory-based", "app.UseMiddleware<FactoryMiddleware>()", "Yes", "Per-request dependencies"],
          ["Convention", "app.UseRequestTiming()", "Yes", "Clean API surface"]
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Custom Middleware Patterns",
        "cards": [
          {
            "title": "Inline Delegate",
            "description": "Anonymous middleware added directly in Program.cs.",
            "pros": ["Fast to write", "No extra files", "Good for simple logic"],
            "cons": ["Harder to test", "Limited DI", "Can clutter Program.cs"]
          },
          {
            "title": "Class-based Middleware",
            "description": "Separate class implementing InvokeAsync or Invoke.",
            "pros": ["Testable", "Full DI support", "Reusable across projects"],
            "cons": ["More boilerplate", "Requires separate files"]
          },
          {
            "title": "Factory-based Middleware",
            "description": "Middleware resolved from DI with per-request or scoped services.",
            "pros": ["Access to scoped services", "Clean separation", "Best for complex logic"],
            "cons": ["Most verbose", "Requires IMiddleware interface"]
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Add a Logging Middleware",
        "description": "Create a custom middleware that logs the HTTP method and path for every incoming request.",
        "steps": [
          "Create a LoggingMiddleware class with a constructor accepting RequestDelegate and ILogger",
          "Implement InvokeAsync to log the request method and path before calling _next",
          "Create an extension method UseRequestLogging",
          "Register the middleware in Program.cs before UseAuthorization"
        ],
        "expectedOutcome": "Every request logs a message like 'GET /api/values received' before reaching the controller."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Custom Middleware Checklist",
        "items": [
          { "text": "I can create a class-based middleware with InvokeAsync", "hint": "Accept RequestDelegate and dependencies in constructor" },
          { "text": "I can register middleware using an extension method", "hint": "Create a static class with UseX extension method" },
          { "text": "I know when to use factory-based activation", "hint": "When you need scoped services in the middleware" },
          { "text": "I can add inline middleware for simple logic", "hint": "Use app.Use with a lambda accepting HttpContext and RequestDelegate" }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Create class with InvokeAsync method",
          "Call _next to continue pipeline",
          "Extension methods for clean registration",
          "Add to pipeline in Program.cs"
        ],
        "takeaway": "Custom middleware handles application-specific logic."
      }
    }
  ],
  "courseSlug": "aspnet-core-web-api"
}
