{
  "id": "lesson-webapi-007",
  "slug": "route-constraints",
  "moduleSlug": "controllers-routing",
  "title": "Route Constraints",
  "description": "Learn how to constrain route parameters.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 18,
  "tags": [
    "web-api",
    "constraints",
    "routing",
    "validation"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Route constraints** restrict how parameters are matched in routes. They ensure the correct type and format, preventing invalid URLs from reaching your actions."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Constraints"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Constraints are added inline to route parameters using a colon, for example `{id:int}`. They can check type, length, range, or match a regular expression."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Constraints",
        "content": "Using route constraints",
        "language": "csharp",
        "code": "// Type constraints\n[HttpGet(\"{id:int}\")] // Only matches integers\npublic IActionResult GetById(int id) { }\n\n[HttpGet(\"{name:alpha}\")] // Only matches letters\npublic IActionResult GetByName(string name) { }\n\n// Range constraint\n[HttpGet(\"{id:int:min(1)}\")] // Minimum value 1\npublic IActionResult GetById(int id) { }\n\n// Regex constraint\n[HttpGet(\"{id:regex(^\\\\d{3}$)}\")] // Exactly 3 digits\npublic IActionResult GetByCode(string id) { }\n\n// Optional parameter\n[HttpGet(\"{id?}\")] // Optional\npublic IActionResult Get(int? id) { }"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Constraint",
          "Matches",
          "Example"
        ],
        "rows": [
          [
            "int",
            "Integer values",
            "{id:int}"
          ],
          [
            "alpha",
            "Letters only",
            "{name:alpha}"
          ],
          [
            "bool",
            "true or false",
            "{flag:bool}"
          ],
          [
            "min(1)",
            "Minimum value",
            "{id:int:min(1)}"
          ],
          [
            "max(100)",
            "Maximum value",
            "{id:int:max(100)}"
          ],
          [
            "regex(...)",
            "Regular expression",
            "{id:regex(^\\\\d{3}$)}"
          ]
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Route Constraint",
            "definition": "A rule added to a route parameter that restricts which values match the route."
          },
          {
            "term": "Type Constraint",
            "definition": "Limits a parameter to a specific type, such as int, bool, or datetime."
          },
          {
            "term": "Range Constraint",
            "definition": "Limits a numeric parameter to a minimum or maximum value."
          },
          {
            "term": "Regex Constraint",
            "definition": "Matches a parameter against a custom regular expression pattern."
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "404 for Invalid Routes",
        "text": "If a route constraint doesn't match, ASP.NET Core returns 404 automatically."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Add the Right Constraint",
        "description": "For each route parameter, choose the appropriate constraint so invalid values are rejected.",
        "steps": [
          "Identify the parameter's expected type (int, string, bool)",
          "Add a type constraint if the value must be numeric or alphabetic",
          "Add a min/max range constraint for numeric bounds",
          "Use a regex constraint for custom formats like postal codes"
        ],
        "expectedOutcome": "Routes that reject non-integer IDs and out-of-range values, returning 404 instead of reaching the action."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Route Constraints Checklist",
        "items": [
          {
            "text": "Use type constraints for int, bool, and datetime",
            "hint": "Prevents type-mismatch errors before they reach your action."
          },
          {
            "text": "Apply min/max range constraints to numeric parameters",
            "hint": "Rejects values outside valid business ranges."
          },
          {
            "text": "Use regex for custom formats",
            "hint": "Ideal for codes, slugs, and identifiers."
          },
          {
            "text": "Remember unmatched routes return 404",
            "hint": "Constraints are part of the routing pipeline, not model validation."
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Constraints restrict parameter matching",
          "int, alpha, bool for types",
          "min, max, range for values",
          "regex for custom patterns"
        ],
        "takeaway": "Route constraints validate URL parameters."
      }
    }
  ],
  "relatedLessons": [
    "routing-basics",
    "attribute-routing"
  ],
  "courseSlug": "aspnet-core-web-api"
}