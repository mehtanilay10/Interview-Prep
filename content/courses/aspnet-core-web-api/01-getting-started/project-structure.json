{
  "id": "lesson-webapi-003",
  "slug": "project-structure",
  "moduleSlug": "getting-started",
  "title": "Project Structure",
  "description": "Understand the structure of an ASP.NET Core Web API project.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 18,
  "tags": [
    "web-api",
    "structure",
    "program",
    "configuration"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Understanding the **project structure** of an ASP.NET Core Web API helps you navigate, debug, and extend your code with confidence. Modern .NET uses a **host model** with a single `Program.cs` entry point that wires everything together — a big improvement over the older `Startup.cs` pattern."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In this lesson you will tour the typical files generated by `dotnet new webapi --use-controllers`, learn how the **middleware pipeline** processes them, and see exactly what each line of `Program.cs` does."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Host",
            "definition": "The object that boots your application, configures services, and runs the web server."
          },
          {
            "term": "DI Container",
            "definition": "Dependency Injection container — holds the registered services your app can ask for via constructor parameters."
          },
          {
            "term": "Middleware",
            "definition": "A component in the request pipeline that can inspect or transform an HTTP request (or short-circuit it)."
          },
          {
            "term": "WebApplicationBuilder",
            "definition": "The fluent builder created in `Program.cs`. You use it to register services (`builder.Services`) and configure the app (`builder.Configuration`, `builder.Logging`)."
          },
          {
            "term": "WebApplication",
            "definition": "The built app. You use it to register middleware (`app.Use...`) and map endpoints (`app.MapControllers()`)."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Program.cs Walkthrough",
        "anchor": "program-cs"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The default `Program.cs` generated by the controller template looks like this. Each region has a clear purpose."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Program.cs (controller template)",
        "content": "Annotated entry point for a controller-based Web API.",
        "language": "csharp",
        "code": "var builder = WebApplication.CreateBuilder(args);\n\n// Register services with the DI container\nbuilder.Services.AddControllers();\nbuilder.Services.AddEndpointsApiExplorer();\nbuilder.Services.AddSwaggerGen();\n\nvar app = builder.Build();\n\n// Configure the HTTP request pipeline (middleware)\nif (app.Environment.IsDevelopment())\n{\n    app.UseSwagger();\n    app.UseSwaggerUI();\n}\n\napp.UseHttpsRedirection();\napp.UseAuthorization();\napp.MapControllers();\n\napp.Run();"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Line",
          "What it does",
          "Why it matters"
        ],
        "rows": [
          [
            "WebApplication.CreateBuilder(args)",
            "Creates the host and loads configuration from `appsettings.json`, env vars, and command-line args.",
            "Without this, nothing else works."
          ],
          [
            "AddControllers()",
            "Registers the MVC services needed to discover and run controller classes.",
            "Required for the controller template; replace with `AddEndpointsApiExplorer` only if you go Minimal."
          ],
          [
            "AddEndpointsApiExplorer()",
            "Adds metadata so endpoint-aware tools (Swagger, health checks) can introspect routes.",
            "Needed for Swagger and minimal endpoints."
          ],
          [
            "AddSwaggerGen()",
            "Registers the OpenAPI generator.",
            "Pairs with `UseSwagger()` / `UseSwaggerUI()` to expose the docs UI."
          ],
          [
            "app.UseHttpsRedirection()",
            "Adds middleware that 307-redirects HTTP requests to HTTPS.",
            "Skip in pure dev environments or behind a TLS-terminating proxy."
          ],
          [
            "app.UseAuthorization()",
            "Adds the authorization middleware to the pipeline.",
            "Required before `MapControllers()` if you use `[Authorize]` attributes."
          ],
          [
            "app.MapControllers()",
            "Maps controller actions to routes based on their attributes.",
            "Where the URL → action mapping happens."
          ],
          [
            "app.Run()",
            "Starts the web server and blocks until shutdown.",
            "Must be the last statement."
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Middleware Pipeline",
        "anchor": "middleware-pipeline"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every request flows through a **chain of middleware** in the order they were registered. Each component can do work *before* calling the next one, *after* it, or *instead of* it (short-circuit)."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-middleware-pipeline",
        "caption": "Order of middleware in a typical ASP.NET Core Web API pipeline.",
        "definition": "flowchart LR\n    A[Incoming request] --> B[HTTPS Redirection]\n    B --> C[Exception Handler]\n    C --> D[Routing]\n    D --> E[Authentication]\n    E --> F[Authorization]\n    F --> G[Controllers]\n    G --> H[Outgoing response]\n    H -.exception.-> C"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Order is everything",
        "text": "Middleware runs in the order it is added. `UseAuthorization()` must come **after** `UseAuthentication()`, and `UseExceptionHandler()` should be registered **first** so it can wrap everything downstream."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Project Layout at a Glance",
        "anchor": "project-layout"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "As your API grows, you will add more folders. Here is a sensible structure for a real-world controller-based API."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-folder-layout",
        "caption": "Recommended folder layout for a medium-sized Web API.",
        "definition": "graph TD\n    A[src/MyWebApi/] --> B[Controllers/]\n    A --> C[Models/]\n    A --> D[Services/]\n    A --> E[Data/]\n    A --> F[Dtos/]\n    A --> G[Middlewares/]\n    A --> H[Program.cs]\n    A --> I[appsettings.json]"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Folder",
          "Holds",
          "Rule of thumb"
        ],
        "rows": [
          [
            "Controllers/",
            "HTTP-facing classes decorated with `[ApiController]`.",
            "Thin — delegate work to services."
          ],
          [
            "Models/",
            "Plain C# classes that represent domain entities.",
            "Mirror your database tables, but never expose them directly."
          ],
          [
            "Dtos/",
            "Data Transfer Objects shaped for the wire (request/response contracts).",
            "One DTO per use case; never reuse an entity as both."
          ],
          [
            "Services/",
            "Business logic and orchestration.",
            "Easy to unit test without spinning up a host."
          ],
          [
            "Data/",
            "EF Core `DbContext`, migrations, configurations.",
            "Only this layer should know about the database."
          ],
          [
            "Middlewares/",
            "Custom middleware classes.",
            "Use sparingly — prefer filters or endpoint conventions when possible."
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Controller vs Minimal API",
        "anchor": "controller-vs-minimal"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Modern .NET 8+ supports two styles in the same project. Pick whichever fits the size of your team and the size of your API."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Controllers vs Minimal APIs",
        "cards": [
          {
            "title": "Controllers",
            "description": "Classes with attributes that handle HTTP requests. Default in the `--use-controllers` template.",
            "tags": [
              "MVC",
              "Attribute routing"
            ],
            "pros": [
              "Familiar structure for larger teams",
              "Filters, model binding, and validation all 'just work'",
              "Easy to organize dozens of endpoints"
            ],
            "cons": [
              "More files and folders",
              "Slight learning curve for attribute conventions"
            ]
          },
          {
            "title": "Minimal APIs",
            "description": "Endpoints defined inline in `Program.cs` with `app.MapGet(...)` and friends.",
            "tags": [
              "Lightweight",
              "Lambda-friendly"
            ],
            "pros": [
              "Single file for tiny services",
              "Great for prototypes and serverless",
              "Less ceremony per endpoint"
            ],
            "cons": [
              "Filters and validation need more wiring as the project grows",
              "`Program.cs` can become unwieldy"
            ]
          }
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "You can mix them",
        "text": "It is perfectly fine to register controllers with `AddControllers()` and *also* add minimal endpoints with `app.MapGet(...)`. The two styles live side by side inside one WebApplication."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "checklist",
      "data": {
        "title": "Can you point to these in your project?",
        "items": [
          {
            "text": "The `Program.cs` entry point.",
            "hint": "Top-level statements with `WebApplication.CreateBuilder`."
          },
          {
            "text": "Where `AddControllers()` is registered.",
            "hint": "Inside `builder.Services`."
          },
          {
            "text": "Where the middleware order is set.",
            "hint": "Between `var app = builder.Build()` and `app.Run()`."
          },
          {
            "text": "The `Controllers/` folder.",
            "hint": "Sibling of `Program.cs`."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Trace a request through your pipeline",
        "description": "Reading the code is not enough — describe the flow in your own words.",
        "steps": [
          "Open `Program.cs` and list every middleware registered between `var app = builder.Build()` and `app.Run()`.",
          "For each middleware, write one sentence describing what it does.",
          "Run the API and hit `/weatherforecast` to confirm the pipeline is wired correctly.",
          "Re-order `UseAuthorization()` to be **before** `UseHttpsRedirection()` and try to start the app — note the warning or behavior, then revert the order."
        ],
        "expectedOutcome": "You can list the middleware in the correct order and explain why HTTPS redirection must run early. You can articulate how a request flows from `app.Run()` all the way back to a controller action."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "`Program.cs` is the single entry point: build services, configure middleware, map endpoints, run.",
          "`WebApplicationBuilder` registers services; `WebApplication` builds the middleware pipeline and route table.",
          "Middleware order matters — register them in the correct sequence or authentication and authorization will silently fail.",
          "A clean folder layout (Controllers, Models, Dtos, Services, Data) keeps concerns separated and tests easy.",
          "Controllers and Minimal APIs can coexist — pick the style that matches the project's size and team familiarity."
        ],
        "takeaway": "Modern ASP.NET Core boots from one file. Master `Program.cs` and you master the entire request lifecycle."
      }
    }
  ],
  "courseSlug": "aspnet-core-web-api"
}
