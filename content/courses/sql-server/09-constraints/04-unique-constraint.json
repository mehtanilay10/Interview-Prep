{
  "id": "lesson-sql-084",
  "slug": "unique-constraint",
  "moduleSlug": "constraints",
  "title": "UNIQUE Constraint",
  "description": "Learn how to use the UNIQUE constraint to ensure uniqueness of data in a column or group of columns.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 10,
  "tags": [
    "unique",
    "uniqueness",
    "constraint"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "SQL Server UNIQUE Constraint"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The UNIQUE constraint ensures that the data in a column or group of columns is unique among all rows in a table. Unlike PRIMARY KEY, UNIQUE allows NULL values (but only one NULL per column)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UNIQUE as Column Constraint"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "UNIQUE Column Constraint",
        "content": "Ensures email values are unique across all rows.",
        "language": "sql",
        "code": "CREATE TABLE hr.persons(\n    person_id INT IDENTITY PRIMARY KEY,\n    first_name VARCHAR(255) NOT NULL,\n    last_name VARCHAR(255) NOT NULL,\n    email VARCHAR(255) UNIQUE\n);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UNIQUE as Table Constraint"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "UNIQUE Table Constraint",
        "content": "Defines UNIQUE as a separate table constraint.",
        "language": "sql",
        "code": "CREATE TABLE hr.persons(\n    person_id INT IDENTITY PRIMARY KEY,\n    first_name VARCHAR(255) NOT NULL,\n    last_name VARCHAR(255) NOT NULL,\n    email VARCHAR(255),\n    UNIQUE(email)\n);"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Behind the scenes, SQL Server automatically creates a UNIQUE index to enforce the uniqueness. If you attempt to insert a duplicate row, SQL Server rejects it."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Named UNIQUE Constraint"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Assigning a specific name makes error messages clearer and the constraint easier to manage:"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Named UNIQUE Constraint",
        "content": "Creates a table with a named UNIQUE constraint on email.",
        "language": "sql",
        "code": "CREATE TABLE hr.persons (\n    person_id INT IDENTITY PRIMARY KEY,\n    first_name VARCHAR(255) NOT NULL,\n    last_name VARCHAR(255) NOT NULL,\n    email VARCHAR(255),\n    CONSTRAINT unique_email UNIQUE(email)\n);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UNIQUE vs. PRIMARY KEY"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Both UNIQUE and PRIMARY KEY enforce uniqueness, but UNIQUE is preferred when the column is not the primary key. UNIQUE allows NULL (one NULL per column), while PRIMARY KEY does not."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UNIQUE for a Group of Columns"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Composite UNIQUE Constraint",
        "content": "Ensures the combination of person_id and skill_id is unique.",
        "language": "sql",
        "code": "CREATE TABLE hr.person_skills (\n    id INT IDENTITY PRIMARY KEY,\n    person_id int,\n    skill_id int,\n    updated_at DATETIME,\n    UNIQUE (person_id, skill_id)\n);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Adding and Removing UNIQUE Constraints"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Add UNIQUE Constraint",
        "content": "Adds a UNIQUE constraint to an existing column.",
        "language": "sql",
        "code": "ALTER TABLE hr.persons\nADD CONSTRAINT unique_email UNIQUE(email);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Drop UNIQUE Constraint",
        "content": "Removes the unique_email constraint.",
        "language": "sql",
        "code": "ALTER TABLE hr.persons\nDROP CONSTRAINT unique_email;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key Takeaways"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "UNIQUE Constraint Key Points",
        "points": [
          "UNIQUE ensures all values in a column or group of columns are unique.",
          "UNIQUE allows NULL values (one NULL per column).",
          "PRIMARY KEY implies UNIQUE but does not allow NULL.",
          "SQL Server creates a UNIQUE index automatically to enforce the constraint.",
          "Named constraints are easier to manage and reference.",
          "You can add or remove UNIQUE constraints using ALTER TABLE."
        ]
      }
    }
  ],
  "courseSlug": "sql-server"
}