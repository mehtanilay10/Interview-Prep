{
  "id": "lesson-sql-021",
  "slug": "cross-join",
  "moduleSlug": "joins",
  "title": "CROSS JOIN",
  "description": "Learn how to use the CROSS JOIN to combine every row from one table with every row from another table, creating a Cartesian product.",
  "order": 6,
  "difficulty": "beginner",
  "estimatedMinutes": 8,
  "tags": [
    "cross-join",
    "joins",
    "cartesian-product",
    "no-condition"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "SQL Server CROSS JOIN"
      }
    },
    {
      "type": "image",
      "data": {
        "src": "https://www.sqlservertutorial.net/wp-content/uploads/SQL-Server-CROSS-JOIN-report-example.png",
        "alt": "SQL Server Cross Join StockTaking example"
      }
    },
    {
      "type": "image",
      "data": {
        "src": "https://www.sqlservertutorial.net/wp-content/uploads/SQL-Server-CROSS-JOIN-stocktaking-example.png",
        "alt": "Image"
      }
    },
    {
      "type": "image",
      "data": {
        "src": "https://www.sqlservertutorial.net/wp-content/uploads/SQL-Server-CROSS-JOIN-example.png",
        "alt": "SQL Server CROSS JOIN example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A CROSS JOIN allows you to combine rows from the first table with every row of the second table. In other words, it returns the Cartesian product of two tables."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS JOIN Syntax"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "CROSS JOIN Syntax",
        "content": "Combine every row of T1 with every row of T2 without a join condition.",
        "language": "sql",
        "code": "SELECT\n    select_list\nFROM\n    T1\nCROSS JOIN T2;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In this syntax, T1 and T2 are the tables that you want to perform a cross join. Unlike other join types such as INNER JOIN or LEFT JOIN, the CROSS JOIN does not require a join condition."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "If T1 has n rows and T2 has m rows, the CROSS JOIN will create a result set with n x m rows. For example, if both tables have 1,000 rows, the CROSS JOIN will return 1,000,000 rows."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Performance Warning",
        "text": "Because a CROSS JOIN may create a very large number of rows in the result set, you should use it carefully to avoid performance issues."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS JOIN Illustration"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Suppose the T1 table contains three rows (1, 2, 3) and the T2 table contains three rows (A, B, C). The CROSS JOIN combines each row from T1 with every row in T2, creating a new row for each combination. In this illustration, the CROSS JOIN creates a total of nine rows."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS JOIN Examples"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Product-Store Combinations",
        "content": "Return all combinations of products and stores.",
        "language": "sql",
        "code": "SELECT\n    product_id,\n    product_name,\n    store_id,\n    0 AS quantity\nFROM\n    production.products\nCROSS JOIN sales.stores\nORDER BY\n    product_name,\n    store_id;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The result set can be used for the stocktaking procedure at the month-end or year-end closing."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS JOIN for Finding Products with No Sales"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Products with No Sales Using CROSS JOIN and LEFT JOIN",
        "content": "Use CROSS JOIN to create all product-store combinations, then LEFT JOIN to find which have no sales.",
        "language": "sql",
        "code": "SELECT\n    s.store_id,\n    p.product_id,\n    ISNULL(sales, 0) sales\nFROM\n    sales.stores s\nCROSS JOIN production.products p\nLEFT JOIN (\n    SELECT\n        s.store_id,\n        p.product_id,\n        SUM(quantity * i.list_price) sales\n    FROM\n        sales.orders o\n    INNER JOIN sales.order_items i ON i.order_id = o.order_id\n    INNER JOIN sales.stores s ON s.store_id = o.store_id\n    INNER JOIN production.products p ON p.product_id = i.product_id\n    GROUP BY\n        s.store_id,\n        p.product_id\n) c ON c.store_id = s.store_id\nAND c.product_id = p.product_id\nWHERE\n    sales IS NULL\nORDER BY\n    product_id,\n    store_id;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "CROSS JOIN returns the Cartesian product of two tables (every row from T1 with every row from T2).",
          "CROSS JOIN does not require a join condition.",
          "If T1 has n rows and T2 has m rows, CROSS JOIN returns n x m rows.",
          "Use CROSS JOIN carefully to avoid performance issues with large tables.",
          "CROSS JOIN is useful for generating all combinations, such as product-store pairs."
        ]
      }
    }
  ]
}