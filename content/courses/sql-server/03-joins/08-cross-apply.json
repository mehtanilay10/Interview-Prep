{
  "id": "lesson-sql-023",
  "slug": "cross-apply",
  "moduleSlug": "joins",
  "title": "CROSS APPLY Clause",
  "description": "Learn how to use the CROSS APPLY clause to perform an inner join between a table and a table-valued function or correlated subquery.",
  "order": 8,
  "difficulty": "beginner",
  "estimatedMinutes": 8,
  "tags": [
    "cross-apply",
    "table-valued-function",
    "correlated-subquery",
    "inner-join"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "SQL Server CROSS APPLY Clause"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The CROSS APPLY clause allows you to perform an inner join between a table and a table-valued function or a correlated subquery."
      }
    },
    {
      "type": "image",
      "data": {
        "src": "https://www.sqlservertutorial.net/wp-content/uploads/products-categories.png",
        "alt": "SQL Server CROSS APPLY Operator - Sample table example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In SQL Server, a table-valued function is a user-defined function that returns multiple rows as a table. The CROSS APPLY clause works like an INNER JOIN clause. Instead of joining two tables, it joins a table with a table-valued function or a correlated subquery."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS APPLY Syntax"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "CROSS APPLY Syntax",
        "content": "Join a table with a table-valued function or correlated subquery.",
        "language": "sql",
        "code": "SELECT\n    select_list\nFROM\n    table1\n    CROSS APPLY table_function(table1.column) AS alias;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In this syntax:"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "table1 is the main table from which you want to join.",
          "table_function is the table-valued function to apply to each row. Alternatively, you can use a correlated subquery.",
          "column is the column from table1 that will be passed as a parameter to the table_function.",
          "alias is the alias for the result set returned by the table_function."
        ]
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The CROSS APPLY clause applies the table_function to each row from the table1. If you use a correlated subquery, the CROSS APPLY clause will execute it for each row from the table1."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "When to Use CROSS APPLY",
        "text": "In practice, you should use the CROSS APPLY clauses when you cannot use INNER JOIN clauses, such as when joining with table-valued functions or correlated subqueries."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS APPLY with Correlated Subquery"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The following example uses CROSS APPLY with a correlated subquery to retrieve the top two most expensive products for each product category."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Top 2 Most Expensive Products per Category",
        "content": "Use a correlated subquery inside CROSS APPLY to get top products per category.",
        "language": "sql",
        "code": "SELECT\n    c.category_name,\n    r.product_name,\n    r.list_price\nFROM\n    production.categories c\n    CROSS APPLY (\n        SELECT\n            TOP 2 *\n        FROM\n            production.products p\n        WHERE\n            p.category_id = c.category_id\n        ORDER BY\n            list_price DESC,\n            product_name\n    ) r\nORDER BY\n    c.category_name,\n    r.list_price DESC;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For each row from the production.categories table, the CROSS APPLY executes the correlated subquery to retrieve the top two most expensive products."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS APPLY with Table-Valued Function"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "First, define a table-valued function that returns the top two most expensive products by category ID."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create Table-Valued Function",
        "content": "Define an inline table-valued function for top products by category.",
        "language": "sql",
        "code": "CREATE FUNCTION GetTopProductsByCategory (@category_id INT)\nRETURNS TABLE\nAS\nRETURN (\n    SELECT TOP 2 *\n    FROM production.products p\n    WHERE p.category_id = @category_id\n    ORDER BY list_price DESC, product_name\n);"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Then, use CROSS APPLY with the table-valued function to retrieve the top two most expensive products within each category."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "CROSS APPLY with Table-Valued Function",
        "content": "Apply the function to each category row.",
        "language": "sql",
        "code": "SELECT\n    c.category_name,\n    r.product_name,\n    r.list_price\nFROM\n    production.categories c\n    CROSS APPLY GetTopProductsByCategory(c.category_id) r\nORDER BY\n    c.category_name,\n    r.list_price DESC;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "This returns the same result as the query that used the correlated subquery above."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "CROSS APPLY to Process JSON Data"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create JSON Table",
        "content": "Create a table to store JSON product data.",
        "language": "sql",
        "code": "CREATE TABLE product_json(\n    id INT IDENTITY PRIMARY KEY,\n    info NVARCHAR(MAX)\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Insert JSON Data",
        "content": "Insert rows containing JSON strings.",
        "language": "sql",
        "code": "INSERT INTO product_json(info)\nVALUES\n    ('{\"Name\": \"Laptop\", \"Price\": 999, \"Category\": \"Electronics\"}'),\n    ('{\"Name\": \"Headphones\", \"Price\": 99, \"Category\": \"Electronics\"}'),\n    ('{\"Name\": \"Book\", \"Price\": 15, \"Category\": \"Books\"}');"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Extract JSON with CROSS APPLY and OPENJSON",
        "content": "Parse JSON data into columns using CROSS APPLY with OPENJSON.",
        "language": "sql",
        "code": "SELECT\n    p.id,\n    j.*\nFROM\n    product_json p\n    CROSS APPLY OPENJSON (p.info) WITH\n    (\n        Name NVARCHAR(100),\n        Price DECIMAL(10, 2),\n        Category NVARCHAR(100)\n    ) AS j;"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "CROSS APPLY performs an inner join between a table and a table-valued function or correlated subquery.",
          "Use CROSS APPLY when you cannot use INNER JOIN, such as with table-valued functions.",
          "The table-valued function or subquery is executed for each row of the main table.",
          "CROSS APPLY is useful for top-N-per-group queries, JSON parsing, and complex transformations."
        ]
      }
    }
  ],
  "courseSlug": "sql-server"
}