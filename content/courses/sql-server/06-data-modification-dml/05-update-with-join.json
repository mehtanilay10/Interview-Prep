{
  "id": "lesson-sql-044",
  "slug": "update-with-join",
  "moduleSlug": "data-modification-dml",
  "title": "SQL Server UPDATE JOIN",
  "description": "Learn how to use UPDATE JOIN to perform cross-table updates in SQL Server.",
  "order": 5,
  "difficulty": "intermediate",
  "estimatedMinutes": 12,
  "tags": ["update", "join", "dml", "cross-table"],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "SQL Server UPDATE JOIN"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In SQL Server, you can use JOIN clauses (INNER JOIN or LEFT JOIN) in the UPDATE statement to perform a cross-table update. This allows you to update a table using values from another related table."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UPDATE JOIN Syntax"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "UPDATE JOIN Syntax",
        "content": "Updates a target table using values from a joined source table.",
        "language": "sql",
        "code": "UPDATE \n    t1\nSET \n    t1.c1 = t2.c2,\n    t1.c2 = expression,\n    ...    \nFROM \n    t1\n    [INNER | LEFT] JOIN t2 ON join_predicate\nWHERE \n    where_predicate;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In this syntax:"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Specify the table you want to update (t1) in the UPDATE clause.",
          "Set the new value for each column using the SET clause.",
          "In the FROM clause, specify the table to join with (t2).",
          "Use INNER JOIN or LEFT JOIN to join the tables using a predicate after the ON keyword.",
          "Add an optional WHERE clause to further limit which rows are updated."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Setting Up Sample Tables"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "We create two tables: sales.targets stores sales targets and commission percentages, and sales.commissions stores staff sales data."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create and Populate Target and Commission Tables",
        "content": "Creates the targets and commissions tables with sample data.",
        "language": "sql",
        "code": "DROP TABLE IF EXISTS sales.targets;\n\nCREATE TABLE sales.targets\n(\n    target_id  INT\tPRIMARY KEY, \n    percentage DECIMAL(4, 2) \n        NOT NULL DEFAULT 0\n);\n\nINSERT INTO \n    sales.targets(target_id, percentage)\nVALUES\n    (1,0.2),\n    (2,0.3),\n    (3,0.5),\n    (4,0.6),\n    (5,0.8);\n\nCREATE TABLE sales.commissions\n(\n    staff_id    INT PRIMARY KEY, \n    target_id   INT, \n    base_amount DECIMAL(10, 2) \n        NOT NULL DEFAULT 0, \n    commission  DECIMAL(10, 2) \n        NOT NULL DEFAULT 0, \n    FOREIGN KEY(target_id) \n        REFERENCES sales.targets(target_id),\n    FOREIGN KEY(staff_id) \n        REFERENCES sales.staffs(staff_id),\n);\n\nINSERT INTO \n    sales.commissions(staff_id, base_amount, target_id)\nVALUES\n    (1,100000,2),\n    (2,120000,1),\n    (3,80000,3),\n    (4,900000,4),\n    (5,950000,5);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UPDATE INNER JOIN Example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The following statement uses UPDATE INNER JOIN to calculate the sales commission for all sales staff based on their targets."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "UPDATE INNER JOIN",
        "content": "Calculates commission by joining commissions with targets and multiplying base amount by target percentage.",
        "language": "sql",
        "code": "UPDATE\n    sales.commissions\nSET\n    sales.commissions.commission = \n        c.base_amount * t.percentage\nFROM \n    sales.commissions c\n    INNER JOIN sales.targets t\n        ON c.target_id = t.target_id;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "SQL Server returns (5 rows affected). The commission column now contains the calculated values for each staff member."
      }
    },
    {
      "type": "image",
      "data": {
        "src": "https://www.sqlservertutorial.net/wp-content/uploads/SQL-Server-UPDATE-JOIN-INNER-JOIN-example.png",
        "alt": "SQL Server UPDATE INNER JOIN example output showing calculated commissions"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "UPDATE LEFT JOIN Example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When some staff members do not have a target yet, use UPDATE LEFT JOIN to handle NULL values. COALESCE is used to substitute a default commission rate of 10% for staff without a target."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "UPDATE LEFT JOIN with COALESCE",
        "content": "Updates commissions for all staff, using 10% default for those without a target.",
        "language": "sql",
        "code": "INSERT INTO \n    sales.commissions(staff_id, base_amount, target_id)\nVALUES\n    (6,100000,NULL),\n    (7,120000,NULL);\n\nUPDATE \n    sales.commissions\nSET   \n    sales.commissions.commission = \n        c.base_amount  * COALESCE(t.percentage,0.1)\nFROM   \n    sales.commissions c\n    LEFT JOIN sales.targets t \n        ON c.target_id = t.target_id;"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Note that if you use UPDATE INNER JOIN, only the five rows whose targets are not NULL will be updated. The LEFT JOIN ensures all rows are updated, using 0.1 as the default percentage when the target is NULL."
      }
    },
    {
      "type": "image",
      "data": {
        "src": "https://www.sqlservertutorial.net/wp-content/uploads/SQL-Server-UPDATE-JOIN-LEFT-JOIN-example.png",
        "alt": "SQL Server UPDATE LEFT JOIN example output showing commissions including default rate"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key Takeaways"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "UPDATE JOIN Key Points",
        "points": [
          "Use UPDATE JOIN to update a table using values from another related table.",
          "INNER JOIN updates only rows that have matching entries in both tables.",
          "LEFT JOIN updates all rows, using COALESCE or ISNULL to handle NULLs from unmatched rows.",
          "The FROM clause in UPDATE specifies the joined table, not the table being updated."
        ]
      }
    }
  ]
}