{
  "id": "lesson-sql-149",
  "slug": "principals-users-roles",
  "moduleSlug": "security-hardening",
  "courseSlug": "sql-server",
  "title": "Principals, Users, and Roles",
  "description": "Learn how SQL Server organizes identities through server principals, database principals, roles, and schemas.",
  "order": 2,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "principals",
    "logins",
    "users",
    "roles",
    "schemas",
    "securables"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "The Principal Hierarchy"
    },
    {
      "type": "paragraph",
      "text": "SQL Server uses a principal hierarchy to organize who can access resources. At the top level, server principals control access to the instance. Inside each database, database principals control access to securables like tables, views, and stored procedures. Roles group principals so you can assign permissions once and manage membership instead of individual grants."
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Server Principal",
          "definition": "An identity at the SQL Server instance level. Examples include logins, certificates, and asymmetric keys."
        },
        {
          "term": "Database Principal",
          "definition": "An identity at the database level. Examples include users, roles, application roles, and certificates."
        },
        {
          "term": "Securable",
          "definition": "Any resource that can have permissions assigned, such as databases, schemas, tables, and assemblies."
        },
        {
          "term": "Schema",
          "definition": "A container that organizes securables like tables and views. Schemas act as a namespace layer between principals and objects."
        }
      ]
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Best Practice: Role-Based Access",
      "text": "Always assign permissions to roles, not individual users. Grant users membership in roles. This makes audits easier and prevents permission sprawl."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Server Principals"
    },
    {
      "type": "paragraph",
      "text": "Server principals exist in the master database and control access to the entire instance. The most common type is a login, which maps a Windows account or group, or stores a SQL Server authentication credential. Fixed server roles like sysadmin, securityadmin, and dbcreator provide predefined permission bundles."
    },
    {
      "type": "example",
      "title": "Create Logins for Different Identity Types",
      "content": "Create Windows, SQL, and group logins at the server level.",
      "code": "-- Windows login from a domain account\nCREATE LOGIN [DOMAIN\\jane.doe] FROM WINDOWS;\n\n-- Windows group login for the IT department\nCREATE LOGIN [DOMAIN\\IT_Admins] FROM WINDOWS;\n\n-- SQL Server authentication login\nCREATE LOGIN report_writer WITH PASSWORD = 'R3p0rt!2026', CHECK_POLICY = ON;\n\n-- Login from a certificate\nCREATE LOGIN cert_login FROM CERTIFICATE ReportingCert;",
      "language": "sql"
    },
    {
      "type": "mermaid",
      "id": "principal-hierarchy",
      "caption": "SQL Server principal and securable hierarchy",
      "definition": "flowchart TD\n    A[Server Instance] --> B[Server Principals]\n    B --> B1[Logins]\n    B --> B2[Certificates]\n    B --> B3[Fixed Server Roles]\n    A --> C[Databases]\n    C --> D[Database Principals]\n    D --> D1[Users]\n    D --> D2[Roles]\n    D --> D3[Application Roles]\n    C --> E[Securables]\n    E --> E1[Schemas]\n    E1 --> E2[Tables]\n    E1 --> E3[Views]\n    E1 --> E4[Procedures]\n    B1 --> D1\n    D2 --> E2"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Database Principals"
    },
    {
      "type": "paragraph",
      "text": "Database principals are stored inside each user database. A user is the database representation of a login. If you create a login in master but never create a corresponding user in a database, that login cannot access that database. Database roles group users to simplify permission management."
    },
    {
      "type": "example",
      "title": "Create Database Users and Roles",
      "content": "Map a server login to a database user and create a custom database role.",
      "code": "USE [AppDB];\n\n-- Create a user mapped to an existing login\nCREATE USER [jane.doe] FOR LOGIN [DOMAIN\\jane.doe] WITH DEFAULT_SCHEMA = [dbo];\n\n-- Create a user without a login (for contained databases)\nCREATE USER [external_consultant] WITH PASSWORD = 'C0nsult!2026';\n\n-- Create a custom database role\nCREATE ROLE [ReportReaders];\n\n-- Add user to the role\nALTER ROLE [ReportReaders] ADD MEMBER [jane.doe];",
      "language": "sql"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Fixed Roles and Custom Roles"
    },
    {
      "type": "paragraph",
      "text": "SQL Server includes fixed server roles and fixed database roles that cannot be modified. You can also create user-defined roles. User-defined roles are the preferred way to manage permissions because you can tailor them to job functions and grant membership dynamically."
    },
    {
      "type": "bullet-list",
      "title": "Common Fixed Server Roles",
      "items": [
        "sysadmin — full control over the entire SQL Server instance",
        "securityadmin — manages logins and permissions",
        "serveradmin — configures server-wide settings",
        "dbcreator — creates and modifies databases",
        "bulkadmin — runs BULK INSERT operations"
      ]
    },
    {
      "type": "bullet-list",
      "title": "Common Fixed Database Roles",
      "items": [
        "db_owner — full control over the database",
        "db_securityadmin — manages database permissions",
        "db_datareader — reads all tables and views",
        "db_datawriter — writes to all tables and views",
        "db_ddladmin — runs DDL statements"
      ]
    },
    {
      "type": "example",
      "title": "Query Role Membership",
      "content": "Inspect which principals belong to which roles.",
      "code": "-- Server role members\nSELECT sp.name AS RoleName,\n       sp.type_desc AS RoleType,\n       spm.membername AS PrincipalName,\n       spm.principal_id AS PrincipalID\nFROM sys.server_role_members AS spm\nJOIN sys.server_principals AS sp ON spm.role_principal_id = sp.principal_id;\n\n-- Database role members\nSELECT dp.name AS RoleName,\n       dp.type_desc AS RoleType,\n       mp.name AS MemberName\nFROM sys.database_role_members AS drm\nJOIN sys.database_principals AS dp ON drm.role_principal_id = dp.principal_id\nJOIN sys.database_principals AS mp ON drm.member_principal_id = mp.principal_id;",
      "language": "sql"
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "Orphaned Users",
      "text": "If a database user exists but the corresponding server login was dropped, the user becomes orphaned. Fix orphans by remapping the user to a valid login or by using contained database users."
    },
    {
      "type": "checklist",
      "title": "Principal Management Checklist",
      "items": [
        {
          "text": "Create logins using Windows groups when possible",
          "hint": "Reduces individual login management"
        },
        {
          "text": "Map every database user to a valid login",
          "hint": "Prevents orphaned users"
        },
        {
          "text": "Use user-defined roles instead of fixed roles",
          "hint": "Principle of least privilege"
        },
        {
          "text": "Assign schemas to owners other than dbo",
          "hint": "Adds separation of duties"
        },
        {
          "text": "Review role membership quarterly",
          "hint": "Prevents privilege accumulation"
        }
      ]
    },
    {
      "type": "summary-box",
      "title": "Key Takeaways",
      "points": [
        "Server principals (logins) live in master and control instance access.",
        "Database principals (users, roles) live in each database and control object access.",
        "A login must have a corresponding user in each database it needs to access.",
        "Roles should be created per job function, and users added as members."
      ],
      "takeaway": "Use roles to manage access, not individual users. Keep logins in master, users in the database."
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "prerequisites": [
    "database-schema-table-objects"
  ]
}
