{
  "id": "lesson-sql-157",
  "slug": "data-warehouse-fundamentals",
  "moduleSlug": "data-warehousing",
  "courseSlug": "sql-server",
  "title": "Data Warehouse Fundamentals",
  "description": "Understand core data warehouse concepts including OLAP vs OLTP, ETL pipelines, fact and dimension tables, slowly changing dimensions, and an introduction to SSAS.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": [
    "data-warehouse",
    "oltp",
    "olap",
    "etl",
    "elt",
    "fact-table",
    "dimension-table",
    "slowly-changing-dimension",
    "ssas"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "What Is a Data Warehouse?"
    },
    {
      "type": "paragraph",
      "text": "A data warehouse is a centralized repository that integrates data from multiple disparate sources for reporting and analysis. Unlike operational databases designed for fast transactions (OLTP), a data warehouse is optimized for complex queries across historical data (OLAP). It stores structured and sometimes semi-structured data, applying transformations to ensure consistency, quality, and analytical usability."
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "Bill Inmon vs. Ralph Kimball",
      "text": "Two philosophies dominate data warehousing. Bill Inmon champions a top-down, normalized enterprise data warehouse (EDW) fed by data marts. Ralph Kimball advocates a bottom-up approach: build independent dimensional data marts first and integrate them later. In practice, most SQL Server implementations blend both — starting with Kimball-style dimensional marts for speed, then consolidating into an Inmon-style EDW as maturity grows."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "OLTP vs. OLAP: The Core Distinction"
    },
    {
      "type": "paragraph",
      "text": "Understanding the difference between Online Transaction Processing (OLTP) and Online Analytical Processing (OLAP) is critical. OLTP systems handle day-to-day business operations: inserting orders, updating inventory, processing payments. They are highly normalized (3NF or BCNF), optimized for writes, and return small result sets. OLAP systems handle strategic analysis: 'What was our best-selling product category in Q3 by region?' They are denormalized, optimized for reads, and often return large aggregated result sets."
    },
    {
      "type": "table",
      "headers": [
        "Characteristic",
        "OLTP (Operational)",
        "OLAP (Analytical)"
      ],
      "rows": [
        [
          "Primary Goal",
          "Support daily transactions",
          "Support decision making"
        ],
        [
          "Data Source",
          "Application databases",
          "Integrated from multiple sources"
        ],
        [
          "Normalization",
          "Highly normalized (3NF/BCNF)",
          "Denormalized (dimensional)"
        ],
        [
          "Query Pattern",
          "Fast, simple, row-by-row",
          "Complex, aggregations, historical"
        ],
        [
          "Data Volume",
          "Current state",
          "Historical, years of data"
        ],
        [
          "Write vs Read",
          "Write-heavy",
          "Read-heavy (batch loads)"
        ],
        [
          "Update Frequency",
          "Continuous",
          "Periodic (daily, weekly)"
        ],
        [
          "Example",
          "AdventureWorks sales orders",
          "Data warehouse sales fact"
        ]
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "The ETL Pipeline"
    },
    {
      "type": "paragraph",
      "text": "ETL (Extract, Transform, Load) is the process that moves data from source systems into the data warehouse. Extract pulls data from operational databases, flat files, APIs, or SaaS platforms. Transform cleans, deduplicates, standardizes, and enriches the data to match warehouse schemas. Load inserts the transformed data into dimension and fact tables. Modern warehouses increasingly use ELT (Extract, Load, Transform), loading raw data first and transforming inside the warehouse using SQL or Spark for greater flexibility."
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "ETL",
          "definition": "Extract, Transform, Load — the traditional data integration pattern for populating data warehouses."
        },
        {
          "term": "ELT",
          "definition": "Extract, Load, Transform — loads raw data first, then transforms within the warehouse using SQL or processing engines."
        },
        {
          "term": "Staging Area",
          "definition": "A temporary holding area where raw extracted data lands before transformation and loading into the warehouse."
        },
        {
          "term": "Data Cleansing",
          "definition": "The process of correcting or removing corrupt, incomplete, or inaccurate records from source data before loading."
        }
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Fact Tables: The Heart of the Warehouse"
    },
    {
      "type": "paragraph",
      "text": "Fact tables store the measurable outcomes of business processes. Every fact table must define its grain — the atomic level at which events are recorded. Grain determines the meaning of 'one row' and must be consistent across the table. After declaring grain, you identify the measures (numeric, additive values) and the foreign keys linking to dimension tables."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Types of Fact Tables"
    },
    {
      "type": "table",
      "headers": [
        "Type",
        "Description",
        "Example",
        "Grain"
      ],
      "rows": [
        [
          "Transaction",
          "One row per business event",
          "Sales order line item",
          "One row per transaction"
        ],
        [
          "Periodic Snapshot",
          "Regular time-based summary",
          "Daily inventory balance",
          "One row per day per product"
        ],
        [
          "Accumulating Snapshot",
          "Tracks lifecycle milestones",
          "Order fulfillment pipeline",
          "One row per order with milestone dates"
        ],
        [
          "Factless Fact",
          "Captures events without measures",
          "Student attendance",
          "One row per student per class session"
        ]
      ]
    },
    {
      "type": "example",
      "title": "Loading a Periodic Snapshot Fact Table",
      "language": "sql",
      "code": "-- Daily sales snapshot fact table\nCREATE TABLE FactDailySales (\n    DateKey INT NOT NULL,\n    ProductKey INT NOT NULL,\n    StoreKey INT NOT NULL,\n    OrderCount INT DEFAULT 0,\n    TotalQuantity DECIMAL(14,2) DEFAULT 0,\n    TotalSalesAmount DECIMAL(14,2) DEFAULT 0,\n    TotalDiscount DECIMAL(14,2) DEFAULT 0,\n    CONSTRAINT PK_FactDailySales PRIMARY KEY (DateKey, ProductKey, StoreKey),\n    CONSTRAINT FK_FactDailySales_Date FOREIGN KEY (DateKey) REFERENCES DimDate(DateKey),\n    CONSTRAINT FK_FactDailySales_Product FOREIGN KEY (ProductKey) REFERENCES DimProduct(ProductKey),\n    CONSTRAINT FK_FactDailySales_Store FOREIGN KEY (StoreKey) REFERENCES DimStore(StoreKey)\n);\n\n-- ELT: Load raw transactional data, then aggregate\nINSERT INTO FactDailySales (DateKey, ProductKey, StoreKey, OrderCount, TotalQuantity, TotalSalesAmount)\nSELECT\n    CONVERT(INT, CONVERT(VARCHAR(8), OrderDate, 112)) AS DateKey,\n    ProductKey,\n    StoreKey,\n    COUNT(*) AS OrderCount,\n    SUM(Quantity) AS TotalQuantity,\n    SUM(LineTotal) AS TotalSalesAmount\nFROM Staging.SalesOrders\nWHERE OrderDate >= '2024-01-01'\nGROUP BY CONVERT(INT, CONVERT(VARCHAR(8), OrderDate, 112)), ProductKey, StoreKey;"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Dimension Tables"
    },
    {
      "type": "paragraph",
      "text": "Dimension tables provide the descriptive context that makes facts meaningful. Each dimension has a surrogate primary key (usually an integer identity) and one or more natural keys from source systems. Dimensions contain attributes used for filtering, grouping, and labeling in reports. A single dimension can be shared across multiple fact tables — for example, DimDate is referenced by sales, inventory, and finance facts."
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Surrogate Keys vs. Natural Keys",
      "text": "Always use surrogate integer keys in dimension tables, not business keys. Natural keys (like ProductCode or CustomerID) can change, contain gaps, or vary in format. Surrogate keys are stable, compact, and independent of source system changes, which is essential when building slowly changing dimensions."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Slowly Changing Dimensions (SCD)"
    },
    {
      "type": "paragraph",
      "text": "A slowly changing dimension is a dimension where attributes change occasionally but unpredictably — like a customer's address, a product's category, or an employee's department. Unlike rapidly changing dimensions (e.g., warehouse inventory levels), SCDs change slowly enough that you need strategies to decide whether and how to preserve history. The three classic types are Type 1, Type 2, and Type 3."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "SCD Type 1: Overwrite"
    },
    {
      "type": "paragraph",
      "text": "Type 1 simply overwrites the old value with the new one. No history is preserved. This is appropriate when the old value was an error, the attribute is not historically significant, or regulatory requirements do not demand historical tracking. The implementation is a straightforward UPDATE statement."
    },
    {
      "type": "example",
      "title": "SCD Type 1 Implementation",
      "language": "sql",
      "code": "-- Correct a misspelled city name — no history needed\nUPDATE DimCustomer\nSET City = 'New York',\n    ModifiedDate = GETDATE(),\n    ModifiedBy = SUSER_NAME()\nWHERE CustomerKey = 10042\n  AND City = 'New Yrok';"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "SCD Type 2: Track History"
    },
    {
      "type": "paragraph",
      "text": "Type 2 preserves the full history of changes by inserting a new row for each version of the dimension record. The previous row is marked inactive (end date set) and the new row becomes the current version. Queries join facts to the dimension using both the surrogate key and effective date ranges. Type 2 is the most common SCD strategy because it answers 'What did this dimension look like at the time of the transaction?' — the fundamental question of historical reporting."
    },
    {
      "type": "example",
      "title": "SCD Type 2 Implementation",
      "language": "sql",
      "code": "-- Close out the current record\nUPDATE DimCustomer\nSET EndDate = GETDATE(),\n    IsCurrent = 0,\n    ModifiedDate = GETDATE()\nWHERE CustomerKey = 10042\n  AND IsCurrent = 1;\n\n-- Insert the new version\nINSERT INTO DimCustomer (\n    CustomerKey, CustomerID, FirstName, LastName, Email,\n    City, State, Country, Segment,\n    StartDate, EndDate, IsCurrent, ModifiedDate, ModifiedBy\n)\nVALUES (\n    10042, 10042, 'Jane', 'Smith', 'jane.smith@example.com',\n    'Austin', 'TX', 'USA', 'Premium',\n    GETDATE(), '9999-12-31', 1, GETDATE(), SUSER_NAME()\n);"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "SCD Type 3: Track Selective History"
    },
    {
      "type": "paragraph",
      "text": "Type 3 adds a limited set of 'previous value' columns to the dimension table rather than creating a new row. For example, a customer dimension might have CurrentRegion and PreviousRegion columns. This approach saves storage compared to Type 2 and simplifies queries, but only tracks one level of history and is rarely suitable when users need full point-in-time accuracy."
    },
    {
      "type": "example",
      "title": "SCD Type 3 Implementation",
      "language": "sql",
      "code": "-- Add previous-value columns during table creation\nCREATE TABLE DimCustomer (\n    CustomerKey INT PRIMARY KEY,\n    CustomerID INT NOT NULL,\n    FirstName NVARCHAR(100),\n    LastName NVARCHAR(100),\n    Email NVARCHAR(200),\n    -- Current values\n    City NVARCHAR(100),\n    State NVARCHAR(50),\n    -- Previous values (Type 3)\n    PreviousCity NVARCHAR(100),\n    PreviousState NVARCHAR(50),\n    ModifiedDate DATETIME\n);\n\n-- On change: shift current to previous, then update\nUPDATE DimCustomer\nSET PreviousCity = City,\n    PreviousState = State,\n    City = 'Austin',\n    State = 'TX',\n    ModifiedDate = GETDATE()\nWHERE CustomerKey = 10042;"
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "Choose SCD Types Carefully",
      "text": "Type 2 is the safest default for dimensions where history matters. Type 3 is useful for quick 'what was it before?' scenarios but breaks down when an attribute changes multiple times. Type 1 is fine for corrections but never for historical audit requirements. Mix types within the same dimension if needed — for example, Type 2 for name changes and Type 1 for phone number corrections."
    },
    {
      "type": "mermaid",
      "id": "scd-type2-flow",
      "caption": "SCD Type 2 preserves full history by closing the old record and inserting a new version whenever an attribute changes.",
      "definition": "flowchart LR\n    A[Customer Record\n    City = Denver\n    StartDate = 2023-01-01\n    EndDate = 9999-12-31] -->|Address Change| B[Close Old Record\n    EndDate = 2024-06-15]\n    B --> C[Insert New Record\n    City = Austin\n    StartDate = 2024-06-15\n    EndDate = 9999-12-31]\n    C --> D[Query with Date Filter\n    Returns Correct City\n    for Any Transaction Date]"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Introduction to SQL Server Analysis Services (SSAS)"
    },
    {
      "type": "paragraph",
      "text": "SQL Server Analysis Services (SSAS) is Microsoft's platform for online analytical processing (OLAP) and data mining. SSAS enables you to build multidimensional cubes and tabular models that pre-calculate aggregations for lightning-fast queries across millions of rows. Instead of querying raw fact tables, analysts interact with cubes through MDX or DAX, slicing data by dimensions and drilling into hierarchies with sub-second response times."
    },
    {
      "type": "paragraph",
      "text": "SSAS offers two model types: Multidimensional (the original cube-based model) and Tabular (an in-memory, columnar model using the VertiPaq engine). Modern SQL Server deployments overwhelmingly favor Tabular models because they are easier to develop, integrate with Power BI, and leverage modern DAX query patterns. However, understanding the core OLAP concepts — dimensions, measures, hierarchies, aggregations — applies to both."
    },
    {
      "type": "mermaid",
      "id": "ssas-architecture",
      "caption": "SSAS sits between raw data sources and BI clients, pre-aggregating data into cubes or tabular models for fast analytical queries.",
      "definition": "flowchart TD\n    A[OLTP Sources\n    SQL Server, Files, APIs] --> B[ETL Pipeline\n    SSIS / Data Factory]\n    B --> C[Data Warehouse\n    Fact & Dimension Tables]\n    C --> D[SSAS Processing\n    Cube / Tabular Model]\n    D --> E[BI Clients\n    Power BI, SSRS, Excel]\n    D --> F[DAX / MDX Queries]"
    },
    {
      "type": "exercise",
      "title": "Implement SCD Type 2 for Product Categories",
      "description": "A retail data warehouse needs to track historical product category changes. Products occasionally move between categories (e.g., from 'Accessories' to 'Wearables'). Implement a Type 2 SCD solution.",
      "steps": [
        "Create a DimProduct dimension with SCD Type 2 columns: StartDate, EndDate, IsCurrent",
        "Insert 5 sample products with category values",
        "Simulate a category change for ProductID 101 from 'Accessories' to 'Wearables'",
        "Write the SQL to close the existing record and insert the new version",
        "Insert 10 sales transactions into a FactSales table spanning before and after the category change",
        "Write a query that joins FactSales to DimProduct using a date range condition",
        "Verify that transactions before the change show 'Accessories' and transactions after show 'Wearables'",
        "Document the difference between this result and a Type 1 approach"
      ],
      "expectedOutcome": "A working Type 2 SCD implementation where historical sales reports correctly reflect the product category that was active at the time of each transaction."
    },
    {
      "type": "divider"
    },
    {
      "type": "faq-block",
      "items": [
        {
          "question": "Is a data mart the same as a data warehouse?",
          "answer": "No. A data mart is a subset of a data warehouse focused on a single business line or department (e.g., sales, finance). A data warehouse is enterprise-wide, integrating multiple data marts and subject areas. Data marts deliver faster time-to-value; the data warehouse ensures consistency across the organization."
        },
        {
          "question": "Should I use SSAS or just query the data warehouse directly?",
          "answer": "For small-to-medium datasets, querying the warehouse directly with T-SQL is often sufficient. SSAS shines when you have large fact tables (hundreds of millions of rows) and need sub-second aggregation across many dimensions. If your team uses Power BI, Tabular models provide the best integration and performance."
        },
        {
          "question": "What is the difference between a data lake and a data warehouse?",
          "answer": "A data lake stores raw, unstructured, and structured data in its native format (files, blobs). A data warehouse stores structured, processed, curated data optimized for SQL queries. Modern architectures combine both: raw data lands in a lake, then curated data is shaped into a warehouse for analytics."
        }
      ]
    },
    {
      "type": "summary-box",
      "title": "Key Takeaways",
      "points": [
        "Data warehouses optimize for analytical reads, not transactional writes",
        "ETL/ELT pipelines extract, transform, and load data from disparate sources",
        "Fact tables store measures; dimension tables store descriptive context",
        "Grain must be declared before designing any fact table",
        "SCD Type 1 overwrites, Type 2 tracks full history, Type 3 tracks one level of history",
        "SSAS provides OLAP cubes and tabular models for fast aggregation over large datasets"
      ],
      "takeaway": "A well-designed data warehouse balances simplicity for analysts with performance for queries, using dimensional modeling, clean ETL pipelines, and appropriate slowly changing dimension strategies to deliver trustworthy historical insights."
    }
  ]
}
