{
  "id": "lesson-aspnet-core-05-razor-views-razor-syntax",
  "slug": "razor-syntax",
  "moduleSlug": "05-razor-views",
  "courseSlug": "aspnet-core",
  "title": "Razor Syntax Fundamentals",
  "description": "Master the basic Razor syntax for mixing C# code and HTML to build dynamic web pages.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 25,
  "tags": [
    "razor",
    "syntax",
    "csharp"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "What Is Razor?"
    },
    {
      "type": "paragraph",
      "text": "Razor is a templating engine that lets you write C# code directly inside HTML files. The result is a Razor view or page that can generate dynamic HTML based on data from your controller or page model."
    },
    {
      "type": "paragraph",
      "text": "Razor files use the .cshtml extension. The @ symbol is the gateway between HTML and C#. Anything after @ is treated as C# until the parser reaches an HTML element again. This makes Razor feel natural if you already know HTML."
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "Razor Is Not a Language",
      "text": "Razor is a syntax that the ASP.NET Core runtime translates into a C# class. The generated class inherits from a base page type and writes HTML to the response stream. You rarely need to think about this translation, but it helps to know it exists."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Code Expressions"
    },
    {
      "type": "paragraph",
      "text": "The simplest Razor construct is the code expression. It evaluates a C# expression and writes the result into the HTML output. You use @ followed by a C# expression. The framework automatically HTML-encodes the output to prevent injection attacks."
    },
    {
      "type": "example",
      "title": "Code Expressions in Razor",
      "content": "Use @ to output values and run simple expressions.",
      "language": "csharp",
      "code": "@{\n    var message = \"Hello, ASP.NET Core\";\n    var year = DateTime.UtcNow.Year;\n}\n\n<h1>@message</h1>\n<p>The current year is @year.</p>\n<p>@(2 + 2) equals four.</p>\n<p>@(new[] { \"Red\", \"Green\", \"Blue\" }[1]) is the second color.</p>"
    },
    {
      "type": "paragraph",
      "text": "Notice the @( ... ) syntax in the last two lines. When you need to mix C# expressions with punctuation that Razor might confuse for HTML, wrap the expression in parentheses."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Code Blocks"
    },
    {
      "type": "paragraph",
      "text": "A code block lets you write multiple C# statements. You start with @{ and close with }. The code inside runs before the HTML is rendered. Use code blocks to declare variables, call methods, or perform calculations."
    },
    {
      "type": "paragraph",
      "text": "Code blocks do not write output directly unless you call a method like WriteLiteral or Write. The variables and logic you define inside a code block are available to the rest of the page."
    },
    {
      "type": "example",
      "title": "Loops and Conditions in Razor",
      "content": "Use standard C# control structures inside Razor.",
      "language": "csharp",
      "code": "@{\n    var products = new[]\n    {\n        new { Name = \"Laptop\", Price = 999.99m },\n        new { Name = \"Mouse\", Price = 25.50m },\n        new { Name = \"Keyboard\", Price = 79.99m }\n    };\n    var total = products.Sum(p => p.Price);\n}\n\n<h2>Product List</h2>\n<ul>\n@foreach (var product in products)\n{\n    <li>@product.Name - @product.Price.ToString(\"C\")</li>\n}\n</ul>\n\n<p>Total inventory value: @total.ToString(\"C\")</p>\n\n@if (total > 1000)\n{\n    <p class=\"alert alert-success\">High-value inventory detected.</p>\n}\nelse\n{\n    <p class=\"alert alert-info\">Inventory value is moderate.</p>\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Working with Models"
    },
    {
      "type": "paragraph",
      "text": "Razor pages and views usually receive a Model object from the framework. You access it directly in the view using the Model property. The @model directive at the top of the file tells Razor what type to expect, which enables IntelliSense and compile-time checking."
    },
    {
      "type": "example",
      "title": "Strongly Typed Razor Page",
      "content": "Declare the model type at the top of the .cshtml file.",
      "language": "csharp",
      "code": "@page\n@model ContactViewModel\n\n<h1>Contact @Model.FullName</h1>\n\n<p>Email: @Model.Email</p>\n\n@if (Model.IsPreferred)\n{\n    <span class=\"badge bg-success\">Preferred Contact</span>\n}\n\n<form method=\"post\">\n    <label asp-for=\"Message\"></label>\n    <textarea asp-for=\"Message\"></textarea>\n    <button type=\"submit\">Send</button>\n</form>"
    },
    {
      "type": "bullet-list",
      "title": "Razor Syntax Quick Reference",
      "items": [
        "@Model.Property - Reads a property from the page model or view model.",
        "@{ ... } - Code block for multiple C# statements.",
        "@(expression) - Inline expression with explicit parentheses.",
        "@if (...) { ... } else { ... } - Conditional rendering.",
        "@foreach (var item in items) { ... } - Loop over a collection.",
        "@using Namespace - Imports a namespace for the rest of the page.",
        "@inject IService Service - Injects a service into the view.",
        "@functions { ... } - Defines helper methods or properties on the generated class."
      ]
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Razor Syntax",
          "definition": "The combination of @ symbols and C# code that allows dynamic content inside HTML files."
        },
        {
          "term": "Code Expression",
          "definition": "A single C# expression prefixed with @ that evaluates and writes its result into the HTML."
        },
        {
          "term": "Code Block",
          "definition": "A section wrapped in @{ ... } containing multiple C# statements that run before rendering."
        },
        {
          "term": "@model Directive",
          "definition": "A declaration at the top of a Razor file that specifies the expected type of the Model property."
        },
        {
          "term": "HTML Encoding",
          "definition": "The automatic conversion of special characters to safe HTML entities, preventing script injection."
        }
      ]
    },
    {
      "type": "exercise",
      "title": "Exercise: Build a Product List Page",
      "description": "Create a Razor Page that displays a list of products with prices and a total.",
      "steps": [
        "Create a Products Razor Page with a ProductsModel that supplies a list of Product objects.",
        "Declare @model List<Product> at the top of the .cshtml file.",
        "Use a @foreach loop to render each product in an HTML table.",
        "Format prices as currency using ToString(\"C\").",
        "Calculate the total price using a code block and display it below the table.",
        "Add an @if block that shows a message when the total exceeds 500."
      ],
      "expectedOutcome": "You will have a page that dynamically renders a table of products and a total price using Razor syntax."
    },
    {
      "type": "checklist",
      "title": "Checklist: Razor Syntax Basics",
      "items": [
        {
          "text": "I can explain what the @ symbol does in a Razor file.",
          "hint": "It switches from HTML into C# code."
        },
        {
          "text": "I know the difference between a code expression and a code block.",
          "hint": "Expression returns a value; block runs statements."
        },
        {
          "text": "I can use @model to make a view strongly typed.",
          "hint": "Place @model ProductList at the top of the file."
        },
        {
          "text": "I can write a foreach loop and an if statement inside Razor.",
          "hint": "Standard C# syntax works inside @{ ... } or inline with @."
        }
      ]
    },
    {
      "type": "summary-box",
      "title": "Summary",
      "points": [
        "Razor mixes C# and HTML using the @ symbol.",
        "Code expressions output a single value; code blocks run multiple statements.",
        "The @model directive gives your view a strongly typed Model property.",
        "Razor automatically HTML-encodes output to prevent injection attacks.",
        "Standard C# control structures like if, foreach, and switch work in Razor."
      ],
      "takeaway": "Razor syntax is intentionally lightweight. Once you know where @ transitions to C#, you can write any logic you need inside a view while keeping the HTML readable."
    }
  ],
  "relatedLessons": [
    "layouts-and-partials",
    "tag-helpers"
  ],
  "technology": null,
  "furtherReading": []
}
