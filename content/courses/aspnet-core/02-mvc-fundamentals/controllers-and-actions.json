{
  "id": "lesson-aspnet-core-02-mvc-fundamentals-controllers-and-actions",
  "slug": "controllers-and-actions",
  "moduleSlug": "02-mvc-fundamentals",
  "courseSlug": "aspnet-core",
  "title": "Controllers and Actions",
  "description": "Learn how controllers handle HTTP requests in ASP.NET Core. Understand action methods, return types, action results, and how to use routing attributes to expose endpoints.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 30,
  "tags": [
    "controllers",
    "actions",
    "routing"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "text": "**Controllers** are the traffic cops of an ASP.NET Core MVC application. Every incoming HTTP request eventually reaches a controller action method. The controller decides what to do with the request — fetch data, update a record, or return an error — and then returns an appropriate response."
    },
    {
      "type": "paragraph",
      "text": "An **action** is simply a public method inside a controller class. Each action handles a specific type of request and returns an `IActionResult` (or a type that can be automatically converted to one)."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Creating a Controller"
    },
    {
      "type": "paragraph",
      "text": "A controller is a C# class that inherits from the `Controller` base class (for MVC) or `ControllerBase` (for APIs). The class name must end with `Controller` and is typically placed in the `Controllers/` folder."
    },
    {
      "type": "example",
      "title": "A basic ProductsController",
      "content": "A simple MVC controller with two action methods for listing and displaying products.",
      "language": "csharp",
      "code": "using Microsoft.AspNetCore.Mvc;\n\nnamespace MyFirstApp.Controllers;\n\npublic class ProductsController : Controller\n{\n    // GET: /Products\n    public IActionResult Index()\n    {\n        var products = new[] { \"Laptop\", \"Coffee Mug\", \"Desk Lamp\" };\n        return View(products); // passes the list to the view\n    }\n\n    // GET: /Products/Details/1\n    public IActionResult Details(int id)\n    {\n        var productName = id switch\n        {\n            1 => \"Laptop\",\n            2 => \"Coffee Mug\",\n            _ => null\n        };\n\n        if (productName == null)\n        {\n            return NotFound(); // 404 response\n        }\n\n        return View(model: productName);\n    }\n}"
    },
    {
      "type": "paragraph",
      "text": "Notice how each action method returns an `IActionResult`. This interface is the key to flexibility — it allows the controller to return different types of responses (HTML views, JSON, redirects, file downloads, error codes) all through the same return type."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Common Action Results"
    },
    {
      "type": "paragraph",
      "text": "ASP.NET Core provides many built-in action result types. Each one produces a different kind of HTTP response."
    },
    {
      "type": "table",
      "headers": [
        "Method",
        "Returns",
        "Use When"
      ],
      "rows": [
        [
          "View(model)",
          "HTML page (200 OK)",
          "You want to render an HTML view with a model"
        ],
        [
          "Json(data)",
          "JSON response (200 OK)",
          "You want to return data as JSON (for APIs)"
        ],
        [
          "RedirectToAction(...)",
          "302 Redirect",
          "You want to send the user to another action"
        ],
        [
          "NotFound()",
          "404 Not Found",
          "The requested resource does not exist"
        ],
        [
          "BadRequest()",
          "400 Bad Request",
          "The request is malformed or invalid"
        ],
        [
          "Content(text)",
          "Plain text response",
          "Returning simple text content"
        ],
        [
          "File(bytes, type)",
          "File download",
          "Returning a file (PDF, image, etc.)"
        ]
      ]
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Prefer specific return types in APIs",
      "text": "For API controllers, you can use the more specific return types like `ActionResult<Product>` or `IResult` instead of the generic `IActionResult`. This provides better API documentation and IntelliSense."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Convention-Based Routing"
    },
    {
      "type": "paragraph",
      "text": "By default, ASP.NET Core uses a **convention-based route** defined in `Program.cs`. This default route maps URLs to controllers and actions using the pattern `{controller=Home}/{action=Index}/{id?}`."
    },
    {
      "type": "example",
      "title": "Default route pattern",
      "content": "The default route maps URL segments to controller, action, and optional id parameters.",
      "language": "csharp",
      "code": "app.MapControllerRoute(\n    name: \"default\",\n    pattern: \"{controller=Home}/{action=Index}/{id?}\");"
    },
    {
      "type": "paragraph",
      "text": "With this route pattern, a URL like `/Products/Details/5` maps to `ProductsController.Details(5)`. The `id?` makes the `id` parameter optional, so `/Products` maps to `ProductsController.Index()` by default."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Attribute Routing"
    },
    {
      "type": "paragraph",
      "text": "While convention-based routing works well for simple applications, **attribute routing** gives you full control by defining routes directly on controller actions using attributes. This is especially useful for APIs and complex URL structures."
    },
    {
      "type": "example",
      "title": "Attribute routing on a controller",
      "content": "Use [Route] on the controller and [HttpGet] on actions to define custom URL patterns.",
      "language": "csharp",
      "code": "[Route(\"products\")]\npublic class ProductsController : Controller\n{\n    // GET /products\n    [HttpGet(\"\")]\n    public IActionResult Index() => View();\n\n    // GET /products/5\n    [HttpGet(\"{id:int}\")]\n    public IActionResult Details(int id) => View(model: id);\n\n    // GET /products/category/electronics\n    [HttpGet(\"category/{category}\")]\n    public IActionResult ByCategory(string category) => View(model: category);\n}"
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "Route tokens",
      "text": "The `[controller]` token in route attributes is replaced with the controller name without the \"Controller\" suffix. So `[Route(\"[controller]\")]` on `ProductsController` becomes `/products`. This keeps routes in sync with controller names automatically."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Action Method Signatures"
    },
    {
      "type": "paragraph",
      "text": "Action methods can be parameterless or accept parameters. ASP.NET Core's model binding automatically populates action parameters from the incoming HTTP request — from the URL route, query string, form data, or request body."
    },
    {
      "type": "example",
      "title": "Action with multiple parameters",
      "content": "Parameters are automatically bound from the route, query string, and form data.",
      "language": "csharp",
      "code": "// Route: /Products/Search?term=laptop&page=1\n[HttpGet(\"Search\")]\npublic IActionResult Search(string term, int page = 1)\n{\n    // 'term' comes from the query string (?term=laptop)\n    // 'page' comes from the query string (page=1) with a default value of 1\n    var results = _repo.Search(term, page);\n    return View(results);\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "HTTP Method Attributes"
    },
    {
      "type": "paragraph",
      "text": "You can restrict which HTTP methods an action responds to using attributes. This is a form of method-level routing that ensures an action only handles the correct verb."
    },
    {
      "type": "bullet-list",
      "title": "HTTP method attributes",
      "items": [
        "**[HttpGet]** — Responds to GET requests (page loads, API reads)",
        "**[HttpPost]** — Responds to POST requests (form submissions, API creates)",
        "**[HttpPut]** — Responds to PUT requests (API updates)",
        "**[HttpDelete]** — Responds to DELETE requests (API deletes)",
        "**[HttpPatch]** — Responds to PATCH requests (partial updates)",
        "**[AcceptVerbs(\"GET\", \"POST\")]** — Responds to multiple specified HTTP methods"
      ]
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Controller",
          "definition": "A C# class that inherits from Controller and groups related action methods. Controllers handle incoming HTTP requests for a specific area of the application."
        },
        {
          "term": "Action Method",
          "definition": "A public method inside a controller that handles a specific HTTP request and returns an IActionResult."
        },
        {
          "term": "IActionResult",
          "definition": "An interface representing the result of an action method. It allows controllers to return different response types (views, JSON, redirects, errors) through a common interface."
        },
        {
          "term": "Attribute Routing",
          "definition": "Defining routes directly on controllers and actions using attributes like [Route] and [HttpGet], rather than relying on convention-based routing."
        },
        {
          "term": "Model Binding",
          "definition": "The automatic process of populating action method parameters from the incoming HTTP request (route data, query string, form data, or request body)."
        }
      ]
    },
    {
      "type": "checklist",
      "title": "Controllers and Actions Checklist",
      "items": [
        {
          "text": "I can create a controller class that inherits from Controller",
          "hint": "Class name must end with Controller; place it in the Controllers/ folder"
        },
        {
          "text": "I understand what IActionResult represents and can name at least three action results",
          "hint": "View(), Json(), RedirectToAction(), NotFound(), BadRequest()"
        },
        {
          "text": "I can explain how convention-based routing maps URLs to actions",
          "hint": "{controller=Home}/{action=Index}/{id?} — segments map to controller, action, and optional id"
        },
        {
          "text": "I have created at least two action methods with different HTTP verb attributes",
          "hint": "Use [HttpGet] and [HttpPost] to restrict actions to specific HTTP methods"
        }
      ]
    },
    {
      "type": "exercise",
      "title": "Build a ProductsController with Multiple Actions",
      "description": "Create a ProductsController with actions for listing all products, viewing a single product, and displaying products by category.",
      "steps": [
        "Create a new file `Controllers/ProductsController.cs` in your project",
        "Add a class `ProductsController` that inherits from `Controller`",
        "Add a static list of product strings as simulated data: Laptop, Coffee Mug, Desk Lamp, Notebook, Headphones",
        "Create an `Index()` action that returns all products to a view",
        "Create a `Details(int id)` action that returns a single product by ID, or `NotFound()` if the ID is out of range",
        "Create a `ByCategory(string category)` action that filters products (hint: use a simple string check)",
        "Run the app and navigate to `/Products`, `/Products/Details/2`, and `/Products/ByCategory?category=Electronics`"
      ],
      "expectedOutcome": "Visiting `/Products` shows a list of products. `/Products/Details/2` shows the product at index 2. Navigating to an invalid ID like `/Products/Details/99` shows a 404 Not Found response."
    },
    {
      "type": "summary-box",
      "title": "Lesson Summary",
      "points": [
        "Controllers are C# classes that group related HTTP request handlers",
        "Action methods are public methods that return IActionResult",
        "Common action results include View(), Json(), RedirectToAction(), NotFound(), BadRequest()",
        "Convention-based routing maps URLs to controllers automatically using the {controller}/{action}/{id?} pattern",
        "Attribute routing lets you define custom URL patterns directly on actions with [Route] and [HttpGet]",
        "Model binding automatically populates action parameters from the request"
      ],
      "takeaway": "Controllers and actions are the central mechanism for handling HTTP requests in ASP.NET Core. Mastering action methods, return types, and routing attributes gives you full control over how your application responds to every URL."
    }
  ],
  "relatedLessons": [
    "mvc-architecture",
    "views-and-templates"
  ],
  "technology": null,
  "furtherReading": [
    {
      "title": "Controllers in ASP.NET Core",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/mvc/controllers/actions",
      "type": "article",
      "author": "Microsoft",
      "description": "Complete guide to controllers, actions, and action results in ASP.NET Core MVC."
    },
    {
      "title": "Routing to Controller Actions",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/mvc/controllers/routing",
      "type": "article",
      "author": "Microsoft",
      "description": "How ASP.NET Core maps URLs to controller actions using convention-based and attribute routing."
    }
  ]
}
