{
  "id": "lesson-aspnet-core-02-mvc-fundamentals-views-and-templates",
  "slug": "views-and-templates",
  "moduleSlug": "02-mvc-fundamentals",
  "courseSlug": "aspnet-core",
  "title": "Views and Templates",
  "description": "Learn how Razor views generate dynamic HTML in ASP.NET Core MVC. Covers Razor syntax, layouts, partial views, view imports, and how to pass data from controllers to views.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 30,
  "tags": [
    "views",
    "razor",
    "templates"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "text": "**Views** are the pieces of your application that users actually see in their browsers. They take data from the controller and turn it into HTML. In ASP.NET Core, views use **Razor syntax** — a combination of HTML markup and embedded C# code — to generate dynamic content."
    },
    {
      "type": "paragraph",
      "text": "Understanding views is essential because they are where the user experience happens. Well-structured views are clean, readable, and easy to modify without touching controller logic."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Razor Syntax Basics"
    },
    {
      "type": "paragraph",
      "text": "Razor view files have the `.cshtml` extension and live in the `Views/` folder. Razor lets you switch between HTML and C# fluidly using the `@` symbol. When Razor sees `@`, it treats everything after it as C# code until the context returns to HTML."
    },
    {
      "type": "bullet-list",
      "title": "Common Razor syntax patterns",
      "items": [
        "`@Model.PropertyName` — Output a property value from the model",
        "`@if (condition) { ... }` — Conditional logic inside HTML",
        "`@foreach (var item in Model.Items) { ... }` — Loop over a collection",
        "`@Html.ActionLink(\"text\", \"action\", \"controller\")` — Generate an HTML hyperlink",
        "`<span asp-for=\"Name\"></span>` — Tag Helper that renders a model property"
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Strongly Typed Views"
    },
    {
      "type": "paragraph",
      "text": "At the top of a Razor view, the `@model` directive declares the type of data the view expects. This is called a **strongly typed view** — it means the compiler knows what properties are available on the model, giving you IntelliSense and compile-time error checking."
    },
    {
      "type": "example",
      "title": "A strongly typed Razor view",
      "content": "A Razor view that receives a Product model and displays its properties as HTML.",
      "language": "csharp",
      "code": "@model MyFirstApp.Models.Product\n\n<!-- Razor switches to HTML mode between tags -->\n<div class=\"product-details\">\n    <h1>@Model.Name</h1>\n    <p class=\"price\">@Model.Price.ToString(\"C\")</p>\n    <p class=\"category\">Category: @Model.Category</p>\n\n    @if (Model.Price > 100)\n    {\n        <span class=\"badge\">Premium</span>\n    }\n    else\n    {\n        <span class=\"badge\">Budget</span>\n    }\n\n    <a asp-action=\"Index\" asp-controller=\"Products\">\n        Back to Products\n    </a>\n</div>"
    },
    {
      "type": "paragraph",
      "text": "In this view, `@Model` accesses the `Product` object passed from the controller. The `@if` block conditionally renders a badge based on the price. Tag Helpers like `asp-action` and `asp-controller` generate correct URLs automatically."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Views for Collections"
    },
    {
      "type": "paragraph",
      "text": "Many views display a list of items. Razor handles this naturally with `@foreach`, iterating over the model collection and rendering HTML for each item."
    },
    {
      "type": "example",
      "title": "Rendering a list of products",
      "content": "Use @foreach to loop over a collection of products and render each as an HTML list item.",
      "language": "csharp",
      "code": "@model IEnumerable<MyFirstApp.Models.Product>\n\n<h1>Our Products</h1>\n\n@if (!Model.Any())\n{\n    <p>No products available yet.</p>\n}\nelse\n{\n    <ul class=\"product-list\">\n        @foreach (var product in Model)\n        {\n            <li>\n                <a asp-action=\"Details\" asp-route-id=\"@product.Id\">\n                    @product.Name — @product.Price.ToString(\"C\")\n                </a>\n            </li>\n        }\n    </ul>\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Layouts: Shared Page Structure"
    },
    {
      "type": "paragraph",
      "text": "Most websites have a consistent layout — a header, navigation bar, footer, and possibly a sidebar — that appears on every page. Rather than repeating this HTML in every view, ASP.NET Core uses **layouts**. A layout is a shared template that wraps around individual views."
    },
    {
      "type": "paragraph",
      "text": "The default layout file is `Views/Shared/_Layout.cshtml`. Each view specifies its layout at the top with `Layout = \"_Layout\"`. Inside the layout, `@RenderBody()` marks where the individual view's content appears."
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "_ViewStart.cshtml",
      "text": "The `_ViewStart.cshtml` file runs before every view in its folder (and subfolders). It is the standard place to set `Layout = \"_Layout\"` so you do not have to repeat it in every view."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Partial Views"
    },
    {
      "type": "paragraph",
      "text": "**Partial views** are reusable chunks of HTML that you can embed inside other views. They are perfect for UI elements that appear on multiple pages, such as product cards, comment sections, or navigation menus."
    },
    {
      "type": "example",
      "title": "A partial view and its usage",
      "content": "Create a partial view for a product card and render it inside another view.",
      "language": "csharp",
      "code": "<!-- Views/Products/_ProductCard.cshtml (partial view) -->\n@model MyFirstApp.Models.Product\n\n<div class=\"card\">\n    <h3>@Model.Name</h3>\n    <p>@Model.Price.ToString(\"C\")</p>\n    <a asp-action=\"Details\" asp-route-id=\"@Model.Id\">View Details</a>\n</div>\n\n<!-- Usage inside another view -->\n@foreach (var product in Model)\n{\n    <partial name=\"_ProductCard\" model=\"product\" />\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "View Imports and View Start"
    },
    {
      "type": "paragraph",
      "text": "Two special Razor files reduce boilerplate across all your views: `_ViewImports.cshtml` and `_ViewStart.cshtml`. They live in the `Views/` folder and apply to every view in that folder and its subfolders."
    },
    {
      "type": "bullet-list",
      "title": "What each file does",
      "items": [
        "**_ViewImports.cshtml** — Imports namespaces and adds tag helpers so you don't need `@using` or `@addTagHelper` in every view",
        "**_ViewStart.cshtml** — Sets the default layout for all views so you don't repeat `Layout = ...` in every file",
        "Both files are optional but strongly recommended for reducing repetition"
      ]
    },
    {
      "type": "example",
      "title": "Standard _ViewImports.cshtml content",
      "content": "The default _ViewImports.cshtml generated by the MVC template.",
      "language": "csharp",
      "code": "@using MyFirstApp\n@using MyFirstApp.Models\n@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Passing Data: ViewData, ViewBag, and Strongly Typed Models"
    },
    {
      "type": "paragraph",
      "text": "There are three common ways to pass data from a controller to a view. Each has tradeoffs in type safety, readability, and convenience."
    },
    {
      "type": "bullet-list",
      "title": "Data passing options",
      "items": [
        "**Strongly typed model** — Pass a typed object to `View(model)`. The view declares `@model Product` for full IntelliSense and compile-time safety. Recommended for most cases.",
        "**ViewData** — A dictionary (`ViewData[\"Key\"] = value`). Values are stored as `object` and require casting. Useful for one-off values like page titles.",
        "**ViewBag** — A dynamic wrapper around `ViewData`. Write `ViewBag.Message = \"hi\"` without casting. Convenient but no compile-time checking."
      ]
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Prefer strongly typed models",
      "text": "Strongly typed models are the most robust option. They catch errors at compile time, provide IntelliSense, and make views self-documenting. Use ViewData or ViewBag only for small, simple values that do not justify a dedicated model."
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Razor",
          "definition": "A markup syntax for embedding C# code into HTML files. Razor views (.cshtml) are the default way to generate dynamic HTML in ASP.NET Core MVC."
        },
        {
          "term": "Layout",
          "definition": "A shared template (_Layout.cshtml) that wraps around individual views, providing consistent header, navigation, and footer across all pages."
        },
        {
          "term": "Partial View",
          "definition": "A reusable view fragment that can be embedded inside another view. Used for components like cards, forms, or navigation elements that appear on multiple pages."
        },
        {
          "term": "Strongly Typed View",
          "definition": "A view that declares its model type with @model, enabling compile-time type checking and IntelliSense for model properties."
        },
        {
          "term": "Tag Helper",
          "definition": "Server-side attributes (e.g., asp-for, asp-action) that generate HTML. Tag Helpers provide an HTML-friendly alternative to HTML Helper methods."
        }
      ]
    },
    {
      "type": "checklist",
      "title": "Views and Templates Checklist",
      "items": [
        {
          "text": "I can write a Razor view that uses @model to receive a strongly typed model",
          "hint": "Start with @model Product at the top of the .cshtml file"
        },
        {
          "text": "I understand the purpose of _Layout.cshtml and @RenderBody()",
          "hint": "Layout provides shared HTML (header, nav, footer); RenderBody() marks where each page's content goes"
        },
        {
          "text": "I can create and use a partial view to reuse a UI component",
          "hint": "Create _ProductCard.cshtml and render it with <partial name=\"_ProductCard\" model=\"product\" />"
        },
        {
          "text": "I can explain the difference between ViewBag, ViewData, and a strongly typed model",
          "hint": "Strongly typed = compile-time safe; ViewBag/ViewData = dynamic/dictionary with no compile-time checks"
        }
      ]
    },
    {
      "type": "exercise",
      "title": "Create a Product Listing View",
      "description": "Build a view that renders a list of products as HTML cards using Razor syntax, a partial view, and a layout.",
      "steps": [
        "In the Views/Products folder, create `Index.cshtml` with `@model IEnumerable<MyFirstApp.Models.Product>`",
        "Add a `<h1>Product Catalog</h1>` heading and a `<ul>` element",
        "Use `@foreach` to loop through the products and render each as a list item with the product name and price",
        "Create a partial view `_ProductCard.cshtml` in Views/Products that renders a single product's name, price, and a Details link",
        "Replace the `<li>` content with `<partial name=\"_ProductCard\" model=\"product\" />`",
        "Confirm `_ViewStart.cshtml` sets the layout so the page includes the navigation bar and footer",
        "Run the app and visit `/Products` to see the rendered product list"
      ],
      "expectedOutcome": "The `/Products` page renders a list of products, each displayed as a card with the name, formatted price, and a Details link. The page uses the shared layout and the product card partial view."
    },
    {
      "type": "summary-box",
      "title": "Lesson Summary",
      "points": [
        "Razor views (.cshtml) combine HTML markup with C# code using the @ symbol",
        "The @model directive makes a view strongly typed for compile-time safety and IntelliSense",
        "Layouts provide consistent page structure across all views via _Layout.cshtml",
        "Partial views let you reuse UI components like product cards across multiple pages",
        "_ViewImports.cshtml and _ViewStart.cshtml reduce boilerplate across all views",
        "Prefer strongly typed models over ViewData/ViewBag for passing data to views"
      ],
      "takeaway": "Views are the user-facing layer of your application. Mastering Razor syntax, layouts, and partial views lets you build clean, maintainable user interfaces that are easy to update as your application grows."
    }
  ],
  "relatedLessons": [
    "controllers-and-actions",
    "models-and-viewdata"
  ],
  "technology": null,
  "furtherReading": [
    {
      "title": "Razor Pages vs. MVC Views",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/mvc/views/overview",
      "type": "article",
      "author": "Microsoft",
      "description": "Comprehensive guide to Razor views, layouts, partial views, and view components."
    },
    {
      "title": "Tag Helpers in ASP.NET Core",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/mvc/views/tag-helpers",
      "type": "article",
      "author": "Microsoft",
      "description": "Learn how Tag Helpers generate HTML from C# attributes, replacing traditional HTML Helpers."
    }
  ]
}
