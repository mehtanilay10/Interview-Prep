{
  "id": "lesson-aspnet-core-01-getting-started-understanding-project-structure",
  "slug": "understanding-project-structure",
  "moduleSlug": "01-getting-started",
  "courseSlug": "aspnet-core",
  "title": "Understanding the Project Structure",
  "description": "Explore the files and folders created by the MVC template. Learn what each folder does, how Program.cs bootstraps the application, and where to find views, static files, and configuration.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 30,
  "tags": [
    "structure",
    "folders",
    "program-cs"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "text": "When you create a new ASP.NET Core MVC project, the CLI generates a well-organized folder structure. Understanding what each folder and file does will help you navigate the codebase confidently as the application grows."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Top-Level Files"
    },
    {
      "type": "paragraph",
      "text": "At the root of your project, you will find several important files that control how the application is built, configured, and executed."
    },
    {
      "type": "bullet-list",
      "title": "Key top-level files",
      "items": [
        "**MyFirstApp.csproj** — The project file. Lists dependencies, target framework, and build settings in XML format",
        "**Program.cs** — The application entry point where the web host is built and configured",
        "**appsettings.json** — Application configuration (connection strings, logging settings, API keys) in JSON format",
        "**appsettings.Development.json** — Environment-specific settings that override appsettings.json during development",
        "**Properties/launchSettings.json** — Configures how the app launches, including URLs, environment, and SSL settings"
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "The Program.cs File"
    },
    {
      "type": "paragraph",
      "text": "`Program.cs` is the heart of an ASP.NET Core application. In .NET 6 and later, it uses the **top-level statements** feature, meaning there are no explicit `Main` method or class declarations — the code runs top-to-bottom."
    },
    {
      "type": "example",
      "title": "Standard Program.cs for an MVC app",
      "content": "The Program.cs file sets up the application builder, registers services, configures the middleware pipeline, and runs the app.",
      "language": "csharp",
      "code": "var builder = WebApplication.CreateBuilder(args);\n\n// Add services to the container.\nbuilder.Services.AddControllersWithViews();\n\nvar app = builder.Build();\n\n// Configure the HTTP request pipeline.\nif (!app.Environment.IsDevelopment())\n{\n    app.UseExceptionHandler(\"/Home/Error\");\n    app.UseHsts();\n}\n\napp.UseHttpsRedirection();\napp.UseStaticFiles();\napp.UseRouting();\napp.UseAuthorization();\n\napp.MapControllerRoute(\n    name: \"default\",\n    pattern: \"{controller=Home}/{action=Index}/{id?}\");\n\napp.Run();"
    },
    {
      "type": "paragraph",
      "text": "This file does four things in order: (1) creates the application builder, (2) registers services like controllers and views, (3) configures the middleware pipeline that processes every request, and (4) starts the web server with `app.Run()`."
    },
    {
      "type": "callout",
      "variant": "important",
      "title": "The order of middleware matters",
      "text": "Middleware runs in the order it is added. For example, `UseStaticFiles()` must come before `UseRouting()`, and `UseAuthorization()` must come after `UseRouting()`. Getting this order wrong causes confusing runtime errors."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Key Folders"
    },
    {
      "type": "paragraph",
      "text": "Beyond the root files, the MVC template creates several folders that organize different types of content in your application."
    },
    {
      "type": "bullet-list",
      "title": "Standard MVC project folders",
      "items": [
        "**Controllers/** — Contains C# controller classes. Each controller handles requests for a related set of actions (e.g., `HomeController`, `ProductsController`)",
        "**Views/** — Contains Razor view files (.cshtml) organized in subfolders matching controller names. Each action typically has its own view",
        "**Models/** — Contains C# classes that represent your application data. These classes define the shape of data passed between controllers and views",
        "**wwwroot/** — The web root folder. Static files like CSS, JavaScript, images, and fonts are placed here and served directly to browsers",
        "**Data/** — (Optional) Contains database context classes and migration files when using Entity Framework Core"
      ]
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Folders are conventions, not requirements",
      "text": "ASP.NET Core uses convention-based routing and file lookup. The framework looks for views in `Views/[ControllerName]/[ActionName].cshtml` by convention. You can change this behavior if needed, but following the convention keeps your project organized and maintainable."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Configuration with appsettings.json"
    },
    {
      "type": "paragraph",
      "text": "The `appsettings.json` file stores application settings in a hierarchical JSON structure. ASP.NET Core automatically reads this file and makes the values available through the `IConfiguration` interface."
    },
    {
      "type": "example",
      "title": "Reading configuration values",
      "content": "Access settings from appsettings.json using the IConfiguration interface, injected into any service or controller.",
      "language": "csharp",
      "code": "// In a controller or service\npublic class HomeController : Controller\n{\n    private readonly IConfiguration _config;\n\n    public HomeController(IConfiguration config)\n    {\n        _config = config;\n    }\n\n    public IActionResult Index()\n    {\n        // Reads from appsettings.json -> \"AppSettings\" -> \"SiteName\"\n        var siteName = _config[\"AppSettings:SiteName\"];\n        return View(model: siteName);\n    }\n}\n\n// appsettings.json\n{\n  \"AppSettings\": {\n    \"SiteName\": \"My ASP.NET Core App\",\n    \"MaxItemsPerPage\": 20\n  }\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Environments: Development vs. Production"
    },
    {
      "type": "paragraph",
      "text": "ASP.NET Core recognizes three environments: **Development**, **Staging**, and **Production**. The current environment is set by the `ASPNETCORE_ENVIRONMENT` variable. You can check the environment in code to show detailed errors in development but friendly error pages in production."
    },
    {
      "type": "example",
      "title": "Environment-specific behavior",
      "content": "Use IHostEnvironment to branch logic based on the current environment, such as showing detailed errors only during development.",
      "language": "csharp",
      "code": "if (app.Environment.IsDevelopment())\n{\n    // Detailed error pages with stack traces\n    app.UseDeveloperExceptionPage();\n}\nelse\n{\n    // Generic error page for users\n    app.UseExceptionHandler(\"/Home/Error\");\n    app.UseHsts(); // Enforce HTTPS\n}"
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "Never expose detailed errors in production",
      "text": "The Developer Exception Page reveals sensitive information like stack traces, configuration values, and database queries. Always ensure `UseDeveloperExceptionPage()` is only active in the Development environment."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Summary of the Project Layout"
    },
    {
      "type": "table",
      "headers": [
        "File / Folder",
        "Purpose"
      ],
      "rows": [
        [
          "Program.cs",
          "Application entry point; configures services and middleware"
        ],
        [
          "appsettings.json",
          "Configuration settings (JSON)"
        ],
        [
          "MyFirstApp.csproj",
          "Project file; lists dependencies and build settings"
        ],
        [
          "Controllers/",
          "C# classes that handle HTTP requests"
        ],
        [
          "Views/",
          "Razor view files (.cshtml) for generating HTML"
        ],
        [
          "Models/",
          "Data model classes"
        ],
        [
          "wwwroot/",
          "Static files (CSS, JS, images, fonts)"
        ],
        [
          "Data/",
          "Database context and EF Core migrations"
        ]
      ]
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Program.cs",
          "definition": "The application entry point using top-level statements. Configures services, middleware, and starts the web server."
        },
        {
          "term": "Middleware Pipeline",
          "definition": "An ordered series of request-processing components. Each middleware can handle a request, pass it to the next, or short-circuit the pipeline."
        },
        {
          "term": "wwwroot",
          "definition": "The web root folder for static files — CSS, JavaScript, images, and fonts are served directly from here without going through MVC."
        },
        {
          "term": "IConfiguration",
          "definition": "The interface for reading configuration values from appsettings.json and other configuration sources."
        }
      ]
    },
    {
      "type": "checklist",
      "title": "Project Structure Checklist",
      "items": [
        {
          "text": "I can identify Program.cs and explain its three main phases",
          "hint": "Builder creation, service registration (AddControllersWithViews), middleware configuration, app.Run()"
        },
        {
          "text": "I understand the purpose of appsettings.json and appsettings.Development.json",
          "hint": "appsettings.json is shared; appsettings.Development.json overrides values in the Development environment"
        },
        {
          "text": "I know where controllers, views, models, and static files live in the project",
          "hint": "Controllers/, Views/, Models/, and wwwroot/ folders respectively"
        },
        {
          "text": "I can explain the order of middleware in Program.cs and why it matters",
          "hint": "UseStaticFiles before UseRouting, UseAuthorization after UseRouting, etc."
        }
      ]
    },
    {
      "type": "exercise",
      "title": "Explore the Project Structure",
      "description": "Navigate through your newly created MVC project and identify the purpose of each key file and folder.",
      "steps": [
        "Open the MyFirstApp project folder in your code editor",
        "Open Program.cs and identify the three main phases: builder creation, service registration, and middleware configuration",
        "Open appsettings.json and add a new setting: \"AppSettings\": { \"SupportEmail\": \"help@example.com\" }",
        "Open the Controllers folder and list the controllers present in the default template",
        "Open the Views folder and identify how views are organized by controller name",
        "Open wwwroot and list the types of static files present (CSS, JS, lib/, etc.)"
      ],
      "expectedOutcome": "You should be able to point to each major file and folder in the project and explain its role. The appsettings.json change you made will be readable via IConfiguration."
    },
    {
      "type": "summary-box",
      "title": "Lesson Summary",
      "points": [
        "Program.cs is the entry point, using top-level statements to configure services and middleware",
        "Controllers/ holds C# classes that process HTTP requests",
        "Views/ holds Razor (.cshtml) files organized by controller name",
        "Models/ holds C# classes representing application data",
        "wwwroot/ serves static files directly to browsers",
        "appsettings.json provides hierarchical JSON configuration accessible via IConfiguration"
      ],
      "takeaway": "The MVC project template creates a logical, convention-based folder structure. Understanding this structure is essential — it tells you exactly where to add new controllers, views, models, and static files as your application grows."
    }
  ],
  "relatedLessons": [
    "creating-first-project",
    "running-and-debugging"
  ],
  "technology": null,
  "furtherReading": [
    {
      "title": "ASP.NET Core Project Structure",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/fundamentals/project-structure",
      "type": "article",
      "author": "Microsoft",
      "description": "Official guide to the files and folders in an ASP.NET Core project."
    }
  ]
}
