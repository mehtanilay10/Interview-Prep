{
  "id": "lesson-aspnet-core-03-routing-urls-attribute-routing",
  "slug": "attribute-routing",
  "moduleSlug": "03-routing-urls",
  "courseSlug": "aspnet-core",
  "title": "Attribute Routing",
  "description": "Learn to define routes directly on controllers and actions using attributes. Understand how attribute routing provides fine-grained control over URL patterns and how it complements convention-based routing.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 25,
  "tags": [
    "attributes",
    "routing",
    "urls"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "text": "**Attribute routing** gives you full control over your application's URL structure by letting you define routes directly on controller classes and action methods using C# attributes. Instead of relying on conventions, you explicitly declare what URL each action responds to."
    },
    {
      "type": "paragraph",
      "text": "This approach is especially powerful for APIs, where you want precise, RESTful URL patterns. It is also useful in MVC applications when you need custom or hierarchical URLs that convention-based routing cannot express cleanly."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Enabling Attribute Routing"
    },
    {
      "type": "paragraph",
      "text": "Attribute routing is enabled by calling `MapControllers()` in `Program.cs` (instead of `MapControllerRoute()`). For MVC applications, you can use both — `MapControllerRoute` for convention-based routes and `MapControllers` for attribute routes."
    },
    {
      "type": "example",
      "title": "Enabling attribute routing",
      "content": "Add MapControllers() to support attribute-routed controllers in your application.",
      "language": "csharp",
      "code": "var builder = WebApplication.CreateBuilder(args);\nbuilder.Services.AddControllersWithViews();\n\nvar app = builder.Build();\n\napp.UseHttpsRedirection();\napp.UseStaticFiles();\napp.UseRouting();\napp.UseAuthorization();\n\n// This enables attribute-routed controllers\napp.MapControllers();\n\n// Optional: also keep convention-based routes\n// app.MapControllerRoute(\"default\", \"{controller=Home}/{action=Index}/{id?}\");\n\napp.Run();"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "The [Route] Attribute"
    },
    {
      "type": "paragraph",
      "text": "The `[Route]` attribute is applied at the controller level to set a base URL prefix. Individual actions then extend this prefix with their own route templates."
    },
    {
      "type": "example",
      "title": "Attribute routing on a controller",
      "content": "Define a base route on the controller and specific routes on each action.",
      "language": "csharp",
      "code": "[Route(\"products\")]\npublic class ProductsController : Controller\n{\n    // GET /products\n    [HttpGet(\"\")]\n    public IActionResult Index()\n    {\n        return View();\n    }\n\n    // GET /products/5\n    [HttpGet(\"{id:int}\")]\n    public IActionResult Details(int id)\n    {\n        return View(model: id);\n    }\n\n    // GET /products/featured\n    [HttpGet(\"featured\")]\n    public IActionResult Featured()\n    {\n        return View();\n    }\n}"
    },
    {
      "type": "paragraph",
      "text": "In this example, the controller's base route is `products`. The `[HttpGet(\"\")]` on `Index` means it responds to exactly `/products`. The `[HttpGet(\"{id:int}\")]` on `Details` means it responds to `/products/5`. The `:int` constraint ensures only numeric IDs match."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Route Tokens"
    },
    {
      "type": "paragraph",
      "text": "Route tokens are special placeholders in route templates that ASP.NET Core replaces automatically. The most useful token is `[controller]`, which is replaced with the controller class name minus the \"Controller\" suffix."
    },
    {
      "type": "bullet-list",
      "title": "Available route tokens",
      "items": [
        "**[controller]** — Replaced with the controller name (e.g., `ProductsController` → `products`)",
        "**[action]** — Replaced with the action method name (e.g., `Details` → `details`)",
        "**[area]** — Replaced with the area name (used in larger applications with Areas)"
      ]
    },
    {
      "type": "example",
      "title": "Using route tokens",
      "content": "Use [controller] and [action] tokens for flexible route definitions that stay in sync with class names.",
      "language": "csharp",
      "code": "[Route(\"[controller]\")]\npublic class ProductsController : Controller\n{\n    // GET /products\n    [HttpGet(\"\")]\n    public IActionResult Index() => View();\n\n    // GET /products/5\n    [HttpGet(\"{id:int}\")]\n    public IActionResult Details(int id) => View(model: id);\n}"
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Combine tokens with static segments",
      "text": "You can mix tokens with static path segments for expressive routes: `[Route(\"api/[controller]\")]` creates `/api/products` — a standard pattern for REST APIs."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Combining Multiple Routes on One Action"
    },
    {
      "type": "paragraph",
      "text": "An action can respond to multiple URLs by applying multiple route attributes. This is useful when you want an action to be accessible under different URL patterns."
    },
    {
      "type": "example",
      "title": "Multiple routes on a single action",
      "content": "Apply multiple [HttpGet] attributes to make an action respond to multiple URL patterns.",
      "language": "csharp",
      "code": "[Route(\"products\")]\npublic class ProductsController : Controller\n{\n    // Responds to both /products and /products/list\n    [HttpGet(\"\")]\n    [HttpGet(\"list\")]\n    public IActionResult Index() => View();\n\n    // Responds to /products/5 and /p/5\n    [HttpGet(\"{id:int}\")]\n    [HttpGet(\"p/{id:int}\")]\n    public IActionResult Details(int id) => View(model: id);\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Attribute Routing in APIs"
    },
    {
      "type": "paragraph",
      "text": "Attribute routing is the standard approach for building REST APIs in ASP.NET Core. It gives you full control over the URL structure, making it easy to follow REST conventions."
    },
    {
      "type": "example",
      "title": "A RESTful API controller with attribute routing",
      "content": "A complete API controller using attribute routing for standard REST endpoints.",
      "language": "csharp",
      "code": "[ApiController]\n[Route(\"api/[controller]\")]\npublic class ProductsController : ControllerBase\n{\n    private static readonly List<Product> _products = new()\n    {\n        new Product { Id = 1, Name = \"Laptop\", Price = 999.99m },\n        new Product { Id = 2, Name = \"Coffee Mug\", Price = 12.50m }\n    };\n\n    // GET /api/products\n    [HttpGet]\n    public ActionResult<IEnumerable<Product>> GetAll()\n    {\n        return Ok(_products);\n    }\n\n    // GET /api/products/1\n    [HttpGet(\"{id:int}\")]\n    public ActionResult<Product> GetById(int id)\n    {\n        var product = _products.FirstOrDefault(p => p.Id == id);\n        if (product == null) return NotFound();\n        return Ok(product);\n    }\n\n    // POST /api/products\n    [HttpPost]\n    public ActionResult<Product> Create([FromBody] Product newProduct)\n    {\n        newProduct.Id = _products.Max(p => p.Id) + 1;\n        _products.Add(newProduct);\n        return CreatedAtAction(nameof(GetById), new { id = newProduct.Id }, newProduct);\n    }\n\n    // DELETE /api/products/1\n    [HttpDelete(\"{id:int}\")]\n    public IActionResult Delete(int id)\n    {\n        var product = _products.FirstOrDefault(p => p.Id == id);\n        if (product == null) return NotFound();\n        _products.Remove(product);\n        return NoContent();\n    }\n}"
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "ApiController attribute",
      "text": "The [ApiController] attribute enables API-specific behaviors: automatic 400 responses for model validation failures, binding source inference, and improved error responses. It is the standard for REST APIs in ASP.NET Core."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Convention vs. Attribute Routing"
    },
    {
      "type": "paragraph",
      "text": "Both routing styles are first-class in ASP.NET Core and can even coexist in the same application. Understanding when to use each is key to a clean URL structure."
    },
    {
      "type": "comparison-cards",
      "title": "Convention-Based vs. Attribute Routing",
      "cards": [
        {
          "title": "Convention-Based Routing",
          "description": "Routes are defined centrally in Program.cs using a URL pattern. Controllers and actions are discovered automatically.",
          "pros": [
            "Simple setup for standard MVC apps",
            "All routes visible in one place",
            "Good for CRUD-heavy applications"
          ],
          "cons": [
            "Less control over individual URLs",
            "Harder to create non-standard patterns",
            "Can become unwieldy for complex APIs"
          ]
        },
        {
          "title": "Attribute Routing",
          "description": "Routes are defined directly on controllers and actions using attributes. Each action declares its own URL.",
          "pros": [
            "Full control over URL structure",
            "URLs defined close to the code they affect",
            "Ideal for REST APIs and custom URL patterns"
          ],
          "cons": [
            "Routes spread across multiple files",
            "Requires more setup per controller",
            "Can lead to duplication if not managed well"
          ]
        }
      ]
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "Attribute Routing",
          "definition": "Defining routes using C# attributes ([Route], [HttpGet], etc.) directly on controllers and actions, rather than using a central route table."
        },
        {
          "term": "Route Token",
          "definition": "A placeholder like [controller] or [action] in a route template that ASP.NET Core replaces with the actual controller or action name."
        },
        {
          "term": "ApiController",
          "definition": "An attribute that enables API-specific behaviors including automatic model validation responses and binding source inference."
        },
        {
          "term": "RESTful Route",
          "definition": "A URL pattern that follows REST conventions (e.g., GET /api/products, POST /api/products, GET /api/products/1) using HTTP methods to indicate the action."
        }
      ]
    },
    {
      "type": "checklist",
      "title": "Attribute Routing Checklist",
      "items": [
        {
          "text": "I have enabled attribute routing with app.MapControllers() in Program.cs",
          "hint": "MapControllers() enables attribute-routed controllers; MapControllerRoute() enables convention routes"
        },
        {
          "text": "I can apply [Route] at the controller level and [HttpGet] on individual actions",
          "hint": "[Route(\"products\")] on the class, [HttpGet(\"{id:int}\")] on the method"
        },
        {
          "text": "I understand how route tokens like [controller] simplify route definitions",
          "hint": "[controller] is replaced with the controller name minus 'Controller' suffix"
        },
        {
          "text": "I can apply multiple route attributes to one action for multiple URL patterns",
          "hint": "Stack two [HttpGet] attributes with different patterns on the same action"
        }
      ]
    },
    {
      "type": "exercise",
      "title": "Convert a Controller to Attribute Routing",
      "description": "Take a convention-routed controller and convert it to use attribute routing with route tokens and constraints.",
      "steps": [
        "Create a new BlogController with actions: Index(), Details(int id), and Archive(string year, string month)",
        "Apply `[Route(\"blog\")]` at the controller level",
        "Add `[HttpGet(\"\")]` to Index() so it responds to `/blog`",
        "Add `[HttpGet(\"{id:int}\")]` to Details() so it responds to `/blog/5`",
        "Add `[HttpGet(\"archive/{year}/{month}\")]` to Archive() so it responds to `/blog/archive/2024/01`",
        "In Program.cs, ensure `app.MapControllers()` is called to enable attribute routing",
        "Run the app and test each URL pattern in the browser"
      ],
      "expectedOutcome": "`/blog` shows the blog index, `/blog/5` calls the Details action with id=5, and `/blog/archive/2024/01` calls Archive with year=\"2024\" and month=\"01\"."
    },
    {
      "type": "summary-box",
      "title": "Lesson Summary",
      "points": [
        "Attribute routing defines routes directly on controllers and actions using C# attributes",
        "Enable attribute routing with `app.MapControllers()` in Program.cs",
        "[Route(\"products\")] on a controller sets the base URL prefix for all actions",
        "Route tokens like [controller] automatically insert the controller name into the URL",
        "Multiple [HttpGet] attributes on one action make it respond to multiple URL patterns",
        "Attribute routing is the standard for REST APIs, often combined with [ApiController]"
      ],
      "takeaway": "Attribute routing gives you precise, declarative control over your URL structure. For APIs it is the standard approach; for MVC applications it complements convention-based routing for custom URL patterns."
    }
  ],
  "relatedLessons": [
    "routing-basics",
    "route-constraints"
  ],
  "technology": null,
  "furtherReading": [
    {
      "title": "Attribute Routing in ASP.NET Core",
      "url": "https://learn.microsoft.com/en-us/aspnet/core/mvc/controllers/routing",
      "type": "article",
      "author": "Microsoft",
      "description": "Complete guide to attribute routing with examples for MVC and Web API."
    }
  ]
}
