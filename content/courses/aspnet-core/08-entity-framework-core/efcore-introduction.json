{
  "id": "lesson-aspnet-core-08-entity-framework-core-efcore-introduction",
  "slug": "efcore-introduction",
  "moduleSlug": "08-entity-framework-core",
  "courseSlug": "aspnet-core",
  "title": "Introduction to EF Core",
  "description": "Discover Entity Framework Core, the official ORM for .NET, and learn how it bridges the gap between your C# object models and relational databases.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 28,
  "tags": [
    "efcore",
    "orm",
    "database"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 2,
      "text": "What Is Entity Framework Core?"
    },
    {
      "type": "paragraph",
      "text": "Entity Framework Core, or EF Core for short, is an object-relational mapper (ORM) that lets you work with databases using .NET objects. Instead of writing raw SQL queries and manually mapping results to classes, EF Core handles the translation between your C# code and the database for you."
    },
    {
      "type": "paragraph",
      "text": "When you use EF Core, you define your data model as C# classes called entities. Each entity represents a table in the database, and each property on the class represents a column. EF Core then takes care of creating tables, inserting data, querying records, updating rows, and deleting entries — all through LINQ queries or method-based APIs."
    },
    {
      "type": "paragraph",
      "text": "EF Core is the recommended data access technology for new .NET applications. It is lightweight, extensible, and supports SQL Server, PostgreSQL, MySQL, SQLite, and many other database providers."
    },
    {
      "type": "callout",
      "variant": "info",
      "title": "EF Core vs. Dapper",
      "text": "EF Core is a full-featured ORM with change tracking, migrations, and lazy loading. Dapper is a micro-ORM focused on raw SQL performance with minimal abstraction. Many production applications use both: EF Core for standard CRUD and Dapper for high-performance reporting queries."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "The DbContext: Your Database Gateway"
    },
    {
      "type": "paragraph",
      "text": "The central class in EF Core is DbContext. Think of DbContext as a session with the database — it tracks changes to your entities, manages connections, and translates LINQ queries into SQL. Every entity set you want to query or save is exposed as a DbSet<T> on the context."
    },
    {
      "type": "example",
      "title": "A simple DbContext with one entity",
      "content": "This DbContext exposes a DbSet for the Product entity and configures the database connection.",
      "language": "csharp",
      "code": "using Microsoft.EntityFrameworkCore;\n\npublic class Product\n{\n    public int Id { get; set; }\n    public string Name { get; set; } = string.Empty;\n    public decimal Price { get; set; }\n    public bool IsActive { get; set; }\n}\n\npublic class AppDbContext : DbContext\n{\n    public DbSet<Product> Products => Set<Product>();\n\n    protected override void OnConfiguring(DbContextOptionsBuilder options)\n        => options.UseSqlServer(\n            \"Server=.;Database=ShopDb;Trusted_Connection=True;TrustServerCertificate=True;\");\n}"
    },
    {
      "type": "paragraph",
      "text": "The Product class is a plain C# class with no EF Core-specific base classes or attributes required. EF Core uses conventions: a property named 'Id' or '<ClassName>Id' is automatically treated as the primary key. The DbSet<Product> property provides the entry point for all Product-related database operations."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "How EF Core Translates Code to SQL"
    },
    {
      "type": "paragraph",
      "text": "One of the most powerful features of EF Core is LINQ query translation. You write queries using familiar C# syntax, and EF Core converts them into optimized SQL at runtime. This means you can work with strongly-typed objects instead of string-based SQL queries."
    },
    {
      "type": "example",
      "title": "LINQ query translated to SQL",
      "content": "Querying active products under a price threshold using LINQ methods.",
      "language": "csharp",
      "code": "using var context = new AppDbContext();\n\nvar cheapProducts = await context.Products\n    .Where(p => p.IsActive && p.Price < 50)\n    .OrderBy(p => p.Name)\n    .Select(p => new { p.Name, p.Price })\n    .ToListAsync();\n\nforeach (var p in cheapProducts)\n{\n    Console.WriteLine($\"{p.Name}: {p.Price:C}\");\n}"
    },
    {
      "type": "paragraph",
      "text": "This LINQ query is translated by EF Core into SQL similar to 'SELECT Name, Price FROM Products WHERE IsActive = 1 AND Price < 50 ORDER BY Name'. You never have to write that SQL yourself. EF Core also handles parameterization automatically, protecting against SQL injection attacks."
    },
    {
      "type": "callout",
      "variant": "important",
      "title": "Important: LINQ queries are parameterized automatically",
      "text": "EF Core automatically converts LINQ parameters into SQL parameters, which prevents SQL injection. Do not concatenate user input into raw SQL strings — always use parameterized queries through LINQ or FromSqlInterpolated."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Adding EF Core to Your Project"
    },
    {
      "type": "paragraph",
      "text": "To use EF Core, you need to install two NuGet packages: the core EF Core package and a provider package for your database. The provider package contains the database-specific SQL generator and type mappings."
    },
    {
      "type": "bullet-list",
      "title": "NuGet packages for common databases",
      "items": [
        "Microsoft.EntityFrameworkCore.SqlServer — for Microsoft SQL Server",
        "Microsoft.EntityFrameworkCore.PostgreSQL — for PostgreSQL",
        "Microsoft.EntityFrameworkCore.Sqlite — for SQLite, great for development and testing",
        "Microsoft.EntityFrameworkCore.MySql — for MySQL",
        "Microsoft.EntityFrameworkCore.InMemory — for unit testing with an in-memory database"
      ]
    },
    {
      "type": "example",
      "title": "Installing EF Core packages via .NET CLI",
      "content": "Install the SQL Server provider and tools packages.",
      "language": "csharp",
      "code": "dotnet add package Microsoft.EntityFrameworkCore.SqlServer\ndotnet add package Microsoft.EntityFrameworkCore.Tools"
    },
    {
      "type": "paragraph",
      "text": "The Tools package provides the 'dotnet ef' CLI commands for creating migrations and updating the database schema. It is not needed at runtime, only during development."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "CRUD Operations with EF Core"
    },
    {
      "type": "paragraph",
      "text": "EF Core supports all four standard database operations: Create, Read, Update, and Delete. Each operation is performed through the DbSet<T> on your DbContext. EF Core tracks entity states automatically, so you only need to call SaveChangesAsync to persist modifications."
    },
    {
      "type": "bullet-list",
      "title": "Basic CRUD with DbSet<T>",
      "items": [
        "Create: Instantiate a new entity and call context.Add(entity), then SaveChangesAsync()",
        "Read: Query with LINQ using Where, FirstOrDefault, ToListAsync, and other methods",
        "Update: Retrieve the entity, modify its properties, then call SaveChangesAsync()",
        "Delete: Retrieve the entity, call context.Remove(entity), then SaveChangesAsync()"
      ]
    },
    {
      "type": "example",
      "title": "Full CRUD example",
      "content": "Creating, reading, updating, and deleting products using EF Core.",
      "language": "csharp",
      "code": "using var context = new AppDbContext();\n\n// CREATE\nvar newProduct = new Product { Name = \"Laptop\", Price = 999.99m, IsActive = true };\ncontext.Products.Add(newProduct);\nawait context.SaveChangesAsync();\nConsole.WriteLine($\"Added product with Id: {newProduct.Id}\");\n\n// READ\nvar product = await context.Products.FirstOrDefaultAsync(p => p.Name == \"Laptop\");\nConsole.WriteLine($\"Found: {product?.Name} at {product?.Price:C}\");\n\n// UPDATE\nif (product != null)\n{\n    product.Price = 899.99m;\n    await context.SaveChangesAsync();\n    Console.WriteLine(\"Price updated.\");\n}\n\n// DELETE\nif (product != null)\n{\n    context.Products.Remove(product);\n    await context.SaveChangesAsync();\n    Console.WriteLine(\"Product deleted.\");\n}"
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "Tip: Always use async methods",
      "text": "Use the async database operations like ToListAsync(), FirstOrDefaultAsync(), and SaveChangesAsync() in web applications. Async database calls free up threads to handle other requests, improving scalability under load."
    },
    {
      "type": "key-terms",
      "terms": [
        {
          "term": "ORM",
          "definition": "Object-Relational Mapper — a library that maps between database tables and .NET objects, eliminating the need for raw SQL and manual mapping."
        },
        {
          "term": "DbContext",
          "definition": "The main EF Core class responsible for database interaction, tracking entity changes, and coordinating queries and saves."
        },
        {
          "term": "DbSet<T>",
          "definition": "A property on DbContext that represents a database table and provides query and CRUD operations for entity type T."
        },
        {
          "term": "Entity",
          "definition": "A C# class that maps to a database table. Its properties map to columns."
        },
        {
          "term": "Convention-based configuration",
          "definition": "EF Core's default behavior of inferring table names, primary keys, and column types from entity class names and property types."
        }
      ]
    },
    {
      "type": "exercise",
      "title": "Create Your First EF Core Model",
      "description": "Set up a new project with EF Core, define an entity model, and perform CRUD operations against a SQLite database.",
      "steps": [
        "Create a new console project: 'dotnet new console -n EfCoreIntro'",
        "Install Microsoft.EntityFrameworkCore.Sqlite and Microsoft.EntityFrameworkCore.Tools packages",
        "Define a Book entity class with Id, Title, Author, PublishedYear, and Price properties",
        "Define a LibraryDbContext that inherits from DbContext with a DbSet<Book> property",
        "Override OnConfiguring to use UseSqlite with a local database file 'library.db'",
        "In Program.cs, ensure the database is created and insert three sample books",
        "Query the books with a LINQ Where clause and print titles and prices to the console"
      ],
      "expectedOutcome": "Three books are inserted into the SQLite database. The console output shows the titles and prices of all books published after 2015, proving the LINQ query was translated to SQL correctly."
    },
    {
      "type": "checklist",
      "title": "EF Core Introduction Checklist",
      "items": [
        {
          "text": "I can explain what an ORM is and why it is useful",
          "hint": "An ORM maps database tables to objects, reducing the need for raw SQL."
        },
        {
          "text": "I understand the role of DbContext and DbSet<T>",
          "hint": "DbContext is the session; DbSet<T> is the table entry point."
        },
        {
          "text": "I can install EF Core and a database provider via NuGet",
          "hint": "You need both the core package and a provider like SqlServer or Sqlite."
        },
        {
          "text": "I know how EF Core uses conventions for primary keys",
          "hint": "A property named 'Id' or '<TypeName>Id' is automatically the primary key."
        },
        {
          "text": "I can write basic CRUD operations using DbSet methods",
          "hint": "Add, Find, FirstOrDefault, Remove, and SaveChangesAsync are the core methods."
        },
        {
          "text": "I understand why EF Core protects against SQL injection",
          "hint": "LINQ queries are translated into parameterized SQL automatically."
        }
      ]
    },
    {
      "type": "summary-box",
      "title": "EF Core Introduction Summary",
      "points": [
        "EF Core is an ORM that maps C# classes to database tables and properties to columns",
        "DbContext is the central class for all database interactions and change tracking",
        "DbSet<T> provides query and CRUD access to a specific entity type",
        "EF Core uses conventions to infer keys, table names, and column types automatically",
        "LINQ queries are translated to SQL at runtime with automatic parameterization",
        "NuGet packages for both the core EF Core library and your database provider are required"
      ],
      "takeaway": "Entity Framework Core dramatically simplifies database access by letting you work with C# objects instead of raw SQL. Understanding DbContext, DbSet, and the LINQ query pipeline is the foundation for everything else in this module."
    }
  ],
  "relatedLessons": [],
  "technology": null,
  "furtherReading": [
    {
      "title": "EF Core Getting Started",
      "url": "https://learn.microsoft.com/en-us/ef/core/get-started/",
      "type": "article",
      "author": "Microsoft",
      "description": "Official getting started guide covering installation, DbContext setup, and first queries."
    },
    {
      "title": "EF Core DbContext Class",
      "url": "https://learn.microsoft.com/en-us/ef/core/dbcontext-configuration/",
      "type": "article",
      "author": "Microsoft",
      "description": "Deep dive into DbContext configuration, lifetime, and best practices."
    }
  ]
}
