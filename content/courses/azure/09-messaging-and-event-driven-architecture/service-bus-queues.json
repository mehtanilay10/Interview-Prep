{
  "id": "lesson-azure-service-bus-queues",
  "slug": "service-bus-queues",
  "moduleSlug": "09-messaging-and-event-driven-architecture",
  "courseSlug": "azure",
  "title": "Working with Service Bus Queues",
  "description": "Create and use Service Bus queues to reliably send and receive messages in order.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 14,
  "tags": [
    "azure",
    "azure",
    "messaging",
    "service-bus",
    "developer"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating a Queue"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A Service Bus queue stores messages until a receiver is ready to process them. You can configure settings such as message time-to-live (TTL), maximum queue size, and whether duplicate detection is enabled."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create a Service Bus Queue (Azure CLI)",
        "content": "Create a queue named orders inside your namespace:",
        "code": "az servicebus queue create \\\n  --resource-group myResourceGroup \\\n  --namespace-name myNamespace \\\n  --name orders \\\n  --max-size-in-megabytes 1024 \\\n  --default-message-time-to-live P14D \\\n  --duplicate-detection-history-time-window PT10M",
        "language": "bash"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Sending a Message"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "To send a message, your application creates a ServiceBusClient, builds a ServiceBusMessage, and sends it using the sender. Messages can be plain text, JSON, or binary data."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Send a Message (JavaScript/TypeScript)",
        "content": "Example using the @azure/service-bus library:",
        "code": "import { ServiceBusClient } from \"@azure/service-bus\";\n\nconst connectionString = process.env.SERVICE_BUS_CONNECTION_STRING;\nconst queueName = \"orders\";\n\nconst sbClient = new ServiceBusClient(connectionString);\nconst sender = sbClient.createSender(queueName);\n\nasync function sendOrderMessage(order) {\n  const message = {\n    body: JSON.stringify(order),\n    contentType: \"application/json\",\n    messageId: order.id\n  };\n\n  await sender.sendMessages(message);\n  console.log(\"Message sent:\", order.id);\n  await sender.close();\n  await sbClient.close();\n}",
        "language": "typescript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Receiving a Message"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Receivers can use the peek-lock mode, which locks a message for a period while the receiver processes it. If processing succeeds, the receiver completes the message. If it fails, the message can be abandoned so another receiver can try."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Receive and Process Messages (JavaScript/TypeScript)",
        "content": "Receive messages from a queue using a processor:",
        "code": "import { ServiceBusClient } from \"@azure/service-bus\";\n\nconst connectionString = process.env.SERVICE_BUS_CONNECTION_STRING;\nconst queueName = \"orders\";\n\nconst sbClient = new ServiceBusClient(connectionString);\nconst processor = sbClient.createProcessor(queueName, {\n  maxConcurrentCalls: 1,\n  autoCompleteMessages: false\n});\n\nprocessor.subscribe({\n  async processMessage(message) {\n    const order = JSON.parse(message.body);\n    console.log(\"Processing order:\", order.id);\n    await processor.completeMessage(message);\n  },\n  async processError(error) {\n    console.error(\"Error in message processing:\", error);\n  }\n});",
        "language": "typescript"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Peek-Lock",
            "definition": "A receive mode where the broker locks a message so no other receiver can process it simultaneously."
          },
          {
            "term": "Complete",
            "definition": "Tell Service Bus that a message was processed successfully and remove it from the queue."
          },
          {
            "term": "Abandon",
            "definition": "Release the lock on a message so it becomes available again for another receiver."
          },
          {
            "term": "TTL",
            "definition": "Time-to-live: how long a message stays in the queue before it expires."
          }
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "m9-lesson3-queue-flow",
        "caption": "Queue message lifecycle",
        "definition": "graph TD\n    A[Producer sends message] --> B[Message stored in queue]\n    B --> C{Receiver available?}\n    C -->|Yes| D[Message locked and delivered]\n    D --> E{Processing succeeded?}\n    E -->|Yes| F[Complete: message removed]\n    E -->|No| G[Abandon: lock released]\n    G --> B\n    C -->|No| B"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Configure a Queue for E-Commerce Orders",
        "description": "Design the right queue settings for an order-processing system that must guarantee ordered processing and no message loss.",
        "steps": [
          "Set the max-size-in-megabytes to support 100,000 pending orders (approximately 1 KB each).",
          "Enable duplicate detection with a 5-minute window.",
          "Set a TTL so messages expire after 7 days if not processed.",
          "Enable partitioning for higher throughput."
        ],
        "expectedOutcome": "A queue configured with 1024 MB max size, duplicate-detection-history-time-window PT5M, default-message-time-to-live P7D, and enable-partitioning true."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Lesson Checkpoint",
        "items": [
          {
            "text": "I can explain what a Service Bus queue is and when to use it."
          },
          {
            "text": "I can create a queue using Azure CLI."
          },
          {
            "text": "I understand the peek-lock receive mode."
          },
          {
            "text": "I can send and receive messages using the Service Bus SDK."
          }
        ]
      }
    }
  ]
}