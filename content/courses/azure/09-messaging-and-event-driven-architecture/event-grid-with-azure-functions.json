{
  "id": "lesson-azure-event-grid-with-azure-functions",
  "slug": "event-grid-with-azure-functions",
  "moduleSlug": "09-messaging-and-event-driven-architecture",
  "courseSlug": "azure",
  "title": "Handling Event Grid Events with Azure Functions",
  "description": "Build event handlers in Azure Functions that respond to Event Grid events automatically.",
  "order": 10,
  "difficulty": "beginner",
  "estimatedMinutes": 13,
  "tags": [
    "azure",
    "azure",
    "messaging",
    "events",
    "event-grid",
    "azure-functions",
    "developer"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Event Grid Trigger in Azure Functions"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Azure Functions has a built-in Event Grid trigger that automatically validates your endpoint and delivers events. The function receives an array of events, and you can use the event type and data to route logic accordingly."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Event Grid Trigger Function (C#)",
        "content": "Handle multiple event types in one function:",
        "code": "using Microsoft.Azure.WebJobs;\nusing Microsoft.Extensions.Logging;\nusing Microsoft.Azure.EventGrid.Models;\n\npublic static class EventGridHandler\n{\n    [FunctionName(\"EventGridHandler\")]\n    public static void Run(\n        [EventGridTrigger] EventGridEvent[] events,\n        ILogger log)\n    {\n        foreach (var evt in events)\n        {\n            log.LogInformation($\"Event: {evt.EventType}, Subject: {evt.Subject}\");\n\n            if (evt.EventType == \"Microsoft.Storage.BlobCreated\")\n            {\n                var data = evt.Data.ToObjectFromJson<StorageBlobCreatedEventData>();\n                log.LogInformation($\"Blob URL: {data.Url}\");\n                // Process the blob\n            }\n        }\n    }\n}",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Webhook Validation Handshake"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When Event Grid delivers events to a webhook endpoint, it first sends a validation event. Your function must respond with the validation code to prove it owns the endpoint. The Event Grid trigger handles this automatically."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "important",
        "title": "Validation Is Automatic",
        "text": "The Event Grid trigger in Azure Functions automatically handles the validation handshake. Do not manually process validation events when using the trigger binding."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "EventGridTrigger",
            "definition": "A Functions binding that invokes a function when Event Grid delivers an event."
          },
          {
            "term": "Validation Event",
            "definition": "A special handshake event Event Grid sends to confirm your endpoint is valid."
          },
          {
            "term": "Dead-Letter",
            "definition": "Events that could not be delivered after retries are stored in a Storage blob if configured."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Event Grid Trigger (JavaScript)",
        "content": "Handle events in a JavaScript Azure Function:",
        "code": "module.exports = async function (context, eventGridEvents) {\n  for (const event of eventGridEvents) {\n    context.log(`Event type: ${event.eventType}`);\n    context.log(`Subject: ${event.subject}`);\n    context.log(`Data: ${JSON.stringify(event.data)}`);\n\n    if (event.eventType === \"order.created\") {\n      await processNewOrder(event.data);\n    }\n  }\n};",
        "language": "javascript"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "m9-lesson10-func-flow",
        "caption": "Event Grid triggering an Azure Function",
        "definition": "sequenceDiagram\n    participant Source as Event Source\n    participant EG as Event Grid\n    participant F as Azure Function\n    Source->>EG: Publish event\n    EG->>F: Validation handshake\n    F-->>EG: Validation response\n    EG->>F: Deliver event\n    F->>F: Process event"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build a Thumbnail Generator Function",
        "description": "Create a function that triggers on blob uploads and logs image metadata.",
        "steps": [
          "Create an Event Grid trigger function.",
          "Filter for Microsoft.Storage.BlobCreated events.",
          "Log the blob URL, content type, and size.",
          "Return a success status to acknowledge the event."
        ],
        "expectedOutcome": "A function that logs every new image upload event from Blob Storage."
      }
    }
  ]
}