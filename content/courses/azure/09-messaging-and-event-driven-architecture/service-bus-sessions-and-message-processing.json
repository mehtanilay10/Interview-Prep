{
  "id": "lesson-azure-service-bus-sessions-and-message-processing",
  "slug": "service-bus-sessions-and-message-processing",
  "moduleSlug": "09-messaging-and-event-driven-architecture",
  "courseSlug": "azure",
  "title": "Service Bus Sessions and Ordered Message Processing",
  "description": "Use sessions to guarantee ordered processing of related messages by the same receiver.",
  "order": 6,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": [
    "azure",
    "azure",
    "messaging",
    "service-bus",
    "developer"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What Are Sessions?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A session is a grouping mechanism for messages. When messages share the same session ID, Service Bus delivers them to the same receiver in order. This is useful for workflows where messages must be processed sequentially, such as an order that has multiple steps (authorize, charge, ship)."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Session ID",
            "definition": "A user-defined property that groups related messages together."
          },
          {
            "term": "Session Lock",
            "definition": "An exclusive lock on a session so only one receiver processes it at a time."
          },
          {
            "term": "Concurrency",
            "definition": "The number of sessions that can be processed simultaneously by the same receiver."
          },
          {
            "term": "State",
            "definition": "Optional per-session state stored by Service Bus for the duration of the session."
          }
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Send Messages with a Session ID (JavaScript)",
        "content": "Group messages into a session by setting the sessionId property:",
        "code": "const orders = [\n  { id: \"order-1\", step: \"authorize\", total: 500 },\n  { id: \"order-1\", step: \"charge\", total: 500 },\n  { id: \"order-1\", step: \"ship\", total: 500 }\n];\n\nfor (const order of orders) {\n  await sender.sendMessages({\n    body: JSON.stringify(order),\n    contentType: \"application/json\",\n    sessionId: order.id  // Groups all steps for this order\n  });\n}",
        "language": "typescript"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Receiving Session Messages"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you use session-aware receivers, Service Bus ensures all messages with the same sessionId go to the same receiver. This preserves order without requiring you to manage sequencing yourself."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Receive Session Messages (JavaScript)",
        "content": "Use a session-aware processor:",
        "code": "import { ServiceBusClient } from \"@azure/service-bus\";\n\nconst client = new ServiceBusClient(connectionString);\nconst processor = client.createSessionProcessor(\"orders\", {\n  maxConcurrentSessions: 5,\n  autoCompleteMessages: false\n});\n\nprocessor.subscribe({\n  async processMessage(message) {\n    const order = JSON.parse(message.body);\n    console.log(`Processing order ${message.sessionId} step ${order.step}`);\n    await processor.completeMessage(message);\n  }\n});",
        "language": "typescript"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "m9-lesson6-sessions",
        "caption": "Session-based message processing",
        "definition": "graph TD\n    A[Send session A messages] --> Q[Queue]\n    B[Send session B messages] --> Q\n    Q --> R[Session-aware Receiver]\n    R -->|Lock Session A| SA[Process Session A in order]\n    R -->|Lock Session B| SB[Process Session B in order]\n    SA --> A1[msg A1] --> A2[msg A2] --> A3[msg A3]\n    SB --> B1[msg B1] --> B2[msg B2]"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Session Best Practice",
        "text": "Set maxConcurrentSessions based on your receiver capacity. Too low and you waste throughput; too high and you risk overwhelming downstream systems."
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Implement Session-Based Order Processing",
        "description": "Configure a session-aware receiver for an order-processing queue.",
        "steps": [
          "Set the sessionId on each order message to the order ID.",
          "Configure the processor with maxConcurrentSessions: 3.",
          "Log each message step and session ID.",
          "Complete each message after successful processing."
        ],
        "expectedOutcome": "A working session-aware receiver that processes up to 3 orders concurrently in order."
      }
    }
  ]
}