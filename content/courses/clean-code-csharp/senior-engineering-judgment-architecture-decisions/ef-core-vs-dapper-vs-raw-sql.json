{
  "id": "lesson-clean-code-csharp-senior-engineering-judgment-architecture-decisions-ef-core-vs-dapper-vs-raw-sql",
  "slug": "ef-core-vs-dapper-vs-raw-sql",
  "moduleSlug": "senior-engineering-judgment-architecture-decisions",
  "courseSlug": "clean-code-csharp",
  "title": "EF Core vs Dapper vs Raw SQL",
  "description": "Learn to choose the right data access strategy in C# by understanding the trade-offs between EF Core's productivity, Dapper's performance, and raw SQL's control.",
  "order": 15,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "ef-core",
    "dapper",
    "sql",
    "csharp"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Every C# application must read and write data, and the choice of data access library is one of the most consequential architecture decisions. EF Core offers productivity, change tracking, and LINQ queries. Dapper offers raw performance and explicit SQL control. Raw ADO.NET offers maximum control but maximum boilerplate. None is universally best. The senior engineer chooses the tool based on the query complexity, performance requirements, and team expertise."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A common mistake is using EF Core for every query because it is the default in new projects. EF Core is excellent for domain modeling, change tracking, and standard CRUD operations. But for high-throughput read paths, complex reporting queries, or bulk operations, EF Core's abstraction can become a performance bottleneck. Dapper or raw SQL are not regressions; they are deliberate choices that match the tool to the workload."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The Three Options"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Data access library comparison",
        "cards": [
          {
            "title": "EF Core",
            "description": "Full-featured ORM with LINQ, migrations, and change tracking.",
            "pros": [
              "Highest developer productivity",
              "Change tracking and lazy loading",
              "Database-agnostic migrations",
              "LINQ integration with C#"
            ],
            "cons": [
              "Query performance can lag raw SQL",
              "Complex queries generate suboptimal SQL",
              "Learning curve for advanced scenarios",
              "Overhead for simple read-heavy paths"
            ]
          },
          {
            "title": "Dapper",
            "description": "Micro-ORM that maps SQL results to objects with minimal overhead.",
            "pros": [
              "Near-raw SQL performance",
              "Explicit, readable queries",
              "Multiple result set mapping",
              "Very lightweight (~100KB)"
            ],
            "cons": [
              "No change tracking",
              "No built-in migrations",
              "Manual SQL writing required",
              "Less protection from SQL injection if careless"
            ]
          },
          {
            "title": "Raw ADO.NET",
            "description": "Direct use of SqlConnection, SqlCommand, and data readers.",
            "pros": [
              "Maximum control and performance",
              "No abstraction overhead",
              "Full access to SQL Server features",
              "Predictable execution plans"
            ],
            "cons": [
              "Verbose and error-prone",
              "Manual object mapping required",
              "Connection management burden",
              "Harder to test and maintain"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Decision Framework"
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "When to use each data access strategy",
        "items": [
          "Use EF Core when the domain is complex, change tracking is valuable, or the team prioritizes development speed over raw query performance.",
          "Use Dapper when queries are performance-critical, the SQL is complex or hand-tuned, or you need multi-mapping for stored procedures.",
          "Use raw ADO.NET only for extreme edge cases: bulk operations, streaming large datasets, or when Dapper's abstraction is still too heavy."
        ]
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Hybrid approach is common and recommended",
        "text": "Many production C# applications use EF Core for most data access and Dapper for specific hot paths. This is not a compromise; it is a deliberate optimization. EF Core handles the domain model, while Dapper handles the high-throughput queries that need every millisecond of performance."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "C# Example: EF Core Query"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "EF Core with LINQ",
        "content": "A typical LINQ query with eager loading and filtering.",
        "language": "csharp",
        "code": "public async Task<IReadOnlyList<OrderDto>> GetRecentOrdersAsync(\n    Guid customerId,\n    DateTime since,\n    CancellationToken ct)\n{\n    return await _context.Orders\n        .Where(o => o.CustomerId == customerId && o.CreatedAt >= since)\n        .Include(o => o.Items)\n        .OrderByDescending(o => o.CreatedAt)\n        .Select(o => new OrderDto\n        {\n            Id = o.Id,\n            Total = o.Total,\n            ItemCount = o.Items.Count,\n            CreatedAt = o.CreatedAt\n        })\n        .AsNoTracking()\n        .ToListAsync(ct);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "C# Example: Dapper Query"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Dapper executes raw SQL and maps the results to objects. The SQL is explicit, which means you can see exactly what hits the database and optimize it directly."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dapper with multi-mapping",
        "content": "A high-performance Dapper query that maps one-to-many relationships in a single round-trip.",
        "language": "csharp",
        "code": "public async Task<IReadOnlyList<OrderWithItems>> GetRecentOrdersAsync(\n    Guid customerId,\n    DateTime since,\n    CancellationToken ct)\n{\n    const string sql = @\"\n        SELECT o.Id, o.Total, o.CreatedAt, i.Id, i.Sku, i.Quantity, i.Price\n        FROM Orders o\n        INNER JOIN OrderItems i ON i.OrderId = o.Id\n        WHERE o.CustomerId = @CustomerId AND o.CreatedAt >= @Since\n        ORDER BY o.CreatedAt DESC\";\n\n    var orderLookup = new Dictionary<Guid, OrderWithItems>();\n\n    await _connection.QueryAsync<Order, OrderItem, OrderWithItems>(\n        sql,\n        (order, item) =>\n        {\n            if (!orderLookup.TryGetValue(order.Id, out var existing))\n            {\n                existing = new OrderWithItems\n                {\n                    Id = order.Id,\n                    Total = order.Total,\n                    CreatedAt = order.CreatedAt,\n                    Items = new List<OrderItem>()\n                };\n                orderLookup.Add(order.Id, existing);\n            }\n            existing.Items.Add(item);\n            return existing;\n        },\n        new { CustomerId = customerId, Since = since },\n        splitOn: \"Id\");\n\n    return orderLookup.Values.ToList();\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Performance Considerations"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "EF Core has improved dramatically in recent versions, but it still generates SQL that is not always optimal. The N+1 query problem, where a loop triggers a database call per item, is the most common performance pitfall. Dapper eliminates this by giving you full control over the SQL. If you use EF Core, always profile the generated SQL with logging or a tool like MiniProfiler to ensure it matches your expectations."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "N+1 query",
            "definition": "A performance anti-pattern where fetching N parent records triggers N+1 database queries for their children."
          },
          {
            "term": "Change tracking",
            "definition": "EF Core's mechanism for monitoring entity changes and generating UPDATE statements automatically."
          },
          {
            "term": "Split query",
            "definition": "An EF Core feature that splits a single LINQ query with Include into multiple SQL queries to avoid Cartesian explosion."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Profile and optimize a data access query",
        "description": "Take an existing EF Core query and evaluate whether Dapper or raw SQL would be more appropriate.",
        "steps": [
          "Find a data access method in your project that uses EF Core.",
          "Enable SQL logging and capture the generated SQL for a representative request.",
          "Analyze the SQL for N+1 queries, unnecessary joins, or missing indexes.",
          "Rewrite the same query using Dapper with explicit SQL.",
          "Compare the execution plans and measure the performance difference.",
          "Decide whether to keep EF Core, switch to Dapper, or split: EF Core for simple paths, Dapper for the hot path."
        ],
        "expectedOutcome": "A side-by-side comparison of EF Core and Dapper for the same query, with a justified recommendation."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "EF Core is best for domain-heavy code where productivity and change tracking matter.",
          "Dapper is best for high-throughput, complex queries where SQL control and performance matter.",
          "Raw ADO.NET is rarely needed; Dapper covers almost every case where EF Core is too slow.",
          "The hybrid approach is common and recommended: EF Core for the domain, Dapper for hot paths."
        ],
        "takeaway": "Data access is not a one-size-fits-all decision. Senior engineers match the tool to the workload, using the right library for each bounded context and query pattern."
      }
    }
  ],
  "relatedLessons": [
    "sql-vs-nosql-decision-framework",
    "synchronous-vs-asynchronous-workflows"
  ],
  "furtherReading": [
    {
      "title": "EF Core Performance Best Practices",
      "url": "https://learn.microsoft.com/en-us/ef/core/performance/",
      "type": "article",
      "author": "Microsoft",
      "description": "Official Entity Framework Core performance guidance including query optimization and avoiding common pitfalls."
    },
    {
      "title": "Dapper Documentation",
      "url": "https://github.com/DapperLib/Dapper",
      "type": "tool",
      "author": "Stack Exchange",
      "description": "Dapper is a simple object mapper for .NET, extending the IDbConnection interface."
    }
  ]
}