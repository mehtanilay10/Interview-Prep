{
  "id": "lesson-git-linux-developer-workflow-advanced-git-workflows-git-internals",
  "slug": "git-internals",
  "moduleSlug": "advanced-git-workflows",
  "courseSlug": "git-linux-developer-workflow",
  "title": "Git Internals",
  "description": "Understand Git's internal object model, references, and plumbing commands to debug and optimize advanced workflows.",
  "order": 1,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Git is often treated as a black box, but its internal design is surprisingly elegant. Everything in Git is stored as objects in a content-addressable database. Understanding this model helps you diagnose repository corruption, recover data, and write scripts that interact directly with Git's storage layer."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The four Git object types"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every piece of data in a Git repository is stored as one of four object types inside the `.git/objects` directory. Git uses SHA-1 hashes as object IDs, so the contents determine the address."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "**blob** — Raw file content. A blob has no filename or metadata; it is just bytes.",
          "**tree** — A directory listing. It maps filenames to blobs (or other trees) and stores permissions and mode bits.",
          "**commit** — A snapshot with an author, committer, message, parent commits, and a pointer to a top-level tree.",
          "**tag** — A signed or unsigned reference to another object, usually a commit, used for releases."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "How commits are connected"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A commit does not store diffs. It stores a pointer to a tree plus parent commit IDs. This means every commit contains the full logical state of the project at that moment, and Git reconstructs changes by comparing commits on demand."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Inspect objects with git cat-file",
        "language": "bash",
        "code": "# Show the type and size of the latest commit\ngit cat-file -t HEAD\ngit cat-file -s HEAD\n\n# Pretty-print a commit\ngit cat-file -p HEAD\n\n# Show the tree referenced by a commit\ngit ls-tree HEAD\n\n# Find the blob for a specific file\ngit ls-tree HEAD src/Program.cs"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "References (refs)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "References are human-friendly pointers to commits. Branches and tags are refs stored in `.git/refs/heads/` and `.git/refs/tags/`. The special ref `HEAD` points to the current branch (or directly to a commit in detached HEAD state)."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Work with refs directly",
        "language": "bash",
        "code": "# See where HEAD points\ngit symbolic-ref HEAD\n\n# See all refs\ngit for-each-ref\n\n# Create a ref manually (rare, but useful in recovery)\necho <commit-sha> > .git/refs/heads/recovery-branch\n\ngit update-ref refs/heads/recovery-branch <commit-sha>"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Plumbing commands are powerful and unforgiving",
        "text": "Commands like `git cat-file`, `git hash-object`, and `git update-ref` operate below Git's safety checks. Use them carefully, especially on shared repositories. Always make a backup before modifying refs or objects directly."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "The reflog: your safety net"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Every time a ref moves, Git records the old and new values in the reflog. The reflog is local to your clone and expires after 90 days by default. It is the primary tool for recovering from force pushes, accidental resets, and rebase disasters."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Use the reflog to recover a lost commit",
        "language": "bash",
        "code": "# View HEAD reflog\ngit reflog\n\n# Recover a branch from a reflog entry\ngit branch recovery HEAD@{5}\n\n# Restore a deleted file from history\ngit checkout HEAD~3 -- path/to/file"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Pack files and garbage collection"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "As a repository grows, Git consolidates loose objects into pack files for efficiency. `git gc` (garbage collection) runs pack compression, prunes unreachable objects, and updates reflogs. Run it manually on large repositories after major history rewrites or bulk imports."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Git stores everything as blobs, trees, commits, and tags in a content-addressable store.",
          "Commits point to trees, not diffs; Git calculates differences dynamically.",
          "Refs are human-friendly pointers to commits; branches and tags are refs.",
          "The reflog records every ref movement and is your best recovery tool.",
          "Pack files and git gc keep large repositories performant."
        ]
      }
    }
  ],
  "relatedLessons": [
    "backup-and-recovery",
    "large-repositories",
    "migration-strategies"
  ],
  "furtherReading": [
    { "title": "Git internals: plumbing and porcelain (git-scm.com)", "url": "https://git-scm.com/book/en/v2/Git-Internals-Plumbing-and-Porcelain" },
    { "title": "Git object model (git-scm.com)", "url": "https://git-scm.com/book/en/v2/Git-Internals-Git-Objects" }
  ]
}
