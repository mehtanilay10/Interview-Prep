{
  "id": "lesson-git-linux-developer-workflow-advanced-git-workflows-hooks-and-automation",
  "slug": "hooks-and-automation",
  "moduleSlug": "advanced-git-workflows",
  "courseSlug": "git-linux-developer-workflow",
  "title": "Hooks and Automation",
  "description": "Use Git hooks and automation tooling to enforce standards, run checks, and streamline repetitive workflow tasks.",
  "order": 2,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Git hooks are scripts that run automatically before or after Git events such as commits, pushes, and merges. They let you enforce formatting, run tests, block secrets, and keep history clean without relying on every developer to remember manual steps."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Client-side hooks"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Client-side hooks live in `.git/hooks/` and run on your machine. They are not shared by default, so teams often manage them with a tool like `husky` or a setup script."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "**pre-commit** — Runs before a commit is created. Ideal for formatting and linting.",
          "**commit-msg** — Receives the commit message path. Use it to enforce conventional commits or reject secrets in messages.",
          "**pre-push** — Runs before data is sent to a remote. Good for blocking pushes with failing tests or large files."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Server-side hooks"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Server-side hooks run on the remote repository host. They enforce policy regardless of which client connects. The most important one is `pre-receive`, which can reject pushes entirely."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Pre-commit hook that blocks secrets",
        "language": "bash",
        "code": "#!/bin/bash\n# .git/hooks/pre-commit\n\nif git diff --cached --name-only | xargs grep -n \"API_KEY\"; then\n  echo \"ERROR: API_KEY found in staged files\"\n  exit 1\nfi"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Automation with git-interactive"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For complex branching, tagging, and release workflows, automation tools like `git-interactive` or custom shell functions reduce repetition. The goal is to make the right workflow the easiest workflow."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Automated release script",
        "language": "bash",
        "code": "#!/bin/bash\nset -euo pipefail\n\nVERSION=$(cat package.json | jq -r '.version')\nBRANCH=$(git rev-parse --abbrev-ref HEAD)\n\nif [ \"$BRANCH\" != \"main\" ]; then\n  echo \"Release only from main\"\n  exit 1\nfi\n\ngit pull --rebase\n\ngit tag -a \"v$VERSION\" -m \"Release $VERSION\"\ngit push origin \"v$VERSION\"\n\necho \"Released v$VERSION\""
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Make hooks versioned",
        "text": "Store hook scripts in a `scripts/` directory and install them during project setup. This ensures every team member uses the same rules without depending on manual `.git/hooks/` setup."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Client-side hooks run locally; server-side hooks enforce policy centrally.",
          "Use `pre-commit` for fast checks and `pre-push` for expensive checks.",
          "Automate repetitive Git tasks with scripts to reduce human error.",
          "Version your hook scripts so the team stays in sync."
        ]
      }
    }
  ],
  "relatedLessons": [
    "git-internals",
    "git-best-practices",
    "git-and-ci-cd"
  ],
  "furtherReading": [
    { "title": "Git hooks documentation (git-scm.com)", "url": "https://git-scm.com/book/en/v2/Customizing-Git-Git-Hooks" },
    { "title": "Husky (npm package)", "url": "https://typicode.github.io/husky/" }
  ]
}
