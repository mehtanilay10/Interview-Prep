{
  "id": "lesson-devops-production-docker-container-security-container-secrets",
  "slug": "container-secrets",
  "moduleSlug": "production-docker-container-security",
  "courseSlug": "devops",
  "title": "Container secrets",
  "description": "Handle API keys, database passwords, and TLS certificates safely inside containers using Docker secrets, environment variables, and mounted secrets.",
  "order": 8,
  "difficulty": "advanced",
  "estimatedMinutes": 35,
  "tags": ["docker", "secrets", "security", "credentials"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Every production container needs secrets: database passwords, API keys, TLS certificates, and OAuth client secrets. The way you pass these secrets into a container determines whether an attacker who gains access to the container can also access your production database, third-party APIs, or certificate authorities."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why environment variables are not secrets"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Environment variables are the most common way to pass configuration to containers, but they are not a security control. Environment variables are visible in `docker inspect`, in process listings inside the container, and in crash dumps. They are also often logged accidentally by application frameworks."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "danger",
        "title": "Environment variables leak easily",
        "text": "Anyone with access to the Docker host can run `docker inspect <container>` and read every environment variable, including secrets. Logging frameworks, crash reporters, and error pages may also print environment variables into logs or error output."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Docker secrets"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Docker secrets are designed specifically for sensitive data. A secret is stored encrypted in the Raft log of a Docker Swarm manager and is decrypted only inside the containers that receive it. On Linux, secrets are exposed as in-memory files under `/run/secrets/<name>` rather than as environment variables, which makes accidental logging less likely."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using Docker secrets with Compose",
        "content": "This Compose file defines a secret from a local file and mounts it into the container.",
        "language": "yaml",
        "code": "services:\n  database:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_PASSWORD_FILE: /run/secrets/db_password\n    secrets:\n      - db_password\n    networks:\n      - data\n    read_only: true\n    tmpfs:\n      - /tmp\n      - /run/postgresql\n\nsecrets:\n  db_password:\n    file: ./secrets/db_password.txt\n\nnetworks:\n  data:\n    internal: true"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Docker secrets rules",
        "items": [
          "Secrets require Swarm mode. Enable it with `docker swarm init` on at least one manager node",
          "Secrets are scoped to services. A service can only access secrets explicitly listed in its `secrets` section",
          "Secret files appear under /run/secrets/<name> in the container, not as environment variables",
          "Secret files are stored in a tmpfs mount and never written to the container's writable layer"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Kubernetes secrets"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "In Kubernetes, secrets are API objects. By default they are stored as base64-encoded strings in etcd, which means you must encrypt etcd at rest. Kubernetes can inject secrets as environment variables or as mounted files. Mounted files are generally safer because they do not appear in process environment tables."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Kubernetes secret as a mounted file",
        "content": "This Pod spec mounts a Kubernetes secret as a file at /run/secrets/db-password.",
        "language": "yaml",
        "code": "apiVersion: v1\nkind: Pod\nmetadata:\n  name: database-client\nspec:\n  containers:\n    - name: app\n      image: myapp:latest\n      volumeMounts:\n        - name: db-password\n          mountPath: /run/secrets\n          readOnly: true\n      env:\n        - name: DB_PASSWORD_FILE\n          value: /run/secrets/db-password\n  volumes:\n    - name: db-password\n      secret:\n        secretName: db-password"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "External secret managers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For large-scale deployments, use a dedicated secret manager such as HashiCorp Vault, AWS Secrets Manager, Azure Key Vault, or Google Secret Manager. These tools provide audit logging, automatic rotation, fine-grained access control, and encryption at rest without requiring you to store secrets in container definitions."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Secret management options",
        "cards": [
          {
            "title": "Docker secrets",
            "description": "Built-in encrypted storage for Swarm clusters.",
            "pros": ["Built into Docker", "Encrypted in transit and at rest", "Simple file-based injection"],
            "cons": ["Requires Swarm", "Limited audit capabilities", "No automatic rotation"]
          },
          {
            "title": "Kubernetes secrets",
            "description": "Native Kubernetes secret objects with base64 encoding.",
            "pros": ["Kubernetes-native", "Works with operators and CSI drivers", "Broad ecosystem support"],
            "cons": ["Base64 is not encryption", "Requires etcd encryption configuration", "Mount as env vars is risky"]
          },
          {
            "title": "External secret manager",
            "description": "Dedicated services such as Vault, AWS Secrets Manager, or Azure Key Vault.",
            "pros": ["Audit logging", "Automatic rotation", "Fine-grained access control", "Encryption by default"],
            "cons": ["Additional infrastructure", "Network call required", "More complex setup"]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Avoiding secret sprawl"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "As your application grows, the number of secrets grows. Secret sprawl makes rotation difficult, auditing impossible, and breaches catastrophic. Follow these rules to keep secrets under control."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "Secret hygiene rules",
        "items": [
          "Store secrets in a single secret manager, not scattered across environment variables, config files, and CI/CD variables",
          "Rotate secrets on a regular schedule and after any suspected exposure",
          "Scope secrets to the minimum set of services that need them",
          "Never commit secrets to version control, including private repositories",
          "Scan repositories for accidentally committed secrets with tools such as gitleaks or trufflehog"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Environment variables are not secrets; they are visible to anyone with Docker host access",
          "Docker secrets encrypt secrets at rest and inject them as files in Swarm",
          "Kubernetes secrets require etcd encryption and should be mounted as files",
          "External secret managers provide rotation, auditing, and fine-grained access control",
          "Never commit secrets to version control or store them in Docker images"
        ],
        "takeaway": "Secrets belong in a dedicated secret manager, not in environment variables, config files, or Docker images."
      }
    }
  ],
  "relatedLessons": ["volumes-and-persistent-data", "container-networking", "image-scanning-and-supply-chain-security"],
  "furtherReading": [
    {
      "title": "Docker documentation: Secrets",
      "url": "https://docs.docker.com/engine/swarm/secrets/",
      "type": "article",
      "author": "Docker"
    },
    {
      "title": "HashiCorp Vault",
      "url": "https://www.hashicorp.com/products/vault",
      "type": "tool",
      "author": "HashiCorp"
    },
    {
      "title": "Kubernetes secrets documentation",
      "url": "https://kubernetes.io/docs/concepts/configuration/secret/",
      "type": "article",
      "author": "Kubernetes"
    }
  ]
}
