{
  "id": "lesson-devops-004",
  "slug": "docker-compose-multi-container",
  "moduleSlug": "docker-advanced",
  "title": "Docker Compose for Multi-Container Apps",
  "description": "Run multi-container applications using Docker Compose and a single docker-compose.yml file.",
  "order": 1,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "docker-compose",
    "multi-container",
    "yaml"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is Docker Compose?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Docker Compose** is a tool for defining and running multi-container Docker applications. Instead of running several `docker run` commands, you describe all your services, networks, and volumes in a single `docker-compose.yml` file and let Compose handle the lifecycle."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A `docker-compose.yml` file is declarative — you describe the desired state (which containers you want, how they connect, and what configuration they need), and Compose figures out how to achieve it."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Basic docker-compose.yml structure"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "A simple two-service stack",
        "content": "This compose file defines a web application backed by a PostgreSQL database. Compose automatically creates a network and handles DNS between services.",
        "code": "services:\n  web:\n    build: ./.\n    ports:\n      - \"3000:3000\"\n    depends_on:\n      - db\n    environment:\n      DATABASE_URL: postgres://postgres:secret@db:5432/mydb\n\n  db:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_DB: mydb\n      POSTGRES_USER: postgres\n      POSTGRES_PASSWORD: secret\n    volumes:\n      - db-data:/var/lib/postgresql/data\n\nvolumes:\n  db-data:",
        "language": "yaml"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "service",
            "definition": "A container definition in Compose — it maps to a single image with its configuration."
          },
          {
            "term": "depends_on",
            "definition": "Specifies startup order dependencies between services."
          },
          {
            "term": "environment",
            "definition": "Sets environment variables inside the container."
          },
          {
            "term": "volumes",
            "definition": "Mounts persistent data or host directories into containers."
          }
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Compose vs docker run",
        "cards": [
          {
            "title": "Docker Compose",
            "description": "Define multi-container apps declaratively in a YAML file and manage them as a single stack.",
            "tags": [
              "Declarative",
              "Multi-service",
              "YAML"
            ],
            "pros": [
              "Single file for entire stack",
              "Built-in service discovery and DNS",
              "Easy to version and share"
            ],
            "cons": [
              "Extra layer of abstraction",
              "Learning curve for YAML schema"
            ]
          },
          {
            "title": "docker run",
            "description": "Imperative CLI command to create and start a single container with inline flags.",
            "tags": [
              "Imperative",
              "Single container",
              "CLI"
            ],
            "pros": [
              "Quick for one-off containers",
              "No extra files needed"
            ],
            "cons": [
              "Hard to manage many containers",
              "Configuration not versioned"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Essential Compose Commands"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Command",
          "Description"
        ],
        "rows": [
          [
            "docker compose up",
            "Start all services defined in the compose file"
          ],
          [
            "docker compose up -d",
            "Start services in detached mode (background)"
          ],
          [
            "docker compose down",
            "Stop and remove containers, networks, and volumes"
          ],
          [
            "docker compose ps",
            "List the status of all services in the stack"
          ],
          [
            "docker compose logs",
            "View logs for all or a specific service"
          ],
          [
            "docker compose build",
            "Build or rebuild service images"
          ],
          [
            "docker compose restart",
            "Restart all services"
          ],
          [
            "docker compose exec",
            "Run a command inside a running service container"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Starting and stopping a stack",
        "content": "Bring the stack up in the background, check status, interact with a service, and tear it down.",
        "code": "docker compose up -d\ndocker compose ps\ndocker compose exec web sh\ndocker compose logs -f web\ndocker compose stop\ndocker compose down -v",
        "language": "bash"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Compose v1 vs v2",
        "text": "The older syntax was `docker-compose` (hyphenated). Modern Docker uses the `docker compose` plugin (no hyphen). If you have an older installation, you may need to use `docker-compose` instead."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Service dependencies"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `depends_on` key controls the order in which services start. However, it only waits for the container to be *running* — not for the application inside to be *ready*. For production readiness, you should use health checks."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Health check in Compose",
        "content": "A health check tells Docker to periodically test the service before considering it ready.",
        "code": "services:\n  web:\n    build: .\n    ports:\n      - \"3000:3000\"\n    depends_on:\n      db:\n        condition: service_healthy\n\n  db:\n    image: postgres:16-alpine\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U postgres\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5",
        "language": "yaml"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Networks and Data Persistence"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "By default, Compose places all services on a single network and sets up DNS so services can reach each other by their service name. Named volumes (declared under the top-level `volumes:` key) ensure that data survives container restarts."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-compose-network",
        "caption": "Docker Compose creates an isolated network and lets services discover each other by name.",
        "definition": "graph LR\n    A[web service] -- DNS --> B[db service]\n    B -- volume --> C[(Named Volume)]\n    style A fill:#4D90DD,stroke:#333\n    style B fill:#4D90DD,stroke:#333\n    style C fill:#F0F0F0,stroke:#333"
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "exercise",
      "data": {
        "title": "Run a WordPress + MySQL stack",
        "description": "Practice Compose with a real multi-container application.",
        "steps": [
          "Create a `docker-compose.yml` with wordpress and mysql services.",
          "Map WordPress to port 8080 on the host.",
          "Set the MySQL root password via an environment variable.",
          "Run `docker compose up -d`.",
          "Open http://localhost:8080 in a browser and verify WordPress loads.",
          "Run `docker compose down -v` to clean up."
        ],
        "expectedOutcome": "WordPress is accessible in the browser and the MySQL data is stored in a named volume."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Docker Compose manages multi-container applications with a single YAML file.",
          "Services can reference each other by name — Compose handles DNS automatically.",
          "Use `depends_on` with health checks for proper startup ordering.",
          "Named volumes keep data persistent across container restarts and `docker compose down`."
        ],
        "takeaway": "Compose is the quickest way to run a full stack of dependent services for local development."
      }
    }
  ],
  "courseSlug": "devops"
}
