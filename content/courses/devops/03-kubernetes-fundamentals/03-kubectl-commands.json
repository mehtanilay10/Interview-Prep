{
  "id": "lesson-devops-008",
  "slug": "kubectl-commands",
  "moduleSlug": "kubernetes-fundamentals",
  "title": "Mastering kubectl",
  "description": "Learn essential kubectl commands for inspecting, debugging, and managing Kubernetes clusters.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 30,
  "tags": [
    "kubernetes",
    "kubectl",
    "cli",
    "debugging"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Introduction to kubectl"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "`kubectl` (abbreviated as `k`) is the command-line tool for interacting with Kubernetes clusters. You use it to deploy applications, inspect resources, debug issues, and manage the cluster lifecycle."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The general syntax is: `kubectl [command] [type] [name] [flags]`. Most commands follow the pattern: `kubectl <verb> <resource> <name-or-yaml>`."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Context",
            "definition": "A configuration group that includes a cluster, user, and namespace — `kubectl config use-context` switches between them."
          },
          {
            "term": "Namespace",
            "definition": "A virtual partition inside a cluster that isolates resources — useful for separating environments or teams."
          },
          {
            "term": "Resource",
            "definition": "An endpoint in the Kubernetes API that represents a cluster object like a Pod, Service, or Deployment."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Inspecting Resources"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Getting and describing resources",
        "content": "View cluster resources at different levels of detail.",
        "code": "kubectl get nodes\nkubectl get pods\nkubectl get pods -n kube-system\nkubectl get deploy,svc\nkubectl get all -o wide\nkubectl describe pod nginx-deployment-abc123",
        "language": "bash"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-kubectl-workflow",
        "caption": "Typical workflow when using kubectl to inspect resources.",
        "definition": "graph LR\n    A[User runs kubectl] --> B[kubectl reads kubeconfig]\n    B --> C[API Server validates]\n    C --> D[etcd returns data]\n    D --> E[kubectl formats output]\n    E --> F[User sees result]"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "get vs describe vs logs",
        "cards": [
          {
            "title": "get",
            "description": "Lists resources in a compact table format.",
            "tags": [
              "Quick overview"
            ],
            "pros": [
              "Fast, tabular output",
              "Supports multiple resource types"
            ],
            "cons": [
              "Limited detail",
              "No events or conditions"
            ]
          },
          {
            "title": "describe",
            "description": "Shows detailed information about a single resource, including events.",
            "tags": [
              "Deep inspection"
            ],
            "pros": [
              "Full resource details",
              "Shows events and conditions"
            ],
            "cons": [
              "One resource at a time",
              "Verbose output"
            ]
          },
          {
            "title": "logs",
            "description": "Streams stdout/stderr from a container in a Pod.",
            "tags": [
              "Debugging"
            ],
            "pros": [
              "Real-time log streaming",
              "Access to previous container logs"
            ],
            "cons": [
              "Pod-specific only",
              "No structured output"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Common flags"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "`-n` or `--namespace` — Scope the command to a specific namespace.",
          "`-o` or `--output` — Format output (yaml, wide, json, jsonpath).",
          "`-w` or `--watch` — Stream live updates.",
          "`-l` or `--selector` — Filter by label."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Creating and Deleting Resources"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Applying and deleting manifests",
        "content": "The standard workflow for managing Kubernetes resources declaratively.",
        "code": "kubectl apply -f app.yaml\nkubectl delete -f app.yaml\nkubectl delete deployment nginx-deployment\nkubectl delete pod bad-pod --grace-period=0",
        "language": "bash"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Debugging and Troubleshooting"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When something is wrong, these commands help you see what's happening inside the cluster."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Command",
          "Purpose"
        ],
        "rows": [
          [
            "kubectl logs <pod>",
            "View pod logs; add `-f` to follow"
          ],
          [
            "kubectl logs --previous <pod>",
            "View logs of a crashed container"
          ],
          [
            "kubectl exec -it <pod> -- /bin/sh",
            "Open an interactive shell in a running pod"
          ],
          [
            "kubectl describe <resource> <name>",
            "See detailed resource status and events"
          ],
          [
            "kubectl top pods",
            "View resource usage (requires Metrics Server)"
          ],
          [
            "kubectl get events --sort-by=.metadata.creationTimestamp",
            "See recent cluster events chronologically"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Debugging a failing pod",
        "content": "Inspect logs and events to diagnose common pod issues.",
        "code": "# Check pod status and events\nkubectl describe pod my-pod\n\n# View logs\nkubectl logs my-pod\nkubectl logs --previous my-pod\n\n# Shell into the pod\nkubectl exec -it my-pod -- /bin/sh",
        "language": "bash"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Useful output formats"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "kubectl can output results in multiple formats. `-o wide` adds extra columns, `-o yaml` and `-o json` print full resource definitions. You can also use `jsonpath` expressions to extract specific fields."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Output formatting",
        "content": "Extract specific fields from kubectl output.",
        "code": "kubectl get pods -o wide\nkubectl get pod my-pod -o yaml\nkubectl get nodes -o jsonpath='{.items[*].status.nodeInfo.kubeletVersion}'",
        "language": "bash"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "kubectl shortcuts",
        "text": "You can alias `k=kubectl` and enable auto-completion with `source <(kubectl completion bash)`. Many operators also use `--dry-run=client -o yaml` to preview manifests before applying."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "exercise",
      "data": {
        "title": "kubectl scavenger hunt",
        "description": "Practice the kubectl commands you have learned by exploring a live or local cluster.",
        "steps": [
          "List all namespaces and identify the system namespaces.",
          "Describe the kube-system namespace and note the running pods.",
          "Get detailed output for one pod using `kubectl describe`.",
          "Stream logs from a running pod using `kubectl logs -f`.",
          "Use a label selector to list only pods with `app=nginx`.",
          "Export one pod definition to YAML using `kubectl get pod <name> -o yaml`."
        ],
        "expectedOutcome": "You can navigate any Kubernetes cluster using `get`, `describe`, `logs`, and label selectors."
      }
    },
    {
      "type": "checklist",
      "data": {
        "title": "Commands to memorize",
        "items": [
          {
            "text": "What command lists all pods in the default namespace?",
            "hint": "kubectl get pods"
          },
          {
            "text": "What command applies a manifest file?",
            "hint": "kubectl apply -f"
          },
          {
            "text": "What flag shows detailed information about a resource?",
            "hint": "--dry-run or describe"
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Use `kubectl get` and `kubectl describe` to inspect resources at all levels.",
          "Always apply manifests with `kubectl apply -f` and clean up with `kubectl delete`.",
          "Use `kubectl logs` and `kubectl exec` for debugging running pods.",
          "Output flags like `-o wide` and `jsonpath` help you extract exactly what you need."
        ],
        "takeaway": "Mastering kubectl is essential for operating Kubernetes clusters efficiently."
      }
    }
  ],
  "courseSlug": "devops"
}
