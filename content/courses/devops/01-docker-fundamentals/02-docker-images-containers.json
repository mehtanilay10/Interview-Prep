{
  "id": "lesson-devops-002",
  "slug": "docker-images-containers",
  "moduleSlug": "docker-fundamentals",
  "title": "Docker Images, Containers & Basic Commands",
  "description": "Learn to manage Docker images and containers with essential docker commands.",
  "order": 2,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "docker",
    "images",
    "containers",
    "cli"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Docker Images"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **Docker image** is a read-only template that contains a snapshot of your application and its environment. Images are built in layers — each layer is a set of filesystem changes. When you run an image, Docker adds a writable layer on top, creating a **container**."
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Images are **immutable** — you cannot change a running image.",
          "Each image is identified by a **name:tag** (e.g., `nginx:1.25`).",
          "Images consist of multiple **layers** that Docker caches for fast rebuilds.",
          "An image is like a **class** in object-oriented programming; a container is like an **instance**."
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Listing and Inspecting Images"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Docker image commands",
        "content": "List local images, inspect one, and remove an unused image.",
        "code": "docker images\ndocker image ls\ndocker inspect nginx:latest\ndocker rmi nginx:1.25",
        "language": "bash"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Docker Containers"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **container** is a running instance of an image. It has its own isolated filesystem, network stack, and process space, but shares the host kernel. Containers are ephemeral — when you remove a container, any data written to its writable layer is lost."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Command",
          "Description"
        ],
        "rows": [
          [
            "docker run",
            "Create and start a new container from an image"
          ],
          [
            "docker start",
            "Start a stopped container"
          ],
          [
            "docker stop",
            "Gracefully stop a running container"
          ],
          [
            "docker restart",
            "Stop and start a container"
          ],
          [
            "docker rm",
            "Remove a stopped container"
          ],
          [
            "docker ps",
            "List running containers (use `-a` for all)"
          ],
          [
            "docker logs",
            "View container output logs"
          ],
          [
            "docker exec",
            "Run a command inside a running container"
          ]
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Managing containers",
        "content": "Run a container, inspect it, and interact with it in real time.",
        "code": "docker run -d --name myapp nginx\n\ndocker ps\ndocker inspect myapp\ndocker logs myapp\ndocker exec -it myapp /bin/sh\ndocker stop myapp\ndocker rm myapp",
        "language": "bash"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Pro tip: run and clean up",
        "text": "Use `--rm` with `docker run` to automatically remove the container when it stops. This is great for one-off tasks or testing."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Image Layers and Caching"
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Image",
            "definition": "A read-only template containing an application and its dependencies. Images are built in layers and identified by name:tag."
          },
          {
            "term": "Container",
            "definition": "A running instance of an image with a writable layer on top. Containers are ephemeral and isolated."
          },
          {
            "term": "Layer",
            "definition": "A filesystem change recorded during image build. Layers are cached and reused to make rebuilds fast."
          },
          {
            "term": "Tag",
            "definition": "A label applied to an image (e.g., `1.25`, `latest`) that identifies a specific version."
          },
          {
            "term": "Registry",
            "definition": "A storage and distribution service for Docker images, such as Docker Hub or a private registry."
          }
        ]
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Docker builds images in layers. Each instruction in a Dockerfile creates a new layer. When you rebuild an image, Docker reuses unchanged layers from its cache — this is what makes builds fast. But it also means the order of your Dockerfile instructions matters: put rarely-changing instructions at the top and frequently-changing ones at the bottom."
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-image-layers",
        "caption": "Docker images are made of stacked layers. Each layer is a diff of filesystem changes.",
        "definition": "graph TD\n    A[Base Layer: ubuntu:22.04] --> B[+ apt-get update]\n    B --> C[+ apt-get install nodejs]\n    C --> D[+ Copy app code]\n    D --> E[Writable Container Layer]\n    style E fill:#f9f,stroke:#333,stroke-width:2px"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Layer best practices"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Install dependencies before copying application code so code changes don't invalidate the dependency layer.",
          "Combine related RUN commands to reduce the number of layers.",
          "Use a small base image (e.g., `alpine`) to reduce image size.",
          "Avoid storing secrets or large temporary files in image layers."
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Images vs Containers",
        "cards": [
          {
            "title": "Images",
            "description": "Read-only templates that package application code and dependencies.",
            "tags": [
              "Static",
              "Reusable",
              "Layered"
            ],
            "pros": [
              "Can be versioned and shared",
              "Fast to distribute via registries",
              "Immutable and reproducible"
            ],
            "cons": [
              "Cannot be modified directly",
              "Require rebuild for changes"
            ]
          },
          {
            "title": "Containers",
            "description": "Running instances of images with an isolated, writable filesystem layer.",
            "tags": [
              "Ephemeral",
              "Isolated",
              "Runtime"
            ],
            "pros": [
              "Lightweight and fast to start",
              "Consistent across environments",
              "Easy to scale and manage"
            ],
            "cons": [
              "Changes are lost when removed",
              "Data must be persisted via volumes"
            ]
          }
        ]
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "exercise",
      "data": {
        "title": "Practice with Docker commands",
        "description": "Get hands-on experience with the most common Docker CLI commands.",
        "steps": [
          "Pull the official Redis image: `docker pull redis:7-alpine`",
          "Run it in the background: `docker run -d --name redis-test -p 6379:6379 redis:7-alpine`",
          "Check that it's running: `docker ps`",
          "Run `docker exec -it redis-test redis-cli ping` to verify Redis responds with PONG.",
          "Clean up: `docker stop redis-test && docker rm redis-test`"
        ],
        "expectedOutcome": "You should see 'PONG' confirming the Redis container is serving requests on port 6379."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Docker images are read-only, layered templates; containers are their running instances.",
          "Use `docker run` with flags like `-d`, `-p`, and `--name` to control container behavior.",
          "Image layers are cached, so ordering Dockerfile instructions for least-churn-at-top maximizes cache hits.",
          "Always clean up stopped containers to save disk space."
        ],
        "takeaway": "Master the docker CLI fundamentals — images, containers, and layers — before moving on to Dockerfiles."
      }
    }
  ],
  "courseSlug": "devops"
}
