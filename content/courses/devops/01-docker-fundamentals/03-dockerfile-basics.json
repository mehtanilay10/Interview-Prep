{
  "id": "lesson-devops-003",
  "slug": "dockerfile-basics",
  "moduleSlug": "docker-fundamentals",
  "title": "Writing Dockerfiles",
  "description": "Learn to write Dockerfiles with common instructions, best practices, and multi-stage builds.",
  "order": 3,
  "difficulty": "intermediate",
  "estimatedMinutes": 35,
  "tags": [
    "docker",
    "dockerfile",
    "best-practices"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What is a Dockerfile?"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "A **Dockerfile** is a plain-text file containing a set of instructions that Docker Engine uses to automatically build an image. Each instruction corresponds to a line in the file and creates a new layer in the resulting image."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "FROM",
            "definition": "Sets the base image for the next instructions. This should be the first instruction in a Dockerfile."
          },
          {
            "term": "RUN",
            "definition": "Executes a command during the build, creating a new layer. Used for installing packages and compiling code."
          },
          {
            "term": "COPY",
            "definition": "Copies files from a source on the host to the filesystem of the image."
          },
          {
            "term": "WORKDIR",
            "definition": "Sets the working directory inside the container. Equivalent to `cd` in a shell."
          },
          {
            "term": "CMD",
            "definition": "Provides default commands that run when a container starts. It can be overridden."
          },
          {
            "term": "ENTRYPOINT",
            "definition": "Configures a container to run as an executable. Unlike CMD, it is not easily overridable."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Common instructions"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Instruction",
          "Purpose",
          "Example"
        ],
        "rows": [
          [
            "FROM",
            "Set the base image",
            "FROM node:20-alpine"
          ],
          [
            "RUN",
            "Execute a command at build time",
            "RUN npm install"
          ],
          [
            "WORKDIR",
            "Set the working directory",
            "WORKDIR /app"
          ],
          [
            "COPY",
            "Copy files into the image",
            "COPY . ."
          ],
          [
            "EXPOSE",
            "Document which port the app listens on",
            "EXPOSE 3000"
          ],
          [
            "CMD",
            "Default command to run at startup",
            "CMD [\"npm\", \"start\"]"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Building a Node.js Dockerfile"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dockerfile for a Node.js app",
        "content": "This Dockerfile builds a container image for a Node.js application. It starts with an official base image, copies the dependencies file, installs packages, then copies the rest of the app.",
        "code": "FROM node:20-alpine\n\nWORKDIR /app\n\nCOPY package*.json ./\nRUN npm ci --only=production\n\nCOPY . .\n\nEXPOSE 3000\n\nCMD [\"node\", \"server.js\"]",
        "language": "dockerfile"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Notice the order: `COPY package*.json` comes before `COPY . .`. This means if only application code changes (but dependencies stay the same), Docker can use the cached layer of installed packages and skip reinstallation. This is a critical best practice."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Multi-Stage Builds"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "For compiled languages like TypeScript, Go, or C#, you often need a build tool installed during image creation but not at runtime. **Multi-stage builds** let you use multiple `FROM` statements in a single Dockerfile. Each stage can copy artifacts from a previous stage, keeping the final image small."
      }
    },
    {
      "type": "example",
      "data":
      {
        "title": "Multi-stage build for a TypeScript app",
        "content": "The first stage installs dependencies and builds the TypeScript code. The second stage copies only the compiled output, leaving behind all build tools.",
        "code": "FROM node:20-alpine AS builder\n\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine AS runtime\n\nWORKDIR /app\nCOPY --from=builder /app/dist ./dist\nCOPY --from=builder /app/package*.json ./\nRUN npm ci --only=production\n\nEXPOSE 3000\nCMD [\"node\", \"dist/index.js\"]",
        "language": "dockerfile"
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "diagram-dockerfile-build",
        "caption": "A Dockerfile builds an image in layers, from base image to final runtime.",
        "definition": "graph TD\n    A[Base Image] --> B[Install Dependencies]\n    B --> C[Copy App Code]\n    C --> D[Build Artifacts]\n    D --> E[Final Runtime Image]\n    style E fill:#f9f,stroke:#333,stroke-width:2px"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "COPY vs ADD",
        "cards": [
          {
            "title": "COPY",
            "description": "Copies files and directories from the host into the image filesystem.",
            "tags": [
              "Simple",
              "Recommended"
            ],
            "pros": [
              "Transparent and predictable",
              "No hidden behavior"
            ],
            "cons": [
              "No URL fetching or extraction"
            ]
          },
          {
            "title": "ADD",
            "description": "Like COPY, but also supports URL fetching and automatic tar extraction.",
            "tags": [
              "Advanced",
              "Auto-extract"
            ],
            "pros": [
              "Can fetch remote URLs",
              "Automatically extracts tar archives"
            ],
            "cons": [
              "Hidden behavior can surprise you",
              "Not needed in most cases"
            ]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Dockerfile best practices"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Use specific version tags instead of `latest` (e.g., `node:20-alpine`, not `node`).",
          "Combine multiple `RUN` commands with `&&` to reduce image layers.",
          "Put frequently changing operations last to maximize layer cache hits.",
          "Use `.dockerignore` to exclude unnecessary files from the build context.",
          "Prefer `COPY` over `ADD` unless you specifically need URL fetching or auto-extraction.",
          "Always run containers as a non-root user with `USER`."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": ".dockerignore file",
        "content": "Exclude files that should never be sent to the Docker daemon during builds.",
        "code": "node_modules\ngit\n.git\nnpm-debug.log\nDockerfile\n.dockerignore\nREADME.md\n.env",
        "language": "text"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Avoid the `latest` trap",
        "text": "Using `latest` makes your builds non-reproducible. If the base image changes, your build may silently break. Always pin to a specific version."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "exercise",
      "data": {
        "title": "Build your own image",
        "description": "Create a simple application container and run it locally.",
        "steps": [
          "Create a new directory for a simple app.",
          "Add a minimal source file (e.g., an index.html or server.js).",
          "Write a Dockerfile for your app.",
          "Create a `.dockerignore` file.",
          "Build the image: `docker build -t my-app .`",
          "Run it: `docker run -d -p 8080:80 my-app`",
          "Verify it works by opening http://localhost:8080 in a browser."
        ],
        "expectedOutcome": "A running container serving your app on the host port you specified."
      }
    },
    {
      "type": "divider",
      "data": {}
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "A Dockerfile is a recipe for building a Docker image using layered instructions.",
          "Instruction order matters: put rarely-changing steps first to benefit from cache.",
          "Multi-stage builds keep final images small by discarding build-time dependencies.",
          "Use `.dockerignore` and specific base image tags for reproducible, lean builds."
        ],
        "takeaway": "A well-written Dockerfile is the foundation of reliable, reproducible container images."
      }
    }
  ],
  "courseSlug": "devops"
}
