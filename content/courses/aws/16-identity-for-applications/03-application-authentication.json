{
  "id": "lesson-aws-application-authentication",
  "slug": "application-authentication",
  "moduleSlug": "16-identity-for-applications",
  "courseSlug": "aws",
  "title": "Application Authentication",
  "description": "Implement JWT-based authentication in your applications with social login, OIDC, and SAML support.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 15,
  "tags": ["aws", "cognito", "jwt", "authentication", "oauth", "saml"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "When a user signs in through Cognito, they receive **JWT (JSON Web Token)** tokens. Your application validates these tokens to confirm the user's identity and permissions — without needing to call a database on every request."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "JWT Tokens Explained"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Three Types of Tokens",
        "items": [
          "**ID Token** — contains user profile claims (name, email, custom attributes)",
          "**Access Token** — used to call APIs and confirm user authorization",
          "**Refresh Token** — used to get new tokens without re-entering credentials"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Validating JWT Tokens in Python"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Validate a Cognito JWT in Python with PyJWT",
        "content": "This Python function validates the ID token from Cognito using the JWKS endpoint.",
        "code": "import jwt\nimport requests\nfrom jwt.algorithms import RSAAlgorithm\n\n# Fetch the JWKS (JSON Web Key Set) from Cognito\nJWKS_URL = 'https://cognito-idp.us-east-1.amazonaws.com/us-east-1_abc123/.well-known/jwks.json'\njwks = requests.get(JWKS_URL).json()\n\n# Build the public key from the JWK\nkey = RSAAlgorithm.from_jwk(jwks['keys'][0])\n\ndef validate_token(token: str) -> dict:\n    try:\n        payload = jwt.decode(\n            token,\n            key,\n            algorithms=['RS256'],\n            audience='4kfeabc123',\n            issuer='https://cognito-idp.us-east-1.amazonaws.com/us-east-1_abc123'\n        )\n        return payload\n    except jwt.ExpiredSignatureError:\n        raise ValueError('Token has expired')\n    except jwt.InvalidTokenError:\n        raise ValueError('Invalid token')"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Social Login and Federation"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Cognito supports federated login through **OIDC**, **SAML**, and social providers like **Google**, **Facebook**, and **Apple**. Users can sign in with an existing account, and Cognito links it to a user profile in your User Pool."
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "JWT",
            "definition": "A compact, URL-safe token that represents claims between two parties."
          },
          {
            "term": "OIDC",
            "definition": "OpenID Connect — an identity layer built on top of OAuth 2.0."
          },
          {
            "term": "SAML",
            "definition": "Security Assertion Markup Language — an XML-based standard for exchanging authentication data."
          },
          {
            "term": "JWKS",
            "definition": "JSON Web Key Set — a set of public keys used to verify JWTs."
          }
        ]
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Exercise: Validate a Token",
        "description": "Create a Lambda function that validates a Cognito ID token and returns the user's email and role. Use the Python code above as a starting point."
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Cognito issues JWT tokens after successful authentication",
          "Validate tokens using the JWKS endpoint and your User Pool's public key",
          "Social login and SAML/OIDC federation are built-in"
        ]
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "aws"
}
