{
  "id": "lesson-iq-s14",
  "slug": "system-q14",
  "moduleSlug": "system-design",
  "title": "Design a rate limiter that works across multiple servers.",
  "description": "Use a centralized store like Redis with sliding window or token bucket algorithms.",
  "order": 14,
  "difficulty": "advanced",
  "estimatedMinutes": 12,
  "tags": ["interview", "system-design", "rate-limiting"],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "**Question:** Design a rate limiter that works across multiple servers."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Short Answer"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Store counters in Redis using a sliding window or token bucket. All app servers read and write to the same Redis instance or cluster."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Detailed Explanation"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Algorithm: sliding window log, sliding window counter, or token bucket",
          "Store: Redis with atomic INCR and EXPIRE",
          "Key format: rate_limit:{userId}:{window}",
          "Response: 429 Too Many Requests with Retry-After header",
          "Granularity: per user, per API key, or per IP"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Example"
      }
    },
    {
      "type": "code-block",
      "data": {
        "language": "csharp",
        "code": "public async Task<bool> AllowRequestAsync(string key, int limit, TimeSpan window) {\n    var count = await _redis.StringIncrementAsync(key);\n    if (count == 1) {\n        await _redis.StringExpireAsync(key, window);\n    }\n    return count <= limit;\n}\n\n// Sliding window log (sorted set)\npublic async Task<bool> SlidingWindowAsync(string key, int limit, TimeSpan window) {\n    var now = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds();\n    var min = now - window.TotalMilliseconds;\n    \n    await _redis.SortedSetRemoveRangeByScoreAsync(key, 0, min);\n    await _redis.SortedSetAddAsync(key, now, now);\n    await _redis.SortedSetExpireAsync(key, window);\n    \n    var count = await _redis.SortedSetLengthAsync(key);\n    return count <= limit;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Mistakes"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Storing counters in memory (not shared across servers)",
          "Using fixed windows that allow bursts at boundaries",
          "Not returning Retry-After"
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Follow-up Questions"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "How do you handle rate limiting in a distributed cache?",
          "What is a token bucket algorithm?",
          "How do you test rate limiters?"
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key Takeaways",
        "points": [
          "Centralize state in Redis",
          "Use atomic operations",
          "Sliding windows prevent boundary bursts",
          "Return Retry-After for clients"
        ],
        "takeaway": "Centralize state in Redis"
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": [],
  "courseSlug": "interview-qa",
  "estimatedMinutes": 12
}
