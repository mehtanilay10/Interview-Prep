{
  "id": "lesson-iq-linq-how-do-you-identify-a-query-that-is-unexpectedly-expensive",
  "slug": "linq-how-do-you-identify-a-query-that-is-unexpectedly-expensive",
  "moduleSlug": "c-net-follow-ups",
  "courseSlug": "interview-qa",
  "title": "LINQ: how do you identify a query that is unexpectedly expensive?",
  "description": "Learn to spot LINQ performance traps such as deferred execution, accidental Cartesian products, and repeated enumeration in .NET code.",
  "order": 4,
  "difficulty": "advanced",
  "estimatedMinutes": 26,
  "tags": [
    "linq",
    "performance",
    "enumerable",
    "iqueryable",
    "optimization"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "LINQ is one of C#'s most expressive features, but its convenience can hide expensive work. Interviewers ask this because a single poorly written query can turn a fast API endpoint into a bottleneck."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The key insight is that LINQ is not a single thing. `IEnumerable<T>` queries execute in memory with delegates. `IQueryable<T>` queries build expression trees that a provider — often Entity Framework Core — translates to SQL. The performance characteristics and failure modes are completely different."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Deferred execution surprises"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Deferred execution means the query does not run when you write it; it runs when you enumerate it. This is efficient for composition, but dangerous if you accidentally enumerate inside a loop."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Enumerating inside a loop",
        "content": "This code re-runs the same query for every order, multiplying the cost by the number of orders.",
        "code": "var orders = GetRecentOrders();\n\nforeach (var order in orders)\n{\n    // GetItems executes a database query on every iteration.\n    var items = GetItems(order.Id).ToList();\n    Process(items);\n}\n\n// Fix: enumerate once outside the loop\nvar allItems = GetItems(orders.Select(o => o.Id)).ToList();",
        "language": "csharp"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "N+1 query pattern",
        "text": "If a repository method returns `IQueryable<T>` and the caller loops over it while accessing navigation properties, Entity Framework may issue one query per row. Always inspect the generated SQL."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Accidental Cartesian products"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Joining two collections in memory with `SelectMany` or multiple `from` clauses produces every combination. If both collections are large, the result set can grow exponentially."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Hidden Cartesian explosion",
        "content": "This query looks innocent but can produce thousands of rows if either collection is large.",
        "code": "var customers = GetActiveCustomers(); // 1,000 rows\nvar orders = GetRecentOrders();        // 5,000 rows\n\nvar pairs = from c in customers\n            from o in orders\n            where o.CustomerId == c.Id\n            select new { c.Name, o.Total };\n\n// Prefer a dictionary or Join to keep the complexity predictable.\nvar orderLookup = orders.ToLookup(o => o.CustomerId);\nvar pairs2 = from c in customers\n             from o in orderLookup[c.Id]\n             select new { c.Name, o.Total };",
        "language": "csharp"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Profiling and measuring"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Never guess whether a query is expensive. Use logging, EF Core's `ToQueryString()`, or a profiler. In production, track the query duration and row count in application telemetry."
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "title": "Investigation steps",
        "items": [
          "Enable EF Core sensitive data logging in development and inspect `ToQueryString()` output.",
          "Add execution-time logging around repository methods.",
          "Compare `IQueryable<T>` versus `IEnumerable<T>` boundaries to see where evaluation happens.",
          "Profile memory allocations if the query materializes large intermediate collections."
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Deferred execution is powerful but can cause repeated execution inside loops.",
          "Always inspect the SQL generated by `IQueryable<T>` providers.",
          "Watch for Cartesian products when joining in-memory collections.",
          "Measure with logs and profilers before refactoring."
        ],
        "takeaway": "A LINQ query is only as efficient as the execution plan it creates — and where you enumerate it."
      }
    }
  ],
  "relatedLessons": [
    "asyncawait-explain-it-then-explain-how-it-can-cause-thread-starvation",
    "garbage-collection-how-would-you-investigate-allocation-and-gc-pressure",
    "concurrency-how-would-you-diagnose-a-race-condition-that-occurs-once-a-day"
  ],
  "furtherReading": [
    {
      "title": "Microsoft — LINQ query performance",
      "url": "https://learn.microsoft.com/en-us/dotnet/csharp/linq/performance",
      "type": "article"
    },
    {
      "title": "Julie Lerman — EF Core query tips",
      "url": "https://learn.microsoft.com/en-us/ef/core/querying/",
      "type": "article"
    },
    {
      "title": "Nick Chapsas — LINQ performance",
      "url": "https://www.youtube.com/watch?v=3b7F7I5T8Hs",
      "type": "video"
    }
  ],
  "technology": "C#"
}
