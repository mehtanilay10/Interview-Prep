{
  "id": "lesson-iq-spant-what-does-it-improve-and-what-constraints-does-it-introduce",
  "slug": "spant-what-does-it-improve-and-what-constraints-does-it-introduce",
  "moduleSlug": "c-net-follow-ups",
  "courseSlug": "interview-qa",
  "title": "Span<T>: what does it improve and what constraints does it introduce?",
  "description": "Learn how Span<T> reduces allocations and copies in .NET, where it excels, and the safety constraints that come with ref-struct semantics.",
  "order": 8,
  "difficulty": "advanced",
  "estimatedMinutes": 30,
  "tags": [
    "span",
    "memory",
    "performance",
    "allocations",
    "ref-struct"
  ],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "`Span<T>` represents a contiguous region of arbitrary memory — an array, a stack-allocated buffer, or unmanaged memory — without copying it. Before `Span<T>`, many performance-critical methods needed separate overloads for arrays, `string`, `Memory<T>`, and pointers. `Span<T>` unifies these cases."
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Because `Span<T>` is a `ref struct`, it lives on the stack and cannot be boxed, stored in fields, or captured by lambdas. This constraint is a safety feature: it guarantees the compiler will prevent you from escaping a slice beyond the lifetime of its backing store."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "What it improves"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "title": "Performance gains",
        "items": [
          "Parsing and formatting can read directly from source buffers without intermediate strings.",
          "Slicing arrays or strings does not allocate a new array or substring object.",
          "Replacing `Array.Copy` with `Span<T>.CopyTo` reduces bounds-checking overhead.",
          "Interop and networking code can work with unmanaged memory safely."
        ]
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Avoiding substring allocations",
        "content": "This code extracts a date portion from a timestamp without creating intermediate strings.",
        "code": "public bool TryParseDate(ReadOnlySpan<char> timestamp, out DateOnly date)\n{\n    // Slicing a ReadOnlySpan<char> is allocation-free.\n    var datePart = timestamp.Slice(0, 10);\n    return DateOnly.TryParse(datePart, out date);\n}\n\n// Caller can pass a string, char[], or stack-allocated buffer.\nTryParseDate(\"2026-09-26T13:00:00Z\", out var d);",
        "language": "csharp"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Prefer ReadOnlySpan<T> when you do not write",
        "text": "`ReadOnlySpan<T>` communicates intent and allows the compiler to accept `string`, arrays, and `ReadOnlyMemory<T>` arguments without copying."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Constraints and trade-offs"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The `ref struct` constraint is the main trade-off. It keeps `Span<T>` safe but limits where you can use it. You cannot box it, store it in a class field, or use it across `await` boundaries because the stack frame may be gone when the method resumes."
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Constraint",
          "Reason",
          "Workaround or alternative"
        ],
        "rows": [
          [
            "Cannot be a class field",
            "Stack-only lifetime",
            "Use `Memory<T>` if you need heap storage"
          ],
          [
            "Cannot be boxed",
            "Prevents escaping the stack",
            "Use `Memory<T>` for interface boundaries"
          ],
          [
            "Cannot cross await",
            "Stack frame may disappear",
            "Copy to `Memory<T>` before awaiting"
          ],
          [
            "Length must be known or sliceable",
            "Requires contiguous memory",
            "Not suitable for linked-list-like structures"
          ]
        ]
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "When you need a `Span<T>`-like handle that can cross `await` or live in a class, use `Memory<T>`. `Memory<T>` can be created from the same sources as `Span<T>` but is a reference type. It copies less efficiently but is more flexible."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Span<T> vs Memory<T>",
        "content": "Choose Span<T> for synchronous, stack-confined algorithms. Choose Memory<T> when the handle must survive async boundaries.",
        "code": "// Stack-only: fast, no allocations, cannot cross await\npublic int Sum(ReadOnlySpan<int> values)\n{\n    int total = 0;\n    foreach (var v in values)\n        total += v;\n    return total;\n}\n\n// Async-safe: heap-backed, slightly more overhead\npublic async Task<int> SumAsync(Memory<int> values)\n{\n    await Task.Yield();\n    return Sum(values.Span);\n}",
        "language": "csharp"
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Key takeaways",
        "points": [
          "Span<T> removes allocations for slicing, parsing, and copying contiguous memory.",
          "ReadOnlySpan<T> is the safest default for read-only parameters.",
          "ref struct constraints prevent boxing, heap storage, and await crossing.",
          "Use Memory<T> when you need the same flexibility across async boundaries."
        ],
        "takeaway": "Span<T> is a zero-cost abstraction over memory, but its safety constraints require matching your API design to its lifetime model."
      }
    }
  ],
  "relatedLessons": [
    "garbage-collection-how-would-you-investigate-allocation-and-gc-pressure",
    "valuetask-what-problem-does-it-solve-and-when-can-it-make-code-worse",
    "linq-how-do-you-identify-a-query-that-is-unexpectedly-expensive"
  ],
  "furtherReading": [
    {
      "title": "Microsoft — Span<T> and Memory<T>",
      "url": "https://learn.microsoft.com/en-us/dotnet/standard/memory-and-spans/",
      "type": "article"
    },
    {
      "title": "Stephen Toub — Ref structs and performance",
      "url": "https://devblogs.microsoft.com/dotnet/ref-structs/",
      "type": "article"
    },
    {
      "title": "Vladimir Khorikov — When to use Span<T>",
      "url": "https://www.youtube.com/watch?v=5nK9mIf6P2A",
      "type": "video"
    }
  ],
  "technology": "C#"
}
