{
  "id": "lesson-cheatsheet-redux",
  "slug": "cheatsheet",
  "moduleSlug": "redux",
  "courseSlug": "cheatsheet",
  "title": "Redux Complete Cheat Sheet",
  "description": "Comprehensive quick reference for Redux: store, actions, reducers, Redux Toolkit, slices, selectors, middleware, async thunks, and React Redux hooks.",
  "order": 4,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": ["redux", "redux-toolkit", "actions", "reducers", "middleware", "thunks", "selectors", "react-redux", "cheatsheet", "quick-reference"],
  "blocks": [
    {
      "type": "example",
      "data": {
        "title": "Redux Core Concepts — Single Source of Truth",
        "language": "jsx",
        "code": "// The entire state of your app is stored in an object tree within a single store.\n// This makes it easy to debug, inspect, and serialize the application state.\n\nconst initialState = {\n  users: [],\n  posts: [],\n  ui: { loading: false, error: null }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Core Concepts — State is Read-Only",
        "language": "jsx",
        "code": "// The only way to change the state is to emit an action — a plain object describing what happened.\n// This ensures that neither the view nor the network call can directly write to the state.\n\nconst increment = { type: 'counter/increment' };\nstore.dispatch(increment);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Core Concepts — Changes via Pure Functions",
        "language": "jsx",
        "code": "// Reducers are pure functions that take the previous state and an action,\n// and return the next state without mutating the original state.\n\nfunction reducer(state = initialState, action) {\n  switch (action.type) {\n    case 'counter/increment':\n      return { ...state, count: state.count + 1 };\n    default:\n      return state;\n  }\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Core Concepts — Store Explanation",
        "language": "jsx",
        "code": "// A store holds the whole state tree of your app.\n// There should only be a single store in a Redux app.\n// The store has three main methods: getState, dispatch, and subscribe.\n\nconst store = createStore(rootReducer);\nconsole.log(store.getState()); // current state\nstore.dispatch({ type: 'INIT' }); // emit action\nconst unsubscribe = store.subscribe(() => console.log('State changed'));\nunsubscribe(); // stop listening"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — configureStore",
        "language": "jsx",
        "code": "import { configureStore } from '@reduxjs/toolkit';\nimport counterReducer from './counterSlice';\n\nconst store = configureStore({\n  reducer: {\n    counter: counterReducer,\n    user: userReducer\n  },\n  // Redux Toolkit includes thunk middleware by default and sets up Redux DevTools.\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — Root Reducer",
        "language": "jsx",
        "code": "import { combineReducers } from 'redux';\nimport counter from './counter';\nimport todos from './todos';\n\nconst rootReducer = combineReducers({\n  counter,\n  todos\n  // Each key becomes a top-level state key.\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — Redux DevTools",
        "language": "jsx",
        "code": "// Redux DevTools extension is automatically configured when using configureStore.\n// For createStore, you can pass an enhanced compose function.\n\nconst store = createStore(\n  rootReducer,\n  window.__REDUX_DEVTOOLS_EXTENSION__\n    ? window.__REDUX_DEVTOOLS_EXTENSION__()\n    : f => f\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — store.dispatch",
        "language": "jsx",
        "code": "// Dispatch an action to trigger a state change.\n\nstore.dispatch({ type: 'todos/add', payload: { id: 1, text: 'Learn Redux' } });\n\n// When using Redux Toolkit, dispatch can also accept thunks.\nstore.dispatch(fetchTodos()); // async thunk"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — store.getState",
        "language": "jsx",
        "code": "// Retrieve the current state tree from the store.\n\nconst state = store.getState();\nconsole.log(state.counter); // { value: 0 }\nconsole.log(state.todos);   // []"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — store.subscribe",
        "language": "jsx",
        "code": "// Subscribe to state changes.\n\nconst unsubscribe = store.subscribe(() => {\n  const state = store.getState();\n  console.log('Current count:', state.counter.value);\n});\n\n// Later, stop listening.\nunsubscribe();"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Creating a Store — store.replaceReducer",
        "language": "jsx",
        "code": "// Replace the current reducer with a new reducer dynamically.\n// Useful for code-splitting or hot-reloading reducers.\n\nconst nextReducer = combineReducers({ counter, settings });\nstore.replaceReducer(nextReducer);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Actions — Action Creators",
        "language": "jsx",
        "code": "// Action creators are functions that create and return action objects.\n\nconst addTodo = (text) => ({\n  type: 'todos/add',\n  payload: { id: Date.now(), text, completed: false }\n});\n\nstore.dispatch(addTodo('Buy milk'));"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Actions — Action Type Constants",
        "language": "jsx",
        "code": "// Defining action types as constants prevents typos and enables IDE autocomplete.\n\nconst ADD_TODO = 'todos/ADD_TODO';\nconst TOGGLE_TODO = 'todos/TOGGLE_TODO';\n\nconst addTodo = (text) => ({\n  type: ADD_TODO,\n  payload: text\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Actions — FSA Pattern (Flux Standard Action)",
        "language": "jsx",
        "code": "// A Flux Standard Action has type, optional payload, optional error, and optional meta.\n\nconst successAction = {\n  type: 'FETCH_SUCCESS',\n  payload: { data: [] },\n  meta: { timestamp: Date.now() }\n};\n\nconst errorAction = {\n  type: 'FETCH_ERROR',\n  payload: new Error('Network failed'),\n  error: true\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Actions — Action Payload",
        "language": "jsx",
        "code": "// The payload carries the data needed to process the action.\n\nconst addUser = (name, email) => ({\n  type: 'users/addUser',\n  payload: { id: nanoid(), name, email }\n});\n\n// In the reducer, access it via action.payload.\ncase 'users/addUser':\n  return { ...state, users: [...state.users, action.payload] };"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Actions — Action Meta",
        "language": "jsx",
        "code": "// Meta contains extra information that is not the payload.\n\nconst addTodo = (text) => ({\n  type: 'todos/add',\n  payload: text,\n  meta: {\n    loggedAt: new Date().toISOString(),\n    source: 'form'\n  }\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducers — Pure Function",
        "language": "jsx",
        "code": "// A reducer must be a pure function: same input always yields same output.\n// It must not mutate the state, perform side effects, or call non-pure functions.\n\nconst counterReducer = (state = 0, action) => {\n  switch (action.type) {\n    case 'increment':\n      return state + 1; // returns new value, does not mutate state\n    case 'decrement':\n      return state - 1;\n    default:\n      return state;\n  }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducers — Initial State",
        "language": "jsx",
        "code": "// Provide a default value for the state parameter.\n\nconst initialState = {\n  items: [],\n  status: 'idle',\n  error: null\n};\n\nconst itemsReducer = (state = initialState, action) => {\n  // handle actions\n  return state;\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducers — Switch Statement",
        "language": "jsx",
        "code": "// Use a switch statement to handle different action types.\n\nconst reducer = (state = {}, action) => {\n  switch (action.type) {\n    case 'SET_NAME':\n      return { ...state, name: action.payload };\n    case 'SET_AGE':\n      return { ...state, age: action.payload };\n    default:\n      return state;\n  }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducers — Handling Actions",
        "language": "jsx",
        "code": "// Each case in the switch returns a new state object.\n\nconst todosReducer = (state = [], action) => {\n  switch (action.type) {\n    case 'ADD_TODO':\n      return [...state, action.payload];\n    case 'TOGGLE_TODO':\n      return state.map(todo =>\n        todo.id === action.payload\n          ? { ...todo, completed: !todo.completed }\n          : todo\n      );\n    default:\n      return state;\n  }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducers — Combining Reducers",
        "language": "jsx",
        "code": "import { combineReducers } from 'redux';\n\nconst counterReducer = (state = 0, action) => { /* ... */ };\nconst todosReducer = (state = [], action) => { /* ... */ };\n\nconst rootReducer = combineReducers({\n  counter: counterReducer,\n  todos: todosReducer\n  // State shape: { counter: number, todos: array }\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Reducers — Immutable Updates with Spread",
        "language": "jsx",
        "code": "// Always return new objects/arrays; never mutate the existing state.\n\n// Update array by adding item\nreturn { ...state, items: [...state.items, newItem] };\n\n// Update array by removing item by id\nreturn {\n  ...state,\n  items: state.items.filter(item => item.id !== action.payload)\n};\n\n// Update nested object\nreturn {\n  ...state,\n  user: { ...state.user, name: action.payload }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — configureStore",
        "language": "jsx",
        "code": "import { configureStore } from '@reduxjs/toolkit';\nimport counterReducer from './counterSlice';\n\nconst store = configureStore({\n  reducer: {\n    counter: counterReducer\n  },\n  devTools: process.env.NODE_ENV !== 'production',\n  // Redux Toolkit enables Redux DevTools and thunk middleware by default.\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createSlice Name and Initial State",
        "language": "jsx",
        "code": "import { createSlice } from '@reduxjs/toolkit';\n\nconst counterSlice = createSlice({\n  name: 'counter',\n  initialState: { value: 0 },\n  reducers: {\n    increment: (state) => {\n      state.value += 1; // Immer allows safe mutating syntax\n    },\n    decrement: (state) => {\n      state.value -= 1;\n    }\n  }\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createSlice Actions and Reducer",
        "language": "jsx",
        "code": "// createSlice auto-generates action creators and action types.\n\nconst { actions, reducer } = counterSlice;\n\nexport const { increment, decrement, incrementByAmount } = actions;\nexport default reducer;\n\n// Dispatch generated actions\nstore.dispatch(increment());\nstore.dispatch(incrementByAmount(5));"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createSlice extraReducers",
        "language": "jsx",
        "code": "// Handle actions generated elsewhere (e.g., createAsyncThunk).\n\nconst counterSlice = createSlice({\n  name: 'counter',\n  initialState: { value: 0, status: 'idle' },\n  reducers: { /* ... */ },\n  extraReducers: (builder) => {\n    builder\n      .addCase(incrementAsync.pending, (state) => {\n        state.status = 'loading';\n      })\n      .addCase(incrementAsync.fulfilled, (state, action) => {\n        state.status = 'succeeded';\n        state.value += action.payload;\n      })\n      .addCase(incrementAsync.rejected, (state) => {\n        state.status = 'failed';\n      });\n  }\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createAsyncThunk Basic",
        "language": "jsx",
        "code": "import { createAsyncThunk } from '@reduxjs/toolkit';\n\nexport const fetchUserById = createAsyncThunk(\n  'users/fetchById',\n  async (userId, thunkAPI) => {\n    const response = await fetch(`/api/users/${userId}`);\n    if (!response.ok) throw new Error('Failed to fetch');\n    return response.json();\n  }\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createAsyncThunk Extra Reducers",
        "language": "jsx",
        "code": "extraReducers: (builder) => {\n  builder\n    .addCase(fetchUserById.pending, (state) => {\n      state.status = 'loading';\n    })\n    .addCase(fetchUserById.fulfilled, (state, action) => {\n      state.status = 'succeeded';\n      state.user = action.payload;\n    })\n    .addCase(fetchUserById.rejected, (state, action) => {\n      state.status = 'failed';\n      state.error = action.error.message;\n    });\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createAsyncThunk Unwrap Result",
        "language": "jsx",
        "code": "// unwrapResult gives direct access to the fulfilled value or throws on rejection.\n\nconst result = await dispatch(fetchUserById(1)).unwrap();\nconsole.log(result.name); // direct access to the user data\n\n// Or handle errors explicitly\nconst { payload, error } = await dispatch(fetchUserById(1));\nif (error) {\n  console.error('Failed:', error);\n} else {\n  console.log('User:', payload);\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createEntityAdapter Setup",
        "language": "jsx",
        "code": "import { createEntityAdapter, createSlice } from '@reduxjs/toolkit';\n\nconst usersAdapter = createEntityAdapter({\n  sortComparer: (a, b) => a.name.localeCompare(b.name)\n});\n\nconst usersSlice = createSlice({\n  name: 'users',\n  initialState: usersAdapter.getInitialState(),\n  reducers: {\n    userAdded: usersAdapter.addOne,\n    userUpdated: usersAdapter.updateOne\n  }\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux Toolkit — createEntityAdapter Operations",
        "language": "jsx",
        "code": "// The adapter provides normalized CRUD operations.\n\nusersAdapter.addOne(state, { id: '1', name: 'Alice' });\nusersAdapter.addMany(state, [{ id: '2', name: 'Bob' }]);\nusersAdapter.updateOne(state, { id: '1', changes: { name: 'Alice Updated' } });\nusersAdapter.removeOne(state, '1');\nusersAdapter.removeMany(state, ['1', '2']);\n\nconst { selectAll, selectById } = usersAdapter.getSelectors();\nconst allUsers = selectAll(state.users);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Selectors — Basic Selector",
        "language": "jsx",
        "code": "// Selectors encapsulate the state structure and extract specific data.\n\nconst selectTodos = (state) => state.todos;\nconst selectCompletedTodos = (state) =>\n  selectTodos(state).filter(todo => todo.completed);\n\n// Usage with useSelector\nconst completedTodos = useSelector(selectCompletedTodos);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Selectors — Reselect createSelector",
        "language": "jsx",
        "code": "import { createSelector } from 'reselect';\n\nconst selectTodos = (state) => state.todos;\nconst selectFilter = (state) => state.filter;\n\n// Memoized selector — only recomputes when inputs change.\nconst selectVisibleTodos = createSelector(\n  [selectTodos, selectFilter],\n  (todos, filter) => {\n    switch (filter) {\n      case 'completed':\n        return todos.filter(t => t.completed);\n      case 'active':\n        return todos.filter(t => !t.completed);\n      default:\n        return todos;\n    }\n  }\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Selectors — Composed Selectors",
        "language": "jsx",
        "code": "// Build complex selectors by composing simpler ones.\n\nconst selectTodos = (state) => state.todos;\nconst selectFilter = (state) => state.filter;\nconst selectVisibleTodos = createSelector(\n  [selectTodos, selectFilter],\n  (todos, filter) => { /* ... */ }\n);\n\nconst selectCompletedCount = createSelector(\n  [selectVisibleTodos],\n  (todos) => todos.filter(t => t.completed).length\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Selectors — Selector with Props",
        "language": "jsx",
        "code": "// createSelector supports arguments after the input selectors.\n\nconst selectTodos = (state) => state.todos;\n\nconst selectTodoById = createSelector(\n  [selectTodos, (state, props) => props.id],\n  (todos, id) => todos.find(todo => todo.id === id)\n);\n\n// Usage in useSelector\nconst todo = useSelector((state) => selectTodoById(state, { id: 1 }));"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useDispatch Hook — Dispatching Actions",
        "language": "jsx",
        "code": "import { useDispatch } from 'react-redux';\nimport { increment } from './counterSlice';\n\nfunction Counter() {\n  const dispatch = useDispatch();\n\n  return (\n    <button onClick={() => dispatch(increment())}>\n      Increment\n    </button>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useDispatch Hook — Dispatching Thunks",
        "language": "jsx",
        "code": "import { useDispatch } from 'react-redux';\nimport { fetchTodos } from './todosSlice';\n\nfunction TodoList() {\n  const dispatch = useDispatch();\n\n  useEffect(() => {\n    dispatch(fetchTodos());\n  }, [dispatch]);\n\n  return null;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useSelector Hook — Basic Selector",
        "language": "jsx",
        "code": "import { useSelector } from 'react-redux';\n\nfunction Counter() {\n  const count = useSelector((state) => state.counter.value);\n\n  return <div>Count: {count}</div>;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useSelector Hook — shallowEqual",
        "language": "jsx",
        "code": "import { useSelector, shallowEqual } from 'react-redux';\n\n// Use shallowEqual when selecting an object or array to avoid unnecessary re-renders.\n\nconst selected = useSelector(\n  (state) => ({ user: state.user, posts: state.posts }),\n  shallowEqual\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useSelector Hook — Multiple Selectors",
        "language": "jsx",
        "code": "// You can call useSelector multiple times for independent values.\n\nconst user = useSelector((state) => state.user);\nconst posts = useSelector((state) => state.posts);\nconst theme = useSelector((state) => state.theme);\n\n// Each subscription triggers re-render only when that specific value changes."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React-Redux Hooks — useSelector vs mapState",
        "language": "jsx",
        "code": "// Modern approach with useSelector vs legacy connect mapState.\n\n// useSelector (recommended)\nconst count = useSelector((state) => state.counter);\n\n// Legacy connect (still supported)\nconst mapState = (state) => ({\n  count: state.counter\n});\n\nexport default connect(mapState)(Counter);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React-Redux Hooks — Provider Component",
        "language": "jsx",
        "code": "import { Provider } from 'react-redux';\nimport store from './store';\nimport App from './App';\n\n// Wrap your app with Provider to make the store available to all components.\n\nfunction Root() {\n  return (\n    <Provider store={store}>\n      <App />\n    </Provider>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React-Redux Hooks — Legacy connect()",
        "language": "jsx",
        "code": "// Legacy pattern using connect() with mapState and mapDispatch.\n\nimport { connect } from 'react-redux';\nimport { increment, decrement } from './counterActions';\n\nfunction Counter({ count, increment, decrement }) {\n  return (\n    <div>\n      <span>{count}</span>\n      <button onClick={increment}>+</button>\n      <button onClick={decrement}>-</button>\n    </div>\n  );\n}\n\nconst mapState = (state) => ({ count: state.counter });\nconst mapDispatch = { increment, decrement };\n\nexport default connect(mapState, mapDispatch)(Counter);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Immutable Updates — Spread for Arrays",
        "language": "jsx",
        "code": "// Use the spread operator to create new arrays.\n\nconst newState = {\n  ...state,\n  items: [...state.items, newItem]\n};\n\n// Remove item by id\nconst filtered = {\n  ...state,\n  items: state.items.filter(item => item.id !== id)\n};\n\n// Update item by index\nconst updated = {\n  ...state,\n  items: state.items.map((item, idx) =>\n    idx === index ? { ...item, name: newName } : item\n  )\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Immutable Updates — Immer in createSlice",
        "language": "jsx",
        "code": "// Redux Toolkit uses Immer internally, so you can write 'mutating' logic safely.\n\nconst counterSlice = createSlice({\n  name: 'counter',\n  initialState: { value: 0, items: [] },\n  reducers: {\n    increment: (state) => {\n      state.value += 1; // Immer produces an immutable update\n    },\n    addItem: (state, action) => {\n      state.items.push(action.payload); // push works safely\n    }\n  }\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Middleware — What is Middleware",
        "language": "jsx",
        "code": "// Middleware provides a third-party extension point between dispatching an action\n// and the action reaching the reducer.\n\nconst logger = store => next => action => {\n  console.log('dispatching', action);\n  let result = next(action);\n  console.log('next state', store.getState());\n  return result;\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Middleware — Custom Logger Middleware",
        "language": "jsx",
        "code": "import { applyMiddleware } from 'redux';\nimport thunk from 'redux-thunk';\n\nconst logger = store => next => action => {\n  console.log('dispatching:', action.type, action);\n  const result = next(action);\n  console.log('next state:', store.getState());\n  return result;\n};\n\nconst store = createStore(\n  rootReducer,\n  applyMiddleware(logger, thunk)\n);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Thunk Middleware — What is Thunk",
        "language": "jsx",
        "code": "// A thunk is a function that returns a function taking (dispatch, getState).\n// Redux Thunk middleware allows action creators to return thunks instead of plain objects.\n\nconst incrementAsync = (amount) => (dispatch, getState) => {\n  setTimeout(() => {\n    dispatch({ type: 'counter/increment', payload: amount });\n  }, 1000);\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Thunk Middleware — Async Thunk Pattern",
        "language": "jsx",
        "code": "const fetchPosts = () => async (dispatch, getState) => {\n  dispatch({ type: 'posts/fetchStart' });\n  try {\n    const res = await fetch('/api/posts');\n    const data = await res.json();\n    dispatch({ type: 'posts/fetchSuccess', payload: data });\n  } catch (err) {\n    dispatch({ type: 'posts/fetchFailure', error: err.message });\n  }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Redux DevTools — Time-Travel Debugging",
        "language": "jsx",
        "code": "// Redux DevTools allows you to inspect every action and state change.\n// You can travel back and forth between states and even dispatch from DevTools.\n\n// In configureStore, DevTools are enabled by default in development.\nconst store = configureStore({\n  reducer: rootReducer,\n  devTools: process.env.NODE_ENV !== 'production'\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Normalized State Shape — byId and allIds",
        "language": "jsx",
        "code": "// Normalized state looks like a database table: entities by ID and an array of IDs.\n\nconst initialState = {\n  users: {\n    byId: {\n      '1': { id: '1', name: 'Alice' },\n      '2': { id: '2', name: 'Bob' }\n    },\n    allIds: ['1', '2']\n  }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Normalized State Shape — Without createEntityAdapter",
        "language": "jsx",
        "code": "// Manually maintaining normalized state.\n\nconst usersReducer = (state = { byId: {}, allIds: [] }, action) => {\n  switch (action.type) {\n    case 'users/add': {\n      const { id, ...rest } = action.payload;\n      return {\n        byId: { ...state.byId, [id]: { id, ...rest } },\n        allIds: [...state.allIds, id]\n      };\n    }\n    default:\n      return state;\n  }\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Performance — Avoiding Re-renders",
        "language": "jsx",
        "code": "// Wrap memoized components with React.memo to prevent re-renders\n// when props haven't changed.\n\nconst TodoItem = React.memo(({ todo, onToggle }) => {\n  console.log('Render TodoItem', todo.id);\n  return <div onClick={() => onToggle(todo.id)}>{todo.text}</div>;\n});\n\n// Use shallowEqual in useSelector when selecting objects.\nconst todo = useSelector(state => state.todos[0], shallowEqual);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Performance — Reselect Memoization",
        "language": "jsx",
        "code": "// Memoized selectors avoid expensive recalculations.\n\nconst selectExpensiveData = createSelector(\n  [selectTodos, selectFilter],\n  (todos, filter) => {\n    console.log('Recalculating...');\n    return heavyComputation(todos, filter);\n  }\n);\n\n// The computation only runs when todos or filter changes.\nconst result = useSelector(selectExpensiveData);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing Redux — Testing Reducers",
        "language": "jsx",
        "code": "// Reducers are pure functions — easy to test.\n\ntest('should handle initial state', () => {\n  expect(reducer(undefined, { type: 'unknown' })).toEqual(initialState);\n});\n\ntest('should handle increment', () => {\n  const state = { value: 0 };\n  expect(reducer(state, { type: 'counter/increment' })).toEqual({ value: 1 });\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing Redux — Testing Thunks",
        "language": "jsx",
        "code": "import fetchMock from 'jest-fetch-mock';\n\nfetchMock.enableMocks();\n\nbeforeEach(() => {\n  fetchMock.resetMocks();\n  fetchMock.mockResponseOnce(JSON.stringify({ id: 1, name: 'Test' }));\n});\n\ntest('fetchUserById dispatches success', async () => {\n  const dispatch = jest.fn();\n  const thunk = fetchUserById(1);\n  await thunk(dispatch, () => ({}), null);\n  expect(dispatch).toHaveBeenCalledWith({\n    type: 'users/fetchById/fulfilled',\n    payload: { id: 1, name: 'Test' }\n  });\n});"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "RTK Query — createApi and useGetQuery",
        "language": "jsx",
        "code": "import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';\n\nexport const api = createApi({\n  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),\n  endpoints: (builder) => ({\n    getPosts: builder.query({\n      query: () => '/posts'\n    }),\n    addPost: builder.mutation({\n      query: (post) => ({ method: 'POST', body: post })\n    })\n  })\n});\n\nexport const { useGetPostsQuery, useAddPostMutation } = api;"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "RTK Query — Automatic Caching and Invalidation",
        "language": "jsx",
        "code": "// RTK Query caches data and invalidates tags automatically.\n\nfunction Posts() {\n  const { data, error, isLoading } = useGetPostsQuery();\n  const [addPost] = useAddPostMutation();\n\n  if (isLoading) return <div>Loading...</div>;\n  if (error) return <div>Error</div>;\n\n  return (\n    <ul>\n      {data.map(post => (\n        <li key={post.id}>{post.title}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
