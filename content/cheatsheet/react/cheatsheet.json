{
  "id": "lesson-cheatsheet-react",
  "slug": "cheatsheet",
  "moduleSlug": "react",
  "courseSlug": "cheatsheet",
  "title": "React Complete Cheat Sheet",
  "description": "Comprehensive quick reference covering React components, hooks, state management, patterns, performance optimization, and testing.",
  "order": 3,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": [
    "react",
    "hooks",
    "components",
    "state",
    "patterns",
    "testing",
    "performance",
    "cheatsheet",
    "quick-reference"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Component Basics"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React components are the building blocks of any React application. They accept inputs (props) and return React elements describing what should appear on screen."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Functional Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Functional component",
        "language": "jsx",
        "code": "function Welcome(props) {\n  return <h1>Hello, {props.name}</h1>;\n}\n\n// Or using arrow function\nconst Welcome = (props) => {\n  return <h1>Hello, {props.name}</h1>;\n};"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Class Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Class component",
        "language": "jsx",
        "code": "class Welcome extends React.Component {\n  render() {\n    return <h1>Hello, {this.props.name}</h1>;\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "JSX Syntax"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "JSX basics",
        "language": "jsx",
        "code": "const element = (\n  <div className=\"container\">\n    <h1>Hello World</h1>\n    <p>Paragraph with {variable}</p>\n  </div>\n);\n\n// Must have a single parent element\n// Use className instead of class\n// Self-closing tags need a slash: <img />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Component Composition"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Component composition",
        "language": "jsx",
        "code": "function App() {\n  return (\n    <div>\n      <Header title=\"My App\" />\n      <main>\n        <Sidebar />\n        <Content />\n      </main>\n      <Footer />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Props"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Using props",
        "language": "jsx",
        "code": "function UserCard(props) {\n  return (\n    <div>\n      <h2>{props.name}</h2>\n      <p>{props.email}</p>\n    </div>\n  );\n}\n\n// Usage:\n<UserCard name=\"John\" email=\"john@example.com\" />\n\n// Destructuring\nfunction UserCard({ name, email }) {\n  return (\n    <div>\n      <h2>{name}</h2>\n      <p>{email}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Children"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Children prop",
        "language": "jsx",
        "code": "function Card(props) {\n  return (\n    <div className=\"card\">\n      {props.children}\n    </div>\n  );\n}\n\n// Usage:\n<Card>\n  <h2>Card Title</h2>\n  <p>Card content goes here</p>\n</Card>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useState Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "The useState hook lets you add state to functional components. It returns a state variable and a function to update it."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Basic useState"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic useState",
        "language": "jsx",
        "code": "import { useState } from 'react';\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Increment\n      </button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Functional Updates"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Functional state updates",
        "language": "jsx",
        "code": "function Counter() {\n  const [count, setCount] = useState(0);\n\n  // Use when new state depends on previous state\n  const increment = () => {\n    setCount(prevCount => prevCount + 1);\n  };\n\n  return (\n    <button onClick={increment}>Count: {count}</button>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Multiple State Variables"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple useState calls",
        "language": "jsx",
        "code": "function Form() {\n  const [name, setName] = useState('');\n  const [email, setEmail] = useState('');\n  const [isSubmitted, setIsSubmitted] = useState(false);\n\n  return (\n    <form>\n      <input value={name} onChange={e => setName(e.target.value)} />\n      <input value={email} onChange={e => setEmail(e.target.value)} />\n      <button onClick={() => setIsSubmitted(true)}>Submit</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Lazy Initialization"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Lazy initial state",
        "language": "jsx",
        "code": "function TodoList() {\n  const [todos, setTodos] = useState(() => {\n    // Expensive computation only runs on initial render\n    const saved = localStorage.getItem('todos');\n    return saved ? JSON.parse(saved) : [];\n  });\n\n  return <ul>{/* render todos */}</ul>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useEffect Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useEffect lets you perform side effects in functional components. It replaces componentDidMount, componentDidUpdate, and componentWillUnmount."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "ComponentDidMount Equivalent"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Effect on mount only",
        "language": "jsx",
        "code": "import { useState, useEffect } from 'react';\n\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n\n  useEffect(() => {\n    // Runs once after mount\n    fetch(`/api/users/${userId}`)\n      .then(res => res.json())\n      .then(data => setUser(data));\n  }, []); // Empty dependency array = run once\n\n  return <div>{user ? user.name : 'Loading...'}</div>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "ComponentDidUpdate Equivalent"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Effect with dependencies",
        "language": "jsx",
        "code": "function SearchResults({ query }) {\n  const [results, setResults] = useState([]);\n\n  useEffect(() => {\n    // Runs when query changes\n    fetch(`/api/search?q=${query}`)\n      .then(res => res.json())\n      .then(data => setResults(data));\n  }, [query]); // Re-run when query changes\n\n  return <ul>{results.map(r => <li key={r.id}>{r.title}</li>)}</ul>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Cleanup Function"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useEffect cleanup",
        "language": "jsx",
        "code": "function ChatRoom({ roomId }) {\n  useEffect(() => {\n    const connection = createConnection(roomId);\n    connection.connect();\n\n    // Cleanup function runs before unmount or before next effect\n    return () => {\n      connection.disconnect();\n    };\n  }, [roomId]);\n\n  return <div>Chat room: {roomId}</div>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Dependency Array"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dependency array behavior",
        "language": "jsx",
        "code": "// No dependency array: runs after every render\nuseEffect(() => {\n  document.title = `Count: ${count}`;\n});\n\n// Empty dependency array: runs once after mount\nuseEffect(() => {\n  document.title = 'App mounted';\n}, []);\n\n// With dependencies: runs when deps change\nuseEffect(() => {\n  document.title = `Count: ${count}`;\n}, [count]);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useRef Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useRef returns a mutable ref object that persists for the component's lifetime. It can access DOM elements directly or store mutable values."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "DOM References"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useRef for DOM access",
        "language": "jsx",
        "code": "function TextInput() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <div>\n      <input ref={inputRef} type=\"text\" />\n      <button onClick={focusInput}>Focus Input</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Storing Mutable Values"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useRef for mutable values",
        "language": "jsx",
        "code": "function Timer() {\n  const [seconds, setSeconds] = useState(0);\n  const intervalRef = useRef(null);\n\n  const startTimer = () => {\n    if (intervalRef.current) return;\n    intervalRef.current = setInterval(() => {\n      setSeconds(s => s + 1);\n    }, 1000);\n  };\n\n  const stopTimer = () => {\n    clearInterval(intervalRef.current);\n    intervalRef.current = null;\n  };\n\n  return (\n    <div>\n      <p>Seconds: {seconds}</p>\n      <button onClick={startTimer}>Start</button>\n      <button onClick={stopTimer}>Stop</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useRef vs useState"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useRef vs useState comparison",
        "language": "jsx",
        "code": "// useRef: changing .current does NOT trigger re-render\nconst prevCountRef = useRef(count);\n\n// useState: changing state DOES trigger re-render\nconst [count, setCount] = useState(0);\n\n// Use useRef when:\n// - You need DOM access\n// - You need a mutable value that doesn't affect rendering\n// Use useState when:\n// - Value changes should update the UI"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useContext Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useContext lets you subscribe to React context without introducing nesting. Context provides a way to pass data through the component tree without prop drilling."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Creating Context"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Create and provide context",
        "language": "jsx",
        "code": "import { createContext, useContext, useState } from 'react';\n\n// Create context with default value\nconst ThemeContext = createContext('light');\n\nfunction App() {\n  const [theme, setTheme] = useState('light');\n\n  return (\n    <ThemeContext.Provider value={{ theme, setTheme }}>\n      <Toolbar />\n    </ThemeContext.Provider>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useContext Consumer"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Consume context with useContext",
        "language": "jsx",
        "code": "function ThemedButton() {\n  // Access context anywhere in the tree\n  const { theme, setTheme } = useContext(ThemeContext);\n\n  return (\n    <button\n      className={theme === 'dark' ? 'dark-btn' : 'light-btn'}\n      onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}\n    >\n      Switch to {theme === 'light' ? 'Dark' : 'Light'}\n    </button>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Multiple Contexts"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Multiple contexts",
        "language": "jsx",
        "code": "const UserContext = createContext();\nconst ThemeContext = createContext();\n\nfunction App() {\n  const [user, setUser] = useState({ name: 'John' });\n  const [theme, setTheme] = useState('light');\n\n  return (\n    <UserContext.Provider value={user}>\n      <ThemeContext.Provider value={theme}>\n        <Profile />\n      </ThemeContext.Provider>\n    </UserContext.Provider>\n  );\n}\n\nfunction Profile() {\n  const user = useContext(UserContext);\n  const theme = useContext(ThemeContext);\n  return <div>User: {user.name}, Theme: {theme}</div>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useReducer Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useReducer is an alternative to useState for complex state logic. It accepts a reducer function and returns the current state paired with a dispatch method."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Reducer Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useReducer basic",
        "language": "jsx",
        "code": "const initialState = { count: 0 };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'increment':\n      return { count: state.count + 1 };\n    case 'decrement':\n      return { count: state.count - 1 };\n    case 'reset':\n      return { count: 0 };\n    default:\n      throw new Error(`Unknown action: ${action.type}`);\n  }\n}\n\nfunction Counter() {\n  const [state, dispatch] = useReducer(reducer, initialState);\n\n  return (\n    <div>\n      <p>Count: {state.count}</p>\n      <button onClick={() => dispatch({ type: 'increment' })}>+</button>\n      <button onClick={() => dispatch({ type: 'decrement' })}>-</button>\n      <button onClick={() => dispatch({ type: 'reset' })}>Reset</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useReducer with useContext (Global State)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Global state with useReducer + useContext",
        "language": "jsx",
        "code": "const StateContext = createContext();\nconst DispatchContext = createContext();\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'addTodo':\n      return { ...state, todos: [...state.todos, action.payload] };\n    case 'removeTodo':\n      return { ...state, todos: state.todos.filter((_, i) => i !== action.payload) };\n    default:\n      return state;\n  }\n}\n\nfunction App() {\n  const [state, dispatch] = useReducer(reducer, { todos: [] });\n\n  return (\n    <StateContext.Provider value={state}>\n      <DispatchContext.Provider value={dispatch}>\n        <TodoList />\n      </DispatchContext.Provider>\n    </StateContext.Provider>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Complex State Logic"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Complex state with useReducer",
        "language": "jsx",
        "code": "function todoReducer(state, action) {\n  switch (action.type) {\n    case 'ADD':\n      return [\n        ...state,\n        {\n          id: Date.now(),\n          text: action.text,\n          completed: false\n        }\n      ];\n    case 'TOGGLE':\n      return state.map(todo =>\n        todo.id === action.id\n          ? { ...todo, completed: !todo.completed }\n          : todo\n      );\n    case 'DELETE':\n      return state.filter(todo => todo.id !== action.id);\n    default:\n      return state;\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useMemo Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useMemo memoizes expensive calculations. It only recalculates the value when one of the dependencies changes."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Memoizing Expensive Calculations"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useMemo for expensive computation",
        "language": "jsx",
        "code": "function ExpensiveComponent({ items, filter }) {\n  const filteredItems = useMemo(() => {\n    console.log('Filtering items...');\n    return items.filter(item => item.category === filter);\n  }, [items, filter]); // Only recalculate when items or filter change\n\n  return <ul>{filteredItems.map(item => <li key={item.id}>{item.name}</li>)}</ul>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "When NOT to use useMemo"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Avoid unnecessary useMemo",
        "language": "jsx",
        "code": "// DON'T use for simple operations\nconst fullName = useMemo(() => `${first} ${last}`, [first, last]);\n\n// DO use for expensive operations\nconst sortedList = useMemo(() => {\n  return largeArray.sort((a, b) => a.id - b.id);\n}, [largeArray]);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useCallback Hook"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useCallback memoizes event handler functions. It returns a memoized version of the callback that only changes if dependencies change."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Memoizing Event Handlers"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useCallback for event handlers",
        "language": "jsx",
        "code": "function Parent() {\n  const [count, setCount] = useState(0);\n\n  // Without useCallback: new function created on every render\n  // With useCallback: memoized, prevents Child re-renders\n  const handleClick = useCallback(() => {\n    console.log('Button clicked');\n  }, []); // Empty deps = stable reference\n\n  return (\n    <div>\n      <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>\n      <Child onClick={handleClick} />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useCallback with React.memo"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useCallback + React.memo",
        "language": "jsx",
        "code": "const Child = React.memo(function Child({ onClick, label }) {\n  console.log('Child rendered:', label);\n  return <button onClick={onClick}>{label}</button>;\n});\n\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  const handleClick = useCallback(() => {\n    console.log('Clicked');\n  }, []);\n\n  // Child won't re-render when count changes\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <Child onClick={handleClick} label=\"Stable\" />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Dependency Array"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useCallback dependencies",
        "language": "jsx",
        "code": "function Search({ query, onSearch }) {\n  // Re-create callback when query changes\n  const debouncedSearch = useCallback(\n    debounce((q) => onSearch(q), 300),\n    [onSearch]\n  );\n\n  useEffect(() => {\n    debouncedSearch(query);\n  }, [query, debouncedSearch]);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Custom Hooks"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Custom hooks let you extract component logic into reusable functions. They must start with 'use' and can call other hooks."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Custom Hook Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Custom hook for fetching",
        "language": "jsx",
        "code": "function useFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    let isMounted = true;\n\n    fetch(url)\n      .then(res => res.json())\n      .then(data => {\n        if (isMounted) {\n          setData(data);\n          setLoading(false);\n        }\n      })\n      .catch(err => {\n        if (isMounted) {\n          setError(err);\n          setLoading(false);\n        }\n      });\n\n    return () => { isMounted = false; };\n  }, [url]);\n\n  return { data, loading, error };\n}\n\n// Usage in component\nfunction Posts() {\n  const { data, loading, error } = useFetch('/api/posts');\n  if (loading) return <p>Loading...</p>;\n  if (error) return <p>Error: {error.message}</p>;\n  return <ul>{data.map(post => <li key={post.id}>{post.title}</li>)}</ul>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useLocalStorage Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useLocalStorage custom hook",
        "language": "jsx",
        "code": "function useLocalStorage(key, initialValue) {\n  const [storedValue, setStoredValue] = useState(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      return initialValue;\n    }\n  });\n\n  const setValue = (value) => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue];\n}\n\n// Usage:\nconst [name, setName] = useLocalStorage('name', '');"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React.memo"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React.memo is a higher-order component that memoizes the rendered output. It prevents re-renders when props haven't changed."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Memoizing Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React.memo basic",
        "language": "jsx",
        "code": "const ExpensiveComponent = React.memo(function ExpensiveComponent({ data }) {\n  console.log('Rendering expensive component');\n  return <div>{/* render data */}</div>;\n});\n\n// Parent component\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  return (\n    <div>\n      <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>\n      {/* Won't re-render when count changes */}\n      <ExpensiveComponent data={largeDataset} />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Custom Comparison Function"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React.memo with comparison function",
        "language": "jsx",
        "code": "const UserCard = React.memo(\n  function UserCard({ user }) {\n    return <div>{user.name}</div>;\n  },\n  (prevProps, nextProps) => {\n    // Custom comparison - return true to skip re-render\n    return prevProps.user.id === nextProps.user.id;\n  }\n);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useTransition & useDeferredValue"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Concurrent React features that let you prioritize updates. useTransition marks state updates as non-urgent. useDeferredValue defers a value."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useTransition"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useTransition for non-urgent updates",
        "language": "jsx",
        "code": "import { useState, useTransition } from 'react';\n\nfunction SearchResults() {\n  const [query, setQuery] = useState('');\n  const [results, setResults] = useState([]);\n  const [isPending, startTransition] = useTransition();\n\n  const handleChange = (e) => {\n    // Urgent: update input immediately\n    setQuery(e.target.value);\n\n    // Non-urgent: defer list update\n    startTransition(() => {\n      const filtered = filterItems(e.target.value);\n      setResults(filtered);\n    });\n  };\n\n  return (\n    <div>\n      <input value={query} onChange={handleChange} />\n      {isPending && <p>Loading results...</p>}\n      <ul>{results.map(r => <li key={r.id}>{r.name}</li>)}</ul>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useDeferredValue"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useDeferredValue",
        "language": "jsx",
        "code": "import { useState, useDeferredValue } from 'react';\n\nfunction SearchResults({ query }) {\n  // Defer the query value\n  const deferredQuery = useDeferredValue(query);\n\n  // Filter with deferred value (lags behind input)\n  const results = useMemo(() => {\n    return filterItems(deferredQuery);\n  }, [deferredQuery]);\n\n  return (\n    <div>\n      <input value={query} onChange={e => setQuery(e.target.value)} />\n      <ResultsList query={deferredQuery} results={results} />\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Form Handling"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Forms in React can be controlled (React manages state) or uncontrolled (DOM manages state)."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Controlled Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Controlled input",
        "language": "jsx",
        "code": "function NameForm() {\n  const [name, setName] = useState('');\n  const [email, setEmail] = useState('');\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    console.log({ name, email });\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        type=\"text\"\n        value={name}\n        onChange={(e) => setName(e.target.value)}\n        placeholder=\"Name\"\n      />\n      <input\n        type=\"email\"\n        value={email}\n        onChange={(e) => setEmail(e.target.value)}\n        placeholder=\"Email\"\n      />\n      <button type=\"submit\">Submit</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Uncontrolled Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Uncontrolled input with useRef",
        "language": "jsx",
        "code": "function NameForm() {\n  const nameRef = useRef(null);\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    // Access DOM value directly\n    console.log(nameRef.current.value);\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input ref={nameRef} type=\"text\" defaultValue=\"\" />\n      <button type=\"submit\">Submit</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Input Validation"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Form validation",
        "language": "jsx",
        "code": "function SignupForm() {\n  const [formData, setFormData] = useState({\n    email: '',\n    password: ''\n  });\n  const [errors, setErrors] = useState({});\n\n  const validate = () => {\n    const newErrors = {};\n    if (!formData.email.includes('@')) {\n      newErrors.email = 'Invalid email';\n    }\n    if (formData.password.length < 8) {\n      newErrors.password = 'Password must be 8+ characters';\n    }\n    setErrors(newErrors);\n    return Object.keys(newErrors).length === 0;\n  };\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    if (validate()) {\n      console.log('Form valid:', formData);\n    }\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        value={formData.email}\n        onChange={e => setFormData({...formData, email: e.target.value})}\n      />\n      {errors.email && <span className=\"error\">{errors.email}</span>}\n      <button type=\"submit\">Sign Up</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Lists & Keys"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Rendering lists in React requires a unique key prop for each item to help React identify which items have changed."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Rendering Lists with map()"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Rendering lists",
        "language": "jsx",
        "code": "function TodoList({ todos }) {\n  return (\n    <ul>\n      {todos.map((todo) => (\n        <li key={todo.id}>\n          {todo.text}\n        </li>\n      ))}\n    </ul>\n  );\n}\n\n// With index (only for static lists)\nfunction NumberList({ numbers }) {\n  return (\n    <ul>\n      {numbers.map((number) => (\n        <li key={number}>{number}</li>\n      ))}\n    </ul>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Key Prop Importance"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Keys in lists",
        "language": "jsx",
        "code": "// Correct: stable unique IDs\n{items.map(item => (\n  <ListItem key={item.id} item={item} />\n))}\n\n// Incorrect: index as key (causes bugs with reordering)\n{items.map((item, index) => (\n  <ListItem key={index} item={item} />\n))}\n\n// Why avoid index as key?\n// - Reordering items causes unnecessary re-renders\n// - State gets associated with wrong items\n// - Performance degradation on large lists"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Conditional Rendering"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React allows you to render different UI based on conditions using JavaScript operators."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Ternary Operator"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Ternary conditional rendering",
        "language": "jsx",
        "code": "function Greeting({ isLoggedIn }) {\n  return (\n    <div>\n      {isLoggedIn ? (\n        <h1>Welcome back!</h1>\n      ) : (\n        <h1>Please sign in.</h1>\n      )}\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Logical AND (&&) Operator"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Short-circuit rendering",
        "language": "jsx",
        "code": "function Notification({ messages }) {\n  return (\n    <div>\n      <h1>Hello!</h1>\n      {messages.length > 0 && (\n        <p>You have {messages.length} unread messages.</p>\n      )}\n    </div>\n  );\n}\n\n// Note: 0 will render, use explicit boolean\n{messages.length > 0 && <p>{messages.length} messages</p>}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "If/Else with Early Returns"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Early return pattern",
        "language": "jsx",
        "code": "function UserProfile({ user }) {\n  if (!user) {\n    return <p>Loading...</p>;\n  }\n\n  if (user.isBanned) {\n    return <p>Account suspended</p>;\n  }\n\n  return (\n    <div>\n      <h1>{user.name}</h1>\n      <p>{user.email}</p>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Switch for Multiple Conditions"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Switch-based rendering",
        "language": "jsx",
        "code": "function StatusBadge({ status }) {\n  const getStatusColor = (status) => {\n    switch (status) {\n      case 'active': return 'green';\n      case 'pending': return 'yellow';\n      case 'inactive': return 'red';\n      default: return 'gray';\n    }\n  };\n\n  return (\n    <span className={`badge badge-${getStatusColor(status)}`}>\n      {status.toUpperCase()}\n    </span>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Custom Hooks (Sharing Logic)"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Extract and reuse stateful logic across components using custom hooks. Name convention: must start with 'use'."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useWindowSize Hook"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useWindowSize custom hook",
        "language": "jsx",
        "code": "function useWindowSize() {\n  const [size, setSize] = useState({\n    width: window.innerWidth,\n    height: window.innerHeight\n  });\n\n  useEffect(() => {\n    const handleResize = () => {\n      setSize({\n        width: window.innerWidth,\n        height: window.innerHeight\n      });\n    };\n\n    window.addEventListener('resize', handleResize);\n    return () => window.removeEventListener('resize', handleResize);\n  }, []);\n\n  return size;\n}\n\n// Usage:\nfunction Component() {\n  const { width, height } = useWindowSize();\n  return <p>Window: {width}x{height}</p>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "useToggle Hook"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "useToggle custom hook",
        "language": "jsx",
        "code": "function useToggle(initialValue = false) {\n  const [value, setValue] = useState(initialValue);\n\n  const toggle = useCallback(() => setValue(v => !v), []);\n  const setTrue = useCallback(() => setValue(true), []);\n  const setFalse = useCallback(() => setValue(false), []);\n\n  return { value, toggle, setTrue, setFalse };\n}\n\n// Usage:\nfunction Modal() {\n  const { value: isOpen, toggle, setFalse: close } = useToggle();\n  return (\n    <>\n      <button onClick={toggle}>Toggle</button>\n      {isOpen && <div onClick={close}>Modal content</div>}\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Error Boundaries"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Error boundaries catch JavaScript errors in child components, log them, and display a fallback UI. They must be class components."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Error Boundary Component"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Error boundary class component",
        "language": "jsx",
        "code": "class ErrorBoundary extends React.Component {\n  constructor(props) {\n    super(props);\n    this.state = { hasError: false, error: null };\n  }\n\n  static getDerivedStateFromError(error) {\n    // Update state to show fallback UI\n    return { hasError: true, error };\n  }\n\n  componentDidCatch(error, errorInfo) {\n    // Log error to service\n    console.error('Error caught:', error, errorInfo);\n  }\n\n  render() {\n    if (this.state.hasError) {\n      return (\n        <div className=\"error-fallback\">\n          <h2>Something went wrong</h2>\n          <button onClick={() => this.setState({ hasError: false })}>\n            Try again\n          </button>\n        </div>\n      );\n    }\n\n    return this.props.children;\n  }\n}\n\n// Usage:\n<ErrorBoundary>\n  <MyComponent />\n</ErrorBoundary>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Fallback UI Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Error boundary with fallback",
        "language": "jsx",
        "code": "function ErrorFallback({ error, resetErrorBoundary }) {\n  return (\n    <div role=\"alert\">\n      <h2>Oops! Something went wrong</h2>\n      <pre>{error.message}</pre>\n      <button onClick={resetErrorBoundary}>Try again</button>\n    </div>\n  );\n}\n\nfunction App() {\n  return (\n    <ErrorBoundary FallbackComponent={ErrorFallback}>\n      <Dashboard />\n    </ErrorBoundary>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React Portals"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Portals let you render children into a DOM node outside the parent component hierarchy, useful for modals, tooltips, and dropdowns."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Portal for Modals"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Modal with React Portal",
        "language": "jsx",
        "code": "import { createPortal } from 'react-dom';\n\nfunction Modal({ isOpen, onClose, children }) {\n  if (!isOpen) return null;\n\n  return createPortal(\n    <div className=\"modal-overlay\" onClick={onClose}>\n      <div className=\"modal-content\" onClick={e => e.stopPropagation()}>\n        <button onClick={onClose}>Close</button>\n        {children}\n      </div>\n    </div>,\n    document.getElementById('modal-root')\n  );\n}\n\n// Usage:\nfunction App() {\n  const [isOpen, setIsOpen] = useState(false);\n  return (\n    <>\n      <button onClick={() => setIsOpen(true)}>Open Modal</button>\n      <Modal isOpen={isOpen} onClose={() => setIsOpen(false)}>\n        <h2>Modal Title</h2>\n        <p>Modal content</p>\n      </Modal>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Portal with Event Bubbling"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Event bubbling through portals",
        "language": "jsx",
        "code": "// Events bubble to React tree, not DOM tree\nfunction Parent() {\n  const handleClick = () => console.log('Parent clicked');\n  return (\n    <div onClick={handleClick}>\n      <Modal />\n    </div>\n  );\n}\n\nfunction Modal() {\n  return createPortal(\n    <button onClick={() => console.log('Modal button clicked')}>\n      Click me\n    </button>,\n    document.getElementById('modal-root')\n  );\n}\n\n// Both handlers fire because React events bubble through React tree"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Code Splitting & Lazy Loading"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Code splitting lets you load components only when needed, reducing initial bundle size."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "React.lazy and Suspense"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Lazy loading with React.lazy",
        "language": "jsx",
        "code": "import { lazy, Suspense } from 'react';\n\n// Lazy load component\nconst Dashboard = lazy(() => import('./Dashboard'));\nconst Settings = lazy(() => import('./Settings'));\n\nfunction App() {\n  const [page, setPage] = useState('home');\n\n  return (\n    <div>\n      <nav>\n        <button onClick={() => setPage('home')}>Home</button>\n        <button onClick={() => setPage('dashboard')}>Dashboard</button>\n        <button onClick={() => setPage('settings')}>Settings</button>\n      </nav>\n\n      <Suspense fallback={<div>Loading...</div>}>\n        {page === 'dashboard' && <Dashboard />}\n        {page === 'settings' && <Settings />}\n      </Suspense>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Error Boundaries with Lazy"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Error boundary for lazy components",
        "language": "jsx",
        "code": "import { lazy, Suspense } from 'react';\n\nconst OtherComponent = lazy(() => import('./OtherComponent'));\n\nfunction ErrorBoundary({ children, fallback }) {\n  const [hasError, setHasError] = useState(false);\n\n  if (hasError) {\n    return fallback || <p>Something went wrong</p>;\n  }\n\n  return (\n    <Suspense fallback={<div>Loading...</div>}>\n      {children}\n    </Suspense>\n  );\n}\n\nfunction App() {\n  return (\n    <ErrorBoundary\n      fallback={<p>Failed to load component</p>}\n    >\n      <OtherComponent />\n    </ErrorBoundary>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Performance Optimization"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React is fast by default, but these patterns help optimize performance for large applications."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Avoid Inline Functions in Render"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Avoid inline functions",
        "language": "jsx",
        "code": "// Bad: creates new function every render\n<button onClick={() => handleClick(id)}>Click</button>\n\n// Good: memoized callback\nconst handleClick = useCallback((id) => {\n  console.log(id);\n}, []);\n\n<button onClick={() => handleClick(id)}>Click</button>\n\n// Better: pass value directly\n<button onClick={handleClick} data-id={id}>Click</button>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Virtualization Concept"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "List virtualization concept",
        "language": "jsx",
        "code": "import { FixedSizeList } from 'react-window';\n\nfunction LargeList({ items }) {\n  const Row = ({ index, style }) => (\n    <div style={style}>\n      {items[index].name}\n    </div>\n  );\n\n  // Only renders visible items + buffer\n  return (\n    <FixedSizeList\n      height={400}\n      itemCount={items.length}\n      itemSize={35}\n      width={300}\n    >\n      {Row}\n    </FixedSizeList>\n  );\n}\n\n// For 10,000 items, only ~20 DOM nodes exist at once"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Memoization Summary"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "When to memoize",
        "language": "jsx",
        "code": "// React.memo: wrap components receiving stable props\nconst MemoizedComponent = React.memo(Component);\n\n// useMemo: cache expensive calculations\nconst result = useMemo(() => heavyCalc(data), [data]);\n\n// useCallback: memoize event handlers passed to memoized children\nconst handler = useCallback(() => doSomething(id), [id]);\n\n// Use when:\n// - Component renders often with same props\n// - Component is pure (same input = same output)\n// - Component is expensive to render\n\n// Don't use when:\n// - Component is small/cheap\n// - Props change frequently anyway"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Testing Components"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React Testing Library encourages testing components as users would use them, focusing on behavior over implementation."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Basic Rendering"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "React Testing Library basics",
        "language": "typescript",
        "code": "import { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport Counter from './Counter';\n\n// Render component\nrender(<Counter initialCount={0} />);\n\n// Query elements\nconst button = screen.getByRole('button', { name: /increment/i });\nconst heading = screen.getByText(/count: 0/i);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Testing User Interactions"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Testing interactions",
        "language": "typescript",
        "code": "import { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport Counter from './Counter';\n\ntest('increments counter when button clicked', async () => {\n  render(<Counter />);\n  const button = screen.getByRole('button');\n\n  await userEvent.click(button);\n\n  expect(screen.getByText('Count: 1')).toBeInTheDocument();\n});\n\ntest('submits form with correct values', async () => {\n  render(<LoginForm />);\n\n  await userEvent.type(screen.getByLabelText(/email/i), 'test@example.com');\n  await userEvent.type(screen.getByLabelText(/password/i), 'password123');\n  await userEvent.click(screen.getByRole('button', { name: /submit/i }));\n\n  expect(await screen.findByText('Welcome!')).toBeInTheDocument();\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Jest Assertions"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Common Jest assertions",
        "language": "typescript",
        "code": "test('renders without crashing', () => {\n  render(<App />);\n  expect(screen.getByText('Hello World')).toBeInTheDocument();\n});\n\ntest('displays error message', () => {\n  render(<ErrorComponent />);\n  expect(screen.getByRole('alert')).toHaveTextContent('Error occurred');\n});\n\ntest('has correct class', () => {\n  render(<Button variant=\"primary\" />);\n  const button = screen.getByRole('button');\n  expect(button).toHaveClass('btn-primary');\n});\n\ntest('calls onSubmit with correct data', async () => {\n  const handleSubmit = jest.fn();\n  render(<Form onSubmit={handleSubmit} />);\n\n  await userEvent.click(screen.getByRole('button'));\n\n  expect(handleSubmit).toHaveBeenCalledWith(\n    expect.objectContaining({ email: 'test@test.com' })\n  );\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "useImperativeHandle"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "useImperativeHandle customizes the instance value exposed to parent components when using ref. Used with forwardRef."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "forwardRef with useImperativeHandle"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Forwarding refs with useImperativeHandle",
        "language": "jsx",
        "code": "import { forwardRef, useImperativeHandle, useRef } from 'react';\n\nconst FancyInput = forwardRef((props, ref) => {\n  const inputRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    focus: () => {\n      inputRef.current.focus();\n    },\n    clear: () => {\n      inputRef.current.value = '';\n    },\n    getValue: () => {\n      return inputRef.current.value;\n    }\n  }));\n\n  return <input ref={inputRef} type=\"text\" />;\n});\n\n// Parent component\nfunction Form() {\n  const inputRef = useRef(null);\n\n  const handleSubmit = () => {\n    console.log(inputRef.current.getValue());\n  };\n\n  return (\n    <>\n      <FancyInput ref={inputRef} />\n      <button onClick={handleSubmit}>Submit</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Ref Callbacks"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Ref callbacks pattern",
        "language": "jsx",
        "code": "function TextInputWithFocusButton() {\n  const inputEl = useRef(null);\n\n  // Ref callback\n  const setInputRef = (node) => {\n    if (node) {\n      inputEl.current = node;\n    }\n  };\n\n  return (\n    <>\n      <input ref={setInputRef} type=\"text\" />\n      <button onClick={() => inputEl.current?.focus()}>Focus</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Synthetic Events"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "React uses Synthetic Events, a cross-browser wrapper around native browser events. They provide consistent properties across browsers."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Event Handling"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Synthetic events",
        "language": "jsx",
        "code": "function Button() {\n  const handleClick = (e) => {\n    // Prevent default browser behavior\n    e.preventDefault();\n\n    // Access event properties\n    console.log(e.type);        // 'click'\n    console.log(e.target);      // DOM element\n    console.log(e.currentTarget); // Element with listener\n\n    // Synthetic events are pooled - access synchronously!\n    setTimeout(() => {\n      // e.target will be null here (pooled)\n    }, 1000);\n  };\n\n  return (\n    <button onClick={handleClick}>\n      Click me\n    </button>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Event Pooling"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Event pooling and persist()",
        "language": "jsx",
        "code": "function AsyncComponent() {\n  const handleClick = (e) => {\n    // Events are pooled and reused by React\n    // Access properties synchronously only\n    const buttonText = e.target.textContent;\n\n    // To use event asynchronously, persist it\n    e.persist();\n\n    setTimeout(() => {\n      // Now e.target is available\n      console.log(e.target.textContent);\n    }, 1000);\n  };\n\n  return <button onClick={handleClick}>Async Click</button>;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Accessing Event Properties"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Common event properties",
        "language": "jsx",
        "code": "function Form() {\n  const handleSubmit = (e) => {\n    e.preventDefault();\n  };\n\n  const handleChange = (e) => {\n    console.log(e.target.value);\n    console.log(e.target.name);\n    console.log(e.target.type);\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input name=\"email\" onChange={handleChange} />\n      <button type=\"submit\">Submit</button>\n    </form>\n  );\n}\n\n// Common events:\n// onClick, onChange, onSubmit, onFocus, onBlur\n// onKeyDown, onKeyUp, onKeyPress, onMouseEnter, onMouseLeave"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Forward Refs"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "forwardRef lets a component expose a DOM node to a parent component. Used for focus management, animations, and integrating with third-party DOM libraries."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Forwarding Refs Through Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "forwardRef pattern",
        "language": "jsx",
        "code": "import { forwardRef } from 'react';\n\n// Component that forwards its ref to the input element\nconst FancyInput = forwardRef((props, ref) => {\n  return (\n    <div className=\"fancy-input\">\n      <input ref={ref} {...props} />\n    </div>\n  );\n});\n\nFancyInput.displayName = 'FancyInput';\n\n// Parent can now access the input DOM node\nfunction Form() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <>\n      <FancyInput ref={inputRef} />\n      <button onClick={focusInput}>Focus</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "forwardRef with useImperativeHandle"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Exposing specific methods via forwardRef",
        "language": "jsx",
        "code": "import { forwardRef, useImperativeHandle, useRef } from 'react';\n\nconst VideoPlayer = forwardRef((props, ref) => {\n  const videoRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    play: () => videoRef.current.play(),\n    pause: () => videoRef.current.pause(),\n    seek: (time) => videoRef.current.currentTime = time\n  }));\n\n  return <video ref={videoRef} src={props.src} />;\n});\n\nfunction App() {\n  const playerRef = useRef(null);\n\n  return (\n    <>\n      <VideoPlayer ref={playerRef} src=\"/video.mp4\" />\n      <button onClick={() => playerRef.current.play()}>Play</button>\n      <button onClick={() => playerRef.current.pause()}>Pause</button>\n    </>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Component Composition Patterns"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Advanced patterns for building flexible and reusable component APIs."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Compound Components"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Compound component pattern",
        "language": "jsx",
        "code": "import { createContext, useContext } from 'react';\n\nconst TabsContext = createContext();\n\nfunction Tabs({ children, defaultIndex }) {\n  const [activeIndex, setActiveIndex] = useState(defaultIndex || 0);\n\n  return (\n    <TabsContext.Provider value={{ activeIndex, setActiveIndex }}>\n      <div className=\"tabs\">{children}</div>\n    </TabsContext.Provider>\n  );\n}\n\nfunction TabList({ children }) {\n  return <div className=\"tab-list\" role=\"tablist\">{children}</div>;\n}\n\nfunction Tab({ children, index }) {\n  const { activeIndex, setActiveIndex } = useContext(TabsContext);\n  return (\n    <button\n      role=\"tab\"\n      aria-selected={activeIndex === index}\n      onClick={() => setActiveIndex(index)}\n    >\n      {children}\n    </button>\n  );\n}\n\nfunction TabPanels({ children }) {\n  const { activeIndex } = useContext(TabsContext);\n  return <div>{React.Children.toArray(children)[activeIndex]}</div>;\n}\n\n// Usage:\n<Tabs defaultIndex={0}>\n  <TabList>\n    <Tab index={0}>Tab 1</Tab>\n    <Tab index={1}>Tab 2</Tab>\n  </TabList>\n  <TabPanels>\n    <div>Panel 1</div>\n    <div>Panel 2</div>\n  </TabPanels>\n</Tabs>"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Render Props Pattern"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Render props pattern",
        "language": "jsx",
        "code": "function MouseTracker({ render }) {\n  const [position, setPosition] = useState({ x: 0, y: 0 });\n\n  useEffect(() => {\n    const handleMouseMove = (e) => {\n      setPosition({ x: e.clientX, y: e.clientY });\n    };\n\n    window.addEventListener('mousemove', handleMouseMove);\n    return () => window.removeEventListener('mousemove', handleMouseMove);\n  }, []);\n\n  // Render prop receives the state\n  return render(position);\n}\n\n// Usage:\nfunction App() {\n  return (\n    <MouseTracker>\n      {({ x, y }) => (\n        <p>Mouse position: {x}, {y}</p>\n      )}\n    </MouseTracker>\n  );\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Higher-Order Components (HOC)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Higher-order component",
        "language": "jsx",
        "code": "// HOC: a function that takes a component and returns a new component\nfunction withLoading(WrappedComponent) {\n  return function WithLoadingComponent({ isLoading, ...props }) {\n    if (isLoading) {\n      return <div>Loading...</div>;\n    }\n\n    return <WrappedComponent {...props} />;\n  };\n}\n\n// Usage:\nfunction UserProfile({ user }) {\n  return <div>{user.name}</div>;\n}\n\nconst UserProfileWithLoading = withLoading(UserProfile);\n\n// In parent:\n<UserProfileWithLoading isLoading={true} user={userData} />"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Quick Reference Summary"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Common Hook Cheat Sheet"
      }
    },
    {
      "type": "table",
      "data": {
        "headers": [
          "Hook",
          "Purpose",
          "Returns"
        ],
        "rows": [
          [
            "useState",
            "Add state to functional components",
            "[state, setState]"
          ],
          [
            "useEffect",
            "Side effects (data fetch, subscriptions)",
            "void (cleanup optional)"
          ],
          [
            "useContext",
            "Access context values",
            "Context value"
          ],
          [
            "useReducer",
            "Complex state management",
            "[state, dispatch]"
          ],
          [
            "useCallback",
            "Memoize functions",
            "Memoized callback"
          ],
          [
            "useMemo",
            "Memoize values/computations",
            "Memoized value"
          ],
          [
            "useRef",
            "DOM access or mutable values",
            "{ current: value }"
          ],
          [
            "useLayoutEffect",
            "Synchronous DOM mutations",
            "void (cleanup optional)"
          ]
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Rules of Hooks"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Rules of Hooks",
        "language": "jsx",
        "code": "// 1. Only call hooks at the top level\n// Don't call inside conditions, loops, or nested functions\nfunction Component({ condition }) {\n  // CORRECT\n  useEffect(() => { ... }, []);\n\n  // INCORRECT - conditional hook call\n  if (condition) {\n    useEffect(() => { ... }, []);\n  }\n}\n\n// 2. Only call hooks from React functions\n// - Functional components\n// - Custom hooks (must start with 'use')\nfunction useCustomHook() {\n  return useState(0); // OK\n}\n\nfunction regularFunction() {\n  useState(0); // Violates rules!"
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
