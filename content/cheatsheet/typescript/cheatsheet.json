{
  "id": "lesson-cheatsheet-typescript",
  "slug": "cheatsheet",
  "moduleSlug": "typescript",
  "courseSlug": "cheatsheet",
  "title": "TypeScript Complete Cheat Sheet",
  "description": "Comprehensive quick reference covering TypeScript type system, interfaces, generics, advanced types, React typing, design patterns, and decorators.",
  "order": 9,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": ["typescript", "generics", "interfaces", "advanced-types", "react", "decorators", "utility-types", "cheatsheet", "quick-reference"],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Basic Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Primitive Types",
        "language": "typescript",
        "code": "// String, number, boolean\nlet name: string = \"Alice\";\nlet age: number = 30;\nlet active: boolean = true;\n\n// Arrays: two syntaxes\nlet scores: number[] = [90, 85, 92];\nlet names: Array<string> = [\"Alice\", \"Bob\"];\n\n// Tuple — fixed length and types\nlet pair: [string, number] = [\"id\", 42];\n\n// Enum\nenum Status { Active, Inactive, Pending }\nlet current: Status = Status.Active;\n\n// Special types\nlet anything: any = 5;\nlet unknownValue: unknown = \"safe\";\nlet neverValue: never = (() => { throw new Error(); })();\nlet empty: void = undefined;\nlet big: bigint = 9007199254740991n;"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Null and Undefined",
        "language": "typescript",
        "code": "// null and undefined are valid values\nlet notAssigned: undefined = undefined;\nlet emptyValue: null = null;\n\n// With strictNullChecks, these are distinct\nlet maybeNumber: number | null = 5;\nlet maybeString: string | undefined = \"hello\";\n\n// void for functions with no return\nfunction log(message: string): void {\n  console.log(message);\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Object Type",
        "language": "typescript",
        "code": "// Generic object type (non-primitive)\nlet config: object = { api: \"/v1\", timeout: 5000 };\n\n// Prefer specific shapes over 'object'\ninterface ConfigShape {\n  api: string;\n  timeout: number;\n}\nlet config2: ConfigShape = { api: \"/v1\", timeout: 5000 };"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Type Annotations & Inference"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Explicit vs Inferred Types",
        "language": "typescript",
        "code": "// Explicit type annotation\nlet score: number = 100;\n\n// Type inference (preferred for simple cases)\nlet inferredScore = 100; // number\nlet inferredName = \"Alice\"; // string\n\n// 'as const' for literal inference\nconst colors = [\"red\", \"green\", \"blue\"] as const;\n// readonly [\"red\", \"green\", \"blue\"]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Interfaces"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Interface",
        "language": "typescript",
        "code": "interface User {\n  id: number;\n  name: string;\n  email: string;\n}\n\nconst user: User = {\n  id: 1,\n  name: \"Alice\",\n  email: \"alice@example.com\",\n};"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Optional and Readonly Properties",
        "language": "typescript",
        "code": "interface Product {\n  id: number;\n  name: string;\n  description?: string; // optional\n  readonly sku: string;   // cannot be reassigned\n}\n\nconst p: Product = { id: 1, name: \"Widget\", sku: \"W-100\" };\n// p.sku = \"X\"; // Error: cannot assign to 'sku'\n// p.id = 2;    // Error: cannot assign to 'id'"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Index Signatures and Extending",
        "language": "typescript",
        "code": "interface StringMap {\n  [key: string]: string;\n}\n\nconst settings: StringMap = {\n  theme: \"dark\",\n  language: \"en\",\n};\n\n// Extending interfaces\ninterface Animal { name: string; }\ninterface Dog extends Animal { breed: string; }\nconst dog: Dog = { name: \"Rex\", breed: \"Labrador\" };"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Hybrid Type (Function + Object)",
        "language": "typescript",
        "code": "interface Counter {\n  (): void;\n  count: number;\n}\n\nfunction createCounter(): Counter {\n  let count = 0;\n  const fn = () => { count++; console.log(count); };\n  fn.count = count;\n  return fn;\n}\n\nconst counter = createCounter();\ncounter();\ncounter();"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Type Aliases"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Union and Intersection Types",
        "language": "typescript",
        "code": "// Union: value can be one of several types\nlet id: string | number = \"abc\";\nid = 123;\n\n// Intersection: combines multiple types\ninterface A { a: string; }\ninterface B { b: number; }\ntype AB = A & B;\nconst ab: AB = { a: \"x\", b: 1 };\n\n// Literal types\nlet status: \"active\" | \"inactive\" = \"active\";"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Discriminated Unions",
        "language": "typescript",
        "code": "interface Circle { kind: \"circle\"; radius: number; }\ninterface Square { kind: \"square\"; side: number; }\n\ntype Shape = Circle | Square;\n\nfunction area(shape: Shape): number {\n  if (shape.kind === \"circle\") {\n    return Math.PI * shape.radius ** 2;\n  }\n  // shape is Square here\n  return shape.side ** 2;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Functions"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Function Types and Parameters",
        "language": "typescript",
        "code": "// Parameter and return types\nfunction add(a: number, b: number): number {\n  return a + b;\n}\n\n// Optional and rest parameters\nfunction greet(name: string, greeting?: string, ...args: string[]): void {\n  console.log(greeting ?? \"Hello\", name, ...args);\n}\n\n// Function type syntax\nlet math: (x: number, y: number) => number;\nmath = (x, y) => x * y;\n\n// Callback type\ntype Handler = (event: MouseEvent) => void;\nlet onClick: Handler = (e) => console.log(e.clientX);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Function Overloading",
        "language": "typescript",
        "code": "function processValue(value: string): string;\nfunction processValue(value: number): number;\nfunction processValue(value: string | number): string | number {\n  if (typeof value === \"string\") {\n    return value.toUpperCase();\n  }\n  return value * 2;\n}\n\nconst s = processValue(\"hello\"); // string\nconst n = processValue(5);        // number"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Objects"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Object Utilities (Partial, Required, Readonly)",
        "language": "typescript",
        "code": "interface Config {\n  api: string;\n  timeout: number;\n  retries?: number;\n}\n\n// Partial — make all properties optional\nconst partial: Partial<Config> = { api: \"/v1\" };\n\n// Required — make all properties required\nconst required: Required<Config> = { api: \"/v1\", timeout: 5000, retries: 3 };\n\n// Readonly — make all properties readonly\nconst frozen: Readonly<Config> = { api: \"/v1\", timeout: 5000 };\n// frozen.api = \"/v2\"; // Error"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Pick, Omit, and Record",
        "language": "typescript",
        "code": "interface User {\n  id: number;\n  name: string;\n  email: string;\n  role: string;\n}\n\n// Pick — select specific keys\ntype UserPreview = Pick<User, \"id\" | \"name\">;\nconst preview: UserPreview = { id: 1, name: \"Alice\" };\n\n// Omit — exclude specific keys\ntype UserWithoutEmail = Omit<User, \"email\">;\nconst withoutEmail: UserWithoutEmail = { id: 1, name: \"Alice\", role: \"admin\" };\n\n// Record — build an object type from a union of keys\ntype Role = \"admin\" | \"user\" | \"guest\";\nconst permissions: Record<Role, string[]> = {\n  admin: [\"read\", \"write\", \"delete\"],\n  user: [\"read\"],\n  guest: [\"read\"],\n};"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Generics"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generic Function and Constraints",
        "language": "typescript",
        "code": "// Generic function — preserves type information\nfunction identity<T>(value: T): T {\n  return value;\n}\n\nconst num = identity<number>(42);\nconst str = identity(\"hello\"); // inferred\n\n// Generic constraint\nfunction getLength<T extends { length: number }>(item: T): number {\n  return item.length;\n}\n\ngetLength([1, 2, 3]);\ngetLength(\"TypeScript\");\n// getLength(42); // Error: no length property"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "keyof and infer Keywords",
        "language": "typescript",
        "code": "interface Person {\n  name: string;\n  age: number;\n}\n\ntype PersonKeys = keyof Person; // \"name\" | \"age\"\n\n// keyof with generic\nfunction getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\n\nconst p: Person = { name: \"Alice\", age: 30 };\nconst nameValue: string = getProperty(p, \"name\");\n\n// infer — extract types from conditional types\ntype ReturnTypeOf<T> = T extends (...args: any[]) => infer R ? R : never;\ntype Str = ReturnTypeOf<() => string>; // string"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Conditional Types",
        "language": "typescript",
        "code": "// T extends U ? X : Y\ntype IsString<T> = T extends string ? true : false;\n\ntype A = IsString<string>; // true\ntype B = IsString<number>; // false\n\n// Nested conditional\ntype ToArray<T> = T extends any[] ? T : T[];\n\ntype NumArr = ToArray<number>; // number[]\ntype AlreadyArr = ToArray<string[]>; // string[]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Utility Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Extract, Exclude, and NonNullable",
        "language": "typescript",
        "code": "type A = \"a\" | \"b\" | \"c\";\ntype B = \"b\" | \"c\";\n\n// Exclude — remove types\ntype AB = Exclude<A, \"a\">; // \"b\" | \"c\"\n\n// Extract — keep only matching types\ntype BA = Extract<A, B>; // \"b\" | \"c\"\n\n// NonNullable — remove null and undefined\ntype MaybeStr = string | null | undefined;\ntype NotNull = NonNullable<MaybeStr>; // string"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ReturnType, Parameters, and Awaited",
        "language": "typescript",
        "code": "function fetchData(url: string): Promise<{ data: string }> {\n  return Promise.resolve({ data: \"result\" });\n}\n\n// ReturnType — extract return type of a function\ntype FetchResult = ReturnType<typeof fetchData>;\n// { data: string }\n\n// Parameters — extract parameter types as a tuple\ntype FetchParams = Parameters<typeof fetchData>;\n// [url: string]\n\n// Awaited — unwrap Promise type (TS 4.5+)\ntype Unwrapped = Awaited<Promise<string>>; // string"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Satisfies Operator",
        "language": "typescript",
        "code": "// TS 4.9+ — validate expression against a type while preserving literal types\nconst config = {\n  api: \"/v1\",\n  retries: 3,\n} satisfies Config;\n\n// config.api is inferred as \"/v1\" (literal), not string\nfunction init(cfg: typeof config) {\n  console.log(cfg.api);\n}\n\n// Also useful for ensuring exhaustive object shapes\nconst handlers = {\n  onSuccess: () => {},\n  onError: () => {},\n} satisfies Record<string, () => void>;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Advanced Types"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Mapped Types",
        "language": "typescript",
        "code": "// Transform each property of a type\ntype Readonly<T> = { readonly [P in keyof T]: T[P]; };\n\ntype Optional<T> = { [P in keyof T]?: T[P] };\n\ninterface User { id: number; name: string; }\ntype ReadonlyUser = Readonly<User>;\ntype OptionalUser = Optional<User>;\n\n// Custom mapped type\ntype Getters<T> = { [P in keyof T as `get${Capitalize<string & P>}`]: () => T[P] };\ninterface Product { name: string; price: number; }\n// type ProductGetters = Getters<Product>;\n// { getName: () => string; getPrice: () => number }"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Template Literal Types",
        "language": "typescript",
        "code": "type EventName = \"click\" | \"focus\" | \"blur\";\ntype HandlerName = `on${Capitalize<EventName>}`;\n// \"onClick\" | \"onFocus\" | \"onBlur\"\n\ntype CSSProperty = \"color\" | \"fontSize\";\ntype CSSValue = `${number}px` | `${number}%` | \"auto\";\ntype Style = Record<CSSProperty, CSSValue>;\n\n// Extract route params from a path\ntype ExtractRouteParams<T extends string> =\n  T extends `${string}:${infer Param}/${infer Rest}`\n    ? { [K in Param]: string } & ExtractRouteParams<Rest>\n    : T extends `${string}:${infer Param}`\n    ? { [K in Param]: string }\n    : {};\n\ntype Params = ExtractRouteParams<\"/users/:id/posts/:postId\">;\n// { id: string; postId: string }"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Branded Types (Nominal Typing)",
        "language": "typescript",
        "code": "// Prevent mixing up structurally identical types\ntype Brand<T, B> = T & { __brand: B };\n\ntype UserId = Brand<number, \"UserId\">;\ntype OrderId = Brand<number, \"OrderId\">;\n\nfunction getUser(id: UserId): string { return \"Alice\"; }\nfunction getOrder(id: OrderId): string { return \"Order #1\"; }\n\nconst userId = 1 as UserId;\nconst orderId = 2 as OrderId;\n\ngetUser(userId);   // OK\ngetUser(orderId);  // Error — different brand"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Null Safety"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Optional Chaining and Nullish Coalescing",
        "language": "typescript",
        "code": "interface User { address?: { city?: string }; }\nconst user: User = {};\n\n// Optional chaining — returns undefined instead of throwing\nconst city = user.address?.city;\nconst zip = user.address?.zipCode ?? \"00000\";\n\n// Nullish coalescing — only for null or undefined\nlet count: number | null = null;\nlet display = count ?? 0; // 0 (not falsy values like \"\" or false)\nlet display2 = count || 0; // 0 if count is null, \"\", 0, false, or NaN\n\n// Non-null assertion — tell TS the value is not null/undefined\nlet value: string | null = \"hello\";\nconsole.log(value!.toUpperCase());"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Classes"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Access Modifiers and Parameter Properties",
        "language": "typescript",
        "code": "class User {\n  // public is default, private hides from outside, protected allows subclass access\n  constructor(\n    public id: number,\n    private password: string,\n    protected role: string,\n  ) {}\n\n  public greet(): void {\n    console.log(`User ${this.id}`);\n  }\n}\n\nconst u = new User(1, \"secret\", \"admin\");\nconsole.log(u.id);       // OK\n// u.password;             // Error: private\n// u.role;                 // Error: protected"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Abstract Classes and readonly",
        "language": "typescript",
        "code": "abstract class Animal {\n  // readonly — cannot be changed after initialization\n  readonly species: string;\n  static count = 0;\n\n  constructor(species: string) {\n    this.species = species;\n    Animal.count++;\n  }\n\n  // Abstract method — must be implemented by subclass\n  abstract makeSound(): void;\n}\n\nclass Dog extends Animal {\n  makeSound() {\n    console.log(\"Woof\");\n  }\n}\n\nconsole.log(Animal.count); // 1"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Inheritance"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Extends, Super, and Polymorphism",
        "language": "typescript",
        "code": "class Animal {\n  constructor(protected name: string) {}\n  speak(): void {\n    console.log(`${this.name} makes a sound`);\n  }\n}\n\nclass Cat extends Animal {\n  constructor(name: string, public color: string) {\n    super(name);\n  }\n\n  speak(): void {\n    console.log(`${this.name} the ${this.color} cat meows`);\n  }\n}\n\nconst cat = new Cat(\"Luna\", \"black\");\ncat.speak();\n\n// Polymorphism\nconst animals: Animal[] = [cat, new Animal(\"Unknown\")];\nanimals.forEach(a => a.speak());\n\n// instanceof\nif (cat instanceof Cat) {\n  console.log(cat.color);\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Decorators"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Class and Method Decorators",
        "language": "typescript",
        "code": "function sealed(constructor: Function) {\n  Object.seal(constructor);\n  Object.seal(constructor.prototype);\n}\n\nfunction log(target: any, key: string, descriptor: PropertyDescriptor) {\n  const original = descriptor.value;\n  descriptor.value = function (...args: any[]) {\n    console.log(`Calling ${key} with`, args);\n    return original.apply(this, args);\n  };\n}\n\n@sealed\nclass Service {\n  @log\n  fetch(id: number) {\n    return \"data\";\n  }\n}\n\nconst svc = new Service();\nsvc.fetch(1);"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Decorator Factories",
        "language": "typescript",
        "code": "// Decorator factory — accepts configuration\nfunction validate(validator: (value: any) => boolean) {\n  return function (target: any, key: string) {\n    const privateKey = Symbol(key);\n\n    Object.defineProperty(target, key, {\n      get() { return (this as any)[privateKey]; },\n      set(value: any) {\n        if (!validator(value)) {\n          throw new Error(`Invalid ${key}: ${value}`);\n        }\n        (this as any)[privateKey] = value;\n      },\n    });\n  };\n}\n\nclass User {\n  @validate((v) => v > 0)\n  age: number = 0;\n}\n\nconst u = new User();\nu.age = 25;   // OK\n// u.age = -1; // Error: Invalid age: -1"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Modules"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "ES Module Syntax",
        "language": "typescript",
        "code": "// utils.ts\nfunction formatDate(date: Date): string {\n  return date.toISOString().split(\"T\")[0];\n}\n\nexport { formatDate };\nexport default class Logger {}\n\n// app.ts — importing\nimport Logger, { formatDate } from \"./utils.js\";\nimport type { User } from \"./types.js\"; // type-only import\n\n// Re-exporting\nexport { formatDate } from \"./utils.js\";\nexport * from \"./constants.js\";"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "React TypeScript"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "FC Type and Props Interface",
        "language": "jsx",
        "code": "import React from \"react\";\n\ninterface ButtonProps {\n  label: string;\n  onClick: () => void;\n  disabled?: boolean;\n}\n\n// React.FC — implicit children prop\nconst Button: React.FC<ButtonProps> = ({ label, onClick, disabled }) => {\n  return (\n    <button onClick={onClick} disabled={disabled}>\n      {label}\n    </button>\n  );\n};\n\n// Generic component\ntype ListProps<T> = {\n  items: T[];\n  renderItem: (item: T) => React.ReactNode;\n};\n\nfunction List<T>({ items, renderItem }: ListProps<T>) {\n  return <ul>{items.map(renderItem)}</ul>;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Hooks Typing (useState, useRef, useCallback)",
        "language": "jsx",
        "code": "import React, { useState, useRef, useCallback } from \"react\";\n\nfunction Counter() {\n  // useState — type inferred from initial value\n  const [count, setCount] = useState<number>(0);\n  const [name, setName] = useState(\"Guest\"); // inferred as string\n\n  // useRef — DOM element typing\n  const inputRef = useRef<HTMLInputElement>(null);\n\n  const handleClick = useCallback((id: number) => {\n    console.log(id);\n  }, []);\n\n  return (\n    <div>\n      <p>{count}</p>\n      <input ref={inputRef} />\n      <button onClick={() => handleClick(1)}>Click</button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Event Handlers",
        "language": "jsx",
        "code": "import React from \"react\";\n\nfunction Form() {\n  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {\n    console.log(e.target.value);\n  };\n\n  const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {\n    e.preventDefault();\n  };\n\n  const handleKey = (e: React.KeyboardEvent<HTMLInputElement>) => {\n    if (e.key === \"Enter\") console.log(\"Enter pressed\");\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input onChange={handleChange} onKeyDown={handleKey} />\n    </form>\n  );\n}\n\n// Children prop\ninterface BoxProps { children: React.ReactNode; }\nconst Box: React.FC<BoxProps> = ({ children }) => <div>{children}</div>;"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "DOM Manipulation"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Document and Element Types",
        "language": "typescript",
        "code": "// querySelector return types\nconst btn = document.querySelector(\"button\");\n// btn: HTMLButtonElement | null\n\nif (btn) {\n  btn.disabled = true;\n  btn.addEventListener(\"click\", (e: MouseEvent) => {\n    console.log(e.clientX, e.clientY);\n  });\n}\n\n// Specific element types\nconst input = document.getElementById(\"name\") as HTMLInputElement | null;\nconst canvas = document.querySelector(\"canvas\") as HTMLCanvasElement | null;\n\n// Keyboard event on input\ninput?.addEventListener(\"keydown\", (e: KeyboardEvent) => {\n  if (e.key === \"Enter\") console.log(\"Submitted\");\n});"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Type Guards"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "typeof, instanceof, and in Guards",
        "language": "typescript",
        "code": "// typeof — primitive types\nfunction print(value: string | number) {\n  if (typeof value === \"string\") {\n    console.log(value.toUpperCase());\n  } else {\n    console.log(value.toFixed(2));\n  }\n}\n\n// instanceof — class instances\nclass Cat { meow() {} }\nclass Dog { bark() {} }\nfunction speak(animal: Cat | Dog) {\n  if (animal instanceof Cat) {\n    animal.meow();\n  } else {\n    animal.bark();\n  }\n}\n\n// in operator\ntype Fish = { swim: () => void };\ntype Bird = { fly: () => void };\nfunction move(animal: Fish | Bird) {\n  if (\"swim\" in animal) {\n    animal.swim();\n  } else {\n    animal.fly();\n  }\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "User-Defined Type Guards",
        "language": "typescript",
        "code": "interface Cat { name: string; meow(): void; }\ninterface Dog { name: string; bark(): void; }\n\ntype Pet = Cat | Dog;\n\n// 'is' keyword narrows the type inside the if-block\nfunction isCat(pet: Pet): pet is Cat {\n  return (pet as Cat).meow !== undefined;\n}\n\nfunction play(pet: Pet) {\n  if (isCat(pet)) {\n    pet.meow(); // pet is Cat here\n  } else {\n    pet.bark(); // pet is Dog here\n  }\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Type Assertions"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Assertion Syntax and const Assertions",
        "language": "typescript",
        "code": "// as syntax (preferred)\nconst input = document.getElementById(\"name\") as HTMLInputElement;\n\n// Angle-bracket syntax (avoid in JSX)\nconst num = \"123\" as number;\n\n// const assertion — literal inference and readonly\ntype Color = \"red\" | \"green\" | \"blue\";\nconst palette = [\"red\", \"green\", \"blue\"] as const;\ntype PaletteType = typeof palette; // readonly [\"red\", \"green\", \"blue\"]\n\n// Non-null assertion — tells TS value is not null/undefined\nlet val: string | null = \"hi\";\nconsole.log(val!.length);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Enums"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Numeric, String, and Const Enums",
        "language": "typescript",
        "code": "// Numeric enum\nenum Direction { Up, Down, Left, Right }\nlet d: Direction = Direction.Up; // 0\n\n// String enum\nenum Status { Active = \"ACTIVE\", Inactive = \"INACTIVE\" }\nlet s: Status = Status.Active;\n\n// Heterogeneous enum\nenum Mixed { No = 0, Yes = \"YES\" }\n\n// Const enum — inlined at compile time, no runtime object\nconst enum Flavor { Vanilla, Chocolate, Strawberry }\nlet f: Flavor = Flavor.Vanilla;\n\n// Enum as const pattern for object literals\nconst COLORS = { Red: \"red\", Green: \"green\" } as const;\ntype Color = typeof COLORS[keyof typeof COLORS]; // \"red\" | \"green\""
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Iterators & Generators"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Generators and Iterables",
        "language": "typescript",
        "code": "// Generator function\nfunction* range(n: number): IterableIterator<number> {\n  for (let i = 0; i < n; i++) {\n    yield i;\n  }\n}\n\nconst gen = range(3);\nfor (const n of gen) {\n  console.log(n); // 0, 1, 2\n}\n\n// yield* — delegate to another iterable\nfunction* flatten<T>(iters: Iterable<T>[]): IterableIterator<T> {\n  for (const iter of iters) {\n    yield* iter;\n  }\n}\n\n// Custom iterator\nconst iterable = {\n  data: [10, 20, 30],\n  [Symbol.iterator]() {\n    let index = 0;\n    return {\n      next: () =>\n        index < this.data.length\n          ? { value: this.data[index++], done: false }\n          : { done: true },\n    };\n  },\n};\n\nfor (const v of iterable) console.log(v);"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Common Patterns"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Builder and Factory Patterns",
        "language": "typescript",
        "code": "// Builder pattern with types\nclass QueryBuilder {\n  private query = \"\";\n\n  select(fields: string[]) {\n    this.query += `SELECT ${fields.join(\", \")} `;\n    return this;\n  }\n\n  from(table: string) {\n    this.query += `FROM ${table} `;\n    return this;\n  }\n\n  where(condition: string) {\n    this.query += `WHERE ${condition}`;\n    return this;\n  }\n\n  build(): string {\n    return this.query.trim();\n  }\n}\n\nconst sql = new QueryBuilder()\n  .select([\"id\", \"name\"])\n  .from(\"users\")\n  .where(\"id = 1\")\n  .build();\n\n// Singleton with private constructor\nclass Database {\n  private static instance: Database;\n  private constructor() {}\n\n  static getInstance(): Database {\n    if (!Database.instance) {\n      Database.instance = new Database();\n    }\n    return Database.instance;\n  }\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "State Machine with Discriminated Union",
        "language": "typescript",
        "code": "type State =\n  | { status: \"idle\" }\n  | { status: \"loading\" }\n  | { status: \"success\"; data: string }\n  | { status: \"error\"; error: Error };\n\nfunction render(state: State) {\n  switch (state.status) {\n    case \"idle\":\n      return <p>Idle</p>;\n    case \"loading\":\n      return <p>Loading...</p>;\n    case \"success\":\n      return <p>Success: {state.data}</p>;\n    case \"error\":\n      return <p>Error: {state.error.message}</p>;\n    default:\n      // Exhaustive check — unhandled states cause a TS error\n      const _exhaustive: never = state;\n      return _exhaustive;\n  }\n}"
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}