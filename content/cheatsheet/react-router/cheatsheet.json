{
  "id": "lesson-cheatsheet-react-router",
  "slug": "cheatsheet",
  "moduleSlug": "react-router",
  "courseSlug": "cheatsheet",
  "title": "React Router Complete Cheat Sheet",
  "description": "Comprehensive quick reference for React Router v5 and v6: routing, navigation, parameters, nested routes, guards, data loading, and hooks.",
  "order": 10,
  "difficulty": "beginner",
  "estimatedMinutes": 35,
  "tags": ["react-router", "routing", "navigation", "url-params", "nested-routes", "route-guards", "v6", "cheatsheet", "quick-reference"],
  "blocks": [
    {
      "type": "example",
      "data": {
        "title": "Router Setup — BrowserRouter",
        "language": "jsx",
        "code": "import { BrowserRouter } from 'react-router-dom';\n\n// BrowserRouter uses the HTML5 history API (pushState, replaceState).\n// Wrap your app at the root level.\n\nfunction App() {\n  return (\n    <BrowserRouter>\n      <Routes>\n        <Route path=\"/\" element={<Home />} />\n      </Routes>\n    </BrowserRouter>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Router Setup — HashRouter",
        "language": "jsx",
        "code": "import { HashRouter } from 'react-router-dom';\n\n// HashRouter uses the hash portion of the URL (e.g. example.com/#/about).\n// Useful for static file hosts without server-side routing support.\n\nfunction App() {\n  return (\n    <HashRouter>\n      <Routes>\n        <Route path=\"/\" element={<Home />} />\n      </Routes>\n    </HashRouter>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Router Setup — MemoryRouter",
        "language": "jsx",
        "code": "import { MemoryRouter } from 'react-router-dom';\n\n// MemoryRouter keeps the history in memory (no URL changes).\n// Great for tests, storybooks, and non-browser environments.\n\n<MemoryRouter initialEntries={['/home']} initialIndex={0}>\n  <App />\n</MemoryRouter>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Router Setup — basename",
        "language": "jsx",
        "code": "// Set a basename if your app is served from a subdirectory.\n\n<BrowserRouter basename=\"/app\">\n  <Routes>\n    <Route path=\"/dashboard\" element={<Dashboard />} />\n  </Routes>\n</BrowserRouter>\n\n// URL: /app/dashboard"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Routes (v6) — Routes Component",
        "language": "jsx",
        "code": "import { Routes, Route } from 'react-router-dom';\n\nfunction App() {\n  return (\n    <Routes>\n      <Route path=\"/\" element={<Home />} />\n      <Route path=\"/about\" element={<About />} />\n      <Route path=\"/users/:id\" element={<UserProfile />} />\n    </Routes>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Routes (v6) — Route element Prop",
        "language": "jsx",
        "code": "// In v6, use element prop to render a React element.\n// No more component prop or render prop.\n\n<Route path=\"/about\" element={<About />} />\n\n// You can also pass an inline element\n<Route path=\"/hello\" element={<h1>Hello</h1>} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Routes (v6) — Index Route",
        "language": "jsx",
        "code": "// Render an index route at the parent's URL using index prop.\n\n<Route path=\"/\" element={<Layout />}>\n  <Route index element={<Home />} />\n  <Route path=\"about\" element={<About />} />\n</Route>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Routes (v6) — path=\"*\" Wildcard",
        "language": "jsx",
        "code": "// Use path=\"*\" as a catch-all route (404 page).\n\n<Routes>\n  <Route path=\"/\" element={<Home />} />\n  <Route path=\"/about\" element={<About />} />\n  <Route path=\"*\" element={<NotFound />} />\n</Routes>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Routes (v5) — Switch Component",
        "language": "jsx",
        "code": "// In v5, Switch renders the first child Route that matches.\n\nimport { Switch, Route } from 'react-router-dom';\n\n<Switch>\n  <Route exact path=\"/\" component={Home} />\n  <Route path=\"/about\" component={About} />\n  <Route component={NotFound} />\n</Switch>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Basic Routes (v5) — exact Prop",
        "language": "jsx",
        "code": "// In v5, Route is not exact by default. Use exact to match only the exact path.\n\n<Route exact path=\"/\" component={Home} />\n<Route path=\"/\" component={About} />\n\n// Without exact, both routes render at '/'.\n// With exact, only Home renders at '/'."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Route Parameters — useParams Hook",
        "language": "jsx",
        "code": "import { useParams } from 'react-router-dom';\n\nfunction UserProfile() {\n  const { id } = useParams();\n  // id is a string extracted from the URL\n  return <div>User ID: {id}</div>;\n}\n\n// Route definition\n<Route path=\"/users/:id\" element={<UserProfile />} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Route Parameters — Multiple Params",
        "language": "jsx",
        "code": "function BlogPost() {\n  const { year, month, slug } = useParams();\n  return (\n    <div>\n      <h1>{slug}</h1>\n      <p>Published: {year}/{month}</p>\n    </div>\n  );\n}\n\n<Route path=\"/blog/:year/:month/:slug\" element={<BlogPost />} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Route Parameters — Optional Params",
        "language": "jsx",
        "code": "// Optional parameters are achieved with multiple routes or regex.\n\n// Option 1: multiple routes\n<Route path=\"/product/:id?\" element={<Product />} />\n\n// Option 2: optional path segment\n<Route path=\"/:lang?/products\" element={<Products />} />\n\nfunction Product() {\n  const { id } = useParams();\n  // id will be undefined if not provided\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Query Parameters — useSearchParams Hook",
        "language": "jsx",
        "code": "import { useSearchParams } from 'react-router-dom';\n\nfunction SearchPage() {\n  const [searchParams, setSearchParams] = useSearchParams();\n  const query = searchParams.get('q');\n  const page = Number(searchParams.get('page')) || 1;\n\n  return (\n    <div>\n      <p>Query: {query}, Page: {page}</p>\n      <button onClick={() => setSearchParams({ q: 'redux', page: '2' })}>\n        Go to page 2\n      </button>\n    </div>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Navigation — Link Component",
        "language": "jsx",
        "code": "import { Link } from 'react-router-dom';\n\n// Link renders an anchor tag with the correct href.\n// It prevents full page reloads.\n\n<Link to=\"/about\">About</Link>\n<Link to=\"/users/1\" replace>User 1 (no history entry)</Link>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Navigation — NavLink with Active Styling",
        "language": "jsx",
        "code": "import { NavLink } from 'react-router-dom';\n\n// NavLink knows if it is active and can apply styles.\n\n<NavLink\n  to=\"/dashboard\"\n  className={({ isActive }) => isActive ? 'active' : ''}\n  style={({ isActive }) => ({ color: isActive ? 'red' : 'black' })}\n>\n  Dashboard\n</NavLink>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Navigation — NavLink end Prop",
        "language": "jsx",
        "code": "// Use end prop so that /dashboard does not match /dashboard/settings.\n\n<NavLink end to=\"/dashboard\" className={({ isActive }) => isActive ? 'active' : ''}>\n  Dashboard\n</NavLink>\n\n// Without end, the link would be active at both /dashboard and /dashboard/settings."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Navigation — useNavigate Hook",
        "language": "jsx",
        "code": "import { useNavigate } from 'react-router-dom';\n\nfunction LoginForm() {\n  const navigate = useNavigate();\n\n  const handleSubmit = () => {\n    // Navigate to a new URL\n    navigate('/dashboard');\n    // Replace current entry\n    navigate('/dashboard', { replace: true });\n    // Go back\n    navigate(-1);\n  };\n\n  return <button onClick={handleSubmit}>Login</button>;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Navigation — useNavigate with State",
        "language": "jsx",
        "code": "// Pass state with navigation, accessible via useLocation.\n\nnavigate('/success', { state: { orderId: 123 } });\n\n// In the target component\nconst location = useLocation();\nconst orderId = location.state?.orderId;"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nested Routes — Outlet Component",
        "language": "jsx",
        "code": "import { Outlet } from 'react-router-dom';\n\nfunction Dashboard() {\n  return (\n    <div>\n      <h1>Dashboard</h1>\n      <nav>\n        <Link to=\"stats\">Stats</Link>\n        <Link to=\"settings\">Settings</Link>\n      </nav>\n      {/* Child routes render here */}\n      <Outlet />\n    </div>\n  );\n}\n\n<Route path=\"/dashboard\" element={<Dashboard />}>\n  <Route path=\"stats\" element={<Stats />} />\n  <Route path=\"settings\" element={<Settings />} />\n</Route>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Nested Routes — Relative Paths",
        "language": "jsx",
        "code": "// Inside nested routes, use relative paths.\n\n<Route path=\"/dashboard\" element={<Dashboard />}>\n  <Route path=\"stats\" element={<Stats />} />\n  <Route path=\"settings\" element={<Settings />} />\n</Route>\n\n// Link to relative path\n<Link to=\"stats\">Stats</Link> // resolves to /dashboard/stats\n<Link to=\"..\">Back to root</Link> // resolves to /"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Programmatic Navigation — After Form Submit",
        "language": "jsx",
        "code": "function CheckoutForm() {\n  const navigate = useNavigate();\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    // process payment...\n    navigate('/order-confirmation', { replace: true });\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <button type=\"submit\">Pay Now</button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Route Guards — RequireAuth Wrapper",
        "language": "jsx",
        "code": "import { Navigate, useLocation } from 'react-router-dom';\n\nfunction RequireAuth({ children }) {\n  const isAuthenticated = false; // replace with real auth\n  const location = useLocation();\n\n  if (!isAuthenticated) {\n    return <Navigate to=\"/login\" state={{ from: location }} replace />;\n  }\n\n  return children;\n}\n\n// Usage\n<Route path=\"/dashboard\" element={\n  <RequireAuth><Dashboard /></RequireAuth>\n} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Route Guards — Redirecting to Login",
        "language": "jsx",
        "code": "function LoginPage() {\n  const navigate = useNavigate();\n  const location = useLocation();\n\n  // After successful login, redirect back to the page they tried to access\n  const from = location.state?.from?.pathname || '/';\n\n  const handleLogin = () => {\n    // authenticate user...\n    navigate(from, { replace: true });\n  };\n\n  return <button onClick={handleLogin}>Log In</button>;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Loading & Error Handling — useNavigation (v6.4+)",
        "language": "jsx",
        "code": "import { useNavigation } from 'react-router-dom';\n\nfunction AddPost() {\n  const navigation = useNavigation();\n  const isSubmitting = navigation.state === 'submitting';\n\n  return (\n    <form method=\"post\">\n      <input name=\"title\" />\n      <button type=\"submit\" disabled={isSubmitting}>\n        {isSubmitting ? 'Saving...' : 'Save'}\n      </button>\n    </form>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Loading & Error Handling — ErrorElement",
        "language": "jsx",
        "code": "import { useRouteError, isRouteErrorResponse } from 'react-router-dom';\n\nfunction ErrorBoundary() {\n  const error = useRouteError();\n\n  if (isRouteErrorResponse(error)) {\n    return (\n      <div>\n        <h1>{error.status} {error.statusText}</h1>\n        <p>{error.data}</p>\n      </div>\n    );\n  }\n\n  return <h1>Something went wrong!</h1>;\n}\n\n<Route path=\"/about\" element={<About />} errorElement={<ErrorBoundary />} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Data Loading (v6.4+) — Loader Function",
        "language": "jsx",
        "code": "import { json } from 'react-router-dom';\n\nconst loader = async ({ params }) => {\n  const res = await fetch(`/api/posts/${params.id}`);\n  if (!res.ok) throw new Response('Not Found', { status: 404 });\n  return json(await res.json());\n};\n\nfunction Post() {\n  const post = useLoaderData();\n  return <h1>{post.title}</h1>;\n}\n\n<Route path=\"/posts/:id\" element={<Post />} loader={loader} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Data Mutation (v6.4+) — Action Function",
        "language": "jsx",
        "code": "import { redirect } from 'react-router-dom';\n\nconst action = async ({ request }) => {\n  const formData = await request.formData();\n  const post = {\n    title: formData.get('title'),\n    body: formData.get('body')\n  };\n  await fetch('/api/posts', {\n    method: 'POST',\n    body: JSON.stringify(post)\n  });\n  return redirect('/posts');\n};\n\n<Route path=\"/new-post\" element={<NewPost />} action={action} />"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Lazy Routes — lazy() and Suspense",
        "language": "jsx",
        "code": "import { lazy, Suspense } from 'react';\n\nconst Dashboard = lazy(() => import('./pages/Dashboard'));\nconst Settings = lazy(() => import('./pages/Settings'));\n\nfunction App() {\n  return (\n    <Suspense fallback={<div>Loading...</div>}>\n      <Routes>\n        <Route path=\"/dashboard\" element={<Dashboard />} />\n        <Route path=\"/settings\" element={<Settings />} />\n      </Routes>\n    </Suspense>\n  );\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Dynamic Routes — Catch-All Routes",
        "language": "jsx",
        "code": "// path=\"*\" matches anything not already matched.\n\n<Routes>\n  <Route path=\"/\" element={<Home />} />\n  <Route path=\"/about\" element={<About />} />\n  <Route path=\"*\" element={<NotFound />} />\n</Routes>\n\n// Optional catch-all: path=\"/*\" matches /files and /files/docs"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Scroll Restoration — useScrollRestoration",
        "language": "jsx",
        "code": "import { useScrollRestoration } from 'react-router-dom';\n\nfunction BlogPost() {\n  useScrollRestoration({\n    // Scroll to top when navigating to this page\n    getKey: () => location.key\n  });\n\n  return <article>Long content...</article>;\n}"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Relative Links — relative=\"path\" vs relative=\"route\"",
        "language": "jsx",
        "code": "// Relative links resolve differently based on the relative prop.\n\n<Link to=\"settings\" relative=\"path\">\n  {/* Appends to the current path */}\n</Link>\n\n<Link to=\"settings\" relative=\"route\">\n  {/* Relative to the current route path */}\n</Link>"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Context Integration — Router State",
        "language": "jsx",
        "code": "// useLocation gives access to the current location object.\n\nimport { useLocation } from 'react-router-dom';\n\nfunction ScrollToTop() {\n  const { pathname } = useLocation();\n\n  useEffect(() => {\n    window.scrollTo(0, 0);\n  }, [pathname]);\n\n  return null;\n}\n\n// Place inside Routes to listen for navigation."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "v5 to v6 Migration — Key Changes",
        "language": "jsx",
        "code": "// Migration checklist:\n// 1. Switch -> Routes\n// 2. component -> element\n// 3. exact prop removed (Routes are exact by default)\n// 4. useHistory -> useNavigate\n// 5. useRouteMatch -> useMatch, useParams, useLocation\n\n// v5\n<Switch>\n  <Route exact path=\"/\" component={Home} />\n</Switch>\n\n// v6\n<Routes>\n  <Route path=\"/\" element={<Home />} />\n</Routes>"
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
