{
  "id": "lesson-sd-react-03",
  "slug": "react-ssr-ssg-hybrid",
  "moduleSlug": "react-system-design",
  "courseSlug": "system-design",
  "title": "Design SSR/SSG Hybrid Rendering Strategy",
  "description": "Choose between SSR, SSG, and CSR for different pages in a large React app.",
  "order": 3,
  "difficulty": "advanced",
  "estimatedMinutes": 25,
  "tags": ["react", "ssr", "ssg", "nextjs", "rendering"],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Problem Statement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Your marketing site needs SEO, your dashboard needs interactivity, and your docs need fast navigation. Choose the right rendering mode per route."
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Next.js App Router Strategy",
        "language": "typescript",
        "code": "// app/layout.tsx - SSR for personalized shell\nexport default async function RootLayout({ children }) {\n  const session = await getServerSession();\n  return (\n    <html>\n      <body>\n        <Header user={session?.user} />\n        {children}\n      </body>\n    </html>\n  );\n}\n\n// app/products/[slug]/page.tsx - SSG for marketing pages\nexport async function generateStaticParams() {\n  const products = await db.product.findMany();\n  return products.map((p) => ({ slug: p.slug }));\n}"
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
