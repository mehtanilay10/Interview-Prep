{
  "id": "lesson-full-stack-senior-projects-project-2-collaborative-communication-platform-offline-handling-and-reconnect-behavior",
  "slug": "offline-handling-and-reconnect-behavior",
  "moduleSlug": "project-2-collaborative-communication-platform",
  "courseSlug": "full-stack-senior-projects",
  "title": "Offline handling and reconnect behavior",
  "description": "Project: Offline handling and reconnect behavior. Build and understand this project with step-by-step guidance.",
  "order": 6,
  "difficulty": "advanced",
  "estimatedMinutes": 15,
  "tags": [],
  "blocks": [
    {
      "type": "paragraph",
      "data": {
        "text": "Users will lose connectivity. They will walk into an elevator, board a subway, or open the app on an airplane. An offline-capable communication platform does not just queue messages; it reconciles state, handles conflicts, and resumes seamlessly when connectivity returns. This lesson covers the patterns that make offline behavior feel like an edge case rather than a crash."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Key Concepts"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Detect offline state using the browser navigator.onLine API combined with WebSocket close events",
          "Queue outbound messages in IndexedDB or localStorage while the connection is down",
          "Flush the queue on reconnect in the order messages were created, not the order they were queued by retry timers",
          "Handle message deduplication on the server using idempotency keys so retried messages do not create duplicates",
          "Show an offline indicator in the UI only after confirming the transport is unavailable, not just on navigator.onLine",
          "Handle the long tail of slow networks: timeouts should be generous on initial connection but aggressive on retries",
          "Reconcile local state with server state after reconnect to avoid showing stale data as current"
        ]
      }
    },
    {
      "type": "key-terms",
      "data": {
        "terms": [
          {
            "term": "Outbox pattern",
            "definition": "Storing outgoing operations in a local outbox before attempting to send them, ensuring no message is lost during disconnection."
          },
          {
            "term": "IndexedDB",
            "definition": "A low-level browser API for storing significant amounts of structured data, including files and blobs, with transaction support."
          },
          {
            "term": "Idempotency key",
            "definition": "A unique identifier generated by the client for each message, allowing the server to safely process retries without creating duplicates."
          },
          {
            "term": "Reconciliation",
            "definition": "The process of comparing local state with server state after a disruption and merging changes without data loss."
          },
          {
            "term": "Network Information API",
            "definition": "A browser API that provides information about the user's network connection, including effective type and round-trip time estimates."
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Why This Matters"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Users judge the quality of a communication platform by what happens when they lose connectivity. If the app crashes, loses messages, or shows stale data after reconnect, trust erodes quickly. Senior engineers design the offline experience intentionally: they show clear status, queue work safely, and reconcile state deterministically so the app recovers as if nothing happened."
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "warning",
        "title": "Test offline behavior before launch",
        "text": "Use Chrome DevTools Network tab to simulate offline and slow-3G conditions. Use the Application tab to inspect IndexedDB entries while offline. Do not rely on airplane mode alone because OS-level behavior varies by platform and browser."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Offline detection and queue design"
      }
    },
    {
      "type": "numbered-list",
      "data": {
        "items": [
          "Listen to window online and offline events as a primary signal",
          "Treat WebSocket close with code 1006 as a secondary offline signal",
          "Start the outbox queue immediately when either signal indicates unavailability",
          "Assign a client-generated UUID to every queued message before storing it",
          "Store messages with createdAt, idempotencyKey, payload, and retryCount",
          "On reconnect, sort the queue by createdAt and send in order, not by insertion order if retries reordered them",
          "Remove messages from the queue only after receiving a server acknowledgment"
        ]
      }
    },
    {
      "type": "mermaid",
      "data": {
        "id": "mermaid-offline-reconnect",
        "caption": "Offline queue and reconnect reconciliation flow",
        "definition": "graph TD\n    A[User sends message] --> B{Online?}\n    B -->|Yes| C[Send via WebSocket]\n    B -->|No| D[Store in IndexedDB outbox]\n    D --> E[Show queued indicator]\n    C --> F{Server ACK?}\n    F -->|Yes| G[Confirm in UI]\n    F -->|No| D\n    H[Network restored] --> I[Flush outbox in order]\n    I --> J[Server processes with idempotency key]\n    J --> K[Remove from outbox]\n    K --> L[Sync missed messages]\n    L --> M[Reconcile local state]"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Conflict resolution strategies"
      }
    },
    {
      "type": "bullet-list",
      "data": {
        "items": [
          "Last-write-wins: use server-assigned timestamps and overwrite local state with newer server state",
          "Operational transforms: not necessary for simple chat; use only for collaborative editing",
          "Vector clocks: complex but precise; use when multiple users can edit the same message",
          "Merge on reconnect: download all messages created since the last sync and insert missing ones by ID"
        ]
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Local storage options",
        "cards": [
          {
            "title": "localStorage",
            "description": "Synchronous key-value store limited to about five megabytes.",
            "pros": ["Simple API", "Synchronous reads", "Widely supported"],
            "cons": ["Small quota", "Blocks main thread", "No transactions"]
          },
          {
            "title": "IndexedDB",
            "description": "Asynchronous transactional database with large storage quotas.",
            "pros": ["Large quota", "Async API", "Structured storage"],
            "cons": ["Verbose API", "Promise-based", "Requires wrapper library for ergonomics"]
          },
          {
            "title": "Cache API (Service Worker)",
            "description": "HTTP cache designed for offline request/response pairs.",
            "pros": ["Built for offline", "Works with fetch events", "Cache-first strategies"],
            "cons": ["Not ideal for message state", "Requires service worker setup"]
          }
        ]
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Exercise"
      }
    },
    {
      "type": "exercise",
      "data": {
        "title": "Implement an offline message outbox",
        "description": "Build an outbox pattern that queues messages in IndexedDB while offline, flushes them in order on reconnect, and handles idempotent server acknowledgment.",
        "steps": [
          "Create an IndexedDB database with an outbox object store keyed by client-generated UUID",
          "Implement a queue manager that intercepts send calls when the socket is disconnected",
          "On reconnect, read all outbox entries sorted by createdAt",
          "Send each message with its idempotency key and remove it only after server ACK",
          "Show the user a badge with the number of queued messages",
          "Handle the edge case where a message was sent but the ACK was lost: deduplicate on the server"
        ],
        "expectedOutcome": "You should be able to write messages while offline, see them queue visually, and have them appear in the conversation after reconnect without duplicates."
      }
    },
    {
      "type": "faq-block",
      "data": {
        "title": "Frequently asked questions",
        "items": [
          {
            "question": "Should I show an offline banner immediately?",
            "answer": "Wait until the WebSocket close event confirms the transport is down. navigator.onLine can be true while the WebSocket is broken, and showing a banner too early creates false alarms."
          },
          {
            "question": "How big should the outbox queue be?",
            "answer": "Set a reasonable limit, such as five hundred messages. If the user exceeds it, show a warning and offer to archive older drafts. Unbounded queues can exhaust storage on low-end devices."
          },
          {
            "question": "What if the server receives messages out of order?",
            "answer": "The server should reorder by client-assigned createdAt or a server-assigned sequence number. If the gap is too large, request a full sync from the client."
          }
        ]
      }
    },
    {
      "type": "summary-box",
      "data": {
        "title": "Summary",
        "points": [
          "Detect offline using both browser events and WebSocket close codes",
          "Queue messages in IndexedDB with idempotency keys",
          "Flush in order on reconnect and reconcile state with the server",
          "Use last-write-wins for simple chat, operational transforms only for collaborative editing"
        ],
        "takeaway": "Offline handling is a first-class feature, not a nice-to-have. Users should be able to read and write even when the network is gone."
      }
    }
  ],
  "relatedLessons": [
    "websocket-connection-lifecycle",
    "message-persistence-and-ordering"
  ],
  "furtherReading": [
    {
      "title": "Offline-First Architecture",
      "url": "https://web.dev/offline-first/",
      "type": "article",
      "author": "web.dev",
      "description": "Patterns for building applications that work offline and synchronize when connectivity returns."
    }
  ]
}
