{
  "id": "lesson-problems-treegraphproblems-22",
  "slug": "all-nodes-distance-k-in-binary-tree",
  "moduleSlug": "tree-graph-problems",
  "courseSlug": "problems",
  "title": "All Nodes Distance K in Binary Tree",
  "description": "Find all nodes at distance K from target.",
  "order": 22,
  "difficulty": "medium",
  "estimatedMinutes": 16,
  "tags": [
    "csharp",
    "trees",
    "bfs",
    "dfs",
    "medium"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Problem Statement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Given a binary tree, target node, and k, return all nodes at distance k."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach: Build Parent Map + BFS"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "O(n)",
        "language": "csharp",
        "code": "public IList<int> DistanceK(TreeNode root, TreeNode target, int k)\n{\n    var parent = new Dictionary<TreeNode, TreeNode?>();\n    BuildParent(root, null, parent);\n    var queue = new Queue<TreeNode?>();\n    queue.Enqueue(target);\n    var visited = new HashSet<TreeNode> { target };\n    int dist = 0;\n    while (queue.Count > 0)\n    {\n        int size = queue.Count;\n        if (dist == k) return queue.Where(n => n != null).Select(n => n.Val).Cast<int>().ToList();\n        for (int i = 0; i < size; i++)\n        {\n            TreeNode? node = queue.Dequeue();\n            if (node == null) continue;\n            foreach (var neighbor in new TreeNode?[] { node.Left, node.Right, parent.GetValueOrDefault(node) })\n            {\n                if (neighbor != null && visited.Add(neighbor))\n                    queue.Enqueue(neighbor);\n            }\n        }\n        dist++;\n    }\n    return new List<int>();\n}\n\nprivate void BuildParent(TreeNode? node, TreeNode? p, Dictionary<TreeNode, TreeNode?> parent)\n{\n    if (node == null) return;\n    parent[node] = p;\n    BuildParent(node.Left, node, parent);\n    BuildParent(node.Right, node, parent);\n}"
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Approach Comparison",
        "cards": [
          {
            "title": "Parent Map + BFS",
            "description": "Treat tree as graph, BFS from target",
            "pros": [
              "O(n) time"
            ],
            "tags": [
              "O(n) time",
              "O(n) space",
              "Recommended"
            ]
          }
        ]
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}