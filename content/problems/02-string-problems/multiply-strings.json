{
  "id": "lesson-problems-arraystringproblems-18",
  "slug": "multiply-strings",
  "moduleSlug": "string-problems",
  "courseSlug": "problems",
  "title": "Multiply Strings",
  "description": "Multiply two non-negative integers represented as strings using digit arrays.",
  "order": 18,
  "difficulty": "medium",
  "estimatedMinutes": 15,
  "tags": [
    "csharp",
    "strings",
    "medium"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Problem Statement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Given two non-negative integers `num1` and `num2` as strings, return the product of `num1` and `num2` as a string without using built-in big-integer libraries."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Input:** `num1 = \"2\", num2 = \"3\"`\n\n**Output:** `\"6\"`"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach 1: Repeated Addition (Simulation)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Add in a loop O(n * m)",
        "language": "csharp",
        "code": "public string MultiplyAddition(string num1, string num2)\n{\n    if (num1 == \"0\" || num2 == \"0\") return \"0\";\n    string result = \"0\";\n    for (int i = num2.Length - 1; i >= 0; i--)\n    {\n        int digit = num2[i] - '0';\n        string addend = num1;\n        for (int k = 0; k < digit; k++)\n            result = AddStrings(result, addend);\n    }\n    return result;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach 2: Grade-School Digit Array"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "int[] product O(n * m)",
        "language": "csharp",
        "code": "public string Multiply(string num1, string num2)\n{\n    if (num1 == \"0\" || num2 == \"0\") return \"0\";\n    int n = num1.Length, m = num2.Length;\n    int[] pos = new int[n + m];\n    for (int i = n - 1; i >= 0; i--)\n    {\n        for (int j = m - 1; j >= 0; j--)\n        {\n            int mul = (num1[i] - '0') * (num2[j] - '0');\n            int p1 = i + j, p2 = i + j + 1;\n            int sum = mul + pos[p2];\n            pos[p2] = sum % 10;\n            pos[p1] += sum / 10;\n        }\n    }\n    var sb = new StringBuilder();\n    foreach (var d in pos) if (sb.Length > 0 || d != 0) sb.Append(d);\n    return sb.ToString();\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "What improved?",
        "text": "The digit array captures each place-value multiplication once. Instead of adding `num1` repeatedly, we place partial products directly into the right positions."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Approach Comparison",
        "cards": [
          {
            "title": "Repeated Addition",
            "description": "Add num1 for each digit",
            "pros": [
              "Simple concept"
            ],
            "cons": [
              "O(n * m * maxDigit)",
              "Very slow for large inputs"
            ],
            "tags": [
              "O(n * m * maxDigit) time",
              "O(max(n,m)) space"
            ]
          },
          {
            "title": "Digit Array",
            "description": "Grade-school multiplication",
            "pros": [
              "O(n * m)",
              "No repeated adds"
            ],
            "cons": [
              "Carry handling is tricky"
            ],
            "tags": [
              "O(n * m) time",
              "O(n + m) space",
              "Recommended"
            ]
          }
        ]
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
