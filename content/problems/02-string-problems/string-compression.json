{
  "id": "lesson-problems-arraystringproblems-12",
  "slug": "string-compression",
  "moduleSlug": "string-problems",
  "courseSlug": "csharp-problems",
  "title": "String Compression",
  "description": "Compress a string by replacing consecutive repeated characters with count.",
  "order": 12,
  "difficulty": "easy",
  "estimatedMinutes": 10,
  "tags": [
    "csharp",
    "strings",
    "easy"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Problem Statement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Compress a string by replacing sequences of the same character with the character followed by the count of consecutive occurrences. Return the compressed string."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Input:** `\"aabcccccaaa\"`\n\n**Output:** `\"a2b1c5a3\"`"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach 1: StringBuilder Append"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "SB O(n)",
        "language": "csharp",
        "code": "public string CompressSB(string s)\n{\n    if (string.IsNullOrEmpty(s)) return s;\n    var sb = new StringBuilder();\n    int count = 1;\n    for (int i = 1; i < s.Length; i++)\n    {\n        if (s[i] == s[i - 1]) count++;\n        else\n        {\n            sb.Append(s[i - 1]).Append(count);\n            count = 1;\n        }\n    }\n    sb.Append(s[^1]).Append(count);\n    return sb.ToString();\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach 2: Span<char> (Best)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Span O(n) fewer allocations",
        "language": "csharp",
        "code": "public string Compress(string s)\n{\n    if (string.IsNullOrEmpty(s)) return s;\n    var sb = new StringBuilder(s.Length * 2);\n    int count = 1;\n    for (int i = 1; i < s.Length; i++)\n    {\n        if (s[i] == s[i - 1]) count++;\n        else\n        {\n            sb.Append(s[i - 1]).Append(count);\n            count = 1;\n        }\n    }\n    sb.Append(s[^1]).Append(count);\n    return sb.ToString();\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "What improved?",
        "text": "Pre-sizing `StringBuilder` avoids internal buffer resizing. The algorithm itself is the same; the win is fewer allocations and copies."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Approach Comparison",
        "cards": [
          {
            "title": "StringBuilder Default",
            "description": "Standard SB usage",
            "pros": [
              "Simple",
              "Standard pattern"
            ],
            "cons": [
              "May resize buffer"
            ],
            "tags": [
              "O(n) time",
              "O(n) space"
            ]
          },
          {
            "title": "Pre-sized StringBuilder",
            "description": "Set initial capacity",
            "pros": [
              "No buffer resize",
              "Slightly faster"
            ],
            "cons": [
              "Slightly more code"
            ],
            "tags": [
              "O(n) time",
              "O(n) space",
              "Recommended"
            ]
          }
        ]
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
