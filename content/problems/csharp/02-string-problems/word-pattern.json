{
  "id": "lesson-problems-arraystringproblems-23",
  "slug": "word-pattern",
  "moduleSlug": "string-problems",
  "courseSlug": "csharp-problems",
  "title": "Word Pattern",
  "description": "Check if a string follows a pattern of word-to-character mappings.",
  "order": 23,
  "difficulty": "easy",
  "estimatedMinutes": 10,
  "tags": [
    "csharp",
    "strings",
    "hashmap",
    "easy"
  ],
  "blocks": [
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Problem Statement"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "Given a `pattern` and a string `s`, find if `s` follows the same pattern. Each letter in `pattern` maps to a non-empty word in `s`, and no two letters map to the same word."
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 3,
        "text": "Example"
      }
    },
    {
      "type": "paragraph",
      "data": {
        "text": "**Input:** `pattern = \"abba\", s = \"dog cat cat dog\"`\n\n**Output:** `true`"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach 1: Two Dictionaries"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Bidirectional map O(n)",
        "language": "csharp",
        "code": "public bool WordPatternTwoMaps(string pattern, string s)\n{\n    var words = s.Split(' ');\n    if (words.Length != pattern.Length) return false;\n    var p2w = new Dictionary<char, string>();\n    var w2p = new Dictionary<string, char>();\n    for (int i = 0; i < pattern.Length; i++)\n    {\n        char c = pattern[i]; string w = words[i];\n        if (p2w.ContainsKey(c) && p2w[c] != w) return false;\n        if (w2p.ContainsKey(w) && w2p[w] != c) return false;\n        p2w[c] = w; w2p[w] = c;\n    }\n    return true;\n}"
      }
    },
    {
      "type": "heading",
      "data": {
        "level": 2,
        "text": "Approach 2: Index Tracking (Best)"
      }
    },
    {
      "type": "example",
      "data": {
        "title": "Last-seen index O(n)",
        "language": "csharp",
        "code": "public bool WordPattern(string pattern, string s)\n{\n    var words = s.Split(' ');\n    if (words.Length != pattern.Length) return false;\n    var p2i = new Dictionary<char, int>();\n    var w2i = new Dictionary<string, int>();\n    for (int i = 0; i < pattern.Length; i++)\n    {\n        if (!p2i.ContainsKey(pattern[i])) p2i[pattern[i]] = i;\n        if (!w2i.ContainsKey(words[i])) w2i[words[i]] = i;\n        if (p2i[pattern[i]] != w2i[words[i]]) return false;\n    }\n    return true;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "What improved?",
        "text": "Storing the last-seen index for each pattern char and word lets us check consistency in one pass without storing full strings in the dictionary."
      }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Approach Comparison",
        "cards": [
          {
            "title": "Two Dictionaries",
            "description": "Map char to word and back",
            "pros": [
              "Clear bidirectional check"
            ],
            "cons": [
              "Stores strings",
              "Slightly more memory"
            ],
            "tags": [
              "O(n) time",
              "O(n) space"
            ]
          },
          {
            "title": "Index Tracking",
            "description": "Store last-seen index",
            "pros": [
              "O(n) time",
              "Minimal storage"
            ],
            "cons": [
              "Less intuitive at first"
            ],
            "tags": [
              "O(n) time",
              "O(n) space",
              "Recommended"
            ]
          }
        ]
      }
    }
  ],
  "relatedLessons": [],
  "furtherReading": []
}
