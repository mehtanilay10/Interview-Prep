{
  "id": "lesson-problems-binary-search",
  "slug": "binary-search",
  "moduleSlug": "sorting-searching-problems",
  "courseSlug": "problems",
  "title": "Binary Search",
  "description": "Implement binary search on a sorted array. Compare recursive and iterative approaches, and understand the loop invariant.",
  "order": 1,
  "difficulty": "beginner",
  "estimatedMinutes": 12,
  "tags": ["csharp", "binary-search", "divide-and-conquer", "arrays"],
  "blocks": [
    {
      "type": "heading",
      "data": { "level": 2, "text": "Problem Statement" }
    },
    {
      "type": "paragraph",
      "data": { "text": "Given a sorted array of integers `nums` and an integer `target`, write a function to search `target` in `nums`. If `target` exists, return its index. Otherwise, return `-1`." }
    },
    {
      "type": "paragraph",
      "data": { "text": "You must write an algorithm with O(log n) runtime complexity." }
    },
    {
      "type": "heading",
      "data": { "level": 3, "text": "Example" }
    },
    {
      "type": "paragraph",
      "data": { "text": "**Input:** `nums = [-1, 0, 3, 5, 9, 12]`, `target = 9`\n\n**Output:** `4`\n\n**Explanation:** 9 exists in `nums` and its index is 4." }
    },
    {
      "type": "heading",
      "data": { "level": 2, "text": "Approach 1: Iterative Binary Search" }
    },
    {
      "type": "paragraph",
      "data": { "text": "Maintain a search window `[left, right]`. At each step, compare the middle element to the target and narrow the window." }
    },
    {
      "type": "example",
      "data": {
        "title": "Iterative O(log n) — Standard",
        "language": "csharp",
        "code": "public int BinarySearch(int[] nums, int target)\n{\n    int left = 0;\n    int right = nums.Length - 1;\n\n    while (left <= right)\n    {\n        // Prevent integer overflow for large arrays\n        int mid = left + (right - left) / 2;\n\n        if (nums[mid] == target)\n        {\n            return mid;\n        }\n        else if (nums[mid] < target)\n        {\n            left = mid + 1; // target is in right half\n        }\n        else\n        {\n            right = mid - 1; // target is in left half\n        }\n    }\n\n    return -1;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "tip",
        "title": "Loop invariant",
        "text": "At the start of each iteration, if `target` exists, it must be in `nums[left..right]`. When the loop ends (`left > right`), the window is empty and target doesn't exist."
      }
    },
    {
      "type": "heading",
      "data": { "level": 2, "text": "Approach 2: Recursive Binary Search" }
    },
    {
      "type": "paragraph",
      "data": { "text": "Recursion naturally divides the search space. Each call searches either the left or right half." }
    },
    {
      "type": "example",
      "data": {
        "title": "Recursive O(log n) time and space",
        "language": "csharp",
        "code": "public int BinarySearchRecursive(int[] nums, int target)\n{\n    return SearchHelper(nums, target, 0, nums.Length - 1);\n}\n\nprivate int SearchHelper(int[] nums, int target, int left, int right)\n{\n    if (left > right) return -1;\n\n    int mid = left + (right - left) / 2;\n\n    if (nums[mid] == target)\n    {\n        return mid;\n    }\n    else if (nums[mid] < target)\n    {\n        return SearchHelper(nums, target, mid + 1, right);\n    }\n    else\n    {\n        return SearchHelper(nums, target, left, mid - 1);\n    }\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "info",
        "title": "Recursive trade-off",
        "text": "Recursive approach uses O(log n) call stack space. The iterative approach is generally preferred because it avoids stack overhead and is easier to debug."
      }
    },
    {
      "type": "heading",
      "data": { "level": 2, "text": "Approach 3: Using Array.BinarySearch (Built-in)" }
    },
    {
      "type": "paragraph",
      "data": { "text": ".NET provides `Array.BinarySearch` as a built-in method. This is the best choice for production code." }
    },
    {
      "type": "example",
      "data": {
        "title": "Built-in O(log n) — Production Ready",
        "language": "csharp",
        "code": "public int BinarySearchBuiltin(int[] nums, int target)\n{\n    int index = Array.BinarySearch(nums, target);\n    return index >= 0 ? index : -1;\n}"
      }
    },
    {
      "type": "callout",
      "data": {
        "variant": "note",
        "title": "When to implement manually",
        "text": "Implement binary search manually when you need custom comparison logic, searching in rotated arrays, or finding boundaries (first/last occurrence). For simple existence checks, use the built-in."
      }
    },
    {
      "type": "heading",
      "data": { "level": 2, "text": "Complexity Summary" }
    },
    {
      "type": "comparison-cards",
      "data": {
        "title": "Approach Comparison",
        "cards": [
          {
            "title": "Iterative",
            "description": "Standard while-loop binary search",
            "pros": ["O(1) space", "No stack risk", "Clear loop invariant"],
            "cons": ["Off-by-one pitfalls with mid"],
            "tags": ["O(log n) time", "O(1) space", "Recommended"]
          },
          {
            "title": "Recursive",
            "description": "Divide-and-conquer recursion",
            "pros": ["Elegant", "Direct mapping to problem"],
            "cons": ["O(log n) stack space"],
            "tags": ["O(log n) time", "O(log n) space"]
          },
          {
            "title": "Array.BinarySearch",
            "description": "Standard library method",
            "pros": ["Shortest code", "Optimized", "Reliable"],
            "cons": ["No customization"],
            "tags": ["O(log n) time", "O(1) space"]
          }
        ]
      }
    }
  ],
  "relatedLessons": ["merge-sorted-array"],
  "furtherReading": []
}
